/*
 * 自动生成的寄存器定义头文件
 * 来源: SF32LB52x.svd
 * 设备: SF32LB52x
 * 
 * 警告: 此文件由脚本自动生成，请勿手动编辑
 * 
 * 元数据格式（用于双向转换）:
 * @SVD:device=名称,version=版本,description=描述
 * @SVD:peripheral=名称,base=基地址,description=描述,groupName=组名
 * @SVD:register=名称,offset=偏移,size=大小,access=访问权限,reset=复位值,description=描述
 * @SVD:field=名称,bitOffset=位偏移,bitWidth=位宽,description=描述
 */

/* @SVD:device=SF32LB52x,version=1.0,description="SF32LB52x" */

#ifndef __SF32LB52X_H__
#define __SF32LB52X_H__

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/* ============================================================================ */
/* HPSYS_RCC 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=HPSYS_RCC,base=0x50000000,description="",groupName=HPSYS_RCC */

#define HPSYS_RCC_BASE                                     (0x50000000UL)

/* @SVD:register=RSTR1,offset=0x0000,size=32,access=read-write,reset=0x0,description="Reset Register 1" */
/* RSTR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DMAC1,bitOffset=0,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t DMAC1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=MAILBOX1,bitOffset=1,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t MAILBOX1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=PINMUX1,bitOffset=2,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t PINMUX1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=USART1,bitOffset=3,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t USART1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=USART2,bitOffset=4,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t USART2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=EZIP1,bitOffset=5,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t EZIP1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=EPIC,bitOffset=6,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t EPIC: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=LCDC1,bitOffset=7,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t LCDC1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=I2S1,bitOffset=8,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t I2S1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD5,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=SYSCFG1,bitOffset=10,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t SYSCFG1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=EFUSEC,bitOffset=11,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t EFUSEC: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=AES,bitOffset=12,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t AES: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=CRC1,bitOffset=13,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t CRC1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=TRNG,bitOffset=14,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t TRNG: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=GPTIM1,bitOffset=15,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t GPTIM1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=GPTIM2,bitOffset=16,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t GPTIM2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=BTIM1,bitOffset=17,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t BTIM1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=BTIM2,bitOffset=18,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t BTIM2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD4,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=SPI1,bitOffset=20,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t SPI1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=SPI2,bitOffset=21,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t SPI2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=EXTDMA,bitOffset=22,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t EXTDMA: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=PDM1,bitOffset=25,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t PDM1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD2,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=I2C1,bitOffset=27,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t I2C1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=I2C2,bitOffset=28,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t I2C2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=PTC1,bitOffset=31,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t PTC1: 1;  /* 0 - no reset; 1 - reset */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_RSTR1_Type;

/* @SVD:register=RSTR2,offset=0x0004,size=32,access=read-write,reset=0x0,description="Reset Register 2" */
/* RSTR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GPIO1,bitOffset=0,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t GPIO1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=MPI1,bitOffset=1,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t MPI1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=MPI2,bitOffset=2,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t MPI2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD8,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=SDMMC1,bitOffset=4,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t SDMMC1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD7,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=USBC,bitOffset=6,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t USBC: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD6,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=I2C3,bitOffset=8,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t I2C3: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=ATIM1,bitOffset=9,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t ATIM1: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD5,bitOffset=10,bitWidth=2,description="" */
        volatile uint32_t RSVD5: 2;
        /* @SVD:field=USART3,bitOffset=12,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t USART3: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD4,bitOffset=13,bitWidth=6,description="" */
        volatile uint32_t RSVD4: 6;
        /* @SVD:field=AUDCODEC,bitOffset=19,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t AUDCODEC: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=AUDPRC,bitOffset=20,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t AUDPRC: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD3,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=GPADC,bitOffset=22,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t GPADC: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=TSEN,bitOffset=23,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t TSEN: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD2,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=I2C4,bitOffset=25,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t I2C4: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_RSTR2_Type;

/* @SVD:register=ENR1,offset=0x0008,size=32,access=read-write,reset=0x0,description="Enable Register 1" */
/* ENR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DMAC1,bitOffset=0,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t DMAC1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=MAILBOX1,bitOffset=1,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t MAILBOX1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=PINMUX1,bitOffset=2,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t PINMUX1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD6,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=USART2,bitOffset=4,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t USART2: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=EZIP1,bitOffset=5,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t EZIP1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=EPIC,bitOffset=6,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t EPIC: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=LCDC1,bitOffset=7,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t LCDC1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=I2S1,bitOffset=8,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t I2S1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD5,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=SYSCFG1,bitOffset=10,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t SYSCFG1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=EFUSEC,bitOffset=11,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t EFUSEC: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=AES,bitOffset=12,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t AES: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=CRC1,bitOffset=13,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t CRC1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=TRNG,bitOffset=14,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t TRNG: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=GPTIM1,bitOffset=15,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t GPTIM1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=GPTIM2,bitOffset=16,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t GPTIM2: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=BTIM1,bitOffset=17,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t BTIM1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=BTIM2,bitOffset=18,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t BTIM2: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD4,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=SPI1,bitOffset=20,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t SPI1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=SPI2,bitOffset=21,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t SPI2: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=EXTDMA,bitOffset=22,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t EXTDMA: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=SECU1,bitOffset=23,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t SECU1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD3,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=PDM1,bitOffset=25,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t PDM1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD2,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=I2C1,bitOffset=27,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t I2C1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=I2C2,bitOffset=28,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t I2C2: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=PTC1,bitOffset=31,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t PTC1: 1;  /* write 1 to set module enable, write 0 to disable module */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_ENR1_Type;

/* @SVD:register=ENR2,offset=0x000C,size=32,access=read-write,reset=0x0,description="Enable Register 2" */
/* ENR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GPIO1,bitOffset=0,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t GPIO1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=MPI1,bitOffset=1,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t MPI1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=MPI2,bitOffset=2,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t MPI2: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD8,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=SDMMC1,bitOffset=4,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t SDMMC1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD7,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=USBC,bitOffset=6,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t USBC: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD6,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=I2C3,bitOffset=8,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t I2C3: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=ATIM1,bitOffset=9,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t ATIM1: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD5,bitOffset=10,bitWidth=2,description="" */
        volatile uint32_t RSVD5: 2;
        /* @SVD:field=USART3,bitOffset=12,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t USART3: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD4,bitOffset=13,bitWidth=6,description="" */
        volatile uint32_t RSVD4: 6;
        /* @SVD:field=AUDCODEC,bitOffset=19,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t AUDCODEC: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=AUDPRC,bitOffset=20,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t AUDPRC: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD3,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=GPADC,bitOffset=22,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t GPADC: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=TSEN,bitOffset=23,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t TSEN: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD2,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=I2C4,bitOffset=25,bitWidth=1,description="write 1 to set module enable, write 0 to disable module" */
        volatile uint32_t I2C4: 1;  /* write 1 to set module enable, write 0 to disable module */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_ENR2_Type;

/* @SVD:register=ESR1,offset=0x0010,size=32,access=read-write,reset=0x0,description="Enable Set Register 1" */
/* ESR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DMAC1,bitOffset=0,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t DMAC1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=MAILBOX1,bitOffset=1,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t MAILBOX1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=PINMUX1,bitOffset=2,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t PINMUX1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD6,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=USART2,bitOffset=4,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t USART2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=EZIP1,bitOffset=5,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t EZIP1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=EPIC,bitOffset=6,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t EPIC: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=LCDC1,bitOffset=7,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t LCDC1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=I2S1,bitOffset=8,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t I2S1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD5,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=SYSCFG1,bitOffset=10,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t SYSCFG1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=EFUSEC,bitOffset=11,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t EFUSEC: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=AES,bitOffset=12,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t AES: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=CRC1,bitOffset=13,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t CRC1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=TRNG,bitOffset=14,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t TRNG: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=GPTIM1,bitOffset=15,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t GPTIM1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=GPTIM2,bitOffset=16,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t GPTIM2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=BTIM1,bitOffset=17,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t BTIM1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=BTIM2,bitOffset=18,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t BTIM2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD4,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=SPI1,bitOffset=20,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t SPI1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=SPI2,bitOffset=21,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t SPI2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=EXTDMA,bitOffset=22,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t EXTDMA: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=SECU1,bitOffset=23,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t SECU1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD3,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=PDM1,bitOffset=25,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t PDM1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD2,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=I2C1,bitOffset=27,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t I2C1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=I2C2,bitOffset=28,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t I2C2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=PTC1,bitOffset=31,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t PTC1: 1;  /* write 1 to set module enable, write 0 has no effect */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_ESR1_Type;

/* @SVD:register=ESR2,offset=0x0014,size=32,access=read-write,reset=0x0,description="Enable Set Register 2" */
/* ESR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GPIO1,bitOffset=0,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t GPIO1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=MPI1,bitOffset=1,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t MPI1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=MPI2,bitOffset=2,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t MPI2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD8,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=SDMMC1,bitOffset=4,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t SDMMC1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD7,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=USBC,bitOffset=6,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t USBC: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD6,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=I2C3,bitOffset=8,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t I2C3: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=ATIM1,bitOffset=9,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t ATIM1: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD5,bitOffset=10,bitWidth=2,description="" */
        volatile uint32_t RSVD5: 2;
        /* @SVD:field=USART3,bitOffset=12,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t USART3: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD4,bitOffset=13,bitWidth=6,description="" */
        volatile uint32_t RSVD4: 6;
        /* @SVD:field=AUDCODEC,bitOffset=19,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t AUDCODEC: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=AUDPRC,bitOffset=20,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t AUDPRC: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD3,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=GPADC,bitOffset=22,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t GPADC: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=TSEN,bitOffset=23,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t TSEN: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD2,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=I2C4,bitOffset=25,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t I2C4: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_ESR2_Type;

/* @SVD:register=ECR1,offset=0x0018,size=32,access=read-write,reset=0x0,description="Enable Clear Register 1" */
/* ECR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DMAC1,bitOffset=0,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t DMAC1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=MAILBOX1,bitOffset=1,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t MAILBOX1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=PINMUX1,bitOffset=2,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t PINMUX1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD6,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=USART2,bitOffset=4,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t USART2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=EZIP1,bitOffset=5,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t EZIP1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=EPIC,bitOffset=6,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t EPIC: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=LCDC1,bitOffset=7,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t LCDC1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=I2S1,bitOffset=8,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t I2S1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD5,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=SYSCFG1,bitOffset=10,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t SYSCFG1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=EFUSEC,bitOffset=11,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t EFUSEC: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=AES,bitOffset=12,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t AES: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=CRC1,bitOffset=13,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t CRC1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=TRNG,bitOffset=14,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t TRNG: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=GPTIM1,bitOffset=15,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t GPTIM1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=GPTIM2,bitOffset=16,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t GPTIM2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=BTIM1,bitOffset=17,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t BTIM1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=BTIM2,bitOffset=18,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t BTIM2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD4,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=SPI1,bitOffset=20,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t SPI1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=SPI2,bitOffset=21,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t SPI2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=EXTDMA,bitOffset=22,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t EXTDMA: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=SECU1,bitOffset=23,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t SECU1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD3,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=PDM1,bitOffset=25,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t PDM1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD2,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=I2C1,bitOffset=27,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t I2C1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=I2C2,bitOffset=28,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t I2C2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=PTC1,bitOffset=31,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t PTC1: 1;  /* write 1 to clear module enable, write 0 has no effect */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_ECR1_Type;

/* @SVD:register=ECR2,offset=0x001C,size=32,access=read-write,reset=0x0,description="Enable Clear Register 2" */
/* ECR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GPIO1,bitOffset=0,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t GPIO1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=MPI1,bitOffset=1,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t MPI1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=MPI2,bitOffset=2,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t MPI2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD8,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=SDMMC1,bitOffset=4,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t SDMMC1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD7,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=USBC,bitOffset=6,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t USBC: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD6,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=I2C3,bitOffset=8,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t I2C3: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=ATIM1,bitOffset=9,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t ATIM1: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD5,bitOffset=10,bitWidth=2,description="" */
        volatile uint32_t RSVD5: 2;
        /* @SVD:field=USART3,bitOffset=12,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t USART3: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD4,bitOffset=13,bitWidth=6,description="" */
        volatile uint32_t RSVD4: 6;
        /* @SVD:field=AUDCODEC,bitOffset=19,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t AUDCODEC: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=AUDPRC,bitOffset=20,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t AUDPRC: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD3,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=GPADC,bitOffset=22,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t GPADC: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=TSEN,bitOffset=23,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t TSEN: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD2,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=I2C4,bitOffset=25,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t I2C4: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_ECR2_Type;

/* @SVD:register=CSR,offset=0x0020,size=32,access=read-write,reset=0x0,description="Clock Select Register" */
/* CSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEL_SYS,bitOffset=0,bitWidth=2,description="select clk_hpsys source\n              0 - clk_hrc48; 1 - clk_hxt48; 2 - reserved; 3 - clk_dll1" */
        volatile uint32_t SEL_SYS: 2;  /* select clk_hpsys source               0 - clk_hrc48; 1 - clk */
        /* @SVD:field=SEL_SYS_LP,bitOffset=2,bitWidth=1,description="select clk_hpsys source\n              0 - selected by SEL_SYS; 1 - clk_wdt" */
        volatile uint32_t SEL_SYS_LP: 1;  /* select clk_hpsys source               0 - selected by SEL_SY */
        /* @SVD:field=RSVD4,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=SEL_MPI1,bitOffset=4,bitWidth=2,description="selet MPI1 function clock\n              0 - clk_peri_hpsys; 1 - clk_dll1; 2 - clk_dll2; 3 - reserved" */
        volatile uint32_t SEL_MPI1: 2;  /* selet MPI1 function clock               0 - clk_peri_hpsys;  */
        /* @SVD:field=SEL_MPI2,bitOffset=6,bitWidth=2,description="selet MPI2 function clock\n              0 - clk_peri_hpsys; 1 - clk_dll1; 2 - clk_dll2; 3 - reserved" */
        volatile uint32_t SEL_MPI2: 2;  /* selet MPI2 function clock               0 - clk_peri_hpsys;  */
        /* @SVD:field=RSVD3,bitOffset=8,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=SEL_PERI,bitOffset=12,bitWidth=1,description="select clk_peri_hpsys source used by USART/SPI/I2C/GPTIM2/BTIM2\n              0 - clk_hrc48; 1 - clk_hxt48" */
        volatile uint32_t SEL_PERI: 1;  /* select clk_peri_hpsys source used by USART/SPI/I2C/GPTIM2/BT */
        /* @SVD:field=SEL_TICK,bitOffset=13,bitWidth=2,description="select clock source for systick reference\n              0 - clk_rtc; 1 - reserved; 2 - clk_hrc48; 3 - clk_hxt48" */
        volatile uint32_t SEL_TICK: 2;  /* select clock source for systick reference               0 -  */
        /* @SVD:field=SEL_USBC,bitOffset=15,bitWidth=1,description="select USB source clock\n              0 - clk_hpsys; 1 - clk_dll2" */
        volatile uint32_t SEL_USBC: 1;  /* select USB source clock               0 - clk_hpsys; 1 - clk */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_CSR_Type;

/* @SVD:register=CFGR,offset=0x0024,size=32,access=read-write,reset=0x0,description="Clock Configuration Register" */
/* CFGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HDIV,bitOffset=0,bitWidth=8,description="hclk_hpsys = clk_hpsys / HDIV\n              if HDIV=0, hclk_hpsys = clk_hpsys" */
        volatile uint32_t HDIV: 8;  /* hclk_hpsys = clk_hpsys / HDIV               if HDIV=0, hclk_ */
        /* @SVD:field=PDIV1,bitOffset=8,bitWidth=3,description="pclk_hpsys = hclk_hpsys / (2^PDIV1), by default divided by 2" */
        volatile uint32_t PDIV1: 3;  /* pclk_hpsys = hclk_hpsys / (2^PDIV1), by default divided by 2 */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=PDIV2,bitOffset=12,bitWidth=3,description="pclk2_hpsys = hclk_hpsys / (2^PDIV2), by default divided by 16" */
        volatile uint32_t PDIV2: 3;  /* pclk2_hpsys = hclk_hpsys / (2^PDIV2), by default divided by  */
        /* @SVD:field=RSVD2,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TICKDIV,bitOffset=16,bitWidth=6,description="systick reference clock is systick reference clock source (selected by SEL_TICK) devided by TICKDIV" */
        volatile uint32_t TICKDIV: 6;  /* systick reference clock is systick reference clock source (s */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_CFGR_Type;

/* @SVD:register=USBCR,offset=0x0028,size=32,access=read-write,reset=0x0,description="USBC Control Register" */
/* USBCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DIV,bitOffset=0,bitWidth=3,description="USB function clock is USB source clock divided by DIV. After divider, USB function clock must be 60MHz. For example, if USBC clock source is 240MHz clk_dll2, DIV should be 4." */
        volatile uint32_t DIV: 3;  /* USB function clock is USB source clock divided by DIV. After */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=29,description="" */
        volatile uint32_t RSVD: 29;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_USBCR_Type;

/* @SVD:register=DLL1CR,offset=0x002C,size=32,access=read-write,reset=0x0,description="DLL1 Control Register" */
/* DLL1CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="0: dll disabled\n              1: dll enabled" */
        volatile uint32_t EN: 1;  /* 0: dll disabled               1: dll enabled */
        /* @SVD:field=SW,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t SW: 1;
        /* @SVD:field=STG,bitOffset=2,bitWidth=4,description="DLL lock freqency is decided by STG.\n              DLL output frequency is (STG+1)*24MHz\n              e.g. STG=9,DLL output is 240M" */
        volatile uint32_t STG: 4;  /* DLL lock freqency is decided by STG.               DLL outpu */
        /* @SVD:field=XTALIN_EN,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t XTALIN_EN: 1;
        /* @SVD:field=MODE48M_EN,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t MODE48M_EN: 1;
        /* @SVD:field=LDO_VREF,bitOffset=8,bitWidth=4,description="" */
        volatile uint32_t LDO_VREF: 4;
        /* @SVD:field=IN_DIV2_EN,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t IN_DIV2_EN: 1;
        /* @SVD:field=OUT_DIV2_EN,bitOffset=13,bitWidth=1,description="0: dll output not divided\n              1: dll output divided by 2" */
        volatile uint32_t OUT_DIV2_EN: 1;  /* 0: dll output not divided               1: dll output divide */
        /* @SVD:field=MCU_PRCHG_EN,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t MCU_PRCHG_EN: 1;
        /* @SVD:field=MCU_PRCHG,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t MCU_PRCHG: 1;
        /* @SVD:field=PRCHG_EN,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t PRCHG_EN: 1;
        /* @SVD:field=PRCHG_EXT,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t PRCHG_EXT: 1;
        /* @SVD:field=VST_SEL,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t VST_SEL: 1;
        /* @SVD:field=BYPASS,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t BYPASS: 1;
        /* @SVD:field=DTEST_EN,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t DTEST_EN: 1;
        /* @SVD:field=DTEST_TR,bitOffset=21,bitWidth=4,description="" */
        volatile uint32_t DTEST_TR: 4;
        /* @SVD:field=PU_DLY,bitOffset=25,bitWidth=3,description="" */
        volatile uint32_t PU_DLY: 3;
        /* @SVD:field=LOCK_DLY,bitOffset=28,bitWidth=3,description="" */
        volatile uint32_t LOCK_DLY: 3;
        /* @SVD:field=READY,bitOffset=31,bitWidth=1,description="0: dll not ready\n              1: dll ready" */
        volatile uint32_t READY: 1;  /* 0: dll not ready               1: dll ready */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_DLL1CR_Type;

/* @SVD:register=DLL2CR,offset=0x0030,size=32,access=read-write,reset=0x0,description="DLL2 Control Register" */
/* DLL2CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="0: dll disabled\n              1: dll enabled" */
        volatile uint32_t EN: 1;  /* 0: dll disabled               1: dll enabled */
        /* @SVD:field=SW,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t SW: 1;
        /* @SVD:field=STG,bitOffset=2,bitWidth=4,description="DLL lock freqency is decided by STG.\n              DLL output frequency is (STG+1)*24MHz\n              e.g. STG=9,DLL output is 240M" */
        volatile uint32_t STG: 4;  /* DLL lock freqency is decided by STG.               DLL outpu */
        /* @SVD:field=XTALIN_EN,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t XTALIN_EN: 1;
        /* @SVD:field=MODE48M_EN,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t MODE48M_EN: 1;
        /* @SVD:field=LDO_VREF,bitOffset=8,bitWidth=4,description="" */
        volatile uint32_t LDO_VREF: 4;
        /* @SVD:field=IN_DIV2_EN,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t IN_DIV2_EN: 1;
        /* @SVD:field=OUT_DIV2_EN,bitOffset=13,bitWidth=1,description="0: dll output not divided\n              1: dll output divided by 2" */
        volatile uint32_t OUT_DIV2_EN: 1;  /* 0: dll output not divided               1: dll output divide */
        /* @SVD:field=MCU_PRCHG_EN,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t MCU_PRCHG_EN: 1;
        /* @SVD:field=MCU_PRCHG,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t MCU_PRCHG: 1;
        /* @SVD:field=PRCHG_EN,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t PRCHG_EN: 1;
        /* @SVD:field=PRCHG_EXT,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t PRCHG_EXT: 1;
        /* @SVD:field=VST_SEL,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t VST_SEL: 1;
        /* @SVD:field=BYPASS,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t BYPASS: 1;
        /* @SVD:field=DTEST_EN,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t DTEST_EN: 1;
        /* @SVD:field=DTEST_TR,bitOffset=21,bitWidth=4,description="" */
        volatile uint32_t DTEST_TR: 4;
        /* @SVD:field=PU_DLY,bitOffset=25,bitWidth=3,description="" */
        volatile uint32_t PU_DLY: 3;
        /* @SVD:field=LOCK_DLY,bitOffset=28,bitWidth=3,description="" */
        volatile uint32_t LOCK_DLY: 3;
        /* @SVD:field=READY,bitOffset=31,bitWidth=1,description="0: dll not ready\n              1: dll ready" */
        volatile uint32_t READY: 1;  /* 0: dll not ready               1: dll ready */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_DLL2CR_Type;

/* @SVD:register=HRCCAL1,offset=0x0034,size=32,access=read-write,reset=0x0,description="HRC Calibration Register 1" */
/* HRCCAL1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CAL_LENGTH,bitOffset=0,bitWidth=16,description="Target clk_hxt48 cycles during calibration" */
        volatile uint32_t CAL_LENGTH: 16;  /* Target clk_hxt48 cycles during calibration */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
        /* @SVD:field=CAL_EN,bitOffset=30,bitWidth=1,description="Calibration enble. \n              Set to 0 to clear result, then set to 1 to start a new calibration" */
        volatile uint32_t CAL_EN: 1;  /* Calibration enble.                Set to 0 to clear result,  */
        /* @SVD:field=CAL_DONE,bitOffset=31,bitWidth=1,description="Calibration done.\n              After a new calibration started, results should be processed only when cal_done asserted." */
        volatile uint32_t CAL_DONE: 1;  /* Calibration done.               After a new calibration star */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_HRCCAL1_Type;

/* @SVD:register=HRCCAL2,offset=0x0038,size=32,access=read-write,reset=0x0,description="HRC Calibration Register 2" */
/* HRCCAL2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HRC_CNT,bitOffset=0,bitWidth=16,description="Total clk_hrc48 cycles during calibration" */
        volatile uint32_t HRC_CNT: 16;  /* Total clk_hrc48 cycles during calibration */
        /* @SVD:field=HXT_CNT,bitOffset=16,bitWidth=16,description="Total clk_hxt48 cycles during calibration" */
        volatile uint32_t HXT_CNT: 16;  /* Total clk_hxt48 cycles during calibration */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_HRCCAL2_Type;

/* @SVD:register=DBGCLKR,offset=0x003C,size=32,access=read-write,reset=0x0,description="Debug Clock Register" */
/* DBGCLKR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLK_SEL,bitOffset=0,bitWidth=2,description="for debug only" */
        volatile uint32_t CLK_SEL: 2;  /* for debug only */
        /* @SVD:field=CLK_EN,bitOffset=2,bitWidth=1,description="for debug only" */
        volatile uint32_t CLK_EN: 1;  /* for debug only */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=DLL1_DBG,bitOffset=4,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL1_DBG: 1;  /* for debug only */
        /* @SVD:field=DLL1_LDO_EN,bitOffset=5,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL1_LDO_EN: 1;  /* for debug only */
        /* @SVD:field=DLL1_OUT_EN,bitOffset=6,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL1_OUT_EN: 1;  /* for debug only */
        /* @SVD:field=DLL1_LOOP_EN,bitOffset=7,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL1_LOOP_EN: 1;  /* for debug only */
        /* @SVD:field=DLL1_OUT_RSTB,bitOffset=8,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL1_OUT_RSTB: 1;  /* for debug only */
        /* @SVD:field=DLL1_CG_EN,bitOffset=9,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL1_CG_EN: 1;  /* for debug only */
        /* @SVD:field=DLL1_OUT_STR,bitOffset=10,bitWidth=2,description="for debug only" */
        volatile uint32_t DLL1_OUT_STR: 2;  /* for debug only */
        /* @SVD:field=DLL2_DBG,bitOffset=12,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL2_DBG: 1;  /* for debug only */
        /* @SVD:field=DLL2_LDO_EN,bitOffset=13,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL2_LDO_EN: 1;  /* for debug only */
        /* @SVD:field=DLL2_OUT_EN,bitOffset=14,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL2_OUT_EN: 1;  /* for debug only */
        /* @SVD:field=DLL2_LOOP_EN,bitOffset=15,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL2_LOOP_EN: 1;  /* for debug only */
        /* @SVD:field=DLL2_OUT_RSTB,bitOffset=16,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL2_OUT_RSTB: 1;  /* for debug only */
        /* @SVD:field=DLL2_CG_EN,bitOffset=17,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL2_CG_EN: 1;  /* for debug only */
        /* @SVD:field=DLL2_OUT_STR,bitOffset=18,bitWidth=2,description="for debug only" */
        volatile uint32_t DLL2_OUT_STR: 2;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_DBGCLKR_Type;

/* @SVD:register=DBGR,offset=0x0040,size=32,access=read-write,reset=0x0,description="Debug Register" */
/* DBGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SYSCLK_AON,bitOffset=0,bitWidth=1,description="for debug only" */
        volatile uint32_t SYSCLK_AON: 1;  /* for debug only */
        /* @SVD:field=SYSCLK_SWLP,bitOffset=1,bitWidth=1,description="for debug only" */
        volatile uint32_t SYSCLK_SWLP: 1;  /* for debug only */
        /* @SVD:field=FORCE_BUS,bitOffset=2,bitWidth=1,description="for debug only" */
        volatile uint32_t FORCE_BUS: 1;  /* for debug only */
        /* @SVD:field=FORCE_GPIO,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t FORCE_GPIO: 1;  /* for debug only */
        /* @SVD:field=FORCE_HP,bitOffset=4,bitWidth=1,description="for debug only" */
        volatile uint32_t FORCE_HP: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_DBGR_Type;

/* @SVD:register=DWCFGR,offset=0x0044,size=32,access=read-write,reset=0x0,description="Deep WFI mode Clock Configuration Register" */
/* DWCFGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HDIV,bitOffset=0,bitWidth=8,description="hclk_hpsys = clk_hpsys / HDIV  during deep wfi" */
        volatile uint32_t HDIV: 8;  /* hclk_hpsys = clk_hpsys / HDIV  during deep wfi */
        /* @SVD:field=PDIV1,bitOffset=8,bitWidth=3,description="pclk_hpsys = hclk_hpsys / (2^PDIV1)  during deep wfi" */
        volatile uint32_t PDIV1: 3;  /* pclk_hpsys = hclk_hpsys / (2^PDIV1)  during deep wfi */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=PDIV2,bitOffset=12,bitWidth=3,description="pclk2_hpsys = hclk_hpsys / (2^PDIV2)  during deep wfi" */
        volatile uint32_t PDIV2: 3;  /* pclk2_hpsys = hclk_hpsys / (2^PDIV2)  during deep wfi */
        /* @SVD:field=DIV_EN,bitOffset=15,bitWidth=1,description="enable PDIV1, PDIV2 and HDIV reconfiguration during deep wfi" */
        volatile uint32_t DIV_EN: 1;  /* enable PDIV1, PDIV2 and HDIV reconfiguration during deep wfi */
        /* @SVD:field=SEL_SYS,bitOffset=16,bitWidth=2,description="select clk_hpsys source during deep WFI\n              0 - clk_hrc48; 1 - clk_hxt48; 2 - RSVD; 3 - clk_dll1" */
        volatile uint32_t SEL_SYS: 2;  /* select clk_hpsys source during deep WFI               0 - cl */
        /* @SVD:field=SEL_SYS_LP,bitOffset=18,bitWidth=1,description="select clk_hpsys source during deep WFI\n              0 - selected by SEL_SYS; 1 - clk_wdt" */
        volatile uint32_t SEL_SYS_LP: 1;  /* select clk_hpsys source during deep WFI               0 - se */
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=DLL1_OUT_EN,bitOffset=24,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL1_OUT_EN: 1;  /* for debug only */
        /* @SVD:field=DLL1_OUT_RSTB,bitOffset=25,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL1_OUT_RSTB: 1;  /* for debug only */
        /* @SVD:field=DLL2_OUT_EN,bitOffset=26,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL2_OUT_EN: 1;  /* for debug only */
        /* @SVD:field=DLL2_OUT_RSTB,bitOffset=27,bitWidth=1,description="for debug only" */
        volatile uint32_t DLL2_OUT_RSTB: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_RCC_DWCFGR_Type;

/* HPSYS_RCC 寄存器结构体 */
typedef struct
{
    HPSYS_RCC_RSTR1_Type RSTR1;  /* 0x0000 - Reset Register 1 */
    HPSYS_RCC_RSTR2_Type RSTR2;  /* 0x0004 - Reset Register 2 */
    HPSYS_RCC_ENR1_Type ENR1;  /* 0x0008 - Enable Register 1 */
    HPSYS_RCC_ENR2_Type ENR2;  /* 0x000C - Enable Register 2 */
    HPSYS_RCC_ESR1_Type ESR1;  /* 0x0010 - Enable Set Register 1 */
    HPSYS_RCC_ESR2_Type ESR2;  /* 0x0014 - Enable Set Register 2 */
    HPSYS_RCC_ECR1_Type ECR1;  /* 0x0018 - Enable Clear Register 1 */
    HPSYS_RCC_ECR2_Type ECR2;  /* 0x001C - Enable Clear Register 2 */
    HPSYS_RCC_CSR_Type CSR;  /* 0x0020 - Clock Select Register */
    HPSYS_RCC_CFGR_Type CFGR;  /* 0x0024 - Clock Configuration Register */
    HPSYS_RCC_USBCR_Type USBCR;  /* 0x0028 - USBC Control Register */
    HPSYS_RCC_DLL1CR_Type DLL1CR;  /* 0x002C - DLL1 Control Register */
    HPSYS_RCC_DLL2CR_Type DLL2CR;  /* 0x0030 - DLL2 Control Register */
    HPSYS_RCC_HRCCAL1_Type HRCCAL1;  /* 0x0034 - HRC Calibration Register 1 */
    HPSYS_RCC_HRCCAL2_Type HRCCAL2;  /* 0x0038 - HRC Calibration Register 2 */
    HPSYS_RCC_DBGCLKR_Type DBGCLKR;  /* 0x003C - Debug Clock Register */
    HPSYS_RCC_DBGR_Type DBGR;  /* 0x0040 - Debug Register */
    HPSYS_RCC_DWCFGR_Type DWCFGR;  /* 0x0044 - Deep WFI mode Clock Configuration Regist */
} HPSYS_RCC_TypeDef;

/* ============================================================================ */
/* EXTDMA 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=EXTDMA,base=0x50001000,description="",groupName=EXTDMA */

#define EXTDMA_BASE                                        (0x50001000UL)

/* @SVD:register=ISR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="interrupt status register " */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GIF,bitOffset=0,bitWidth=1,description="GIF,  global interrupt flag" */
        volatile uint32_t GIF: 1;  /* GIF,  global interrupt flag */
        /* @SVD:field=TCIF,bitOffset=1,bitWidth=1,description="TCIF, transfer complete flag" */
        volatile uint32_t TCIF: 1;  /* TCIF, transfer complete flag */
        /* @SVD:field=HTIF,bitOffset=2,bitWidth=1,description="HTIF, half transfer flag" */
        volatile uint32_t HTIF: 1;  /* HTIF, half transfer flag */
        /* @SVD:field=TEIF,bitOffset=3,bitWidth=1,description="TEIF, transfer error flag" */
        volatile uint32_t TEIF: 1;  /* TEIF, transfer error flag */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EXTDMA_ISR_Type;

/* @SVD:register=IFCR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="interrupt clear register " */
/* IFCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CGIF,bitOffset=0,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF,bitOffset=1,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF,bitOffset=2,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF,bitOffset=3,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EXTDMA_IFCR_Type;

/* @SVD:register=CCR,offset=0x0008,size=32,access=read-write,reset=0x000F0AC0,description="channel control register" */
/* CCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="extdma enable. Will be cleared if ccr_reset is written" */
        volatile uint32_t EN: 1;  /* extdma enable. Will be cleared if ccr_reset is written */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=RSVD3,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=DSTINC,bitOffset=6,bitWidth=1,description="destination increment mode\n              Defines the increment mode for each DMA transfer to the destination memory.\n              0: disabled\n              1: enabled" */
        volatile uint32_t DSTINC: 1;  /* destination increment mode               Defines the increme */
        /* @SVD:field=SRCINC,bitOffset=7,bitWidth=1,description="source increment mode\n              Defines the increment mode for each DMA transfer to the source memory.\n              0: disabled\n              1: enabled" */
        volatile uint32_t SRCINC: 1;  /* source increment mode               Defines the increment mo */
        /* @SVD:field=DSTSIZE,bitOffset=8,bitWidth=2,description="destination size\n              Defines the data size of each DMA transfer to the destination memory.\n              Should be fixed to 10 (32 bits), word access allowed only." */
        volatile uint32_t DSTSIZE: 2;  /* destination size               Defines the data size of each */
        /* @SVD:field=SRCSIZE,bitOffset=10,bitWidth=2,description="source size\n              Defines the data size of each DMA transfer to the source memory.\n              Should be fixed to 10 (32 bits), word access allowed only." */
        volatile uint32_t SRCSIZE: 2;  /* source size               Defines the data size of each DMA  */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=DSTBURST,bitOffset=16,bitWidth=2,description="destination burst transfer configuration\n              00: single transfer\n              01: INCR4 (incremental burst of 4 beats)\n              10: INCR8 (incremental burst of 8 beats)\n              11: INCR16 (incremental burst of 16 beats)" */
        volatile uint32_t DSTBURST: 2;  /* destination burst transfer configuration               00: s */
        /* @SVD:field=SRCBURST,bitOffset=18,bitWidth=2,description="source burst transfer configuration\n              00: single transfer\n              01: INCR4 (incremental burst of 4 beats)\n              10: INCR8 (incremental burst of 8 beats)\n              11: INCR16 (incremental burst of 16 beats)" */
        volatile uint32_t SRCBURST: 2;  /* source burst transfer configuration               00: single */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
        /* @SVD:field=RESET,bitOffset=31,bitWidth=1,description="Software reset, will clear extdma status. Active high. Will be cleared by HW automatically" */
        volatile uint32_t RESET: 1;  /* Software reset, will clear extdma status. Active high. Will  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EXTDMA_CCR_Type;

/* @SVD:register=CNDTR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="number of data register" */
/* CNDTR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=20,description="number of data to transfer (0 to 2^20 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached.\n              If this field is zero, no transfer can be served whatever the channel enabled or not" */
        volatile uint32_t NDT: 20;  /* number of data to transfer (0 to 2^20 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EXTDMA_CNDTR_Type;

/* @SVD:register=SRCAR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="source address register" */
/* SRCAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SRCADDR,bitOffset=0,bitWidth=32,description="source address\n              It contains the base address of the source data to be read. Should be word aligned" */
        volatile uint32_t SRCADDR: 32;  /* source address               It contains the base address of */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EXTDMA_SRCAR_Type;

/* @SVD:register=DSTAR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="destination 0 address register " */
/* DSTAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DSTADDR,bitOffset=0,bitWidth=32,description="destination address\n              It contains the base address of the destination data to be written. Should be word aligned" */
        volatile uint32_t DSTADDR: 32;  /* destination address               It contains the base addre */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EXTDMA_DSTAR_Type;

/* EXTDMA 寄存器结构体 */
typedef struct
{
    EXTDMA_ISR_Type ISR;  /* 0x0000 - interrupt status register */
    EXTDMA_IFCR_Type IFCR;  /* 0x0004 - interrupt clear register */
    EXTDMA_CCR_Type CCR;  /* 0x0008 - channel control register */
    EXTDMA_CNDTR_Type CNDTR;  /* 0x000C - number of data register */
    EXTDMA_SRCAR_Type SRCAR;  /* 0x0010 - source address register */
    EXTDMA_DSTAR_Type DSTAR;  /* 0x0014 - destination 0 address register */
} EXTDMA_TypeDef;

/* ============================================================================ */
/* HPSYS_PINMUX 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=HPSYS_PINMUX,base=0x50003000,description="",groupName=HPSYS_PINMUX */

#define HPSYS_PINMUX_BASE                                  (0x50003000UL)

/* @SVD:register=PAD_SA00,offset=0x0000,size=32,access=read-write,reset=0x0,description="PAD_SA00" */
/* PAD_SA00 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA00_Type;

/* @SVD:register=PAD_SA01,offset=0x0004,size=32,access=read-write,reset=0x0,description="PAD_SA01" */
/* PAD_SA01 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA01_Type;

/* @SVD:register=PAD_SA02,offset=0x0008,size=32,access=read-write,reset=0x0,description="PAD_SA02" */
/* PAD_SA02 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA02_Type;

/* @SVD:register=PAD_SA03,offset=0x000C,size=32,access=read-write,reset=0x0,description="PAD_SA03" */
/* PAD_SA03 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA03_Type;

/* @SVD:register=PAD_SA04,offset=0x0010,size=32,access=read-write,reset=0x0,description="PAD_SA04" */
/* PAD_SA04 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA04_Type;

/* @SVD:register=PAD_SA05,offset=0x0014,size=32,access=read-write,reset=0x0,description="PAD_SA05" */
/* PAD_SA05 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA05_Type;

/* @SVD:register=PAD_SA06,offset=0x0018,size=32,access=read-write,reset=0x0,description="PAD_SA06" */
/* PAD_SA06 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA06_Type;

/* @SVD:register=PAD_SA07,offset=0x001C,size=32,access=read-write,reset=0x0,description="PAD_SA07" */
/* PAD_SA07 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA07_Type;

/* @SVD:register=PAD_SA08,offset=0x0020,size=32,access=read-write,reset=0x0,description="PAD_SA08" */
/* PAD_SA08 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA08_Type;

/* @SVD:register=PAD_SA09,offset=0x0024,size=32,access=read-write,reset=0x0,description="PAD_SA09" */
/* PAD_SA09 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA09_Type;

/* @SVD:register=PAD_SA10,offset=0x0028,size=32,access=read-write,reset=0x0,description="PAD_SA10" */
/* PAD_SA10 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA10_Type;

/* @SVD:register=PAD_SA11,offset=0x002C,size=32,access=read-write,reset=0x0,description="PAD_SA11" */
/* PAD_SA11 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA11_Type;

/* @SVD:register=PAD_SA12,offset=0x0030,size=32,access=read-write,reset=0x0,description="PAD_SA12" */
/* PAD_SA12 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_SA12_Type;

/* @SVD:register=PAD_PA00,offset=0x0034,size=32,access=read-write,reset=0x0,description="PAD_PA00" */
/* PAD_PA00 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA00_Type;

/* @SVD:register=PAD_PA01,offset=0x0038,size=32,access=read-write,reset=0x0,description="PAD_PA01" */
/* PAD_PA01 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA01_Type;

/* @SVD:register=PAD_PA02,offset=0x003C,size=32,access=read-write,reset=0x0,description="PAD_PA02" */
/* PAD_PA02 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA02_Type;

/* @SVD:register=PAD_PA03,offset=0x0040,size=32,access=read-write,reset=0x0,description="PAD_PA03" */
/* PAD_PA03 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA03_Type;

/* @SVD:register=PAD_PA04,offset=0x0044,size=32,access=read-write,reset=0x0,description="PAD_PA04" */
/* PAD_PA04 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA04_Type;

/* @SVD:register=PAD_PA05,offset=0x0048,size=32,access=read-write,reset=0x0,description="PAD_PA05" */
/* PAD_PA05 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA05_Type;

/* @SVD:register=PAD_PA06,offset=0x004C,size=32,access=read-write,reset=0x0,description="PAD_PA06" */
/* PAD_PA06 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA06_Type;

/* @SVD:register=PAD_PA07,offset=0x0050,size=32,access=read-write,reset=0x0,description="PAD_PA07" */
/* PAD_PA07 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA07_Type;

/* @SVD:register=PAD_PA08,offset=0x0054,size=32,access=read-write,reset=0x0,description="PAD_PA08" */
/* PAD_PA08 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA08_Type;

/* @SVD:register=PAD_PA09,offset=0x0058,size=32,access=read-write,reset=0x0,description="PAD_PA09" */
/* PAD_PA09 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA09_Type;

/* @SVD:register=PAD_PA10,offset=0x005C,size=32,access=read-write,reset=0x0,description="PAD_PA10" */
/* PAD_PA10 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA10_Type;

/* @SVD:register=PAD_PA11,offset=0x0060,size=32,access=read-write,reset=0x0,description="PAD_PA11" */
/* PAD_PA11 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA11_Type;

/* @SVD:register=PAD_PA12,offset=0x0064,size=32,access=read-write,reset=0x0,description="PAD_PA12" */
/* PAD_PA12 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA12_Type;

/* @SVD:register=PAD_PA13,offset=0x0068,size=32,access=read-write,reset=0x0,description="PAD_PA13" */
/* PAD_PA13 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA13_Type;

/* @SVD:register=PAD_PA14,offset=0x006C,size=32,access=read-write,reset=0x0,description="PAD_PA14" */
/* PAD_PA14 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA14_Type;

/* @SVD:register=PAD_PA15,offset=0x0070,size=32,access=read-write,reset=0x0,description="PAD_PA15" */
/* PAD_PA15 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA15_Type;

/* @SVD:register=PAD_PA16,offset=0x0074,size=32,access=read-write,reset=0x0,description="PAD_PA16" */
/* PAD_PA16 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA16_Type;

/* @SVD:register=PAD_PA17,offset=0x0078,size=32,access=read-write,reset=0x0,description="PAD_PA17" */
/* PAD_PA17 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA17_Type;

/* @SVD:register=PAD_PA18,offset=0x007C,size=32,access=read-write,reset=0x0,description="PAD_PA18" */
/* PAD_PA18 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA18_Type;

/* @SVD:register=PAD_PA19,offset=0x0080,size=32,access=read-write,reset=0x0,description="PAD_PA19" */
/* PAD_PA19 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA19_Type;

/* @SVD:register=PAD_PA20,offset=0x0084,size=32,access=read-write,reset=0x0,description="PAD_PA20" */
/* PAD_PA20 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA20_Type;

/* @SVD:register=PAD_PA21,offset=0x0088,size=32,access=read-write,reset=0x0,description="PAD_PA21" */
/* PAD_PA21 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA21_Type;

/* @SVD:register=PAD_PA22,offset=0x008C,size=32,access=read-write,reset=0x0,description="PAD_PA22" */
/* PAD_PA22 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA22_Type;

/* @SVD:register=PAD_PA23,offset=0x0090,size=32,access=read-write,reset=0x0,description="PAD_PA23" */
/* PAD_PA23 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA23_Type;

/* @SVD:register=PAD_PA24,offset=0x0094,size=32,access=read-write,reset=0x0,description="PAD_PA24" */
/* PAD_PA24 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA24_Type;

/* @SVD:register=PAD_PA25,offset=0x0098,size=32,access=read-write,reset=0x0,description="PAD_PA25" */
/* PAD_PA25 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA25_Type;

/* @SVD:register=PAD_PA26,offset=0x009C,size=32,access=read-write,reset=0x0,description="PAD_PA26" */
/* PAD_PA26 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA26_Type;

/* @SVD:register=PAD_PA27,offset=0x00A0,size=32,access=read-write,reset=0x0,description="PAD_PA27" */
/* PAD_PA27 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA27_Type;

/* @SVD:register=PAD_PA28,offset=0x00A4,size=32,access=read-write,reset=0x0,description="PAD_PA28" */
/* PAD_PA28 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA28_Type;

/* @SVD:register=PAD_PA29,offset=0x00A8,size=32,access=read-write,reset=0x0,description="PAD_PA29" */
/* PAD_PA29 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA29_Type;

/* @SVD:register=PAD_PA30,offset=0x00AC,size=32,access=read-write,reset=0x0,description="PAD_PA30" */
/* PAD_PA30 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA30_Type;

/* @SVD:register=PAD_PA31,offset=0x00B0,size=32,access=read-write,reset=0x0,description="PAD_PA31" */
/* PAD_PA31 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA31_Type;

/* @SVD:register=PAD_PA32,offset=0x00B4,size=32,access=read-write,reset=0x0,description="PAD_PA32" */
/* PAD_PA32 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA32_Type;

/* @SVD:register=PAD_PA33,offset=0x00B8,size=32,access=read-write,reset=0x0,description="PAD_PA33" */
/* PAD_PA33 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA33_Type;

/* @SVD:register=PAD_PA34,offset=0x00BC,size=32,access=read-write,reset=0x0,description="PAD_PA34" */
/* PAD_PA34 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA34_Type;

/* @SVD:register=PAD_PA35,offset=0x00C0,size=32,access=read-write,reset=0x0,description="PAD_PA35" */
/* PAD_PA35 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA35_Type;

/* @SVD:register=PAD_PA36,offset=0x00C4,size=32,access=read-write,reset=0x0,description="PAD_PA36" */
/* PAD_PA36 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA36_Type;

/* @SVD:register=PAD_PA37,offset=0x00C8,size=32,access=read-write,reset=0x0,description="PAD_PA37" */
/* PAD_PA37 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA37_Type;

/* @SVD:register=PAD_PA38,offset=0x00CC,size=32,access=read-write,reset=0x0,description="PAD_PA38" */
/* PAD_PA38 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA38_Type;

/* @SVD:register=PAD_PA39,offset=0x00D0,size=32,access=read-write,reset=0x0,description="PAD_PA39" */
/* PAD_PA39 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=MODE,bitOffset=8,bitWidth=1,description="Mode Select. Logic LOW enables GPIO mode,logic HIGH enables I2C mode" */
        volatile uint32_t MODE: 1;  /* Mode Select. Logic LOW enables GPIO mode,logic HIGH enables  */
        /* @SVD:field=RSVD2,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=DS,bitOffset=10,bitWidth=1,description="Drive Select. Logic LOW selects 4mA drive,logic HIGH selects 20mA drive" */
        volatile uint32_t DS: 1;  /* Drive Select. Logic LOW selects 4mA drive,logic HIGH selects */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA39_Type;

/* @SVD:register=PAD_PA40,offset=0x00D4,size=32,access=read-write,reset=0x0,description="PAD_PA40" */
/* PAD_PA40 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=MODE,bitOffset=8,bitWidth=1,description="Mode Select. Logic LOW enables GPIO mode,logic HIGH enables I2C mode" */
        volatile uint32_t MODE: 1;  /* Mode Select. Logic LOW enables GPIO mode,logic HIGH enables  */
        /* @SVD:field=RSVD2,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=DS,bitOffset=10,bitWidth=1,description="Drive Select. Logic LOW selects 4mA drive,logic HIGH selects 20mA drive" */
        volatile uint32_t DS: 1;  /* Drive Select. Logic LOW selects 4mA drive,logic HIGH selects */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA40_Type;

/* @SVD:register=PAD_PA41,offset=0x00D8,size=32,access=read-write,reset=0x0,description="PAD_PA41" */
/* PAD_PA41 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=MODE,bitOffset=8,bitWidth=1,description="Mode Select. Logic LOW enables GPIO mode,logic HIGH enables I2C mode" */
        volatile uint32_t MODE: 1;  /* Mode Select. Logic LOW enables GPIO mode,logic HIGH enables  */
        /* @SVD:field=RSVD2,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=DS,bitOffset=10,bitWidth=1,description="Drive Select. Logic LOW selects 4mA drive,logic HIGH selects 20mA drive" */
        volatile uint32_t DS: 1;  /* Drive Select. Logic LOW selects 4mA drive,logic HIGH selects */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA41_Type;

/* @SVD:register=PAD_PA42,offset=0x00DC,size=32,access=read-write,reset=0x0,description="PAD_PA42" */
/* PAD_PA42 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=MODE,bitOffset=8,bitWidth=1,description="Mode Select. Logic LOW enables GPIO mode,logic HIGH enables I2C mode" */
        volatile uint32_t MODE: 1;  /* Mode Select. Logic LOW enables GPIO mode,logic HIGH enables  */
        /* @SVD:field=RSVD2,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=DS,bitOffset=10,bitWidth=1,description="Drive Select. Logic LOW selects 4mA drive,logic HIGH selects 20mA drive" */
        volatile uint32_t DS: 1;  /* Drive Select. Logic LOW selects 4mA drive,logic HIGH selects */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA42_Type;

/* @SVD:register=PAD_PA43,offset=0x00E0,size=32,access=read-write,reset=0x0,description="PAD_PA43" */
/* PAD_PA43 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA43_Type;

/* @SVD:register=PAD_PA44,offset=0x00E4,size=32,access=read-write,reset=0x0,description="PAD_PA44" */
/* PAD_PA44 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=4,description="Function Select" */
        volatile uint32_t FSEL: 4;  /* Function Select */
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_PINMUX_PAD_PA44_Type;

/* HPSYS_PINMUX 寄存器结构体 */
typedef struct
{
    HPSYS_PINMUX_PAD_SA00_Type PAD_SA00;  /* 0x0000 - PAD_SA00 */
    HPSYS_PINMUX_PAD_SA01_Type PAD_SA01;  /* 0x0004 - PAD_SA01 */
    HPSYS_PINMUX_PAD_SA02_Type PAD_SA02;  /* 0x0008 - PAD_SA02 */
    HPSYS_PINMUX_PAD_SA03_Type PAD_SA03;  /* 0x000C - PAD_SA03 */
    HPSYS_PINMUX_PAD_SA04_Type PAD_SA04;  /* 0x0010 - PAD_SA04 */
    HPSYS_PINMUX_PAD_SA05_Type PAD_SA05;  /* 0x0014 - PAD_SA05 */
    HPSYS_PINMUX_PAD_SA06_Type PAD_SA06;  /* 0x0018 - PAD_SA06 */
    HPSYS_PINMUX_PAD_SA07_Type PAD_SA07;  /* 0x001C - PAD_SA07 */
    HPSYS_PINMUX_PAD_SA08_Type PAD_SA08;  /* 0x0020 - PAD_SA08 */
    HPSYS_PINMUX_PAD_SA09_Type PAD_SA09;  /* 0x0024 - PAD_SA09 */
    HPSYS_PINMUX_PAD_SA10_Type PAD_SA10;  /* 0x0028 - PAD_SA10 */
    HPSYS_PINMUX_PAD_SA11_Type PAD_SA11;  /* 0x002C - PAD_SA11 */
    HPSYS_PINMUX_PAD_SA12_Type PAD_SA12;  /* 0x0030 - PAD_SA12 */
    HPSYS_PINMUX_PAD_PA00_Type PAD_PA00;  /* 0x0034 - PAD_PA00 */
    HPSYS_PINMUX_PAD_PA01_Type PAD_PA01;  /* 0x0038 - PAD_PA01 */
    HPSYS_PINMUX_PAD_PA02_Type PAD_PA02;  /* 0x003C - PAD_PA02 */
    HPSYS_PINMUX_PAD_PA03_Type PAD_PA03;  /* 0x0040 - PAD_PA03 */
    HPSYS_PINMUX_PAD_PA04_Type PAD_PA04;  /* 0x0044 - PAD_PA04 */
    HPSYS_PINMUX_PAD_PA05_Type PAD_PA05;  /* 0x0048 - PAD_PA05 */
    HPSYS_PINMUX_PAD_PA06_Type PAD_PA06;  /* 0x004C - PAD_PA06 */
    HPSYS_PINMUX_PAD_PA07_Type PAD_PA07;  /* 0x0050 - PAD_PA07 */
    HPSYS_PINMUX_PAD_PA08_Type PAD_PA08;  /* 0x0054 - PAD_PA08 */
    HPSYS_PINMUX_PAD_PA09_Type PAD_PA09;  /* 0x0058 - PAD_PA09 */
    HPSYS_PINMUX_PAD_PA10_Type PAD_PA10;  /* 0x005C - PAD_PA10 */
    HPSYS_PINMUX_PAD_PA11_Type PAD_PA11;  /* 0x0060 - PAD_PA11 */
    HPSYS_PINMUX_PAD_PA12_Type PAD_PA12;  /* 0x0064 - PAD_PA12 */
    HPSYS_PINMUX_PAD_PA13_Type PAD_PA13;  /* 0x0068 - PAD_PA13 */
    HPSYS_PINMUX_PAD_PA14_Type PAD_PA14;  /* 0x006C - PAD_PA14 */
    HPSYS_PINMUX_PAD_PA15_Type PAD_PA15;  /* 0x0070 - PAD_PA15 */
    HPSYS_PINMUX_PAD_PA16_Type PAD_PA16;  /* 0x0074 - PAD_PA16 */
    HPSYS_PINMUX_PAD_PA17_Type PAD_PA17;  /* 0x0078 - PAD_PA17 */
    HPSYS_PINMUX_PAD_PA18_Type PAD_PA18;  /* 0x007C - PAD_PA18 */
    HPSYS_PINMUX_PAD_PA19_Type PAD_PA19;  /* 0x0080 - PAD_PA19 */
    HPSYS_PINMUX_PAD_PA20_Type PAD_PA20;  /* 0x0084 - PAD_PA20 */
    HPSYS_PINMUX_PAD_PA21_Type PAD_PA21;  /* 0x0088 - PAD_PA21 */
    HPSYS_PINMUX_PAD_PA22_Type PAD_PA22;  /* 0x008C - PAD_PA22 */
    HPSYS_PINMUX_PAD_PA23_Type PAD_PA23;  /* 0x0090 - PAD_PA23 */
    HPSYS_PINMUX_PAD_PA24_Type PAD_PA24;  /* 0x0094 - PAD_PA24 */
    HPSYS_PINMUX_PAD_PA25_Type PAD_PA25;  /* 0x0098 - PAD_PA25 */
    HPSYS_PINMUX_PAD_PA26_Type PAD_PA26;  /* 0x009C - PAD_PA26 */
    HPSYS_PINMUX_PAD_PA27_Type PAD_PA27;  /* 0x00A0 - PAD_PA27 */
    HPSYS_PINMUX_PAD_PA28_Type PAD_PA28;  /* 0x00A4 - PAD_PA28 */
    HPSYS_PINMUX_PAD_PA29_Type PAD_PA29;  /* 0x00A8 - PAD_PA29 */
    HPSYS_PINMUX_PAD_PA30_Type PAD_PA30;  /* 0x00AC - PAD_PA30 */
    HPSYS_PINMUX_PAD_PA31_Type PAD_PA31;  /* 0x00B0 - PAD_PA31 */
    HPSYS_PINMUX_PAD_PA32_Type PAD_PA32;  /* 0x00B4 - PAD_PA32 */
    HPSYS_PINMUX_PAD_PA33_Type PAD_PA33;  /* 0x00B8 - PAD_PA33 */
    HPSYS_PINMUX_PAD_PA34_Type PAD_PA34;  /* 0x00BC - PAD_PA34 */
    HPSYS_PINMUX_PAD_PA35_Type PAD_PA35;  /* 0x00C0 - PAD_PA35 */
    HPSYS_PINMUX_PAD_PA36_Type PAD_PA36;  /* 0x00C4 - PAD_PA36 */
    HPSYS_PINMUX_PAD_PA37_Type PAD_PA37;  /* 0x00C8 - PAD_PA37 */
    HPSYS_PINMUX_PAD_PA38_Type PAD_PA38;  /* 0x00CC - PAD_PA38 */
    HPSYS_PINMUX_PAD_PA39_Type PAD_PA39;  /* 0x00D0 - PAD_PA39 */
    HPSYS_PINMUX_PAD_PA40_Type PAD_PA40;  /* 0x00D4 - PAD_PA40 */
    HPSYS_PINMUX_PAD_PA41_Type PAD_PA41;  /* 0x00D8 - PAD_PA41 */
    HPSYS_PINMUX_PAD_PA42_Type PAD_PA42;  /* 0x00DC - PAD_PA42 */
    HPSYS_PINMUX_PAD_PA43_Type PAD_PA43;  /* 0x00E0 - PAD_PA43 */
    HPSYS_PINMUX_PAD_PA44_Type PAD_PA44;  /* 0x00E4 - PAD_PA44 */
} HPSYS_PINMUX_TypeDef;

/* ============================================================================ */
/* ATIM1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=ATIM1,base=0x50004000,description="",groupName=ATIM */

#define ATIM1_BASE                                         (0x50004000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="TIM control register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CEN,bitOffset=0,bitWidth=1,description="Counter enable\n              0: Counter disabled\n              1: Counter enabled\n              External clock, gated mode and encoder mode can work only if the CEN bit has been previously set by software. However trigger mode can set the CEN bit automatically by hardware. CEN is cleared automatically in one-pulse mode, when an update event occurs." */
        volatile uint32_t CEN: 1;  /* Counter enable               0: Counter disabled             */
        /* @SVD:field=UDIS,bitOffset=1,bitWidth=1,description="Update disable\n              This bit is set and cleared by software to enable/disable UEV event generation.\n              0: UEV enabled. The Update (UEV) event is generated by one of the following events:\n                 Counter overflow/underflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              Buffered registers are then loaded with their preload values.\n              1: UEV disabled. The Update event is not generated, shadow registers keep their value (ARR, PSC, CCRx). However the counter and the prescaler are reinitialized if the UG bit is set or if a hardware reset is received from the slave mode controller." */
        volatile uint32_t UDIS: 1;  /* Update disable               This bit is set and cleared by  */
        /* @SVD:field=URS,bitOffset=2,bitWidth=1,description="Update request source\n              This bit is set and cleared by software to select the UEV event sources.\n              0: Any of the following events generate an update interrupt or DMA request if enabled. \n              These events can be: \n                 Counter overflow/underflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              1: Only counter overflow/underflow generates an update interrupt or DMA request if enabled." */
        volatile uint32_t URS: 1;  /* Update request source               This bit is set and clea */
        /* @SVD:field=OPM,bitOffset=3,bitWidth=1,description="One-pulse mode\n              0: Counter is not stopped at update event\n              1: Counter stops counting at the next update event (clearing the bit CEN)" */
        volatile uint32_t OPM: 1;  /* One-pulse mode               0: Counter is not stopped at up */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="Direction\n              0: Counter used as upcounter\n              1: Counter used as downcounter" */
        volatile uint32_t DIR: 1;  /* Direction               0: Counter used as upcounter         */
        /* @SVD:field=CMS,bitOffset=5,bitWidth=2,description="Center-aligned mode selection\n              00: Edge-aligned mode. The counter counts up or down depending on the direction bit (DIR).\n              01: Center-aligned mode 1. The counter counts up and down alternatively. Output compare interrupt flags of channels configured in output (CCxS=00 in CCMRx register) are set only when the counter is counting down.\n              10: Center-aligned mode 2. The counter counts up and down alternatively. Output compare interrupt flags of channels configured in output (CCxS=00 in CCMRx register) are set only when the counter is counting up.\n              11: Center-aligned mode 3. The counter counts up and down alternatively. Output compare interrupt flags of channels configured in output (CCxS=00 in CCMRx register) are set both when the counter is counting up or down." */
        volatile uint32_t CMS: 2;  /* Center-aligned mode selection               00: Edge-aligned */
        /* @SVD:field=ARPE,bitOffset=7,bitWidth=1,description="Auto-reload preload enable\n              0: ARR register is not buffered\n              1: ARR register is buffered" */
        volatile uint32_t ARPE: 1;  /* Auto-reload preload enable               0: ARR register is  */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=UIFREMAP,bitOffset=11,bitWidth=1,description="UIF status bit remapping\n              0: No remapping. UIF status bit is not copied to CNT register bit 31\n              1: Remapping enabled. UIF status bit is copied to CNT register bit 31." */
        volatile uint32_t UIFREMAP: 1;  /* UIF status bit remapping               0: No remapping. UIF  */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="TIM control register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCPC,bitOffset=0,bitWidth=1,description="Capture/compare preloaded control\n              0: CCxE, CCxNE and OCxM bits are not preloaded\n              1: CCxE, CCxNE and OCxM bits are preloaded, after having been written, they are updated only when a commutation event (COM) occurs (COMG bit set or edge detected on TRGI after Trigger selection, depending on the CCUS bit).\n              This bit acts only on channels that have a complementary output." */
        volatile uint32_t CCPC: 1;  /* Capture/compare preloaded control               0: CCxE, CCx */
        /* @SVD:field=RSVD4,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=CCUS,bitOffset=2,bitWidth=1,description="Capture/compare control update selection\n              0: When capture/compare control bits are preloaded (CCPC=1), they are updated by setting the COMG bit only\n              1: When capture/compare control bits are preloaded (CCPC=1), they are updated by setting the COMG bit or when an edge occurs on TRGI after Trigger selection\n              This bit acts only on channels that have a complementary output." */
        volatile uint32_t CCUS: 1;  /* Capture/compare control update selection               0: Wh */
        /* @SVD:field=CCDS,bitOffset=3,bitWidth=1,description="Capture/compare DMA selection\n              0: CCx DMA request sent when CCx event occurs\n              1: CCx DMA requests sent when update event occurs" */
        volatile uint32_t CCDS: 1;  /* Capture/compare DMA selection               0: CCx DMA reque */
        /* @SVD:field=MMS,bitOffset=4,bitWidth=3,description="Master mode selection\n              These bits allow to select the information to be sent in master mode to slave timers for synchronization (TRGO). The combination is as follows:\n              000: Reset - the UG bit from the EGR register is used as trigger output (TRGO). If the reset is generated by the trigger input (slave mode controller configured in reset mode) then the signal on TRGO is delayed compared to the actual reset.\n              001: Enable - the Counter enable signal, CNT_EN, is used as trigger output (TRGO). It is useful to start several timers at the same time or to control a window in which a slave timer is enabled. The Counter Enable signal is generated by a logic OR between CEN control bit and the trigger input when configured in gated mode. \n              When the Counter Enable signal is controlled by the trigger input, there is a delay on TRGO, except if the master/slave mode is selected.\n              010: Update - The update event is selected as trigger output (TRGO). For instance a master timer can then be used as a prescaler for a slave timer.\n              011: Compare Pulse - The trigger output send a positive pulse when the CC1IF flag is to be set (even if it was already high), as soon as a capture or a compare match occurred. (TRGO)\n              100: Compare - OC1REFC signal is used as trigger output (TRGO)\n              101: Compare - OC2REFC signal is used as trigger output (TRGO)\n              110: Compare - OC3REFC signal is used as trigger output (TRGO)\n              111: Compare - OC4REFC signal is used as trigger output (TRGO)" */
        volatile uint32_t MMS: 3;  /* Master mode selection               These bits allow to sele */
        /* @SVD:field=TI1S,bitOffset=7,bitWidth=1,description="TI1 selection\n              0: The CH1 pin is connected to TI1 input\n              1: The CH1, CH2 and CH3 pins are connected to the TI1 input (XOR combination)" */
        volatile uint32_t TI1S: 1;  /* TI1 selection               0: The CH1 pin is connected to T */
        /* @SVD:field=OIS1,bitOffset=8,bitWidth=1,description="Output Idle state 1 (OC1 output)\n              0: OC1=0 (after a dead-time if OC1N is implemented) when MOE=0\n              1: OC1=1 (after a dead-time if OC1N is implemented) when MOE=0\n              This bit, as well as other OISx, can not be modified as long as LOCK level 1, 2 or 3 has been programmed" */
        volatile uint32_t OIS1: 1;  /* Output Idle state 1 (OC1 output)               0: OC1=0 (aft */
        /* @SVD:field=OIS1N,bitOffset=9,bitWidth=1,description="Output Idle state 1 (OC1N output)\n              0: OC1N=0 after a dead-time when MOE=0\n              1: OC1N=1 after a dead-time when MOE=0\n              This bit, as well as other OISxN, can not be modified as long as LOCK level 1, 2 or 3 has been programmed" */
        volatile uint32_t OIS1N: 1;  /* Output Idle state 1 (OC1N output)               0: OC1N=0 af */
        /* @SVD:field=OIS2,bitOffset=10,bitWidth=1,description="Output Idle state 2 (OC2 output)" */
        volatile uint32_t OIS2: 1;  /* Output Idle state 2 (OC2 output) */
        /* @SVD:field=OIS2N,bitOffset=11,bitWidth=1,description="Output Idle state 2 (OC2N output)" */
        volatile uint32_t OIS2N: 1;  /* Output Idle state 2 (OC2N output) */
        /* @SVD:field=OIS3,bitOffset=12,bitWidth=1,description="Output Idle state 3 (OC3 output)" */
        volatile uint32_t OIS3: 1;  /* Output Idle state 3 (OC3 output) */
        /* @SVD:field=OIS3N,bitOffset=13,bitWidth=1,description="Output Idle state 3 (OC3N output)" */
        volatile uint32_t OIS3N: 1;  /* Output Idle state 3 (OC3N output) */
        /* @SVD:field=OIS4,bitOffset=14,bitWidth=1,description="Output Idle state 4 (OC4 output)" */
        volatile uint32_t OIS4: 1;  /* Output Idle state 4 (OC4 output) */
        /* @SVD:field=RSVD3,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=OIS5,bitOffset=16,bitWidth=1,description="Output Idle state 5 (OC5 output)" */
        volatile uint32_t OIS5: 1;  /* Output Idle state 5 (OC5 output) */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=OIS6,bitOffset=18,bitWidth=1,description="Output Idle state 6 (OC6 output)" */
        volatile uint32_t OIS6: 1;  /* Output Idle state 6 (OC6 output) */
        /* @SVD:field=RSVD,bitOffset=19,bitWidth=13,description="" */
        volatile uint32_t RSVD: 13;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CR2_Type;

/* @SVD:register=SMCR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="TIM slave mode control register" */
/* SMCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=TS,bitOffset=4,bitWidth=3,description="Trigger selection\n              This bit-field selects the trigger input to be used to synchronize the counter.\n              000: Internal Trigger 0 (ITR0)\n              001: Internal Trigger 1 (ITR1) \n              010: Internal Trigger 2 (ITR2)\n              011: Internal Trigger 3 (ITR3)\n              100: TI1 Edge Detector (TI1F_ED)\n              101: Filtered Timer Input 1 (TI1FP1)\n              110: Filtered Timer Input 2 (TI2FP2)\n              111: External Trigger input (ETRF)" */
        volatile uint32_t TS: 3;  /* Trigger selection               This bit-field selects the t */
        /* @SVD:field=MSM,bitOffset=7,bitWidth=1,description="Master/Slave mode\n              0: No action\n              1: The effect of an event on the trigger input (TRGI) is delayed to allow a perfect synchronization between the current timer and its slaves (through TRGO). It is useful if we want to synchronize several timers on a single external event." */
        volatile uint32_t MSM: 1;  /* Master/Slave mode               0: No action               1 */
        /* @SVD:field=ETF,bitOffset=8,bitWidth=4,description="External trigger filter\n              This bit-field then defines the frequency used to sample ETRP signal and the length of the digital filter applied to ETRP. The digital filter is made of an event counter in which N \n              consecutive events are needed to validate a transition on the output:\n              0000: No filter, sampling is done at fCLK\n              0001:   fSAMPLING=fCLK, N=2\n              0010:   fSAMPLING=fCLK, N=4\n              0011:   fSAMPLING=fCLK, N=8\n              0100:   fSAMPLING=fCLK/2, N=6\n              0101:   fSAMPLING=fCLK/2, N=8\n              0110:   fSAMPLING=fCLK/4, N=6\n              0111:   fSAMPLING=fCLK/4, N=8\n              1000:   fSAMPLING=fCLK/8, N=6\n              1001:   fSAMPLING=fCLK/8, N=8\n              1010:   fSAMPLING=fCLK/16, N=5\n              1011:   fSAMPLING=fCLK/16, N=6\n              1100:   fSAMPLING=fCLK/16, N=8\n              1101:   fSAMPLING=fCLK/32, N=5\n              1110:   fSAMPLING=fCLK/32, N=6\n              1111:   fSAMPLING=fCLK/32, N=8" */
        volatile uint32_t ETF: 4;  /* External trigger filter               This bit-field then de */
        /* @SVD:field=ETPS,bitOffset=12,bitWidth=2,description="External trigger prescaler\n              External trigger signal ETRP frequency must be at most 1/4 of CK_INT frequency. A prescaler can be enabled to reduce ETRP frequency. It is useful when inputting fast external clocks.\n              00: Prescaler OFF\n              01: ETRP frequency divided by 2\n              10: ETRP frequency divided by 4\n              11: ETRP frequency divided by 8" */
        volatile uint32_t ETPS: 2;  /* External trigger prescaler               External trigger si */
        /* @SVD:field=ECE,bitOffset=14,bitWidth=1,description="External clock enable\n              This bit enables External clock mode 2.\n              0: External clock mode 2 disabled\n              1: External clock mode 2 enabled. The counter is clocked by any active edge on the ETRF signal." */
        volatile uint32_t ECE: 1;  /* External clock enable               This bit enables Externa */
        /* @SVD:field=ETP,bitOffset=15,bitWidth=1,description="External trigger polarity\n              This bit selects whether ETR or ETR is used for trigger operations \n              0: ETR is non-inverted, active at high level or rising edge\n              1: ETR is inverted, active at low level or falling edge" */
        volatile uint32_t ETP: 1;  /* External trigger polarity               This bit selects whe */
        /* @SVD:field=SMS,bitOffset=16,bitWidth=4,description="Slave mode selection\n              When external signals are selected the active edge of the trigger signal (TRGI) is linked to the polarity selected on the external input.\n              0000: Slave mode disabled.\n              0001: Encoder mode 1 - Counter counts up/down on TI1FP1 edge depending on TI2FP2 level.\n              0010: Encoder mode 2 - Counter counts up/down on TI2FP2 edge depending on TI1FP1 level.\n              0011: Encoder mode 3 - Counter counts up/down on both TI1FP1 and TI2FP2 edges depending on the level of the other input.\n              0100: Reset Mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter and generates an update of the registers.\n              0101: Gated Mode - The counter clock is enabled when the trigger input (TRGI) is high. The counter stops (but is not reset) as soon as the trigger becomes low. Both start and stop of the counter are controlled.\n              0110: Trigger Mode - The counter starts at a rising edge of the trigger TRGI (but it is not reset). Only the start of the counter is controlled.\n              0111: External Clock Mode 1 - Rising edges of the selected trigger (TRGI) clock the counter.\n              1000: Combined reset + trigger mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter, generates an update of the registers and starts the counter." */
        volatile uint32_t SMS: 4;  /* Slave mode selection               When external signals are */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_SMCR_Type;

/* @SVD:register=DIER,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="TIM DMA/Interrupt enable register" */
/* DIER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIE,bitOffset=0,bitWidth=1,description="Update interrupt enable\n              0: Update interrupt disabled.\n              1: Update interrupt enabled" */
        volatile uint32_t UIE: 1;  /* Update interrupt enable               0: Update interrupt di */
        /* @SVD:field=CC1IE,bitOffset=1,bitWidth=1,description="Capture/Compare 1 interrupt enable\n              0: CC1 interrupt disabled.\n              1: CC1 interrupt enabled" */
        volatile uint32_t CC1IE: 1;  /* Capture/Compare 1 interrupt enable               0: CC1 inte */
        /* @SVD:field=CC2IE,bitOffset=2,bitWidth=1,description="Capture/Compare 2 interrupt enable\n              0: CC2 interrupt disabled.\n              1: CC2 interrupt enabled." */
        volatile uint32_t CC2IE: 1;  /* Capture/Compare 2 interrupt enable               0: CC2 inte */
        /* @SVD:field=CC3IE,bitOffset=3,bitWidth=1,description="Capture/Compare 3 interrupt enable\n              0: CC3 interrupt disabled.\n              1: CC3 interrupt enabled" */
        volatile uint32_t CC3IE: 1;  /* Capture/Compare 3 interrupt enable               0: CC3 inte */
        /* @SVD:field=CC4IE,bitOffset=4,bitWidth=1,description="Capture/Compare 4 interrupt enable\n              0: CC4 interrupt disabled.\n              1: CC4 interrupt enabled" */
        volatile uint32_t CC4IE: 1;  /* Capture/Compare 4 interrupt enable               0: CC4 inte */
        /* @SVD:field=COMIE,bitOffset=5,bitWidth=1,description="COM interrupt enable\n              0: COM interrupt disabled\n              1: COM interrupt enabled" */
        volatile uint32_t COMIE: 1;  /* COM interrupt enable               0: COM interrupt disabled */
        /* @SVD:field=TIE,bitOffset=6,bitWidth=1,description="Trigger interrupt enable\n              0: Trigger interrupt disabled.\n              1: Trigger interrupt enabled" */
        volatile uint32_t TIE: 1;  /* Trigger interrupt enable               0: Trigger interrupt  */
        /* @SVD:field=BIE,bitOffset=7,bitWidth=1,description="Break interrupt enable\n              0: Break interrupt disabled\n              1: Break interrupt enabled" */
        volatile uint32_t BIE: 1;  /* Break interrupt enable               0: Break interrupt disa */
        /* @SVD:field=UDE,bitOffset=8,bitWidth=1,description="Update DMA request enable\n              0: Update DMA request disabled.\n              1: Update DMA request enabled" */
        volatile uint32_t UDE: 1;  /* Update DMA request enable               0: Update DMA reques */
        /* @SVD:field=CC1DE,bitOffset=9,bitWidth=1,description="Capture/Compare 1 DMA request enable\n              0: CC1 DMA request disabled.\n              1: CC1 DMA request enabled." */
        volatile uint32_t CC1DE: 1;  /* Capture/Compare 1 DMA request enable               0: CC1 DM */
        /* @SVD:field=CC2DE,bitOffset=10,bitWidth=1,description="Capture/Compare 2 DMA request enable\n              0: CC2 DMA request disabled.\n              1: CC2 DMA request enabled." */
        volatile uint32_t CC2DE: 1;  /* Capture/Compare 2 DMA request enable               0: CC2 DM */
        /* @SVD:field=CC3DE,bitOffset=11,bitWidth=1,description="Capture/Compare 3 DMA request enable\n              0: CC3 DMA request disabled.\n              1: CC3 DMA request enabled." */
        volatile uint32_t CC3DE: 1;  /* Capture/Compare 3 DMA request enable               0: CC3 DM */
        /* @SVD:field=CC4DE,bitOffset=12,bitWidth=1,description="Capture/Compare 4 DMA request enable\n              0: CC4 DMA request disabled.\n              1: CC4 DMA request enabled" */
        volatile uint32_t CC4DE: 1;  /* Capture/Compare 4 DMA request enable               0: CC4 DM */
        /* @SVD:field=COMDE,bitOffset=13,bitWidth=1,description="COM DMA request enable\n              0: COM DMA request disabled\n              1: COM DMA request enabled" */
        volatile uint32_t COMDE: 1;  /* COM DMA request enable               0: COM DMA request disa */
        /* @SVD:field=TDE,bitOffset=14,bitWidth=1,description="Trigger DMA request enable\n              0: Trigger DMA request disabled.\n              1: Trigger DMA request enabled." */
        volatile uint32_t TDE: 1;  /* Trigger DMA request enable               0: Trigger DMA requ */
        /* @SVD:field=RSVD2,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CC5IE,bitOffset=16,bitWidth=1,description="Capture/Compare 5 interrupt enable\n              0: CC5 interrupt disabled.\n              1: CC5 interrupt enabled" */
        volatile uint32_t CC5IE: 1;  /* Capture/Compare 5 interrupt enable               0: CC5 inte */
        /* @SVD:field=CC6IE,bitOffset=17,bitWidth=1,description="Capture/Compare 6 interrupt enable\n              0: CC6 interrupt disabled.\n              1: CC6 interrupt enabled" */
        volatile uint32_t CC6IE: 1;  /* Capture/Compare 6 interrupt enable               0: CC6 inte */
        /* @SVD:field=RSVD,bitOffset=18,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_DIER_Type;

/* @SVD:register=SR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="TIM status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIF,bitOffset=0,bitWidth=1,description="Update interrupt flag\n              This bit is set by hardware on an update event. It is cleared by software.\n              0: No update occurred\n              1: Update interrupt pending. This bit is set by hardware when the registers are updated:\n              - At overflow or underflow regarding the repetition counter value (update if repetition counter = 0)  and if UDIS=0 in the CR1 register.\n              - When CNT is reinitialized by software using the UG bit in EGR register, if URS=0 and UDIS=0 in the CR1 register.\n              - When CNT is reinitialized by a trigger event, if URS=0 and UDIS=0 in the CR1 register." */
        volatile uint32_t UIF: 1;  /* Update interrupt flag               This bit is set by hardw */
        /* @SVD:field=CC1IF,bitOffset=1,bitWidth=1,description="Capture/Compare 1 interrupt flag\n              If channel CC1 is configured as output: This flag is set by hardware when the counter matches the compare value and in retriggerable one pulse mode. It is cleared by software.\n              0: No match.\n              1: The content of the counter CNT has matched the content of the CCR1 register.\n              If channel CC1 is configured as input: This bit is set by hardware on a capture. It is cleared by software or by reading the CCR1 register.\n              0: No input capture occurred.\n              1: The counter value has been captured in CCR1 register." */
        volatile uint32_t CC1IF: 1;  /* Capture/Compare 1 interrupt flag               If channel CC */
        /* @SVD:field=CC2IF,bitOffset=2,bitWidth=1,description="Capture/Compare 2 interrupt flag" */
        volatile uint32_t CC2IF: 1;  /* Capture/Compare 2 interrupt flag */
        /* @SVD:field=CC3IF,bitOffset=3,bitWidth=1,description="Capture/Compare 3 interrupt flag" */
        volatile uint32_t CC3IF: 1;  /* Capture/Compare 3 interrupt flag */
        /* @SVD:field=CC4IF,bitOffset=4,bitWidth=1,description="Capture/Compare 4 interrupt flag" */
        volatile uint32_t CC4IF: 1;  /* Capture/Compare 4 interrupt flag */
        /* @SVD:field=COMIF,bitOffset=5,bitWidth=1,description="COM interrupt flag\n              This flag is set by hardware on COM event (when Capture/compare Control bits - CCxE, CCxNE, OCxM - have been updated). It is cleared by software.\n              0: No COM event occurred.\n              1: COM interrupt pending." */
        volatile uint32_t COMIF: 1;  /* COM interrupt flag               This flag is set by hardwar */
        /* @SVD:field=TIF,bitOffset=6,bitWidth=1,description="Trigger interrupt flag\n              This flag is set by hardware on trigger event. It is set when the counter starts or stops when gated mode is selected. It is cleared by software.\n              0: No trigger event occurred.\n              1: Trigger interrupt pending." */
        volatile uint32_t TIF: 1;  /* Trigger interrupt flag               This flag is set by har */
        /* @SVD:field=BIF,bitOffset=7,bitWidth=1,description="Break interrupt flag\n              This flag is set by hardware as soon as the break input goes active. It can be cleared by software if the break input is not active.\n              0: No break event occurred.\n              1: An active level has been detected on the break input. An interrupt is generated if BIE=1 in the DIER register." */
        volatile uint32_t BIF: 1;  /* Break interrupt flag               This flag is set by hardw */
        /* @SVD:field=B2IF,bitOffset=8,bitWidth=1,description="Break 2 interrupt flag\n              This flag is set by hardware as soon as the break 2 input goes active. It can be cleared by software if the break 2 input is not active.\n              0: No break event occurred.\n              1: An active level has been detected on the break 2 input. An interrupt is generated if BIE=1 in the DIER register." */
        volatile uint32_t B2IF: 1;  /* Break 2 interrupt flag               This flag is set by har */
        /* @SVD:field=CC1OF,bitOffset=9,bitWidth=1,description="Capture/Compare 1 overcapture flag\n              This flag is set by hardware only when the corresponding channel is configured in input capture mode. It is cleared by software by writing it to '0'.\n              0: No overcapture has been detected.\n              1: The counter value has been captured in CCR1 register while CC1IF flag was already set" */
        volatile uint32_t CC1OF: 1;  /* Capture/Compare 1 overcapture flag               This flag i */
        /* @SVD:field=CC2OF,bitOffset=10,bitWidth=1,description="Capture/Compare 2 overcapture flag" */
        volatile uint32_t CC2OF: 1;  /* Capture/Compare 2 overcapture flag */
        /* @SVD:field=CC3OF,bitOffset=11,bitWidth=1,description="Capture/Compare 3 overcapture flag" */
        volatile uint32_t CC3OF: 1;  /* Capture/Compare 3 overcapture flag */
        /* @SVD:field=CC4OF,bitOffset=12,bitWidth=1,description="Capture/Compare 4 overcapture flag" */
        volatile uint32_t CC4OF: 1;  /* Capture/Compare 4 overcapture flag */
        /* @SVD:field=SBIF,bitOffset=13,bitWidth=1,description="System Break interrupt flag\n              This flag is set by hardware as soon as the system break input goes active. It can be cleared by software if the system break input is not active.\n              This flag must be reset to re-start PWM operation.\n              0: No break event occurred.\n              1: An active level has been detected on the system break input. An interrupt is generated if BIE=1 in the DIER register." */
        volatile uint32_t SBIF: 1;  /* System Break interrupt flag               This flag is set b */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CC5IF,bitOffset=16,bitWidth=1,description="Compare 5 interrupt flag" */
        volatile uint32_t CC5IF: 1;  /* Compare 5 interrupt flag */
        /* @SVD:field=CC6IF,bitOffset=17,bitWidth=1,description="Compare 6 interrupt flag" */
        volatile uint32_t CC6IF: 1;  /* Compare 6 interrupt flag */
        /* @SVD:field=RSVD,bitOffset=18,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_SR_Type;

/* @SVD:register=EGR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Event generation register" */
/* EGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UG,bitOffset=0,bitWidth=1,description="Update generation\n              This bit can be set by software, it is automatically cleared by hardware.\n              0: No action\n              1: Re-initialize the counter and generates an update of the registers. The prescaler counter is cleared too (anyway the prescaler ratio is not affected). The counter is cleared if the center-aligned mode is selected or if DIR=0 (upcounting), else it takes the auto-reload value (ARR) if DIR=1 (downcounting)." */
        volatile uint32_t UG: 1;  /* Update generation               This bit can be set by softw */
        /* @SVD:field=CC1G,bitOffset=1,bitWidth=1,description="Capture/compare 1 generation\n              This bit is set by software in order to generate an event, it is automatically cleared by hardware.\n              0: No action\n              1: A capture/compare event is generated on channel 1:\n              If channel CC1 is configured as output:\n              CC1IF flag is set, Corresponding interrupt or DMA request is sent if enabled.\n              If channel CC1 is configured as input:\n              The current value of the counter is captured in CCR1 register. The CC1IF flag is set, the corresponding interrupt or DMA request is sent if enabled. The CC1OF flag is set if the CC1IF flag was already high." */
        volatile uint32_t CC1G: 1;  /* Capture/compare 1 generation               This bit is set b */
        /* @SVD:field=CC2G,bitOffset=2,bitWidth=1,description="Capture/compare 2 generation" */
        volatile uint32_t CC2G: 1;  /* Capture/compare 2 generation */
        /* @SVD:field=CC3G,bitOffset=3,bitWidth=1,description="Capture/compare 3 generation" */
        volatile uint32_t CC3G: 1;  /* Capture/compare 3 generation */
        /* @SVD:field=CC4G,bitOffset=4,bitWidth=1,description="Capture/compare 4 generation" */
        volatile uint32_t CC4G: 1;  /* Capture/compare 4 generation */
        /* @SVD:field=COMG,bitOffset=5,bitWidth=1,description="Capture/Compare control update generation\n              This bit can be set by software, it is automatically cleared by hardware\n              0: No action\n              1: When CCPC bit is set, it allows to update CCxE, CCxNE and OCxM bits\n              This bit acts only on channels having a complementary output." */
        volatile uint32_t COMG: 1;  /* Capture/Compare control update generation               This */
        /* @SVD:field=TG,bitOffset=6,bitWidth=1,description="Trigger generation\n              This bit is set by software in order to generate an event, it is automatically cleared by hardware.\n              0: No action\n              1: The TIF flag is set in SR register. Related interrupt or DMA transfer can occur if enabled." */
        volatile uint32_t TG: 1;  /* Trigger generation               This bit is set by software */
        /* @SVD:field=BG,bitOffset=7,bitWidth=1,description="Break generation\n              This bit is set by software in order to generate an event, it is automatically cleared by hardware.\n              0: No action\n              1: A break event is generated. MOE bit is cleared and BIF flag is set. Related interrupt or DMA transfer can occur if enabled." */
        volatile uint32_t BG: 1;  /* Break generation               This bit is set by software i */
        /* @SVD:field=B2G,bitOffset=8,bitWidth=1,description="Break 2 generation\n              This bit is set by software in order to generate an event, it is automatically cleared by hardware.\n              0: No action\n              1: A break 2 event is generated. MOE bit is cleared and B2IF flag is set. Related interrupt can occur if enabled." */
        volatile uint32_t B2G: 1;  /* Break 2 generation               This bit is set by software */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_EGR_Type;

/* @SVD:register=CCMR1,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="TIM capture/compare mode register 1" */
/* CCMR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CC1S,bitOffset=0,bitWidth=2,description="Capture/Compare 1 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC1 channel is configured as output\n              01: CC1 channel is configured as input, IC1 is mapped on TI1\n              10: CC1 channel is configured as input, IC1 is mapped on TI2\n              11: CC1 channel is configured as input, IC1 is mapped on TRC. This mode is working only if an internal trigger input is selected through TS bit (SMCR register)" */
        volatile uint32_t CC1S: 2;  /* Capture/Compare 1 selection               This bit-field def */
        /* @SVD:field=IC1PSC,bitOffset=2,bitWidth=2,description="Input capture 1 prescaler\n              This bit-field defines the ratio of the prescaler acting on CC1 input (IC1). The prescaler is reset as soon as CC1E=0 (CCER register).\n              00: no prescaler, capture is done each time an edge is detected on the capture input\n              01: capture is done once every 2 events\n              10: capture is done once every 4 events\n              11: capture is done once every 8 events" */
        volatile uint32_t IC1PSC: 2;  /* Input capture 1 prescaler               This bit-field defin */
        /* @SVD:field=IC1F,bitOffset=4,bitWidth=4,description="Input capture 1 filter\n              This bit-field defines the frequency used to sample TI1 input and the length of the digital filter applied to TI1. The digital filter is made of an event counter in which N consecutive events are needed to validate a transition on the output:\n              0000: No filter, sampling is done at fCLK\n              0001: fSAMPLING=fCLK, N=2\n              0010: fSAMPLING=fCLK, N=4\n              0011: fSAMPLING=fCLK, N=8\n              0100: fSAMPLING=fCLK/2, N=6\n              0101: fSAMPLING=fCLK/2, N=8\n              0110: fSAMPLING=fCLK/4, N=6\n              0111: fSAMPLING=fCLK/4, N=8\n              1000: fSAMPLING=fCLK/8, N=6\n              1001: fSAMPLING=fCLK/8, N=8\n              1010: fSAMPLING=fCLK/16, N=5\n              1011: fSAMPLING=fCLK/16, N=6\n              1100: fSAMPLING=fCLK/16, N=8\n              1101: fSAMPLING=fCLK/32, N=5\n              1110: fSAMPLING=fCLK/32, N=6\n              1111: fSAMPLING=fCLK/32, N=8" */
        volatile uint32_t IC1F: 4;  /* Input capture 1 filter               This bit-field defines  */
        /* @SVD:field=CC2S,bitOffset=8,bitWidth=2,description="Capture/Compare 2 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC2 channel is configured as output\n              01: CC2 channel is configured as input, IC2 is mapped on TI2\n              10: CC2 channel is configured as input, IC2 is mapped on TI1\n              11: CC2 channel is configured as input, IC2 is mapped on TRC. This mode is working only if an internal trigger input is selected through the TS bit (SMCR register)" */
        volatile uint32_t CC2S: 2;  /* Capture/Compare 2 selection               This bit-field def */
        /* @SVD:field=IC2PSC,bitOffset=10,bitWidth=2,description="Input capture 2 prescaler" */
        volatile uint32_t IC2PSC: 2;  /* Input capture 2 prescaler */
        /* @SVD:field=IC2F,bitOffset=12,bitWidth=4,description="Input capture 2 filter" */
        volatile uint32_t IC2F: 4;  /* Input capture 2 filter */
        /* @SVD:field=OC1CE,bitOffset=16,bitWidth=1,description="Output compare 1 clear enable\n              0: OC1Ref is not affected by the ETRF input\n              1: OC1Ref is cleared as soon as a High level is detected on ETRF input" */
        volatile uint32_t OC1CE: 1;  /* Output compare 1 clear enable               0: OC1Ref is not */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=OC1PE,bitOffset=19,bitWidth=1,description="Output compare 1 preload enable\n              0: Preload register on CCR1 disabled. CCR1 can be written at anytime, the new value is taken in account immediately.\n              1: Preload register on CCR1 enabled. Read/Write operations access the preload register. CCR1 preload value is loaded in the active register at each update event.\n              These bits can not be modified as long as LOCK level 3 has been programmed and CC1S='00' (the channel is configured in output)." */
        volatile uint32_t OC1PE: 1;  /* Output compare 1 preload enable               0: Preload reg */
        /* @SVD:field=OC1M,bitOffset=20,bitWidth=4,description="Output compare 1 mode\n              These bits define the behavior of the output reference signal OC1REF from which OC1 and OC1N are derived. OC1REF is active high whereas OC1 and OC1N active level depends on CC1P and CC1NP bits.\n              0000: Frozen - The comparison between the output compare register CCR1 and the counter CNT has no effect on the outputs.\n              0001: Set channel 1 to active level on match. OC1REF signal is forced high when the counter CNT matches the capture/compare register 1 (CCR1).\n              0010: Set channel 1 to inactive level on match. OC1REF signal is forced low when the counter CNT matches the capture/compare register 1 (CCR1).\n              0011: Toggle - OC1REF toggles when CNT=CCR1.\n              0100: Force inactive level - OC1REF is forced low.\n              0101: Force active level - OC1REF is forced high.\n              0110: PWM mode 1 - In upcounting, channel 1 is active as long as CNTltCCR1 else inactive. In downcounting, channel 1 is inactive (OC1REF=0) as long as CNT>CCR1 else active (OC1REF=1).\n              0111: PWM mode 2 - In upcounting, channel 1 is inactive as long as CNTltCCR1 else active. In downcounting, channel 1 is active as long as CNT>CCR1 else inactive.\n              1000: Retriggerable OPM mode 1 - In up-counting mode, the channel is active until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 1 and the channels becomes inactive again at the next update. In down-counting mode, the channel is inactive until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 1 and the channels becomes inactive again at the next update.\n              1001: Retriggerable OPM mode 2 - In up-counting mode, the channel is inactive until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 2 and the channels becomes inactive again at the next update. In down-counting mode, the channel is active until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 1 and the channels becomes active again at the next update.\n              1010: Reserved,\n              1011: Reserved,\n              1100: Combined PWM mode 1 - OC1REF has the same behavior as in PWM mode 1. OC1REFC is the logical OR between OC1REF and OC2REF.\n              1101: Combined PWM mode 2 - OC1REF has the same behavior as in PWM mode 2. OC1REFC is the logical AND between OC1REF and OC2REF.\n              1110: Asymmetric PWM mode 1 - OC1REF has the same behavior as in PWM mode 1. OC1REFC outputs OC1REF when the counter is counting up, OC2REF when it is counting down.\n              1111: Asymmetric PWM mode 2 - OC1REF has the same behavior as in PWM mode 2. OC1REFC outputs OC1REF when the counter is counting up, OC2REF when it is counting down.\n              These bits can not be modified as long as LOCK level 3 has been programmed and CC1S=00 (the channel is configured in output).\n              On channels having a complementary output, this bit field is preloaded. If the CCPC bit is set in the CR2 register then the OC1M active bits take the new value from the preloaded bits only when a COM event is generated." */
        volatile uint32_t OC1M: 4;  /* Output compare 1 mode               These bits define the be */
        /* @SVD:field=OC2CE,bitOffset=24,bitWidth=1,description="Output compare 2 clear enable" */
        volatile uint32_t OC2CE: 1;  /* Output compare 2 clear enable */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=OC2PE,bitOffset=27,bitWidth=1,description="Output compare 2 preload enable" */
        volatile uint32_t OC2PE: 1;  /* Output compare 2 preload enable */
        /* @SVD:field=OC2M,bitOffset=28,bitWidth=4,description="Output compare 2 mode" */
        volatile uint32_t OC2M: 4;  /* Output compare 2 mode */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCMR1_Type;

/* @SVD:register=CCMR2,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="TIM capture/compare mode register 2" */
/* CCMR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CC3S,bitOffset=0,bitWidth=2,description="Capture/Compare 3 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC3 channel is configured as output\n              01: CC3 channel is configured as input, IC3 is mapped on TI3\n              10: CC3 channel is configured as input, IC3 is mapped on TI4\n              11: CC3 channel is configured as input, IC3 is mapped on TRC. This mode is working only if an internal trigger input is selected through TS bit (SMCR register)" */
        volatile uint32_t CC3S: 2;  /* Capture/Compare 3 selection               This bit-field def */
        /* @SVD:field=IC3PSC,bitOffset=2,bitWidth=2,description="Input capture 3 prescaler" */
        volatile uint32_t IC3PSC: 2;  /* Input capture 3 prescaler */
        /* @SVD:field=IC3F,bitOffset=4,bitWidth=4,description="Input capture 3 filter" */
        volatile uint32_t IC3F: 4;  /* Input capture 3 filter */
        /* @SVD:field=CC4S,bitOffset=8,bitWidth=2,description="Capture/Compare 4 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC4 channel is configured as output\n              01: CC4 channel is configured as input, IC4 is mapped on TI4\n              10: CC4 channel is configured as input, IC4 is mapped on TI3\n              11: CC4 channel is configured as input, IC4 is mapped on TRC. This mode is working only if an internal trigger input is selected through TS bit (SMCR register)" */
        volatile uint32_t CC4S: 2;  /* Capture/Compare 4 selection               This bit-field def */
        /* @SVD:field=IC4PSC,bitOffset=10,bitWidth=2,description="Input capture 4 prescaler" */
        volatile uint32_t IC4PSC: 2;  /* Input capture 4 prescaler */
        /* @SVD:field=IC4F,bitOffset=12,bitWidth=4,description="Input capture 4 filter" */
        volatile uint32_t IC4F: 4;  /* Input capture 4 filter */
        /* @SVD:field=OC3CE,bitOffset=16,bitWidth=1,description="Output compare 3 clear enable" */
        volatile uint32_t OC3CE: 1;  /* Output compare 3 clear enable */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=OC3PE,bitOffset=19,bitWidth=1,description="Output compare 3 preload enable" */
        volatile uint32_t OC3PE: 1;  /* Output compare 3 preload enable */
        /* @SVD:field=OC3M,bitOffset=20,bitWidth=4,description="Output compare 3 mode" */
        volatile uint32_t OC3M: 4;  /* Output compare 3 mode */
        /* @SVD:field=OC4CE,bitOffset=24,bitWidth=1,description="Output compare 4 clear enable" */
        volatile uint32_t OC4CE: 1;  /* Output compare 4 clear enable */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=OC4PE,bitOffset=27,bitWidth=1,description="Output compare 4 preload enable" */
        volatile uint32_t OC4PE: 1;  /* Output compare 4 preload enable */
        /* @SVD:field=OC4M,bitOffset=28,bitWidth=4,description="Output compare 4 mode" */
        volatile uint32_t OC4M: 4;  /* Output compare 4 mode */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCMR2_Type;

/* @SVD:register=CCER,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Capture/Compare enable register" */
/* CCER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CC1E,bitOffset=0,bitWidth=1,description="Capture/Compare 1 output enable\n              CC1 channel configured as output:\n              0: Off - OC1 is not active. OC1 level is then function of MOE, OSSI, OSSR, OIS1, OIS1N and CC1NE bits.\n              1: On - OC1 signal is output on the corresponding output pin depending on MOE, OSSI, OSSR, OIS1, OIS1N and CC1NE bits.\n              CC1 channel configured as input: This bit determines if a capture of the counter value can actually be done into the input capture/compare register 1 (CCR1) or not.\n              0: Capture disabled.\n              1: Capture enabled.\n              On channels having a complementary output, this bit is preloaded. If the CCPC bit is set in the CR2 register then the CC1E active bit takes the new value from the preloaded bit only when a Commutation event is generated." */
        volatile uint32_t CC1E: 1;  /* Capture/Compare 1 output enable               CC1 channel co */
        /* @SVD:field=CC1P,bitOffset=1,bitWidth=1,description="Capture/Compare 1 output Polarity.\n              CC1 channel configured as output:\n              0: OC1 active high\n              1: OC1 active low\n              CC1 channel configured as input: CC1NP/CC1P bits select TI1FP1 and TI2FP1 polarity for trigger or capture operations.\n              00: noninverted/rising edge. Circuit is sensitive to TIxFP1 rising edge (capture, trigger in reset, external clock or trigger mode), TIxFP1 is not inverted (trigger in gated mode, encoder mode).\n              01: inverted/falling edge. Circuit is sensitive to TIxFP1 falling edge (capture, trigger in reset, external clock or trigger mode), TIxFP1 is inverted (trigger in gated mode, encoder mode).\n              10: reserved, do not use this configuration.\n              11: noninverted/both edges. Circuit is sensitive to both TIxFP1 rising and falling edges (capture, trigger in reset, external clock or trigger mode), TIxFP1 is not inverted (trigger in gated mode). This configuration must not be used for encoder mode.\n              On channels having a complementary output, this bit is preloaded. If the CCPC bit is set in the CR2 register then the CC1P active bit takes the new value from the preloaded bit only when a Commutation event is generated.\n              This bit as well as other CCxP is not writable as soon as LOCK level 2 or 3 has been programmed." */
        volatile uint32_t CC1P: 1;  /* Capture/Compare 1 output Polarity.               CC1 channel */
        /* @SVD:field=CC1NE,bitOffset=2,bitWidth=1,description="Capture/Compare 1 complementary output enable\n              0: Off - OC1N is not active. OC1N level is then function of MOE, OSSI, OSSR, OIS1, OIS1N and CC1E bits.\n              1: On - OC1N signal is output on the corresponding output pin depending on MOE, OSSI, OSSR, OIS1, OIS1N and CC1E bits.\n              On channels having a complementary output, this bit is preloaded. If the CCPC bit is set in the CR2 register then the CC1NE active bit takes the new value from the preloaded bit only when a Commutation event is generated." */
        volatile uint32_t CC1NE: 1;  /* Capture/Compare 1 complementary output enable                */
        /* @SVD:field=CC1NP,bitOffset=3,bitWidth=1,description="Capture/Compare 1 complementary output polarity\n              CC1 channel configured as output:\n              0: OC1N active high.\n              1: OC1N active low.\n              CC1 channel configured as input:\n              This bit is used in conjunction with CC1P to define the polarity of TI1FP1 and TI2FP1. Refer to CC1P description.\n              On channels having a complementary output, this bit is preloaded. If the CCPC bit is set in the CR2 register then the CC1NP active bit takes the new value from the preloaded bit only when a Commutation event is generated.\n              This bit as well as other CCxNP is not writable as soon as LOCK level 2 or 3 has been programmed and CC1S=00 (channel configured as output)." */
        volatile uint32_t CC1NP: 1;  /* Capture/Compare 1 complementary output polarity              */
        /* @SVD:field=CC2E,bitOffset=4,bitWidth=1,description="Capture/Compare 2 output enable." */
        volatile uint32_t CC2E: 1;  /* Capture/Compare 2 output enable. */
        /* @SVD:field=CC2P,bitOffset=5,bitWidth=1,description="Capture/Compare 2 output Polarity." */
        volatile uint32_t CC2P: 1;  /* Capture/Compare 2 output Polarity. */
        /* @SVD:field=CC2NE,bitOffset=6,bitWidth=1,description="Capture/Compare 2 complementary output enable" */
        volatile uint32_t CC2NE: 1;  /* Capture/Compare 2 complementary output enable */
        /* @SVD:field=CC2NP,bitOffset=7,bitWidth=1,description="Capture/Compare 2 complementary output polarity" */
        volatile uint32_t CC2NP: 1;  /* Capture/Compare 2 complementary output polarity */
        /* @SVD:field=CC3E,bitOffset=8,bitWidth=1,description="Capture/Compare 3 output enable." */
        volatile uint32_t CC3E: 1;  /* Capture/Compare 3 output enable. */
        /* @SVD:field=CC3P,bitOffset=9,bitWidth=1,description="Capture/Compare 3 output Polarity." */
        volatile uint32_t CC3P: 1;  /* Capture/Compare 3 output Polarity. */
        /* @SVD:field=CC3NE,bitOffset=10,bitWidth=1,description="Capture/Compare 3 complementary output enable" */
        volatile uint32_t CC3NE: 1;  /* Capture/Compare 3 complementary output enable */
        /* @SVD:field=CC3NP,bitOffset=11,bitWidth=1,description="Capture/Compare 3 complementary output polarity" */
        volatile uint32_t CC3NP: 1;  /* Capture/Compare 3 complementary output polarity */
        /* @SVD:field=CC4E,bitOffset=12,bitWidth=1,description="Capture/Compare 4 output enable." */
        volatile uint32_t CC4E: 1;  /* Capture/Compare 4 output enable. */
        /* @SVD:field=CC4P,bitOffset=13,bitWidth=1,description="Capture/Compare 4 output Polarity." */
        volatile uint32_t CC4P: 1;  /* Capture/Compare 4 output Polarity. */
        /* @SVD:field=RSVD4,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=CC4NP,bitOffset=15,bitWidth=1,description="Capture/Compare 4 complementary output polarity" */
        volatile uint32_t CC4NP: 1;  /* Capture/Compare 4 complementary output polarity */
        /* @SVD:field=CC5E,bitOffset=16,bitWidth=1,description="Capture/Compare 5 output enable." */
        volatile uint32_t CC5E: 1;  /* Capture/Compare 5 output enable. */
        /* @SVD:field=CC5P,bitOffset=17,bitWidth=1,description="Capture/Compare 5 output Polarity." */
        volatile uint32_t CC5P: 1;  /* Capture/Compare 5 output Polarity. */
        /* @SVD:field=RSVD3,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CC6E,bitOffset=20,bitWidth=1,description="Capture/Compare 6 output enable." */
        volatile uint32_t CC6E: 1;  /* Capture/Compare 6 output enable. */
        /* @SVD:field=CC6P,bitOffset=21,bitWidth=1,description="Capture/Compare 6 output Polarity." */
        volatile uint32_t CC6P: 1;  /* Capture/Compare 6 output Polarity. */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCER_Type;

/* @SVD:register=CNT,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Counter" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=32,description="bit 30 to 0 is the lower bits of counter value\n              bit 31 depends on IUFREMAP in CR1.\n              If UIFREMAP = 1 this bit is a read-only copy of the UIF bit of the ISR register\n              If UIFREMAP = 0 this bit is counter value bit 31" */
        volatile uint32_t CNT: 32;  /* bit 30 to 0 is the lower bits of counter value               */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CNT_Type;

/* @SVD:register=PSC,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Prescaler" */
/* PSC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PSC,bitOffset=0,bitWidth=16,description="Prescaler value\n              The counter clock frequency is fCLK/(PSC+1).\n              PSC contains the value to be loaded in the active prescaler register at each update event (including when the counter is cleared through UG bit of EGR register or through trigger controller when configured in \"reset mode\")." */
        volatile uint32_t PSC: 16;  /* Prescaler value               The counter clock frequency is */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_PSC_Type;

/* @SVD:register=ARR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Auto-reload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=32,description="Auto-reload value\n              ARR is the value to be loaded in the actual auto-reload register." */
        volatile uint32_t ARR: 32;  /* Auto-reload value               ARR is the value to be loade */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_ARR_Type;

/* @SVD:register=RCR,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Repetition counter register" */
/* RCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=16,description="Repetition counter value\n              These bits allow the user to set-up the update rate of the compare registers when preload registers are enable, as well as the update interrupt generation rate, if this interrupt is enable.\n              Each time the REP_CNT related downcounter reaches zero, an update event is generated and it restarts counting from REP value. As REP_CNT is reloaded with REP value only at the repetition update event, any write to the RCR register is not taken in account until the next repetition update event.\n              It means in PWM mode (REP+1) corresponds to the number of PWM periods in edge-aligned mode or the number of half PWM period in center-aligned mode.." */
        volatile uint32_t REP: 16;  /* Repetition counter value               These bits allow the  */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_RCR_Type;

/* @SVD:register=CCR1,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 1" */
/* CCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR1,bitOffset=0,bitWidth=32,description="Capture/Compare 1 value\n              If channel CC1 is configured as output:\n              CCR1 is the value to be loaded in the actual capture/compare 1 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR1 register (bit OC1PE). Else the preload value is copied in the active capture/compare 1 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signaled on OC1 output.\n              If channel CC1is configured as input:\n              CCR1 is the counter value transferred by the last input capture 1 event (IC1)." */
        volatile uint32_t CCR1: 32;  /* Capture/Compare 1 value               If channel CC1 is conf */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCR1_Type;

/* @SVD:register=CCR2,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 2" */
/* CCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR2,bitOffset=0,bitWidth=32,description="Capture/Compare 2 value\n              If channel CC2 is configured as output:\n              CCR2 is the value to be loaded in the actual capture/compare 2 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR1 register (bit OC2PE). Else the preload value is copied in the active capture/compare 2 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signalled on OC2 output.\n              If channel CC2 is configured as input:\n              CCR2 is the counter value transferred by the last input capture 2 event (IC2)." */
        volatile uint32_t CCR2: 32;  /* Capture/Compare 2 value               If channel CC2 is conf */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCR2_Type;

/* @SVD:register=CCR3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 3" */
/* CCR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR3,bitOffset=0,bitWidth=32,description="Capture/Compare value\n              If channel CC3 is configured as output:\n              CCR3 is the value to be loaded in the actual capture/compare 3 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR2 register (bit OC3PE). Else the preload value is copied in the active capture/compare 3 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signalled on OC3 output.\n              If channel CC3is configured as input:\n              CCR3 is the counter value transferred by the last input capture 3 event (IC3)." */
        volatile uint32_t CCR3: 32;  /* Capture/Compare value               If channel CC3 is config */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCR3_Type;

/* @SVD:register=CCR4,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 4" */
/* CCR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR4,bitOffset=0,bitWidth=32,description="Capture/Compare value\n              1. if CC4 channel is configured as output (CC4S bits):\n              CCR4 is the value to be loaded in the actual capture/compare 4 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR2 register (bit OC4PE). Else the preload value is copied in the active capture/compare 4 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signalled on OC4 output.\n              2.  if CC4 channel is configured as input (CC4S bits in CCMR4 register):\n              CCR4 is the counter value transferred by the last input capture 4 event (IC4)." */
        volatile uint32_t CCR4: 32;  /* Capture/Compare value               1. if CC4 channel is con */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCR4_Type;

/* @SVD:register=BDTR,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="TIM break and dead-time register" */
/* BDTR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DTG,bitOffset=0,bitWidth=10,description="Dead-time generator setup\n              This bit-field, together with DTPSC, defines the duration of the dead-time inserted between the complementary outputs.\n              If DTG=0, dead-time is disabled. \n              Example if tCLK=8.33ns (120MHz), dead-time possible values are:\n              16.67ns to 8533.33 ns by 8.33 ns steps if DTPSC=0,\n              266.67ns to 136.53 us by 133.33 ns steps if DTPSC=1\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t DTG: 10;  /* Dead-time generator setup               This bit-field, toge */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
        /* @SVD:field=DTPSC,bitOffset=11,bitWidth=1,description="Dead-time prescaler\n              This bit-field enables dead-time prescaler. \n              0: dead-time is tCLK*(DTG+1) if DTG is not zero\n              1: dead-time is tCLK*(DTG+1)*16 if DTG is not zero\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t DTPSC: 1;  /* Dead-time prescaler               This bit-field enables dea */
        /* @SVD:field=BKE,bitOffset=12,bitWidth=1,description="Break enable\n              This bit enables the complete break protection.\n              0: Break function disabled\n              1: Break function enabled\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BKE: 1;  /* Break enable               This bit enables the complete bre */
        /* @SVD:field=BKP,bitOffset=13,bitWidth=1,description="Break polarity\n              0: Break input BRK is active low\n              1: Break input BRK is active high\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BKP: 1;  /* Break polarity               0: Break input BRK is active lo */
        /* @SVD:field=AOE,bitOffset=14,bitWidth=1,description="Automatic output enable\n              0: MOE can be set only by software\n              1: MOE can be set by software or automatically at the next update event (if none of the break inputs BRK and BRK2 is active)\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t AOE: 1;  /* Automatic output enable               0: MOE can be set only */
        /* @SVD:field=MOE,bitOffset=15,bitWidth=1,description="Main output enable\n              This bit is cleared asynchronously by hardware as soon as one of the break inputs is active (BRK or BRK2). It is set by software or automatically depending on the AOE bit. It is acting only on the channels which are configured in output. \n              0: In response to a break 2 event. OC and OCN outputs are disabled\n              In response to a break event or if MOE is written to 0: OC and OCN outputs are disabled or forced to idle state depending on the OSSI bit.\n              1: OC and OCN outputs are enabled if their respective enable bits are set (CCxE, CCxNE in CCER register)." */
        volatile uint32_t MOE: 1;  /* Main output enable               This bit is cleared asynchr */
        /* @SVD:field=BKF,bitOffset=16,bitWidth=4,description="Break filter\n              This bit-field defines the frequency used to sample BRK input and the length of the digital filter applied to BRK. The digital filter is made of an event counter in which N consecutive events are needed to validate a transition on the output:\n              0000: No filter, BRK acts asynchronously\n              0001: fSAMPLING=fCLK, N=2\n              0010: fSAMPLING=fCLK, N=4\n              0011: fSAMPLING=fCLK, N=8\n              0100: fSAMPLING=fCLK/2, N=6\n              0101: fSAMPLING=fCLK/2, N=8\n              0110: fSAMPLING=fCLK/4, N=6\n              0111: fSAMPLING=fCLK/4, N=8\n              1000: fSAMPLING=fCLK/8, N=6\n              1001: fSAMPLING=fCLK/8, N=8\n              1010: fSAMPLING=fCLK/16, N=5\n              1011: fSAMPLING=fCLK/16, N=6\n              1100: fSAMPLING=fCLK/16, N=8\n              1101: fSAMPLING=fCLK/32, N=5\n              1110: fSAMPLING=fCLK/32, N=6\n              1111: fSAMPLING=fCLK/32, N=8\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BKF: 4;  /* Break filter               This bit-field defines the freque */
        /* @SVD:field=BK2F,bitOffset=20,bitWidth=4,description="Break 2 filter\n              This bit-field defines the frequency used to sample BRK2 input and the length of the digital filter applied to BRK2. The digital filter is made of an event counter in which N consecutive events are needed to validate a transition on the output:\n              0000: No filter, BRK2 acts asynchronously\n              0001: fSAMPLING=fCLK, N=2\n              0010: fSAMPLING=fCLK, N=4\n              0011: fSAMPLING=fCLK, N=8\n              0100: fSAMPLING=fCLK/2, N=6\n              0101: fSAMPLING=fCLK/2, N=8\n              0110: fSAMPLING=fCLK/4, N=6\n              0111: fSAMPLING=fCLK/4, N=8\n              1000: fSAMPLING=fCLK/8, N=6\n              1001: fSAMPLING=fCLK/8, N=8\n              1010: fSAMPLING=fCLK/16, N=5\n              1011: fSAMPLING=fCLK/16, N=6\n              1100: fSAMPLING=fCLK/16, N=8\n              1101: fSAMPLING=fCLK/32, N=5\n              1110: fSAMPLING=fCLK/32, N=6\n              1111: fSAMPLING=fCLK/32, N=8\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BK2F: 4;  /* Break 2 filter               This bit-field defines the freq */
        /* @SVD:field=BK2E,bitOffset=24,bitWidth=1,description="Break 2 enable\n              This bit enables the complete break 2 protection.\n              0: Break2 function disabled\n              1: Break2 function enabled\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BK2E: 1;  /* Break 2 enable               This bit enables the complete b */
        /* @SVD:field=BK2P,bitOffset=25,bitWidth=1,description="BK2P: Break 2 polarity\n              0: Break input BRK2 is active low\n              1: Break input BRK2 is active high\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BK2P: 1;  /* BK2P: Break 2 polarity               0: Break input BRK2 is  */
        /* @SVD:field=BKDSRM,bitOffset=26,bitWidth=1,description="Break Disarm\n              0: Break input BRK is armed\n              1: Break input BRK is disarmed\n              This bit is cleared by hardware when no break source is active.\n              The BKDSRM bit must be set by software to release the bidirectional output control (open-drain output in Hi-Z state) and then be polled it until it is reset by hardware, indicating that the fault condition has disappeared." */
        volatile uint32_t BKDSRM: 1;  /* Break Disarm               0: Break input BRK is armed       */
        /* @SVD:field=BK2DSRM,bitOffset=27,bitWidth=1,description="Break2 Disarm" */
        volatile uint32_t BK2DSRM: 1;  /* Break2 Disarm */
        /* @SVD:field=BKBID,bitOffset=28,bitWidth=1,description="Break Bidirectional\n              0: Break input BRK in input mode\n              1: Break input BRK in bidirectional mode\n              In the bidirectional mode (BKBID bit set to 1), the break input is configured both in input mode and in open drain output mode. Any active break event asserts a low logic level on the Break input to indicate an internal break event to external devices.\n              This bit cannot be modified as long as LOCK level 1 has been programmed (LOCK bits in BDTR register)." */
        volatile uint32_t BKBID: 1;  /* Break Bidirectional               0: Break input BRK in inpu */
        /* @SVD:field=BK2BID,bitOffset=29,bitWidth=1,description="Break2 bidirectional" */
        volatile uint32_t BK2BID: 1;  /* Break2 bidirectional */
        /* @SVD:field=OSSI,bitOffset=30,bitWidth=1,description="Off-state selection for Idle mode\n              This bit is used when MOE=0 due to a break event or by a software write, on channels configured as outputs.\n              0: When inactive, OC/OCN outputs are disabled (the timer releases the output control, imposes a Hi-Z state). \n              1: When inactive, OC/OCN outputs are first forced with their inactive level then forced to their idle level after the deadtime. The timer maintains its control over the output.\n              This bit can not be modified as soon as the LOCK level 2 has been programmed." */
        volatile uint32_t OSSI: 1;  /* Off-state selection for Idle mode               This bit is  */
        /* @SVD:field=OSSR,bitOffset=31,bitWidth=1,description="Off-state selection for Run mode\n              This bit is used when MOE=1 on channels having a complementary output which are configured as outputs. OSSR is not implemented if no complementary output is implemented in the timer.\n              0: When inactive, OC/OCN outputs are disabled (the timer releases the output control, forces a Hi-Z state).\n              1: When inactive, OC/OCN outputs are enabled with their inactive level as soon as CCxE=1 or CCxNE=1 (the output is still controlled by the timer).\n              This bit can not be modified as soon as the LOCK level 2 has been programmed." */
        volatile uint32_t OSSR: 1;  /* Off-state selection for Run mode               This bit is u */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_BDTR_Type;

/* @SVD:register=CCMR3,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="TIM capture/compare mode register 3" */
/* CCMR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD3,bitOffset=0,bitWidth=13,description="" */
        volatile uint32_t RSVD3: 13;
        /* @SVD:field=GC5C1,bitOffset=13,bitWidth=1,description="Group Channel 5 and Channel 1\n              Distortion on Channel 1 output:\n              0: No effect of OC5REF on OC1REFC5\n              1: OC1REFC is the logical AND of OC1REFC and OC5REF\n              This bit can either have immediate effect or be preloaded and taken into account after an update event (if preload feature is selected in TIMxCCMR1)." */
        volatile uint32_t GC5C1: 1;  /* Group Channel 5 and Channel 1               Distortion on Ch */
        /* @SVD:field=GC5C2,bitOffset=14,bitWidth=1,description="Group Channel 5 and Channel 2\n              Distortion on Channel 2 output:\n              0: No effect of OC5REF on OC2REFC\n              1: OC2REFC is the logical AND of OC2REFC and OC5REF\n              This bit can either have immediate effect or be preloaded and taken into account after an update event (if preload feature is selected in TIMxCCMR1)." */
        volatile uint32_t GC5C2: 1;  /* Group Channel 5 and Channel 2               Distortion on Ch */
        /* @SVD:field=GC5C3,bitOffset=15,bitWidth=1,description="Group Channel 5 and Channel 3\n              Distortion on Channel 3 output:\n              0: No effect of OC5REF on OC3REFC\n              1: OC3REFC is the logical AND of OC3REFC and OC5REF\n              This bit can either have immediate effect or be preloaded and taken into account after an update event (if preload feature is selected in TIMxCCMR2)." */
        volatile uint32_t GC5C3: 1;  /* Group Channel 5 and Channel 3               Distortion on Ch */
        /* @SVD:field=OC5CE,bitOffset=16,bitWidth=1,description="Output compare 5 clear enable" */
        volatile uint32_t OC5CE: 1;  /* Output compare 5 clear enable */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=OC5PE,bitOffset=19,bitWidth=1,description="Output compare 5 preload enable" */
        volatile uint32_t OC5PE: 1;  /* Output compare 5 preload enable */
        /* @SVD:field=OC5M,bitOffset=20,bitWidth=4,description="Output compare 5 mode" */
        volatile uint32_t OC5M: 4;  /* Output compare 5 mode */
        /* @SVD:field=OC6CE,bitOffset=24,bitWidth=1,description="Output compare 6 clear enable" */
        volatile uint32_t OC6CE: 1;  /* Output compare 6 clear enable */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=OC6PE,bitOffset=27,bitWidth=1,description="Output compare 6 preload enable" */
        volatile uint32_t OC6PE: 1;  /* Output compare 6 preload enable */
        /* @SVD:field=OC6M,bitOffset=28,bitWidth=4,description="Output compare 6 mode" */
        volatile uint32_t OC6M: 4;  /* Output compare 6 mode */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCMR3_Type;

/* @SVD:register=CCR5,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 5" */
/* CCR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR5,bitOffset=0,bitWidth=32,description="Capture/Compare 5 value\n              CCR5 is the value to be loaded in the actual capture/compare 5 register (preload value). It is loaded permanently if the preload feature is not selected in the CCMR3 register (bit OC5PE). Else the preload value is copied in the active capture/compare 5 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signaled on OC5 output." */
        volatile uint32_t CCR5: 32;  /* Capture/Compare 5 value               CCR5 is the value to b */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCR5_Type;

/* @SVD:register=CCR6,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 6" */
/* CCR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR6,bitOffset=0,bitWidth=32,description="Capture/Compare 6 value\n              CCR6 is the value to be loaded in the actual capture/compare 6 register (preload value). It is loaded permanently if the preload feature is not selected in the CCMR3 register (bit OC6PE). Else the preload value is copied in the active capture/compare 6 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signaled on OC6 output." */
        volatile uint32_t CCR6: 32;  /* Capture/Compare 6 value               CCR6 is the value to b */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_CCR6_Type;

/* @SVD:register=AF1,offset=0x0060,size=32,access=read-write,reset=0x00000001,description="Alternate function option register" */
/* AF1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BKINE,bitOffset=0,bitWidth=1,description="BRK BKIN input enable\n              This bit enables the BKIN input. BKIN input is 'Ored' with the other BRK sources.\n              0: BKIN input disabled\n              1: BKIN input enabled\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BKINE: 1;  /* BRK BKIN input enable               This bit enables the BKI */
        /* @SVD:field=BKCMP1E,bitOffset=1,bitWidth=1,description="BRK LPCOMP output1 enable\n              This bit enables the LPCOMP output1 (if LPCOMP integrated) for the timer's BRK input. LPCOMP output1 is 'ORed' with the other BRK sources.\n              0: LPCOMP output1 disabled\n              1: LPCOMP output1 enabled\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BKCMP1E: 1;  /* BRK LPCOMP output1 enable               This bit enables the */
        /* @SVD:field=BKCMP2E,bitOffset=2,bitWidth=1,description="BRK LPCOMP output2 enable\n              This bit enables the LPCOMP output2 (if LPCOMP integrated) for the timer's BRK input. LPCOMP output2 is 'ORed' with the other BRK sources.\n              0: LPCOMP output2 disabled\n              1: LPCOMP output2 enabled\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BKCMP2E: 1;  /* BRK LPCOMP output2 enable               This bit enables the */
        /* @SVD:field=RSVD3,bitOffset=3,bitWidth=6,description="" */
        volatile uint32_t RSVD3: 6;
        /* @SVD:field=BKINP,bitOffset=9,bitWidth=1,description="BRK BKIN input polarity\n              This bit selects the BKIN input sensitivity. It must be programmed together with the BKP polarity bit.\n              0: BKIN input is active high\n              1: BKIN input is active low\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BKINP: 1;  /* BRK BKIN input polarity               This bit selects the B */
        /* @SVD:field=BKCMP1P,bitOffset=10,bitWidth=1,description="BRK LPCOMP output1 polarity\n              This bit selects the LPCOMP output1 sensitivity (if LPCOMP integrated). It must be programmed together with the BKP polarity bit.\n              0: LPCOMP output1 is active high\n              1: LPCOMP output1 is active low\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BKCMP1P: 1;  /* BRK LPCOMP output1 polarity               This bit selects t */
        /* @SVD:field=BKCMP2P,bitOffset=11,bitWidth=1,description="BRK LPCOMP output2 polarity\n              This bit selects the LPCOMP output2 sensitivity (if LPCOMP integrated). It must be programmed together with the BKP polarity bit.\n              0: LPCOMP output2 is active high\n              1: LPCOMP output2 is active low\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BKCMP2P: 1;  /* BRK LPCOMP output2 polarity               This bit selects t */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=ETRSEL,bitOffset=14,bitWidth=2,description="ETR source selection\n              00: ETR input is connected to I/O\n              01: LPCOMP output1 (if LPCOMP integrated)\n              10: LPCOMP output2 (if LPCOMP integrated)\n              11: ETR input is connected to I/O\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t ETRSEL: 2;  /* ETR source selection               00: ETR input is connecte */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
        /* @SVD:field=LOCK,bitOffset=30,bitWidth=2,description="Lock configuration\n              These bits offer a write protection against software errors.\n              00: LOCK OFF - No bit is write protected.\n              01: LOCK Level 1 = OISx and OISxN bits in CR2 register, BK2BID, BKBID, BK2DSRM, BKDSRM, BK2P, BK2E, BK2F[3:0], BKF[3:0], AOE, BKP, BKE, OSSI, OSSR, DTPSC and DTG bits in BDTR register, AF1 register and AF2 register can no longer be written.\n              10: LOCK Level 2 = LOCK Level 1 + CC Polarity bits (CCxP/CCxNP bits in CCER register, as long as the related channel is configured in output through the CCxS bits) as well as OSSR and OSSI bits can no longer be written.\n              11: LOCK Level 3 = LOCK Level 2 + CC Control bits (OCxM and OCxPE bits in CCMRx registers, as long as the related channel is configured in output through the CCxS bits) can no longer be written.\n              The LOCK bits can be written to non-zero only once after reset." */
        volatile uint32_t LOCK: 2;  /* Lock configuration               These bits offer a write pr */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_AF1_Type;

/* @SVD:register=AF2,offset=0x0064,size=32,access=read-write,reset=0x00000001,description="Alternate function option register 2" */
/* AF2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BK2INE,bitOffset=0,bitWidth=1,description="BRK2 BKIN input enable\n              This bit enables the BKIN2 input. BKIN2 input is 'Ored' with the other BRK2 sources.\n              0: BKIN2 input disabled\n              1: BKIN2 input enabled\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BK2INE: 1;  /* BRK2 BKIN input enable               This bit enables the BK */
        /* @SVD:field=BK2CMP1E,bitOffset=1,bitWidth=1,description="BRK2 LPCOMP output1 enable\n              This bit enables the LPCOMP output1 (if LPCOMP integrated) for the timer's BRK2 input. LPCOMP output1 is 'ORed' with the other BRK2 sources.\n              0: LPCOMP output1 disabled\n              1: LPCOMP output1 enabled\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BK2CMP1E: 1;  /* BRK2 LPCOMP output1 enable               This bit enables th */
        /* @SVD:field=BK2CMP2E,bitOffset=2,bitWidth=1,description="BRK2 LPCOMP output2 enable\n              This bit enables the LPCOMP output2 (if LPCOMP integrated) for the timer's BRK2 input. LPCOMP output2 is 'ORed' with the other BRK2 sources.\n              0: LPCOMP output2 disabled\n              1: LPCOMP output2 enabled\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BK2CMP2E: 1;  /* BRK2 LPCOMP output2 enable               This bit enables th */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=BK2INP,bitOffset=9,bitWidth=1,description="BRK2 BKIN2 input polarity\n              This bit selects the BKIN2 input sensitivity. It must be programmed together with the BK2P polarity bit.\n              0: BKIN2 input is active low\n              1: BKIN2 input is active high\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BK2INP: 1;  /* BRK2 BKIN2 input polarity               This bit selects the */
        /* @SVD:field=BK2CMP1P,bitOffset=10,bitWidth=1,description="BRK2 LPCOMP output1 polarity\n              This bit selects the LPCOMP output1 sensitivity (if LPCOMP integrated). It must be programmed together with the BK2P polarity bit.\n              0: LPCOMP output1 is active high\n              1: LPCOMP output1 is active low\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BK2CMP1P: 1;  /* BRK2 LPCOMP output1 polarity               This bit selects  */
        /* @SVD:field=BK2CMP2P,bitOffset=11,bitWidth=1,description="BRK2 LPCOMP output2 polarity\n              This bit selects the LPCOMP output2 sensitivity (if LPCOMP integrated). It must be programmed together with the BK2P polarity bit.\n              0: LPCOMP output2 is active high\n              1: LPCOMP output2 is active low\n              This bit cannot be modified as long as LOCK level 1 has been programmed." */
        volatile uint32_t BK2CMP2P: 1;  /* BRK2 LPCOMP output2 polarity               This bit selects  */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} ATIM1_AF2_Type;

/* ATIM1 寄存器结构体 */
typedef struct
{
    ATIM1_CR1_Type CR1;  /* 0x0000 - TIM control register 1 */
    ATIM1_CR2_Type CR2;  /* 0x0004 - TIM control register 2 */
    ATIM1_SMCR_Type SMCR;  /* 0x0008 - TIM slave mode control register */
    ATIM1_DIER_Type DIER;  /* 0x000C - TIM DMA/Interrupt enable register */
    ATIM1_SR_Type SR;  /* 0x0010 - TIM status register */
    ATIM1_EGR_Type EGR;  /* 0x0014 - Event generation register */
    ATIM1_CCMR1_Type CCMR1;  /* 0x0018 - TIM capture/compare mode register 1 */
    ATIM1_CCMR2_Type CCMR2;  /* 0x001C - TIM capture/compare mode register 2 */
    ATIM1_CCER_Type CCER;  /* 0x0020 - Capture/Compare enable register */
    ATIM1_CNT_Type CNT;  /* 0x0024 - Counter */
    ATIM1_PSC_Type PSC;  /* 0x0028 - Prescaler */
    ATIM1_ARR_Type ARR;  /* 0x002C - Auto-reload register */
    ATIM1_RCR_Type RCR;  /* 0x0030 - Repetition counter register */
    ATIM1_CCR1_Type CCR1;  /* 0x0034 - Capture/Compare register 1 */
    ATIM1_CCR2_Type CCR2;  /* 0x0038 - Capture/Compare register 2 */
    ATIM1_CCR3_Type CCR3;  /* 0x003C - Capture/Compare register 3 */
    ATIM1_CCR4_Type CCR4;  /* 0x0040 - Capture/Compare register 4 */
    ATIM1_BDTR_Type BDTR;  /* 0x0044 - TIM break and dead-time register */
    /* @SVD:register=RSVD1,offset=0x0048,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0048 - RSVD1 */
    volatile uint32_t RESERVED19[2];  /* 0x004C - 0x0053 */
    ATIM1_CCMR3_Type CCMR3;  /* 0x0054 - TIM capture/compare mode register 3 */
    ATIM1_CCR5_Type CCR5;  /* 0x0058 - Capture/Compare register 5 */
    ATIM1_CCR6_Type CCR6;  /* 0x005C - Capture/Compare register 6 */
    ATIM1_AF1_Type AF1;  /* 0x0060 - Alternate function option register */
    ATIM1_AF2_Type AF2;  /* 0x0064 - Alternate function option register 2 */
} ATIM1_TypeDef;

/* ============================================================================ */
/* AUDPRC 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=AUDPRC,base=0x50005000,description="",groupName=AUDPRC */

#define AUDPRC_BASE                                        (0x50005000UL)

/* @SVD:register=ID,offset=0x0000,size=32,access=read-write,reset=0x000A0000,description="ID" */
/* ID 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REV,bitOffset=0,bitWidth=32,description="revision id" */
        volatile uint32_t REV: 32;  /* revision id */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_ID_Type;

/* @SVD:register=CFG,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="CFG" */
/* CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="audprc enable" */
        volatile uint32_t ENABLE: 1;  /* audprc enable */
        /* @SVD:field=SRESET,bitOffset=1,bitWidth=1,description="audprc software reset, high active" */
        volatile uint32_t SRESET: 1;  /* audprc software reset, high active */
        /* @SVD:field=DAC_PATH_FLUSH,bitOffset=2,bitWidth=1,description="dac path fifo flush, high active" */
        volatile uint32_t DAC_PATH_FLUSH: 1;  /* dac path fifo flush, high active */
        /* @SVD:field=ADC_PATH_FLUSH,bitOffset=3,bitWidth=1,description="adc path fifo flush, high active" */
        volatile uint32_t ADC_PATH_FLUSH: 1;  /* adc path fifo flush, high active */
        /* @SVD:field=DAC_PATH_SRESET,bitOffset=4,bitWidth=1,description="dac path software reset, high active" */
        volatile uint32_t DAC_PATH_SRESET: 1;  /* dac path software reset, high active */
        /* @SVD:field=ADC_PATH_SRESET,bitOffset=5,bitWidth=1,description="adc path software reset, high active" */
        volatile uint32_t ADC_PATH_SRESET: 1;  /* adc path software reset, high active */
        /* @SVD:field=DAC_PATH_EN,bitOffset=6,bitWidth=1,description="dac path enable" */
        volatile uint32_t DAC_PATH_EN: 1;  /* dac path enable */
        /* @SVD:field=ADC_PATH_EN,bitOffset=7,bitWidth=1,description="adc path enable" */
        volatile uint32_t ADC_PATH_EN: 1;  /* adc path enable */
        /* @SVD:field=AUTO_GATE_EN,bitOffset=8,bitWidth=1,description="auto clock gating enable, high active" */
        volatile uint32_t AUTO_GATE_EN: 1;  /* auto clock gating enable, high active */
        /* @SVD:field=STB_CLK_SEL,bitOffset=9,bitWidth=1,description="audio strobe clock select\n              0: use xtal clock to generate strobe\n              1: use pll clock to generate strobe" */
        volatile uint32_t STB_CLK_SEL: 1;  /* audio strobe clock select               0: use xtal clock to */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=AUDCLK_DIV,bitOffset=16,bitWidth=4,description="audprc clock divider, 0 and 1 means divide by 1" */
        volatile uint32_t AUDCLK_DIV: 4;  /* audprc clock divider, 0 and 1 means divide by 1 */
        /* @SVD:field=AUDCLK_DIV_UPDATE,bitOffset=20,bitWidth=1,description="audprc clock divider update, write 1 to update" */
        volatile uint32_t AUDCLK_DIV_UPDATE: 1;  /* audprc clock divider update, write 1 to update */
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_CFG_Type;

/* @SVD:register=STB,offset=0x0008,size=32,access=read-write,reset=0x00010001,description="STB" */
/* STB 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DAC_DIV,bitOffset=0,bitWidth=16,description="dac strobe divider" */
        volatile uint32_t DAC_DIV: 16;  /* dac strobe divider */
        /* @SVD:field=ADC_DIV,bitOffset=16,bitWidth=16,description="adc strobe divider" */
        volatile uint32_t ADC_DIV: 16;  /* adc strobe divider */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_STB_Type;

/* @SVD:register=IRQ,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="IRQ" */
/* IRQ 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TX0_FIFO_OF,bitOffset=0,bitWidth=1,description="tx channel 0 fifo overflow, write 1 to clear" */
        volatile uint32_t TX0_FIFO_OF: 1;  /* tx channel 0 fifo overflow, write 1 to clear */
        /* @SVD:field=TX1_FIFO_OF,bitOffset=1,bitWidth=1,description="tx channel 1 fifo overflow, write 1 to clear" */
        volatile uint32_t TX1_FIFO_OF: 1;  /* tx channel 1 fifo overflow, write 1 to clear */
        /* @SVD:field=TX2_FIFO_OF,bitOffset=2,bitWidth=1,description="tx channel 2 fifo overflow, write 1 to clear" */
        volatile uint32_t TX2_FIFO_OF: 1;  /* tx channel 2 fifo overflow, write 1 to clear */
        /* @SVD:field=TX3_FIFO_OF,bitOffset=3,bitWidth=1,description="tx channel 3 fifo overflow, write 1 to clear" */
        volatile uint32_t TX3_FIFO_OF: 1;  /* tx channel 3 fifo overflow, write 1 to clear */
        /* @SVD:field=RX0_FIFO_UF,bitOffset=4,bitWidth=1,description="rx channel 0 fifo underflow, write 1 to clear" */
        volatile uint32_t RX0_FIFO_UF: 1;  /* rx channel 0 fifo underflow, write 1 to clear */
        /* @SVD:field=RX1_FIFO_UF,bitOffset=5,bitWidth=1,description="rx channel 1 fifo underflow, write 1 to clear" */
        volatile uint32_t RX1_FIFO_UF: 1;  /* rx channel 1 fifo underflow, write 1 to clear */
        /* @SVD:field=TX_OUT_FIFO_UF,bitOffset=6,bitWidth=1,description="tx output fifo underflow, write 1 to clear" */
        volatile uint32_t TX_OUT_FIFO_UF: 1;  /* tx output fifo underflow, write 1 to clear */
        /* @SVD:field=RX_IN_FIFO_OF,bitOffset=7,bitWidth=1,description="rx input fifo overflow, write 1 to clear" */
        volatile uint32_t RX_IN_FIFO_OF: 1;  /* rx input fifo overflow, write 1 to clear */
        /* @SVD:field=TX_OUT0_FIFO_UF,bitOffset=8,bitWidth=1,description="tx_out channel 0 fifo underflow, write 1 to clear" */
        volatile uint32_t TX_OUT0_FIFO_UF: 1;  /* tx_out channel 0 fifo underflow, write 1 to clear */
        /* @SVD:field=TX_OUT1_FIFO_UF,bitOffset=9,bitWidth=1,description="tx_out channel 1 fifo underflow, write 1 to clear" */
        volatile uint32_t TX_OUT1_FIFO_UF: 1;  /* tx_out channel 1 fifo underflow, write 1 to clear */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=TX0_FIFO_OF_MASK,bitOffset=16,bitWidth=1,description="tx channel 0 fifo overflow mask, 0: mask the interrupt" */
        volatile uint32_t TX0_FIFO_OF_MASK: 1;  /* tx channel 0 fifo overflow mask, 0: mask the interrupt */
        /* @SVD:field=TX1_FIFO_OF_MASK,bitOffset=17,bitWidth=1,description="tx channel 1 fifo overflow mask, 0: mask the interrupt" */
        volatile uint32_t TX1_FIFO_OF_MASK: 1;  /* tx channel 1 fifo overflow mask, 0: mask the interrupt */
        /* @SVD:field=TX2_FIFO_OF_MASK,bitOffset=18,bitWidth=1,description="tx channel 2 fifo overflow mask, 0: mask the interrupt" */
        volatile uint32_t TX2_FIFO_OF_MASK: 1;  /* tx channel 2 fifo overflow mask, 0: mask the interrupt */
        /* @SVD:field=TX3_FIFO_OF_MASK,bitOffset=19,bitWidth=1,description="tx channel 3 fifo overflow mask, 0: mask the interrupt" */
        volatile uint32_t TX3_FIFO_OF_MASK: 1;  /* tx channel 3 fifo overflow mask, 0: mask the interrupt */
        /* @SVD:field=RX0_FIFO_UF_MASK,bitOffset=20,bitWidth=1,description="rx channel 0 fifo underflow mask, 0: mask the interrupt" */
        volatile uint32_t RX0_FIFO_UF_MASK: 1;  /* rx channel 0 fifo underflow mask, 0: mask the interrupt */
        /* @SVD:field=RX1_FIFO_UF_MASK,bitOffset=21,bitWidth=1,description="rx channel 1 fifo underflow mask, 0: mask the interrupt" */
        volatile uint32_t RX1_FIFO_UF_MASK: 1;  /* rx channel 1 fifo underflow mask, 0: mask the interrupt */
        /* @SVD:field=TX_OUT_FIFO_UF_MASK,bitOffset=22,bitWidth=1,description="tx output fifo underflow mask, 0: mask the interrupt" */
        volatile uint32_t TX_OUT_FIFO_UF_MASK: 1;  /* tx output fifo underflow mask, 0: mask the interrupt */
        /* @SVD:field=RX_IN_FIFO_OF_MASK,bitOffset=23,bitWidth=1,description="rx input fifo overflow mask, 0: mask the interrupt" */
        volatile uint32_t RX_IN_FIFO_OF_MASK: 1;  /* rx input fifo overflow mask, 0: mask the interrupt */
        /* @SVD:field=TX_OUT0_FIFO_UF_MASK,bitOffset=24,bitWidth=1,description="tx_out channel 0 fifo underflow mask, 0: mask the interrupt" */
        volatile uint32_t TX_OUT0_FIFO_UF_MASK: 1;  /* tx_out channel 0 fifo underflow mask, 0: mask the interrupt */
        /* @SVD:field=TX_OUT1_FIFO_UF_MASK,bitOffset=25,bitWidth=1,description="tx_out channel 1 fifo underflow mask, 0: mask the interrupt" */
        volatile uint32_t TX_OUT1_FIFO_UF_MASK: 1;  /* tx_out channel 1 fifo underflow mask, 0: mask the interrupt */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_IRQ_Type;

/* @SVD:register=TX_CH0_CFG,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="TX_CH0_CFG" */
/* TX_CH0_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="tx channel 0 enable" */
        volatile uint32_t ENABLE: 1;  /* tx channel 0 enable */
        /* @SVD:field=FORMAT,bitOffset=1,bitWidth=1,description="tx format\n              0: 16-bit mode\n              1: 24-bit mode" */
        volatile uint32_t FORMAT: 1;  /* tx format               0: 16-bit mode               1: 24-b */
        /* @SVD:field=MODE,bitOffset=2,bitWidth=1,description="tx mode\n              1'h0: mono mode\n              1'h1: stereo mode\n              This bit is only used for 16-bit mode, in 24-bit mode, channel can only be set in mono mode.\n              In 16-bit stereo mode, tx channel 1 is not working, both left and right audio data comes from channel 0." */
        volatile uint32_t MODE: 1;  /* tx mode               1'h0: mono mode               1'h1: st */
        /* @SVD:field=DMA_MSK,bitOffset=3,bitWidth=1,description="1: mask the dma request for tx ch0" */
        volatile uint32_t DMA_MSK: 1;  /* 1: mask the dma request for tx ch0 */
        /* @SVD:field=FIFO_CNT,bitOffset=4,bitWidth=4,description="tx fifo counter" */
        volatile uint32_t FIFO_CNT: 4;  /* tx fifo counter */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_CH0_CFG_Type;

/* @SVD:register=TX_CH0_ENTRY,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="TX_CH0_ENTRY" */
/* TX_CH0_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="tx channel 0 data entry" */
        volatile uint32_t DATA: 32;  /* tx channel 0 data entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_CH0_ENTRY_Type;

/* @SVD:register=TX_CH1_CFG,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="TX_CH1_CFG" */
/* TX_CH1_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="tx channel 0 enable" */
        volatile uint32_t ENABLE: 1;  /* tx channel 0 enable */
        /* @SVD:field=FORMAT,bitOffset=1,bitWidth=1,description="tx format\n              0: 16-bit mode\n              1: 24-bit mode" */
        volatile uint32_t FORMAT: 1;  /* tx format               0: 16-bit mode               1: 24-b */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=DMA_MSK,bitOffset=3,bitWidth=1,description="1: mask the dma request for tx ch1" */
        volatile uint32_t DMA_MSK: 1;  /* 1: mask the dma request for tx ch1 */
        /* @SVD:field=FIFO_CNT,bitOffset=4,bitWidth=4,description="tx fifo counter" */
        volatile uint32_t FIFO_CNT: 4;  /* tx fifo counter */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_CH1_CFG_Type;

/* @SVD:register=TX_CH1_ENTRY,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="TX_CH1_ENTRY" */
/* TX_CH1_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="tx channel 1 data entry" */
        volatile uint32_t DATA: 32;  /* tx channel 1 data entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_CH1_ENTRY_Type;

/* @SVD:register=TX_CH2_CFG,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="TX_CH2_CFG" */
/* TX_CH2_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="tx channel 0 enable" */
        volatile uint32_t ENABLE: 1;  /* tx channel 0 enable */
        /* @SVD:field=FORMAT,bitOffset=1,bitWidth=1,description="tx format\n              0: 16-bit mode\n              1: 24-bit mode" */
        volatile uint32_t FORMAT: 1;  /* tx format               0: 16-bit mode               1: 24-b */
        /* @SVD:field=MODE,bitOffset=2,bitWidth=1,description="tx mode\n              1'h0: mono mode\n              1'h1: stereo mode\n              This bit is only used for 16-bit mode, in 24-bit mode, channel can only be set in mono mode.\n              In 16-bit stereo mode, tx channel 3 is not working, both left and right audio data comes from channel 2." */
        volatile uint32_t MODE: 1;  /* tx mode               1'h0: mono mode               1'h1: st */
        /* @SVD:field=DMA_MSK,bitOffset=3,bitWidth=1,description="1: mask the dma request for tx ch2" */
        volatile uint32_t DMA_MSK: 1;  /* 1: mask the dma request for tx ch2 */
        /* @SVD:field=FIFO_CNT,bitOffset=4,bitWidth=4,description="tx fifo counter" */
        volatile uint32_t FIFO_CNT: 4;  /* tx fifo counter */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_CH2_CFG_Type;

/* @SVD:register=TX_CH2_ENTRY,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="TX_CH2_ENTRY" */
/* TX_CH2_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="tx channel 2 data entry" */
        volatile uint32_t DATA: 32;  /* tx channel 2 data entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_CH2_ENTRY_Type;

/* @SVD:register=TX_CH3_CFG,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="TX_CH3_CFG" */
/* TX_CH3_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="tx channel 0 enable" */
        volatile uint32_t ENABLE: 1;  /* tx channel 0 enable */
        /* @SVD:field=FORMAT,bitOffset=1,bitWidth=1,description="tx format\n              0: 16-bit mode\n              1: 24-bit mode" */
        volatile uint32_t FORMAT: 1;  /* tx format               0: 16-bit mode               1: 24-b */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=DMA_MSK,bitOffset=3,bitWidth=1,description="1: mask the dma request for tx ch3" */
        volatile uint32_t DMA_MSK: 1;  /* 1: mask the dma request for tx ch3 */
        /* @SVD:field=FIFO_CNT,bitOffset=4,bitWidth=4,description="tx fifo counter" */
        volatile uint32_t FIFO_CNT: 4;  /* tx fifo counter */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_CH3_CFG_Type;

/* @SVD:register=TX_CH3_ENTRY,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="TX_CH3_ENTRY" */
/* TX_CH3_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="tx channel 3 data entry" */
        volatile uint32_t DATA: 32;  /* tx channel 3 data entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_CH3_ENTRY_Type;

/* @SVD:register=RX_CH0_CFG,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="RX_CH0_CFG" */
/* RX_CH0_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="rx channel 0 enable" */
        volatile uint32_t ENABLE: 1;  /* rx channel 0 enable */
        /* @SVD:field=FORMAT,bitOffset=1,bitWidth=1,description="rx format\n              0: 16-bit mode\n              1: 24-bit mode" */
        volatile uint32_t FORMAT: 1;  /* rx format               0: 16-bit mode               1: 24-b */
        /* @SVD:field=MODE,bitOffset=2,bitWidth=1,description="rx mode\n              1'h0: mono mode\n              1'h1: stereo mode\n              This bit is only used for 16-bit mode, in 24-bit mode, channel can only be set in mono mode.\n              In 16-bit stereo mode, rx channel 1 is not working, both left and right audio data comes from channel 0." */
        volatile uint32_t MODE: 1;  /* rx mode               1'h0: mono mode               1'h1: st */
        /* @SVD:field=DMA_MSK,bitOffset=3,bitWidth=1,description="1: mask the dma request for rx ch0" */
        volatile uint32_t DMA_MSK: 1;  /* 1: mask the dma request for rx ch0 */
        /* @SVD:field=FIFO_CNT,bitOffset=4,bitWidth=4,description="rx fifo counter" */
        volatile uint32_t FIFO_CNT: 4;  /* rx fifo counter */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_RX_CH0_CFG_Type;

/* @SVD:register=RX_CH0_ENTRY,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="RX_CH0_ENTRY" */
/* RX_CH0_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="rx channel 0 data entry" */
        volatile uint32_t DATA: 32;  /* rx channel 0 data entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_RX_CH0_ENTRY_Type;

/* @SVD:register=RX_CH1_CFG,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="RX_CH1_CFG" */
/* RX_CH1_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="rx channel 1 enable" */
        volatile uint32_t ENABLE: 1;  /* rx channel 1 enable */
        /* @SVD:field=FORMAT,bitOffset=1,bitWidth=1,description="rx format\n              0: 16-bit mode\n              1: 24-bit mode" */
        volatile uint32_t FORMAT: 1;  /* rx format               0: 16-bit mode               1: 24-b */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=DMA_MSK,bitOffset=3,bitWidth=1,description="1: mask the dma request for rx ch1" */
        volatile uint32_t DMA_MSK: 1;  /* 1: mask the dma request for rx ch1 */
        /* @SVD:field=FIFO_CNT,bitOffset=4,bitWidth=4,description="rx fifo counter" */
        volatile uint32_t FIFO_CNT: 4;  /* rx fifo counter */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_RX_CH1_CFG_Type;

/* @SVD:register=RX_CH1_ENTRY,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="RX_CH1_ENTRY" */
/* RX_CH1_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="rx channel 1 data entry" */
        volatile uint32_t DATA: 32;  /* rx channel 1 data entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_RX_CH1_ENTRY_Type;

/* @SVD:register=TX_OUT_CH0_CFG,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="TX_OUT_CH0_CFG" */
/* TX_OUT_CH0_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="tx out channel 0 enable" */
        volatile uint32_t ENABLE: 1;  /* tx out channel 0 enable */
        /* @SVD:field=FORMAT,bitOffset=1,bitWidth=1,description="tx out format\n              0: 16-bit mode\n              1: 24-bit mode" */
        volatile uint32_t FORMAT: 1;  /* tx out format               0: 16-bit mode               1:  */
        /* @SVD:field=MODE,bitOffset=2,bitWidth=1,description="tx out mode\n              1'h0: mono mode\n              1'h1: stereo mode\n              This bit is only used for 16-bit mode, in 24-bit mode, channel can only be set in mono mode.\n              In 16-bit stereo mode, rx channel 1 is not working, both left and right audio data comes from channel 0." */
        volatile uint32_t MODE: 1;  /* tx out mode               1'h0: mono mode               1'h1 */
        /* @SVD:field=DMA_MSK,bitOffset=3,bitWidth=1,description="1: mask the dma request for tx out ch0" */
        volatile uint32_t DMA_MSK: 1;  /* 1: mask the dma request for tx out ch0 */
        /* @SVD:field=FIFO_CNT,bitOffset=4,bitWidth=4,description="tx out fifo counter" */
        volatile uint32_t FIFO_CNT: 4;  /* tx out fifo counter */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_OUT_CH0_CFG_Type;

/* @SVD:register=TX_OUT_CH0_ENTRY,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="TX_OUT_CH0_ENTRY" */
/* TX_OUT_CH0_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="tx out channel 0 data entry" */
        volatile uint32_t DATA: 32;  /* tx out channel 0 data entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_OUT_CH0_ENTRY_Type;

/* @SVD:register=TX_OUT_CH1_CFG,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="TX_OUT_CH1_CFG" */
/* TX_OUT_CH1_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="tx out channel 1 enable" */
        volatile uint32_t ENABLE: 1;  /* tx out channel 1 enable */
        /* @SVD:field=FORMAT,bitOffset=1,bitWidth=1,description="tx out format\n              0: 16-bit mode\n              1: 24-bit mode" */
        volatile uint32_t FORMAT: 1;  /* tx out format               0: 16-bit mode               1:  */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=DMA_MSK,bitOffset=3,bitWidth=1,description="1: mask the dma request for tx out ch1" */
        volatile uint32_t DMA_MSK: 1;  /* 1: mask the dma request for tx out ch1 */
        /* @SVD:field=FIFO_CNT,bitOffset=4,bitWidth=4,description="tx out fifo counter" */
        volatile uint32_t FIFO_CNT: 4;  /* tx out fifo counter */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_OUT_CH1_CFG_Type;

/* @SVD:register=TX_OUT_CH1_ENTRY,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="TX_OUT_CH1_ENTRY" */
/* TX_OUT_CH1_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="tx out channel 1 data entry" */
        volatile uint32_t DATA: 32;  /* tx out channel 1 data entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_TX_OUT_CH1_ENTRY_Type;

/* @SVD:register=DAC_PATH_CFG0,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="DAC_PATH_CFG0" */
/* DAC_PATH_CFG0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ROUGH_VOL_L,bitOffset=0,bitWidth=4,description="dac mixer left channel rough volume control\n              range from -36dB to 54dB\n              step is 6dB\n              4'h0: -36dB\n              4'h1: -30dB\n              ......\n              4'h6: 0dB\n              ......\n              4'he: 48dB\n              4'hf: 54dB" */
        volatile uint32_t ROUGH_VOL_L: 4;  /* dac mixer left channel rough volume control               ra */
        /* @SVD:field=FINE_VOL_L,bitOffset=4,bitWidth=4,description="dac mixer left channel fine volume control\n              range from 0dB to 6dB\n              step is 0.5dB\n              4'h0: 0dB\n              4'h1: 0.5dB\n              ......\n              4'hb: 5.5dB\n              4'hc, 4'hd, 4'he, 4'hf: mute" */
        volatile uint32_t FINE_VOL_L: 4;  /* dac mixer left channel fine volume control               ran */
        /* @SVD:field=ROUGH_VOL_R,bitOffset=8,bitWidth=4,description="dac mixer right channel rough volume control\n              range from -36dB to 54dB\n              step is 6dB\n              4'h0: -36dB\n              4'h1: -30dB\n              ......\n              4'h6: 0dB\n              ......\n              4'he: 48dB\n              4'hf: 54dB" */
        volatile uint32_t ROUGH_VOL_R: 4;  /* dac mixer right channel rough volume control               r */
        /* @SVD:field=FINE_VOL_R,bitOffset=12,bitWidth=4,description="dac mixer right channel  fine volume control\n              range from 0dB to 6dB\n              step is 0.5dB\n              4'h0: 0dB\n              4'h1: 0.5dB\n              ......\n              4'hb: 5.5dB\n              4'hc, 4'hd, 4'he, 4'hf: mute" */
        volatile uint32_t FINE_VOL_R: 4;  /* dac mixer right channel  fine volume control               r */
        /* @SVD:field=MIXLSRC0,bitOffset=16,bitWidth=3,description="dac mixer left channel input source0 select\n              3'h0:tx ch0\n              3'h1:tx ch1\n              3'h2:tx ch2\n              3'h3:tx ch3\n              3'h4:mute\n              other: mute" */
        volatile uint32_t MIXLSRC0: 3;  /* dac mixer left channel input source0 select               3' */
        /* @SVD:field=MIXLSRC1,bitOffset=19,bitWidth=3,description="dac mixer left channel input source1 select\n              3'h0:tx ch0\n              3'h1:tx ch1\n              3'h2:tx ch2\n              3'h3:tx ch3\n              3'h4:mute\n              other: mute" */
        volatile uint32_t MIXLSRC1: 3;  /* dac mixer left channel input source1 select               3' */
        /* @SVD:field=MIXRSRC0,bitOffset=22,bitWidth=3,description="dac mixer right channel input source0 select\n              3'h0:tx ch0\n              3'h1:tx ch1\n              3'h2:tx ch2\n              3'h3:tx ch3\n              3'h4:mute\n              other: mute" */
        volatile uint32_t MIXRSRC0: 3;  /* dac mixer right channel input source0 select               3 */
        /* @SVD:field=MIXRSRC1,bitOffset=25,bitWidth=3,description="dac mixer right channel input source0 select\n              3'h0:tx ch0\n              3'h1:tx ch1\n              3'h2:tx ch2\n              3'h3:tx ch3\n              3'h4:mute\n              other: mute" */
        volatile uint32_t MIXRSRC1: 3;  /* dac mixer right channel input source0 select               3 */
        /* @SVD:field=DST_SEL,bitOffset=28,bitWidth=2,description="dac path destination select\n              2'h0: select audio codec\n              2'h1: select external interface\n              2'h2: select apb interface\n              2'h3: reserved" */
        volatile uint32_t DST_SEL: 2;  /* dac path destination select               2'h0: select audio */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_PATH_CFG0_Type;

/* @SVD:register=DAC_PATH_CFG1,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="DAC_PATH_CFG1" */
/* DAC_PATH_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MUXLSRC0,bitOffset=0,bitWidth=3,description="dac mux left channel input source0 select\n              3'h0:tx ch0\n              3'h1:tx ch1\n              3'h2:rx ch0\n              3'h3:rx ch1\n              3'h4:mute\n              other: mute" */
        volatile uint32_t MUXLSRC0: 3;  /* dac mux left channel input source0 select               3'h0 */
        /* @SVD:field=MUXLSRC1,bitOffset=3,bitWidth=3,description="dac mux left channel input source1 select\n              3'h0:tx ch0\n              3'h1:tx ch1\n              3'h2:rx ch0\n              3'h3:rx ch1\n              3'h4:mute\n              other: mute" */
        volatile uint32_t MUXLSRC1: 3;  /* dac mux left channel input source1 select               3'h0 */
        /* @SVD:field=MUXRSRC0,bitOffset=6,bitWidth=3,description="dac mux right channel input source0 select\n              3'h0:tx ch0\n              3'h1:tx ch1\n              3'h2:rx ch0\n              3'h3:rx ch1\n              3'h4:mute\n              other: mute" */
        volatile uint32_t MUXRSRC0: 3;  /* dac mux right channel input source0 select               3'h */
        /* @SVD:field=MUXRSRC1,bitOffset=9,bitWidth=3,description="dac mux right channel input source0 select\n              3'h0:tx ch0\n              3'h1:tx ch1\n              3'h2:rx ch0\n              3'h3:rx ch1\n              3'h4:mute\n              other: mute" */
        volatile uint32_t MUXRSRC1: 3;  /* dac mux right channel input source0 select               3'h */
        /* @SVD:field=EQ_CH_EN,bitOffset=12,bitWidth=2,description="equalizer channel enable\n              2'b11: enable both channel\n              2'b10: enable right chanel only\n              2'b01: enable left channel only\n              2'b00: bypass equalizer" */
        volatile uint32_t EQ_CH_EN: 2;  /* equalizer channel enable               2'b11: enable both ch */
        /* @SVD:field=EQ_STAGE,bitOffset=14,bitWidth=4,description="set equalizer stage, max is 10." */
        volatile uint32_t EQ_STAGE: 4;  /* set equalizer stage, max is 10. */
        /* @SVD:field=EQ_CLR_DONE,bitOffset=18,bitWidth=1,description="equalizer clear done flag" */
        volatile uint32_t EQ_CLR_DONE: 1;  /* equalizer clear done flag */
        /* @SVD:field=EQ_CLR,bitOffset=19,bitWidth=1,description="equalizer clear request" */
        volatile uint32_t EQ_CLR: 1;  /* equalizer clear request */
        /* @SVD:field=SRC_CH_EN,bitOffset=20,bitWidth=2,description="source rate converter channel enable" */
        volatile uint32_t SRC_CH_EN: 2;  /* source rate converter channel enable */
        /* @SVD:field=SRC_HBF1_EN,bitOffset=22,bitWidth=1,description="1st stage hbf enable" */
        volatile uint32_t SRC_HBF1_EN: 1;  /* 1st stage hbf enable */
        /* @SVD:field=SRC_HBF1_MODE,bitOffset=23,bitWidth=1,description="1st stage hbf mode:\n              0: upsampling\n              1: downsampling" */
        volatile uint32_t SRC_HBF1_MODE: 1;  /* 1st stage hbf mode:               0: upsampling              */
        /* @SVD:field=SRC_HBF2_EN,bitOffset=24,bitWidth=1,description="2nd stage hbf enable" */
        volatile uint32_t SRC_HBF2_EN: 1;  /* 2nd stage hbf enable */
        /* @SVD:field=SRC_HBF2_MODE,bitOffset=25,bitWidth=1,description="2nd stage hbf mode:\n              0: upsampling\n              1: downsampling" */
        volatile uint32_t SRC_HBF2_MODE: 1;  /* 2nd stage hbf mode:               0: upsampling              */
        /* @SVD:field=SRC_HBF3_EN,bitOffset=26,bitWidth=1,description="3rd stage hbf enable" */
        volatile uint32_t SRC_HBF3_EN: 1;  /* 3rd stage hbf enable */
        /* @SVD:field=SRC_HBF3_MODE,bitOffset=27,bitWidth=1,description="3rd stage hbf mode:\n              0: upsampling\n              1: downsampling" */
        volatile uint32_t SRC_HBF3_MODE: 1;  /* 3rd stage hbf mode:               0: upsampling              */
        /* @SVD:field=SRC_CH_CLR_DONE,bitOffset=28,bitWidth=2,description="src channel internal data clear done" */
        volatile uint32_t SRC_CH_CLR_DONE: 2;  /* src channel internal data clear done */
        /* @SVD:field=SRC_CH_CLR,bitOffset=30,bitWidth=2,description="clear src channal internal data" */
        volatile uint32_t SRC_CH_CLR: 2;  /* clear src channal internal data */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_PATH_CFG1_Type;

/* @SVD:register=DAC_PATH_CFG2,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="DAC_PATH_CFG2" */
/* DAC_PATH_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SINC_RATIO,bitOffset=0,bitWidth=31,description="sinc filter ratio, s31.30 format. Range from 0~2" */
        volatile uint32_t SINC_RATIO: 31;  /* sinc filter ratio, s31.30 format. Range from 0~2 */
        /* @SVD:field=SRC_SINC_EN,bitOffset=31,bitWidth=1,description="sinc filter enable" */
        volatile uint32_t SRC_SINC_EN: 1;  /* sinc filter enable */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_PATH_CFG2_Type;

/* @SVD:register=DAC_PATH_CFG3,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="DAC_PATH_CFG3" */
/* DAC_PATH_CFG3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RAMP_EN_L,bitOffset=0,bitWidth=1,description="dac mixer left channel volume ramp enable" */
        volatile uint32_t RAMP_EN_L: 1;  /* dac mixer left channel volume ramp enable */
        /* @SVD:field=RAMP_MODE_L,bitOffset=1,bitWidth=1,description="dac mixer left channel volume ramp mode:\n              1: slowly ramp to target volume. Step is 0.5db\n              0: directly ramp to target volume." */
        volatile uint32_t RAMP_MODE_L: 1;  /* dac mixer left channel volume ramp mode:               1: sl */
        /* @SVD:field=ZERO_ADJUST_EN_L,bitOffset=2,bitWidth=1,description="dac mixer left channel volume adjustment during 0 volume cross enable" */
        volatile uint32_t ZERO_ADJUST_EN_L: 1;  /* dac mixer left channel volume adjustment during 0 volume cro */
        /* @SVD:field=RAMP_INTERVAL_L,bitOffset=3,bitWidth=4,description="dac mixer left channel volume ramp interval." */
        volatile uint32_t RAMP_INTERVAL_L: 4;  /* dac mixer left channel volume ramp interval. */
        /* @SVD:field=RAMP_STAT_L,bitOffset=7,bitWidth=2,description="dac mixer left channel ramp module status" */
        volatile uint32_t RAMP_STAT_L: 2;  /* dac mixer left channel ramp module status */
        /* @SVD:field=RAMP_EN_R,bitOffset=9,bitWidth=1,description="dac mixer right channel volume ramp enable" */
        volatile uint32_t RAMP_EN_R: 1;  /* dac mixer right channel volume ramp enable */
        /* @SVD:field=RAMP_MODE_R,bitOffset=10,bitWidth=1,description="dac mixer right channel volume ramp mode:\n              1: slowly ramp to target volume. Step is 0.5db\n              0: directly ramp to target volume." */
        volatile uint32_t RAMP_MODE_R: 1;  /* dac mixer right channel volume ramp mode:               1: s */
        /* @SVD:field=ZERO_ADJUST_EN_R,bitOffset=11,bitWidth=1,description="dac mixer right channel volume adjustment during 0 volume cross enable" */
        volatile uint32_t ZERO_ADJUST_EN_R: 1;  /* dac mixer right channel volume adjustment during 0 volume cr */
        /* @SVD:field=RAMP_INTERVAL_R,bitOffset=12,bitWidth=4,description="dac mixer right channel volume ramp interval." */
        volatile uint32_t RAMP_INTERVAL_R: 4;  /* dac mixer right channel volume ramp interval. */
        /* @SVD:field=RAMP_STAT_R,bitOffset=16,bitWidth=2,description="dac mixer right channel ramp module status" */
        volatile uint32_t RAMP_STAT_R: 2;  /* dac mixer right channel ramp module status */
        /* @SVD:field=RSVD,bitOffset=18,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_PATH_CFG3_Type;

/* @SVD:register=ADC_PATH_CFG0,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="ADC_PATH_CFG0" */
/* ADC_PATH_CFG0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ROUGH_VOL_L,bitOffset=0,bitWidth=4,description="adc left channel rough volume control\n              range from -36dB to 54dB\n              step is 6dB\n              4'h0: -36dB\n              4'h1: -30dB\n              ......\n              4'h6: 0dB\n              ......\n              4'he: 48dB\n              4'hf: 54dB" */
        volatile uint32_t ROUGH_VOL_L: 4;  /* adc left channel rough volume control               range fr */
        /* @SVD:field=FINE_VOL_L,bitOffset=4,bitWidth=4,description="adc left channel fine volume control\n              range from 0dB to 6dB\n              step is 0.5dB\n              4'h0: 0dB\n              4'h1: 0.5dB\n              ......\n              4'hb: 5.5dB\n              4'hc, 4'hd, 4'he, 4'hf: mute" */
        volatile uint32_t FINE_VOL_L: 4;  /* adc left channel fine volume control               range fro */
        /* @SVD:field=ROUGH_VOL_R,bitOffset=8,bitWidth=4,description="adc right channel rough volume control\n              range from -36dB to 54dB\n              step is 6dB\n              4'h0: -36dB\n              4'h1: -30dB\n              ......\n              4'h6: 0dB\n              ......\n              4'he: 48dB\n              4'hf: 54dB" */
        volatile uint32_t ROUGH_VOL_R: 4;  /* adc right channel rough volume control               range f */
        /* @SVD:field=FINE_VOL_R,bitOffset=12,bitWidth=4,description="adc right channel  fine volume control\n              range from 0dB to 6dB\n              step is 0.5dB\n              4'h0: 0dB\n              4'h1: 0.5dB\n              ......\n              4'hb: 5.5dB\n              4'hc, 4'hd, 4'he, 4'hf: mute" */
        volatile uint32_t FINE_VOL_R: 4;  /* adc right channel  fine volume control               range f */
        /* @SVD:field=SRC_SEL,bitOffset=16,bitWidth=1,description="adc path source select\n              1'h0: select audio codec\n              1'h1: select external interface" */
        volatile uint32_t SRC_SEL: 1;  /* adc path source select               1'h0: select audio code */
        /* @SVD:field=DATA_SWAP,bitOffset=17,bitWidth=1,description="swap adc path left and right channel data" */
        volatile uint32_t DATA_SWAP: 1;  /* swap adc path left and right channel data */
        /* @SVD:field=RX2TX_LOOPBACK,bitOffset=18,bitWidth=1,description="rx to tx loopback enable" */
        volatile uint32_t RX2TX_LOOPBACK: 1;  /* rx to tx loopback enable */
        /* @SVD:field=RSVD,bitOffset=19,bitWidth=13,description="" */
        volatile uint32_t RSVD: 13;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_ADC_PATH_CFG0_Type;

/* @SVD:register=DAC_EQ_CFG0,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG0" */
/* DAC_EQ_CFG0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG0_Type;

/* @SVD:register=DAC_EQ_CFG1,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG1" */
/* DAC_EQ_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG1_Type;

/* @SVD:register=DAC_EQ_CFG2,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG2" */
/* DAC_EQ_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG2_Type;

/* @SVD:register=DAC_EQ_CFG3,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG3" */
/* DAC_EQ_CFG3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG3_Type;

/* @SVD:register=DAC_EQ_CFG4,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG4" */
/* DAC_EQ_CFG4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG4_Type;

/* @SVD:register=DAC_EQ_CFG5,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG5" */
/* DAC_EQ_CFG5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG5_Type;

/* @SVD:register=DAC_EQ_CFG6,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG6" */
/* DAC_EQ_CFG6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG6_Type;

/* @SVD:register=DAC_EQ_CFG7,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG7" */
/* DAC_EQ_CFG7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG7_Type;

/* @SVD:register=DAC_EQ_CFG8,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG8" */
/* DAC_EQ_CFG8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG8_Type;

/* @SVD:register=DAC_EQ_CFG9,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG9" */
/* DAC_EQ_CFG9 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG9_Type;

/* @SVD:register=DAC_EQ_CFG10,offset=0x0098,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG10" */
/* DAC_EQ_CFG10 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG10_Type;

/* @SVD:register=DAC_EQ_CFG11,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG11" */
/* DAC_EQ_CFG11 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG11_Type;

/* @SVD:register=DAC_EQ_CFG12,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG12" */
/* DAC_EQ_CFG12 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG12_Type;

/* @SVD:register=DAC_EQ_CFG13,offset=0x00A4,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG13" */
/* DAC_EQ_CFG13 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG13_Type;

/* @SVD:register=DAC_EQ_CFG14,offset=0x00A8,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG14" */
/* DAC_EQ_CFG14 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG14_Type;

/* @SVD:register=DAC_EQ_CFG15,offset=0x00AC,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG15" */
/* DAC_EQ_CFG15 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG15_Type;

/* @SVD:register=DAC_EQ_CFG16,offset=0x00B0,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG16" */
/* DAC_EQ_CFG16 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG16_Type;

/* @SVD:register=DAC_EQ_CFG17,offset=0x00B4,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG17" */
/* DAC_EQ_CFG17 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG17_Type;

/* @SVD:register=DAC_EQ_CFG18,offset=0x00B8,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG18" */
/* DAC_EQ_CFG18 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG18_Type;

/* @SVD:register=DAC_EQ_CFG19,offset=0x00BC,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG19" */
/* DAC_EQ_CFG19 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG19_Type;

/* @SVD:register=DAC_EQ_CFG20,offset=0x00C0,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG20" */
/* DAC_EQ_CFG20 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG20_Type;

/* @SVD:register=DAC_EQ_CFG21,offset=0x00C4,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG21" */
/* DAC_EQ_CFG21 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG21_Type;

/* @SVD:register=DAC_EQ_CFG22,offset=0x00C8,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG22" */
/* DAC_EQ_CFG22 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG22_Type;

/* @SVD:register=DAC_EQ_CFG23,offset=0x00CC,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG23" */
/* DAC_EQ_CFG23 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG23_Type;

/* @SVD:register=DAC_EQ_CFG24,offset=0x00D0,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG24" */
/* DAC_EQ_CFG24 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG24_Type;

/* @SVD:register=DAC_EQ_CFG25,offset=0x00D4,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG25" */
/* DAC_EQ_CFG25 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG25_Type;

/* @SVD:register=DAC_EQ_CFG26,offset=0x00D8,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG26" */
/* DAC_EQ_CFG26 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG26_Type;

/* @SVD:register=DAC_EQ_CFG27,offset=0x00DC,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG27" */
/* DAC_EQ_CFG27 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG27_Type;

/* @SVD:register=DAC_EQ_CFG28,offset=0x00E0,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG28" */
/* DAC_EQ_CFG28 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG28_Type;

/* @SVD:register=DAC_EQ_CFG29,offset=0x00E4,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG29" */
/* DAC_EQ_CFG29 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG29_Type;

/* @SVD:register=DAC_EQ_CFG30,offset=0x00E8,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG30" */
/* DAC_EQ_CFG30 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG30_Type;

/* @SVD:register=DAC_EQ_CFG31,offset=0x00EC,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG31" */
/* DAC_EQ_CFG31 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG31_Type;

/* @SVD:register=DAC_EQ_CFG32,offset=0x00F0,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG32" */
/* DAC_EQ_CFG32 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG32_Type;

/* @SVD:register=DAC_EQ_CFG33,offset=0x00F4,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG33" */
/* DAC_EQ_CFG33 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG33_Type;

/* @SVD:register=DAC_EQ_CFG34,offset=0x00F8,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG34" */
/* DAC_EQ_CFG34 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG34_Type;

/* @SVD:register=DAC_EQ_CFG35,offset=0x00FC,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG35" */
/* DAC_EQ_CFG35 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG35_Type;

/* @SVD:register=DAC_EQ_CFG36,offset=0x0100,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG36" */
/* DAC_EQ_CFG36 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG36_Type;

/* @SVD:register=DAC_EQ_CFG37,offset=0x0104,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG37" */
/* DAC_EQ_CFG37 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG37_Type;

/* @SVD:register=DAC_EQ_CFG38,offset=0x0108,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG38" */
/* DAC_EQ_CFG38 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG38_Type;

/* @SVD:register=DAC_EQ_CFG39,offset=0x010C,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG39" */
/* DAC_EQ_CFG39 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG39_Type;

/* @SVD:register=DAC_EQ_CFG40,offset=0x0110,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG40" */
/* DAC_EQ_CFG40 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG40_Type;

/* @SVD:register=DAC_EQ_CFG41,offset=0x0114,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG41" */
/* DAC_EQ_CFG41 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG41_Type;

/* @SVD:register=DAC_EQ_CFG42,offset=0x0118,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG42" */
/* DAC_EQ_CFG42 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG42_Type;

/* @SVD:register=DAC_EQ_CFG43,offset=0x011C,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG43" */
/* DAC_EQ_CFG43 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG43_Type;

/* @SVD:register=DAC_EQ_CFG44,offset=0x0120,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG44" */
/* DAC_EQ_CFG44 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG44_Type;

/* @SVD:register=DAC_EQ_CFG45,offset=0x0124,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG45" */
/* DAC_EQ_CFG45 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG45_Type;

/* @SVD:register=DAC_EQ_CFG46,offset=0x0128,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG46" */
/* DAC_EQ_CFG46 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG46_Type;

/* @SVD:register=DAC_EQ_CFG47,offset=0x012C,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG47" */
/* DAC_EQ_CFG47 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG47_Type;

/* @SVD:register=DAC_EQ_CFG48,offset=0x0130,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG48" */
/* DAC_EQ_CFG48 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG48_Type;

/* @SVD:register=DAC_EQ_CFG49,offset=0x0134,size=32,access=read-write,reset=0x00000000,description="DAC_EQ_CFG49" */
/* DAC_EQ_CFG49 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COEF,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t COEF: 24;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_DAC_EQ_CFG49_Type;

/* @SVD:register=RESERVED_IN,offset=0x0138,size=32,access=read-write,reset=0x00000000,description="RESERVED_IN" */
/* RESERVED_IN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CTRL_0,bitOffset=0,bitWidth=8,description="reserved control 0" */
        volatile uint32_t CTRL_0: 8;  /* reserved control 0 */
        /* @SVD:field=CTRL_1,bitOffset=8,bitWidth=8,description="reserved control 1" */
        volatile uint32_t CTRL_1: 8;  /* reserved control 1 */
        /* @SVD:field=CTRL_2,bitOffset=16,bitWidth=8,description="reserved control 2" */
        volatile uint32_t CTRL_2: 8;  /* reserved control 2 */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_RESERVED_IN_Type;

/* @SVD:register=RESERVED_OUT,offset=0x013C,size=32,access=read-write,reset=0x00000000,description="RESERVED_OUT" */
/* RESERVED_OUT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=STAT,bitOffset=0,bitWidth=8,description="reserved status" */
        volatile uint32_t STAT: 8;  /* reserved status */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDPRC_RESERVED_OUT_Type;

/* AUDPRC 寄存器结构体 */
typedef struct
{
    AUDPRC_ID_Type ID;  /* 0x0000 - ID */
    AUDPRC_CFG_Type CFG;  /* 0x0004 - CFG */
    AUDPRC_STB_Type STB;  /* 0x0008 - STB */
    AUDPRC_IRQ_Type IRQ;  /* 0x000C - IRQ */
    AUDPRC_TX_CH0_CFG_Type TX_CH0_CFG;  /* 0x0010 - TX_CH0_CFG */
    AUDPRC_TX_CH0_ENTRY_Type TX_CH0_ENTRY;  /* 0x0014 - TX_CH0_ENTRY */
    AUDPRC_TX_CH1_CFG_Type TX_CH1_CFG;  /* 0x0018 - TX_CH1_CFG */
    AUDPRC_TX_CH1_ENTRY_Type TX_CH1_ENTRY;  /* 0x001C - TX_CH1_ENTRY */
    AUDPRC_TX_CH2_CFG_Type TX_CH2_CFG;  /* 0x0020 - TX_CH2_CFG */
    AUDPRC_TX_CH2_ENTRY_Type TX_CH2_ENTRY;  /* 0x0024 - TX_CH2_ENTRY */
    AUDPRC_TX_CH3_CFG_Type TX_CH3_CFG;  /* 0x0028 - TX_CH3_CFG */
    AUDPRC_TX_CH3_ENTRY_Type TX_CH3_ENTRY;  /* 0x002C - TX_CH3_ENTRY */
    AUDPRC_RX_CH0_CFG_Type RX_CH0_CFG;  /* 0x0030 - RX_CH0_CFG */
    AUDPRC_RX_CH0_ENTRY_Type RX_CH0_ENTRY;  /* 0x0034 - RX_CH0_ENTRY */
    AUDPRC_RX_CH1_CFG_Type RX_CH1_CFG;  /* 0x0038 - RX_CH1_CFG */
    AUDPRC_RX_CH1_ENTRY_Type RX_CH1_ENTRY;  /* 0x003C - RX_CH1_ENTRY */
    AUDPRC_TX_OUT_CH0_CFG_Type TX_OUT_CH0_CFG;  /* 0x0040 - TX_OUT_CH0_CFG */
    AUDPRC_TX_OUT_CH0_ENTRY_Type TX_OUT_CH0_ENTRY;  /* 0x0044 - TX_OUT_CH0_ENTRY */
    AUDPRC_TX_OUT_CH1_CFG_Type TX_OUT_CH1_CFG;  /* 0x0048 - TX_OUT_CH1_CFG */
    AUDPRC_TX_OUT_CH1_ENTRY_Type TX_OUT_CH1_ENTRY;  /* 0x004C - TX_OUT_CH1_ENTRY */
    AUDPRC_DAC_PATH_CFG0_Type DAC_PATH_CFG0;  /* 0x0050 - DAC_PATH_CFG0 */
    AUDPRC_DAC_PATH_CFG1_Type DAC_PATH_CFG1;  /* 0x0054 - DAC_PATH_CFG1 */
    AUDPRC_DAC_PATH_CFG2_Type DAC_PATH_CFG2;  /* 0x0058 - DAC_PATH_CFG2 */
    AUDPRC_DAC_PATH_CFG3_Type DAC_PATH_CFG3;  /* 0x005C - DAC_PATH_CFG3 */
    AUDPRC_ADC_PATH_CFG0_Type ADC_PATH_CFG0;  /* 0x0060 - ADC_PATH_CFG0 */
    /* @SVD:register=RSVD1,offset=0x0064,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0064 - RSVD1 */
    volatile uint32_t RESERVED26[2];  /* 0x0068 - 0x006F */
    AUDPRC_DAC_EQ_CFG0_Type DAC_EQ_CFG0;  /* 0x0070 - DAC_EQ_CFG0 */
    AUDPRC_DAC_EQ_CFG1_Type DAC_EQ_CFG1;  /* 0x0074 - DAC_EQ_CFG1 */
    AUDPRC_DAC_EQ_CFG2_Type DAC_EQ_CFG2;  /* 0x0078 - DAC_EQ_CFG2 */
    AUDPRC_DAC_EQ_CFG3_Type DAC_EQ_CFG3;  /* 0x007C - DAC_EQ_CFG3 */
    AUDPRC_DAC_EQ_CFG4_Type DAC_EQ_CFG4;  /* 0x0080 - DAC_EQ_CFG4 */
    AUDPRC_DAC_EQ_CFG5_Type DAC_EQ_CFG5;  /* 0x0084 - DAC_EQ_CFG5 */
    AUDPRC_DAC_EQ_CFG6_Type DAC_EQ_CFG6;  /* 0x0088 - DAC_EQ_CFG6 */
    AUDPRC_DAC_EQ_CFG7_Type DAC_EQ_CFG7;  /* 0x008C - DAC_EQ_CFG7 */
    AUDPRC_DAC_EQ_CFG8_Type DAC_EQ_CFG8;  /* 0x0090 - DAC_EQ_CFG8 */
    AUDPRC_DAC_EQ_CFG9_Type DAC_EQ_CFG9;  /* 0x0094 - DAC_EQ_CFG9 */
    AUDPRC_DAC_EQ_CFG10_Type DAC_EQ_CFG10;  /* 0x0098 - DAC_EQ_CFG10 */
    AUDPRC_DAC_EQ_CFG11_Type DAC_EQ_CFG11;  /* 0x009C - DAC_EQ_CFG11 */
    AUDPRC_DAC_EQ_CFG12_Type DAC_EQ_CFG12;  /* 0x00A0 - DAC_EQ_CFG12 */
    AUDPRC_DAC_EQ_CFG13_Type DAC_EQ_CFG13;  /* 0x00A4 - DAC_EQ_CFG13 */
    AUDPRC_DAC_EQ_CFG14_Type DAC_EQ_CFG14;  /* 0x00A8 - DAC_EQ_CFG14 */
    AUDPRC_DAC_EQ_CFG15_Type DAC_EQ_CFG15;  /* 0x00AC - DAC_EQ_CFG15 */
    AUDPRC_DAC_EQ_CFG16_Type DAC_EQ_CFG16;  /* 0x00B0 - DAC_EQ_CFG16 */
    AUDPRC_DAC_EQ_CFG17_Type DAC_EQ_CFG17;  /* 0x00B4 - DAC_EQ_CFG17 */
    AUDPRC_DAC_EQ_CFG18_Type DAC_EQ_CFG18;  /* 0x00B8 - DAC_EQ_CFG18 */
    AUDPRC_DAC_EQ_CFG19_Type DAC_EQ_CFG19;  /* 0x00BC - DAC_EQ_CFG19 */
    AUDPRC_DAC_EQ_CFG20_Type DAC_EQ_CFG20;  /* 0x00C0 - DAC_EQ_CFG20 */
    AUDPRC_DAC_EQ_CFG21_Type DAC_EQ_CFG21;  /* 0x00C4 - DAC_EQ_CFG21 */
    AUDPRC_DAC_EQ_CFG22_Type DAC_EQ_CFG22;  /* 0x00C8 - DAC_EQ_CFG22 */
    AUDPRC_DAC_EQ_CFG23_Type DAC_EQ_CFG23;  /* 0x00CC - DAC_EQ_CFG23 */
    AUDPRC_DAC_EQ_CFG24_Type DAC_EQ_CFG24;  /* 0x00D0 - DAC_EQ_CFG24 */
    AUDPRC_DAC_EQ_CFG25_Type DAC_EQ_CFG25;  /* 0x00D4 - DAC_EQ_CFG25 */
    AUDPRC_DAC_EQ_CFG26_Type DAC_EQ_CFG26;  /* 0x00D8 - DAC_EQ_CFG26 */
    AUDPRC_DAC_EQ_CFG27_Type DAC_EQ_CFG27;  /* 0x00DC - DAC_EQ_CFG27 */
    AUDPRC_DAC_EQ_CFG28_Type DAC_EQ_CFG28;  /* 0x00E0 - DAC_EQ_CFG28 */
    AUDPRC_DAC_EQ_CFG29_Type DAC_EQ_CFG29;  /* 0x00E4 - DAC_EQ_CFG29 */
    AUDPRC_DAC_EQ_CFG30_Type DAC_EQ_CFG30;  /* 0x00E8 - DAC_EQ_CFG30 */
    AUDPRC_DAC_EQ_CFG31_Type DAC_EQ_CFG31;  /* 0x00EC - DAC_EQ_CFG31 */
    AUDPRC_DAC_EQ_CFG32_Type DAC_EQ_CFG32;  /* 0x00F0 - DAC_EQ_CFG32 */
    AUDPRC_DAC_EQ_CFG33_Type DAC_EQ_CFG33;  /* 0x00F4 - DAC_EQ_CFG33 */
    AUDPRC_DAC_EQ_CFG34_Type DAC_EQ_CFG34;  /* 0x00F8 - DAC_EQ_CFG34 */
    AUDPRC_DAC_EQ_CFG35_Type DAC_EQ_CFG35;  /* 0x00FC - DAC_EQ_CFG35 */
    AUDPRC_DAC_EQ_CFG36_Type DAC_EQ_CFG36;  /* 0x0100 - DAC_EQ_CFG36 */
    AUDPRC_DAC_EQ_CFG37_Type DAC_EQ_CFG37;  /* 0x0104 - DAC_EQ_CFG37 */
    AUDPRC_DAC_EQ_CFG38_Type DAC_EQ_CFG38;  /* 0x0108 - DAC_EQ_CFG38 */
    AUDPRC_DAC_EQ_CFG39_Type DAC_EQ_CFG39;  /* 0x010C - DAC_EQ_CFG39 */
    AUDPRC_DAC_EQ_CFG40_Type DAC_EQ_CFG40;  /* 0x0110 - DAC_EQ_CFG40 */
    AUDPRC_DAC_EQ_CFG41_Type DAC_EQ_CFG41;  /* 0x0114 - DAC_EQ_CFG41 */
    AUDPRC_DAC_EQ_CFG42_Type DAC_EQ_CFG42;  /* 0x0118 - DAC_EQ_CFG42 */
    AUDPRC_DAC_EQ_CFG43_Type DAC_EQ_CFG43;  /* 0x011C - DAC_EQ_CFG43 */
    AUDPRC_DAC_EQ_CFG44_Type DAC_EQ_CFG44;  /* 0x0120 - DAC_EQ_CFG44 */
    AUDPRC_DAC_EQ_CFG45_Type DAC_EQ_CFG45;  /* 0x0124 - DAC_EQ_CFG45 */
    AUDPRC_DAC_EQ_CFG46_Type DAC_EQ_CFG46;  /* 0x0128 - DAC_EQ_CFG46 */
    AUDPRC_DAC_EQ_CFG47_Type DAC_EQ_CFG47;  /* 0x012C - DAC_EQ_CFG47 */
    AUDPRC_DAC_EQ_CFG48_Type DAC_EQ_CFG48;  /* 0x0130 - DAC_EQ_CFG48 */
    AUDPRC_DAC_EQ_CFG49_Type DAC_EQ_CFG49;  /* 0x0134 - DAC_EQ_CFG49 */
    AUDPRC_RESERVED_IN_Type RESERVED_IN;  /* 0x0138 - RESERVED_IN */
    AUDPRC_RESERVED_OUT_Type RESERVED_OUT;  /* 0x013C - RESERVED_OUT */
} AUDPRC_TypeDef;

/* ============================================================================ */
/* EZIP1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=EZIP1,base=0x50006000,description="",groupName=EZIP */

#define EZIP1_BASE                                         (0x50006000UL)

/* @SVD:register=EZIP_CTRL,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="ezip/aezip_frame decoder ctrl" */
/* EZIP_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EZIP_CTRL,bitOffset=0,bitWidth=1,description="1:start or run\n              0:stop or end" */
        volatile uint32_t EZIP_CTRL: 1;  /* 1:start or run               0:stop or end */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_EZIP_CTRL_Type;

/* @SVD:register=SRC_ADDR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="ezip decoder source address" */
/* SRC_ADDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SRC_ADDR,bitOffset=0,bitWidth=32,description="ezip decoder source address" */
        volatile uint32_t SRC_ADDR: 32;  /* ezip decoder source address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_SRC_ADDR_Type;

/* @SVD:register=DST_ADDR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="ezip decoder destination address" */
/* DST_ADDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DST_ADDR,bitOffset=0,bitWidth=32,description="ezip decoder destination address(ahb_out mode)" */
        volatile uint32_t DST_ADDR: 32;  /* ezip decoder destination address(ahb_out mode) */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DST_ADDR_Type;

/* @SVD:register=EZIP_PARA,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="ezip decoder  parameter" */
/* EZIP_PARA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OUT_SEL,bitOffset=0,bitWidth=1,description="only used in ezip decoder mode. must select ahb in gzip/lz4 decoder mode.\n              0:epic\n              1:ahb" */
        volatile uint32_t OUT_SEL: 1;  /* only used in ezip decoder mode. must select ahb in gzip/lz4  */
        /* @SVD:field=MOD_SEL,bitOffset=1,bitWidth=2,description="0:ezip or aezip\n              1:gzip\n              2:Lz4" */
        volatile uint32_t MOD_SEL: 2;  /* 0:ezip or aezip               1:gzip               2:Lz4 */
        /* @SVD:field=CACHE_EN,bitOffset=3,bitWidth=1,description="no used" */
        volatile uint32_t CACHE_EN: 1;  /* no used */
        /* @SVD:field=IN_SEL,bitOffset=4,bitWidth=1,description="don't support ezip type2\type4. \n              0:ahb\n              1:fifo" */
        volatile uint32_t IN_SEL: 1;  /* don't support ezip type2\type4.                0:ahb         */
        /* @SVD:field=SPI_SEL,bitOffset=5,bitWidth=1,description="0:QSPI4\n              1:QSPI3" */
        volatile uint32_t SPI_SEL: 1;  /* 0:QSPI4               1:QSPI3 */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_EZIP_PARA_Type;

/* @SVD:register=CACHE_CLR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="ezip index cache clear" */
/* CACHE_CLR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CACHE_CLR,bitOffset=0,bitWidth=1,description="no used" */
        volatile uint32_t CACHE_CLR: 1;  /* no used */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_CACHE_CLR_Type;

/* @SVD:register=START_POINT,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="ezip decoder start point" */
/* START_POINT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=START_ROW,bitOffset=0,bitWidth=16,description="ezip start row,count from 0" */
        volatile uint32_t START_ROW: 16;  /* ezip start row,count from 0 */
        /* @SVD:field=START_COL,bitOffset=16,bitWidth=16,description="ezip start col,count from 0" */
        volatile uint32_t START_COL: 16;  /* ezip start col,count from 0 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_START_POINT_Type;

/* @SVD:register=END_POINT,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="ezip decoder end point" */
/* END_POINT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=END_ROW,bitOffset=0,bitWidth=16,description="ezip end row" */
        volatile uint32_t END_ROW: 16;  /* ezip end row */
        /* @SVD:field=END_COL,bitOffset=16,bitWidth=16,description="ezip end col" */
        volatile uint32_t END_COL: 16;  /* ezip end col */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_END_POINT_Type;

/* @SVD:register=ROW_SIGN,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="ezip decoder row sign" */
/* ROW_SIGN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ROW_SIGN,bitOffset=0,bitWidth=16,description="arrived row sign,ezip can generate a interrupt" */
        volatile uint32_t ROW_SIGN: 16;  /* arrived row sign,ezip can generate a interrupt */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_ROW_SIGN_Type;

/* @SVD:register=INT_EN,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="ezip decoder _int_en" */
/* INT_EN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=END_INT_EN,bitOffset=0,bitWidth=1,description="ezip_end _int_en" */
        volatile uint32_t END_INT_EN: 1;  /* ezip_end _int_en */
        /* @SVD:field=ROW_INT_EN,bitOffset=1,bitWidth=1,description="row_int_en" */
        volatile uint32_t ROW_INT_EN: 1;  /* row_int_en */
        /* @SVD:field=ROW_ERR_EN,bitOffset=2,bitWidth=1,description="row_err_en" */
        volatile uint32_t ROW_ERR_EN: 1;  /* row_err_en */
        /* @SVD:field=BTYPE_ERR_EN,bitOffset=3,bitWidth=1,description="btype_err_en" */
        volatile uint32_t BTYPE_ERR_EN: 1;  /* btype_err_en */
        /* @SVD:field=ETYPE_ERR_EN,bitOffset=4,bitWidth=1,description="ezip_type_err_en" */
        volatile uint32_t ETYPE_ERR_EN: 1;  /* ezip_type_err_en */
        /* @SVD:field=AEZIP_INT_EN,bitOffset=5,bitWidth=1,description="aezip_frame_int_en" */
        volatile uint32_t AEZIP_INT_EN: 1;  /* aezip_frame_int_en */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_INT_EN_Type;

/* @SVD:register=INT_STA,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="ezip decoder _int_sta" */
/* INT_STA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=END_INT_STA,bitOffset=0,bitWidth=1,description="ezip_end _int_sta/aezip_frame_int_sta" */
        volatile uint32_t END_INT_STA: 1;  /* ezip_end _int_sta/aezip_frame_int_sta */
        /* @SVD:field=ROW_INT_STA,bitOffset=1,bitWidth=1,description="arrive row sign sta" */
        volatile uint32_t ROW_INT_STA: 1;  /* arrive row sign sta */
        /* @SVD:field=ROW_ERR_STA,bitOffset=2,bitWidth=1,description="overflow height sta" */
        volatile uint32_t ROW_ERR_STA: 1;  /* overflow height sta */
        /* @SVD:field=BTYPE_ERR_STA,bitOffset=3,bitWidth=1,description="btype_err_sta" */
        volatile uint32_t BTYPE_ERR_STA: 1;  /* btype_err_sta */
        /* @SVD:field=ETYPE_ERR_STA,bitOffset=4,bitWidth=1,description="ezip_type_err_sta" */
        volatile uint32_t ETYPE_ERR_STA: 1;  /* ezip_type_err_sta */
        /* @SVD:field=AEZIP_INT_STA,bitOffset=5,bitWidth=1,description="aezip_end_int_sta" */
        volatile uint32_t AEZIP_INT_STA: 1;  /* aezip_end_int_sta */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_INT_STA_Type;

/* @SVD:register=INT_MASK,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="ezip decoder int mask state" */
/* INT_MASK 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=END_INT_MASK,bitOffset=0,bitWidth=1,description="ezip_end _int mask sta/aezip_frame_int_mask_Sta" */
        volatile uint32_t END_INT_MASK: 1;  /* ezip_end _int mask sta/aezip_frame_int_mask_Sta */
        /* @SVD:field=ROW_INT_MASK,bitOffset=1,bitWidth=1,description="arrive row sign mask sta" */
        volatile uint32_t ROW_INT_MASK: 1;  /* arrive row sign mask sta */
        /* @SVD:field=ROW_ERR_MASK,bitOffset=2,bitWidth=1,description="overflow height mask sta" */
        volatile uint32_t ROW_ERR_MASK: 1;  /* overflow height mask sta */
        /* @SVD:field=BTYPE_ERR_MASK,bitOffset=3,bitWidth=1,description="btype_err_mask sta" */
        volatile uint32_t BTYPE_ERR_MASK: 1;  /* btype_err_mask sta */
        /* @SVD:field=ETYPE_ERR_MASK,bitOffset=4,bitWidth=1,description="ezip_type_err_mask sta" */
        volatile uint32_t ETYPE_ERR_MASK: 1;  /* ezip_type_err_mask sta */
        /* @SVD:field=AEZIP_INT_MASK,bitOffset=5,bitWidth=1,description="aezip_end_int_mask sta" */
        volatile uint32_t AEZIP_INT_MASK: 1;  /* aezip_end_int_mask sta */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_INT_MASK_Type;

/* @SVD:register=NAP_PARA,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="ezip decoder release bus parameter" */
/* NAP_PARA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NAP_TIM,bitOffset=0,bitWidth=4,description="ezip decoder release bus time\n              0000: not nap\n              0001: 16 cycle\n              0010: 32 cycle\n              0100: 64 cycle\n              1000: 128 cycle\n              other: not nap" */
        volatile uint32_t NAP_TIM: 4;  /* ezip decoder release bus time               0000: not nap    */
        /* @SVD:field=BURST_NUM,bitOffset=4,bitWidth=4,description="ezip decoder burst number\n              0000: 16\n              0001: 32 \n              0010: 64 \n              0100: 128 \n              1000: 256 \n              other: 16" */
        volatile uint32_t BURST_NUM: 4;  /* ezip decoder burst number               0000: 16             */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_NAP_PARA_Type;

/* @SVD:register=SRC_LEN,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="ezip source data length" */
/* SRC_LEN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SRC_LEN,bitOffset=0,bitWidth=32,description="source data byte length only in source data fifo mode" */
        volatile uint32_t SRC_LEN: 32;  /* source data byte length only in source data fifo mode */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_SRC_LEN_Type;

/* @SVD:register=AEZIP_CTRL,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="AEZIP ctrl" */
/* AEZIP_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=AEZIP_CTRL,bitOffset=0,bitWidth=1,description="AEZIP ctrl" */
        volatile uint32_t AEZIP_CTRL: 1;  /* AEZIP ctrl */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_AEZIP_CTRL_Type;

/* @SVD:register=FRAME_START,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="Aezip start number of frames" */
/* FRAME_START 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FRAME_START,bitOffset=0,bitWidth=32,description="start number of frames,count from 1" */
        volatile uint32_t FRAME_START: 32;  /* start number of frames,count from 1 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_FRAME_START_Type;

/* @SVD:register=PLAY_START,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="Aezip start number of play" */
/* PLAY_START 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PLAY_START,bitOffset=0,bitWidth=32,description="start number of times to loop this AEZIP,,count from 1" */
        volatile uint32_t PLAY_START: 32;  /* start number of times to loop this AEZIP,,count from 1 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_PLAY_START_Type;

/* @SVD:register=FRAME_NUM,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="Aezip number of frames" */
/* FRAME_NUM 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FRAME_NUM,bitOffset=0,bitWidth=32,description="number of frames" */
        volatile uint32_t FRAME_NUM: 32;  /* number of frames */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_FRAME_NUM_Type;

/* @SVD:register=PLAY_NUM,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="Aezip number of times to loop this AEZIP" */
/* PLAY_NUM 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PLAY_NUM,bitOffset=0,bitWidth=32,description="number of times to loop this AEZIP,0 indicates infinite looping" */
        volatile uint32_t PLAY_NUM: 32;  /* number of times to loop this AEZIP,0 indicates infinite loop */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_PLAY_NUM_Type;

/* @SVD:register=SEQ_NUM,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="Aezip sequence  number" */
/* SEQ_NUM 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEQ_NUM,bitOffset=0,bitWidth=32,description="sequence  number of the animation chunk,starting from 0" */
        volatile uint32_t SEQ_NUM: 32;  /* sequence  number of the animation chunk,starting from 0 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_SEQ_NUM_Type;

/* @SVD:register=FRAME_AREA,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="Aezip frame area" */
/* FRAME_AREA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FRAME_HEIGHT,bitOffset=0,bitWidth=16,description="AEZIP frame height" */
        volatile uint32_t FRAME_HEIGHT: 16;  /* AEZIP frame height */
        /* @SVD:field=FRAME_WIDTH,bitOffset=16,bitWidth=16,description="AEZIP frame width" */
        volatile uint32_t FRAME_WIDTH: 16;  /* AEZIP frame width */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_FRAME_AREA_Type;

/* @SVD:register=FRAME_OFFSET,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="Aezip frame area" */
/* FRAME_OFFSET 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OFFEST_ROW,bitOffset=0,bitWidth=16,description="AEZIP frame offset row" */
        volatile uint32_t OFFEST_ROW: 16;  /* AEZIP frame offset row */
        /* @SVD:field=OFFSET_COL,bitOffset=16,bitWidth=16,description="AEZIP frame  offset col" */
        volatile uint32_t OFFSET_COL: 16;  /* AEZIP frame  offset col */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_FRAME_OFFSET_Type;

/* @SVD:register=FRAME_DELAY,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="Aezip frame delay" */
/* FRAME_DELAY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DELAY_DEN,bitOffset=0,bitWidth=16,description="AEZIP frame delay fraction denominator" */
        volatile uint32_t DELAY_DEN: 16;  /* AEZIP frame delay fraction denominator */
        /* @SVD:field=DELAY_NUM,bitOffset=16,bitWidth=16,description="AEZIP frame  delay fraction numerator" */
        volatile uint32_t DELAY_NUM: 16;  /* AEZIP frame  delay fraction numerator */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_FRAME_DELAY_Type;

/* @SVD:register=FRAME_TYPE,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="   " */
/* FRAME_TYPE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BLEND_OP,bitOffset=0,bitWidth=8,description="AEZIP type of frame area renndering for this frame" */
        volatile uint32_t BLEND_OP: 8;  /* AEZIP type of frame area renndering for this frame */
        /* @SVD:field=DISPOSE_OP,bitOffset=8,bitWidth=8,description="AEZIP type of frame area disposal to be done after rendering this frame" */
        volatile uint32_t DISPOSE_OP: 8;  /* AEZIP type of frame area disposal to be done after rendering */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_FRAME_TYPE_Type;

/* @SVD:register=FRAME_SIZE,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="Aezip frame size" */
/* FRAME_SIZE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FRAME_SIZE,bitOffset=0,bitWidth=32,description="frame size" */
        volatile uint32_t FRAME_SIZE: 32;  /* frame size */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_FRAME_SIZE_Type;

/* @SVD:register=GREY_PARA,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="   " */
/* GREY_PARA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GREY_PARA,bitOffset=0,bitWidth=24,description="fill color parameter, when send grey data to epic . [23:16]-R,[15:8]-G,[7:0]-B" */
        volatile uint32_t GREY_PARA: 24;  /* fill color parameter, when send grey data to epic . [23:16]- */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_GREY_PARA_Type;

/* @SVD:register=DB_SEL,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug sel" */
/* DB_SEL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_SEL,bitOffset=0,bitWidth=16,description="bit[15] 0:line_first                 1:out_buf_en[1]\n              bit[14] 0:rd_head3                1:out_buf_en[0]\n              bit[13] 0:rd_head2                1:inbuf_empty\n              bit[12] 0:rd_heas1                 1:inbuf_half_empty\n              bit[11] 0:blk_restart              1: inbuf_full\n              bit[10] 0:ezip_buf_end        1:ezip_pixel_end\n              bit[9]   0:ezip_buf_full           1:0\n              bit[8]   0:ezip_buf_empty      1:0\n              bit[7]   0:dec_buf_empty         1:0\n              bit[6]   0:dec_buf_full               1:para_ok\n              bit[5]   0:dec_on         1:ezip_fuf_push\n              bit[4]   0:ind3_on       1:copy_on\n              bit[3]   0:ind2_on       1:bypass_on\n              bit[2]   0:ind1_on       1:blk_clr\n              bit[1]   0:ezip_on       1:para_val\n              bit[0]   0:ezip_int      1:para_req" */
        volatile uint32_t DB_SEL: 16;  /* bit[15] 0:line_first                 1:out_buf_en[1]         */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_SEL_Type;

/* @SVD:register=DB_DATA0,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data0" */
/* DB_DATA0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA0,bitOffset=0,bitWidth=32,description="bit[31:24]  bit_depth\n              bit[23:16] color_type\n              bit[15:0] block number" */
        volatile uint32_t DB_DATA0: 32;  /* bit[31:24]  bit_depth               bit[23:16] color_type    */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA0_Type;

/* @SVD:register=DB_DATA1,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data1" */
/* DB_DATA1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA1,bitOffset=0,bitWidth=32,description="bit[31:16] width\n              bit[15:0] height" */
        volatile uint32_t DB_DATA1: 32;  /* bit[31:16] width               bit[15:0] height */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA1_Type;

/* @SVD:register=DB_DATA2,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data2" */
/* DB_DATA2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA2,bitOffset=0,bitWidth=32,description="bit[31:0] total_len" */
        volatile uint32_t DB_DATA2: 32;  /* bit[31:0] total_len */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA2_Type;

/* @SVD:register=DB_DATA3,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data3" */
/* DB_DATA3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA3,bitOffset=0,bitWidth=32,description="bit[31:24] ezip_type\n              bit[23:20] bfinal\n              bit[19:16] btype\n              bit[11:8] ahb_state\n              bit[7:4] ctrl_state\n              bir[3:0] build_stste" */
        volatile uint32_t DB_DATA3: 32;  /* bit[31:24] ezip_type               bit[23:20] bfinal         */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA3_Type;

/* @SVD:register=DB_DATA4,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data4" */
/* DB_DATA4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA4,bitOffset=0,bitWidth=32,description="bit[9]:ezip_buf_full\n              bit[8]:ezip_buf_empty\n              bit[7]:dec_buf_full\n              bit[6]:dec_buf_empty\n              bit[5]:bypass_on\n              bit[4]:dec_on\n              bit[3]:ind3_on\n              bit[2]:ind2_on\n              bit[1]:ind1_on\n              bit[0]:ezip_on" */
        volatile uint32_t DB_DATA4: 32;  /* bit[9]:ezip_buf_full               bit[8]:ezip_buf_empty     */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA4_Type;

/* @SVD:register=DB_DATA5,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data5" */
/* DB_DATA5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA5,bitOffset=0,bitWidth=32,description="bit[31:16] width_cnt_cur\n              bit[15:0] height_cnt_cur" */
        volatile uint32_t DB_DATA5: 32;  /* bit[31:16] width_cnt_cur               bit[15:0] height_cnt_ */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA5_Type;

/* @SVD:register=DB_DATA6,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data6" */
/* DB_DATA6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA6,bitOffset=0,bitWidth=32,description="seq_num" */
        volatile uint32_t DB_DATA6: 32;  /* seq_num */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA6_Type;

/* @SVD:register=DB_DATA7,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data7" */
/* DB_DATA7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA7,bitOffset=0,bitWidth=32,description="bit[31:16] frame_width_cur\n              bit[15:0] frame_height_cur" */
        volatile uint32_t DB_DATA7: 32;  /* bit[31:16] frame_width_cur               bit[15:0] frame_hei */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA7_Type;

/* @SVD:register=DB_DATA8,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data8" */
/* DB_DATA8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA8,bitOffset=0,bitWidth=32,description="bit[31:16 ]frame_offsetx_cur\n              bit[15:0] frame_offsety_cur" */
        volatile uint32_t DB_DATA8: 32;  /* bit[31:16 ]frame_offsetx_cur               bit[15:0] frame_o */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA8_Type;

/* @SVD:register=DB_DATA9,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data9" */
/* DB_DATA9 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA9,bitOffset=0,bitWidth=32,description="bit[31:16 ]delay_num_cur\n              bit[15:0] delay_den_cur" */
        volatile uint32_t DB_DATA9: 32;  /* bit[31:16 ]delay_num_cur               bit[15:0] delay_den_c */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA9_Type;

/* @SVD:register=DB_DATA10,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data10" */
/* DB_DATA10 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA10,bitOffset=0,bitWidth=32,description="bit[15:8 dispos_op_cur\n              bit[7:0] blend_op_cur" */
        volatile uint32_t DB_DATA10: 32;  /* bit[15:8 dispos_op_cur               bit[7:0] blend_op_cur */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA10_Type;

/* @SVD:register=DB_DATA11,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data11" */
/* DB_DATA11 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA11,bitOffset=0,bitWidth=32,description="frame_cont_cur" */
        volatile uint32_t DB_DATA11: 32;  /* frame_cont_cur */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA11_Type;

/* @SVD:register=DB_DATA12,offset=0x0098,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data12" */
/* DB_DATA12 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA12,bitOffset=0,bitWidth=32,description="paly_cont_cur" */
        volatile uint32_t DB_DATA12: 32;  /* paly_cont_cur */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA12_Type;

/* @SVD:register=DB_DATA13,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="ezip decoder debug data13" */
/* DB_DATA13 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_DATA13,bitOffset=0,bitWidth=32,description="frame_size_cur" */
        volatile uint32_t DB_DATA13: 32;  /* frame_size_cur */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EZIP1_DB_DATA13_Type;

/* EZIP1 寄存器结构体 */
typedef struct
{
    EZIP1_EZIP_CTRL_Type EZIP_CTRL;  /* 0x0000 - ezip/aezip_frame decoder ctrl */
    EZIP1_SRC_ADDR_Type SRC_ADDR;  /* 0x0004 - ezip decoder source address */
    EZIP1_DST_ADDR_Type DST_ADDR;  /* 0x0008 - ezip decoder destination address */
    EZIP1_EZIP_PARA_Type EZIP_PARA;  /* 0x000C - ezip decoder  parameter */
    EZIP1_CACHE_CLR_Type CACHE_CLR;  /* 0x0010 - ezip index cache clear */
    EZIP1_START_POINT_Type START_POINT;  /* 0x0014 - ezip decoder start point */
    EZIP1_END_POINT_Type END_POINT;  /* 0x0018 - ezip decoder end point */
    EZIP1_ROW_SIGN_Type ROW_SIGN;  /* 0x001C - ezip decoder row sign */
    EZIP1_INT_EN_Type INT_EN;  /* 0x0020 - ezip decoder _int_en */
    EZIP1_INT_STA_Type INT_STA;  /* 0x0024 - ezip decoder _int_sta */
    EZIP1_INT_MASK_Type INT_MASK;  /* 0x0028 - ezip decoder int mask state */
    EZIP1_NAP_PARA_Type NAP_PARA;  /* 0x002C - ezip decoder release bus parameter */
    EZIP1_SRC_LEN_Type SRC_LEN;  /* 0x0030 - ezip source data length */
    EZIP1_AEZIP_CTRL_Type AEZIP_CTRL;  /* 0x0034 - AEZIP ctrl */
    EZIP1_FRAME_START_Type FRAME_START;  /* 0x0038 - Aezip start number of frames */
    EZIP1_PLAY_START_Type PLAY_START;  /* 0x003C - Aezip start number of play */
    EZIP1_FRAME_NUM_Type FRAME_NUM;  /* 0x0040 - Aezip number of frames */
    EZIP1_PLAY_NUM_Type PLAY_NUM;  /* 0x0044 - Aezip number of times to loop this AEZIP */
    EZIP1_SEQ_NUM_Type SEQ_NUM;  /* 0x0048 - Aezip sequence  number */
    EZIP1_FRAME_AREA_Type FRAME_AREA;  /* 0x004C - Aezip frame area */
    EZIP1_FRAME_OFFSET_Type FRAME_OFFSET;  /* 0x0050 - Aezip frame area */
    EZIP1_FRAME_DELAY_Type FRAME_DELAY;  /* 0x0054 - Aezip frame delay */
    EZIP1_FRAME_TYPE_Type FRAME_TYPE;  /* 0x0058 -  */
    EZIP1_FRAME_SIZE_Type FRAME_SIZE;  /* 0x005C - Aezip frame size */
    EZIP1_GREY_PARA_Type GREY_PARA;  /* 0x0060 -  */
    EZIP1_DB_SEL_Type DB_SEL;  /* 0x0064 - ezip decoder debug sel */
    EZIP1_DB_DATA0_Type DB_DATA0;  /* 0x0068 - ezip decoder debug data0 */
    EZIP1_DB_DATA1_Type DB_DATA1;  /* 0x006C - ezip decoder debug data1 */
    EZIP1_DB_DATA2_Type DB_DATA2;  /* 0x0070 - ezip decoder debug data2 */
    EZIP1_DB_DATA3_Type DB_DATA3;  /* 0x0074 - ezip decoder debug data3 */
    EZIP1_DB_DATA4_Type DB_DATA4;  /* 0x0078 - ezip decoder debug data4 */
    EZIP1_DB_DATA5_Type DB_DATA5;  /* 0x007C - ezip decoder debug data5 */
    EZIP1_DB_DATA6_Type DB_DATA6;  /* 0x0080 - ezip decoder debug data6 */
    EZIP1_DB_DATA7_Type DB_DATA7;  /* 0x0084 - ezip decoder debug data7 */
    EZIP1_DB_DATA8_Type DB_DATA8;  /* 0x0088 - ezip decoder debug data8 */
    EZIP1_DB_DATA9_Type DB_DATA9;  /* 0x008C - ezip decoder debug data9 */
    EZIP1_DB_DATA10_Type DB_DATA10;  /* 0x0090 - ezip decoder debug data10 */
    EZIP1_DB_DATA11_Type DB_DATA11;  /* 0x0094 - ezip decoder debug data11 */
    EZIP1_DB_DATA12_Type DB_DATA12;  /* 0x0098 - ezip decoder debug data12 */
    EZIP1_DB_DATA13_Type DB_DATA13;  /* 0x009C - ezip decoder debug data13 */
} EZIP1_TypeDef;

/* ============================================================================ */
/* EPIC 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=EPIC,base=0x50007000,description="",groupName=EPIC */

#define EPIC_BASE                                          (0x50007000UL)

/* @SVD:register=COMMAND,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="COMMAND" */
/* COMMAND 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=START,bitOffset=0,bitWidth=1,description="write 1 to trigger the lcd interface block" */
        volatile uint32_t START: 1;  /* write 1 to trigger the lcd interface block */
        /* @SVD:field=RESET,bitOffset=1,bitWidth=1,description="1: reset the whole graphics   0: release the reset" */
        volatile uint32_t RESET: 1;  /* 1: reset the whole graphics   0: release the reset */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_COMMAND_Type;

/* @SVD:register=STATUS,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="STATUS" */
/* STATUS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IA_BUSY,bitOffset=0,bitWidth=1,description="Graphics accelerator busy flag" */
        volatile uint32_t IA_BUSY: 1;  /* Graphics accelerator busy flag */
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=LCD_BUSY,bitOffset=4,bitWidth=1,description="LCD controll busy flag" */
        volatile uint32_t LCD_BUSY: 1;  /* LCD controll busy flag */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_STATUS_Type;

/* @SVD:register=EOF_IRQ,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="EOF_IRQ" */
/* EOF_IRQ 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IRQ_CAUSE,bitOffset=0,bitWidth=1,description="end of frame interrupt, can be masked by EOF_IRQ_MASK" */
        volatile uint32_t IRQ_CAUSE: 1;  /* end of frame interrupt, can be masked by EOF_IRQ_MASK */
        /* @SVD:field=LINE_HIT_CAUSE,bitOffset=1,bitWidth=1,description="line hit interrupt, can be masked by LINE_IRQ_MASK" */
        volatile uint32_t LINE_HIT_CAUSE: 1;  /* line hit interrupt, can be masked by LINE_IRQ_MASK */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=14,description="" */
        volatile uint32_t RSVD2: 14;
        /* @SVD:field=IRQ_STATUS,bitOffset=16,bitWidth=1,description="raw status of end of frame interrupt" */
        volatile uint32_t IRQ_STATUS: 1;  /* raw status of end of frame interrupt */
        /* @SVD:field=LINE_HIT_STATUS,bitOffset=17,bitWidth=1,description="raw status of line hit interrupt" */
        volatile uint32_t LINE_HIT_STATUS: 1;  /* raw status of line hit interrupt */
        /* @SVD:field=RSVD,bitOffset=18,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_EOF_IRQ_Type;

/* @SVD:register=SETTING,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="SETTING" */
/* SETTING 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EOF_IRQ_MASK,bitOffset=0,bitWidth=1,description="end of frame interrupt mask, 0: mask the interrupt" */
        volatile uint32_t EOF_IRQ_MASK: 1;  /* end of frame interrupt mask, 0: mask the interrupt */
        /* @SVD:field=LINE_IRQ_MASK,bitOffset=1,bitWidth=1,description="canvas line hit interrupt mask, 0: mask the interrupt" */
        volatile uint32_t LINE_IRQ_MASK: 1;  /* canvas line hit interrupt mask, 0: mask the interrupt */
        /* @SVD:field=AUTO_GATE_EN,bitOffset=2,bitWidth=1,description="auto clock gating enable" */
        volatile uint32_t AUTO_GATE_EN: 1;  /* auto clock gating enable */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=13,description="" */
        volatile uint32_t RSVD2: 13;
        /* @SVD:field=LINE_IRQ_NUM,bitOffset=16,bitWidth=10,description="canvas line hit interrupt line number" */
        volatile uint32_t LINE_IRQ_NUM: 10;  /* canvas line hit interrupt line number */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_SETTING_Type;

/* @SVD:register=CANVAS_TL_POS,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="Top-Left pixel coordinate" */
/* CANVAS_TL_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X0,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t X0: 10;
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y0,bitOffset=16,bitWidth=10,description="" */
        volatile uint32_t Y0: 10;
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_CANVAS_TL_POS_Type;

/* @SVD:register=CANVAS_BR_POS,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Bottom-Right pixel coordinate" */
/* CANVAS_BR_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X1,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t X1: 10;
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y1,bitOffset=16,bitWidth=10,description="" */
        volatile uint32_t Y1: 10;
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_CANVAS_BR_POS_Type;

/* @SVD:register=CANVAS_BG,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="Background color" */
/* CANVAS_BG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BLUE,bitOffset=0,bitWidth=8,description="blue color" */
        volatile uint32_t BLUE: 8;  /* blue color */
        /* @SVD:field=GREEN,bitOffset=8,bitWidth=8,description="green color" */
        volatile uint32_t GREEN: 8;  /* green color */
        /* @SVD:field=RED,bitOffset=16,bitWidth=8,description="Red color" */
        volatile uint32_t RED: 8;  /* Red color */
        /* @SVD:field=BG_BLENDING_BYPASS,bitOffset=24,bitWidth=1,description="if this bit is set, the layer is not blending with background. The alpha value will be reserved to output." */
        volatile uint32_t BG_BLENDING_BYPASS: 1;  /* if this bit is set, the layer is not blending with backgroun */
        /* @SVD:field=ALL_BLENDING_BYPASS,bitOffset=25,bitWidth=1,description="if this bit is set, epic is in pure dma mode. No blending operation." */
        volatile uint32_t ALL_BLENDING_BYPASS: 1;  /* if this bit is set, epic is in pure dma mode. No blending op */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_CANVAS_BG_Type;

/* @SVD:register=VL_CFG,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="VL_CFG" */
/* VL_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FORMAT,bitOffset=0,bitWidth=4,description="video layer input format\n              4'h0: RGB565\n              4'h1: RGB888\n              4'h2: ARGB8888\n              4'h3: ARGB8565\n              4'h4: A8\n              4'h5: A4\n              4'h6: L8\n              4'h7: A2" */
        volatile uint32_t FORMAT: 4;  /* video layer input format               4'h0: RGB565          */
        /* @SVD:field=ALPHA_SEL,bitOffset=4,bitWidth=1,description="alpha selection\n              1'b0: select alpha according to image format\n              1'b1: select layer alpha" */
        volatile uint32_t ALPHA_SEL: 1;  /* alpha selection               1'b0: select alpha according t */
        /* @SVD:field=ALPHA,bitOffset=5,bitWidth=8,description="layer alpha value" */
        volatile uint32_t ALPHA: 8;  /* layer alpha value */
        /* @SVD:field=BLEND_DEPTH,bitOffset=13,bitWidth=2,description="video layer blending depth" */
        volatile uint32_t BLEND_DEPTH: 2;  /* video layer blending depth */
        /* @SVD:field=FILTER_EN,bitOffset=15,bitWidth=1,description="layer color filter enable" */
        volatile uint32_t FILTER_EN: 1;  /* layer color filter enable */
        /* @SVD:field=WIDTH,bitOffset=16,bitWidth=13,description="source image width(including padding), unit is bytes" */
        volatile uint32_t WIDTH: 13;  /* source image width(including padding), unit is bytes */
        /* @SVD:field=PREFETCH_EN,bitOffset=29,bitWidth=1,description="preload 64 bytes extra data when reading pixel from memory" */
        volatile uint32_t PREFETCH_EN: 1;  /* preload 64 bytes extra data when reading pixel from memory */
        /* @SVD:field=ACTIVE,bitOffset=30,bitWidth=1,description="layer active flag" */
        volatile uint32_t ACTIVE: 1;  /* layer active flag */
        /* @SVD:field=ALPHA_BLEND,bitOffset=31,bitWidth=1,description="set 1 to enable alpha blending mode. Use layer alpha as blending factor for image with Alpha.\n              Alpha_out = Layer_alpha * Image_alpha" */
        volatile uint32_t ALPHA_BLEND: 1;  /* set 1 to enable alpha blending mode. Use layer alpha as blen */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_CFG_Type;

/* @SVD:register=VL_TL_POS,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="VL_TL_POS" */
/* VL_TL_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X0,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X0: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y0,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y0: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_TL_POS_Type;

/* @SVD:register=VL_BR_POS,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="VL_BR_POS" */
/* VL_BR_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X1,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X1: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y1,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y1: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_BR_POS_Type;

/* @SVD:register=VL_EXTENTS,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="VL_EXTENTS" */
/* VL_EXTENTS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MAX_LINE,bitOffset=0,bitWidth=10,description="number of pixels of each column of source image(not including padding)" */
        volatile uint32_t MAX_LINE: 10;  /* number of pixels of each column of source image(not includin */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=MAX_COL,bitOffset=16,bitWidth=10,description="number of pixels of each line of source image(not including padding)" */
        volatile uint32_t MAX_COL: 10;  /* number of pixels of each line of source image(not including  */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_EXTENTS_Type;

/* @SVD:register=VL_FILTER,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="VL_FILTER" */
/* VL_FILTER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FILTER_B,bitOffset=0,bitWidth=8,description="filter b color" */
        volatile uint32_t FILTER_B: 8;  /* filter b color */
        /* @SVD:field=FILTER_G,bitOffset=8,bitWidth=8,description="filter g color" */
        volatile uint32_t FILTER_G: 8;  /* filter g color */
        /* @SVD:field=FILTER_R,bitOffset=16,bitWidth=8,description="filter r color" */
        volatile uint32_t FILTER_R: 8;  /* filter r color */
        /* @SVD:field=FILTER_MASK,bitOffset=24,bitWidth=8,description="layer color filter mask" */
        volatile uint32_t FILTER_MASK: 8;  /* layer color filter mask */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_FILTER_Type;

/* @SVD:register=VL_SRC,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="VL_SRC" */
/* VL_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="source image RGB data address[31:0]. For RGB565 format, address should be aligned to halfword. For ARGB8888 format, address should be aligned to word." */
        volatile uint32_t ADDR: 32;  /* source image RGB data address[31:0]. For RGB565 format, addr */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_SRC_Type;

/* @SVD:register=VL_ROT,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="VL_ROT" */
/* VL_ROT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CALC_REQ,bitOffset=0,bitWidth=1,description="rot_max_col and rot_max_line calculation request. Write 1 to trigger the calculation." */
        volatile uint32_t CALC_REQ: 1;  /* rot_max_col and rot_max_line calculation request. Write 1 to */
        /* @SVD:field=CALC_CLR,bitOffset=1,bitWidth=1,description="rot_max_col and rot_max_line calculation clear request. Write 1 to clear the result." */
        volatile uint32_t CALC_CLR: 1;  /* rot_max_col and rot_max_line calculation clear request. Writ */
        /* @SVD:field=ROT_DEG,bitOffset=2,bitWidth=9,description="rotation degree, rotation is clockwise." */
        volatile uint32_t ROT_DEG: 9;  /* rotation degree, rotation is clockwise. */
        /* @SVD:field=CALC_DONE,bitOffset=11,bitWidth=1,description="calculation done indicator" */
        volatile uint32_t CALC_DONE: 1;  /* calculation done indicator */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_ROT_Type;

/* @SVD:register=VL_ROT_STAT,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="VL_ROT_STAT" */
/* VL_ROT_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ROT_MAX_LINE,bitOffset=0,bitWidth=11,description="max line of rotated image" */
        volatile uint32_t ROT_MAX_LINE: 11;  /* max line of rotated image */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=ROT_MAX_COL,bitOffset=16,bitWidth=11,description="max column of rotated image" */
        volatile uint32_t ROT_MAX_COL: 11;  /* max column of rotated image */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_ROT_STAT_Type;

/* @SVD:register=VL_SCALE_RATIO_H,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="VL_SCALE_RATIO_H" */
/* VL_SCALE_RATIO_H 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=XPITCH,bitOffset=0,bitWidth=26,description="x-axis rescaling ration, 10.16 fixed point number, XPITCH lt MAX_COL/(X1-X0)" */
        volatile uint32_t XPITCH: 26;  /* x-axis rescaling ration, 10.16 fixed point number, XPITCH lt */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_SCALE_RATIO_H_Type;

/* @SVD:register=VL_SCALE_RATIO_V,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="VL_SCALE_RATIO_V" */
/* VL_SCALE_RATIO_V 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=YPITCH,bitOffset=0,bitWidth=26,description="y-axis rescaling ratio, 10.16 fixed point number, YPITCH lt MAX_LINE/(Y1-Y0)" */
        volatile uint32_t YPITCH: 26;  /* y-axis rescaling ratio, 10.16 fixed point number, YPITCH lt  */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_SCALE_RATIO_V_Type;

/* @SVD:register=VL_FILL,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="VL_FILL" */
/* VL_FILL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BG_B,bitOffset=0,bitWidth=8,description="background b color" */
        volatile uint32_t BG_B: 8;  /* background b color */
        /* @SVD:field=BG_G,bitOffset=8,bitWidth=8,description="background g color" */
        volatile uint32_t BG_G: 8;  /* background g color */
        /* @SVD:field=BG_R,bitOffset=16,bitWidth=8,description="background r color" */
        volatile uint32_t BG_R: 8;  /* background r color */
        /* @SVD:field=BG_MODE,bitOffset=24,bitWidth=1,description="Not used." */
        volatile uint32_t BG_MODE: 1;  /* Not used. */
        /* @SVD:field=ENDIAN,bitOffset=25,bitWidth=1,description="input 565 data format endian\n              0: {R[4:0], G[5:3], G[2:0], B[4:0]}\n              1: {G[2:0], R[4:0], B[4:0], G[5:3]}" */
        volatile uint32_t ENDIAN: 1;  /* input 565 data format endian               0: {R[4:0], G[5:3 */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_FILL_Type;

/* @SVD:register=VL_MISC_CFG,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="VL_MISC_CFG" */
/* VL_MISC_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLUT_SEL,bitOffset=0,bitWidth=1,description="VL CLUT select:\n              1'h1: select pallette1\n              1'h0: select pallette0" */
        volatile uint32_t CLUT_SEL: 1;  /* VL CLUT select:               1'h1: select pallette1         */
        /* @SVD:field=V_MIRROR,bitOffset=1,bitWidth=1,description="vertical mirror enable" */
        volatile uint32_t V_MIRROR: 1;  /* vertical mirror enable */
        /* @SVD:field=H_MIRROR,bitOffset=2,bitWidth=1,description="horizontal mirror enable" */
        volatile uint32_t H_MIRROR: 1;  /* horizontal mirror enable */
        /* @SVD:field=COS_FORCE_VALUE,bitOffset=3,bitWidth=13,description="external absolute value of cos. U13.12 format." */
        volatile uint32_t COS_FORCE_VALUE: 13;  /* external absolute value of cos. U13.12 format. */
        /* @SVD:field=SIN_FORCE_VALUE,bitOffset=16,bitWidth=13,description="external absolute value of sin. U13.12 format." */
        volatile uint32_t SIN_FORCE_VALUE: 13;  /* external absolute value of sin. U13.12 format. */
        /* @SVD:field=DEG_FORCE,bitOffset=29,bitWidth=1,description="force epic use external sin and cos value, quadrant is still calculated from ROT_DEG." */
        volatile uint32_t DEG_FORCE: 1;  /* force epic use external sin and cos value, quadrant is still */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_MISC_CFG_Type;

/* @SVD:register=L0_CFG,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="L0_CFG" */
/* L0_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FORMAT,bitOffset=0,bitWidth=4,description="layer input format\n              4'h0: RGB565\n              4'h1: RGB888\n              4'h2: ARGB8888\n              4'h3: ARGB8565\n              4'h4: A8\n              4'h5: A4\n              4'h6: L8\n              4'h7: A2" */
        volatile uint32_t FORMAT: 4;  /* layer input format               4'h0: RGB565                */
        /* @SVD:field=ALPHA_SEL,bitOffset=4,bitWidth=1,description="alpha selection\n              1'b0: select alpha according to image format\n              1'b1: select layer alpha" */
        volatile uint32_t ALPHA_SEL: 1;  /* alpha selection               1'b0: select alpha according t */
        /* @SVD:field=ALPHA,bitOffset=5,bitWidth=8,description="layer alpha value" */
        volatile uint32_t ALPHA: 8;  /* layer alpha value */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=FILTER_EN,bitOffset=15,bitWidth=1,description="layer color filter enable" */
        volatile uint32_t FILTER_EN: 1;  /* layer color filter enable */
        /* @SVD:field=WIDTH,bitOffset=16,bitWidth=13,description="source image width(including padding), unit is bytes" */
        volatile uint32_t WIDTH: 13;  /* source image width(including padding), unit is bytes */
        /* @SVD:field=PREFETCH_EN,bitOffset=29,bitWidth=1,description="preload 64 bytes extra data when reading pixel from memory" */
        volatile uint32_t PREFETCH_EN: 1;  /* preload 64 bytes extra data when reading pixel from memory */
        /* @SVD:field=ACTIVE,bitOffset=30,bitWidth=1,description="layer active flag" */
        volatile uint32_t ACTIVE: 1;  /* layer active flag */
        /* @SVD:field=ALPHA_BLEND,bitOffset=31,bitWidth=1,description="set 1 to enable alpha blending mode. Use layer alpha as blending factor for image with Alpha.\n              Alpha_out = Layer_alpha * Image_alpha" */
        volatile uint32_t ALPHA_BLEND: 1;  /* set 1 to enable alpha blending mode. Use layer alpha as blen */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L0_CFG_Type;

/* @SVD:register=L0_TL_POS,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="L0_TL_POS" */
/* L0_TL_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X0,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X0: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y0,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y0: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L0_TL_POS_Type;

/* @SVD:register=L0_BR_POS,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="L0_BR_POS" */
/* L0_BR_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X1,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X1: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y1,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y1: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L0_BR_POS_Type;

/* @SVD:register=L0_FILTER,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="L0_FILTER" */
/* L0_FILTER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FILTER_B,bitOffset=0,bitWidth=8,description="filter b color" */
        volatile uint32_t FILTER_B: 8;  /* filter b color */
        /* @SVD:field=FILTER_G,bitOffset=8,bitWidth=8,description="filter g color" */
        volatile uint32_t FILTER_G: 8;  /* filter g color */
        /* @SVD:field=FILTER_R,bitOffset=16,bitWidth=8,description="filter r color" */
        volatile uint32_t FILTER_R: 8;  /* filter r color */
        /* @SVD:field=FILTER_MASK,bitOffset=24,bitWidth=8,description="layer color filter mask" */
        volatile uint32_t FILTER_MASK: 8;  /* layer color filter mask */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L0_FILTER_Type;

/* @SVD:register=L0_SRC,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="L0_SRC" */
/* L0_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="source image RGB data address[31:0]. For RGB565 format, address should be aligned to halfword. For ARGB8888 format, address should be aligned to word." */
        volatile uint32_t ADDR: 32;  /* source image RGB data address[31:0]. For RGB565 format, addr */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L0_SRC_Type;

/* @SVD:register=L0_FILL,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="L0_FILL" */
/* L0_FILL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BG_B,bitOffset=0,bitWidth=8,description="background b color" */
        volatile uint32_t BG_B: 8;  /* background b color */
        /* @SVD:field=BG_G,bitOffset=8,bitWidth=8,description="background g color" */
        volatile uint32_t BG_G: 8;  /* background g color */
        /* @SVD:field=BG_R,bitOffset=16,bitWidth=8,description="background r color" */
        volatile uint32_t BG_R: 8;  /* background r color */
        /* @SVD:field=BG_MODE,bitOffset=24,bitWidth=1,description="Not used." */
        volatile uint32_t BG_MODE: 1;  /* Not used. */
        /* @SVD:field=ENDIAN,bitOffset=25,bitWidth=1,description="input 565 data format endian\n              0: {R[4:0], G[5:3], G[2:0], B[4:0]}\n              1: {G[2:0], R[4:0], B[4:0], G[5:3]}" */
        volatile uint32_t ENDIAN: 1;  /* input 565 data format endian               0: {R[4:0], G[5:3 */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L0_FILL_Type;

/* @SVD:register=L0_MISC_CFG,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="L0_MISC_CFG" */
/* L0_MISC_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLUT_SEL,bitOffset=0,bitWidth=1,description="L0 CLUT select:\n              1'h1: select pallette1\n              1'h0: select pallette0" */
        volatile uint32_t CLUT_SEL: 1;  /* L0 CLUT select:               1'h1: select pallette1         */
        /* @SVD:field=V_MIRROR,bitOffset=1,bitWidth=1,description="vertical mirror enable" */
        volatile uint32_t V_MIRROR: 1;  /* vertical mirror enable */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L0_MISC_CFG_Type;

/* @SVD:register=L1_CFG,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="L1_CFG" */
/* L1_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FORMAT,bitOffset=0,bitWidth=4,description="layer input format\n              4'h0: RGB565\n              4'h1: RGB888\n              4'h2: ARGB8888\n              4'h3: ARGB8565\n              4'h4: A8\n              4'h5: A4\n              4'h6: L8\n              4'h7: A2" */
        volatile uint32_t FORMAT: 4;  /* layer input format               4'h0: RGB565                */
        /* @SVD:field=ALPHA_SEL,bitOffset=4,bitWidth=1,description="alpha selection\n              1'b0: select alpha according to image format\n              1'b1: select layer alpha" */
        volatile uint32_t ALPHA_SEL: 1;  /* alpha selection               1'b0: select alpha according t */
        /* @SVD:field=ALPHA,bitOffset=5,bitWidth=8,description="layer alpha value" */
        volatile uint32_t ALPHA: 8;  /* layer alpha value */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=FILTER_EN,bitOffset=15,bitWidth=1,description="layer color filter enable" */
        volatile uint32_t FILTER_EN: 1;  /* layer color filter enable */
        /* @SVD:field=WIDTH,bitOffset=16,bitWidth=13,description="source image width(including padding), unit is bytes" */
        volatile uint32_t WIDTH: 13;  /* source image width(including padding), unit is bytes */
        /* @SVD:field=PREFETCH_EN,bitOffset=29,bitWidth=1,description="preload 64 bytes extra data when reading pixel from memory" */
        volatile uint32_t PREFETCH_EN: 1;  /* preload 64 bytes extra data when reading pixel from memory */
        /* @SVD:field=ACTIVE,bitOffset=30,bitWidth=1,description="layer active flag" */
        volatile uint32_t ACTIVE: 1;  /* layer active flag */
        /* @SVD:field=ALPHA_BLEND,bitOffset=31,bitWidth=1,description="set 1 to enable alpha blending mode. Use layer alpha as blending factor for image with Alpha.\n              Alpha_out = Layer_alpha * Image_alpha" */
        volatile uint32_t ALPHA_BLEND: 1;  /* set 1 to enable alpha blending mode. Use layer alpha as blen */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L1_CFG_Type;

/* @SVD:register=L1_TL_POS,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="L1_TL_POS" */
/* L1_TL_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X0,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X0: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y0,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y0: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L1_TL_POS_Type;

/* @SVD:register=L1_BR_POS,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="L1_BR_POS" */
/* L1_BR_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X1,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X1: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y1,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y1: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L1_BR_POS_Type;

/* @SVD:register=L1_FILTER,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="L1_FILTER" */
/* L1_FILTER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FILTER_B,bitOffset=0,bitWidth=8,description="filter b color" */
        volatile uint32_t FILTER_B: 8;  /* filter b color */
        /* @SVD:field=FILTER_G,bitOffset=8,bitWidth=8,description="filter g color" */
        volatile uint32_t FILTER_G: 8;  /* filter g color */
        /* @SVD:field=FILTER_R,bitOffset=16,bitWidth=8,description="filter r color" */
        volatile uint32_t FILTER_R: 8;  /* filter r color */
        /* @SVD:field=FILTER_MASK,bitOffset=24,bitWidth=8,description="layer color filter mask" */
        volatile uint32_t FILTER_MASK: 8;  /* layer color filter mask */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L1_FILTER_Type;

/* @SVD:register=L1_SRC,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="L1_SRC" */
/* L1_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="source image RGB data address[31:0]. For RGB565 format, address should be aligned to halfword. For ARGB8888 format, address should be aligned to word." */
        volatile uint32_t ADDR: 32;  /* source image RGB data address[31:0]. For RGB565 format, addr */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L1_SRC_Type;

/* @SVD:register=L1_FILL,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="L1_FILL" */
/* L1_FILL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BG_B,bitOffset=0,bitWidth=8,description="background b color" */
        volatile uint32_t BG_B: 8;  /* background b color */
        /* @SVD:field=BG_G,bitOffset=8,bitWidth=8,description="background g color" */
        volatile uint32_t BG_G: 8;  /* background g color */
        /* @SVD:field=BG_R,bitOffset=16,bitWidth=8,description="background r color" */
        volatile uint32_t BG_R: 8;  /* background r color */
        /* @SVD:field=BG_MODE,bitOffset=24,bitWidth=1,description="Not used." */
        volatile uint32_t BG_MODE: 1;  /* Not used. */
        /* @SVD:field=ENDIAN,bitOffset=25,bitWidth=1,description="input 565 data format endian\n              0: {R[4:0], G[5:3], G[2:0], B[4:0]}\n              1: {G[2:0], R[4:0], B[4:0], G[5:3]}" */
        volatile uint32_t ENDIAN: 1;  /* input 565 data format endian               0: {R[4:0], G[5:3 */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L1_FILL_Type;

/* @SVD:register=L1_MISC_CFG,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="L1_MISC_CFG" */
/* L1_MISC_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLUT_SEL,bitOffset=0,bitWidth=1,description="L1 CLUT select:\n              1'h1: select pallette1\n              1'h0: select pallette0" */
        volatile uint32_t CLUT_SEL: 1;  /* L1 CLUT select:               1'h1: select pallette1         */
        /* @SVD:field=V_MIRROR,bitOffset=1,bitWidth=1,description="vertical mirror enable" */
        volatile uint32_t V_MIRROR: 1;  /* vertical mirror enable */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L1_MISC_CFG_Type;

/* @SVD:register=L2_CFG,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="L2_CFG" */
/* L2_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FORMAT,bitOffset=0,bitWidth=4,description="layer input format\n              4'h0: RGB565\n              4'h1: RGB888\n              4'h2: ARGB8888\n              4'h3: ARGB8565\n              4'h4: A8\n              4'h5: A4\n              4'h6: L8\n              4'h7: A2" */
        volatile uint32_t FORMAT: 4;  /* layer input format               4'h0: RGB565                */
        /* @SVD:field=ALPHA_SEL,bitOffset=4,bitWidth=1,description="alpha selection\n              1'b0: select alpha according to image format\n              1'b1: select layer alpha" */
        volatile uint32_t ALPHA_SEL: 1;  /* alpha selection               1'b0: select alpha according t */
        /* @SVD:field=ALPHA,bitOffset=5,bitWidth=8,description="layer alpha value" */
        volatile uint32_t ALPHA: 8;  /* layer alpha value */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=FILTER_EN,bitOffset=15,bitWidth=1,description="layer color filter enable" */
        volatile uint32_t FILTER_EN: 1;  /* layer color filter enable */
        /* @SVD:field=WIDTH,bitOffset=16,bitWidth=13,description="source image width(including padding), unit is bytes" */
        volatile uint32_t WIDTH: 13;  /* source image width(including padding), unit is bytes */
        /* @SVD:field=PREFETCH_EN,bitOffset=29,bitWidth=1,description="preload 64 bytes extra data when reading pixel from memory" */
        volatile uint32_t PREFETCH_EN: 1;  /* preload 64 bytes extra data when reading pixel from memory */
        /* @SVD:field=ACTIVE,bitOffset=30,bitWidth=1,description="layer active flag" */
        volatile uint32_t ACTIVE: 1;  /* layer active flag */
        /* @SVD:field=ALPHA_BLEND,bitOffset=31,bitWidth=1,description="set 1 to enable alpha blending mode. Use layer alpha as blending factor for image with Alpha.\n              Alpha_out = Layer_alpha * Image_alpha" */
        volatile uint32_t ALPHA_BLEND: 1;  /* set 1 to enable alpha blending mode. Use layer alpha as blen */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_CFG_Type;

/* @SVD:register=L2_TL_POS,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="L2_TL_POS" */
/* L2_TL_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X0,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X0: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y0,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y0: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_TL_POS_Type;

/* @SVD:register=L2_BR_POS,offset=0x0098,size=32,access=read-write,reset=0x00000000,description="L2_BR_POS" */
/* L2_BR_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X1,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X1: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y1,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y1: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_BR_POS_Type;

/* @SVD:register=L2_EXTENTS,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="L2_EXTENTS" */
/* L2_EXTENTS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MAX_LINE,bitOffset=0,bitWidth=10,description="number of pixels of each column of source image(not including padding)" */
        volatile uint32_t MAX_LINE: 10;  /* number of pixels of each column of source image(not includin */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=MAX_COL,bitOffset=16,bitWidth=10,description="number of pixels of each line of source image(not including padding)" */
        volatile uint32_t MAX_COL: 10;  /* number of pixels of each line of source image(not including  */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_EXTENTS_Type;

/* @SVD:register=L2_FILTER,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="L2_FILTER" */
/* L2_FILTER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FILTER_B,bitOffset=0,bitWidth=8,description="filter b color" */
        volatile uint32_t FILTER_B: 8;  /* filter b color */
        /* @SVD:field=FILTER_G,bitOffset=8,bitWidth=8,description="filter g color" */
        volatile uint32_t FILTER_G: 8;  /* filter g color */
        /* @SVD:field=FILTER_R,bitOffset=16,bitWidth=8,description="filter r color" */
        volatile uint32_t FILTER_R: 8;  /* filter r color */
        /* @SVD:field=FILTER_MASK,bitOffset=24,bitWidth=8,description="layer color filter mask" */
        volatile uint32_t FILTER_MASK: 8;  /* layer color filter mask */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_FILTER_Type;

/* @SVD:register=L2_SRC,offset=0x00A4,size=32,access=read-write,reset=0x00000000,description="L2_SRC" */
/* L2_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="source image RGB data address[31:0]. For RGB565 format, address should be aligned to halfword. For ARGB8888 format, address should be aligned to word." */
        volatile uint32_t ADDR: 32;  /* source image RGB data address[31:0]. For RGB565 format, addr */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_SRC_Type;

/* @SVD:register=L2_ROT,offset=0x00A8,size=32,access=read-write,reset=0x00000000,description="L2_ROT" */
/* L2_ROT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CALC_REQ,bitOffset=0,bitWidth=1,description="rot_max_col and rot_max_line calculation request. Write 1 to trigger the calculation." */
        volatile uint32_t CALC_REQ: 1;  /* rot_max_col and rot_max_line calculation request. Write 1 to */
        /* @SVD:field=CALC_CLR,bitOffset=1,bitWidth=1,description="rot_max_col and rot_max_line calculation clear request. Write 1 to clear the result." */
        volatile uint32_t CALC_CLR: 1;  /* rot_max_col and rot_max_line calculation clear request. Writ */
        /* @SVD:field=ROT_DEG,bitOffset=2,bitWidth=9,description="rotation degree, rotation is clockwise." */
        volatile uint32_t ROT_DEG: 9;  /* rotation degree, rotation is clockwise. */
        /* @SVD:field=CALC_DONE,bitOffset=11,bitWidth=1,description="calculation done indicator" */
        volatile uint32_t CALC_DONE: 1;  /* calculation done indicator */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_ROT_Type;

/* @SVD:register=L2_ROT_STAT,offset=0x00AC,size=32,access=read-write,reset=0x00000000,description="L2_ROT_STAT" */
/* L2_ROT_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ROT_MAX_LINE,bitOffset=0,bitWidth=11,description="max line of rotated image" */
        volatile uint32_t ROT_MAX_LINE: 11;  /* max line of rotated image */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=ROT_MAX_COL,bitOffset=16,bitWidth=11,description="max column of rotated image" */
        volatile uint32_t ROT_MAX_COL: 11;  /* max column of rotated image */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_ROT_STAT_Type;

/* @SVD:register=L2_SCALE_RATIO_H,offset=0x00B0,size=32,access=read-write,reset=0x00000000,description="L2_SCALE_RATIO_H" */
/* L2_SCALE_RATIO_H 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=XPITCH,bitOffset=0,bitWidth=26,description="x-axis rescaling ration, 10.16 fixed point number, XPITCH lt MAX_COL/(X1-X0)" */
        volatile uint32_t XPITCH: 26;  /* x-axis rescaling ration, 10.16 fixed point number, XPITCH lt */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_SCALE_RATIO_H_Type;

/* @SVD:register=L2_SCALE_RATIO_V,offset=0x00B4,size=32,access=read-write,reset=0x00000000,description="L2_SCALE_RATIO_V" */
/* L2_SCALE_RATIO_V 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=YPITCH,bitOffset=0,bitWidth=26,description="y-axis rescaling ratio, 10.16 fixed point number, YPITCH lt MAX_LINE/(Y1-Y0)" */
        volatile uint32_t YPITCH: 26;  /* y-axis rescaling ratio, 10.16 fixed point number, YPITCH lt  */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_SCALE_RATIO_V_Type;

/* @SVD:register=L2_FILL,offset=0x00B8,size=32,access=read-write,reset=0x00000000,description="L2_FILL" */
/* L2_FILL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BG_B,bitOffset=0,bitWidth=8,description="background b color" */
        volatile uint32_t BG_B: 8;  /* background b color */
        /* @SVD:field=BG_G,bitOffset=8,bitWidth=8,description="background g color" */
        volatile uint32_t BG_G: 8;  /* background g color */
        /* @SVD:field=BG_R,bitOffset=16,bitWidth=8,description="background r color" */
        volatile uint32_t BG_R: 8;  /* background r color */
        /* @SVD:field=BG_MODE,bitOffset=24,bitWidth=1,description="Not used." */
        volatile uint32_t BG_MODE: 1;  /* Not used. */
        /* @SVD:field=ENDIAN,bitOffset=25,bitWidth=1,description="input 565 data format endian\n              0: {R[4:0], G[5:3], G[2:0], B[4:0]}\n              1: {G[2:0], R[4:0], B[4:0], G[5:3]}" */
        volatile uint32_t ENDIAN: 1;  /* input 565 data format endian               0: {R[4:0], G[5:3 */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_FILL_Type;

/* @SVD:register=L2_MISC_CFG,offset=0x00BC,size=32,access=read-write,reset=0x00000000,description="L2_MISC_CFG" */
/* L2_MISC_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLUT_SEL,bitOffset=0,bitWidth=1,description="L2 CLUT select:\n              1'h1: select pallette1\n              1'h0: select pallette0" */
        volatile uint32_t CLUT_SEL: 1;  /* L2 CLUT select:               1'h1: select pallette1         */
        /* @SVD:field=V_MIRROR,bitOffset=1,bitWidth=1,description="vertical mirror enable" */
        volatile uint32_t V_MIRROR: 1;  /* vertical mirror enable */
        /* @SVD:field=H_MIRROR,bitOffset=2,bitWidth=1,description="horizontal mirror enable" */
        volatile uint32_t H_MIRROR: 1;  /* horizontal mirror enable */
        /* @SVD:field=COS_FORCE_VALUE,bitOffset=3,bitWidth=13,description="external absolute value of cos. U13.12 format." */
        volatile uint32_t COS_FORCE_VALUE: 13;  /* external absolute value of cos. U13.12 format. */
        /* @SVD:field=SIN_FORCE_VALUE,bitOffset=16,bitWidth=13,description="external absolute value of sin. U13.12 format." */
        volatile uint32_t SIN_FORCE_VALUE: 13;  /* external absolute value of sin. U13.12 format. */
        /* @SVD:field=DEG_FORCE,bitOffset=29,bitWidth=1,description="force epic use external sin and cos value, quadrant is still calculated from ROT_DEG." */
        volatile uint32_t DEG_FORCE: 1;  /* force epic use external sin and cos value, quadrant is still */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_MISC_CFG_Type;

/* @SVD:register=MASK_CFG,offset=0x00C0,size=32,access=read-write,reset=0x00000000,description="MASK_CFG" */
/* MASK_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FORMAT,bitOffset=0,bitWidth=1,description="mask input format\n              1'h0: A8\n              1'h1: A4" */
        volatile uint32_t FORMAT: 1;  /* mask input format               1'h0: A8               1'h1: */
        /* @SVD:field=MIX_MODE,bitOffset=1,bitWidth=1,description="mask mix mode\n              1'h0: mult mode\n              1'h1: overwrite mode" */
        volatile uint32_t MIX_MODE: 1;  /* mask mix mode               1'h0: mult mode               1' */
        /* @SVD:field=L0_MASK_EN,bitOffset=2,bitWidth=1,description="layer0 mask enable" */
        volatile uint32_t L0_MASK_EN: 1;  /* layer0 mask enable */
        /* @SVD:field=L1_MASK_EN,bitOffset=3,bitWidth=1,description="layer1 mask enable" */
        volatile uint32_t L1_MASK_EN: 1;  /* layer1 mask enable */
        /* @SVD:field=L2_MASK_EN,bitOffset=4,bitWidth=1,description="layer2 mask enable" */
        volatile uint32_t L2_MASK_EN: 1;  /* layer2 mask enable */
        /* @SVD:field=VL_MASK_EN,bitOffset=5,bitWidth=1,description="video layer mask enable" */
        volatile uint32_t VL_MASK_EN: 1;  /* video layer mask enable */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=WIDTH,bitOffset=14,bitWidth=13,description="source image width(including padding), unit is bytes" */
        volatile uint32_t WIDTH: 13;  /* source image width(including padding), unit is bytes */
        /* @SVD:field=PREFETCH_EN,bitOffset=27,bitWidth=1,description="preload 64 bytes extra data when reading pixel from memory" */
        volatile uint32_t PREFETCH_EN: 1;  /* preload 64 bytes extra data when reading pixel from memory */
        /* @SVD:field=ACTIVE,bitOffset=28,bitWidth=1,description="layer active flag" */
        volatile uint32_t ACTIVE: 1;  /* layer active flag */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_MASK_CFG_Type;

/* @SVD:register=MASK_TL_POS,offset=0x00C4,size=32,access=read-write,reset=0x00000000,description="MASK_TL_POS" */
/* MASK_TL_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X0,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X0: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y0,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y0: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_MASK_TL_POS_Type;

/* @SVD:register=MASK_BR_POS,offset=0x00C8,size=32,access=read-write,reset=0x00000000,description="MASK_BR_POS" */
/* MASK_BR_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X1,bitOffset=0,bitWidth=10,description="Coordinate X-value" */
        volatile uint32_t X1: 10;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=Y1,bitOffset=16,bitWidth=10,description="Coordingate Y-value" */
        volatile uint32_t Y1: 10;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_MASK_BR_POS_Type;

/* @SVD:register=MASK_SRC,offset=0x00CC,size=32,access=read-write,reset=0x00000000,description="MASK_SRC" */
/* MASK_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="mask data address[31:0]. This is byte address, even for A4, this address is byte aligned." */
        volatile uint32_t ADDR: 32;  /* mask data address[31:0]. This is byte address, even for A4,  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_MASK_SRC_Type;

/* @SVD:register=COENG_CFG,offset=0x00D0,size=32,access=read-write,reset=0x00000000,description="COENG_CFG" */
/* COENG_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EZIP_EN,bitOffset=0,bitWidth=1,description="ezip enable" */
        volatile uint32_t EZIP_EN: 1;  /* ezip enable */
        /* @SVD:field=EZIP_CH_SEL,bitOffset=1,bitWidth=2,description="ezip channel select" */
        volatile uint32_t EZIP_CH_SEL: 2;  /* ezip channel select */
        /* @SVD:field=YUV_EN,bitOffset=3,bitWidth=1,description="yuv enable" */
        volatile uint32_t YUV_EN: 1;  /* yuv enable */
        /* @SVD:field=YUV_CH_SEL,bitOffset=4,bitWidth=2,description="yuv engine channel select" */
        volatile uint32_t YUV_CH_SEL: 2;  /* yuv engine channel select */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_COENG_CFG_Type;

/* @SVD:register=YUV_ENG_CFG0,offset=0x00D4,size=32,access=read-write,reset=0x00000000,description="YUV_ENG_CFG0" */
/* YUV_ENG_CFG0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WIDTH_U,bitOffset=0,bitWidth=13,description="yuv u vector line width, unit is bytes" */
        volatile uint32_t WIDTH_U: 13;  /* yuv u vector line width, unit is bytes */
        /* @SVD:field=WIDTH_Y,bitOffset=13,bitWidth=13,description="yuv y vector line width, unit is bytes" */
        volatile uint32_t WIDTH_Y: 13;  /* yuv y vector line width, unit is bytes */
        /* @SVD:field=FORMAT,bitOffset=26,bitWidth=3,description="yuv format" */
        volatile uint32_t FORMAT: 3;  /* yuv format */
        /* @SVD:field=RANGE_SEL,bitOffset=29,bitWidth=1,description="yuv input range\n              1'h0: tv range\n              1'h1: pc range" */
        volatile uint32_t RANGE_SEL: 1;  /* yuv input range               1'h0: tv range               1 */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_YUV_ENG_CFG0_Type;

/* @SVD:register=YUV_ENG_CFG1,offset=0x00D8,size=32,access=read-write,reset=0x00000000,description="YUV_ENG_CFG1" */
/* YUV_ENG_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WIDTH_V,bitOffset=0,bitWidth=13,description="yuv v vector line width, unit is bytes" */
        volatile uint32_t WIDTH_V: 13;  /* yuv v vector line width, unit is bytes */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_YUV_ENG_CFG1_Type;

/* @SVD:register=Y_SRC,offset=0x00DC,size=32,access=read-write,reset=0x00000000,description="Y_SRC" */
/* Y_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="y vector address" */
        volatile uint32_t ADDR: 32;  /* y vector address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_Y_SRC_Type;

/* @SVD:register=U_SRC,offset=0x00E0,size=32,access=read-write,reset=0x00000000,description="U_SRC" */
/* U_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="u vector address" */
        volatile uint32_t ADDR: 32;  /* u vector address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_U_SRC_Type;

/* @SVD:register=V_SRC,offset=0x00E4,size=32,access=read-write,reset=0x00000000,description="V_SRC" */
/* V_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="v vector address" */
        volatile uint32_t ADDR: 32;  /* v vector address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_V_SRC_Type;

/* @SVD:register=COEF0,offset=0x00E8,size=32,access=read-write,reset=0x00000000,description="COEF0" */
/* COEF0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FUB,bitOffset=0,bitWidth=10,description="YUV Fub coef" */
        volatile uint32_t FUB: 10;  /* YUV Fub coef */
        /* @SVD:field=FUG,bitOffset=10,bitWidth=10,description="YUV Fug coef" */
        volatile uint32_t FUG: 10;  /* YUV Fug coef */
        /* @SVD:field=FY,bitOffset=20,bitWidth=10,description="YUV Fy coef" */
        volatile uint32_t FY: 10;  /* YUV Fy coef */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_COEF0_Type;

/* @SVD:register=COEF1,offset=0x00EC,size=32,access=read-write,reset=0x00000000,description="COEF1" */
/* COEF1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FVG,bitOffset=0,bitWidth=10,description="YUV Fvg coef" */
        volatile uint32_t FVG: 10;  /* YUV Fvg coef */
        /* @SVD:field=FVR,bitOffset=10,bitWidth=10,description="YUV Fvr coef" */
        volatile uint32_t FVR: 10;  /* YUV Fvr coef */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_COEF1_Type;

/* @SVD:register=DITHER_CONF,offset=0x00F0,size=32,access=read-write,reset=0x00000000,description="DITHER_CONF" */
/* DITHER_CONF 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="dither enable" */
        volatile uint32_t EN: 1;  /* dither enable */
        /* @SVD:field=W_B,bitOffset=1,bitWidth=3,description="blue dither width" */
        volatile uint32_t W_B: 3;  /* blue dither width */
        /* @SVD:field=W_G,bitOffset=4,bitWidth=3,description="green dither width" */
        volatile uint32_t W_G: 3;  /* green dither width */
        /* @SVD:field=W_R,bitOffset=7,bitWidth=3,description="red dither width" */
        volatile uint32_t W_R: 3;  /* red dither width */
        /* @SVD:field=LFSR_LOAD_SEL,bitOffset=10,bitWidth=2,description="select lfsr\n              0: none\n              1: red\n              2: green\n              3: blue" */
        volatile uint32_t LFSR_LOAD_SEL: 2;  /* select lfsr               0: none               1: red       */
        /* @SVD:field=LFSR_LOAD,bitOffset=12,bitWidth=1,description="load lfsr init value" */
        volatile uint32_t LFSR_LOAD: 1;  /* load lfsr init value */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_DITHER_CONF_Type;

/* @SVD:register=DITHER_LFSR,offset=0x00F4,size=32,access=read-write,reset=0x00000000,description="DITHER_LFSR" */
/* DITHER_LFSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INIT_VAL,bitOffset=0,bitWidth=32,description="lfsr init load value" */
        volatile uint32_t INIT_VAL: 32;  /* lfsr init load value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_DITHER_LFSR_Type;

/* @SVD:register=AHB_CTRL,offset=0x00F8,size=32,access=read-write,reset=0x00000000,description="AHB_CTRL" */
/* AHB_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DESTINATION,bitOffset=0,bitWidth=1,description="The Data can be sent to two destinations:\n              2'b0: AHB RAM\n              2'b1: AHB LCD" */
        volatile uint32_t DESTINATION: 1;  /* The Data can be sent to two destinations:               2'b0 */
        /* @SVD:field=O_FORMAT,bitOffset=1,bitWidth=2,description="AHB output format:\n              2'h0: RGB565\n              2'h1: RGB888\n              2'h2: ARGB8888\n              2'h3: ARGB8565" */
        volatile uint32_t O_FORMAT: 2;  /* AHB output format:               2'h0: RGB565                */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=29,description="" */
        volatile uint32_t RSVD: 29;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_AHB_CTRL_Type;

/* @SVD:register=AHB_MEM,offset=0x00FC,size=32,access=read-write,reset=0x00000000,description="AHB_MEM" */
/* AHB_MEM 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="AHB RAM/AHB LCD target address" */
        volatile uint32_t ADDR: 32;  /* AHB RAM/AHB LCD target address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_AHB_MEM_Type;

/* @SVD:register=AHB_STRIDE,offset=0x0100,size=32,access=read-write,reset=0x00000000,description="AHB_STRIDE" */
/* AHB_STRIDE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OFFSET,bitOffset=0,bitWidth=16,description="" */
        volatile uint32_t OFFSET: 16;
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_AHB_STRIDE_Type;

/* @SVD:register=DEBUG,offset=0x0104,size=32,access=read-write,reset=0x00000000,description="DEBUG" */
/* DEBUG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DEBUG_OUT_SEL,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t DEBUG_OUT_SEL: 4;
        /* @SVD:field=DEBUG_INT_SEL,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t DEBUG_INT_SEL: 4;
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
        /* @SVD:field=DEBUG_INT_DATA,bitOffset=16,bitWidth=16,description="4'h0: RSVD\n              4'h1: OL0 debug info\n              4'h2: OL1 debug info\n              4'h3: OL2 debug info\n              4'h4: VL debug info1\n              4'h5: VL debug info2\n              4'h6: ROI debug out\n              4'h7: mem intfa debug out\n              4'h8: mem intfb debug out\n              4'h9: ahb ctrl debug out\n              4'ha: ROI XX\n              4'hb: ROI YY\n              4'hc: EPIC_EZIP debug out\n              others: RSVD" */
        volatile uint32_t DEBUG_INT_DATA: 16;  /* 4'h0: RSVD               4'h1: OL0 debug info                */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_DEBUG_Type;

/* @SVD:register=VL_ROT_M_CFG1,offset=0x0108,size=32,access=read-write,reset=0x00000000,description="VL_ROT_M_CFG1" */
/* VL_ROT_M_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=M_ROT_MAX_LINE,bitOffset=0,bitWidth=11,description="manual mode rotation max line, unsigned value" */
        volatile uint32_t M_ROT_MAX_LINE: 11;  /* manual mode rotation max line, unsigned value */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=M_ROT_MAX_COL,bitOffset=16,bitWidth=11,description="manual mode rotation max column, unsigned value" */
        volatile uint32_t M_ROT_MAX_COL: 11;  /* manual mode rotation max column, unsigned value */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
        /* @SVD:field=M_MODE,bitOffset=31,bitWidth=1,description="rotation mode setting\n              1'b0: auto mode\n              1'b1: manual mode" */
        volatile uint32_t M_MODE: 1;  /* rotation mode setting               1'b0: auto mode          */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_ROT_M_CFG1_Type;

/* @SVD:register=VL_ROT_M_CFG2,offset=0x010C,size=32,access=read-write,reset=0x00000000,description="VL_ROT_M_CFG2" */
/* VL_ROT_M_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=M_PIVOT_X,bitOffset=0,bitWidth=11,description="manual mode pivot x, signed value, -1023~1023, -1024 is not supported" */
        volatile uint32_t M_PIVOT_X: 11;  /* manual mode pivot x, signed value, -1023~1023, -1024 is not  */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=M_PIVOT_Y,bitOffset=16,bitWidth=11,description="manual mode pivot y, signed value, -1023~1023, -1024 is not supported" */
        volatile uint32_t M_PIVOT_Y: 11;  /* manual mode pivot y, signed value, -1023~1023, -1024 is not  */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_ROT_M_CFG2_Type;

/* @SVD:register=VL_ROT_M_CFG3,offset=0x0110,size=32,access=read-write,reset=0x00000000,description="VL_ROT_M_CFG3" */
/* VL_ROT_M_CFG3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=M_XTL,bitOffset=0,bitWidth=11,description="manual mode top left x cordinate, signed value, -1023~1023, -1024 is not supported" */
        volatile uint32_t M_XTL: 11;  /* manual mode top left x cordinate, signed value, -1023~1023,  */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=M_YTL,bitOffset=16,bitWidth=11,description="manual mode top left y cordinate, signed value, -1023~1023, -1024 is not supported" */
        volatile uint32_t M_YTL: 11;  /* manual mode top left y cordinate, signed value, -1023~1023,  */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_ROT_M_CFG3_Type;

/* @SVD:register=VL_SCALE_INIT_CFG1,offset=0x0114,size=32,access=read-write,reset=0x00000000,description="VL_SCALE_INIT_CFG1" */
/* VL_SCALE_INIT_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X_VAL,bitOffset=0,bitWidth=26,description="x-axis scale initial value, 10.16 format" */
        volatile uint32_t X_VAL: 26;  /* x-axis scale initial value, 10.16 format */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_SCALE_INIT_CFG1_Type;

/* @SVD:register=VL_SCALE_INIT_CFG2,offset=0x0118,size=32,access=read-write,reset=0x00000000,description="VL_SCALE_INIT_CFG2" */
/* VL_SCALE_INIT_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=Y_VAL,bitOffset=0,bitWidth=26,description="y-axis scale initial value, 10.16 format" */
        volatile uint32_t Y_VAL: 26;  /* y-axis scale initial value, 10.16 format */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_SCALE_INIT_CFG2_Type;

/* @SVD:register=L2_ROT_M_CFG1,offset=0x011C,size=32,access=read-write,reset=0x00000000,description="L2_ROT_M_CFG1" */
/* L2_ROT_M_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=M_ROT_MAX_LINE,bitOffset=0,bitWidth=11,description="manual mode rotation max line, unsigned value" */
        volatile uint32_t M_ROT_MAX_LINE: 11;  /* manual mode rotation max line, unsigned value */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=M_ROT_MAX_COL,bitOffset=16,bitWidth=11,description="manual mode rotation max column, unsigned value" */
        volatile uint32_t M_ROT_MAX_COL: 11;  /* manual mode rotation max column, unsigned value */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
        /* @SVD:field=M_MODE,bitOffset=31,bitWidth=1,description="rotation mode setting\n              1'b0: auto mode\n              1'b1: manual mode" */
        volatile uint32_t M_MODE: 1;  /* rotation mode setting               1'b0: auto mode          */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_ROT_M_CFG1_Type;

/* @SVD:register=L2_ROT_M_CFG2,offset=0x0120,size=32,access=read-write,reset=0x00000000,description="L2_ROT_M_CFG2" */
/* L2_ROT_M_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=M_PIVOT_X,bitOffset=0,bitWidth=11,description="manual mode pivot x, signed value, -1023~1023, -1024 is not supported" */
        volatile uint32_t M_PIVOT_X: 11;  /* manual mode pivot x, signed value, -1023~1023, -1024 is not  */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=M_PIVOT_Y,bitOffset=16,bitWidth=11,description="manual mode pivot y, signed value, -1023~1023, -1024 is not supported" */
        volatile uint32_t M_PIVOT_Y: 11;  /* manual mode pivot y, signed value, -1023~1023, -1024 is not  */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_ROT_M_CFG2_Type;

/* @SVD:register=L2_ROT_M_CFG3,offset=0x0124,size=32,access=read-write,reset=0x00000000,description="L2_ROT_M_CFG3" */
/* L2_ROT_M_CFG3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=M_XTL,bitOffset=0,bitWidth=11,description="manual mode top left x cordinate, signed value, -1023~1023, -1024 is not supported" */
        volatile uint32_t M_XTL: 11;  /* manual mode top left x cordinate, signed value, -1023~1023,  */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=M_YTL,bitOffset=16,bitWidth=11,description="manual mode top left y cordinate, signed value, -1023~1023, -1024 is not supported" */
        volatile uint32_t M_YTL: 11;  /* manual mode top left y cordinate, signed value, -1023~1023,  */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_ROT_M_CFG3_Type;

/* @SVD:register=L2_SCALE_INIT_CFG1,offset=0x0128,size=32,access=read-write,reset=0x00000000,description="L2_SCALE_INIT_CFG1" */
/* L2_SCALE_INIT_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X_VAL,bitOffset=0,bitWidth=26,description="x-axis scale initial value, 10.16 format" */
        volatile uint32_t X_VAL: 26;  /* x-axis scale initial value, 10.16 format */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_SCALE_INIT_CFG1_Type;

/* @SVD:register=L2_SCALE_INIT_CFG2,offset=0x012C,size=32,access=read-write,reset=0x00000000,description="L2_SCALE_INIT_CFG2" */
/* L2_SCALE_INIT_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=Y_VAL,bitOffset=0,bitWidth=26,description="y-axis scale initial value, 10.16 format" */
        volatile uint32_t Y_VAL: 26;  /* y-axis scale initial value, 10.16 format */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_L2_SCALE_INIT_CFG2_Type;

/* @SVD:register=PERF_CNT,offset=0x0130,size=32,access=read-write,reset=0x00000000,description="PERF_CNT" */
/* PERF_CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="epic performance counter" */
        volatile uint32_t VAL: 32;  /* epic performance counter */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_PERF_CNT_Type;

/* @SVD:register=CANVAS_STAT,offset=0x0140,size=32,access=read-write,reset=0x00000000,description="CANVAS_STAT" */
/* CANVAS_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X_COR,bitOffset=0,bitWidth=10,description="canvas x cordinate" */
        volatile uint32_t X_COR: 10;  /* canvas x cordinate */
        /* @SVD:field=Y_COR,bitOffset=10,bitWidth=10,description="canvas y cordinate" */
        volatile uint32_t Y_COR: 10;  /* canvas y cordinate */
        /* @SVD:field=FIFO_CNT,bitOffset=20,bitWidth=3,description="pre calc fifo count" */
        volatile uint32_t FIFO_CNT: 3;  /* pre calc fifo count */
        /* @SVD:field=POSTC_STAT,bitOffset=23,bitWidth=3,description="postc_status" */
        volatile uint32_t POSTC_STAT: 3;  /* postc_status */
        /* @SVD:field=PREC_STAT,bitOffset=26,bitWidth=3,description="prec status" */
        volatile uint32_t PREC_STAT: 3;  /* prec status */
        /* @SVD:field=FETCH_STAT,bitOffset=29,bitWidth=3,description="fetch status" */
        volatile uint32_t FETCH_STAT: 3;  /* fetch status */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_CANVAS_STAT_Type;

/* @SVD:register=EZIP_STAT,offset=0x0144,size=32,access=read-write,reset=0x00000000,description="EZIP_STAT" */
/* EZIP_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LINE_CNT0,bitOffset=0,bitWidth=10,description="ezip engine 0 line count" */
        volatile uint32_t LINE_CNT0: 10;  /* ezip engine 0 line count */
        /* @SVD:field=BUF_CNT0,bitOffset=10,bitWidth=2,description="ezip engine 0 buffer count" */
        volatile uint32_t BUF_CNT0: 2;  /* ezip engine 0 buffer count */
        /* @SVD:field=RUN_STAT0,bitOffset=12,bitWidth=3,description="ezip engine 0 status" */
        volatile uint32_t RUN_STAT0: 3;  /* ezip engine 0 status */
        /* @SVD:field=RSVD2,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=LINE_CNT1,bitOffset=16,bitWidth=10,description="ezip engine 1 line count" */
        volatile uint32_t LINE_CNT1: 10;  /* ezip engine 1 line count */
        /* @SVD:field=BUF_CNT1,bitOffset=26,bitWidth=2,description="ezip engine 1 buffer count" */
        volatile uint32_t BUF_CNT1: 2;  /* ezip engine 1 buffer count */
        /* @SVD:field=RUN_STAT1,bitOffset=28,bitWidth=3,description="ezip engine 1 status" */
        volatile uint32_t RUN_STAT1: 3;  /* ezip engine 1 status */
        /* @SVD:field=RSVD,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_EZIP_STAT_Type;

/* @SVD:register=OL_STAT,offset=0x0148,size=32,access=read-write,reset=0x00000000,description="OL_STAT" */
/* OL_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DONE_REQ0,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t DONE_REQ0: 1;
        /* @SVD:field=PREFETCH_HOLD0,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_HOLD0: 1;
        /* @SVD:field=PREFETCH_OUT0,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_OUT0: 1;
        /* @SVD:field=PREFETCH_READ0,bitOffset=3,bitWidth=2,description="" */
        volatile uint32_t PREFETCH_READ0: 2;
        /* @SVD:field=DATA_CONV0,bitOffset=5,bitWidth=2,description="" */
        volatile uint32_t DATA_CONV0: 2;
        /* @SVD:field=PF_DF0,bitOffset=7,bitWidth=2,description="" */
        volatile uint32_t PF_DF0: 2;
        /* @SVD:field=PF_PR0,bitOffset=9,bitWidth=3,description="" */
        volatile uint32_t PF_PR0: 3;
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=DONE_REQ1,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t DONE_REQ1: 1;
        /* @SVD:field=PREFETCH_HOLD1,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_HOLD1: 1;
        /* @SVD:field=PREFETCH_OUT1,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_OUT1: 1;
        /* @SVD:field=PREFETCH_READ1,bitOffset=19,bitWidth=2,description="" */
        volatile uint32_t PREFETCH_READ1: 2;
        /* @SVD:field=DATA_CONV1,bitOffset=21,bitWidth=2,description="" */
        volatile uint32_t DATA_CONV1: 2;
        /* @SVD:field=PF_DF1,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t PF_DF1: 2;
        /* @SVD:field=PF_PR1,bitOffset=25,bitWidth=3,description="" */
        volatile uint32_t PF_PR1: 3;
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_OL_STAT_Type;

/* @SVD:register=OL2_STAT,offset=0x014C,size=32,access=read-write,reset=0x00000000,description="OL2_STAT" */
/* OL2_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PREFETCH_OUT,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_OUT: 1;
        /* @SVD:field=PREFETCH_READ,bitOffset=1,bitWidth=2,description="" */
        volatile uint32_t PREFETCH_READ: 2;
        /* @SVD:field=RF_ROT,bitOffset=3,bitWidth=4,description="" */
        volatile uint32_t RF_ROT: 4;
        /* @SVD:field=NF_PR,bitOffset=7,bitWidth=3,description="" */
        volatile uint32_t NF_PR: 3;
        /* @SVD:field=NF_DF,bitOffset=10,bitWidth=2,description="" */
        volatile uint32_t NF_DF: 2;
        /* @SVD:field=NF_DATA_CONV,bitOffset=12,bitWidth=2,description="" */
        volatile uint32_t NF_DATA_CONV: 2;
        /* @SVD:field=SC_OUT,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t SC_OUT: 2;
        /* @SVD:field=SC_BE,bitOffset=16,bitWidth=3,description="" */
        volatile uint32_t SC_BE: 3;
        /* @SVD:field=SC_FE,bitOffset=19,bitWidth=4,description="" */
        volatile uint32_t SC_FE: 4;
        /* @SVD:field=SC_LB1,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t SC_LB1: 2;
        /* @SVD:field=SC_LB0,bitOffset=25,bitWidth=2,description="" */
        volatile uint32_t SC_LB0: 2;
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_OL2_STAT_Type;

/* @SVD:register=VL_STAT,offset=0x0150,size=32,access=read-write,reset=0x00000000,description="VL_STAT" */
/* VL_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PREFETCH_OUT,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_OUT: 1;
        /* @SVD:field=PREFETCH_READ,bitOffset=1,bitWidth=2,description="" */
        volatile uint32_t PREFETCH_READ: 2;
        /* @SVD:field=RF_ROT,bitOffset=3,bitWidth=4,description="" */
        volatile uint32_t RF_ROT: 4;
        /* @SVD:field=NF_PR,bitOffset=7,bitWidth=3,description="" */
        volatile uint32_t NF_PR: 3;
        /* @SVD:field=NF_DF,bitOffset=10,bitWidth=2,description="" */
        volatile uint32_t NF_DF: 2;
        /* @SVD:field=NF_DATA_CONV,bitOffset=12,bitWidth=2,description="" */
        volatile uint32_t NF_DATA_CONV: 2;
        /* @SVD:field=SC_OUT,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t SC_OUT: 2;
        /* @SVD:field=SC_BE,bitOffset=16,bitWidth=3,description="" */
        volatile uint32_t SC_BE: 3;
        /* @SVD:field=SC_FE,bitOffset=19,bitWidth=4,description="" */
        volatile uint32_t SC_FE: 4;
        /* @SVD:field=SC_LB1,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t SC_LB1: 2;
        /* @SVD:field=SC_LB0,bitOffset=25,bitWidth=2,description="" */
        volatile uint32_t SC_LB0: 2;
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_VL_STAT_Type;

/* @SVD:register=ML_STAT,offset=0x0154,size=32,access=read-write,reset=0x00000000,description="ML_STAT" */
/* ML_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DONE_REQ,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t DONE_REQ: 1;
        /* @SVD:field=PREFETCH_HOLD,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_HOLD: 1;
        /* @SVD:field=PREFETCH_OUT,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_OUT: 1;
        /* @SVD:field=PREFETCH_READ,bitOffset=3,bitWidth=2,description="" */
        volatile uint32_t PREFETCH_READ: 2;
        /* @SVD:field=MF_DF,bitOffset=5,bitWidth=2,description="" */
        volatile uint32_t MF_DF: 2;
        /* @SVD:field=MF_PR,bitOffset=7,bitWidth=3,description="" */
        volatile uint32_t MF_PR: 3;
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_ML_STAT_Type;

/* @SVD:register=MEM_IF_STAT,offset=0x0158,size=32,access=read-write,reset=0x00000000,description="MEM_IF_STAT" */
/* MEM_IF_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=AHB0,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t AHB0: 4;
        /* @SVD:field=ARB_READ_PORT0,bitOffset=4,bitWidth=3,description="" */
        volatile uint32_t ARB_READ_PORT0: 3;
        /* @SVD:field=ARB_MAIN0,bitOffset=7,bitWidth=3,description="" */
        volatile uint32_t ARB_MAIN0: 3;
        /* @SVD:field=AHB1,bitOffset=10,bitWidth=4,description="" */
        volatile uint32_t AHB1: 4;
        /* @SVD:field=ARB_READ_PORT1,bitOffset=14,bitWidth=3,description="" */
        volatile uint32_t ARB_READ_PORT1: 3;
        /* @SVD:field=ARB_MAIN1,bitOffset=17,bitWidth=3,description="" */
        volatile uint32_t ARB_MAIN1: 3;
        /* @SVD:field=AHB_CTRL,bitOffset=20,bitWidth=3,description="" */
        volatile uint32_t AHB_CTRL: 3;
        /* @SVD:field=AHB_CTRL_EOL,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t AHB_CTRL_EOL: 1;
        /* @SVD:field=AHB_CTRL_FIFO_CNT,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t AHB_CTRL_FIFO_CNT: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EPIC_MEM_IF_STAT_Type;

/* EPIC 寄存器结构体 */
typedef struct
{
    EPIC_COMMAND_Type COMMAND;  /* 0x0000 - COMMAND */
    EPIC_STATUS_Type STATUS;  /* 0x0004 - STATUS */
    EPIC_EOF_IRQ_Type EOF_IRQ;  /* 0x0008 - EOF_IRQ */
    EPIC_SETTING_Type SETTING;  /* 0x000C - SETTING */
    EPIC_CANVAS_TL_POS_Type CANVAS_TL_POS;  /* 0x0010 - Top-Left pixel coordinate */
    EPIC_CANVAS_BR_POS_Type CANVAS_BR_POS;  /* 0x0014 - Bottom-Right pixel coordinate */
    EPIC_CANVAS_BG_Type CANVAS_BG;  /* 0x0018 - Background color */
    EPIC_VL_CFG_Type VL_CFG;  /* 0x001C - VL_CFG */
    EPIC_VL_TL_POS_Type VL_TL_POS;  /* 0x0020 - VL_TL_POS */
    EPIC_VL_BR_POS_Type VL_BR_POS;  /* 0x0024 - VL_BR_POS */
    EPIC_VL_EXTENTS_Type VL_EXTENTS;  /* 0x0028 - VL_EXTENTS */
    EPIC_VL_FILTER_Type VL_FILTER;  /* 0x002C - VL_FILTER */
    EPIC_VL_SRC_Type VL_SRC;  /* 0x0030 - VL_SRC */
    EPIC_VL_ROT_Type VL_ROT;  /* 0x0034 - VL_ROT */
    EPIC_VL_ROT_STAT_Type VL_ROT_STAT;  /* 0x0038 - VL_ROT_STAT */
    EPIC_VL_SCALE_RATIO_H_Type VL_SCALE_RATIO_H;  /* 0x003C - VL_SCALE_RATIO_H */
    EPIC_VL_SCALE_RATIO_V_Type VL_SCALE_RATIO_V;  /* 0x0040 - VL_SCALE_RATIO_V */
    EPIC_VL_FILL_Type VL_FILL;  /* 0x0044 - VL_FILL */
    EPIC_VL_MISC_CFG_Type VL_MISC_CFG;  /* 0x0048 - VL_MISC_CFG */
    /* @SVD:register=RSVD4,offset=0x004C,size=32,access=read-write,reset=0x0,description="RSVD4" */
    volatile uint32_t RSVD4;  /* 0x004C - RSVD4 */
    EPIC_L0_CFG_Type L0_CFG;  /* 0x0050 - L0_CFG */
    EPIC_L0_TL_POS_Type L0_TL_POS;  /* 0x0054 - L0_TL_POS */
    EPIC_L0_BR_POS_Type L0_BR_POS;  /* 0x0058 - L0_BR_POS */
    EPIC_L0_FILTER_Type L0_FILTER;  /* 0x005C - L0_FILTER */
    EPIC_L0_SRC_Type L0_SRC;  /* 0x0060 - L0_SRC */
    EPIC_L0_FILL_Type L0_FILL;  /* 0x0064 - L0_FILL */
    EPIC_L0_MISC_CFG_Type L0_MISC_CFG;  /* 0x0068 - L0_MISC_CFG */
    /* @SVD:register=RSVD3,offset=0x006C,size=32,access=read-write,reset=0x0,description="RSVD3" */
    volatile uint32_t RSVD3;  /* 0x006C - RSVD3 */
    EPIC_L1_CFG_Type L1_CFG;  /* 0x0070 - L1_CFG */
    EPIC_L1_TL_POS_Type L1_TL_POS;  /* 0x0074 - L1_TL_POS */
    EPIC_L1_BR_POS_Type L1_BR_POS;  /* 0x0078 - L1_BR_POS */
    EPIC_L1_FILTER_Type L1_FILTER;  /* 0x007C - L1_FILTER */
    EPIC_L1_SRC_Type L1_SRC;  /* 0x0080 - L1_SRC */
    EPIC_L1_FILL_Type L1_FILL;  /* 0x0084 - L1_FILL */
    EPIC_L1_MISC_CFG_Type L1_MISC_CFG;  /* 0x0088 - L1_MISC_CFG */
    /* @SVD:register=RSVD2,offset=0x008C,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x008C - RSVD2 */
    EPIC_L2_CFG_Type L2_CFG;  /* 0x0090 - L2_CFG */
    EPIC_L2_TL_POS_Type L2_TL_POS;  /* 0x0094 - L2_TL_POS */
    EPIC_L2_BR_POS_Type L2_BR_POS;  /* 0x0098 - L2_BR_POS */
    EPIC_L2_EXTENTS_Type L2_EXTENTS;  /* 0x009C - L2_EXTENTS */
    EPIC_L2_FILTER_Type L2_FILTER;  /* 0x00A0 - L2_FILTER */
    EPIC_L2_SRC_Type L2_SRC;  /* 0x00A4 - L2_SRC */
    EPIC_L2_ROT_Type L2_ROT;  /* 0x00A8 - L2_ROT */
    EPIC_L2_ROT_STAT_Type L2_ROT_STAT;  /* 0x00AC - L2_ROT_STAT */
    EPIC_L2_SCALE_RATIO_H_Type L2_SCALE_RATIO_H;  /* 0x00B0 - L2_SCALE_RATIO_H */
    EPIC_L2_SCALE_RATIO_V_Type L2_SCALE_RATIO_V;  /* 0x00B4 - L2_SCALE_RATIO_V */
    EPIC_L2_FILL_Type L2_FILL;  /* 0x00B8 - L2_FILL */
    EPIC_L2_MISC_CFG_Type L2_MISC_CFG;  /* 0x00BC - L2_MISC_CFG */
    EPIC_MASK_CFG_Type MASK_CFG;  /* 0x00C0 - MASK_CFG */
    EPIC_MASK_TL_POS_Type MASK_TL_POS;  /* 0x00C4 - MASK_TL_POS */
    EPIC_MASK_BR_POS_Type MASK_BR_POS;  /* 0x00C8 - MASK_BR_POS */
    EPIC_MASK_SRC_Type MASK_SRC;  /* 0x00CC - MASK_SRC */
    EPIC_COENG_CFG_Type COENG_CFG;  /* 0x00D0 - COENG_CFG */
    EPIC_YUV_ENG_CFG0_Type YUV_ENG_CFG0;  /* 0x00D4 - YUV_ENG_CFG0 */
    EPIC_YUV_ENG_CFG1_Type YUV_ENG_CFG1;  /* 0x00D8 - YUV_ENG_CFG1 */
    EPIC_Y_SRC_Type Y_SRC;  /* 0x00DC - Y_SRC */
    EPIC_U_SRC_Type U_SRC;  /* 0x00E0 - U_SRC */
    EPIC_V_SRC_Type V_SRC;  /* 0x00E4 - V_SRC */
    EPIC_COEF0_Type COEF0;  /* 0x00E8 - COEF0 */
    EPIC_COEF1_Type COEF1;  /* 0x00EC - COEF1 */
    EPIC_DITHER_CONF_Type DITHER_CONF;  /* 0x00F0 - DITHER_CONF */
    EPIC_DITHER_LFSR_Type DITHER_LFSR;  /* 0x00F4 - DITHER_LFSR */
    EPIC_AHB_CTRL_Type AHB_CTRL;  /* 0x00F8 - AHB_CTRL */
    EPIC_AHB_MEM_Type AHB_MEM;  /* 0x00FC - AHB_MEM */
    EPIC_AHB_STRIDE_Type AHB_STRIDE;  /* 0x0100 - AHB_STRIDE */
    EPIC_DEBUG_Type DEBUG;  /* 0x0104 - DEBUG */
    EPIC_VL_ROT_M_CFG1_Type VL_ROT_M_CFG1;  /* 0x0108 - VL_ROT_M_CFG1 */
    EPIC_VL_ROT_M_CFG2_Type VL_ROT_M_CFG2;  /* 0x010C - VL_ROT_M_CFG2 */
    EPIC_VL_ROT_M_CFG3_Type VL_ROT_M_CFG3;  /* 0x0110 - VL_ROT_M_CFG3 */
    EPIC_VL_SCALE_INIT_CFG1_Type VL_SCALE_INIT_CFG1;  /* 0x0114 - VL_SCALE_INIT_CFG1 */
    EPIC_VL_SCALE_INIT_CFG2_Type VL_SCALE_INIT_CFG2;  /* 0x0118 - VL_SCALE_INIT_CFG2 */
    EPIC_L2_ROT_M_CFG1_Type L2_ROT_M_CFG1;  /* 0x011C - L2_ROT_M_CFG1 */
    EPIC_L2_ROT_M_CFG2_Type L2_ROT_M_CFG2;  /* 0x0120 - L2_ROT_M_CFG2 */
    EPIC_L2_ROT_M_CFG3_Type L2_ROT_M_CFG3;  /* 0x0124 - L2_ROT_M_CFG3 */
    EPIC_L2_SCALE_INIT_CFG1_Type L2_SCALE_INIT_CFG1;  /* 0x0128 - L2_SCALE_INIT_CFG1 */
    EPIC_L2_SCALE_INIT_CFG2_Type L2_SCALE_INIT_CFG2;  /* 0x012C - L2_SCALE_INIT_CFG2 */
    EPIC_PERF_CNT_Type PERF_CNT;  /* 0x0130 - PERF_CNT */
    /* @SVD:register=RSVD1,offset=0x0134,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0134 - RSVD1 */
    volatile uint32_t RESERVED78[2];  /* 0x0138 - 0x013F */
    EPIC_CANVAS_STAT_Type CANVAS_STAT;  /* 0x0140 - CANVAS_STAT */
    EPIC_EZIP_STAT_Type EZIP_STAT;  /* 0x0144 - EZIP_STAT */
    EPIC_OL_STAT_Type OL_STAT;  /* 0x0148 - OL_STAT */
    EPIC_OL2_STAT_Type OL2_STAT;  /* 0x014C - OL2_STAT */
    EPIC_VL_STAT_Type VL_STAT;  /* 0x0150 - VL_STAT */
    EPIC_ML_STAT_Type ML_STAT;  /* 0x0154 - ML_STAT */
    EPIC_MEM_IF_STAT_Type MEM_IF_STAT;  /* 0x0158 - MEM_IF_STAT */
} EPIC_TypeDef;

/* ============================================================================ */
/* LCDC1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=LCDC1,base=0x50008000,description="",groupName=LCDC */

#define LCDC1_BASE                                         (0x50008000UL)

/* @SVD:register=COMMAND,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="COMMAND" */
/* COMMAND 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=START,bitOffset=0,bitWidth=1,description="write 1 to trigger the lcd interface block" */
        volatile uint32_t START: 1;  /* write 1 to trigger the lcd interface block */
        /* @SVD:field=RESET,bitOffset=1,bitWidth=1,description="1: reset the whole graphics   0: release the reset" */
        volatile uint32_t RESET: 1;  /* 1: reset the whole graphics   0: release the reset */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_COMMAND_Type;

/* @SVD:register=STATUS,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="STATUS" */
/* STATUS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LCD_BUSY,bitOffset=0,bitWidth=1,description="LCS controll busy flag" */
        volatile uint32_t LCD_BUSY: 1;  /* LCS controll busy flag */
        /* @SVD:field=DPI_RUN,bitOffset=1,bitWidth=1,description="DPI interface is running" */
        volatile uint32_t DPI_RUN: 1;  /* DPI interface is running */
        /* @SVD:field=JDI_PAR_RUN,bitOffset=2,bitWidth=1,description="JDI parallel interface is running" */
        volatile uint32_t JDI_PAR_RUN: 1;  /* JDI parallel interface is running */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=29,description="" */
        volatile uint32_t RSVD: 29;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_STATUS_Type;

/* @SVD:register=IRQ,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="IRQ" */
/* IRQ 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EOF_STAT,bitOffset=0,bitWidth=1,description="end of frame interrupt, masked by mask register" */
        volatile uint32_t EOF_STAT: 1;  /* end of frame interrupt, masked by mask register */
        /* @SVD:field=ICB_OF_STAT,bitOffset=1,bitWidth=1,description="icb overflow interrupt, masked by mask register" */
        volatile uint32_t ICB_OF_STAT: 1;  /* icb overflow interrupt, masked by mask register */
        /* @SVD:field=DPIL_INTR_STAT,bitOffset=2,bitWidth=1,description="dpi line interrupt, masked by mask register" */
        volatile uint32_t DPIL_INTR_STAT: 1;  /* dpi line interrupt, masked by mask register */
        /* @SVD:field=DPI_UDR_STAT,bitOffset=3,bitWidth=1,description="dpi under run interrupt, masked by mask register" */
        volatile uint32_t DPI_UDR_STAT: 1;  /* dpi under run interrupt, masked by mask register */
        /* @SVD:field=JDI_PARL_INTR_STAT,bitOffset=4,bitWidth=1,description="jdi parallel interface line interrupt, masked by mask register" */
        volatile uint32_t JDI_PARL_INTR_STAT: 1;  /* jdi parallel interface line interrupt, masked by mask regist */
        /* @SVD:field=JDI_PAR_UDR_STAT,bitOffset=5,bitWidth=1,description="jdi parallel interface under run interrupt, masked by mask register" */
        volatile uint32_t JDI_PAR_UDR_STAT: 1;  /* jdi parallel interface under run interrupt, masked by mask r */
        /* @SVD:field=LINE_DONE_STAT,bitOffset=6,bitWidth=1,description="line process done interrupt, masked by mask register" */
        volatile uint32_t LINE_DONE_STAT: 1;  /* line process done interrupt, masked by mask register */
        /* @SVD:field=RSVD2,bitOffset=7,bitWidth=9,description="" */
        volatile uint32_t RSVD2: 9;
        /* @SVD:field=EOF_RAW_STAT,bitOffset=16,bitWidth=1,description="raw status of end of frame interrupt" */
        volatile uint32_t EOF_RAW_STAT: 1;  /* raw status of end of frame interrupt */
        /* @SVD:field=ICB_OF_RAW_STAT,bitOffset=17,bitWidth=1,description="raw status of icb overflow interrupt" */
        volatile uint32_t ICB_OF_RAW_STAT: 1;  /* raw status of icb overflow interrupt */
        /* @SVD:field=DPIL_INTR_RAW_STAT,bitOffset=18,bitWidth=1,description="raw status of dpi line interrupt" */
        volatile uint32_t DPIL_INTR_RAW_STAT: 1;  /* raw status of dpi line interrupt */
        /* @SVD:field=DPI_UDR_RAW_STAT,bitOffset=19,bitWidth=1,description="raw status of dpi under run interrupt" */
        volatile uint32_t DPI_UDR_RAW_STAT: 1;  /* raw status of dpi under run interrupt */
        /* @SVD:field=JDI_PARL_INTR_RAW_STAT,bitOffset=20,bitWidth=1,description="raw_status of jdi parallel interface line interrupt" */
        volatile uint32_t JDI_PARL_INTR_RAW_STAT: 1;  /* raw_status of jdi parallel interface line interrupt */
        /* @SVD:field=JDI_PAR_UDR_RAW_STAT,bitOffset=21,bitWidth=1,description="raw_status of jdi parallel interface under run interrupt" */
        volatile uint32_t JDI_PAR_UDR_RAW_STAT: 1;  /* raw_status of jdi parallel interface under run interrupt */
        /* @SVD:field=LINE_DONE_RAW_STAT,bitOffset=22,bitWidth=1,description="raw_status of line process done interrupt" */
        volatile uint32_t LINE_DONE_RAW_STAT: 1;  /* raw_status of line process done interrupt */
        /* @SVD:field=RSVD,bitOffset=23,bitWidth=9,description="" */
        volatile uint32_t RSVD: 9;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_IRQ_Type;

/* @SVD:register=SETTING,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="SETTING" */
/* SETTING 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EOF_MASK,bitOffset=0,bitWidth=1,description="end of frame interrupt mask, 0: mask the interrupt" */
        volatile uint32_t EOF_MASK: 1;  /* end of frame interrupt mask, 0: mask the interrupt */
        /* @SVD:field=ICB_OF_MASK,bitOffset=1,bitWidth=1,description="icb overflow interrupt mask, 0: mask the interrupt" */
        volatile uint32_t ICB_OF_MASK: 1;  /* icb overflow interrupt mask, 0: mask the interrupt */
        /* @SVD:field=DPIL_INTR_MASK,bitOffset=2,bitWidth=1,description="dpi line interrupt, 0: mask the interrupt" */
        volatile uint32_t DPIL_INTR_MASK: 1;  /* dpi line interrupt, 0: mask the interrupt */
        /* @SVD:field=DPI_UDR_MASK,bitOffset=3,bitWidth=1,description="dpi under run interrupt mask, 0: mask the interrupt" */
        volatile uint32_t DPI_UDR_MASK: 1;  /* dpi under run interrupt mask, 0: mask the interrupt */
        /* @SVD:field=JDI_PARL_INTR_MASK,bitOffset=4,bitWidth=1,description="jdi parallel interface line interrupt, 0: mask the interrupt" */
        volatile uint32_t JDI_PARL_INTR_MASK: 1;  /* jdi parallel interface line interrupt, 0: mask the interrupt */
        /* @SVD:field=JDI_PAR_UDR_MASK,bitOffset=5,bitWidth=1,description="jdi parallel interface under run interrupt mask, 0: mask the interrupt" */
        volatile uint32_t JDI_PAR_UDR_MASK: 1;  /* jdi parallel interface under run interrupt mask, 0: mask the */
        /* @SVD:field=LINE_DONE_MASK,bitOffset=6,bitWidth=1,description="line process done interrupt, 0: mask the interrupt" */
        volatile uint32_t LINE_DONE_MASK: 1;  /* line process done interrupt, 0: mask the interrupt */
        /* @SVD:field=RSVD3,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=AUTO_GATE_EN,bitOffset=8,bitWidth=1,description="auto clock gating enable" */
        volatile uint32_t AUTO_GATE_EN: 1;  /* auto clock gating enable */
        /* @SVD:field=RSVD2,bitOffset=9,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=LINE_DONE_NUM,bitOffset=16,bitWidth=11,description="line number of line process done interrupt" */
        volatile uint32_t LINE_DONE_NUM: 11;  /* line number of line process done interrupt */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_SETTING_Type;

/* @SVD:register=CANVAS_TL_POS,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="CANVAS_TL_POS" */
/* CANVAS_TL_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X0,bitOffset=0,bitWidth=11,description="" */
        volatile uint32_t X0: 11;
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=Y0,bitOffset=16,bitWidth=11,description="" */
        volatile uint32_t Y0: 11;
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_CANVAS_TL_POS_Type;

/* @SVD:register=CANVAS_BR_POS,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="CANVAS_BR_POS" */
/* CANVAS_BR_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X1,bitOffset=0,bitWidth=11,description="" */
        volatile uint32_t X1: 11;
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=Y1,bitOffset=16,bitWidth=11,description="" */
        volatile uint32_t Y1: 11;
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_CANVAS_BR_POS_Type;

/* @SVD:register=CANVAS_BG,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="CANVAS_BG" */
/* CANVAS_BG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BLUE,bitOffset=0,bitWidth=8,description="blue color" */
        volatile uint32_t BLUE: 8;  /* blue color */
        /* @SVD:field=GREEN,bitOffset=8,bitWidth=8,description="green color" */
        volatile uint32_t GREEN: 8;  /* green color */
        /* @SVD:field=RED,bitOffset=16,bitWidth=8,description="Red color" */
        volatile uint32_t RED: 8;  /* Red color */
        /* @SVD:field=BG_BLENDING_BYPASS,bitOffset=24,bitWidth=1,description="if this bit is set, the layer is not blending with background. The alpha value will be reserved to output." */
        volatile uint32_t BG_BLENDING_BYPASS: 1;  /* if this bit is set, the layer is not blending with backgroun */
        /* @SVD:field=ALL_BLENDING_BYPASS,bitOffset=25,bitWidth=1,description="if this bit is set, lcdc is in pure dma mode. No blending operation." */
        volatile uint32_t ALL_BLENDING_BYPASS: 1;  /* if this bit is set, lcdc is in pure dma mode. No blending op */
        /* @SVD:field=LB_BYPASS,bitOffset=26,bitWidth=1,description="line buffer bypass. Set 1 to bypass line buffer." */
        volatile uint32_t LB_BYPASS: 1;  /* line buffer bypass. Set 1 to bypass line buffer. */
        /* @SVD:field=H_MIRROR,bitOffset=27,bitWidth=1,description="set 1 to do horizontal mirror for output image" */
        volatile uint32_t H_MIRROR: 1;  /* set 1 to do horizontal mirror for output image */
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_CANVAS_BG_Type;

/* @SVD:register=LAYER0_CONFIG,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="LAYER0_CONFIG" */
/* LAYER0_CONFIG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FORMAT,bitOffset=0,bitWidth=3,description="overlay layer input format\n              3'h0: RGB565\n              3'h1: RGB888\n              3'h2: ARGB8888\n              3'h3: ARGB8565\n              3'h4: RGB332\n              3'h5: A8\n              3'h6: L8\n              others: reserved" */
        volatile uint32_t FORMAT: 3;  /* overlay layer input format               3'h0: RGB565        */
        /* @SVD:field=ALPHA_SEL,bitOffset=3,bitWidth=1,description="alpha selection\n              1'b0: select alpha according to image format\n              1'b1: select layer alpha" */
        volatile uint32_t ALPHA_SEL: 1;  /* alpha selection               1'b0: select alpha according t */
        /* @SVD:field=ALPHA,bitOffset=4,bitWidth=8,description="layer alpha value" */
        volatile uint32_t ALPHA: 8;  /* layer alpha value */
        /* @SVD:field=FILTER_EN,bitOffset=12,bitWidth=1,description="layer color filter enable" */
        volatile uint32_t FILTER_EN: 1;  /* layer color filter enable */
        /* @SVD:field=WIDTH,bitOffset=13,bitWidth=13,description="source image width(including padding), unit is bytes" */
        volatile uint32_t WIDTH: 13;  /* source image width(including padding), unit is bytes */
        /* @SVD:field=PREFETCH_EN,bitOffset=26,bitWidth=1,description="preload 64 bytes extra data when reading pixel from memory" */
        volatile uint32_t PREFETCH_EN: 1;  /* preload 64 bytes extra data when reading pixel from memory */
        /* @SVD:field=LINE_FETCH_MODE,bitOffset=27,bitWidth=1,description="line fetch mode\n              0: address skip every single line\n              1: address skip every two line" */
        volatile uint32_t LINE_FETCH_MODE: 1;  /* line fetch mode               0: address skip every single l */
        /* @SVD:field=ACTIVE,bitOffset=28,bitWidth=1,description="layer active flag" */
        volatile uint32_t ACTIVE: 1;  /* layer active flag */
        /* @SVD:field=ALPHA_BLEND,bitOffset=29,bitWidth=1,description="set 1 to enable alpha blending mode. Use layer alpha as blending factor for image with Alpha.\n              Alpha_out = Layer_alpha * Image_alpha" */
        volatile uint32_t ALPHA_BLEND: 1;  /* set 1 to enable alpha blending mode. Use layer alpha as blen */
        /* @SVD:field=V_MIRROR,bitOffset=30,bitWidth=1,description="set 1 to do vertical mirror for the layer" */
        volatile uint32_t V_MIRROR: 1;  /* set 1 to do vertical mirror for the layer */
        /* @SVD:field=RSVD,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_CONFIG_Type;

/* @SVD:register=LAYER0_TL_POS,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="LAYER0_TL_POS" */
/* LAYER0_TL_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X0,bitOffset=0,bitWidth=11,description="Coordinate X-value" */
        volatile uint32_t X0: 11;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=Y0,bitOffset=16,bitWidth=11,description="Coordingate Y-value" */
        volatile uint32_t Y0: 11;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_TL_POS_Type;

/* @SVD:register=LAYER0_BR_POS,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="LAYER0_BR_POS" */
/* LAYER0_BR_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X1,bitOffset=0,bitWidth=11,description="Coordinate X-value" */
        volatile uint32_t X1: 11;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=Y1,bitOffset=16,bitWidth=11,description="Coordingate Y-value" */
        volatile uint32_t Y1: 11;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_BR_POS_Type;

/* @SVD:register=LAYER0_FILTER,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="LAYER0_FILTER" */
/* LAYER0_FILTER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FILTER_B,bitOffset=0,bitWidth=8,description="filter b color" */
        volatile uint32_t FILTER_B: 8;  /* filter b color */
        /* @SVD:field=FILTER_G,bitOffset=8,bitWidth=8,description="filter g color" */
        volatile uint32_t FILTER_G: 8;  /* filter g color */
        /* @SVD:field=FILTER_R,bitOffset=16,bitWidth=8,description="filter r color" */
        volatile uint32_t FILTER_R: 8;  /* filter r color */
        /* @SVD:field=FILTER_MASK,bitOffset=24,bitWidth=8,description="layer color filter mask" */
        volatile uint32_t FILTER_MASK: 8;  /* layer color filter mask */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_FILTER_Type;

/* @SVD:register=LAYER0_SRC,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="LAYER0_SRC" */
/* LAYER0_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="source image RGB data address[31:0]. For RGB565 format, address should be aligned to halfword. For ARGB8888 format, address should be aligned to word." */
        volatile uint32_t ADDR: 32;  /* source image RGB data address[31:0]. For RGB565 format, addr */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_SRC_Type;

/* @SVD:register=LAYER0_FILL,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="LAYER0_FILL" */
/* LAYER0_FILL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BG_B,bitOffset=0,bitWidth=8,description="background b color" */
        volatile uint32_t BG_B: 8;  /* background b color */
        /* @SVD:field=BG_G,bitOffset=8,bitWidth=8,description="background g color" */
        volatile uint32_t BG_G: 8;  /* background g color */
        /* @SVD:field=BG_R,bitOffset=16,bitWidth=8,description="background r color" */
        volatile uint32_t BG_R: 8;  /* background r color */
        /* @SVD:field=BG_MODE,bitOffset=24,bitWidth=1,description="not used" */
        volatile uint32_t BG_MODE: 1;  /* not used */
        /* @SVD:field=ENDIAN,bitOffset=25,bitWidth=1,description="input 565 data format endian\n              0: {R[4:0], G[5:3], G[2:0], B[4:0]}\n              1: {G[2:0], R[4:0], B[4:0], G[5:3]}" */
        volatile uint32_t ENDIAN: 1;  /* input 565 data format endian               0: {R[4:0], G[5:3 */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_FILL_Type;

/* @SVD:register=LAYER0_DECOMP,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="LAYER0_DECOMP" */
/* LAYER0_DECOMP 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="decompression enable" */
        volatile uint32_t ENABLE: 1;  /* decompression enable */
        /* @SVD:field=TARGET_WORDS,bitOffset=1,bitWidth=12,description="size of a single channel data before decompression. Unit is half word. Each line has 3 channels. So for each line, the compressed data size is target_words * 3 * 2 bytes." */
        volatile uint32_t TARGET_WORDS: 12;  /* size of a single channel data before decompression. Unit is  */
        /* @SVD:field=COL_SIZE,bitOffset=13,bitWidth=11,description="number of colums in a line of original image, max column size is 1024" */
        volatile uint32_t COL_SIZE: 11;  /* number of colums in a line of original image, max column siz */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_DECOMP_Type;

/* @SVD:register=LAYER0_DECOMP_CFG0,offset=0x0038,size=32,access=read-write,reset=0x01055982,description="LAYER0_DECOMP_CFG0" */
/* LAYER0_DECOMP_CFG0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EXTRA_HIGH,bitOffset=0,bitWidth=4,description="extra bit for high quality bit" */
        volatile uint32_t EXTRA_HIGH: 4;  /* extra bit for high quality bit */
        /* @SVD:field=EXTRA_THRESHOLD,bitOffset=4,bitWidth=4,description="the threshold to distinguish high/low quality block" */
        volatile uint32_t EXTRA_THRESHOLD: 4;  /* the threshold to distinguish high/low quality block */
        /* @SVD:field=USE_LOSSLESS_QIDX,bitOffset=8,bitWidth=4,description="condition to increase qidx" */
        volatile uint32_t USE_LOSSLESS_QIDX: 4;  /* condition to increase qidx */
        /* @SVD:field=LOSSLESS_QIDX1,bitOffset=12,bitWidth=4,description="up level for adjusted qidx value for low quality block" */
        volatile uint32_t LOSSLESS_QIDX1: 4;  /* up level for adjusted qidx value for low quality block */
        /* @SVD:field=LOSSLESS_QIDX2,bitOffset=16,bitWidth=4,description="condition to decrease qidx" */
        volatile uint32_t LOSSLESS_QIDX2: 4;  /* condition to decrease qidx */
        /* @SVD:field=CFG0_RESERVED,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t CFG0_RESERVED: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_DECOMP_CFG0_Type;

/* @SVD:register=LAYER0_DECOMP_CFG1,offset=0x003C,size=32,access=read-write,reset=0x80023307,description="LAYER0_DECOMP_CFG1" */
/* LAYER0_DECOMP_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BLOCK_WIDTH,bitOffset=0,bitWidth=1,description="block_size in pixel unit.\n              0: 16 pixels\n              1: 32 pixels\n              Small block size will cause more blocks and more bits to store block information." */
        volatile uint32_t BLOCK_WIDTH: 1;  /* block_size in pixel unit.               0: 16 pixels         */
        /* @SVD:field=DITHER,bitOffset=1,bitWidth=1,description="dithering function\n              0: off\n              1: on" */
        volatile uint32_t DITHER: 1;  /* dithering function               0: off               1: on */
        /* @SVD:field=CFG1_RESERVED,bitOffset=2,bitWidth=6,description="" */
        volatile uint32_t CFG1_RESERVED: 6;
        /* @SVD:field=FAILOVER_BITS_R,bitOffset=8,bitWidth=4,description="failover compression mode target bits(Red)" */
        volatile uint32_t FAILOVER_BITS_R: 4;  /* failover compression mode target bits(Red) */
        /* @SVD:field=FAILOVER_BITS_G,bitOffset=12,bitWidth=4,description="failover compression mode target bits(Green)" */
        volatile uint32_t FAILOVER_BITS_G: 4;  /* failover compression mode target bits(Green) */
        /* @SVD:field=FAILOVER_BITS_B,bitOffset=16,bitWidth=4,description="failover compression mode target bits(Blue)" */
        volatile uint32_t FAILOVER_BITS_B: 4;  /* failover compression mode target bits(Blue) */
        /* @SVD:field=LINE_MIN_QIDX,bitOffset=20,bitWidth=4,description="minimum qidx for line mode" */
        volatile uint32_t LINE_MIN_QIDX: 4;  /* minimum qidx for line mode */
        /* @SVD:field=BLOCK_MIN_QIDX,bitOffset=24,bitWidth=4,description="minimum qidx for block mode" */
        volatile uint32_t BLOCK_MIN_QIDX: 4;  /* minimum qidx for block mode */
        /* @SVD:field=EXTRA_LOW,bitOffset=28,bitWidth=4,description="extra bit for low quality block" */
        volatile uint32_t EXTRA_LOW: 4;  /* extra bit for low quality block */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_DECOMP_CFG1_Type;

/* @SVD:register=LAYER0_DECOMP_STAT,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="LAYER0_DECOMP_STAT" */
/* LAYER0_DECOMP_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BUF_MAX_DEPTH,bitOffset=0,bitWidth=7,description="buf max usage" */
        volatile uint32_t BUF_MAX_DEPTH: 7;  /* buf max usage */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER0_DECOMP_STAT_Type;

/* @SVD:register=LAYER1_CONFIG,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="LAYER1_CONFIG" */
/* LAYER1_CONFIG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FORMAT,bitOffset=0,bitWidth=3,description="overlay layer input format\n              3'h0: RGB565\n              3'h1: RGB888\n              3'h2: ARGB8888\n              3'h3: ARGB8565\n              3'h4: RGB332\n              3'h5: A8\n              3'h6: L8\n              others: reserved" */
        volatile uint32_t FORMAT: 3;  /* overlay layer input format               3'h0: RGB565        */
        /* @SVD:field=ALPHA_SEL,bitOffset=3,bitWidth=1,description="alpha selection\n              1'b0: select alpha according to image format\n              1'b1: select layer alpha" */
        volatile uint32_t ALPHA_SEL: 1;  /* alpha selection               1'b0: select alpha according t */
        /* @SVD:field=ALPHA,bitOffset=4,bitWidth=8,description="layer alpha value" */
        volatile uint32_t ALPHA: 8;  /* layer alpha value */
        /* @SVD:field=FILTER_EN,bitOffset=12,bitWidth=1,description="layer color filter enable" */
        volatile uint32_t FILTER_EN: 1;  /* layer color filter enable */
        /* @SVD:field=WIDTH,bitOffset=13,bitWidth=13,description="source image width(including padding), unit is bytes" */
        volatile uint32_t WIDTH: 13;  /* source image width(including padding), unit is bytes */
        /* @SVD:field=PREFETCH_EN,bitOffset=26,bitWidth=1,description="preload 64 bytes extra data when reading pixel from memory" */
        volatile uint32_t PREFETCH_EN: 1;  /* preload 64 bytes extra data when reading pixel from memory */
        /* @SVD:field=LINE_FETCH_MODE,bitOffset=27,bitWidth=1,description="line fetch mode\n              0: address skip every single line\n              1: address skip every two line" */
        volatile uint32_t LINE_FETCH_MODE: 1;  /* line fetch mode               0: address skip every single l */
        /* @SVD:field=ACTIVE,bitOffset=28,bitWidth=1,description="layer active flag" */
        volatile uint32_t ACTIVE: 1;  /* layer active flag */
        /* @SVD:field=ALPHA_BLEND,bitOffset=29,bitWidth=1,description="set 1 to enable alpha blending mode. Use layer alpha as blending factor for image with Alpha.\n              Alpha_out = Layer_alpha * Image_alpha" */
        volatile uint32_t ALPHA_BLEND: 1;  /* set 1 to enable alpha blending mode. Use layer alpha as blen */
        /* @SVD:field=V_MIRROR,bitOffset=30,bitWidth=1,description="set 1 to do vertical mirror for the layer" */
        volatile uint32_t V_MIRROR: 1;  /* set 1 to do vertical mirror for the layer */
        /* @SVD:field=RSVD,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER1_CONFIG_Type;

/* @SVD:register=LAYER1_TL_POS,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="LAYER1_TL_POS" */
/* LAYER1_TL_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X0,bitOffset=0,bitWidth=11,description="Coordinate X-value" */
        volatile uint32_t X0: 11;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=Y0,bitOffset=16,bitWidth=11,description="Coordingate Y-value" */
        volatile uint32_t Y0: 11;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER1_TL_POS_Type;

/* @SVD:register=LAYER1_BR_POS,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="LAYER1_BR_POS" */
/* LAYER1_BR_POS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X1,bitOffset=0,bitWidth=11,description="Coordinate X-value" */
        volatile uint32_t X1: 11;  /* Coordinate X-value */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=Y1,bitOffset=16,bitWidth=11,description="Coordingate Y-value" */
        volatile uint32_t Y1: 11;  /* Coordingate Y-value */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER1_BR_POS_Type;

/* @SVD:register=LAYER1_FILTER,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="LAYER1_FILTER" */
/* LAYER1_FILTER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FILTER_B,bitOffset=0,bitWidth=8,description="filter b color" */
        volatile uint32_t FILTER_B: 8;  /* filter b color */
        /* @SVD:field=FILTER_G,bitOffset=8,bitWidth=8,description="filter g color" */
        volatile uint32_t FILTER_G: 8;  /* filter g color */
        /* @SVD:field=FILTER_R,bitOffset=16,bitWidth=8,description="filter r color" */
        volatile uint32_t FILTER_R: 8;  /* filter r color */
        /* @SVD:field=FILTER_MASK,bitOffset=24,bitWidth=8,description="layer color filter mask" */
        volatile uint32_t FILTER_MASK: 8;  /* layer color filter mask */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER1_FILTER_Type;

/* @SVD:register=LAYER1_SRC,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="LAYER1_SRC" */
/* LAYER1_SRC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="source image RGB data address[31:0]. For RGB565 format, address should be aligned to halfword. For ARGB8888 format, address should be aligned to word." */
        volatile uint32_t ADDR: 32;  /* source image RGB data address[31:0]. For RGB565 format, addr */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER1_SRC_Type;

/* @SVD:register=LAYER1_FILL,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="LAYER1_FILL" */
/* LAYER1_FILL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BG_B,bitOffset=0,bitWidth=8,description="background b color" */
        volatile uint32_t BG_B: 8;  /* background b color */
        /* @SVD:field=BG_G,bitOffset=8,bitWidth=8,description="background g color" */
        volatile uint32_t BG_G: 8;  /* background g color */
        /* @SVD:field=BG_R,bitOffset=16,bitWidth=8,description="background r color" */
        volatile uint32_t BG_R: 8;  /* background r color */
        /* @SVD:field=BG_MODE,bitOffset=24,bitWidth=1,description="not used" */
        volatile uint32_t BG_MODE: 1;  /* not used */
        /* @SVD:field=ENDIAN,bitOffset=25,bitWidth=1,description="input 565 data format endian\n              0: {R[4:0], G[5:3], G[2:0], B[4:0]}\n              1: {G[2:0], R[4:0], B[4:0], G[5:3]}" */
        volatile uint32_t ENDIAN: 1;  /* input 565 data format endian               0: {R[4:0], G[5:3 */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LAYER1_FILL_Type;

/* @SVD:register=DITHER_CONF,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="DITHER_CONF" */
/* DITHER_CONF 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="dither enable" */
        volatile uint32_t EN: 1;  /* dither enable */
        /* @SVD:field=W_B,bitOffset=1,bitWidth=3,description="blue dither width" */
        volatile uint32_t W_B: 3;  /* blue dither width */
        /* @SVD:field=W_G,bitOffset=4,bitWidth=3,description="green dither width" */
        volatile uint32_t W_G: 3;  /* green dither width */
        /* @SVD:field=W_R,bitOffset=7,bitWidth=3,description="red dither width" */
        volatile uint32_t W_R: 3;  /* red dither width */
        /* @SVD:field=LFSR_LOAD_SEL,bitOffset=10,bitWidth=2,description="select lfsr\n              0: none\n              1: red\n              2: green\n              3: blue" */
        volatile uint32_t LFSR_LOAD_SEL: 2;  /* select lfsr               0: none               1: red       */
        /* @SVD:field=LFSR_LOAD,bitOffset=12,bitWidth=1,description="load lfsr init value" */
        volatile uint32_t LFSR_LOAD: 1;  /* load lfsr init value */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_DITHER_CONF_Type;

/* @SVD:register=DITHER_LFSR,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="DITHER_LFSR" */
/* DITHER_LFSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INIT_VAL,bitOffset=0,bitWidth=32,description="lfsr init load value" */
        volatile uint32_t INIT_VAL: 32;  /* lfsr init load value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_DITHER_LFSR_Type;

/* @SVD:register=LCD_CONF,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="LCD_CONF" */
/* LCD_CONF 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TARGET_LCD,bitOffset=0,bitWidth=2,description="The Data can be sent to four destinations:\n              2'b00: LCD panel 0\n              2'b01: LCD panel 1\n              2'b10: AHB LCD\n              2'b11: AHB RAM" */
        volatile uint32_t TARGET_LCD: 2;  /* The Data can be sent to four destinations:               2'b */
        /* @SVD:field=LCD_INTF_SEL,bitOffset=2,bitWidth=3,description="3'b000: 8080 DBI Type B\n              3'b001: SPI interface\n              3'b010: DBI to DSI interface\n              3'b011: DPI interface\n              3'b100: JDI serial interface\n              3'b101: JDI parallel interface\n              3'b110: 8080 DBI Type A\n              3'b111: DPI to DSI interface" */
        volatile uint32_t LCD_INTF_SEL: 3;  /* 3'b000: 8080 DBI Type B               3'b001: SPI interface  */
        /* @SVD:field=LCD_FORMAT,bitOffset=5,bitWidth=3,description="LCD output format:\n              3'b000: 8-bit RGB 3:3:2\n              3'b001: 16-bit RGB 5:6:5 over 8-bit bus, 2 cycles/pixel\n              3'b010: 12-bit RGB 4:4:4\n              3'b011: 16-bit RGB 5:6:5\n              3'b100: 18-bit RGB 6:6:6\n              3'b101: 24-bit RGB 8:8:8\n              3'b110: 24-bit RGB 8:8:8 over 16-bit bus, 1.5 cycles/pixel\n              3'b111: 24-bit RGB 8:8:8 over 8-bit bus, 3cycles/pixel\n              others: Reserved" */
        volatile uint32_t LCD_FORMAT: 3;  /* LCD output format:               3'b000: 8-bit RGB 3:3:2     */
        /* @SVD:field=AHB_FORMAT,bitOffset=8,bitWidth=2,description="AHB LCD/RAM output format:\n              0: RGB565\n              1: RGB888\n              2: ARGB8888\n              3: RGB332" */
        volatile uint32_t AHB_FORMAT: 2;  /* AHB LCD/RAM output format:               0: RGB565           */
        /* @SVD:field=SPI_LCD_FORMAT,bitOffset=10,bitWidth=2,description="SPI LCD format\n              2'b00: 8-bit RGB 3:3:2\n              2'b01: 16-bit RGB 5:6:5\n              2'b10: 24-bit RGB 8:8:8\n              2'b11: Reserved" */
        volatile uint32_t SPI_LCD_FORMAT: 2;  /* SPI LCD format               2'b00: 8-bit RGB 3:3:2          */
        /* @SVD:field=DPI_LCD_FORMAT,bitOffset=12,bitWidth=3,description="DPI LCD format\n              3'b000: 16-bit conf1\n              3'b001: 16-bit conf2\n              3'b010: 16-bit conf3\n              3'b011: 18-bit conf1\n              3'b100: 18-bit conf2\n              3'b101: 24-bit\n              others: Reserved" */
        volatile uint32_t DPI_LCD_FORMAT: 3;  /* DPI LCD format               3'b000: 16-bit conf1            */
        /* @SVD:field=JDI_SER_FORMAT,bitOffset=15,bitWidth=2,description="JDI serial format\n              2'b00: 3-bit mode\n              2'b01: 4-bit mode\n              2'b10: 1-bit mode\n              2'b11: reserved" */
        volatile uint32_t JDI_SER_FORMAT: 2;  /* JDI serial format               2'b00: 3-bit mode            */
        /* @SVD:field=DIRECT_INTF_EN,bitOffset=17,bitWidth=1,description="when the target LCD is AHB LCD, this bit enable the direct interface to DSI module. Direct interface has higher bandwidth and speed than AHB interface." */
        volatile uint32_t DIRECT_INTF_EN: 1;  /* when the target LCD is AHB LCD, this bit enable the direct i */
        /* @SVD:field=ENDIAN,bitOffset=18,bitWidth=1,description="LCD 565 data format endian, this bit would affect SPI, DPI, DBI and AHB interface 565 format\n              0: {R[4:0], G[5:3], G[2:0], B[4:0]}\n              1: {G[2:0], R[4:0], B[4:0], G[5:3]}" */
        volatile uint32_t ENDIAN: 1;  /* LCD 565 data format endian, this bit would affect SPI, DPI,  */
        /* @SVD:field=SPI_RD_SEL,bitOffset=19,bitWidth=2,description="spi read line select.\n              0: select line 0\n              1: select line 1\n              2: select line 2\n              3: select line 3" */
        volatile uint32_t SPI_RD_SEL: 2;  /* spi read line select.               0: select line 0         */
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LCD_CONF_Type;

/* @SVD:register=LCD_IF_CONF,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="LCD_IF_CONF" */
/* LCD_IF_CONF 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TAS,bitOffset=0,bitWidth=3,description="setup cycles, delay from LCD_CS active to LCD_WR/LCD_RD active" */
        volatile uint32_t TAS: 3;  /* setup cycles, delay from LCD_CS active to LCD_WR/LCD_RD acti */
        /* @SVD:field=TAH,bitOffset=3,bitWidth=3,description="hold cycles, delay from LCD_WR/LCD_RD inactive to LCD_CS inactive" */
        volatile uint32_t TAH: 3;  /* hold cycles, delay from LCD_WR/LCD_RD inactive to LCD_CS ina */
        /* @SVD:field=PWL,bitOffset=6,bitWidth=6,description="active cycles of LCD_WR/LCD_RD" */
        volatile uint32_t PWL: 6;  /* active cycles of LCD_WR/LCD_RD */
        /* @SVD:field=PWH,bitOffset=12,bitWidth=6,description="inactive cycles of LCD_WR/LCD_RD for consecutive write/read operation" */
        volatile uint32_t PWH: 6;  /* inactive cycles of LCD_WR/LCD_RD for consecutive write/read  */
        /* @SVD:field=CS0_POL,bitOffset=18,bitWidth=1,description="LCD1 CS pin polarity. CS is 0 for LCD chip select if polarity bit is set as 0. CS bit definition is opposite if polarity bit is set as 1." */
        volatile uint32_t CS0_POL: 1;  /* LCD1 CS pin polarity. CS is 0 for LCD chip select if polarit */
        /* @SVD:field=CS1_POL,bitOffset=19,bitWidth=1,description="LCD0 CS pin polarity. CS is 0 for LCD chip select if polarity bit is set as 0. CS bit definition is opposite if polarity bit is set as 1." */
        volatile uint32_t CS1_POL: 1;  /* LCD0 CS pin polarity. CS is 0 for LCD chip select if polarit */
        /* @SVD:field=RS_POL,bitOffset=20,bitWidth=1,description="LCD RS pin polarity. RS is 1 for data access, 0 for command access if polarity bit is set as 0. RS bit definition is opposite if polarity bit is set as 1." */
        volatile uint32_t RS_POL: 1;  /* LCD RS pin polarity. RS is 1 for data access, 0 for command  */
        /* @SVD:field=WR_POL,bitOffset=21,bitWidth=1,description="LCD WR pin polarity. WR is 0 for write operation, 1 for idle if polarity bit is set as 0. WR bit definition is opposite if polarity bit is set as 1." */
        volatile uint32_t WR_POL: 1;  /* LCD WR pin polarity. WR is 0 for write operation, 1 for idle */
        /* @SVD:field=RD_POL,bitOffset=22,bitWidth=1,description="LCD RD pin polarity. RD is 0 for write operation, 1 for idle if polarity bit is set as 0. RD bit definition is opposite if polarity bit is set as 1." */
        volatile uint32_t RD_POL: 1;  /* LCD RD pin polarity. RD is 0 for write operation, 1 for idle */
        /* @SVD:field=LCD_RSTB,bitOffset=23,bitWidth=1,description="LCD RSTB pin, direct to output" */
        volatile uint32_t LCD_RSTB: 1;  /* LCD RSTB pin, direct to output */
        /* @SVD:field=DO_DLY_SET,bitOffset=24,bitWidth=1,description="if this bit is set to 1,  LCD data output will be delayed for 1 lcdc clock cycle" */
        volatile uint32_t DO_DLY_SET: 1;  /* if this bit is set to 1,  LCD data output will be delayed fo */
        /* @SVD:field=CTRL_DLY_SET,bitOffset=25,bitWidth=1,description="if this bit is set to 1,  LCD control output will be delayed for 1 lcdc clock cycle" */
        volatile uint32_t CTRL_DLY_SET: 1;  /* if this bit is set to 1,  LCD control output will be delayed */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LCD_IF_CONF_Type;

/* @SVD:register=LCD_MEM,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="LCD_MEM" */
/* LCD_MEM 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="address for AHB LCD/AHB RAM" */
        volatile uint32_t ADDR: 32;  /* address for AHB LCD/AHB RAM */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LCD_MEM_Type;

/* @SVD:register=LCD_O_WIDTH,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="LCD_O_WIDTH" */
/* LCD_O_WIDTH 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OFFSET,bitOffset=0,bitWidth=16,description="AHB RAM address offset for each line" */
        volatile uint32_t OFFSET: 16;  /* AHB RAM address offset for each line */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LCD_O_WIDTH_Type;

/* @SVD:register=LCD_SINGLE,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="LCD_SINGLE" */
/* LCD_SINGLE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TYPE,bitOffset=0,bitWidth=1,description="LCD access type, this bit could affect all LCD interface including SPI, parellel and AHB\n              1'b0: command\n              1'b1: data" */
        volatile uint32_t TYPE: 1;  /* LCD access type, this bit could affect all LCD interface inc */
        /* @SVD:field=WR_TRIG,bitOffset=1,bitWidth=1,description="Single write operation trigger" */
        volatile uint32_t WR_TRIG: 1;  /* Single write operation trigger */
        /* @SVD:field=RD_TRIG,bitOffset=2,bitWidth=1,description="Single read operation trigger" */
        volatile uint32_t RD_TRIG: 1;  /* Single read operation trigger */
        /* @SVD:field=LCD_BUSY,bitOffset=3,bitWidth=1,description="LCD/SPI LCD interface is busy for single access" */
        volatile uint32_t LCD_BUSY: 1;  /* LCD/SPI LCD interface is busy for single access */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LCD_SINGLE_Type;

/* @SVD:register=LCD_WR,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="LCD_WR" */
/* LCD_WR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="LCD write data" */
        volatile uint32_t DATA: 32;  /* LCD write data */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LCD_WR_Type;

/* @SVD:register=LCD_RD,offset=0x0098,size=32,access=read-write,reset=0x00000000,description="LCD_RD" */
/* LCD_RD 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="LCD read data" */
        volatile uint32_t DATA: 32;  /* LCD read data */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_LCD_RD_Type;

/* @SVD:register=SPI_IF_CONF,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="SPI_IF_CONF" */
/* SPI_IF_CONF 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WAIT_CYCLE,bitOffset=0,bitWidth=6,description="SPI line wait cycle, wait cycle is after each line and is according to SPI clock. 0 refers to no wait cycle." */
        volatile uint32_t WAIT_CYCLE: 6;  /* SPI line wait cycle, wait cycle is after each line and is ac */
        /* @SVD:field=CLK_DIV,bitOffset=6,bitWidth=8,description="SPI clock divider" */
        volatile uint32_t CLK_DIV: 8;  /* SPI clock divider */
        /* @SVD:field=DUMMY_CYCLE,bitOffset=14,bitWidth=3,description="SPI transaction dummy cycle" */
        volatile uint32_t DUMMY_CYCLE: 3;  /* SPI transaction dummy cycle */
        /* @SVD:field=LINE,bitOffset=17,bitWidth=3,description="SPI line mode\n              0: 4-line\n              1: 4-line with 2 data line(support RGB565 and RGB888)\n              2: 4-line with 4 data line(support RGB565 and RGB888)\n              3: reserved\n              4: 3-line \n              5: 3-line with 2 data line(support RGB565 and RGB888)\n              6: 3-line with 4 data line(support RGB565 and RGB888)\n              7: reserved" */
        volatile uint32_t LINE: 3;  /* SPI line mode               0: 4-line               1: 4-lin */
        /* @SVD:field=RD_LEN,bitOffset=20,bitWidth=2,description="SPI read data length(single access)" */
        volatile uint32_t RD_LEN: 2;  /* SPI read data length(single access) */
        /* @SVD:field=WR_LEN,bitOffset=22,bitWidth=2,description="SPI write data length(single access)" */
        volatile uint32_t WR_LEN: 2;  /* SPI write data length(single access) */
        /* @SVD:field=SPI_RD_MODE,bitOffset=24,bitWidth=1,description="SPI read mode:\n              1'b0: normal read. Send write request before read.\n              1'b1: direct read. Read data without write request." */
        volatile uint32_t SPI_RD_MODE: 1;  /* SPI read mode:               1'b0: normal read. Send write r */
        /* @SVD:field=SPI_CLK_AUTO_DIS,bitOffset=25,bitWidth=1,description="1: SPI clock auto disable in wait state during data transaction\n              0: SPI clock is always on in wait state during data transaction" */
        volatile uint32_t SPI_CLK_AUTO_DIS: 1;  /* 1: SPI clock auto disable in wait state during data transact */
        /* @SVD:field=SPI_CS_NO_IDLE,bitOffset=26,bitWidth=1,description="1: SPI CS is always active during data transaction\n              0: SPI CS is IDLE in wait state during data transaction" */
        volatile uint32_t SPI_CS_NO_IDLE: 1;  /* 1: SPI CS is always active during data transaction           */
        /* @SVD:field=SPI_CS_AUTO_DIS,bitOffset=27,bitWidth=1,description="1: SPI CS is automatically disabled after data transaction\n              0: SPI CS is not disabled after data transaction" */
        volatile uint32_t SPI_CS_AUTO_DIS: 1;  /* 1: SPI CS is automatically disabled after data transaction   */
        /* @SVD:field=SPI_CS_POL,bitOffset=28,bitWidth=1,description="SPI CS polarity\n              0: low active\n              1: high active" */
        volatile uint32_t SPI_CS_POL: 1;  /* SPI CS polarity               0: low active               1: */
        /* @SVD:field=SPI_CLK_POL,bitOffset=29,bitWidth=1,description="SPI CLK polarity\n              1'h0: normal\n              1'h1: inverted" */
        volatile uint32_t SPI_CLK_POL: 1;  /* SPI CLK polarity               1'h0: normal               1' */
        /* @SVD:field=SPI_CLK_INIT,bitOffset=30,bitWidth=1,description="SPI CLK idle state value\n              1'h0: high\n              1'h1: low" */
        volatile uint32_t SPI_CLK_INIT: 1;  /* SPI CLK idle state value               1'h0: high            */
        /* @SVD:field=RSVD,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_SPI_IF_CONF_Type;

/* @SVD:register=TE_CONF,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="TE_CONF" */
/* TE_CONF 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="TE enable" */
        volatile uint32_t ENABLE: 1;  /* TE enable */
        /* @SVD:field=FMARK_POL,bitOffset=1,bitWidth=1,description="TE signal polarity" */
        volatile uint32_t FMARK_POL: 1;  /* TE signal polarity */
        /* @SVD:field=MODE,bitOffset=2,bitWidth=1,description="0: vsync only TE mode \n              1: vsync+hsync TE mode" */
        volatile uint32_t MODE: 1;  /* 0: vsync only TE mode                1: vsync+hsync TE mode */
        /* @SVD:field=VSYNC_DET_CNT,bitOffset=3,bitWidth=16,description="vsync signal detect counter, used for mode 1 to detect vsync signal" */
        volatile uint32_t VSYNC_DET_CNT: 16;  /* vsync signal detect counter, used for mode 1 to detect vsync */
        /* @SVD:field=FMARK_MODE,bitOffset=19,bitWidth=1,description="TE signal trigger mode\n              1: edge trigger\n              0: pulse trigger" */
        volatile uint32_t FMARK_MODE: 1;  /* TE signal trigger mode               1: edge trigger         */
        /* @SVD:field=FMARK_SOURCE,bitOffset=20,bitWidth=1,description="TE signal source\n              1: use TE signal from DSI\n              0: use TE signal from external pin" */
        volatile uint32_t FMARK_SOURCE: 1;  /* TE signal source               1: use TE signal from DSI     */
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_TE_CONF_Type;

/* @SVD:register=TE_CONF2,offset=0x00A4,size=32,access=read-write,reset=0x00000000,description="TE_CONF2" */
/* TE_CONF2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DLY_CNT,bitOffset=0,bitWidth=32,description="TE delay counter" */
        volatile uint32_t DLY_CNT: 32;  /* TE delay counter */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_TE_CONF2_Type;

/* @SVD:register=DPI_IF_CONF1,offset=0x00A8,size=32,access=read-write,reset=0x00000000,description="DPI_IF_CONF1" */
/* DPI_IF_CONF1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VSH,bitOffset=0,bitWidth=11,description="dpi vsync height" */
        volatile uint32_t VSH: 11;  /* dpi vsync height */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=HSW,bitOffset=16,bitWidth=11,description="dpi hsync width" */
        volatile uint32_t HSW: 11;  /* dpi hsync width */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_DPI_IF_CONF1_Type;

/* @SVD:register=DPI_IF_CONF2,offset=0x00AC,size=32,access=read-write,reset=0x00000000,description="DPI_IF_CONF2" */
/* DPI_IF_CONF2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VBP,bitOffset=0,bitWidth=11,description="vertical back porch" */
        volatile uint32_t VBP: 11;  /* vertical back porch */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=HBP,bitOffset=16,bitWidth=11,description="horizontal back porch" */
        volatile uint32_t HBP: 11;  /* horizontal back porch */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_DPI_IF_CONF2_Type;

/* @SVD:register=DPI_IF_CONF3,offset=0x00B0,size=32,access=read-write,reset=0x00000000,description="DPI_IF_CONF3" */
/* DPI_IF_CONF3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VFP,bitOffset=0,bitWidth=11,description="vertical front porch" */
        volatile uint32_t VFP: 11;  /* vertical front porch */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=HFP,bitOffset=16,bitWidth=11,description="horizontal front porch" */
        volatile uint32_t HFP: 11;  /* horizontal front porch */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_DPI_IF_CONF3_Type;

/* @SVD:register=DPI_IF_CONF4,offset=0x00B4,size=32,access=read-write,reset=0x00000000,description="DPI_IF_CONF4" */
/* DPI_IF_CONF4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAH,bitOffset=0,bitWidth=11,description="vertical active height" */
        volatile uint32_t VAH: 11;  /* vertical active height */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=HAW,bitOffset=16,bitWidth=11,description="horizontal active width" */
        volatile uint32_t HAW: 11;  /* horizontal active width */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_DPI_IF_CONF4_Type;

/* @SVD:register=DPI_IF_CONF5,offset=0x00B8,size=32,access=read-write,reset=0x00000000,description="DPI_IF_CONF5" */
/* DPI_IF_CONF5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PCLK_DIV,bitOffset=0,bitWidth=8,description="pixel clock divider" */
        volatile uint32_t PCLK_DIV: 8;  /* pixel clock divider */
        /* @SVD:field=PCLKPOL,bitOffset=8,bitWidth=1,description="pixel clock polarity" */
        volatile uint32_t PCLKPOL: 1;  /* pixel clock polarity */
        /* @SVD:field=DEPOL,bitOffset=9,bitWidth=1,description="de polarity" */
        volatile uint32_t DEPOL: 1;  /* de polarity */
        /* @SVD:field=VSPOL,bitOffset=10,bitWidth=1,description="vsync polarity" */
        volatile uint32_t VSPOL: 1;  /* vsync polarity */
        /* @SVD:field=HSPOL,bitOffset=11,bitWidth=1,description="hsync polarity" */
        volatile uint32_t HSPOL: 1;  /* hsync polarity */
        /* @SVD:field=INT_LINE_NUM,bitOffset=12,bitWidth=11,description="DPI interrupt line number" */
        volatile uint32_t INT_LINE_NUM: 11;  /* DPI interrupt line number */
        /* @SVD:field=CLK_FORCE_ON,bitOffset=23,bitWidth=1,description="1: force DPI clock on\n              0: DPI clock is controlled by hardware" */
        volatile uint32_t CLK_FORCE_ON: 1;  /* 1: force DPI clock on               0: DPI clock is controll */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_DPI_IF_CONF5_Type;

/* @SVD:register=DPI_CTRL,offset=0x00BC,size=32,access=read-write,reset=0x00000000,description="DPI_CTRL" */
/* DPI_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DPI_EN,bitOffset=0,bitWidth=1,description="dpi interface enable" */
        volatile uint32_t DPI_EN: 1;  /* dpi interface enable */
        /* @SVD:field=DPI_CM,bitOffset=1,bitWidth=1,description="dpi color mode" */
        volatile uint32_t DPI_CM: 1;  /* dpi color mode */
        /* @SVD:field=DPI_SD,bitOffset=2,bitWidth=1,description="dpi shutdown" */
        volatile uint32_t DPI_SD: 1;  /* dpi shutdown */
        /* @SVD:field=DPI_UC,bitOffset=3,bitWidth=1,description="dpi update config" */
        volatile uint32_t DPI_UC: 1;  /* dpi update config */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_DPI_CTRL_Type;

/* @SVD:register=DPI_STAT,offset=0x00C0,size=32,access=read-write,reset=0x00000000,description="DPI_STAT" */
/* DPI_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HPOS,bitOffset=0,bitWidth=11,description="dpi horizontal position" */
        volatile uint32_t HPOS: 11;  /* dpi horizontal position */
        /* @SVD:field=HSTAT,bitOffset=11,bitWidth=3,description="horizontal status\n              0: idle\n              1: prep\n              2: hsync\n              3: hbp\n              4: hact\n              5: hfp\n              6: wait" */
        volatile uint32_t HSTAT: 3;  /* horizontal status               0: idle               1: pre */
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=VPOS,bitOffset=16,bitWidth=16,description="dpi vertical position" */
        volatile uint32_t VPOS: 16;  /* dpi vertical position */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_DPI_STAT_Type;

/* @SVD:register=JDI_SER_CONF1,offset=0x00C4,size=32,access=read-write,reset=0x00000000,description="JDI_SER_CONF1" */
/* JDI_SER_CONF1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WR_LEN,bitOffset=0,bitWidth=5,description="jdi single write bit length" */
        volatile uint32_t WR_LEN: 5;  /* jdi single write bit length */
        /* @SVD:field=RSVD2,bitOffset=5,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=CLK_DIV,bitOffset=8,bitWidth=8,description="jdi serial clock divider" */
        volatile uint32_t CLK_DIV: 8;  /* jdi serial clock divider */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_SER_CONF1_Type;

/* @SVD:register=JDI_SER_CONF2,offset=0x00C8,size=32,access=read-write,reset=0x00000000,description="JDI_SER_CONF2" */
/* JDI_SER_CONF2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WR_CMD,bitOffset=0,bitWidth=16,description="jdi serial data transfer write command" */
        volatile uint32_t WR_CMD: 16;  /* jdi serial data transfer write command */
        /* @SVD:field=INIT_LINE_CNT,bitOffset=16,bitWidth=16,description="jdi serial init line counter" */
        volatile uint32_t INIT_LINE_CNT: 16;  /* jdi serial init line counter */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_SER_CONF2_Type;

/* @SVD:register=JDI_SER_CTRL,offset=0x00CC,size=32,access=read-write,reset=0x00000000,description="JDI_SER_CTRL" */
/* JDI_SER_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DISP,bitOffset=0,bitWidth=1,description="jdi serial interface disp control" */
        volatile uint32_t DISP: 1;  /* jdi serial interface disp control */
        /* @SVD:field=EXTCOMIN,bitOffset=1,bitWidth=1,description="jdi serial interface extcomin control" */
        volatile uint32_t EXTCOMIN: 1;  /* jdi serial interface extcomin control */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_SER_CTRL_Type;

/* @SVD:register=JDI_PAR_CONF1,offset=0x00D0,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF1" */
/* JDI_PAR_CONF1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MAX_COL,bitOffset=0,bitWidth=16,description="jdi parallel interface max column, column number start from 0" */
        volatile uint32_t MAX_COL: 16;  /* jdi parallel interface max column, column number start from  */
        /* @SVD:field=MAX_LINE,bitOffset=16,bitWidth=16,description="jdi parallel interface max line, line number start from 0" */
        volatile uint32_t MAX_LINE: 16;  /* jdi parallel interface max line, line number start from 0 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF1_Type;

/* @SVD:register=JDI_PAR_CONF2,offset=0x00D4,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF2" */
/* JDI_PAR_CONF2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=END_LINE,bitOffset=0,bitWidth=16,description="jdi parallel interface end line, line number start from 0" */
        volatile uint32_t END_LINE: 16;  /* jdi parallel interface end line, line number start from 0 */
        /* @SVD:field=ST_LINE,bitOffset=16,bitWidth=16,description="jdi parallel interface start line, line number start from 0" */
        volatile uint32_t ST_LINE: 16;  /* jdi parallel interface start line, line number start from 0 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF2_Type;

/* @SVD:register=JDI_PAR_CONF3,offset=0x00D8,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF3" */
/* JDI_PAR_CONF3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=END_COL,bitOffset=0,bitWidth=16,description="jdi parallel interface end column, column number start from 0" */
        volatile uint32_t END_COL: 16;  /* jdi parallel interface end column, column number start from  */
        /* @SVD:field=ST_COL,bitOffset=16,bitWidth=16,description="jdi parallel interface start column, column number start from 0" */
        volatile uint32_t ST_COL: 16;  /* jdi parallel interface start column, column number start fro */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF3_Type;

/* @SVD:register=JDI_PAR_CONF4,offset=0x00DC,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF4" */
/* JDI_PAR_CONF4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HST_WIDTH,bitOffset=0,bitWidth=16,description="jdi parallel interface HST width, HST width = lcd_ck_cycle * HST_WIDTH" */
        volatile uint32_t HST_WIDTH: 16;  /* jdi parallel interface HST width, HST width = lcd_ck_cycle * */
        /* @SVD:field=HCK_WIDTH,bitOffset=16,bitWidth=16,description="jdi parallel interface HCK width, HSK width = lcd_ck_cycle * HCK_WIDTH" */
        volatile uint32_t HCK_WIDTH: 16;  /* jdi parallel interface HCK width, HSK width = lcd_ck_cycle * */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF4_Type;

/* @SVD:register=JDI_PAR_CONF5,offset=0x00E0,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF5" */
/* JDI_PAR_CONF5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VST_WIDTH,bitOffset=0,bitWidth=16,description="jdi parallel interface VST width, VST width = lcd_ck_cycle * VST_WIDTH" */
        volatile uint32_t VST_WIDTH: 16;  /* jdi parallel interface VST width, VST width = lcd_ck_cycle * */
        /* @SVD:field=VCK_WIDTH,bitOffset=16,bitWidth=16,description="jdi parallel interface VCK width, VCK width = lcd_ck_cycle * VCK_WIDTH" */
        volatile uint32_t VCK_WIDTH: 16;  /* jdi parallel interface VCK width, VCK width = lcd_ck_cycle * */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF5_Type;

/* @SVD:register=JDI_PAR_CONF6,offset=0x00E4,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF6" */
/* JDI_PAR_CONF6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HST_DLY,bitOffset=0,bitWidth=16,description="jdi parallel interface VCK to HST delay, VCK2HST delay = lcd_ck_cycle * HST_DLY" */
        volatile uint32_t HST_DLY: 16;  /* jdi parallel interface VCK to HST delay, VCK2HST delay = lcd */
        /* @SVD:field=VCK_DLY,bitOffset=16,bitWidth=16,description="jdi parallel interface VST to VCK delay, VST2VCK delay = lcd_ck_cycle * VCK_DLY" */
        volatile uint32_t VCK_DLY: 16;  /* jdi parallel interface VST to VCK delay, VST2VCK delay = lcd */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF6_Type;

/* @SVD:register=JDI_PAR_CONF7,offset=0x00E8,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF7" */
/* JDI_PAR_CONF7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HCK_DLY,bitOffset=0,bitWidth=16,description="jdi parallel interface HST to HCK delay" */
        volatile uint32_t HCK_DLY: 16;  /* jdi parallel interface HST to HCK delay */
        /* @SVD:field=DP_MODE,bitOffset=16,bitWidth=1,description="double pixel mode. Some jdi parallel screens use large pixel+small pixel structure. Set this bit to 1 to support this structure." */
        volatile uint32_t DP_MODE: 1;  /* double pixel mode. Some jdi parallel screens use large pixel */
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF7_Type;

/* @SVD:register=JDI_PAR_CTRL,offset=0x00EC,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CTRL" */
/* JDI_PAR_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="jdi parallel interface enable" */
        volatile uint32_t ENABLE: 1;  /* jdi parallel interface enable */
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=XRST,bitOffset=4,bitWidth=1,description="jdi parallel interface XRST" */
        volatile uint32_t XRST: 1;  /* jdi parallel interface XRST */
        /* @SVD:field=ENBPOL,bitOffset=5,bitWidth=1,description="jdi parallel enb polarity" */
        volatile uint32_t ENBPOL: 1;  /* jdi parallel enb polarity */
        /* @SVD:field=HCKPOL,bitOffset=6,bitWidth=1,description="jdi parallel hck polarity" */
        volatile uint32_t HCKPOL: 1;  /* jdi parallel hck polarity */
        /* @SVD:field=HSTPOL,bitOffset=7,bitWidth=1,description="jdi parallel hst polarity" */
        volatile uint32_t HSTPOL: 1;  /* jdi parallel hst polarity */
        /* @SVD:field=VCKPOL,bitOffset=8,bitWidth=1,description="jdi parallel vck polarity" */
        volatile uint32_t VCKPOL: 1;  /* jdi parallel vck polarity */
        /* @SVD:field=VSTPOL,bitOffset=9,bitWidth=1,description="jdi parallel vst polarity" */
        volatile uint32_t VSTPOL: 1;  /* jdi parallel vst polarity */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=INT_LINE_NUM,bitOffset=16,bitWidth=16,description="jdi parallel interface interrupt line number, line number start from 0." */
        volatile uint32_t INT_LINE_NUM: 16;  /* jdi parallel interface interrupt line number, line number st */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CTRL_Type;

/* @SVD:register=JDI_PAR_STAT,offset=0x00F0,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_STAT" */
/* JDI_PAR_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HPOS,bitOffset=0,bitWidth=16,description="jdi parallel horizontal position" */
        volatile uint32_t HPOS: 16;  /* jdi parallel horizontal position */
        /* @SVD:field=VPOS,bitOffset=16,bitWidth=16,description="jdi parallel vertical position" */
        volatile uint32_t VPOS: 16;  /* jdi parallel vertical position */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_STAT_Type;

/* @SVD:register=JDI_PAR_EX_CTRL,offset=0x00F4,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_EX_CTRL" */
/* JDI_PAR_EX_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MAX_CNT,bitOffset=0,bitWidth=24,description="VCOM/FRP/XFRP max counter" */
        volatile uint32_t MAX_CNT: 24;  /* VCOM/FRP/XFRP max counter */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
        /* @SVD:field=CNT_EN,bitOffset=28,bitWidth=1,description="VCOM/FRP/XFRP counter enable" */
        volatile uint32_t CNT_EN: 1;  /* VCOM/FRP/XFRP counter enable */
        /* @SVD:field=XFRP,bitOffset=29,bitWidth=1,description="XFRP value" */
        volatile uint32_t XFRP: 1;  /* XFRP value */
        /* @SVD:field=FRP,bitOffset=30,bitWidth=1,description="FRP value" */
        volatile uint32_t FRP: 1;  /* FRP value */
        /* @SVD:field=VCOM,bitOffset=31,bitWidth=1,description="VCOM value" */
        volatile uint32_t VCOM: 1;  /* VCOM value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_EX_CTRL_Type;

/* @SVD:register=JDI_PAR_CONF8,offset=0x00F8,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF8" */
/* JDI_PAR_CONF8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENB_END_COL,bitOffset=0,bitWidth=16,description="jdi parallel interface enb end column, column number start from 0" */
        volatile uint32_t ENB_END_COL: 16;  /* jdi parallel interface enb end column, column number start f */
        /* @SVD:field=ENB_ST_COL,bitOffset=16,bitWidth=16,description="jdi parallel interface enb start column, column number start from 0" */
        volatile uint32_t ENB_ST_COL: 16;  /* jdi parallel interface enb start column, column number start */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF8_Type;

/* @SVD:register=JDI_PAR_CONF9,offset=0x00FC,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF9" */
/* JDI_PAR_CONF9 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENB_END_LINE,bitOffset=0,bitWidth=16,description="jdi parallel interface enb end line, line number start from 0" */
        volatile uint32_t ENB_END_LINE: 16;  /* jdi parallel interface enb end line, line number start from  */
        /* @SVD:field=ENB_ST_LINE,bitOffset=16,bitWidth=16,description="jdi parallel interface enb start line, line number start from 0" */
        volatile uint32_t ENB_ST_LINE: 16;  /* jdi parallel interface enb start line, line number start fro */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF9_Type;

/* @SVD:register=JDI_PAR_CONF10,offset=0x0100,size=32,access=read-write,reset=0x00000000,description="JDI_PAR_CONF10" */
/* JDI_PAR_CONF10 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HC_END_LINE,bitOffset=0,bitWidth=16,description="jdi parallel interface horizontal control end line, line number start from 0" */
        volatile uint32_t HC_END_LINE: 16;  /* jdi parallel interface horizontal control end line, line num */
        /* @SVD:field=HC_ST_LINE,bitOffset=16,bitWidth=16,description="jdi parallel interface horizontal control start line, line number start from 0" */
        volatile uint32_t HC_ST_LINE: 16;  /* jdi parallel interface horizontal control start line, line n */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_JDI_PAR_CONF10_Type;

/* @SVD:register=CANVAS_STAT0,offset=0x0110,size=32,access=read-write,reset=0x00000000,description="CANVAS_STAT0" */
/* CANVAS_STAT0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=X_COR,bitOffset=0,bitWidth=11,description="canvas x cordinate" */
        volatile uint32_t X_COR: 11;  /* canvas x cordinate */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=Y_COR,bitOffset=16,bitWidth=11,description="canvas y cordinate" */
        volatile uint32_t Y_COR: 11;  /* canvas y cordinate */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_CANVAS_STAT0_Type;

/* @SVD:register=CANVAS_STAT1,offset=0x0114,size=32,access=read-write,reset=0x00000000,description="CANVAS_STAT1" */
/* CANVAS_STAT1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FIFO_CNT,bitOffset=0,bitWidth=3,description="pre calc fifo count" */
        volatile uint32_t FIFO_CNT: 3;  /* pre calc fifo count */
        /* @SVD:field=POSTC_STAT,bitOffset=3,bitWidth=3,description="postc_status" */
        volatile uint32_t POSTC_STAT: 3;  /* postc_status */
        /* @SVD:field=PREC_STAT,bitOffset=6,bitWidth=3,description="prec status" */
        volatile uint32_t PREC_STAT: 3;  /* prec status */
        /* @SVD:field=FETCH_STAT,bitOffset=9,bitWidth=3,description="fetch status" */
        volatile uint32_t FETCH_STAT: 3;  /* fetch status */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_CANVAS_STAT1_Type;

/* @SVD:register=OL0_STAT,offset=0x0118,size=32,access=read-write,reset=0x00000000,description="OL0_STAT" */
/* OL0_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DONE_REQ,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t DONE_REQ: 1;
        /* @SVD:field=PREFETCH_OUT,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_OUT: 1;
        /* @SVD:field=PREFETCH_READ,bitOffset=2,bitWidth=2,description="" */
        volatile uint32_t PREFETCH_READ: 2;
        /* @SVD:field=DATA_CONV,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t DATA_CONV: 2;
        /* @SVD:field=PF_DF,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t PF_DF: 2;
        /* @SVD:field=PF_PR,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PF_PR: 3;
        /* @SVD:field=SC_OUT,bitOffset=11,bitWidth=2,description="" */
        volatile uint32_t SC_OUT: 2;
        /* @SVD:field=SC_BE,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t SC_BE: 3;
        /* @SVD:field=SC_FE,bitOffset=16,bitWidth=4,description="" */
        volatile uint32_t SC_FE: 4;
        /* @SVD:field=SC_LB1,bitOffset=20,bitWidth=2,description="" */
        volatile uint32_t SC_LB1: 2;
        /* @SVD:field=SC_LB0,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t SC_LB0: 2;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_OL0_STAT_Type;

/* @SVD:register=OL1_STAT,offset=0x011C,size=32,access=read-write,reset=0x00000000,description="OL1_STAT" */
/* OL1_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DONE_REQ,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t DONE_REQ: 1;
        /* @SVD:field=PREFETCH_OUT,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t PREFETCH_OUT: 1;
        /* @SVD:field=PREFETCH_READ,bitOffset=2,bitWidth=2,description="" */
        volatile uint32_t PREFETCH_READ: 2;
        /* @SVD:field=DATA_CONV,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t DATA_CONV: 2;
        /* @SVD:field=PF_DF,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t PF_DF: 2;
        /* @SVD:field=PF_PR,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PF_PR: 3;
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_OL1_STAT_Type;

/* @SVD:register=MEM_IF_STAT,offset=0x0120,size=32,access=read-write,reset=0x00000000,description="MEM_IF_STAT" */
/* MEM_IF_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=AHB,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t AHB: 4;
        /* @SVD:field=ARB_READ_PORT,bitOffset=4,bitWidth=3,description="" */
        volatile uint32_t ARB_READ_PORT: 3;
        /* @SVD:field=ARB_MAIN,bitOffset=7,bitWidth=3,description="" */
        volatile uint32_t ARB_MAIN: 3;
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_MEM_IF_STAT_Type;

/* @SVD:register=PERF_CNT,offset=0x0124,size=32,access=read-write,reset=0x00000000,description="PERF_CNT" */
/* PERF_CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="lcdc performance counter" */
        volatile uint32_t VAL: 32;  /* lcdc performance counter */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LCDC1_PERF_CNT_Type;

/* LCDC1 寄存器结构体 */
typedef struct
{
    LCDC1_COMMAND_Type COMMAND;  /* 0x0000 - COMMAND */
    LCDC1_STATUS_Type STATUS;  /* 0x0004 - STATUS */
    LCDC1_IRQ_Type IRQ;  /* 0x0008 - IRQ */
    LCDC1_SETTING_Type SETTING;  /* 0x000C - SETTING */
    LCDC1_CANVAS_TL_POS_Type CANVAS_TL_POS;  /* 0x0010 - CANVAS_TL_POS */
    LCDC1_CANVAS_BR_POS_Type CANVAS_BR_POS;  /* 0x0014 - CANVAS_BR_POS */
    LCDC1_CANVAS_BG_Type CANVAS_BG;  /* 0x0018 - CANVAS_BG */
    LCDC1_LAYER0_CONFIG_Type LAYER0_CONFIG;  /* 0x001C - LAYER0_CONFIG */
    LCDC1_LAYER0_TL_POS_Type LAYER0_TL_POS;  /* 0x0020 - LAYER0_TL_POS */
    LCDC1_LAYER0_BR_POS_Type LAYER0_BR_POS;  /* 0x0024 - LAYER0_BR_POS */
    LCDC1_LAYER0_FILTER_Type LAYER0_FILTER;  /* 0x0028 - LAYER0_FILTER */
    LCDC1_LAYER0_SRC_Type LAYER0_SRC;  /* 0x002C - LAYER0_SRC */
    LCDC1_LAYER0_FILL_Type LAYER0_FILL;  /* 0x0030 - LAYER0_FILL */
    LCDC1_LAYER0_DECOMP_Type LAYER0_DECOMP;  /* 0x0034 - LAYER0_DECOMP */
    LCDC1_LAYER0_DECOMP_CFG0_Type LAYER0_DECOMP_CFG0;  /* 0x0038 - LAYER0_DECOMP_CFG0 */
    LCDC1_LAYER0_DECOMP_CFG1_Type LAYER0_DECOMP_CFG1;  /* 0x003C - LAYER0_DECOMP_CFG1 */
    LCDC1_LAYER0_DECOMP_STAT_Type LAYER0_DECOMP_STAT;  /* 0x0040 - LAYER0_DECOMP_STAT */
    /* @SVD:register=RSVD2,offset=0x0044,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x0044 - RSVD2 */
    volatile uint32_t RESERVED18[6];  /* 0x0048 - 0x005F */
    LCDC1_LAYER1_CONFIG_Type LAYER1_CONFIG;  /* 0x0060 - LAYER1_CONFIG */
    LCDC1_LAYER1_TL_POS_Type LAYER1_TL_POS;  /* 0x0064 - LAYER1_TL_POS */
    LCDC1_LAYER1_BR_POS_Type LAYER1_BR_POS;  /* 0x0068 - LAYER1_BR_POS */
    LCDC1_LAYER1_FILTER_Type LAYER1_FILTER;  /* 0x006C - LAYER1_FILTER */
    LCDC1_LAYER1_SRC_Type LAYER1_SRC;  /* 0x0070 - LAYER1_SRC */
    LCDC1_LAYER1_FILL_Type LAYER1_FILL;  /* 0x0074 - LAYER1_FILL */
    LCDC1_DITHER_CONF_Type DITHER_CONF;  /* 0x0078 - DITHER_CONF */
    LCDC1_DITHER_LFSR_Type DITHER_LFSR;  /* 0x007C - DITHER_LFSR */
    LCDC1_LCD_CONF_Type LCD_CONF;  /* 0x0080 - LCD_CONF */
    LCDC1_LCD_IF_CONF_Type LCD_IF_CONF;  /* 0x0084 - LCD_IF_CONF */
    LCDC1_LCD_MEM_Type LCD_MEM;  /* 0x0088 - LCD_MEM */
    LCDC1_LCD_O_WIDTH_Type LCD_O_WIDTH;  /* 0x008C - LCD_O_WIDTH */
    LCDC1_LCD_SINGLE_Type LCD_SINGLE;  /* 0x0090 - LCD_SINGLE */
    LCDC1_LCD_WR_Type LCD_WR;  /* 0x0094 - LCD_WR */
    LCDC1_LCD_RD_Type LCD_RD;  /* 0x0098 - LCD_RD */
    LCDC1_SPI_IF_CONF_Type SPI_IF_CONF;  /* 0x009C - SPI_IF_CONF */
    LCDC1_TE_CONF_Type TE_CONF;  /* 0x00A0 - TE_CONF */
    LCDC1_TE_CONF2_Type TE_CONF2;  /* 0x00A4 - TE_CONF2 */
    LCDC1_DPI_IF_CONF1_Type DPI_IF_CONF1;  /* 0x00A8 - DPI_IF_CONF1 */
    LCDC1_DPI_IF_CONF2_Type DPI_IF_CONF2;  /* 0x00AC - DPI_IF_CONF2 */
    LCDC1_DPI_IF_CONF3_Type DPI_IF_CONF3;  /* 0x00B0 - DPI_IF_CONF3 */
    LCDC1_DPI_IF_CONF4_Type DPI_IF_CONF4;  /* 0x00B4 - DPI_IF_CONF4 */
    LCDC1_DPI_IF_CONF5_Type DPI_IF_CONF5;  /* 0x00B8 - DPI_IF_CONF5 */
    LCDC1_DPI_CTRL_Type DPI_CTRL;  /* 0x00BC - DPI_CTRL */
    LCDC1_DPI_STAT_Type DPI_STAT;  /* 0x00C0 - DPI_STAT */
    LCDC1_JDI_SER_CONF1_Type JDI_SER_CONF1;  /* 0x00C4 - JDI_SER_CONF1 */
    LCDC1_JDI_SER_CONF2_Type JDI_SER_CONF2;  /* 0x00C8 - JDI_SER_CONF2 */
    LCDC1_JDI_SER_CTRL_Type JDI_SER_CTRL;  /* 0x00CC - JDI_SER_CTRL */
    LCDC1_JDI_PAR_CONF1_Type JDI_PAR_CONF1;  /* 0x00D0 - JDI_PAR_CONF1 */
    LCDC1_JDI_PAR_CONF2_Type JDI_PAR_CONF2;  /* 0x00D4 - JDI_PAR_CONF2 */
    LCDC1_JDI_PAR_CONF3_Type JDI_PAR_CONF3;  /* 0x00D8 - JDI_PAR_CONF3 */
    LCDC1_JDI_PAR_CONF4_Type JDI_PAR_CONF4;  /* 0x00DC - JDI_PAR_CONF4 */
    LCDC1_JDI_PAR_CONF5_Type JDI_PAR_CONF5;  /* 0x00E0 - JDI_PAR_CONF5 */
    LCDC1_JDI_PAR_CONF6_Type JDI_PAR_CONF6;  /* 0x00E4 - JDI_PAR_CONF6 */
    LCDC1_JDI_PAR_CONF7_Type JDI_PAR_CONF7;  /* 0x00E8 - JDI_PAR_CONF7 */
    LCDC1_JDI_PAR_CTRL_Type JDI_PAR_CTRL;  /* 0x00EC - JDI_PAR_CTRL */
    LCDC1_JDI_PAR_STAT_Type JDI_PAR_STAT;  /* 0x00F0 - JDI_PAR_STAT */
    LCDC1_JDI_PAR_EX_CTRL_Type JDI_PAR_EX_CTRL;  /* 0x00F4 - JDI_PAR_EX_CTRL */
    LCDC1_JDI_PAR_CONF8_Type JDI_PAR_CONF8;  /* 0x00F8 - JDI_PAR_CONF8 */
    LCDC1_JDI_PAR_CONF9_Type JDI_PAR_CONF9;  /* 0x00FC - JDI_PAR_CONF9 */
    LCDC1_JDI_PAR_CONF10_Type JDI_PAR_CONF10;  /* 0x0100 - JDI_PAR_CONF10 */
    /* @SVD:register=RSVD1,offset=0x0104,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0104 - RSVD1 */
    volatile uint32_t RESERVED66[2];  /* 0x0108 - 0x010F */
    LCDC1_CANVAS_STAT0_Type CANVAS_STAT0;  /* 0x0110 - CANVAS_STAT0 */
    LCDC1_CANVAS_STAT1_Type CANVAS_STAT1;  /* 0x0114 - CANVAS_STAT1 */
    LCDC1_OL0_STAT_Type OL0_STAT;  /* 0x0118 - OL0_STAT */
    LCDC1_OL1_STAT_Type OL1_STAT;  /* 0x011C - OL1_STAT */
    LCDC1_MEM_IF_STAT_Type MEM_IF_STAT;  /* 0x0120 - MEM_IF_STAT */
    LCDC1_PERF_CNT_Type PERF_CNT;  /* 0x0124 - PERF_CNT */
} LCDC1_TypeDef;

/* ============================================================================ */
/* I2S1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=I2S1,base=0x50009000,description="",groupName=I2S */

#define I2S1_BASE                                          (0x50009000UL)

/* @SVD:register=TX_PCM_FORMAT,offset=0x0010,size=32,access=read-write,reset=0x00000010,description="TX_PCM_FORMAT" */
/* TX_PCM_FORMAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DW,bitOffset=0,bitWidth=5,description="tx source pcm data width N(N>=8)\n              common value is 8,13,14,16,18,20,22,24\n              This data width indicate the tx fifo output data width.\n              When writing to tx fifo, please refer to following format:\n                  Mono 8 bit: fifo_data[31:0] = {L3,L2,L1,L0}, each word contains 4 samples, so four samples need read one word\n                  Stereo 8 bit: fifo_data[31:0] = { R1,L1,R0,L0 }, each word contains 2 samples, so two samples need read one word\n                  Mono 13/14/16 bit: fifo_data[31:0] = {L1,L0}, each word contains 2 samples, so two samples need read one word\n                  Stereo 13/14/16 bit: fifo_data[31:0] = {R0,L0}, each word contains 1 samples, so each sample need read one word\n                  Mono 18/20/22/24 bit: fifo_data[31:0] = L0, each word contains 1 samples, so each sample need read one word \n                  Stereo 18/20/22/24 bit: fifo_data[31:0][0] = {L0}, fifo_data[31:0][1]={R0}, each 2 words contain 1 samples, so each sample need read two word" */
        volatile uint32_t DW: 5;  /* tx source pcm data width N(N>=8)               common value  */
        /* @SVD:field=TRACK_FLAG,bitOffset=5,bitWidth=1,description="0: stereo\n              1: mono" */
        volatile uint32_t TRACK_FLAG: 1;  /* 0: stereo               1: mono */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_PCM_FORMAT_Type;

/* @SVD:register=TX_PCM_SAMPLE_CLK,offset=0x0020,size=32,access=read-write,reset=0x000000FA,description="TX_PCM_SAMPLE_CLK" */
/* TX_PCM_SAMPLE_CLK 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FS_DUTY,bitOffset=0,bitWidth=13,description="source PCM sample clock duty cycle(with GCLK=12MHz):\n              250 for 48K FS\n              272 for 44.1K FS\n              375 for 32K FS\n              500 for 24K FS\n              544 for 22.05K FS\n              750 for 16K FS\n              1000 for 12K FS\n              1088 for 11.025K FS\n              1500 for 8K FS" */
        volatile uint32_t FS_DUTY: 13;  /* source PCM sample clock duty cycle(with GCLK=12MHz):         */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_PCM_SAMPLE_CLK_Type;

/* @SVD:register=TX_RS_SMOOTH,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="TX_RS_SMOOTH" */
/* TX_RS_SMOOTH 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="0: Disable TX re-sample smooth filter\n              1: Enable TX re-sample smooth filter\n              This function is not implemented." */
        volatile uint32_t EN: 1;  /* 0: Disable TX re-sample smooth filter               1: Enabl */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_RS_SMOOTH_Type;

/* @SVD:register=TX_PCM_CH_SEL,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="TX_PCM_CH_SEL" */
/* TX_PCM_CH_SEL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RIGHT_CHANNEL_SEL,bitOffset=0,bitWidth=2,description="TX re-sampling module setting:\n              00: TX right = source right\n              01: TX right = source left\n              10,11: TX right = (source left + source right)/2" */
        volatile uint32_t RIGHT_CHANNEL_SEL: 2;  /* TX re-sampling module setting:               00: TX right =  */
        /* @SVD:field=LEFT_CHANNEL_SEL,bitOffset=2,bitWidth=2,description="TX re-sampling module setting:\n              00: TX left = source left\n              01: TX left = source right\n              10,11: TX left = (source left + source right)/2" */
        volatile uint32_t LEFT_CHANNEL_SEL: 2;  /* TX re-sampling module setting:               00: TX left = s */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_PCM_CH_SEL_Type;

/* @SVD:register=TX_VOL_CTRL,offset=0x0050,size=32,access=read-write,reset=0x0000000F,description="TX_VOL_CTRL" */
/* TX_VOL_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VOL,bitOffset=0,bitWidth=4,description="volume control:\n              0000: +6dB,    0001: +4.5dB,\n              0010: +3dB,    0011: +1.5dB,\n              0100: 0dB,     0101: -1.5dB,\n              0110: -3.0dB,  0111: -4.5dB,\n              1000: -6.0dB,  1001: -7.5dB,\n              1010: -9dB,    1011: -10.5dB,\n              1100: -12dB,   1101: -13.5dB,\n              1110: -15dB,   1111: mute\n              Note:\n              1) +1.5db = 20log(1+1/4-1/16+1/1024)\n              2) -1.5dB = 20log(1-1/8-1/32-1/512-1/2048)" */
        volatile uint32_t VOL: 4;  /* volume control:               0000: +6dB,    0001: +4.5dB,   */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_VOL_CTRL_Type;

/* @SVD:register=TX_LR_BAL_CTRL,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="TX_LR_BAL_CTRL" */
/* TX_LR_BAL_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BAL_VOL,bitOffset=0,bitWidth=4,description="Balance volume control:\n              0000: Reserved,  0001: -1.5dB,\n              0010: -3.0dB,  0011: -4.5dB,\n              0100: -6.0dB,  0101: -7.5dB,\n              0110: -9.0dB,  0111: -10.5dB,\n              1000: -12dB,   1001: -13.5dB,\n              1010: -15dB,   1011: -16.5dB,\n              1100: -18dB,   1101: -19.5dB,\n              1110: -21dB,   1111: mute\n              Note:\n                1) bit[5:0] = 101111 for left mute\n                2) bit[5:0] = 011111 for right mute\n                3) bit[5:4] = 00 or 11, bit[3:0] is don't care\n                4) +1.5db = 20log(1+1/4-1/16+1/1024)\n                5) -1.5dB = 20log(1-1/8-1/32-1/512-1/2048)" */
        volatile uint32_t BAL_VOL: 4;  /* Balance volume control:               0000: Reserved,  0001: */
        /* @SVD:field=EN,bitOffset=4,bitWidth=2,description="LR balance enable:\n              00: both left and right in full volume\n              10: left channel balance volume adjustment enable\n              01: right channel balance volume adjustment enable\n              11: reserved, still kepp left and right in full volume" */
        volatile uint32_t EN: 2;  /* LR balance enable:               00: both left and right in  */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_LR_BAL_CTRL_Type;

/* @SVD:register=AUDIO_TX_LRCK_DIV,offset=0x0070,size=32,access=read-write,reset=0x007D007D,description="AUDIO_TX_LRCK_DIV" */
/* AUDIO_TX_LRCK_DIV 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DUTY_LOW,bitOffset=0,bitWidth=12,description="TX LRCK duty cycle low:\n              125 for 48K FS\n              136 for 44.1K FS\n              190 for 32K FS\n              250 for 24K FS\n              272 for 22.05K FS\n              375 for 16K FS\n              500 for 12K FS\n              544 for 11.025K FS\n              750 for 8K FS\n              Note:\n              1)duty_cycle = 12M/FS" */
        volatile uint32_t DUTY_LOW: 12;  /* TX LRCK duty cycle low:               125 for 48K FS         */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=DUTY_HIGH,bitOffset=16,bitWidth=12,description="TX LRCK duty cycle high:\n              125 for 48K FS\n              136 for 44.1K FS\n              185 for 32K FS\n              250 for 24K FS\n              272 for 22.05K FS\n              375 for 16K FS\n              500 for 12K FS\n              544 for 11.025K FS\n              750 for 8K FS" */
        volatile uint32_t DUTY_HIGH: 12;  /* TX LRCK duty cycle high:               125 for 48K FS        */
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_TX_LRCK_DIV_Type;

/* @SVD:register=AUDIO_TX_BCLK_DIV,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="AUDIO_TX_BCLK_DIV" */
/* AUDIO_TX_BCLK_DIV 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DUTY,bitOffset=0,bitWidth=6,description="TX serial bit clock duty cycle\n              5 for 48K FS\n              4 for 44.1K FS\n              5 for 32KFS\n              10 for 24K FS\n              8 for 22.05K FS\n              15 for 16K FS\n              20 for 12K FS\n              16 for 11.025K FS\n              30 for 8KFs" */
        volatile uint32_t DUTY: 6;  /* TX serial bit clock duty cycle               5 for 48K FS    */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_TX_BCLK_DIV_Type;

/* @SVD:register=AUDIO_TX_FORMAT,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="AUDIO_TX_FORMAT" */
/* AUDIO_TX_FORMAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PCM_DATA_WIDTH,bitOffset=0,bitWidth=5,description="I2S out pcm data width\n              M >= 16,\n              common value: 16, 18, 20, 22, 24" */
        volatile uint32_t PCM_DATA_WIDTH: 5;  /* I2S out pcm data width               M >= 16,                */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_TX_FORMAT_Type;

/* @SVD:register=AUDIO_SERIAL_TIMING,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="AUDIO_SERIAL_TIMING" */
/* AUDIO_SERIAL_TIMING 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TIMING,bitOffset=0,bitWidth=2,description="00: I2S mode\n              01: Left justified\n              10: right justified\n              11: reserved" */
        volatile uint32_t TIMING: 2;  /* 00: I2S mode               01: Left justified                */
        /* @SVD:field=SLAVE_EN,bitOffset=2,bitWidth=1,description="audio code transmit mode select.\n              0: master mode, 1: slave mode" */
        volatile uint32_t SLAVE_EN: 1;  /* audio code transmit mode select.               0: master mod */
        /* @SVD:field=LRCK_POL,bitOffset=3,bitWidth=1,description="TX LRCK polarity control.\n              0: disable TX_LRCK inventor\n              1: enable TX_LRCK inventor\n              for standard I2S, set tx_lrck_pol to low\n              for Left/Right Justified, set tx_lrck_pol to hgih" */
        volatile uint32_t LRCK_POL: 1;  /* TX LRCK polarity control.               0: disable TX_LRCK i */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_SERIAL_TIMING_Type;

/* @SVD:register=AUDIO_TX_FUNC_EN,offset=0x00B0,size=32,access=read-write,reset=0x00000000,description="AUDIO_TX_FUNC_EN" */
/* AUDIO_TX_FUNC_EN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TX_EN,bitOffset=0,bitWidth=1,description="1: enable 0:disable" */
        volatile uint32_t TX_EN: 1;  /* 1: enable 0:disable */
        /* @SVD:field=TX_INTF_SEL,bitOffset=1,bitWidth=1,description="1: select external tx interface  0: select internal apb tx interface" */
        volatile uint32_t TX_INTF_SEL: 1;  /* 1: select external tx interface  0: select internal apb tx i */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_TX_FUNC_EN_Type;

/* @SVD:register=AUDIO_TX_PAUSE,offset=0x00C0,size=32,access=read-write,reset=0x00000000,description="AUDIO_TX_PAUSE" */
/* AUDIO_TX_PAUSE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TX_PAUSE,bitOffset=0,bitWidth=1,description="TX pause control when tx_enable = 1.\n              1: pause\n              0: TX work" */
        volatile uint32_t TX_PAUSE: 1;  /* TX pause control when tx_enable = 1.               1: pause  */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_TX_PAUSE_Type;

/* @SVD:register=AUDIO_I2S_SL_MERGE,offset=0x00C8,size=32,access=read-write,reset=0x00000000,description="AUDIO_I2S_SL_MERGE" */
/* AUDIO_I2S_SL_MERGE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLAVE_TIMING_MERGE,bitOffset=0,bitWidth=1,description="when work as an I2S slave, and external I2S master TX/RX share an only BCLK/LRCK, we need set this bit high.\n              0: I2S slave use separated timing control port. TX_BCLK_IN/TX_LRCK_IN and RX_BCLK/RX_LRCK_IN are separated.\n              1: I2S slave use the same BCLK/LRCK, the TX_BCLK_IN/TX_LRCK also is used for RX controller." */
        volatile uint32_t SLAVE_TIMING_MERGE: 1;  /* when work as an I2S slave, and external I2S master TX/RX sha */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_I2S_SL_MERGE_Type;

/* @SVD:register=AUDIO_RX_FUNC_EN,offset=0x0100,size=32,access=read-write,reset=0x00000000,description="AUDIO_RX_FUNC_EN" */
/* AUDIO_RX_FUNC_EN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RX_EN,bitOffset=0,bitWidth=1,description="1: enable  0: disable" */
        volatile uint32_t RX_EN: 1;  /* 1: enable  0: disable */
        /* @SVD:field=RX_INTF_SEL,bitOffset=1,bitWidth=1,description="1: select external rx interface  0: select internal apb rx interface" */
        volatile uint32_t RX_INTF_SEL: 1;  /* 1: select external rx interface  0: select internal apb rx i */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_RX_FUNC_EN_Type;

/* @SVD:register=AUDIO_RX_PAUSE,offset=0x0110,size=32,access=read-write,reset=0x00000000,description="AUDIO_RX_PAUSE" */
/* AUDIO_RX_PAUSE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RX_PAUSE,bitOffset=0,bitWidth=1,description="RX pause control when rx_enable = 1.\n              1: pause\n              0: RX work" */
        volatile uint32_t RX_PAUSE: 1;  /* RX pause control when rx_enable = 1.               1: pause  */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_RX_PAUSE_Type;

/* @SVD:register=AUDIO_RX_SERIAL_TIMING,offset=0x0120,size=32,access=read-write,reset=0x00040000,description="AUDIO_RX_SERIAL_TIMING" */
/* AUDIO_RX_SERIAL_TIMING 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TIMING,bitOffset=0,bitWidth=2,description="00: I2S\n              01: Left justified\n              10: right justified\n              11: reserved" */
        volatile uint32_t TIMING: 2;  /* 00: I2S               01: Left justified               10: r */
        /* @SVD:field=SLAVE_EN,bitOffset=2,bitWidth=1,description="audio code receiver mode select.\n              0: master mode, 1: slave mode" */
        volatile uint32_t SLAVE_EN: 1;  /* audio code receiver mode select.               0: master mod */
        /* @SVD:field=LRCK_POL,bitOffset=3,bitWidth=1,description="RX LRCK polarity control.\n              0: disable RX_LRCK inventor\n              1: enable RX_LRCK inventor\n              for standard I2S, set tx_lrck_pol to low\n              for Left/Right Justified, set tx_lrck_pol to hgih" */
        volatile uint32_t LRCK_POL: 1;  /* RX LRCK polarity control.               0: disable RX_LRCK i */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_RX_SERIAL_TIMING_Type;

/* @SVD:register=AUDIO_RX_PCM_DW,offset=0x0130,size=32,access=read-write,reset=0x00000010,description="AUDIO_RX_PCM_DW" */
/* AUDIO_RX_PCM_DW 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PCM_DATA_WIDTH,bitOffset=0,bitWidth=5,description="For I2S and left justified mode, M can be 8,13,14,16\n              For right justified mode, M can be 8, 13, 14, 16, 18, 20, 22, 24" */
        volatile uint32_t PCM_DATA_WIDTH: 5;  /* For I2S and left justified mode, M can be 8,13,14,16         */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_RX_PCM_DW_Type;

/* @SVD:register=AUDIO_RX_LRCK_DIV,offset=0x0140,size=32,access=read-write,reset=0x007D007D,description="AUDIO_RX_LRCK_DIV" */
/* AUDIO_RX_LRCK_DIV 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DUTY_LOW,bitOffset=0,bitWidth=12,description="RX LRCK duty cycle low:\n              125 for 48K FS\n              136 for 44.1K FS\n              190 for 32K FS\n              250 for 24K FS\n              272 for 22.05K FS\n              375 for 16K FS\n              500 for 12K FS\n              544 for 11.025K FS\n              750 for 8K FS\n              Note:\n              1)duty_cycle = 12M/FS" */
        volatile uint32_t DUTY_LOW: 12;  /* RX LRCK duty cycle low:               125 for 48K FS         */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=DUTY_HIGH,bitOffset=16,bitWidth=12,description="RX LRCK duty cycle high:\n              125 for 48K FS\n              136 for 44.1K FS\n              185 for 32K FS\n              250 for 24K FS\n              272 for 22.05K FS\n              375 for 16K FS\n              500 for 12K FS\n              544 for 11.025K FS\n              750 for 8K FS" */
        volatile uint32_t DUTY_HIGH: 12;  /* RX LRCK duty cycle high:               125 for 48K FS        */
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_RX_LRCK_DIV_Type;

/* @SVD:register=AUDIO_RX_BCLK_DIV,offset=0x0150,size=32,access=read-write,reset=0x00000005,description="AUDIO_RX_BCLK_DIV" */
/* AUDIO_RX_BCLK_DIV 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DUTY,bitOffset=0,bitWidth=10,description="RX serial bit clock duty cycle\n              5 for 48K FS\n              4 for 44.1K FS\n              5 for 32KFS\n              10 for 24K FS\n              8 for 22.05K FS\n              15 for 16K FS\n              20 for 12K FS\n              16 for 11.025K FS\n              30 for 8KFs" */
        volatile uint32_t DUTY: 10;  /* RX serial bit clock duty cycle               5 for 48K FS    */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_AUDIO_RX_BCLK_DIV_Type;

/* @SVD:register=RECORD_DATA_SEL,offset=0x0160,size=32,access=read-write,reset=0x00000000,description="RECORD_DATA_SEL" */
/* RECORD_DATA_SEL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RS_DATA_SEL,bitOffset=0,bitWidth=1,description="0: I2S audio recording  1: BT recording" */
        volatile uint32_t RS_DATA_SEL: 1;  /* 0: I2S audio recording  1: BT recording */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_RECORD_DATA_SEL_Type;

/* @SVD:register=RX_RE_SAMPLE_CLK_DIV,offset=0x0170,size=32,access=read-write,reset=0x0000007D,description="RX_RE_SAMPLE_CLK_DIV" */
/* RX_RE_SAMPLE_CLK_DIV 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RS_DUTY,bitOffset=0,bitWidth=13,description="source PCM sample clock duty cycle:\n              250 for 48K FS\n              272 for 44.1K FS\n              375 for 32K FS\n              500 for 24K FS\n              544 for 22.05K FS\n              750 for 16K FS\n              1000 for 12K FS\n              1088 for 11.025K FS\n              1500 for 8K FS\n              Note:\n              1)duty_cycle = 12M/FS" */
        volatile uint32_t RS_DUTY: 13;  /* source PCM sample clock duty cycle:               250 for 48 */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_RX_RE_SAMPLE_CLK_DIV_Type;

/* @SVD:register=RX_RE_SAMPLE,offset=0x0180,size=32,access=read-write,reset=0x00000000,description="RX_RE_SAMPLE" */
/* RX_RE_SAMPLE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SMOOTH_EN,bitOffset=0,bitWidth=1,description="0: Disable RX re-sample smooth filter\n              1: Enable RX re-sample smooth filter" */
        volatile uint32_t SMOOTH_EN: 1;  /* 0: Disable RX re-sample smooth filter               1: Enabl */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_RX_RE_SAMPLE_Type;

/* @SVD:register=RECORD_FORMAT,offset=0x0190,size=32,access=read-write,reset=0x00000000,description="RECORD_FORMAT" */
/* RECORD_FORMAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DW,bitOffset=0,bitWidth=1,description="0: 8bit 1: 16bit\n              RX fifo data format:\n                  Mono 8 bit (unsigned): RX FIFO_DIN[31:0] = {L3,L2,L1,L0}, each four samples need one FIFO write operation\n                  Stereo 8 bit (unsigned): RX_FIFO_DIN[31:0] = {R1,L1,R0,L0}, each tow samples need one FIFO write operation\n                  Mono 16 bit (Signed 2's complement): RX_FIFO_DIN[31:0] = {L1,L0}, each two samples need one FIFO write operation\n                  Stereo 16 bit (Signed 2's complement): RX_FIFO_DIN[31:0] = {R0,L0}, each sample need one FIFO write operation" */
        volatile uint32_t DW: 1;  /* 0: 8bit 1: 16bit               RX fifo data format:          */
        /* @SVD:field=TRACK,bitOffset=1,bitWidth=1,description="1: mono recording, 0: stereo recording" */
        volatile uint32_t TRACK: 1;  /* 1: mono recording, 0: stereo recording */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_RECORD_FORMAT_Type;

/* @SVD:register=RX_CH_SEL,offset=0x01A0,size=32,access=read-write,reset=0x00000000,description="RX_CH_SEL" */
/* RX_CH_SEL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RIGHT_CHANNEL_SEL,bitOffset=0,bitWidth=2,description="RX re-sampling module setting:\n              00: RD right = RX right\n              01: RD right = RX left\n              10,11: RD right = (RX left + RX right)/2" */
        volatile uint32_t RIGHT_CHANNEL_SEL: 2;  /* RX re-sampling module setting:               00: RD right =  */
        /* @SVD:field=LEFT_CHANNEL_SEL,bitOffset=2,bitWidth=2,description="RX re-sampling module setting:\n              00: RD left = RX left\n              01: RD left = RX right\n              10,11: RD left = (RX left + RX right)/2" */
        volatile uint32_t LEFT_CHANNEL_SEL: 2;  /* RX re-sampling module setting:               00: RD left = R */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_RX_CH_SEL_Type;

/* @SVD:register=BT_PHONE_CTRL,offset=0x0200,size=32,access=read-write,reset=0x00000000,description="BT_PHONE_CTRL" */
/* BT_PHONE_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BT_PH_EN,bitOffset=0,bitWidth=1,description="BT phone enable\n              0: disable, 1: enable" */
        volatile uint32_t BT_PH_EN: 1;  /* BT phone enable               0: disable, 1: enable */
        /* @SVD:field=BT_BACK_MIX_EN,bitOffset=1,bitWidth=1,description="background mixer enable\n              0: disable, 1: enable" */
        volatile uint32_t BT_BACK_MIX_EN: 1;  /* background mixer enable               0: disable, 1: enable */
        /* @SVD:field=BT_MIX_SMOOTH_FILTER_EN,bitOffset=2,bitWidth=1,description="0: disable the smooth filter for background mixer\n              1: enable the smooth filer for background mixer" */
        volatile uint32_t BT_MIX_SMOOTH_FILTER_EN: 1;  /* 0: disable the smooth filter for background mixer            */
        /* @SVD:field=BT_PATH_SEL,bitOffset=3,bitWidth=1,description="BT path select\n              0: digital path, 1: analog path" */
        volatile uint32_t BT_PATH_SEL: 1;  /* BT path select               0: digital path, 1: analog path */
        /* @SVD:field=BT_PCM_IF_BPS,bitOffset=4,bitWidth=1,description="bypass baseband PCM signals to BT VCI master:\n              0: no bypass, 1: bypass" */
        volatile uint32_t BT_PCM_IF_BPS: 1;  /* bypass baseband PCM signals to BT VCI master:                */
        /* @SVD:field=BB_I2S_BPS_TO_CDC,bitOffset=5,bitWidth=1,description="bypass baseband I2S interface to audio codec i2s interface\n              0: no bypass, 1: bypass" */
        volatile uint32_t BB_I2S_BPS_TO_CDC: 1;  /* bypass baseband I2S interface to audio codec i2s interface   */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_BT_PHONE_CTRL_Type;

/* @SVD:register=BB_PCM_FORMAT,offset=0x0210,size=32,access=read-write,reset=0x00000000,description="BB_PCM_FORMAT" */
/* BB_PCM_FORMAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PCM_DW,bitOffset=0,bitWidth=5,description="Baseband Master PCM data width (>=8)\n              Common value: 8, 13,14, 16, 18, 20, 22, 24.\n              for I2S/Left Justified/Right Kistified timing, bb_pcm_dw >=16\n              For PCM timing, only 8, 13, 14, 16 configure value is available." */
        volatile uint32_t PCM_DW: 5;  /* Baseband Master PCM data width (>=8)               Common va */
        /* @SVD:field=PCM_TIM_SEL,bitOffset=5,bitWidth=2,description="00: I2S timing, 01: Left Justified\n              10: Right Justified, 11: PCM timing" */
        volatile uint32_t PCM_TIM_SEL: 2;  /* 00: I2S timing, 01: Left Justified               10: Right J */
        /* @SVD:field=PCM_SYNC_FLAG,bitOffset=7,bitWidth=1,description="0: short sync, 1: long sync" */
        volatile uint32_t PCM_SYNC_FLAG: 1;  /* 0: short sync, 1: long sync */
        /* @SVD:field=PCM_LSB_FLAG,bitOffset=8,bitWidth=1,description="Serial PCM data bit sequence.\n              0: MSB first, 1: LSB first" */
        volatile uint32_t PCM_LSB_FLAG: 1;  /* Serial PCM data bit sequence.               0: MSB first, 1: */
        /* @SVD:field=I2S_LRCK_POL,bitOffset=9,bitWidth=1,description="0: no bb_i2s_lrck input inventor\n              1: enable bb_i2s_lrck input inventor\n              for standard I2S, set tx_lrck_pol to low\n              for Left/Right Justified, set tx_lrck_pol to high" */
        volatile uint32_t I2S_LRCK_POL: 1;  /* 0: no bb_i2s_lrck input inventor               1: enable bb_ */
        /* @SVD:field=PCM_CLK_POL,bitOffset=10,bitWidth=1,description="input BB pcm clock polarity:\n              0: rising edge for data transmitting, falling edge for data receiving\n              1: rising edge for data receiving, falling edge for data transmitting" */
        volatile uint32_t PCM_CLK_POL: 1;  /* input BB pcm clock polarity:               0: rising edge fo */
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_BB_PCM_FORMAT_Type;

/* @SVD:register=BT_PCM_DW,offset=0x0220,size=32,access=read-write,reset=0x00000010,description="BT_PCM_DW" */
/* BT_PCM_DW 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DW,bitOffset=0,bitWidth=5,description="BT PCM master data width (>= 8),\n              common value: 8, 13,14, 16" */
        volatile uint32_t DW: 5;  /* BT PCM master data width (>= 8),               common value: */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_BT_PCM_DW_Type;

/* @SVD:register=BT_PCM_TIMING,offset=0x0230,size=32,access=read-write,reset=0x00000000,description="BT_PCM_TIMING" */
/* BT_PCM_TIMING 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LSB_FLAG,bitOffset=0,bitWidth=1,description="Serial PCM data bit sequence.\n              0: MSB first, 1: LSB first" */
        volatile uint32_t LSB_FLAG: 1;  /* Serial PCM data bit sequence.               0: MSB first, 1: */
        /* @SVD:field=SYNC_FLAG,bitOffset=1,bitWidth=1,description="0: short sync, 1: long sync" */
        volatile uint32_t SYNC_FLAG: 1;  /* 0: short sync, 1: long sync */
        /* @SVD:field=CLK_POL,bitOffset=2,bitWidth=1,description="BT PCM master output pcm clock polarity:\n              0: rising edge for data transmitting, falling edge for data receiving\n              1: rising edge for data receiving, falling edge for data transmitting" */
        volatile uint32_t CLK_POL: 1;  /* BT PCM master output pcm clock polarity:               0: ri */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=29,description="" */
        volatile uint32_t RSVD: 29;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_BT_PCM_TIMING_Type;

/* @SVD:register=BT_PCM_CLK_DUTY,offset=0x0240,size=32,access=read-write,reset=0x00000000,description="BT_PCM_CLK_DUTY" */
/* BT_PCM_CLK_DUTY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLK_DUTY,bitOffset=0,bitWidth=10,description="BT_PCM_CLK duty cycle\n              = (GCLK/(bt_pcm_sync*bt_pcm_dw))" */
        volatile uint32_t CLK_DUTY: 10;  /* BT_PCM_CLK duty cycle               = (GCLK/(bt_pcm_sync*bt_ */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_BT_PCM_CLK_DUTY_Type;

/* @SVD:register=BT_PCM_SYNC_DUTY,offset=0x0250,size=32,access=read-write,reset=0x00000000,description="BT_PCM_SYNC_DUTY" */
/* BT_PCM_SYNC_DUTY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SYNC_DUTY,bitOffset=0,bitWidth=6,description="PCM_SYNC duty cycle  (bt_pcm_sync frequency =  bt_pclk_clk/bt_pcm_sync_duty)" */
        volatile uint32_t SYNC_DUTY: 6;  /* PCM_SYNC duty cycle  (bt_pcm_sync frequency =  bt_pclk_clk/b */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_BT_PCM_SYNC_DUTY_Type;

/* @SVD:register=BT_VOL_CTRL,offset=0x0260,size=32,access=read-write,reset=0x00000000,description="BT_VOL_CTRL" */
/* BT_VOL_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VOL,bitOffset=0,bitWidth=3,description="BT master volume" */
        volatile uint32_t VOL: 3;  /* BT master volume */
        /* @SVD:field=VOL_ADJ_EN,bitOffset=3,bitWidth=1,description="BT volume adjust enable" */
        volatile uint32_t VOL_ADJ_EN: 1;  /* BT volume adjust enable */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_BT_VOL_CTRL_Type;

/* @SVD:register=INT_MASK,offset=0x0300,size=32,access=read-write,reset=0x00000003,description="INT_MASK" */
/* INT_MASK 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RX_FIFO_INT_MASK,bitOffset=0,bitWidth=1,description="Interrupt mask for RX FIFO push overflow, high active" */
        volatile uint32_t RX_FIFO_INT_MASK: 1;  /* Interrupt mask for RX FIFO push overflow, high active */
        /* @SVD:field=TX_FIFO_INT_MASK,bitOffset=1,bitWidth=1,description="Interrupt mask for TX FIFO pop underflow, high active" */
        volatile uint32_t TX_FIFO_INT_MASK: 1;  /* Interrupt mask for TX FIFO pop underflow, high active */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_INT_MASK_Type;

/* @SVD:register=INT_STATUS,offset=0x0310,size=32,access=read-write,reset=0x00000000,description="INT_STATUS" */
/* INT_STATUS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RX_FIFO_OVERFLOW,bitOffset=0,bitWidth=1,description="RX FIFO push overflow" */
        volatile uint32_t RX_FIFO_OVERFLOW: 1;  /* RX FIFO push overflow */
        /* @SVD:field=TX_FIFO_UNDERFLOW,bitOffset=1,bitWidth=1,description="TX FIFO pop underflow" */
        volatile uint32_t TX_FIFO_UNDERFLOW: 1;  /* TX FIFO pop underflow */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_INT_STATUS_Type;

/* @SVD:register=TX_DMA_ENTRY,offset=0x0400,size=32,access=read-write,reset=0x00000000,description="TX_DMA_ENTRY" */
/* TX_DMA_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TX_DMA_ENTRY,bitOffset=0,bitWidth=32,description="TX DMA entry" */
        volatile uint32_t TX_DMA_ENTRY: 32;  /* TX DMA entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_DMA_ENTRY_Type;

/* @SVD:register=RX_DMA_ENTRY,offset=0x0440,size=32,access=read-write,reset=0x00000000,description="RX_DMA_ENTRY" */
/* RX_DMA_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RX_DMA_ENTRY,bitOffset=0,bitWidth=32,description="RX DMA entry" */
        volatile uint32_t RX_DMA_ENTRY: 32;  /* RX DMA entry */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_RX_DMA_ENTRY_Type;

/* @SVD:register=DMA_MASK,offset=0x0480,size=32,access=read-write,reset=0x00000003,description="DMA_MASK" */
/* DMA_MASK 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RX_DMA_MASK,bitOffset=0,bitWidth=1,description="RX DMA mask enable:1: mask0: do not mask" */
        volatile uint32_t RX_DMA_MASK: 1;  /* RX DMA mask enable:1: mask0: do not mask */
        /* @SVD:field=TX_DMA_MASK,bitOffset=1,bitWidth=1,description="TX DMA mask enable:1: mask0: do not mask" */
        volatile uint32_t TX_DMA_MASK: 1;  /* TX DMA mask enable:1: mask0: do not mask */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_DMA_MASK_Type;

/* @SVD:register=DEBUG_LOOP,offset=0x0500,size=32,access=read-write,reset=0x00000000,description="DEBUG_LOOP" */
/* DEBUG_LOOP 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DA2AD_LOOP_BACK,bitOffset=0,bitWidth=1,description="TX-->RX Loop debug control:\n              0: disable\n              1: enable, internally connect TX SDTO to RX SDTI" */
        volatile uint32_t DA2AD_LOOP_BACK: 1;  /* TX-->RX Loop debug control:               0: disable         */
        /* @SVD:field=AD2DA_LOOP_BACK,bitOffset=1,bitWidth=1,description="RX-->TX Loop debug control:\n              0: disable\n              1: enable, internally connect RX Resampled PCM to TX Resample PCM input" */
        volatile uint32_t AD2DA_LOOP_BACK: 1;  /* RX-->TX Loop debug control:               0: disable         */
        /* @SVD:field=SP_CLK_SEL,bitOffset=2,bitWidth=1,description="clock select\n              0: xtal clock\n              1: pll clock" */
        volatile uint32_t SP_CLK_SEL: 1;  /* clock select               0: xtal clock               1: pl */
        /* @SVD:field=RSVD3,bitOffset=3,bitWidth=5,description="" */
        volatile uint32_t RSVD3: 5;
        /* @SVD:field=SP_CLK_DIV_UPDATE,bitOffset=8,bitWidth=1,description="update sp clock divider" */
        volatile uint32_t SP_CLK_DIV_UPDATE: 1;  /* update sp clock divider */
        /* @SVD:field=RSVD2,bitOffset=9,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=SP_CLK_DIV,bitOffset=16,bitWidth=8,description="sp clock divider value" */
        volatile uint32_t SP_CLK_DIV: 8;  /* sp clock divider value */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_DEBUG_LOOP_Type;

/* @SVD:register=FIFO_STATUS,offset=0x0600,size=32,access=read-write,reset=0x00000000,description="FIFO_STATUS" */
/* FIFO_STATUS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FIFO_STATUS_OUT,bitOffset=0,bitWidth=8,description="FIFO Status output:\n               Bit [7:0] = {tx_full,tx_empty,tx_almost_full,tx_almost_empty,rx_full,rx_empty,rx_almost_full,rx_almost_empty}" */
        volatile uint32_t FIFO_STATUS_OUT: 8;  /* FIFO Status output:                Bit [7:0] = {tx_full,tx_e */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_FIFO_STATUS_Type;

/* @SVD:register=TX_EQUALIZER_EN,offset=0x0700,size=32,access=read-write,reset=0x00000000,description="TX_EQUALIZER_EN" */
/* TX_EQUALIZER_EN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TX_EQUALIZER_EN,bitOffset=0,bitWidth=1,description="0: Disable TX equalizer\n              1: Enable TX equalizer\n              equalizer is not implemented" */
        volatile uint32_t TX_EQUALIZER_EN: 1;  /* 0: Disable TX equalizer               1: Enable TX equalizer */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_EQUALIZER_EN_Type;

/* @SVD:register=TX_EQUALIZER_GAIN1,offset=0x0710,size=32,access=read-write,reset=0x00000000,description="TX_EQUALIZER_GAIN1" */
/* TX_EQUALIZER_GAIN1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BAND1_GAIN,bitOffset=0,bitWidth=5,description="" */
        volatile uint32_t BAND1_GAIN: 5;
        /* @SVD:field=BAND2_GAIN,bitOffset=5,bitWidth=5,description="" */
        volatile uint32_t BAND2_GAIN: 5;
        /* @SVD:field=BAND3_GAIN,bitOffset=10,bitWidth=5,description="" */
        volatile uint32_t BAND3_GAIN: 5;
        /* @SVD:field=BAND4_GAIN,bitOffset=15,bitWidth=5,description="" */
        volatile uint32_t BAND4_GAIN: 5;
        /* @SVD:field=BAND5_GAIN,bitOffset=20,bitWidth=5,description="" */
        volatile uint32_t BAND5_GAIN: 5;
        /* @SVD:field=BAND6_GAIN,bitOffset=25,bitWidth=5,description="" */
        volatile uint32_t BAND6_GAIN: 5;
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_EQUALIZER_GAIN1_Type;

/* @SVD:register=TX_EQUALIZER_GAIN2,offset=0x0720,size=32,access=read-write,reset=0x00000000,description="TX_EQUALIZER_GAIN2" */
/* TX_EQUALIZER_GAIN2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BAND7_GAIN,bitOffset=0,bitWidth=5,description="" */
        volatile uint32_t BAND7_GAIN: 5;
        /* @SVD:field=BAND8_GAIN,bitOffset=5,bitWidth=5,description="" */
        volatile uint32_t BAND8_GAIN: 5;
        /* @SVD:field=BAND9_GAIN,bitOffset=10,bitWidth=5,description="" */
        volatile uint32_t BAND9_GAIN: 5;
        /* @SVD:field=BAND10_GAIN,bitOffset=15,bitWidth=5,description="" */
        volatile uint32_t BAND10_GAIN: 5;
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2S1_TX_EQUALIZER_GAIN2_Type;

/* I2S1 寄存器结构体 */
typedef struct
{
    /* @SVD:register=RSVD41,offset=0x0000,size=32,access=read-write,reset=0x0,description="RSVD41" */
    volatile uint32_t RSVD41;  /* 0x0000 - RSVD41 */
    volatile uint32_t RESERVED1[3];  /* 0x0004 - 0x000F */
    I2S1_TX_PCM_FORMAT_Type TX_PCM_FORMAT;  /* 0x0010 - TX_PCM_FORMAT */
    /* @SVD:register=RSVD40,offset=0x0014,size=32,access=read-write,reset=0x0,description="RSVD40" */
    volatile uint32_t RSVD40;  /* 0x0014 - RSVD40 */
    volatile uint32_t RESERVED6[2];  /* 0x0018 - 0x001F */
    I2S1_TX_PCM_SAMPLE_CLK_Type TX_PCM_SAMPLE_CLK;  /* 0x0020 - TX_PCM_SAMPLE_CLK */
    /* @SVD:register=RSVD39,offset=0x0024,size=32,access=read-write,reset=0x0,description="RSVD39" */
    volatile uint32_t RSVD39;  /* 0x0024 - RSVD39 */
    volatile uint32_t RESERVED10[2];  /* 0x0028 - 0x002F */
    I2S1_TX_RS_SMOOTH_Type TX_RS_SMOOTH;  /* 0x0030 - TX_RS_SMOOTH */
    /* @SVD:register=RSVD38,offset=0x0034,size=32,access=read-write,reset=0x0,description="RSVD38" */
    volatile uint32_t RSVD38;  /* 0x0034 - RSVD38 */
    volatile uint32_t RESERVED14[2];  /* 0x0038 - 0x003F */
    I2S1_TX_PCM_CH_SEL_Type TX_PCM_CH_SEL;  /* 0x0040 - TX_PCM_CH_SEL */
    /* @SVD:register=RSVD37,offset=0x0044,size=32,access=read-write,reset=0x0,description="RSVD37" */
    volatile uint32_t RSVD37;  /* 0x0044 - RSVD37 */
    volatile uint32_t RESERVED18[2];  /* 0x0048 - 0x004F */
    I2S1_TX_VOL_CTRL_Type TX_VOL_CTRL;  /* 0x0050 - TX_VOL_CTRL */
    /* @SVD:register=RSVD36,offset=0x0054,size=32,access=read-write,reset=0x0,description="RSVD36" */
    volatile uint32_t RSVD36;  /* 0x0054 - RSVD36 */
    volatile uint32_t RESERVED22[2];  /* 0x0058 - 0x005F */
    I2S1_TX_LR_BAL_CTRL_Type TX_LR_BAL_CTRL;  /* 0x0060 - TX_LR_BAL_CTRL */
    /* @SVD:register=RSVD35,offset=0x0064,size=32,access=read-write,reset=0x0,description="RSVD35" */
    volatile uint32_t RSVD35;  /* 0x0064 - RSVD35 */
    volatile uint32_t RESERVED26[2];  /* 0x0068 - 0x006F */
    I2S1_AUDIO_TX_LRCK_DIV_Type AUDIO_TX_LRCK_DIV;  /* 0x0070 - AUDIO_TX_LRCK_DIV */
    /* @SVD:register=RSVD34,offset=0x0074,size=32,access=read-write,reset=0x0,description="RSVD34" */
    volatile uint32_t RSVD34;  /* 0x0074 - RSVD34 */
    volatile uint32_t RESERVED30[2];  /* 0x0078 - 0x007F */
    I2S1_AUDIO_TX_BCLK_DIV_Type AUDIO_TX_BCLK_DIV;  /* 0x0080 - AUDIO_TX_BCLK_DIV */
    /* @SVD:register=RSVD33,offset=0x0084,size=32,access=read-write,reset=0x0,description="RSVD33" */
    volatile uint32_t RSVD33;  /* 0x0084 - RSVD33 */
    volatile uint32_t RESERVED34[2];  /* 0x0088 - 0x008F */
    I2S1_AUDIO_TX_FORMAT_Type AUDIO_TX_FORMAT;  /* 0x0090 - AUDIO_TX_FORMAT */
    /* @SVD:register=RSVD32,offset=0x0094,size=32,access=read-write,reset=0x0,description="RSVD32" */
    volatile uint32_t RSVD32;  /* 0x0094 - RSVD32 */
    volatile uint32_t RESERVED38[2];  /* 0x0098 - 0x009F */
    I2S1_AUDIO_SERIAL_TIMING_Type AUDIO_SERIAL_TIMING;  /* 0x00A0 - AUDIO_SERIAL_TIMING */
    /* @SVD:register=RSVD31,offset=0x00A4,size=32,access=read-write,reset=0x0,description="RSVD31" */
    volatile uint32_t RSVD31;  /* 0x00A4 - RSVD31 */
    volatile uint32_t RESERVED42[2];  /* 0x00A8 - 0x00AF */
    I2S1_AUDIO_TX_FUNC_EN_Type AUDIO_TX_FUNC_EN;  /* 0x00B0 - AUDIO_TX_FUNC_EN */
    /* @SVD:register=RSVD30,offset=0x00B4,size=32,access=read-write,reset=0x0,description="RSVD30" */
    volatile uint32_t RSVD30;  /* 0x00B4 - RSVD30 */
    volatile uint32_t RESERVED46[2];  /* 0x00B8 - 0x00BF */
    I2S1_AUDIO_TX_PAUSE_Type AUDIO_TX_PAUSE;  /* 0x00C0 - AUDIO_TX_PAUSE */
    /* @SVD:register=RSVD29,offset=0x00C4,size=32,access=read-write,reset=0x0,description="RSVD29" */
    volatile uint32_t RSVD29;  /* 0x00C4 - RSVD29 */
    I2S1_AUDIO_I2S_SL_MERGE_Type AUDIO_I2S_SL_MERGE;  /* 0x00C8 - AUDIO_I2S_SL_MERGE */
    /* @SVD:register=RSVD28,offset=0x00CC,size=32,access=read-write,reset=0x0,description="RSVD28" */
    volatile uint32_t RSVD28;  /* 0x00CC - RSVD28 */
    volatile uint32_t RESERVED52[12];  /* 0x00D0 - 0x00FF */
    I2S1_AUDIO_RX_FUNC_EN_Type AUDIO_RX_FUNC_EN;  /* 0x0100 - AUDIO_RX_FUNC_EN */
    /* @SVD:register=RSVD27,offset=0x0104,size=32,access=read-write,reset=0x0,description="RSVD27" */
    volatile uint32_t RSVD27;  /* 0x0104 - RSVD27 */
    volatile uint32_t RESERVED66[2];  /* 0x0108 - 0x010F */
    I2S1_AUDIO_RX_PAUSE_Type AUDIO_RX_PAUSE;  /* 0x0110 - AUDIO_RX_PAUSE */
    /* @SVD:register=RSVD26,offset=0x0114,size=32,access=read-write,reset=0x0,description="RSVD26" */
    volatile uint32_t RSVD26;  /* 0x0114 - RSVD26 */
    volatile uint32_t RESERVED70[2];  /* 0x0118 - 0x011F */
    I2S1_AUDIO_RX_SERIAL_TIMING_Type AUDIO_RX_SERIAL_TIMING;  /* 0x0120 - AUDIO_RX_SERIAL_TIMING */
    /* @SVD:register=RSVD25,offset=0x0124,size=32,access=read-write,reset=0x0,description="RSVD25" */
    volatile uint32_t RSVD25;  /* 0x0124 - RSVD25 */
    volatile uint32_t RESERVED74[2];  /* 0x0128 - 0x012F */
    I2S1_AUDIO_RX_PCM_DW_Type AUDIO_RX_PCM_DW;  /* 0x0130 - AUDIO_RX_PCM_DW */
    /* @SVD:register=RSVD24,offset=0x0134,size=32,access=read-write,reset=0x0,description="RSVD24" */
    volatile uint32_t RSVD24;  /* 0x0134 - RSVD24 */
    volatile uint32_t RESERVED78[2];  /* 0x0138 - 0x013F */
    I2S1_AUDIO_RX_LRCK_DIV_Type AUDIO_RX_LRCK_DIV;  /* 0x0140 - AUDIO_RX_LRCK_DIV */
    /* @SVD:register=RSVD23,offset=0x0144,size=32,access=read-write,reset=0x0,description="RSVD23" */
    volatile uint32_t RSVD23;  /* 0x0144 - RSVD23 */
    volatile uint32_t RESERVED82[2];  /* 0x0148 - 0x014F */
    I2S1_AUDIO_RX_BCLK_DIV_Type AUDIO_RX_BCLK_DIV;  /* 0x0150 - AUDIO_RX_BCLK_DIV */
    /* @SVD:register=RSVD22,offset=0x0154,size=32,access=read-write,reset=0x0,description="RSVD22" */
    volatile uint32_t RSVD22;  /* 0x0154 - RSVD22 */
    volatile uint32_t RESERVED86[2];  /* 0x0158 - 0x015F */
    I2S1_RECORD_DATA_SEL_Type RECORD_DATA_SEL;  /* 0x0160 - RECORD_DATA_SEL */
    /* @SVD:register=RSVD21,offset=0x0164,size=32,access=read-write,reset=0x0,description="RSVD21" */
    volatile uint32_t RSVD21;  /* 0x0164 - RSVD21 */
    volatile uint32_t RESERVED90[2];  /* 0x0168 - 0x016F */
    I2S1_RX_RE_SAMPLE_CLK_DIV_Type RX_RE_SAMPLE_CLK_DIV;  /* 0x0170 - RX_RE_SAMPLE_CLK_DIV */
    /* @SVD:register=RSVD20,offset=0x0174,size=32,access=read-write,reset=0x0,description="RSVD20" */
    volatile uint32_t RSVD20;  /* 0x0174 - RSVD20 */
    volatile uint32_t RESERVED94[2];  /* 0x0178 - 0x017F */
    I2S1_RX_RE_SAMPLE_Type RX_RE_SAMPLE;  /* 0x0180 - RX_RE_SAMPLE */
    /* @SVD:register=RSVD19,offset=0x0184,size=32,access=read-write,reset=0x0,description="RSVD19" */
    volatile uint32_t RSVD19;  /* 0x0184 - RSVD19 */
    volatile uint32_t RESERVED98[2];  /* 0x0188 - 0x018F */
    I2S1_RECORD_FORMAT_Type RECORD_FORMAT;  /* 0x0190 - RECORD_FORMAT */
    /* @SVD:register=RSVD18,offset=0x0194,size=32,access=read-write,reset=0x0,description="RSVD18" */
    volatile uint32_t RSVD18;  /* 0x0194 - RSVD18 */
    volatile uint32_t RESERVED102[2];  /* 0x0198 - 0x019F */
    I2S1_RX_CH_SEL_Type RX_CH_SEL;  /* 0x01A0 - RX_CH_SEL */
    /* @SVD:register=RSVD17,offset=0x01A4,size=32,access=read-write,reset=0x0,description="RSVD17" */
    volatile uint32_t RSVD17;  /* 0x01A4 - RSVD17 */
    volatile uint32_t RESERVED106[22];  /* 0x01A8 - 0x01FF */
    I2S1_BT_PHONE_CTRL_Type BT_PHONE_CTRL;  /* 0x0200 - BT_PHONE_CTRL */
    /* @SVD:register=RSVD16,offset=0x0204,size=32,access=read-write,reset=0x0,description="RSVD16" */
    volatile uint32_t RSVD16;  /* 0x0204 - RSVD16 */
    volatile uint32_t RESERVED130[2];  /* 0x0208 - 0x020F */
    I2S1_BB_PCM_FORMAT_Type BB_PCM_FORMAT;  /* 0x0210 - BB_PCM_FORMAT */
    /* @SVD:register=RSVD15,offset=0x0214,size=32,access=read-write,reset=0x0,description="RSVD15" */
    volatile uint32_t RSVD15;  /* 0x0214 - RSVD15 */
    volatile uint32_t RESERVED134[2];  /* 0x0218 - 0x021F */
    I2S1_BT_PCM_DW_Type BT_PCM_DW;  /* 0x0220 - BT_PCM_DW */
    /* @SVD:register=RSVD14,offset=0x0224,size=32,access=read-write,reset=0x0,description="RSVD14" */
    volatile uint32_t RSVD14;  /* 0x0224 - RSVD14 */
    volatile uint32_t RESERVED138[2];  /* 0x0228 - 0x022F */
    I2S1_BT_PCM_TIMING_Type BT_PCM_TIMING;  /* 0x0230 - BT_PCM_TIMING */
    /* @SVD:register=RSVD13,offset=0x0234,size=32,access=read-write,reset=0x0,description="RSVD13" */
    volatile uint32_t RSVD13;  /* 0x0234 - RSVD13 */
    volatile uint32_t RESERVED142[2];  /* 0x0238 - 0x023F */
    I2S1_BT_PCM_CLK_DUTY_Type BT_PCM_CLK_DUTY;  /* 0x0240 - BT_PCM_CLK_DUTY */
    /* @SVD:register=RSVD12,offset=0x0244,size=32,access=read-write,reset=0x0,description="RSVD12" */
    volatile uint32_t RSVD12;  /* 0x0244 - RSVD12 */
    volatile uint32_t RESERVED146[2];  /* 0x0248 - 0x024F */
    I2S1_BT_PCM_SYNC_DUTY_Type BT_PCM_SYNC_DUTY;  /* 0x0250 - BT_PCM_SYNC_DUTY */
    /* @SVD:register=RSVD11,offset=0x0254,size=32,access=read-write,reset=0x0,description="RSVD11" */
    volatile uint32_t RSVD11;  /* 0x0254 - RSVD11 */
    volatile uint32_t RESERVED150[2];  /* 0x0258 - 0x025F */
    I2S1_BT_VOL_CTRL_Type BT_VOL_CTRL;  /* 0x0260 - BT_VOL_CTRL */
    /* @SVD:register=RSVD10,offset=0x0264,size=32,access=read-write,reset=0x0,description="RSVD10" */
    volatile uint32_t RSVD10;  /* 0x0264 - RSVD10 */
    volatile uint32_t RESERVED154[38];  /* 0x0268 - 0x02FF */
    I2S1_INT_MASK_Type INT_MASK;  /* 0x0300 - INT_MASK */
    /* @SVD:register=RSVD9,offset=0x0304,size=32,access=read-write,reset=0x0,description="RSVD9" */
    volatile uint32_t RSVD9;  /* 0x0304 - RSVD9 */
    volatile uint32_t RESERVED194[2];  /* 0x0308 - 0x030F */
    I2S1_INT_STATUS_Type INT_STATUS;  /* 0x0310 - INT_STATUS */
    /* @SVD:register=RSVD8,offset=0x0314,size=32,access=read-write,reset=0x0,description="RSVD8" */
    volatile uint32_t RSVD8;  /* 0x0314 - RSVD8 */
    volatile uint32_t RESERVED198[58];  /* 0x0318 - 0x03FF */
    I2S1_TX_DMA_ENTRY_Type TX_DMA_ENTRY;  /* 0x0400 - TX_DMA_ENTRY */
    /* @SVD:register=RSVD7,offset=0x0404,size=32,access=read-write,reset=0x0,description="RSVD7" */
    volatile uint32_t RSVD7;  /* 0x0404 - RSVD7 */
    volatile uint32_t RESERVED258[14];  /* 0x0408 - 0x043F */
    I2S1_RX_DMA_ENTRY_Type RX_DMA_ENTRY;  /* 0x0440 - RX_DMA_ENTRY */
    /* @SVD:register=RSVD6,offset=0x0444,size=32,access=read-write,reset=0x0,description="RSVD6" */
    volatile uint32_t RSVD6;  /* 0x0444 - RSVD6 */
    volatile uint32_t RESERVED274[14];  /* 0x0448 - 0x047F */
    I2S1_DMA_MASK_Type DMA_MASK;  /* 0x0480 - DMA_MASK */
    /* @SVD:register=RSVD5,offset=0x0484,size=32,access=read-write,reset=0x0,description="RSVD5" */
    volatile uint32_t RSVD5;  /* 0x0484 - RSVD5 */
    volatile uint32_t RESERVED290[30];  /* 0x0488 - 0x04FF */
    I2S1_DEBUG_LOOP_Type DEBUG_LOOP;  /* 0x0500 - DEBUG_LOOP */
    /* @SVD:register=RSVD4,offset=0x0504,size=32,access=read-write,reset=0x0,description="RSVD4" */
    volatile uint32_t RSVD4;  /* 0x0504 - RSVD4 */
    volatile uint32_t RESERVED322[62];  /* 0x0508 - 0x05FF */
    I2S1_FIFO_STATUS_Type FIFO_STATUS;  /* 0x0600 - FIFO_STATUS */
    /* @SVD:register=RSVD3,offset=0x0604,size=32,access=read-write,reset=0x0,description="RSVD3" */
    volatile uint32_t RSVD3;  /* 0x0604 - RSVD3 */
    volatile uint32_t RESERVED386[62];  /* 0x0608 - 0x06FF */
    I2S1_TX_EQUALIZER_EN_Type TX_EQUALIZER_EN;  /* 0x0700 - TX_EQUALIZER_EN */
    /* @SVD:register=RSVD2,offset=0x0704,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x0704 - RSVD2 */
    volatile uint32_t RESERVED450[2];  /* 0x0708 - 0x070F */
    I2S1_TX_EQUALIZER_GAIN1_Type TX_EQUALIZER_GAIN1;  /* 0x0710 - TX_EQUALIZER_GAIN1 */
    /* @SVD:register=RSVD1,offset=0x0714,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0714 - RSVD1 */
    volatile uint32_t RESERVED454[2];  /* 0x0718 - 0x071F */
    I2S1_TX_EQUALIZER_GAIN2_Type TX_EQUALIZER_GAIN2;  /* 0x0720 - TX_EQUALIZER_GAIN2 */
} I2S1_TypeDef;

/* ============================================================================ */
/* HPSYS_CFG 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=HPSYS_CFG,base=0x5000B000,description="",groupName=HPSYS_CFG */

#define HPSYS_CFG_BASE                                     (0x5000B000UL)

/* @SVD:register=BMR,offset=0x0000,size=32,access=read-write,reset=0x0,description="Boot Mode Register" */
/* BMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BOOT_MODE,bitOffset=0,bitWidth=1,description="0 - normal mode, 1 - download mode" */
        volatile uint32_t BOOT_MODE: 1;  /* 0 - normal mode, 1 - download mode */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_BMR_Type;

/* @SVD:register=IDR,offset=0x0004,size=32,access=read-write,reset=0x0,description="ID Register" */
/* IDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REVID,bitOffset=0,bitWidth=8,description="Revision ID" */
        volatile uint32_t REVID: 8;  /* Revision ID */
        /* @SVD:field=PID,bitOffset=8,bitWidth=8,description="Package ID" */
        volatile uint32_t PID: 8;  /* Package ID */
        /* @SVD:field=CID,bitOffset=16,bitWidth=8,description="Chip ID" */
        volatile uint32_t CID: 8;  /* Chip ID */
        /* @SVD:field=SID,bitOffset=24,bitWidth=8,description="Series ID" */
        volatile uint32_t SID: 8;  /* Series ID */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_IDR_Type;

/* @SVD:register=SWCR,offset=0x0008,size=32,access=read-write,reset=0x0,description="SW Control Register" */
/* SWCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SWSEL,bitOffset=0,bitWidth=1,description="reserved for debug" */
        volatile uint32_t SWSEL: 1;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_SWCR_Type;

/* @SVD:register=SCR,offset=0x000C,size=32,access=read-write,reset=0x0,description="Security Control Register" */
/* SCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FKEY_MODE,bitOffset=0,bitWidth=1,description="reserved for debug" */
        volatile uint32_t FKEY_MODE: 1;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_SCR_Type;

/* @SVD:register=SYSCR,offset=0x0010,size=32,access=read-write,reset=0x0,description="System Configure Register" */
/* SYSCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WDT1_REBOOT,bitOffset=0,bitWidth=1,description="If set to 1, WDT1 reset will reboot the whole chip" */
        volatile uint32_t WDT1_REBOOT: 1;  /* If set to 1, WDT1 reset will reboot the whole chip */
        /* @SVD:field=SDNAND,bitOffset=1,bitWidth=1,description="0: MPI2 AHB space is allocated to MPI2\n              1: MPI2 AHB space is allocated to SDMMC1" */
        volatile uint32_t SDNAND: 1;  /* 0: MPI2 AHB space is allocated to MPI2               1: MPI2 */
        /* @SVD:field=LDO_VSEL,bitOffset=2,bitWidth=1,description="select work mode\n              0: enhanced mode\n              1: base mode" */
        volatile uint32_t LDO_VSEL: 1;  /* select work mode               0: enhanced mode              */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=29,description="" */
        volatile uint32_t RSVD: 29;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_SYSCR_Type;

/* @SVD:register=RTC_TR,offset=0x0014,size=32,access=read-write,reset=0x0,description="Mirrored RTC Time Register" */
/* RTC_TR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SS,bitOffset=0,bitWidth=10,description="Sub-second counter" */
        volatile uint32_t SS: 10;  /* Sub-second counter */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
        /* @SVD:field=SU,bitOffset=11,bitWidth=4,description="Second units in BCD format" */
        volatile uint32_t SU: 4;  /* Second units in BCD format */
        /* @SVD:field=ST,bitOffset=15,bitWidth=3,description="Second tens in BCD format" */
        volatile uint32_t ST: 3;  /* Second tens in BCD format */
        /* @SVD:field=MNU,bitOffset=18,bitWidth=4,description="Minute units in BCD format" */
        volatile uint32_t MNU: 4;  /* Minute units in BCD format */
        /* @SVD:field=MNT,bitOffset=22,bitWidth=3,description="Minute tens in BCD format" */
        volatile uint32_t MNT: 3;  /* Minute tens in BCD format */
        /* @SVD:field=HU,bitOffset=25,bitWidth=4,description="Hour units in BCD format" */
        volatile uint32_t HU: 4;  /* Hour units in BCD format */
        /* @SVD:field=HT,bitOffset=29,bitWidth=2,description="Hour tens in BCD format" */
        volatile uint32_t HT: 2;  /* Hour tens in BCD format */
        /* @SVD:field=PM,bitOffset=31,bitWidth=1,description="AM/PM notation\n              0: AM\n              1: PM" */
        volatile uint32_t PM: 1;  /* AM/PM notation               0: AM               1: PM */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_RTC_TR_Type;

/* @SVD:register=RTC_DR,offset=0x0018,size=32,access=read-write,reset=0x0,description="Mirrored RTC Date Register" */
/* RTC_DR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DU,bitOffset=0,bitWidth=4,description="Date units in BCD format" */
        volatile uint32_t DU: 4;  /* Date units in BCD format */
        /* @SVD:field=DT,bitOffset=4,bitWidth=2,description="Date tens in BCD format" */
        volatile uint32_t DT: 2;  /* Date tens in BCD format */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=MU,bitOffset=8,bitWidth=4,description="Month units in BCD format" */
        volatile uint32_t MU: 4;  /* Month units in BCD format */
        /* @SVD:field=MT,bitOffset=12,bitWidth=1,description="Month tens in BCD format" */
        volatile uint32_t MT: 1;  /* Month tens in BCD format */
        /* @SVD:field=WD,bitOffset=13,bitWidth=3,description="Week day units\n              000: forbidden\n              001: Monday\n              ...\n              111: Sunday" */
        volatile uint32_t WD: 3;  /* Week day units               000: forbidden               00 */
        /* @SVD:field=YU,bitOffset=16,bitWidth=4,description="Year units in BCD format" */
        volatile uint32_t YU: 4;  /* Year units in BCD format */
        /* @SVD:field=YT,bitOffset=20,bitWidth=4,description="Year tens in BCD format" */
        volatile uint32_t YT: 4;  /* Year tens in BCD format */
        /* @SVD:field=CB,bitOffset=24,bitWidth=1,description="Century flag" */
        volatile uint32_t CB: 1;  /* Century flag */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=ERR,bitOffset=31,bitWidth=1,description="reserved for debug" */
        volatile uint32_t ERR: 1;  /* reserved for debug */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_RTC_DR_Type;

/* @SVD:register=ULPMCR,offset=0x001C,size=32,access=read-write,reset=0x0,description="ULP Memory Control register" */
/* ULPMCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RAM_RM,bitOffset=0,bitWidth=2,description="reserved for debug" */
        volatile uint32_t RAM_RM: 2;  /* reserved for debug */
        /* @SVD:field=RSVD4,bitOffset=2,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=RAM_RME,bitOffset=4,bitWidth=1,description="reserved for debug" */
        volatile uint32_t RAM_RME: 1;  /* reserved for debug */
        /* @SVD:field=RAM_RA,bitOffset=5,bitWidth=2,description="reserved for debug" */
        volatile uint32_t RAM_RA: 2;  /* reserved for debug */
        /* @SVD:field=RAM_WA,bitOffset=7,bitWidth=3,description="reserved for debug" */
        volatile uint32_t RAM_WA: 3;  /* reserved for debug */
        /* @SVD:field=RAM_WPULSE,bitOffset=10,bitWidth=3,description="reserved for debug" */
        volatile uint32_t RAM_WPULSE: 3;  /* reserved for debug */
        /* @SVD:field=RSVD3,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t RSVD3: 3;
        /* @SVD:field=ROM_RM,bitOffset=16,bitWidth=2,description="reserved for debug" */
        volatile uint32_t ROM_RM: 2;  /* reserved for debug */
        /* @SVD:field=RSVD2,bitOffset=18,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=ROM_RME,bitOffset=20,bitWidth=1,description="reserved for debug" */
        volatile uint32_t ROM_RME: 1;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=9,description="" */
        volatile uint32_t RSVD: 9;
        /* @SVD:field=ROM_DIS,bitOffset=30,bitWidth=1,description="reserved for debug" */
        volatile uint32_t ROM_DIS: 1;  /* reserved for debug */
        /* @SVD:field=FORCE_ON,bitOffset=31,bitWidth=1,description="reserved for debug" */
        volatile uint32_t FORCE_ON: 1;  /* reserved for debug */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_ULPMCR_Type;

/* @SVD:register=DBGR,offset=0x0020,size=32,access=read-write,reset=0x0,description="Debug Select Register" */
/* DBGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEL_L,bitOffset=0,bitWidth=4,description="reserved for debug" */
        volatile uint32_t SEL_L: 4;  /* reserved for debug */
        /* @SVD:field=SEL_H,bitOffset=4,bitWidth=4,description="reserved for debug" */
        volatile uint32_t SEL_H: 4;  /* reserved for debug */
        /* @SVD:field=BITEN_L,bitOffset=8,bitWidth=8,description="reserved for debug" */
        volatile uint32_t BITEN_L: 8;  /* reserved for debug */
        /* @SVD:field=BITEN_H,bitOffset=16,bitWidth=8,description="reserved for debug" */
        volatile uint32_t BITEN_H: 8;  /* reserved for debug */
        /* @SVD:field=CLK_SEL,bitOffset=24,bitWidth=3,description="reserved for debug" */
        volatile uint32_t CLK_SEL: 3;  /* reserved for debug */
        /* @SVD:field=CLK_EN,bitOffset=27,bitWidth=1,description="reserved for debug" */
        volatile uint32_t CLK_EN: 1;  /* reserved for debug */
        /* @SVD:field=HP2LP_NMI,bitOffset=28,bitWidth=1,description="set 1 to send NMI interrupt to LCPU" */
        volatile uint32_t HP2LP_NMI: 1;  /* set 1 to send NMI interrupt to LCPU */
        /* @SVD:field=LP2HP_NMIE,bitOffset=29,bitWidth=1,description="LP2HP NMI interrupt enable" */
        volatile uint32_t LP2HP_NMIE: 1;  /* LP2HP NMI interrupt enable */
        /* @SVD:field=LP2HP_NMIF,bitOffset=30,bitWidth=1,description="LP2HP NMI interrupt flag" */
        volatile uint32_t LP2HP_NMIF: 1;  /* LP2HP NMI interrupt flag */
        /* @SVD:field=SWAP,bitOffset=31,bitWidth=1,description="reserved for debug" */
        volatile uint32_t SWAP: 1;  /* reserved for debug */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_DBGR_Type;

/* @SVD:register=MDBGR,offset=0x0024,size=32,access=read-write,reset=0x0,description="Memory Debug Register" */
/* MDBGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LS_RAM0,bitOffset=0,bitWidth=1,description="reserved for debug" */
        volatile uint32_t LS_RAM0: 1;  /* reserved for debug */
        /* @SVD:field=LS_RAM1,bitOffset=1,bitWidth=1,description="reserved for debug" */
        volatile uint32_t LS_RAM1: 1;  /* reserved for debug */
        /* @SVD:field=LS_RAM2,bitOffset=2,bitWidth=1,description="reserved for debug" */
        volatile uint32_t LS_RAM2: 1;  /* reserved for debug */
        /* @SVD:field=LS_ROM,bitOffset=3,bitWidth=1,description="reserved for debug" */
        volatile uint32_t LS_ROM: 1;  /* reserved for debug */
        /* @SVD:field=PD_ROM,bitOffset=4,bitWidth=1,description="reserved for debug" */
        volatile uint32_t PD_ROM: 1;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_MDBGR_Type;

/* @SVD:register=LPIRQ,offset=0x003C,size=32,access=read-write,reset=0x0,description="Interrupt Selection for LCPU" */
/* LPIRQ 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEL0,bitOffset=0,bitWidth=6,description="select hp2lp0 interrupt source" */
        volatile uint32_t SEL0: 6;  /* select hp2lp0 interrupt source */
        /* @SVD:field=RSVD3,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=IF0,bitOffset=7,bitWidth=1,description="hp2lp0 interrupt status. Write 1 to clear." */
        volatile uint32_t IF0: 1;  /* hp2lp0 interrupt status. Write 1 to clear. */
        /* @SVD:field=SEL1,bitOffset=8,bitWidth=6,description="select hp2lp1 interrupt source" */
        volatile uint32_t SEL1: 6;  /* select hp2lp1 interrupt source */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=IF1,bitOffset=15,bitWidth=1,description="hp2lp1 interrupt status. Write 1 to clear." */
        volatile uint32_t IF1: 1;  /* hp2lp1 interrupt status. Write 1 to clear. */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_LPIRQ_Type;

/* @SVD:register=USBCR,offset=0x0040,size=32,access=read-write,reset=0x0,description="USB Control register" */
/* USBCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=USB_EN,bitOffset=0,bitWidth=1,description="USB PHY enable, turn on power swith, power up LDO and bias" */
        volatile uint32_t USB_EN: 1;  /* USB PHY enable, turn on power swith, power up LDO and bias */
        /* @SVD:field=LDO_VSEL,bitOffset=1,bitWidth=3,description="2.5V LDO output voltage setting\n              0 = 2.40 V, 1 = 2.47 V, 2 = 2.53 V, 3 = 2.60 V, 4 = 2.60 V, 5 = 2.67 V, 6 = 2.73 V, 7 = 2.8 V" */
        volatile uint32_t LDO_VSEL: 3;  /* 2.5V LDO output voltage setting               0 = 2.40 V, 1  */
        /* @SVD:field=LDO_LP_EN,bitOffset=4,bitWidth=1,description="2.5V LDO low power mode enable. 0 = 240 uA, 1 = 50 uA" */
        volatile uint32_t LDO_LP_EN: 1;  /* 2.5V LDO low power mode enable. 0 = 240 uA, 1 = 50 uA */
        /* @SVD:field=DM_PD,bitOffset=5,bitWidth=1,description="enable DM 15k Ohm pull down resistor" */
        volatile uint32_t DM_PD: 1;  /* enable DM 15k Ohm pull down resistor */
        /* @SVD:field=DP_EN,bitOffset=6,bitWidth=1,description="0:disable dp pull up or pull down 1:enable dp pull or pull down" */
        volatile uint32_t DP_EN: 1;  /* 0:disable dp pull up or pull down 1:enable dp pull or pull d */
        /* @SVD:field=RSVD2,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TX_RTUNE,bitOffset=8,bitWidth=3,description="TX outp impedance tuning\n              0 = 50 Ohm, 1 = 46 Ohm, 2 = 43 Ohm, 3 = 40 Ohm, 4 = 37.5 Ohm, 5 = 35 Ohm, 6 = 33 Ohm, 7 = 31.5 Ohm" */
        volatile uint32_t TX_RTUNE: 3;  /* TX outp impedance tuning               0 = 50 Ohm, 1 = 46 Oh */
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
        /* @SVD:field=DC_TE,bitOffset=12,bitWidth=1,description="reserved for debug" */
        volatile uint32_t DC_TE: 1;  /* reserved for debug */
        /* @SVD:field=DC_TR,bitOffset=13,bitWidth=3,description="reserved for debug" */
        volatile uint32_t DC_TR: 3;  /* reserved for debug */
        /* @SVD:field=RSVD0,bitOffset=16,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RSVD0: 8;  /* reserved for debug */
        /* @SVD:field=RSVD1,bitOffset=24,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RSVD1: 8;  /* reserved for debug */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_USBCR_Type;

/* @SVD:register=SYS_RSVD,offset=0x0044,size=32,access=read-write,reset=0x0,description="HPSYS RSVD Register" */
/* SYS_RSVD 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESERVE0,bitOffset=0,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE0: 8;  /* reserved for debug */
        /* @SVD:field=RESERVE1,bitOffset=8,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE1: 8;  /* reserved for debug */
        /* @SVD:field=RESERVE2,bitOffset=16,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE2: 8;  /* reserved for debug */
        /* @SVD:field=RESERVE3,bitOffset=24,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE3: 8;  /* reserved for debug */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_SYS_RSVD_Type;

/* @SVD:register=I2C1_PINR,offset=0x0048,size=32,access=read-write,reset=0x0,description="I2C1 Pin Register" */
/* I2C1_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SCL_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t SCL_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=SDA_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t SDA_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_I2C1_PINR_Type;

/* @SVD:register=I2C2_PINR,offset=0x004C,size=32,access=read-write,reset=0x0,description="I2C2 Pin Register" */
/* I2C2_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SCL_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t SCL_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=SDA_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t SDA_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_I2C2_PINR_Type;

/* @SVD:register=I2C3_PINR,offset=0x0050,size=32,access=read-write,reset=0x0,description="I2C3 Pin Register" */
/* I2C3_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SCL_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t SCL_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=SDA_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t SDA_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_I2C3_PINR_Type;

/* @SVD:register=I2C4_PINR,offset=0x0054,size=32,access=read-write,reset=0x0,description="I2C4 Pin Register" */
/* I2C4_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SCL_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t SCL_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=SDA_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t SDA_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_I2C4_PINR_Type;

/* @SVD:register=USART1_PINR,offset=0x0058,size=32,access=read-write,reset=0x0,description="USART1 Pin Register" */
/* USART1_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TXD_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t TXD_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=RXD_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t RXD_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RTS_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t RTS_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CTS_PIN,bitOffset=24,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CTS_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_USART1_PINR_Type;

/* @SVD:register=USART2_PINR,offset=0x005C,size=32,access=read-write,reset=0x0,description="USART2 Pin Register" */
/* USART2_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TXD_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t TXD_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=RXD_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t RXD_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RTS_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t RTS_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CTS_PIN,bitOffset=24,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CTS_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_USART2_PINR_Type;

/* @SVD:register=USART3_PINR,offset=0x0060,size=32,access=read-write,reset=0x0,description="USART3 Pin Register" */
/* USART3_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TXD_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t TXD_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=RXD_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t RXD_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RTS_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t RTS_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CTS_PIN,bitOffset=24,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CTS_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_USART3_PINR_Type;

/* @SVD:register=GPTIM1_PINR,offset=0x0064,size=32,access=read-write,reset=0x0,description="GPTIM1 Pin Register" */
/* GPTIM1_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CH1_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH1_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=CH2_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH2_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=CH3_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH3_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CH4_PIN,bitOffset=24,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH4_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_GPTIM1_PINR_Type;

/* @SVD:register=GPTIM2_PINR,offset=0x0068,size=32,access=read-write,reset=0x0,description="GPTIM2 Pin Register" */
/* GPTIM2_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CH1_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH1_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=CH2_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH2_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=CH3_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH3_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CH4_PIN,bitOffset=24,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH4_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_GPTIM2_PINR_Type;

/* @SVD:register=ETR_PINR,offset=0x006C,size=32,access=read-write,reset=0x0,description="GPTIM ETR Pin Register" */
/* ETR_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ETR1_PIN,bitOffset=0,bitWidth=6,description="Connect GPTIM1_ETR to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t ETR1_PIN: 6;  /* Connect GPTIM1_ETR to selected IO(PA).                0 to 4 */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=ETR2_PIN,bitOffset=8,bitWidth=6,description="Connect GPTIM2_ETR to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t ETR2_PIN: 6;  /* Connect GPTIM2_ETR to selected IO(PA).                0 to 4 */
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_ETR_PINR_Type;

/* @SVD:register=LPTIM1_PINR,offset=0x0070,size=32,access=read-write,reset=0x0,description="LPTIM1 Pin Register" */
/* LPTIM1_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IN_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t IN_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=OUT_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t OUT_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=ETR_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t ETR_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_LPTIM1_PINR_Type;

/* @SVD:register=LPTIM2_PINR,offset=0x0074,size=32,access=read-write,reset=0x0,description="LPTIM2 Pin Register" */
/* LPTIM2_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IN_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t IN_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=OUT_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t OUT_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=ETR_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t ETR_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_LPTIM2_PINR_Type;

/* @SVD:register=ATIM1_PINR1,offset=0x0078,size=32,access=read-write,reset=0x0,description="ATIM1 Pin Register 1" */
/* ATIM1_PINR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CH1_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH1_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=CH2_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH2_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=CH3_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH3_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CH4_PIN,bitOffset=24,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH4_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_ATIM1_PINR1_Type;

/* @SVD:register=ATIM1_PINR2,offset=0x007C,size=32,access=read-write,reset=0x0,description="ATIM1 Pin Register 2" */
/* ATIM1_PINR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CH1N_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH1N_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=CH2N_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH2N_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CH3N_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t CH3N_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_ATIM1_PINR2_Type;

/* @SVD:register=ATIM1_PINR3,offset=0x0080,size=32,access=read-write,reset=0x0,description="ATIM1 Pin Register 3" */
/* ATIM1_PINR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BK_PIN,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t BK_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=BK2_PIN,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t BK2_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=ETR_PIN,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t ETR_PIN: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_ATIM1_PINR3_Type;

/* @SVD:register=PTA_PINR,offset=0x0084,size=32,access=read-write,reset=0x0,description="PTA Pin Register" */
/* PTA_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BT_ACTIVE,bitOffset=0,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t BT_ACTIVE: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=BT_COLLISION,bitOffset=8,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t BT_COLLISION: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=BT_PRIORITY,bitOffset=16,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t BT_PRIORITY: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=WLAN_ACTIVE,bitOffset=24,bitWidth=6,description="Connect function pin to selected IO(PA). \n              0 to 44 for PA00 to PA44.\n              Other values for floating." */
        volatile uint32_t WLAN_ACTIVE: 6;  /* Connect function pin to selected IO(PA).                0 to */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_PTA_PINR_Type;

/* @SVD:register=ANAU_CR,offset=0x0088,size=32,access=read-write,reset=0x0,description="ANAU Control Register" */
/* ANAU_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN_BG,bitOffset=0,bitWidth=1,description="reserved for debug" */
        volatile uint32_t EN_BG: 1;  /* reserved for debug */
        /* @SVD:field=EN_VBAT_MON,bitOffset=1,bitWidth=1,description="reserved for debug" */
        volatile uint32_t EN_VBAT_MON: 1;  /* reserved for debug */
        /* @SVD:field=EFUSE_VDD_EN,bitOffset=2,bitWidth=1,description="reserved for debug" */
        volatile uint32_t EFUSE_VDD_EN: 1;  /* reserved for debug */
        /* @SVD:field=EFUSE_VDD_PD,bitOffset=3,bitWidth=1,description="reserved for debug" */
        volatile uint32_t EFUSE_VDD_PD: 1;  /* reserved for debug */
        /* @SVD:field=DC_MR,bitOffset=4,bitWidth=3,description="reserved for debug" */
        volatile uint32_t DC_MR: 3;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_ANAU_CR_Type;

/* @SVD:register=ANAU_RSVD,offset=0x008C,size=32,access=read-write,reset=0x0,description="ANAU Reserve Register" */
/* ANAU_RSVD 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESERVE0,bitOffset=0,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE0: 8;  /* reserved for debug */
        /* @SVD:field=RESERVE1,bitOffset=8,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE1: 8;  /* reserved for debug */
        /* @SVD:field=RESERVE2,bitOffset=16,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE2: 8;  /* reserved for debug */
        /* @SVD:field=RESERVE3,bitOffset=24,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE3: 8;  /* reserved for debug */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_ANAU_RSVD_Type;

/* @SVD:register=ANATR,offset=0x0090,size=32,access=read-write,reset=0x0,description="Analog Test Register" */
/* ANATR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DC_TE_ATEST0,bitOffset=0,bitWidth=1,description="reserved for debug" */
        volatile uint32_t DC_TE_ATEST0: 1;  /* reserved for debug */
        /* @SVD:field=DC_UR_ATEST0,bitOffset=1,bitWidth=3,description="reserved for debug" */
        volatile uint32_t DC_UR_ATEST0: 3;  /* reserved for debug */
        /* @SVD:field=DC_TE_ATEST1,bitOffset=4,bitWidth=1,description="reserved for debug" */
        volatile uint32_t DC_TE_ATEST1: 1;  /* reserved for debug */
        /* @SVD:field=DC_UR_ATEST1,bitOffset=5,bitWidth=3,description="reserved for debug" */
        volatile uint32_t DC_UR_ATEST1: 3;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_ANATR_Type;

/* @SVD:register=CAU2_CR,offset=0x0094,size=32,access=read-write,reset=0x0,description="CAU2 Control Register" */
/* CAU2_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HPBG_VDDPSW_EN,bitOffset=0,bitWidth=1,description="reserved for debug" */
        volatile uint32_t HPBG_VDDPSW_EN: 1;  /* reserved for debug */
        /* @SVD:field=HPBG_EN,bitOffset=1,bitWidth=1,description="reserved for debug" */
        volatile uint32_t HPBG_EN: 1;  /* reserved for debug */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=DC_TR,bitOffset=4,bitWidth=3,description="reserved for debug" */
        volatile uint32_t DC_TR: 3;  /* reserved for debug */
        /* @SVD:field=DC_BR,bitOffset=7,bitWidth=3,description="reserved for debug" */
        volatile uint32_t DC_BR: 3;  /* reserved for debug */
        /* @SVD:field=DC_MR,bitOffset=10,bitWidth=3,description="reserved for debug" */
        volatile uint32_t DC_MR: 3;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_CAU2_CR_Type;

/* @SVD:register=CAU2_RSVD,offset=0x0098,size=32,access=read-write,reset=0x0,description="CAU2 RSVD Register1" */
/* CAU2_RSVD 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESERVE0,bitOffset=0,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE0: 8;  /* reserved for debug */
        /* @SVD:field=RESERVE1,bitOffset=8,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE1: 8;  /* reserved for debug */
        /* @SVD:field=RESERVE2,bitOffset=16,bitWidth=8,description="reserved for debug" */
        volatile uint32_t RESERVE2: 8;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_CFG_CAU2_RSVD_Type;

/* HPSYS_CFG 寄存器结构体 */
typedef struct
{
    HPSYS_CFG_BMR_Type BMR;  /* 0x0000 - Boot Mode Register */
    HPSYS_CFG_IDR_Type IDR;  /* 0x0004 - ID Register */
    HPSYS_CFG_SWCR_Type SWCR;  /* 0x0008 - SW Control Register */
    HPSYS_CFG_SCR_Type SCR;  /* 0x000C - Security Control Register */
    HPSYS_CFG_SYSCR_Type SYSCR;  /* 0x0010 - System Configure Register */
    HPSYS_CFG_RTC_TR_Type RTC_TR;  /* 0x0014 - Mirrored RTC Time Register */
    HPSYS_CFG_RTC_DR_Type RTC_DR;  /* 0x0018 - Mirrored RTC Date Register */
    HPSYS_CFG_ULPMCR_Type ULPMCR;  /* 0x001C - ULP Memory Control register */
    HPSYS_CFG_DBGR_Type DBGR;  /* 0x0020 - Debug Select Register */
    HPSYS_CFG_MDBGR_Type MDBGR;  /* 0x0024 - Memory Debug Register */
    /* @SVD:register=RSVD1,offset=0x0028,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0028 - RSVD1 */
    volatile uint32_t RESERVED11[4];  /* 0x002C - 0x003B */
    HPSYS_CFG_LPIRQ_Type LPIRQ;  /* 0x003C - Interrupt Selection for LCPU */
    HPSYS_CFG_USBCR_Type USBCR;  /* 0x0040 - USB Control register */
    HPSYS_CFG_SYS_RSVD_Type SYS_RSVD;  /* 0x0044 - HPSYS RSVD Register */
    HPSYS_CFG_I2C1_PINR_Type I2C1_PINR;  /* 0x0048 - I2C1 Pin Register */
    HPSYS_CFG_I2C2_PINR_Type I2C2_PINR;  /* 0x004C - I2C2 Pin Register */
    HPSYS_CFG_I2C3_PINR_Type I2C3_PINR;  /* 0x0050 - I2C3 Pin Register */
    HPSYS_CFG_I2C4_PINR_Type I2C4_PINR;  /* 0x0054 - I2C4 Pin Register */
    HPSYS_CFG_USART1_PINR_Type USART1_PINR;  /* 0x0058 - USART1 Pin Register */
    HPSYS_CFG_USART2_PINR_Type USART2_PINR;  /* 0x005C - USART2 Pin Register */
    HPSYS_CFG_USART3_PINR_Type USART3_PINR;  /* 0x0060 - USART3 Pin Register */
    HPSYS_CFG_GPTIM1_PINR_Type GPTIM1_PINR;  /* 0x0064 - GPTIM1 Pin Register */
    HPSYS_CFG_GPTIM2_PINR_Type GPTIM2_PINR;  /* 0x0068 - GPTIM2 Pin Register */
    HPSYS_CFG_ETR_PINR_Type ETR_PINR;  /* 0x006C - GPTIM ETR Pin Register */
    HPSYS_CFG_LPTIM1_PINR_Type LPTIM1_PINR;  /* 0x0070 - LPTIM1 Pin Register */
    HPSYS_CFG_LPTIM2_PINR_Type LPTIM2_PINR;  /* 0x0074 - LPTIM2 Pin Register */
    HPSYS_CFG_ATIM1_PINR1_Type ATIM1_PINR1;  /* 0x0078 - ATIM1 Pin Register 1 */
    HPSYS_CFG_ATIM1_PINR2_Type ATIM1_PINR2;  /* 0x007C - ATIM1 Pin Register 2 */
    HPSYS_CFG_ATIM1_PINR3_Type ATIM1_PINR3;  /* 0x0080 - ATIM1 Pin Register 3 */
    HPSYS_CFG_PTA_PINR_Type PTA_PINR;  /* 0x0084 - PTA Pin Register */
    HPSYS_CFG_ANAU_CR_Type ANAU_CR;  /* 0x0088 - ANAU Control Register */
    HPSYS_CFG_ANAU_RSVD_Type ANAU_RSVD;  /* 0x008C - ANAU Reserve Register */
    HPSYS_CFG_ANATR_Type ANATR;  /* 0x0090 - Analog Test Register */
    HPSYS_CFG_CAU2_CR_Type CAU2_CR;  /* 0x0094 - CAU2 Control Register */
    HPSYS_CFG_CAU2_RSVD_Type CAU2_RSVD;  /* 0x0098 - CAU2 RSVD Register1 */
} HPSYS_CFG_TypeDef;

/* ============================================================================ */
/* EFUSEC 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=EFUSEC,base=0x5000C000,description="",groupName=EFUSEC */

#define EFUSEC_BASE                                        (0x5000C000UL)

/* @SVD:register=CR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control Register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="Write 1 to enable PGM/READ. Self clear" */
        volatile uint32_t EN: 1;  /* Write 1 to enable PGM/READ. Self clear */
        /* @SVD:field=MODE,bitOffset=1,bitWidth=1,description="0 - READ, 1 - PGM" */
        volatile uint32_t MODE: 1;  /* 0 - READ, 1 - PGM */
        /* @SVD:field=BANKSEL,bitOffset=2,bitWidth=2,description="Bank select" */
        volatile uint32_t BANKSEL: 2;  /* Bank select */
        /* @SVD:field=IE,bitOffset=4,bitWidth=1,description="Interrupt enable" */
        volatile uint32_t IE: 1;  /* Interrupt enable */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_CR_Type;

/* @SVD:register=TIMR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Timer Register" */
/* TIMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=THRCK,bitOffset=0,bitWidth=7,description="SCLK to CSB hold time into READ mode. Recmmended value > 500ns" */
        volatile uint32_t THRCK: 7;  /* SCLK to CSB hold time into READ mode. Recmmended value > 500 */
        /* @SVD:field=THPCK,bitOffset=7,bitWidth=3,description="SCLK to CSB hold time into PGM mode. Recommended value > 20ns" */
        volatile uint32_t THPCK: 3;  /* SCLK to CSB hold time into PGM mode. Recommended value > 20n */
        /* @SVD:field=TCKHP,bitOffset=10,bitWidth=11,description="SCLK high period for PGM. Recommended value  ~10us" */
        volatile uint32_t TCKHP: 11;  /* SCLK high period for PGM. Recommended value  ~10us */
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_TIMR_Type;

/* @SVD:register=SR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Status Register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DONE,bitOffset=0,bitWidth=1,description="Indicates PGM/READ done. Write 1 to clear" */
        volatile uint32_t DONE: 1;  /* Indicates PGM/READ done. Write 1 to clear */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_SR_Type;

/* @SVD:register=RSVDR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Reserved Register" */
/* RSVDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t RSVD: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_RSVDR_Type;

/* @SVD:register=PGM_DATA0,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="Program Data0" */
/* PGM_DATA0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_PGM_DATA0_Type;

/* @SVD:register=PGM_DATA1,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Program Data1" */
/* PGM_DATA1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_PGM_DATA1_Type;

/* @SVD:register=PGM_DATA2,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="Program Data2" */
/* PGM_DATA2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_PGM_DATA2_Type;

/* @SVD:register=PGM_DATA3,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="Program Data3" */
/* PGM_DATA3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_PGM_DATA3_Type;

/* @SVD:register=PGM_DATA4,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Program Data4" */
/* PGM_DATA4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_PGM_DATA4_Type;

/* @SVD:register=PGM_DATA5,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Program Data5" */
/* PGM_DATA5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_PGM_DATA5_Type;

/* @SVD:register=PGM_DATA6,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Program Data6" */
/* PGM_DATA6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_PGM_DATA6_Type;

/* @SVD:register=PGM_DATA7,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Program Data7" */
/* PGM_DATA7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_PGM_DATA7_Type;

/* @SVD:register=BANK0_DATA0,offset=0x0030,size=32,access=read-write,reset=0x0,description="Bank0 Data0" */
/* BANK0_DATA0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK0_DATA0_Type;

/* @SVD:register=BANK0_DATA1,offset=0x0034,size=32,access=read-write,reset=0x0,description="Bank0 Data1" */
/* BANK0_DATA1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK0_DATA1_Type;

/* @SVD:register=BANK0_DATA2,offset=0x0038,size=32,access=read-write,reset=0x0,description="Bank0 Data2" */
/* BANK0_DATA2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK0_DATA2_Type;

/* @SVD:register=BANK0_DATA3,offset=0x003C,size=32,access=read-write,reset=0x0,description="Bank0 Data3" */
/* BANK0_DATA3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK0_DATA3_Type;

/* @SVD:register=BANK0_DATA4,offset=0x0040,size=32,access=read-write,reset=0x0,description="Bank0 Data4" */
/* BANK0_DATA4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK0_DATA4_Type;

/* @SVD:register=BANK0_DATA5,offset=0x0044,size=32,access=read-write,reset=0x0,description="Bank0 Data5" */
/* BANK0_DATA5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK0_DATA5_Type;

/* @SVD:register=BANK0_DATA6,offset=0x0048,size=32,access=read-write,reset=0x0,description="Bank0 Data6" */
/* BANK0_DATA6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK0_DATA6_Type;

/* @SVD:register=BANK0_DATA7,offset=0x004C,size=32,access=read-write,reset=0x0,description="Bank0 Data7" */
/* BANK0_DATA7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK0_DATA7_Type;

/* @SVD:register=BANK1_DATA0,offset=0x0050,size=32,access=read-write,reset=0x0,description="Bank1 Data0" */
/* BANK1_DATA0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK1_DATA0_Type;

/* @SVD:register=BANK1_DATA1,offset=0x0054,size=32,access=read-write,reset=0x0,description="Bank1 Data1" */
/* BANK1_DATA1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK1_DATA1_Type;

/* @SVD:register=BANK1_DATA2,offset=0x0058,size=32,access=read-write,reset=0x0,description="Bank1 Data2" */
/* BANK1_DATA2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK1_DATA2_Type;

/* @SVD:register=BANK1_DATA3,offset=0x005C,size=32,access=read-write,reset=0x0,description="Bank1 Data3" */
/* BANK1_DATA3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK1_DATA3_Type;

/* @SVD:register=BANK1_DATA4,offset=0x0060,size=32,access=read-write,reset=0x0,description="Bank1 Data4" */
/* BANK1_DATA4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK1_DATA4_Type;

/* @SVD:register=BANK1_DATA5,offset=0x0064,size=32,access=read-write,reset=0x0,description="Bank1 Data5" */
/* BANK1_DATA5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK1_DATA5_Type;

/* @SVD:register=BANK1_DATA6,offset=0x0068,size=32,access=read-write,reset=0x0,description="Bank1 Data6" */
/* BANK1_DATA6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK1_DATA6_Type;

/* @SVD:register=BANK1_DATA7,offset=0x006C,size=32,access=read-write,reset=0x0,description="Bank1 Data7" */
/* BANK1_DATA7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK1_DATA7_Type;

/* @SVD:register=BANK2_DATA0,offset=0x0070,size=32,access=read-write,reset=0x0,description="Bank2 Data0" */
/* BANK2_DATA0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK2_DATA0_Type;

/* @SVD:register=BANK2_DATA1,offset=0x0074,size=32,access=read-write,reset=0x0,description="Bank2 Data1" */
/* BANK2_DATA1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK2_DATA1_Type;

/* @SVD:register=BANK2_DATA2,offset=0x0078,size=32,access=read-write,reset=0x0,description="Bank2 Data2" */
/* BANK2_DATA2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK2_DATA2_Type;

/* @SVD:register=BANK2_DATA3,offset=0x007C,size=32,access=read-write,reset=0x0,description="Bank2 Data3" */
/* BANK2_DATA3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK2_DATA3_Type;

/* @SVD:register=BANK2_DATA4,offset=0x0080,size=32,access=read-write,reset=0x0,description="Bank2 Data4" */
/* BANK2_DATA4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK2_DATA4_Type;

/* @SVD:register=BANK2_DATA5,offset=0x0084,size=32,access=read-write,reset=0x0,description="Bank2 Data5" */
/* BANK2_DATA5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK2_DATA5_Type;

/* @SVD:register=BANK2_DATA6,offset=0x0088,size=32,access=read-write,reset=0x0,description="Bank2 Data6" */
/* BANK2_DATA6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK2_DATA6_Type;

/* @SVD:register=BANK2_DATA7,offset=0x008C,size=32,access=read-write,reset=0x0,description="Bank2 Data7" */
/* BANK2_DATA7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK2_DATA7_Type;

/* @SVD:register=BANK3_DATA0,offset=0x0090,size=32,access=read-write,reset=0x0,description="Bank3 Data0" */
/* BANK3_DATA0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK3_DATA0_Type;

/* @SVD:register=BANK3_DATA1,offset=0x0094,size=32,access=read-write,reset=0x0,description="Bank3 Data1" */
/* BANK3_DATA1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK3_DATA1_Type;

/* @SVD:register=BANK3_DATA2,offset=0x0098,size=32,access=read-write,reset=0x0,description="Bank3 Data2" */
/* BANK3_DATA2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK3_DATA2_Type;

/* @SVD:register=BANK3_DATA3,offset=0x009C,size=32,access=read-write,reset=0x0,description="Bank3 Data3" */
/* BANK3_DATA3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK3_DATA3_Type;

/* @SVD:register=BANK3_DATA4,offset=0x00A0,size=32,access=read-write,reset=0x0,description="Bank3 Data4" */
/* BANK3_DATA4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK3_DATA4_Type;

/* @SVD:register=BANK3_DATA5,offset=0x00A4,size=32,access=read-write,reset=0x0,description="Bank3 Data5" */
/* BANK3_DATA5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK3_DATA5_Type;

/* @SVD:register=BANK3_DATA6,offset=0x00A8,size=32,access=read-write,reset=0x0,description="Bank3 Data6" */
/* BANK3_DATA6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK3_DATA6_Type;

/* @SVD:register=BANK3_DATA7,offset=0x00AC,size=32,access=read-write,reset=0x0,description="Bank3 Data7" */
/* BANK3_DATA7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_BANK3_DATA7_Type;

/* @SVD:register=ANACR,offset=0x00B0,size=32,access=read-write,reset=0x0,description="Bank3 Data7" */
/* ANACR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LDO_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t LDO_EN: 1;
        /* @SVD:field=LDO_VREF_SEL,bitOffset=1,bitWidth=3,description="" */
        volatile uint32_t LDO_VREF_SEL: 3;
        /* @SVD:field=LDO_MODE,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t LDO_MODE: 1;
        /* @SVD:field=RSVD2,bitOffset=5,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=LDO_DC_TR,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t LDO_DC_TR: 3;
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
        /* @SVD:field=RESERVE0,bitOffset=16,bitWidth=8,description="" */
        volatile uint32_t RESERVE0: 8;
        /* @SVD:field=RESERVE1,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RESERVE1: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_ANACR_Type;

/* @SVD:register=DB_SEL,offset=0x00B4,size=32,access=read-write,reset=0x00000000,description="debug signal select" */
/* DB_SEL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DB_SEL,bitOffset=0,bitWidth=32,description="debug signal select" */
        volatile uint32_t DB_SEL: 32;  /* debug signal select */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} EFUSEC_DB_SEL_Type;

/* EFUSEC 寄存器结构体 */
typedef struct
{
    EFUSEC_CR_Type CR;  /* 0x0000 - Control Register */
    EFUSEC_TIMR_Type TIMR;  /* 0x0004 - Timer Register */
    EFUSEC_SR_Type SR;  /* 0x0008 - Status Register */
    EFUSEC_RSVDR_Type RSVDR;  /* 0x000C - Reserved Register */
    EFUSEC_PGM_DATA0_Type PGM_DATA0;  /* 0x0010 - Program Data0 */
    EFUSEC_PGM_DATA1_Type PGM_DATA1;  /* 0x0014 - Program Data1 */
    EFUSEC_PGM_DATA2_Type PGM_DATA2;  /* 0x0018 - Program Data2 */
    EFUSEC_PGM_DATA3_Type PGM_DATA3;  /* 0x001C - Program Data3 */
    EFUSEC_PGM_DATA4_Type PGM_DATA4;  /* 0x0020 - Program Data4 */
    EFUSEC_PGM_DATA5_Type PGM_DATA5;  /* 0x0024 - Program Data5 */
    EFUSEC_PGM_DATA6_Type PGM_DATA6;  /* 0x0028 - Program Data6 */
    EFUSEC_PGM_DATA7_Type PGM_DATA7;  /* 0x002C - Program Data7 */
    EFUSEC_BANK0_DATA0_Type BANK0_DATA0;  /* 0x0030 - Bank0 Data0 */
    EFUSEC_BANK0_DATA1_Type BANK0_DATA1;  /* 0x0034 - Bank0 Data1 */
    EFUSEC_BANK0_DATA2_Type BANK0_DATA2;  /* 0x0038 - Bank0 Data2 */
    EFUSEC_BANK0_DATA3_Type BANK0_DATA3;  /* 0x003C - Bank0 Data3 */
    EFUSEC_BANK0_DATA4_Type BANK0_DATA4;  /* 0x0040 - Bank0 Data4 */
    EFUSEC_BANK0_DATA5_Type BANK0_DATA5;  /* 0x0044 - Bank0 Data5 */
    EFUSEC_BANK0_DATA6_Type BANK0_DATA6;  /* 0x0048 - Bank0 Data6 */
    EFUSEC_BANK0_DATA7_Type BANK0_DATA7;  /* 0x004C - Bank0 Data7 */
    EFUSEC_BANK1_DATA0_Type BANK1_DATA0;  /* 0x0050 - Bank1 Data0 */
    EFUSEC_BANK1_DATA1_Type BANK1_DATA1;  /* 0x0054 - Bank1 Data1 */
    EFUSEC_BANK1_DATA2_Type BANK1_DATA2;  /* 0x0058 - Bank1 Data2 */
    EFUSEC_BANK1_DATA3_Type BANK1_DATA3;  /* 0x005C - Bank1 Data3 */
    EFUSEC_BANK1_DATA4_Type BANK1_DATA4;  /* 0x0060 - Bank1 Data4 */
    EFUSEC_BANK1_DATA5_Type BANK1_DATA5;  /* 0x0064 - Bank1 Data5 */
    EFUSEC_BANK1_DATA6_Type BANK1_DATA6;  /* 0x0068 - Bank1 Data6 */
    EFUSEC_BANK1_DATA7_Type BANK1_DATA7;  /* 0x006C - Bank1 Data7 */
    EFUSEC_BANK2_DATA0_Type BANK2_DATA0;  /* 0x0070 - Bank2 Data0 */
    EFUSEC_BANK2_DATA1_Type BANK2_DATA1;  /* 0x0074 - Bank2 Data1 */
    EFUSEC_BANK2_DATA2_Type BANK2_DATA2;  /* 0x0078 - Bank2 Data2 */
    EFUSEC_BANK2_DATA3_Type BANK2_DATA3;  /* 0x007C - Bank2 Data3 */
    EFUSEC_BANK2_DATA4_Type BANK2_DATA4;  /* 0x0080 - Bank2 Data4 */
    EFUSEC_BANK2_DATA5_Type BANK2_DATA5;  /* 0x0084 - Bank2 Data5 */
    EFUSEC_BANK2_DATA6_Type BANK2_DATA6;  /* 0x0088 - Bank2 Data6 */
    EFUSEC_BANK2_DATA7_Type BANK2_DATA7;  /* 0x008C - Bank2 Data7 */
    EFUSEC_BANK3_DATA0_Type BANK3_DATA0;  /* 0x0090 - Bank3 Data0 */
    EFUSEC_BANK3_DATA1_Type BANK3_DATA1;  /* 0x0094 - Bank3 Data1 */
    EFUSEC_BANK3_DATA2_Type BANK3_DATA2;  /* 0x0098 - Bank3 Data2 */
    EFUSEC_BANK3_DATA3_Type BANK3_DATA3;  /* 0x009C - Bank3 Data3 */
    EFUSEC_BANK3_DATA4_Type BANK3_DATA4;  /* 0x00A0 - Bank3 Data4 */
    EFUSEC_BANK3_DATA5_Type BANK3_DATA5;  /* 0x00A4 - Bank3 Data5 */
    EFUSEC_BANK3_DATA6_Type BANK3_DATA6;  /* 0x00A8 - Bank3 Data6 */
    EFUSEC_BANK3_DATA7_Type BANK3_DATA7;  /* 0x00AC - Bank3 Data7 */
    EFUSEC_ANACR_Type ANACR;  /* 0x00B0 - Bank3 Data7 */
    EFUSEC_DB_SEL_Type DB_SEL;  /* 0x00B4 - debug signal select */
} EFUSEC_TypeDef;

/* ============================================================================ */
/* AES 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=AES,base=0x5000D000,description="",groupName=AES */

#define AES_BASE                                           (0x5000D000UL)

/* @SVD:register=COMMAND,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="COMMAND" */
/* COMMAND 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=START,bitOffset=0,bitWidth=1,description="write 1 to trigger the AES_ACC block" */
        volatile uint32_t START: 1;  /* write 1 to trigger the AES_ACC block */
        /* @SVD:field=AES_ACC_RESET,bitOffset=1,bitWidth=1,description="AES_ACC soft reset, 1'h1: reset the AES_ACC block" */
        volatile uint32_t AES_ACC_RESET: 1;  /* AES_ACC soft reset, 1'h1: reset the AES_ACC block */
        /* @SVD:field=HASH_START,bitOffset=2,bitWidth=1,description="write 1 to trigger the HASH_ACC block" */
        volatile uint32_t HASH_START: 1;  /* write 1 to trigger the HASH_ACC block */
        /* @SVD:field=HASH_RESET,bitOffset=3,bitWidth=1,description="HASH_ACC soft reset, 1'h1: reset the HASH_ACC block" */
        volatile uint32_t HASH_RESET: 1;  /* HASH_ACC soft reset, 1'h1: reset the HASH_ACC block */
        /* @SVD:field=AUTO_GATE,bitOffset=4,bitWidth=1,description="auto clock gating" */
        volatile uint32_t AUTO_GATE: 1;  /* auto clock gating */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_COMMAND_Type;

/* @SVD:register=STATUS,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="STATUS" */
/* STATUS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BUSY,bitOffset=0,bitWidth=1,description="AES_ACC block is busy" */
        volatile uint32_t BUSY: 1;  /* AES_ACC block is busy */
        /* @SVD:field=FLASH_KEY_VALID,bitOffset=1,bitWidth=1,description="flash key valid indicator" */
        volatile uint32_t FLASH_KEY_VALID: 1;  /* flash key valid indicator */
        /* @SVD:field=HASH_BUSY,bitOffset=2,bitWidth=1,description="HASH_ACC block is busy" */
        volatile uint32_t HASH_BUSY: 1;  /* HASH_ACC block is busy */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=29,description="" */
        volatile uint32_t RSVD: 29;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_STATUS_Type;

/* @SVD:register=IRQ,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="IRQ" */
/* IRQ 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DONE_STAT,bitOffset=0,bitWidth=1,description="AES_ACC done status" */
        volatile uint32_t DONE_STAT: 1;  /* AES_ACC done status */
        /* @SVD:field=BUS_ERR_STAT,bitOffset=1,bitWidth=1,description="AES_ACC bus error status" */
        volatile uint32_t BUS_ERR_STAT: 1;  /* AES_ACC bus error status */
        /* @SVD:field=SETUP_ERR_STAT,bitOffset=2,bitWidth=1,description="AES_ACC setup error status" */
        volatile uint32_t SETUP_ERR_STAT: 1;  /* AES_ACC setup error status */
        /* @SVD:field=HASH_DONE_STAT,bitOffset=3,bitWidth=1,description="HASH_ACC done status" */
        volatile uint32_t HASH_DONE_STAT: 1;  /* HASH_ACC done status */
        /* @SVD:field=HASH_BUS_ERR_STAT,bitOffset=4,bitWidth=1,description="HASH_ACC bus error status" */
        volatile uint32_t HASH_BUS_ERR_STAT: 1;  /* HASH_ACC bus error status */
        /* @SVD:field=HASH_PAD_ERR_STAT,bitOffset=5,bitWidth=1,description="HASH_ACC padding error status" */
        volatile uint32_t HASH_PAD_ERR_STAT: 1;  /* HASH_ACC padding error status */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=10,description="" */
        volatile uint32_t RSVD2: 10;
        /* @SVD:field=DONE_RAW_STAT,bitOffset=16,bitWidth=1,description="AES_ACC done raw status" */
        volatile uint32_t DONE_RAW_STAT: 1;  /* AES_ACC done raw status */
        /* @SVD:field=BUS_ERR_RAW_STAT,bitOffset=17,bitWidth=1,description="AES_ACC bus error raw status" */
        volatile uint32_t BUS_ERR_RAW_STAT: 1;  /* AES_ACC bus error raw status */
        /* @SVD:field=SETUP_ERR_RAW_STAT,bitOffset=18,bitWidth=1,description="AES_ACC setup error raw status" */
        volatile uint32_t SETUP_ERR_RAW_STAT: 1;  /* AES_ACC setup error raw status */
        /* @SVD:field=HASH_DONE_RAW_STAT,bitOffset=19,bitWidth=1,description="HASH_ACC done raw status" */
        volatile uint32_t HASH_DONE_RAW_STAT: 1;  /* HASH_ACC done raw status */
        /* @SVD:field=HASH_BUS_ERR_RAW_STAT,bitOffset=20,bitWidth=1,description="HASH_ACC bus error raw status" */
        volatile uint32_t HASH_BUS_ERR_RAW_STAT: 1;  /* HASH_ACC bus error raw status */
        /* @SVD:field=HASH_PAD_ERR_RAW_STAT,bitOffset=21,bitWidth=1,description="HASH_ACC padding error raw status" */
        volatile uint32_t HASH_PAD_ERR_RAW_STAT: 1;  /* HASH_ACC padding error raw status */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_IRQ_Type;

/* @SVD:register=SETTING,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="SETTING" */
/* SETTING 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DONE_IRQ_MASK,bitOffset=0,bitWidth=1,description="AES_ACC done interrupt mask, 0: mask the interrupt" */
        volatile uint32_t DONE_IRQ_MASK: 1;  /* AES_ACC done interrupt mask, 0: mask the interrupt */
        /* @SVD:field=BUS_ERR_IRQ_MASK,bitOffset=1,bitWidth=1,description="AES_ACC bus error interrupt mask, 0: mask the interrupt" */
        volatile uint32_t BUS_ERR_IRQ_MASK: 1;  /* AES_ACC bus error interrupt mask, 0: mask the interrupt */
        /* @SVD:field=SETUP_ERR_IRQ_MASK,bitOffset=2,bitWidth=1,description="AES_ACC setup error interrupt mask, 0: mask the interrupt" */
        volatile uint32_t SETUP_ERR_IRQ_MASK: 1;  /* AES_ACC setup error interrupt mask, 0: mask the interrupt */
        /* @SVD:field=HASH_DONE_MASK,bitOffset=3,bitWidth=1,description="HASH_ACC done interrupt mask, 0: mask the interrupt" */
        volatile uint32_t HASH_DONE_MASK: 1;  /* HASH_ACC done interrupt mask, 0: mask the interrupt */
        /* @SVD:field=HASH_BUS_ERR_MASK,bitOffset=4,bitWidth=1,description="HASH_ACC bus error interrpt mask, 0: mask the interrupt" */
        volatile uint32_t HASH_BUS_ERR_MASK: 1;  /* HASH_ACC bus error interrpt mask, 0: mask the interrupt */
        /* @SVD:field=HASH_PAD_ERR_MASK,bitOffset=5,bitWidth=1,description="HASH_ACC padding error interrupt mask, 0: mask the interrupt" */
        volatile uint32_t HASH_PAD_ERR_MASK: 1;  /* HASH_ACC padding error interrupt mask, 0: mask the interrupt */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_SETTING_Type;

/* @SVD:register=AES_SETTING,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="AES_SETTING" */
/* AES_SETTING 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=AES_MODE,bitOffset=0,bitWidth=3,description="AES Mode:\n              3'h0: ECB\n              3'h1: CTR\n              3'h2: CBC\n              Others: Reserved" */
        volatile uint32_t AES_MODE: 3;  /* AES Mode:               3'h0: ECB               3'h1: CTR    */
        /* @SVD:field=AES_LENGTH,bitOffset=3,bitWidth=2,description="AES Length:\n              2'h0: 128-bit\n              2'h1: 192-bit\n              2'h2: 256-bit\n              2'h3: Reserved" */
        volatile uint32_t AES_LENGTH: 2;  /* AES Length:               2'h0: 128-bit               2'h1:  */
        /* @SVD:field=KEY_SEL,bitOffset=5,bitWidth=1,description="1'h0: select key from AES_ACC key registers\n              1'h1: use internal root key" */
        volatile uint32_t KEY_SEL: 1;  /* 1'h0: select key from AES_ACC key registers               1' */
        /* @SVD:field=ALGO_STANDARD,bitOffset=6,bitWidth=1,description="1'h0: AES\n              1'h1: SM4" */
        volatile uint32_t ALGO_STANDARD: 1;  /* 1'h0: AES               1'h1: SM4 */
        /* @SVD:field=AES_OP_MODE,bitOffset=7,bitWidth=1,description="1'h0: decryption\n              1'h1: encryption" */
        volatile uint32_t AES_OP_MODE: 1;  /* 1'h0: decryption               1'h1: encryption */
        /* @SVD:field=AES_BYPASS,bitOffset=8,bitWidth=1,description="1'h0: normal operation\n              1'h1: bypass" */
        volatile uint32_t AES_BYPASS: 1;  /* 1'h0: normal operation               1'h1: bypass */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_AES_SETTING_Type;

/* @SVD:register=DMA_IN,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="DMA_IN" */
/* DMA_IN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="AES_ACC input data address" */
        volatile uint32_t ADDR: 32;  /* AES_ACC input data address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_DMA_IN_Type;

/* @SVD:register=DMA_OUT,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="DMA_OUT" */
/* DMA_OUT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="AES_ACC output data address" */
        volatile uint32_t ADDR: 32;  /* AES_ACC output data address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_DMA_OUT_Type;

/* @SVD:register=DMA_DATA,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="DMA_DATA" */
/* DMA_DATA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SIZE,bitOffset=0,bitWidth=28,description="AES_ACC data block size, AES_ACC only support block aligned transaction. Each block contains 16 bytes." */
        volatile uint32_t SIZE: 28;  /* AES_ACC data block size, AES_ACC only support block aligned  */
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_DMA_DATA_Type;

/* @SVD:register=IV_W0,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="IV_W0" */
/* IV_W0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="Initial Vector Word0" */
        volatile uint32_t DATA: 32;  /* Initial Vector Word0 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_IV_W0_Type;

/* @SVD:register=IV_W1,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="IV_W1" */
/* IV_W1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="Initial Vector Word1" */
        volatile uint32_t DATA: 32;  /* Initial Vector Word1 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_IV_W1_Type;

/* @SVD:register=IV_W2,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="IV_W2" */
/* IV_W2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="Initial Vector Word2" */
        volatile uint32_t DATA: 32;  /* Initial Vector Word2 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_IV_W2_Type;

/* @SVD:register=IV_W3,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="IV_W3" */
/* IV_W3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="Initial Vector Word3" */
        volatile uint32_t DATA: 32;  /* Initial Vector Word3 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_IV_W3_Type;

/* @SVD:register=EXT_KEY_W0,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="EXT_KEY_W0" */
/* EXT_KEY_W0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="External Key Word0" */
        volatile uint32_t DATA: 32;  /* External Key Word0 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_EXT_KEY_W0_Type;

/* @SVD:register=EXT_KEY_W1,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="EXT_KEY_W1" */
/* EXT_KEY_W1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="External Key Word1" */
        volatile uint32_t DATA: 32;  /* External Key Word1 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_EXT_KEY_W1_Type;

/* @SVD:register=EXT_KEY_W2,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="EXT_KEY_W2" */
/* EXT_KEY_W2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="External Key Word2" */
        volatile uint32_t DATA: 32;  /* External Key Word2 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_EXT_KEY_W2_Type;

/* @SVD:register=EXT_KEY_W3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="EXT_KEY_W3" */
/* EXT_KEY_W3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="External Key Word3" */
        volatile uint32_t DATA: 32;  /* External Key Word3 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_EXT_KEY_W3_Type;

/* @SVD:register=EXT_KEY_W4,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="EXT_KEY_W4" */
/* EXT_KEY_W4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="External Key Word4" */
        volatile uint32_t DATA: 32;  /* External Key Word4 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_EXT_KEY_W4_Type;

/* @SVD:register=EXT_KEY_W5,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="EXT_KEY_W5" */
/* EXT_KEY_W5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="External Key Word5" */
        volatile uint32_t DATA: 32;  /* External Key Word5 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_EXT_KEY_W5_Type;

/* @SVD:register=EXT_KEY_W6,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="EXT_KEY_W6" */
/* EXT_KEY_W6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="External Key Word6" */
        volatile uint32_t DATA: 32;  /* External Key Word6 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_EXT_KEY_W6_Type;

/* @SVD:register=EXT_KEY_W7,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="EXT_KEY_W7" */
/* EXT_KEY_W7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="External Key Word7" */
        volatile uint32_t DATA: 32;  /* External Key Word7 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_EXT_KEY_W7_Type;

/* @SVD:register=HASH_SETTING,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="HASH_SETTING" */
/* HASH_SETTING 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HASH_MODE,bitOffset=0,bitWidth=3,description="HASH Mode:\n              3'h0: SHA-1\n              3'h1: SHA-224\n              3'h2: SHA-256\n              3'h3: SM3\n              Others: Reserved" */
        volatile uint32_t HASH_MODE: 3;  /* HASH Mode:               3'h0: SHA-1               3'h1: SHA */
        /* @SVD:field=DO_PADDING,bitOffset=3,bitWidth=1,description="HASH padding enable.\n              Set 1 to do padding after data transfer." */
        volatile uint32_t DO_PADDING: 1;  /* HASH padding enable.               Set 1 to do padding after */
        /* @SVD:field=BYTE_SWAP,bitOffset=4,bitWidth=1,description="HASH byte swap option. Set 1 to swap byte order when read data from memory." */
        volatile uint32_t BYTE_SWAP: 1;  /* HASH byte swap option. Set 1 to swap byte order when read da */
        /* @SVD:field=DFT_IV_SEL,bitOffset=5,bitWidth=1,description="HASH default iv select.\n              1'h0: default iv according to hash mode\n              1'h1: default iv from HASH_IV_H* registers" */
        volatile uint32_t DFT_IV_SEL: 1;  /* HASH default iv select.               1'h0: default iv accor */
        /* @SVD:field=RESULT_ENDIAN,bitOffset=6,bitWidth=1,description="hash result endian setting: \n              1'h0: little endian\n              1'h1: big endian" */
        volatile uint32_t RESULT_ENDIAN: 1;  /* hash result endian setting:                1'h0: little endi */
        /* @SVD:field=HASH_IV_LOAD,bitOffset=7,bitWidth=1,description="write 1 to load hash iv" */
        volatile uint32_t HASH_IV_LOAD: 1;  /* write 1 to load hash iv */
        /* @SVD:field=HASH_LEN_LOAD,bitOffset=8,bitWidth=1,description="write 1 to load hash length" */
        volatile uint32_t HASH_LEN_LOAD: 1;  /* write 1 to load hash length */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_SETTING_Type;

/* @SVD:register=HASH_DMA_IN,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="HASH_DMA_IN" */
/* HASH_DMA_IN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="input data address" */
        volatile uint32_t ADDR: 32;  /* input data address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_DMA_IN_Type;

/* @SVD:register=HASH_DMA_DATA,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="HASH_DMA_DATA" */
/* HASH_DMA_DATA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SIZE,bitOffset=0,bitWidth=32,description="HASH input data byte size." */
        volatile uint32_t SIZE: 32;  /* HASH input data byte size. */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_DMA_DATA_Type;

/* @SVD:register=HASH_IV_H0,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="HASH_IV_H0" */
/* HASH_IV_H0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH IV H0" */
        volatile uint32_t DATA: 32;  /* HASH IV H0 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_IV_H0_Type;

/* @SVD:register=HASH_IV_H1,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="HASH_IV_H1" */
/* HASH_IV_H1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH IV H1" */
        volatile uint32_t DATA: 32;  /* HASH IV H1 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_IV_H1_Type;

/* @SVD:register=HASH_IV_H2,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="HASH_IV_H2" */
/* HASH_IV_H2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH IV H2" */
        volatile uint32_t DATA: 32;  /* HASH IV H2 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_IV_H2_Type;

/* @SVD:register=HASH_IV_H3,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="HASH_IV_H3" */
/* HASH_IV_H3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH IV H3" */
        volatile uint32_t DATA: 32;  /* HASH IV H3 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_IV_H3_Type;

/* @SVD:register=HASH_IV_H4,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="HASH_IV_H4" */
/* HASH_IV_H4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH IV H4" */
        volatile uint32_t DATA: 32;  /* HASH IV H4 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_IV_H4_Type;

/* @SVD:register=HASH_IV_H5,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="HASH_IV_H5" */
/* HASH_IV_H5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH IV H5" */
        volatile uint32_t DATA: 32;  /* HASH IV H5 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_IV_H5_Type;

/* @SVD:register=HASH_IV_H6,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="HASH_IV_H6" */
/* HASH_IV_H6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH IV H6" */
        volatile uint32_t DATA: 32;  /* HASH IV H6 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_IV_H6_Type;

/* @SVD:register=HASH_IV_H7,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="HASH_IV_H7" */
/* HASH_IV_H7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH IV H7" */
        volatile uint32_t DATA: 32;  /* HASH IV H7 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_IV_H7_Type;

/* @SVD:register=HASH_RESULT_H0,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_H0" */
/* HASH_RESULT_H0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH result H0" */
        volatile uint32_t DATA: 32;  /* HASH result H0 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_H0_Type;

/* @SVD:register=HASH_RESULT_H1,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_H1" */
/* HASH_RESULT_H1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH result H1" */
        volatile uint32_t DATA: 32;  /* HASH result H1 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_H1_Type;

/* @SVD:register=HASH_RESULT_H2,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_H2" */
/* HASH_RESULT_H2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH result H2" */
        volatile uint32_t DATA: 32;  /* HASH result H2 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_H2_Type;

/* @SVD:register=HASH_RESULT_H3,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_H3" */
/* HASH_RESULT_H3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH result H3" */
        volatile uint32_t DATA: 32;  /* HASH result H3 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_H3_Type;

/* @SVD:register=HASH_RESULT_H4,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_H4" */
/* HASH_RESULT_H4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH result H4" */
        volatile uint32_t DATA: 32;  /* HASH result H4 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_H4_Type;

/* @SVD:register=HASH_RESULT_H5,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_H5" */
/* HASH_RESULT_H5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH result H5" */
        volatile uint32_t DATA: 32;  /* HASH result H5 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_H5_Type;

/* @SVD:register=HASH_RESULT_H6,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_H6" */
/* HASH_RESULT_H6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH result H6" */
        volatile uint32_t DATA: 32;  /* HASH result H6 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_H6_Type;

/* @SVD:register=HASH_RESULT_H7,offset=0x0098,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_H7" */
/* HASH_RESULT_H7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH result H7" */
        volatile uint32_t DATA: 32;  /* HASH result H7 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_H7_Type;

/* @SVD:register=HASH_LEN_L,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="HASH_LEN_L" */
/* HASH_LEN_L 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH load length l" */
        volatile uint32_t DATA: 32;  /* HASH load length l */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_LEN_L_Type;

/* @SVD:register=HASH_LEN_H,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="HASH_LEN_H" */
/* HASH_LEN_H 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=29,description="HASH load length h" */
        volatile uint32_t DATA: 29;  /* HASH load length h */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_LEN_H_Type;

/* @SVD:register=HASH_RESULT_LEN_L,offset=0x00A4,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_LEN_L" */
/* HASH_RESULT_LEN_L 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="HASH result length l" */
        volatile uint32_t DATA: 32;  /* HASH result length l */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_LEN_L_Type;

/* @SVD:register=HASH_RESULT_LEN_H,offset=0x00A8,size=32,access=read-write,reset=0x00000000,description="HASH_RESULT_LEN_H" */
/* HASH_RESULT_LEN_H 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=29,description="HASH result length h" */
        volatile uint32_t DATA: 29;  /* HASH result length h */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AES_HASH_RESULT_LEN_H_Type;

/* AES 寄存器结构体 */
typedef struct
{
    AES_COMMAND_Type COMMAND;  /* 0x0000 - COMMAND */
    AES_STATUS_Type STATUS;  /* 0x0004 - STATUS */
    AES_IRQ_Type IRQ;  /* 0x0008 - IRQ */
    AES_SETTING_Type SETTING;  /* 0x000C - SETTING */
    AES_AES_SETTING_Type AES_SETTING;  /* 0x0010 - AES_SETTING */
    AES_DMA_IN_Type DMA_IN;  /* 0x0014 - DMA_IN */
    AES_DMA_OUT_Type DMA_OUT;  /* 0x0018 - DMA_OUT */
    AES_DMA_DATA_Type DMA_DATA;  /* 0x001C - DMA_DATA */
    AES_IV_W0_Type IV_W0;  /* 0x0020 - IV_W0 */
    AES_IV_W1_Type IV_W1;  /* 0x0024 - IV_W1 */
    AES_IV_W2_Type IV_W2;  /* 0x0028 - IV_W2 */
    AES_IV_W3_Type IV_W3;  /* 0x002C - IV_W3 */
    AES_EXT_KEY_W0_Type EXT_KEY_W0;  /* 0x0030 - EXT_KEY_W0 */
    AES_EXT_KEY_W1_Type EXT_KEY_W1;  /* 0x0034 - EXT_KEY_W1 */
    AES_EXT_KEY_W2_Type EXT_KEY_W2;  /* 0x0038 - EXT_KEY_W2 */
    AES_EXT_KEY_W3_Type EXT_KEY_W3;  /* 0x003C - EXT_KEY_W3 */
    AES_EXT_KEY_W4_Type EXT_KEY_W4;  /* 0x0040 - EXT_KEY_W4 */
    AES_EXT_KEY_W5_Type EXT_KEY_W5;  /* 0x0044 - EXT_KEY_W5 */
    AES_EXT_KEY_W6_Type EXT_KEY_W6;  /* 0x0048 - EXT_KEY_W6 */
    AES_EXT_KEY_W7_Type EXT_KEY_W7;  /* 0x004C - EXT_KEY_W7 */
    AES_HASH_SETTING_Type HASH_SETTING;  /* 0x0050 - HASH_SETTING */
    AES_HASH_DMA_IN_Type HASH_DMA_IN;  /* 0x0054 - HASH_DMA_IN */
    AES_HASH_DMA_DATA_Type HASH_DMA_DATA;  /* 0x0058 - HASH_DMA_DATA */
    AES_HASH_IV_H0_Type HASH_IV_H0;  /* 0x005C - HASH_IV_H0 */
    AES_HASH_IV_H1_Type HASH_IV_H1;  /* 0x0060 - HASH_IV_H1 */
    AES_HASH_IV_H2_Type HASH_IV_H2;  /* 0x0064 - HASH_IV_H2 */
    AES_HASH_IV_H3_Type HASH_IV_H3;  /* 0x0068 - HASH_IV_H3 */
    AES_HASH_IV_H4_Type HASH_IV_H4;  /* 0x006C - HASH_IV_H4 */
    AES_HASH_IV_H5_Type HASH_IV_H5;  /* 0x0070 - HASH_IV_H5 */
    AES_HASH_IV_H6_Type HASH_IV_H6;  /* 0x0074 - HASH_IV_H6 */
    AES_HASH_IV_H7_Type HASH_IV_H7;  /* 0x0078 - HASH_IV_H7 */
    AES_HASH_RESULT_H0_Type HASH_RESULT_H0;  /* 0x007C - HASH_RESULT_H0 */
    AES_HASH_RESULT_H1_Type HASH_RESULT_H1;  /* 0x0080 - HASH_RESULT_H1 */
    AES_HASH_RESULT_H2_Type HASH_RESULT_H2;  /* 0x0084 - HASH_RESULT_H2 */
    AES_HASH_RESULT_H3_Type HASH_RESULT_H3;  /* 0x0088 - HASH_RESULT_H3 */
    AES_HASH_RESULT_H4_Type HASH_RESULT_H4;  /* 0x008C - HASH_RESULT_H4 */
    AES_HASH_RESULT_H5_Type HASH_RESULT_H5;  /* 0x0090 - HASH_RESULT_H5 */
    AES_HASH_RESULT_H6_Type HASH_RESULT_H6;  /* 0x0094 - HASH_RESULT_H6 */
    AES_HASH_RESULT_H7_Type HASH_RESULT_H7;  /* 0x0098 - HASH_RESULT_H7 */
    AES_HASH_LEN_L_Type HASH_LEN_L;  /* 0x009C - HASH_LEN_L */
    AES_HASH_LEN_H_Type HASH_LEN_H;  /* 0x00A0 - HASH_LEN_H */
    AES_HASH_RESULT_LEN_L_Type HASH_RESULT_LEN_L;  /* 0x00A4 - HASH_RESULT_LEN_L */
    AES_HASH_RESULT_LEN_H_Type HASH_RESULT_LEN_H;  /* 0x00A8 - HASH_RESULT_LEN_H */
} AES_TypeDef;

/* ============================================================================ */
/* TRNG 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=TRNG,base=0x5000F000,description="",groupName=TRNG */

#define TRNG_BASE                                          (0x5000F000UL)

/* @SVD:register=CTRL,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="CTRL" */
/* CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GEN_SEED_START,bitOffset=0,bitWidth=1,description="write 1 to trigger the random seed generation engine" */
        volatile uint32_t GEN_SEED_START: 1;  /* write 1 to trigger the random seed generation engine */
        /* @SVD:field=GEN_RAND_NUM_START,bitOffset=1,bitWidth=1,description="write 1 to trigger the random number generation engine" */
        volatile uint32_t GEN_RAND_NUM_START: 1;  /* write 1 to trigger the random number generation engine */
        /* @SVD:field=GEN_SEED_STOP,bitOffset=2,bitWidth=1,description="Set 1 to stop random seed generation. This will reset the random seed generation engine. After release the stop bit, user should write 1 to gen_seed_start to trigger the random seed engine." */
        volatile uint32_t GEN_SEED_STOP: 1;  /* Set 1 to stop random seed generation. This will reset the ra */
        /* @SVD:field=GEN_RAND_NUM_STOP,bitOffset=3,bitWidth=1,description="Set 1 to stop random number generation and update. This will reset the random number generation engine. After release the stop bit, user should write 1 to gen_rand_num_start to trigger the random number engine." */
        volatile uint32_t GEN_RAND_NUM_STOP: 1;  /* Set 1 to stop random number generation and update. This will */
        /* @SVD:field=GEN_RAND_NUM_SUSPEND,bitOffset=4,bitWidth=1,description="Set 1 to suspend random number generation and update. Set 0 to recover the process." */
        volatile uint32_t GEN_RAND_NUM_SUSPEND: 1;  /* Set 1 to suspend random number generation and update. Set 0  */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_CTRL_Type;

/* @SVD:register=STAT,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="STAT" */
/* STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEED_GEN_BUSY,bitOffset=0,bitWidth=1,description="random seed engine busy flag" */
        volatile uint32_t SEED_GEN_BUSY: 1;  /* random seed engine busy flag */
        /* @SVD:field=SEED_VALID,bitOffset=1,bitWidth=1,description="random seed valid flag" */
        volatile uint32_t SEED_VALID: 1;  /* random seed valid flag */
        /* @SVD:field=RAND_NUM_GEN_BUSY,bitOffset=2,bitWidth=1,description="random number engine busy flag" */
        volatile uint32_t RAND_NUM_GEN_BUSY: 1;  /* random number engine busy flag */
        /* @SVD:field=RAND_NUM_VALID,bitOffset=3,bitWidth=1,description="random number valid flag" */
        volatile uint32_t RAND_NUM_VALID: 1;  /* random number valid flag */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_STAT_Type;

/* @SVD:register=CFG,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="CFG" */
/* CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=AUTO_CLOCK_ENABLE,bitOffset=0,bitWidth=1,description="auto clock gating enable" */
        volatile uint32_t AUTO_CLOCK_ENABLE: 1;  /* auto clock gating enable */
        /* @SVD:field=USE_EXT_SEED,bitOffset=1,bitWidth=1,description="set 1 to use external seed to generate random number" */
        volatile uint32_t USE_EXT_SEED: 1;  /* set 1 to use external seed to generate random number */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=REJECT_THRESHOLD,bitOffset=8,bitWidth=8,description="random seed internal VN corrector check threshold" */
        volatile uint32_t REJECT_THRESHOLD: 8;  /* random seed internal VN corrector check threshold */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_CFG_Type;

/* @SVD:register=IRQ,offset=0x000C,size=32,access=read-write,reset=0x00070000,description="IRQ" */
/* IRQ 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEED_GEN_DONE,bitOffset=0,bitWidth=1,description="random seed generation done raw interrupt" */
        volatile uint32_t SEED_GEN_DONE: 1;  /* random seed generation done raw interrupt */
        /* @SVD:field=RAND_NUM_AVAIL,bitOffset=1,bitWidth=1,description="random number available raw interrupt" */
        volatile uint32_t RAND_NUM_AVAIL: 1;  /* random number available raw interrupt */
        /* @SVD:field=PRNG_LOCKUP,bitOffset=2,bitWidth=1,description="prng lockup raw interrupt" */
        volatile uint32_t PRNG_LOCKUP: 1;  /* prng lockup raw interrupt */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=13,description="" */
        volatile uint32_t RSVD2: 13;
        /* @SVD:field=SEED_GEN_DONE_MSK,bitOffset=16,bitWidth=1,description="random seed generation done interrupt mask" */
        volatile uint32_t SEED_GEN_DONE_MSK: 1;  /* random seed generation done interrupt mask */
        /* @SVD:field=RAND_NUM_AVAIL_MSK,bitOffset=17,bitWidth=1,description="random number available interrupt mask" */
        volatile uint32_t RAND_NUM_AVAIL_MSK: 1;  /* random number available interrupt mask */
        /* @SVD:field=PRNG_LOCKUP_MSK,bitOffset=18,bitWidth=1,description="prng lockup interrupt mask" */
        volatile uint32_t PRNG_LOCKUP_MSK: 1;  /* prng lockup interrupt mask */
        /* @SVD:field=RSVD,bitOffset=19,bitWidth=13,description="" */
        volatile uint32_t RSVD: 13;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_IRQ_Type;

/* @SVD:register=RAND_SEED0,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="RAND_SEED0" */
/* RAND_SEED0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random seed value0. If using external random seed, write value to this register will update the random seed in use." */
        volatile uint32_t VAL: 32;  /* random seed value0. If using external random seed, write val */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_SEED0_Type;

/* @SVD:register=RAND_SEED1,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="RAND_SEED1" */
/* RAND_SEED1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random seed value1. If using external random seed, write value to this register will update the random seed in use." */
        volatile uint32_t VAL: 32;  /* random seed value1. If using external random seed, write val */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_SEED1_Type;

/* @SVD:register=RAND_SEED2,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="RAND_SEED2" */
/* RAND_SEED2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random seed value2. If using external random seed, write value to this register will update the random seed in use." */
        volatile uint32_t VAL: 32;  /* random seed value2. If using external random seed, write val */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_SEED2_Type;

/* @SVD:register=RAND_SEED3,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="RAND_SEED3" */
/* RAND_SEED3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random seed value3. If using external random seed, write value to this register will update the random seed in use." */
        volatile uint32_t VAL: 32;  /* random seed value3. If using external random seed, write val */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_SEED3_Type;

/* @SVD:register=RAND_SEED4,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="RAND_SEED4" */
/* RAND_SEED4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random seed value4. If using external random seed, write value to this register will update the random seed in use." */
        volatile uint32_t VAL: 32;  /* random seed value4. If using external random seed, write val */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_SEED4_Type;

/* @SVD:register=RAND_SEED5,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="RAND_SEED5" */
/* RAND_SEED5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random seed value5. If using external random seed, write value to this register will update the random seed in use." */
        volatile uint32_t VAL: 32;  /* random seed value5. If using external random seed, write val */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_SEED5_Type;

/* @SVD:register=RAND_SEED6,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="RAND_SEED6" */
/* RAND_SEED6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random seed value6. If using external random seed, write value to this register will update the random seed in use." */
        volatile uint32_t VAL: 32;  /* random seed value6. If using external random seed, write val */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_SEED6_Type;

/* @SVD:register=RAND_SEED7,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="RAND_SEED7" */
/* RAND_SEED7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random seed value7. If using external random seed, write value to this register will update the random seed in use." */
        volatile uint32_t VAL: 32;  /* random seed value7. If using external random seed, write val */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_SEED7_Type;

/* @SVD:register=RAND_NUM0,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="RAND_NUM0" */
/* RAND_NUM0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random number value0" */
        volatile uint32_t VAL: 32;  /* random number value0 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_NUM0_Type;

/* @SVD:register=RAND_NUM1,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="RAND_NUM1" */
/* RAND_NUM1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random number value1" */
        volatile uint32_t VAL: 32;  /* random number value1 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_NUM1_Type;

/* @SVD:register=RAND_NUM2,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="RAND_NUM2" */
/* RAND_NUM2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random number value2" */
        volatile uint32_t VAL: 32;  /* random number value2 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_NUM2_Type;

/* @SVD:register=RAND_NUM3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="RAND_NUM3" */
/* RAND_NUM3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random number value3" */
        volatile uint32_t VAL: 32;  /* random number value3 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_NUM3_Type;

/* @SVD:register=RAND_NUM4,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="RAND_NUM4" */
/* RAND_NUM4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random number value4" */
        volatile uint32_t VAL: 32;  /* random number value4 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_NUM4_Type;

/* @SVD:register=RAND_NUM5,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="RAND_NUM5" */
/* RAND_NUM5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random number value5" */
        volatile uint32_t VAL: 32;  /* random number value5 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_NUM5_Type;

/* @SVD:register=RAND_NUM6,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="RAND_NUM6" */
/* RAND_NUM6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random number value6" */
        volatile uint32_t VAL: 32;  /* random number value6 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_NUM6_Type;

/* @SVD:register=RAND_NUM7,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="RAND_NUM7" */
/* RAND_NUM7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VAL,bitOffset=0,bitWidth=32,description="random number value7" */
        volatile uint32_t VAL: 32;  /* random number value7 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_RAND_NUM7_Type;

/* @SVD:register=CAL_CFG,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="CAL_CFG" */
/* CAL_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OSC_CLK_FORCE_ON,bitOffset=0,bitWidth=1,description="osc force enable" */
        volatile uint32_t OSC_CLK_FORCE_ON: 1;  /* osc force enable */
        /* @SVD:field=OSC_CLK_SEL,bitOffset=1,bitWidth=3,description="osc clock select" */
        volatile uint32_t OSC_CLK_SEL: 3;  /* osc clock select */
        /* @SVD:field=ENABLE,bitOffset=4,bitWidth=1,description="calibration enable" */
        volatile uint32_t ENABLE: 1;  /* calibration enable */
        /* @SVD:field=DONE,bitOffset=5,bitWidth=1,description="calibration done" */
        volatile uint32_t DONE: 1;  /* calibration done */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=LENGTH,bitOffset=16,bitWidth=16,description="calibration length" */
        volatile uint32_t LENGTH: 16;  /* calibration length */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_CAL_CFG_Type;

/* @SVD:register=CAL_RESULT,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="CAL_RESULT" */
/* CAL_RESULT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PCLK_CNT,bitOffset=0,bitWidth=16,description="pclk calibration counter result" */
        volatile uint32_t PCLK_CNT: 16;  /* pclk calibration counter result */
        /* @SVD:field=OSC_CNT,bitOffset=16,bitWidth=16,description="osc clock calibration counter result" */
        volatile uint32_t OSC_CNT: 16;  /* osc clock calibration counter result */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TRNG_CAL_RESULT_Type;

/* TRNG 寄存器结构体 */
typedef struct
{
    TRNG_CTRL_Type CTRL;  /* 0x0000 - CTRL */
    TRNG_STAT_Type STAT;  /* 0x0004 - STAT */
    TRNG_CFG_Type CFG;  /* 0x0008 - CFG */
    TRNG_IRQ_Type IRQ;  /* 0x000C - IRQ */
    TRNG_RAND_SEED0_Type RAND_SEED0;  /* 0x0010 - RAND_SEED0 */
    TRNG_RAND_SEED1_Type RAND_SEED1;  /* 0x0014 - RAND_SEED1 */
    TRNG_RAND_SEED2_Type RAND_SEED2;  /* 0x0018 - RAND_SEED2 */
    TRNG_RAND_SEED3_Type RAND_SEED3;  /* 0x001C - RAND_SEED3 */
    TRNG_RAND_SEED4_Type RAND_SEED4;  /* 0x0020 - RAND_SEED4 */
    TRNG_RAND_SEED5_Type RAND_SEED5;  /* 0x0024 - RAND_SEED5 */
    TRNG_RAND_SEED6_Type RAND_SEED6;  /* 0x0028 - RAND_SEED6 */
    TRNG_RAND_SEED7_Type RAND_SEED7;  /* 0x002C - RAND_SEED7 */
    TRNG_RAND_NUM0_Type RAND_NUM0;  /* 0x0030 - RAND_NUM0 */
    TRNG_RAND_NUM1_Type RAND_NUM1;  /* 0x0034 - RAND_NUM1 */
    TRNG_RAND_NUM2_Type RAND_NUM2;  /* 0x0038 - RAND_NUM2 */
    TRNG_RAND_NUM3_Type RAND_NUM3;  /* 0x003C - RAND_NUM3 */
    TRNG_RAND_NUM4_Type RAND_NUM4;  /* 0x0040 - RAND_NUM4 */
    TRNG_RAND_NUM5_Type RAND_NUM5;  /* 0x0044 - RAND_NUM5 */
    TRNG_RAND_NUM6_Type RAND_NUM6;  /* 0x0048 - RAND_NUM6 */
    TRNG_RAND_NUM7_Type RAND_NUM7;  /* 0x004C - RAND_NUM7 */
    TRNG_CAL_CFG_Type CAL_CFG;  /* 0x0050 - CAL_CFG */
    TRNG_CAL_RESULT_Type CAL_RESULT;  /* 0x0054 - CAL_RESULT */
} TRNG_TypeDef;

/* ============================================================================ */
/* MPI1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=MPI1,base=0x50041000,description="",groupName=MPI */

#define MPI1_BASE                                          (0x50041000UL)

/* @SVD:register=CR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control Register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="Enable MPI" */
        volatile uint32_t EN: 1;  /* Enable MPI */
        /* @SVD:field=WPE,bitOffset=1,bitWidth=1,description="Enable WP function on IO2. Use this only in SPI or Dual SPI mode" */
        volatile uint32_t WPE: 1;  /* Enable WP function on IO2. Use this only in SPI or Dual SPI  */
        /* @SVD:field=WP,bitOffset=2,bitWidth=1,description="The value of WP when WPE is set" */
        volatile uint32_t WP: 1;  /* The value of WP when WPE is set */
        /* @SVD:field=HOLDE,bitOffset=3,bitWidth=1,description="Enable HOLD function on IO3. Use this only in SPI or Dual SPI mode" */
        volatile uint32_t HOLDE: 1;  /* Enable HOLD function on IO3. Use this only in SPI or Dual SP */
        /* @SVD:field=HOLD,bitOffset=4,bitWidth=1,description="The value of HOLD when HOLDE is set" */
        volatile uint32_t HOLD: 1;  /* The value of HOLD when HOLDE is set */
        /* @SVD:field=DMAE,bitOffset=5,bitWidth=1,description="DMA enable\n              0: disabled\n              1: enable DMA to read or write DR register" */
        volatile uint32_t DMAE: 1;  /* DMA enable               0: disabled               1: enable */
        /* @SVD:field=CTRE,bitOffset=6,bitWidth=1,description="AES-CTR on-the-fly decryption enable\n              0: disabled\n              1: enabled, data read from memory will be decrypted on the fly by MPI controller" */
        volatile uint32_t CTRE: 1;  /* AES-CTR on-the-fly decryption enable               0: disabl */
        /* @SVD:field=CTRM,bitOffset=7,bitWidth=1,description="AES-CTR mode\n              0: AES-128\n              1: AES-256" */
        volatile uint32_t CTRM: 1;  /* AES-CTR mode               0: AES-128               1: AES-2 */
        /* @SVD:field=TCIE,bitOffset=8,bitWidth=1,description="Transfer complete interrupt enable" */
        volatile uint32_t TCIE: 1;  /* Transfer complete interrupt enable */
        /* @SVD:field=RSVD4,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=SMIE,bitOffset=11,bitWidth=1,description="Status match interrupt enable" */
        volatile uint32_t SMIE: 1;  /* Status match interrupt enable */
        /* @SVD:field=CSVIE,bitOffset=12,bitWidth=1,description="CS max violation interrupt enable" */
        volatile uint32_t CSVIE: 1;  /* CS max violation interrupt enable */
        /* @SVD:field=RBXIE,bitOffset=13,bitWidth=1,description="Row boundary crossing interrupt enable" */
        volatile uint32_t RBXIE: 1;  /* Row boundary crossing interrupt enable */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CMD2E,bitOffset=16,bitWidth=1,description="Enable CMD2\n              0: disabled\n              1: CMD2 is enabled and will be issued after CMD1 with an interval of TI2" */
        volatile uint32_t CMD2E: 1;  /* Enable CMD2               0: disabled               1: CMD2  */
        /* @SVD:field=SME1,bitOffset=17,bitWidth=1,description="Status match enable. If enabled, CMD1 will be issued repeatedly until the data match the value in SMR and SMKR\n              0: disabled\n              1: enabled (either SME1 or SME2 can be enabled, and SME1 has high priority)" */
        volatile uint32_t SME1: 1;  /* Status match enable. If enabled, CMD1 will be issued repeate */
        /* @SVD:field=SME2,bitOffset=18,bitWidth=1,description="Status match enable. If enabled, CMD2 will be issued repeatedly until the data match the value in SMR and SMKR\n              0: disabled\n              1: enabled" */
        volatile uint32_t SME2: 1;  /* Status match enable. If enabled, CMD2 will be issued repeate */
        /* @SVD:field=SMM,bitOffset=19,bitWidth=1,description="Status match mode\n              0: AND mode\n              1: OR mode" */
        volatile uint32_t SMM: 1;  /* Status match mode               0: AND mode               1: */
        /* @SVD:field=HWIFE,bitOffset=20,bitWidth=1,description="Hardware interface enableReserved-Do not modify" */
        volatile uint32_t HWIFE: 1;  /* Hardware interface enableReserved-Do not modify */
        /* @SVD:field=OPIE,bitOffset=21,bitWidth=1,description="OPI interface enable\n              0: x8 mode disabled\n              1: x8 mode enabled" */
        volatile uint32_t OPIE: 1;  /* OPI interface enable               0: x8 mode disabled       */
        /* @SVD:field=PREFE,bitOffset=22,bitWidth=1,description="Prefetch enable. If enabled, MPI will prefetch at consequtive address following a read transaction. \n              Recommend to use when reading large data in a burst manner.\n              0: prefetch disabled\n              1: prefetch enabled" */
        volatile uint32_t PREFE: 1;  /* Prefetch enable. If enabled, MPI will prefetch at consequtiv */
        /* @SVD:field=MX16,bitOffset=23,bitWidth=1,description="Mode X16Reserved-Do not modify" */
        volatile uint32_t MX16: 1;  /* Mode X16Reserved-Do not modify */
        /* @SVD:field=DFM,bitOffset=24,bitWidth=1,description="Dual Flash ModeReserved-Do not modify" */
        volatile uint32_t DFM: 1;  /* Dual Flash ModeReserved-Do not modify */
        /* @SVD:field=AHBDIS,bitOffset=25,bitWidth=1,description="Hold hreadyout low if AHB access" */
        volatile uint32_t AHBDIS: 1;  /* Hold hreadyout low if AHB access */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
        /* @SVD:field=ABORT,bitOffset=31,bitWidth=1,description="Write 1 to abort internal state machine. For debug purpose only" */
        volatile uint32_t ABORT: 1;  /* Write 1 to abort internal state machine. For debug purpose o */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CR_Type;

/* @SVD:register=DR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Data Register" */
/* DR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="The entry of internal data FIFO" */
        volatile uint32_t DATA: 32;  /* The entry of internal data FIFO */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_DR_Type;

/* @SVD:register=DCR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Device Control Register" */
/* DCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RBSIZE,bitOffset=0,bitWidth=3,description="Row boundary size.\n              0: no row boundary\n              1: 2^(1+3) = 16 bytes\n              2: 2^(2+3) = 32 bytes\n              …\n              n: 2^(n+3) bytes" */
        volatile uint32_t RBSIZE: 3;  /* Row boundary size.               0: no row boundary          */
        /* @SVD:field=DQSE,bitOffset=3,bitWidth=1,description="DQS enable. Setting to 1 indicates device provides DQS signal for Rx data latching" */
        volatile uint32_t DQSE: 1;  /* DQS enable. Setting to 1 indicates device provides DQS signa */
        /* @SVD:field=HYPER,bitOffset=4,bitWidth=1,description="HyperBus protocol. Set to 1 for HyperRAM." */
        volatile uint32_t HYPER: 1;  /* HyperBus protocol. Set to 1 for HyperRAM. */
        /* @SVD:field=XLEGACY,bitOffset=5,bitWidth=1,description="Xccela legacy protocol. Set to 1 for AP 32Mb PSRAM only, othersize always set to 0." */
        volatile uint32_t XLEGACY: 1;  /* Xccela legacy protocol. Set to 1 for AP 32Mb PSRAM only, oth */
        /* @SVD:field=CSLMAX,bitOffset=6,bitWidth=12,description="Maximum CS low active time in MCLK cycles\n              For example, if PSRAM clock is 120MHz (i.e. internal MCLK is 240MHz) and CSLMAX = n, then\n              CS Low time = (n+1) * 1000/240 ns which must meet the maximum tCEM requirement for PSRAM" */
        volatile uint32_t CSLMAX: 12;  /* Maximum CS low active time in MCLK cycles               For  */
        /* @SVD:field=CSLMIN,bitOffset=18,bitWidth=4,description="Minimum CS low active time in MCLK cycles.\n              For example, if PSRAM clock is 120MHz (i.e. internal MCLK is 240MHz) and CSLMIN = n, then\n              CS Low time = (n+1) * 1000/240 ns which must meet the minimum tCEM requirement for PSRAM" */
        volatile uint32_t CSLMIN: 4;  /* Minimum CS low active time in MCLK cycles.               For */
        /* @SVD:field=CSHMIN,bitOffset=22,bitWidth=4,description="Minimum CS high deselect time in MCLK cycles.\n              For example, if PSRAM clock is 120MHz (i.e. internal MCLK is 240MHz) and CSHMIN = n, then\n              CS High time = (n+1) * 1000/240 ns which must meet minimum tCPH requirement for PSRAM" */
        volatile uint32_t CSHMIN: 4;  /* Minimum CS high deselect time in MCLK cycles.                */
        /* @SVD:field=TRCMIN,bitOffset=26,bitWidth=5,description="Write/Read cycle minimum time in internal MCLK cycles. Please see MCLK frequency in PSCLR description.\n              For example, if PSRAM clock is 120MHz (i.e. internal MCLK is 240MHz) and TRCMIN = n, then\n              tRC time = (n+1) * 1000/240 ns which must meet minimum tRC requirement for PSRAM" */
        volatile uint32_t TRCMIN: 5;  /* Write/Read cycle minimum time in internal MCLK cycles. Pleas */
        /* @SVD:field=FIXLAT,bitOffset=31,bitWidth=1,description="Indicate PSRAM is fixed latency or variable latency. It must be compatible to the configuration in PSRAM registers.\n              Recommend always set to 1.\n              0: variable latency\n              1: fixed latency" */
        volatile uint32_t FIXLAT: 1;  /* Indicate PSRAM is fixed latency or variable latency. It must */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_DCR_Type;

/* @SVD:register=PSCLR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Prescaler Register" */
/* PSCLR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DIV,bitOffset=0,bitWidth=8,description="Prescaler divider.\n              0: MCLK = FCLK/1\n              1: MCLK = FCLK/1\n              2: MCLK = FCLK/2\n              n: MCLK = FCLK/n\n              Note: FLASH clock = MCLK. E.g. FCLK=192M and DIV=2, then FLASH clock = MCLK = 192/2 = 96MHz\n              PSRAM clock = MCLK/2. E.g. FCLK=240M and DIV=1, then PSRAM clock = MCLK/2 = 240/2 = 120MHz" */
        volatile uint32_t DIV: 8;  /* Prescaler divider.               0: MCLK = FCLK/1            */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_PSCLR_Type;

/* @SVD:register=SR,offset=0x0010,size=32,access=read-write,reset=0x00000006,description="Status Register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCF,bitOffset=0,bitWidth=1,description="Transfer complete flag" */
        volatile uint32_t TCF: 1;  /* Transfer complete flag */
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SMF,bitOffset=3,bitWidth=1,description="Status match flag in Polling Mode" */
        volatile uint32_t SMF: 1;  /* Status match flag in Polling Mode */
        /* @SVD:field=CSVF,bitOffset=4,bitWidth=1,description="CS max violation flag" */
        volatile uint32_t CSVF: 1;  /* CS max violation flag */
        /* @SVD:field=RBXF,bitOffset=5,bitWidth=1,description="Row boundary crossing flag" */
        volatile uint32_t RBXF: 1;  /* Row boundary crossing flag */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
        /* @SVD:field=BUSY,bitOffset=31,bitWidth=1,description="For debug purpose only" */
        volatile uint32_t BUSY: 1;  /* For debug purpose only */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_SR_Type;

/* @SVD:register=SCR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Status Clear Register" */
/* SCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCFC,bitOffset=0,bitWidth=1,description="Write 1 to clear TCF" */
        volatile uint32_t TCFC: 1;  /* Write 1 to clear TCF */
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SMFC,bitOffset=3,bitWidth=1,description="Write 1 to clear SMF" */
        volatile uint32_t SMFC: 1;  /* Write 1 to clear SMF */
        /* @SVD:field=CSVFC,bitOffset=4,bitWidth=1,description="Write 1 to clear CSVF" */
        volatile uint32_t CSVFC: 1;  /* Write 1 to clear CSVF */
        /* @SVD:field=RBXFC,bitOffset=5,bitWidth=1,description="Write 1 to clear RBXF" */
        volatile uint32_t RBXFC: 1;  /* Write 1 to clear RBXF */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_SCR_Type;

/* @SVD:register=CMDR1,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="Command Register" */
/* CMDR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMD,bitOffset=0,bitWidth=8,description="Command. Write to this register will trigger the sequence specified in CCR1" */
        volatile uint32_t CMD: 8;  /* Command. Write to this register will trigger the sequence sp */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CMDR1_Type;

/* @SVD:register=AR1,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="Address Register" */
/* AR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="Address" */
        volatile uint32_t ADDR: 32;  /* Address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_AR1_Type;

/* @SVD:register=ABR1,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Alternate Byte Register" */
/* ABR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ABYTE,bitOffset=0,bitWidth=32,description="Alternate byte" */
        volatile uint32_t ABYTE: 32;  /* Alternate byte */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_ABR1_Type;

/* @SVD:register=DLR1,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Data Length Register" */
/* DLR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DLEN,bitOffset=0,bitWidth=20,description="Data length\n              0: one byte\n              1: two bytes\n              …\n              n: (n+1) bytes" */
        volatile uint32_t DLEN: 20;  /* Data length               0: one byte               1: two b */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_DLR1_Type;

/* @SVD:register=CCR1,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Communication Configuration Register" */
/* CCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IMODE,bitOffset=0,bitWidth=3,description="Instruction mode\n              0: no instruction phase\n              1: single line\n              2: dual lines\n              3: quad lines\n              4/5/6 - reserved\n              7 - quad lines DDR" */
        volatile uint32_t IMODE: 3;  /* Instruction mode               0: no instruction phase       */
        /* @SVD:field=ADMODE,bitOffset=3,bitWidth=3,description="Address mode\n              0: no address phase\n              1: single line\n              2: dual line\n              3: quad line\n              4/5/6: reserved\n              7: quad line DDR" */
        volatile uint32_t ADMODE: 3;  /* Address mode               0: no address phase               */
        /* @SVD:field=ADSIZE,bitOffset=6,bitWidth=2,description="Address size\n              0: one byte\n              1: two bytes\n              2: three bytes\n              3: four bytes" */
        volatile uint32_t ADSIZE: 2;  /* Address size               0: one byte               1: two  */
        /* @SVD:field=ABMODE,bitOffset=8,bitWidth=3,description="Alternate byte mode\n              0: no alternate byte\n              1: single line\n              2: dual lines\n              3: quad lines\n              4/5/6: reserved\n              7: quad lines DDR" */
        volatile uint32_t ABMODE: 3;  /* Alternate byte mode               0: no alternate byte       */
        /* @SVD:field=ABSIZE,bitOffset=11,bitWidth=2,description="Alternate byte size\n              0: one byte\n              1: two bytes\n              2: three bytes\n              3: four bytes" */
        volatile uint32_t ABSIZE: 2;  /* Alternate byte size               0: one byte                */
        /* @SVD:field=DCYC,bitOffset=13,bitWidth=5,description="Number of dummy cycles\n              0: no dummy cycle\n              1: one dummy cycle\n              2: two dummy cycles" */
        volatile uint32_t DCYC: 5;  /* Number of dummy cycles               0: no dummy cycle       */
        /* @SVD:field=DMODE,bitOffset=18,bitWidth=3,description="Data Mode\n              0: no data phase\n              1: single line\n              2: dual lines\n              3: quad lines\n              4/5/6: reserved\n              7: quad lines DDR" */
        volatile uint32_t DMODE: 3;  /* Data Mode               0: no data phase               1: si */
        /* @SVD:field=FMODE,bitOffset=21,bitWidth=1,description="Function Mode\n              0: read mode\n              1: write mode" */
        volatile uint32_t FMODE: 1;  /* Function Mode               0: read mode               1: wr */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CCR1_Type;

/* @SVD:register=CMDR2,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Command Register" */
/* CMDR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMD,bitOffset=0,bitWidth=8,description="Command 2. If CMD2E is enabled, the CMD2 sequence will be issued after CMD1 as specified in CCR2\n              Note: CMD2 sequence cannot be issue individually" */
        volatile uint32_t CMD: 8;  /* Command 2. If CMD2E is enabled, the CMD2 sequence will be is */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CMDR2_Type;

/* @SVD:register=AR2,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Address Register" */
/* AR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="Address byte in CMD2 sequence" */
        volatile uint32_t ADDR: 32;  /* Address byte in CMD2 sequence */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_AR2_Type;

/* @SVD:register=ABR2,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Alternate Byte Register" */
/* ABR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ABYTE,bitOffset=0,bitWidth=32,description="Alternate byte in CMD2 sequence" */
        volatile uint32_t ABYTE: 32;  /* Alternate byte in CMD2 sequence */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_ABR2_Type;

/* @SVD:register=DLR2,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="Data Length Register" */
/* DLR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DLEN,bitOffset=0,bitWidth=20,description="Data length in CMD2 sequence" */
        volatile uint32_t DLEN: 20;  /* Data length in CMD2 sequence */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_DLR2_Type;

/* @SVD:register=CCR2,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="Communication Configuration Register" */
/* CCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IMODE,bitOffset=0,bitWidth=3,description="This register specifies the format of CMD2 sequence. Refer to CCR1 description" */
        volatile uint32_t IMODE: 3;  /* This register specifies the format of CMD2 sequence. Refer t */
        /* @SVD:field=ADMODE,bitOffset=3,bitWidth=3,description="" */
        volatile uint32_t ADMODE: 3;
        /* @SVD:field=ADSIZE,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t ADSIZE: 2;
        /* @SVD:field=ABMODE,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t ABMODE: 3;
        /* @SVD:field=ABSIZE,bitOffset=11,bitWidth=2,description="" */
        volatile uint32_t ABSIZE: 2;
        /* @SVD:field=DCYC,bitOffset=13,bitWidth=5,description="" */
        volatile uint32_t DCYC: 5;
        /* @SVD:field=DMODE,bitOffset=18,bitWidth=3,description="" */
        volatile uint32_t DMODE: 3;
        /* @SVD:field=FMODE,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t FMODE: 1;
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CCR2_Type;

/* @SVD:register=HCMDR,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="AHB Command Register" */
/* HCMDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RCMD,bitOffset=0,bitWidth=8,description="AHB read command. During XIP, the AHB read transaction will be translated into this Read Command on memory interface" */
        volatile uint32_t RCMD: 8;  /* AHB read command. During XIP, the AHB read transaction will  */
        /* @SVD:field=WCMD,bitOffset=8,bitWidth=8,description="AHB write command. During XIP, the AHB write transaction will be translated into this Write Command on memory interface" */
        volatile uint32_t WCMD: 8;  /* AHB write command. During XIP, the AHB write transaction wil */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_HCMDR_Type;

/* @SVD:register=HRABR,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="AHB Read Alternate Byte Register" */
/* HRABR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ABYTE,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t ABYTE: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_HRABR_Type;

/* @SVD:register=HRCCR,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="AHB Read Communication Configuration Register" */
/* HRCCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IMODE,bitOffset=0,bitWidth=3,description="This register specifies the format of AHB read command sequence. Refer to CCR1 description" */
        volatile uint32_t IMODE: 3;  /* This register specifies the format of AHB read command seque */
        /* @SVD:field=ADMODE,bitOffset=3,bitWidth=3,description="" */
        volatile uint32_t ADMODE: 3;
        /* @SVD:field=ADSIZE,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t ADSIZE: 2;
        /* @SVD:field=ABMODE,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t ABMODE: 3;
        /* @SVD:field=ABSIZE,bitOffset=11,bitWidth=2,description="" */
        volatile uint32_t ABSIZE: 2;
        /* @SVD:field=DCYC,bitOffset=13,bitWidth=5,description="" */
        volatile uint32_t DCYC: 5;
        /* @SVD:field=DMODE,bitOffset=18,bitWidth=3,description="" */
        volatile uint32_t DMODE: 3;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_HRCCR_Type;

/* @SVD:register=HWABR,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="AHB Write Alternate Byte Register" */
/* HWABR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ABYTE,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t ABYTE: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_HWABR_Type;

/* @SVD:register=HWCCR,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="AHB Write Communication Configuration Register" */
/* HWCCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IMODE,bitOffset=0,bitWidth=3,description="This register specifies the format of AHB write command sequence. Refer to CCR1 description" */
        volatile uint32_t IMODE: 3;  /* This register specifies the format of AHB write command sequ */
        /* @SVD:field=ADMODE,bitOffset=3,bitWidth=3,description="" */
        volatile uint32_t ADMODE: 3;
        /* @SVD:field=ADSIZE,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t ADSIZE: 2;
        /* @SVD:field=ABMODE,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t ABMODE: 3;
        /* @SVD:field=ABSIZE,bitOffset=11,bitWidth=2,description="" */
        volatile uint32_t ABSIZE: 2;
        /* @SVD:field=DCYC,bitOffset=13,bitWidth=5,description="" */
        volatile uint32_t DCYC: 5;
        /* @SVD:field=DMODE,bitOffset=18,bitWidth=3,description="" */
        volatile uint32_t DMODE: 3;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_HWCCR_Type;

/* @SVD:register=FIFOCR,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="FIFO Control Register" */
/* FIFOCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RXCLR,bitOffset=0,bitWidth=1,description="write 1 to clear Rx FIFO" */
        volatile uint32_t RXCLR: 1;  /* write 1 to clear Rx FIFO */
        /* @SVD:field=RXE,bitOffset=1,bitWidth=1,description="Rx FIFO empty" */
        volatile uint32_t RXE: 1;  /* Rx FIFO empty */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=TXCLR,bitOffset=8,bitWidth=1,description="write 1 to clear Tx FIFO" */
        volatile uint32_t TXCLR: 1;  /* write 1 to clear Tx FIFO */
        /* @SVD:field=TXF,bitOffset=9,bitWidth=1,description="Tx FIFO full flag" */
        volatile uint32_t TXF: 1;  /* Tx FIFO full flag */
        /* @SVD:field=TXSLOTS,bitOffset=10,bitWidth=5,description="When DMA enabled, asserts DMA reqeust if TXFIFO vacant slots is greater than or equal to TXSLOTS.\n              Note: this field should be set in accordance to the burst length in DMA. For example, if DMA employs BURST8 transction, then this filed is set to 8" */
        volatile uint32_t TXSLOTS: 5;  /* When DMA enabled, asserts DMA reqeust if TXFIFO vacant slots */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_FIFOCR_Type;

/* @SVD:register=MISCR,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="Miscelaneous Register" */
/* MISCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RXCLKDLY,bitOffset=0,bitWidth=8,description="Add delay on internal Rx clock  to fine tune the sampling position. Note: effective 5-bit" */
        volatile uint32_t RXCLKDLY: 8;  /* Add delay on internal Rx clock  to fine tune the sampling po */
        /* @SVD:field=SCKDLY,bitOffset=8,bitWidth=8,description="Add delay on output clock to fine tune the clock position. Note: effective 7-bit" */
        volatile uint32_t SCKDLY: 8;  /* Add delay on output clock to fine tune the clock position. N */
        /* @SVD:field=DQSDLY,bitOffset=16,bitWidth=8,description="Delay the input DQS signal to the appropriate sampling position.  For device w/ DQS signal only. Note: effective 7-bit" */
        volatile uint32_t DQSDLY: 8;  /* Delay the input DQS signal to the appropriate sampling posit */
        /* @SVD:field=RXCLKINV,bitOffset=24,bitWidth=1,description="Invert internal Rx clock to add half-cycle delay (coarse tune) when sampling data. It is usually used for FLASH device w/ higher frequency." */
        volatile uint32_t RXCLKINV: 1;  /* Invert internal Rx clock to add half-cycle delay (coarse tun */
        /* @SVD:field=SCKINV,bitOffset=25,bitWidth=1,description="Invert output clock. This bit is used to align (coarse tune) the output clock to the center of output data." */
        volatile uint32_t SCKINV: 1;  /* Invert output clock. This bit is used to align (coarse tune) */
        /* @SVD:field=DTRPRE,bitOffset=26,bitWidth=1,description="Enable pre-sampling for DTRReserved-Do not modify" */
        volatile uint32_t DTRPRE: 1;  /* Enable pre-sampling for DTRReserved-Do not modify */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
        /* @SVD:field=DBGSEL,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t DBGSEL: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_MISCR_Type;

/* @SVD:register=CTRSAR,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="CTR Starting Address Register" */
/* CTRSAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=SA,bitOffset=10,bitWidth=22,description="Starting address of the AES decryption area. Since the lowest 10 bits are zero, the address is always 1KB aligned.\n              Together with CTREAR, the total area is [CTRSAR, CTREAR)\n              For example, CTRSAR = 32'h0, CTREAR = 32'h200000, then the on-the-fly decryption area is 0x0 - 0x1FFFFF" */
        volatile uint32_t SA: 22;  /* Starting address of the AES decryption area. Since the lowes */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CTRSAR_Type;

/* @SVD:register=CTREAR,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="CTR Ending Address Register" */
/* CTREAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=EA,bitOffset=10,bitWidth=22,description="Ending address of the AES decryption area" */
        volatile uint32_t EA: 22;  /* Ending address of the AES decryption area */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CTREAR_Type;

/* @SVD:register=NONCEA,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="Nonce A Register" */
/* NONCEA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NONCEA,bitOffset=0,bitWidth=32,description="Used for on-the-fly decryption" */
        volatile uint32_t NONCEA: 32;  /* Used for on-the-fly decryption */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_NONCEA_Type;

/* @SVD:register=NONCEB,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="Nonce B Register" */
/* NONCEB 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NONCEB,bitOffset=0,bitWidth=32,description="Used for on-the-fly decryption" */
        volatile uint32_t NONCEB: 32;  /* Used for on-the-fly decryption */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_NONCEB_Type;

/* @SVD:register=AASAR,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="Address Aliasing Start Address Register" */
/* AASAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=SA,bitOffset=10,bitWidth=22,description="Starting address of the address aliasing area. Always 1KB aligned.Together with AAEAR, the aliasing area is [AASAR, AAEAR). \n              If the address falls into this area, an offset AAOAR is added and the aliased address will be used to access external memory" */
        volatile uint32_t SA: 22;  /* Starting address of the address aliasing area. Always 1KB al */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_AASAR_Type;

/* @SVD:register=AAEAR,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="Address Aliasing Ending Address Register" */
/* AAEAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=EA,bitOffset=10,bitWidth=22,description="Ending address of the address aliasing area" */
        volatile uint32_t EA: 22;  /* Ending address of the address aliasing area */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_AAEAR_Type;

/* @SVD:register=AAOAR,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="Address Aliasing Offset Address Register" */
/* AAOAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=OA,bitOffset=10,bitWidth=22,description="The offset to be added to the original address" */
        volatile uint32_t OA: 22;  /* The offset to be added to the original address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_AAOAR_Type;

/* @SVD:register=CIR,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="Command Interval Register" */
/* CIR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INTERVAL1,bitOffset=0,bitWidth=16,description="The interval between CMD1 itself. The unit is in MCLK cycles" */
        volatile uint32_t INTERVAL1: 16;  /* The interval between CMD1 itself. The unit is in MCLK cycles */
        /* @SVD:field=INTERVAL2,bitOffset=16,bitWidth=16,description="The interval between CMD1 and CMD2 (or between CMD2 itself) if CMD2E is enabled. The unit is in MCLK cycles" */
        volatile uint32_t INTERVAL2: 16;  /* The interval between CMD1 and CMD2 (or between CMD2 itself)  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CIR_Type;

/* @SVD:register=SMR,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="Status Match Register" */
/* SMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=STATUS,bitOffset=0,bitWidth=32,description="If status match is enabled, this register is compared with the data read from external memory.\n              Together with SMKR, only the bits with mask=1 will be considered to compare in AND or OR mode as configured in SMM field." */
        volatile uint32_t STATUS: 32;  /* If status match is enabled, this register is compared with t */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_SMR_Type;

/* @SVD:register=SMKR,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="Status Mask Register" */
/* SMKR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MASK,bitOffset=0,bitWidth=32,description="Status mask\n              0: the corresponding bit is not considered to compare\n              1: the corresponding bit is considered to compare" */
        volatile uint32_t MASK: 32;  /* Status mask               0: the corresponding bit is not co */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_SMKR_Type;

/* @SVD:register=TIMR,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="Timer Register" */
/* TIMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TIMEOUT,bitOffset=0,bitWidth=16,description="After the transaction is complete, CS remains low for multiple cycles of MCLK as specified by this register.\n              For example if TIMEOUT=n, CS remains active for n cycles, during which if a new transaction occurs and the address is consecutive, the memory access can be resumed w/o sending the command and address again." */
        volatile uint32_t TIMEOUT: 16;  /* After the transaction is complete, CS remains low for multip */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_TIMR_Type;

/* @SVD:register=WDTR,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="WDT Register" */
/* WDTR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TIMEOUT,bitOffset=0,bitWidth=16,description="Set timeout value in number of clk_wdt cycles" */
        volatile uint32_t TIMEOUT: 16;  /* Set timeout value in number of clk_wdt cycles */
        /* @SVD:field=EN,bitOffset=16,bitWidth=1,description="WDT enable. This watchdog is on AHB side such that bus access will not hang in exceptional cases" */
        volatile uint32_t EN: 1;  /* WDT enable. This watchdog is on AHB side such that bus acces */
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
        /* @SVD:field=TOF,bitOffset=31,bitWidth=1,description="Timeout flag. Self cleared when HREADYOUT becomes ready" */
        volatile uint32_t TOF: 1;  /* Timeout flag. Self cleared when HREADYOUT becomes ready */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_WDTR_Type;

/* @SVD:register=PRSAR,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="Prefetch Starting Address Register" */
/* PRSAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=SA,bitOffset=10,bitWidth=22,description="Starting address of the prefetch area\n              If prefetch is enabled and the read address falls into [PRSAR, PREAR), controller will prefetch the following data" */
        volatile uint32_t SA: 22;  /* Starting address of the prefetch area               If prefe */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_PRSAR_Type;

/* @SVD:register=PREAR,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="Prefetch Ending Address Register" */
/* PREAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=EA,bitOffset=10,bitWidth=22,description="Ending address of the prefetch area" */
        volatile uint32_t EA: 22;  /* Ending address of the prefetch area */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_PREAR_Type;

/* @SVD:register=CALCR,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="Calibration Clock Register" */
/* CALCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DELAY,bitOffset=0,bitWidth=8,description="calibration delay result" */
        volatile uint32_t DELAY: 8;  /* calibration delay result */
        /* @SVD:field=DONE,bitOffset=8,bitWidth=1,description="calibration done flag" */
        volatile uint32_t DONE: 1;  /* calibration done flag */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
        /* @SVD:field=EN,bitOffset=31,bitWidth=1,description="calibration enable" */
        volatile uint32_t EN: 1;  /* calibration enable */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CALCR_Type;

/* @SVD:register=APM32CR,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="APM32 Control Register" */
/* APM32CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCPHR,bitOffset=0,bitWidth=4,description="For special use by AP 32Mb PSRAM.Reserved-Do not modify" */
        volatile uint32_t TCPHR: 4;  /* For special use by AP 32Mb PSRAM.Reserved-Do not modify */
        /* @SVD:field=TCPHW,bitOffset=4,bitWidth=4,description="For special use by AP 32Mb PSRAM.Reserved-Do not modify" */
        volatile uint32_t TCPHW: 4;  /* For special use by AP 32Mb PSRAM.Reserved-Do not modify */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_APM32CR_Type;

/* @SVD:register=CR2,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="Control Register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LOOP,bitOffset=0,bitWidth=8,description="Repeat CMD1->CMD2 sequence for n times. This filed is only valid when CMD2E=1 and SME2=0.\n              For example if LOOP=0, then the sequence is CMD1 -> CMD2.\n              If LOOP=2, then the sequence is (CMD1->CMD2) -> (CMD1->CMD2) -> (CMD1->CMD2)" */
        volatile uint32_t LOOP: 8;  /* Repeat CMD1->CMD2 sequence for n times. This filed is only v */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI1_CR2_Type;

/* MPI1 寄存器结构体 */
typedef struct
{
    MPI1_CR_Type CR;  /* 0x0000 - Control Register */
    MPI1_DR_Type DR;  /* 0x0004 - Data Register */
    MPI1_DCR_Type DCR;  /* 0x0008 - Device Control Register */
    MPI1_PSCLR_Type PSCLR;  /* 0x000C - Prescaler Register */
    MPI1_SR_Type SR;  /* 0x0010 - Status Register */
    MPI1_SCR_Type SCR;  /* 0x0014 - Status Clear Register */
    MPI1_CMDR1_Type CMDR1;  /* 0x0018 - Command Register */
    MPI1_AR1_Type AR1;  /* 0x001C - Address Register */
    MPI1_ABR1_Type ABR1;  /* 0x0020 - Alternate Byte Register */
    MPI1_DLR1_Type DLR1;  /* 0x0024 - Data Length Register */
    MPI1_CCR1_Type CCR1;  /* 0x0028 - Communication Configuration Register */
    MPI1_CMDR2_Type CMDR2;  /* 0x002C - Command Register */
    MPI1_AR2_Type AR2;  /* 0x0030 - Address Register */
    MPI1_ABR2_Type ABR2;  /* 0x0034 - Alternate Byte Register */
    MPI1_DLR2_Type DLR2;  /* 0x0038 - Data Length Register */
    MPI1_CCR2_Type CCR2;  /* 0x003C - Communication Configuration Register */
    MPI1_HCMDR_Type HCMDR;  /* 0x0040 - AHB Command Register */
    MPI1_HRABR_Type HRABR;  /* 0x0044 - AHB Read Alternate Byte Register */
    MPI1_HRCCR_Type HRCCR;  /* 0x0048 - AHB Read Communication Configuration Reg */
    MPI1_HWABR_Type HWABR;  /* 0x004C - AHB Write Alternate Byte Register */
    MPI1_HWCCR_Type HWCCR;  /* 0x0050 - AHB Write Communication Configuration Re */
    MPI1_FIFOCR_Type FIFOCR;  /* 0x0054 - FIFO Control Register */
    MPI1_MISCR_Type MISCR;  /* 0x0058 - Miscelaneous Register */
    MPI1_CTRSAR_Type CTRSAR;  /* 0x005C - CTR Starting Address Register */
    MPI1_CTREAR_Type CTREAR;  /* 0x0060 - CTR Ending Address Register */
    MPI1_NONCEA_Type NONCEA;  /* 0x0064 - Nonce A Register */
    MPI1_NONCEB_Type NONCEB;  /* 0x0068 - Nonce B Register */
    MPI1_AASAR_Type AASAR;  /* 0x006C - Address Aliasing Start Address Register */
    MPI1_AAEAR_Type AAEAR;  /* 0x0070 - Address Aliasing Ending Address Register */
    MPI1_AAOAR_Type AAOAR;  /* 0x0074 - Address Aliasing Offset Address Register */
    MPI1_CIR_Type CIR;  /* 0x0078 - Command Interval Register */
    MPI1_SMR_Type SMR;  /* 0x007C - Status Match Register */
    MPI1_SMKR_Type SMKR;  /* 0x0080 - Status Mask Register */
    MPI1_TIMR_Type TIMR;  /* 0x0084 - Timer Register */
    MPI1_WDTR_Type WDTR;  /* 0x0088 - WDT Register */
    MPI1_PRSAR_Type PRSAR;  /* 0x008C - Prefetch Starting Address Register */
    MPI1_PREAR_Type PREAR;  /* 0x0090 - Prefetch Ending Address Register */
    MPI1_CALCR_Type CALCR;  /* 0x0094 - Calibration Clock Register */
    /* @SVD:register=RSVD1,offset=0x0098,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0098 - RSVD1 */
    MPI1_APM32CR_Type APM32CR;  /* 0x009C - APM32 Control Register */
    MPI1_CR2_Type CR2;  /* 0x00A0 - Control Register 2 */
} MPI1_TypeDef;

/* ============================================================================ */
/* MPI2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=MPI2,base=0x50042000,description="",groupName=MPI */

#define MPI2_BASE                                          (0x50042000UL)

/* @SVD:register=CR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control Register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="Enable MPI" */
        volatile uint32_t EN: 1;  /* Enable MPI */
        /* @SVD:field=WPE,bitOffset=1,bitWidth=1,description="Enable WP function on IO2. Use this only in SPI or Dual SPI mode" */
        volatile uint32_t WPE: 1;  /* Enable WP function on IO2. Use this only in SPI or Dual SPI  */
        /* @SVD:field=WP,bitOffset=2,bitWidth=1,description="The value of WP when WPE is set" */
        volatile uint32_t WP: 1;  /* The value of WP when WPE is set */
        /* @SVD:field=HOLDE,bitOffset=3,bitWidth=1,description="Enable HOLD function on IO3. Use this only in SPI or Dual SPI mode" */
        volatile uint32_t HOLDE: 1;  /* Enable HOLD function on IO3. Use this only in SPI or Dual SP */
        /* @SVD:field=HOLD,bitOffset=4,bitWidth=1,description="The value of HOLD when HOLDE is set" */
        volatile uint32_t HOLD: 1;  /* The value of HOLD when HOLDE is set */
        /* @SVD:field=DMAE,bitOffset=5,bitWidth=1,description="DMA enable\n              0: disabled\n              1: enable DMA to read or write DR register" */
        volatile uint32_t DMAE: 1;  /* DMA enable               0: disabled               1: enable */
        /* @SVD:field=CTRE,bitOffset=6,bitWidth=1,description="AES-CTR on-the-fly decryption enable\n              0: disabled\n              1: enabled, data read from memory will be decrypted on the fly by MPI controller" */
        volatile uint32_t CTRE: 1;  /* AES-CTR on-the-fly decryption enable               0: disabl */
        /* @SVD:field=CTRM,bitOffset=7,bitWidth=1,description="AES-CTR mode\n              0: AES-128\n              1: AES-256" */
        volatile uint32_t CTRM: 1;  /* AES-CTR mode               0: AES-128               1: AES-2 */
        /* @SVD:field=TCIE,bitOffset=8,bitWidth=1,description="Transfer complete interrupt enable" */
        volatile uint32_t TCIE: 1;  /* Transfer complete interrupt enable */
        /* @SVD:field=RSVD4,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=SMIE,bitOffset=11,bitWidth=1,description="Status match interrupt enable" */
        volatile uint32_t SMIE: 1;  /* Status match interrupt enable */
        /* @SVD:field=CSVIE,bitOffset=12,bitWidth=1,description="CS max violation interrupt enable" */
        volatile uint32_t CSVIE: 1;  /* CS max violation interrupt enable */
        /* @SVD:field=RBXIE,bitOffset=13,bitWidth=1,description="Row boundary crossing interrupt enable" */
        volatile uint32_t RBXIE: 1;  /* Row boundary crossing interrupt enable */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CMD2E,bitOffset=16,bitWidth=1,description="Enable CMD2\n              0: disabled\n              1: CMD2 is enabled and will be issued after CMD1 with an interval of TI2" */
        volatile uint32_t CMD2E: 1;  /* Enable CMD2               0: disabled               1: CMD2  */
        /* @SVD:field=SME1,bitOffset=17,bitWidth=1,description="Status match enable. If enabled, CMD1 will be issued repeatedly until the data match the value in SMR and SMKR\n              0: disabled\n              1: enabled (either SME1 or SME2 can be enabled, and SME1 has high priority)" */
        volatile uint32_t SME1: 1;  /* Status match enable. If enabled, CMD1 will be issued repeate */
        /* @SVD:field=SME2,bitOffset=18,bitWidth=1,description="Status match enable. If enabled, CMD2 will be issued repeatedly until the data match the value in SMR and SMKR\n              0: disabled\n              1: enabled" */
        volatile uint32_t SME2: 1;  /* Status match enable. If enabled, CMD2 will be issued repeate */
        /* @SVD:field=SMM,bitOffset=19,bitWidth=1,description="Status match mode\n              0: AND mode\n              1: OR mode" */
        volatile uint32_t SMM: 1;  /* Status match mode               0: AND mode               1: */
        /* @SVD:field=HWIFE,bitOffset=20,bitWidth=1,description="Hardware interface enableReserved-Do not modify" */
        volatile uint32_t HWIFE: 1;  /* Hardware interface enableReserved-Do not modify */
        /* @SVD:field=OPIE,bitOffset=21,bitWidth=1,description="OPI interface enable\n              0: x8 mode disabled\n              1: x8 mode enabled" */
        volatile uint32_t OPIE: 1;  /* OPI interface enable               0: x8 mode disabled       */
        /* @SVD:field=PREFE,bitOffset=22,bitWidth=1,description="Prefetch enable. If enabled, MPI will prefetch at consequtive address following a read transaction. \n              Recommend to use when reading large data in a burst manner.\n              0: prefetch disabled\n              1: prefetch enabled" */
        volatile uint32_t PREFE: 1;  /* Prefetch enable. If enabled, MPI will prefetch at consequtiv */
        /* @SVD:field=MX16,bitOffset=23,bitWidth=1,description="Mode X16Reserved-Do not modify" */
        volatile uint32_t MX16: 1;  /* Mode X16Reserved-Do not modify */
        /* @SVD:field=DFM,bitOffset=24,bitWidth=1,description="Dual Flash ModeReserved-Do not modify" */
        volatile uint32_t DFM: 1;  /* Dual Flash ModeReserved-Do not modify */
        /* @SVD:field=AHBDIS,bitOffset=25,bitWidth=1,description="Hold hreadyout low if AHB access" */
        volatile uint32_t AHBDIS: 1;  /* Hold hreadyout low if AHB access */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
        /* @SVD:field=ABORT,bitOffset=31,bitWidth=1,description="Write 1 to abort internal state machine. For debug purpose only" */
        volatile uint32_t ABORT: 1;  /* Write 1 to abort internal state machine. For debug purpose o */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CR_Type;

/* @SVD:register=DR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Data Register" */
/* DR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="The entry of internal data FIFO" */
        volatile uint32_t DATA: 32;  /* The entry of internal data FIFO */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_DR_Type;

/* @SVD:register=DCR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Device Control Register" */
/* DCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RBSIZE,bitOffset=0,bitWidth=3,description="Row boundary size.\n              0: no row boundary\n              1: 2^(1+3) = 16 bytes\n              2: 2^(2+3) = 32 bytes\n              …\n              n: 2^(n+3) bytes" */
        volatile uint32_t RBSIZE: 3;  /* Row boundary size.               0: no row boundary          */
        /* @SVD:field=DQSE,bitOffset=3,bitWidth=1,description="DQS enable. Setting to 1 indicates device provides DQS signal for Rx data latching" */
        volatile uint32_t DQSE: 1;  /* DQS enable. Setting to 1 indicates device provides DQS signa */
        /* @SVD:field=HYPER,bitOffset=4,bitWidth=1,description="HyperBus protocol. Set to 1 for HyperRAM." */
        volatile uint32_t HYPER: 1;  /* HyperBus protocol. Set to 1 for HyperRAM. */
        /* @SVD:field=XLEGACY,bitOffset=5,bitWidth=1,description="Xccela legacy protocol. Set to 1 for AP 32Mb PSRAM only, othersize always set to 0." */
        volatile uint32_t XLEGACY: 1;  /* Xccela legacy protocol. Set to 1 for AP 32Mb PSRAM only, oth */
        /* @SVD:field=CSLMAX,bitOffset=6,bitWidth=12,description="Maximum CS low active time in MCLK cycles\n              For example, if PSRAM clock is 120MHz (i.e. internal MCLK is 240MHz) and CSLMAX = n, then\n              CS Low time = (n+1) * 1000/240 ns which must meet the maximum tCEM requirement for PSRAM" */
        volatile uint32_t CSLMAX: 12;  /* Maximum CS low active time in MCLK cycles               For  */
        /* @SVD:field=CSLMIN,bitOffset=18,bitWidth=4,description="Minimum CS low active time in MCLK cycles.\n              For example, if PSRAM clock is 120MHz (i.e. internal MCLK is 240MHz) and CSLMIN = n, then\n              CS Low time = (n+1) * 1000/240 ns which must meet the minimum tCEM requirement for PSRAM" */
        volatile uint32_t CSLMIN: 4;  /* Minimum CS low active time in MCLK cycles.               For */
        /* @SVD:field=CSHMIN,bitOffset=22,bitWidth=4,description="Minimum CS high deselect time in MCLK cycles.\n              For example, if PSRAM clock is 120MHz (i.e. internal MCLK is 240MHz) and CSHMIN = n, then\n              CS High time = (n+1) * 1000/240 ns which must meet minimum tCPH requirement for PSRAM" */
        volatile uint32_t CSHMIN: 4;  /* Minimum CS high deselect time in MCLK cycles.                */
        /* @SVD:field=TRCMIN,bitOffset=26,bitWidth=5,description="Write/Read cycle minimum time in internal MCLK cycles. Please see MCLK frequency in PSCLR description.\n              For example, if PSRAM clock is 120MHz (i.e. internal MCLK is 240MHz) and TRCMIN = n, then\n              tRC time = (n+1) * 1000/240 ns which must meet minimum tRC requirement for PSRAM" */
        volatile uint32_t TRCMIN: 5;  /* Write/Read cycle minimum time in internal MCLK cycles. Pleas */
        /* @SVD:field=FIXLAT,bitOffset=31,bitWidth=1,description="Indicate PSRAM is fixed latency or variable latency. It must be compatible to the configuration in PSRAM registers.\n              Recommend always set to 1.\n              0: variable latency\n              1: fixed latency" */
        volatile uint32_t FIXLAT: 1;  /* Indicate PSRAM is fixed latency or variable latency. It must */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_DCR_Type;

/* @SVD:register=PSCLR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Prescaler Register" */
/* PSCLR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DIV,bitOffset=0,bitWidth=8,description="Prescaler divider.\n              0: MCLK = FCLK/1\n              1: MCLK = FCLK/1\n              2: MCLK = FCLK/2\n              n: MCLK = FCLK/n\n              Note: FLASH clock = MCLK. E.g. FCLK=192M and DIV=2, then FLASH clock = MCLK = 192/2 = 96MHz\n              PSRAM clock = MCLK/2. E.g. FCLK=240M and DIV=1, then PSRAM clock = MCLK/2 = 240/2 = 120MHz" */
        volatile uint32_t DIV: 8;  /* Prescaler divider.               0: MCLK = FCLK/1            */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_PSCLR_Type;

/* @SVD:register=SR,offset=0x0010,size=32,access=read-write,reset=0x00000006,description="Status Register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCF,bitOffset=0,bitWidth=1,description="Transfer complete flag" */
        volatile uint32_t TCF: 1;  /* Transfer complete flag */
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SMF,bitOffset=3,bitWidth=1,description="Status match flag in Polling Mode" */
        volatile uint32_t SMF: 1;  /* Status match flag in Polling Mode */
        /* @SVD:field=CSVF,bitOffset=4,bitWidth=1,description="CS max violation flag" */
        volatile uint32_t CSVF: 1;  /* CS max violation flag */
        /* @SVD:field=RBXF,bitOffset=5,bitWidth=1,description="Row boundary crossing flag" */
        volatile uint32_t RBXF: 1;  /* Row boundary crossing flag */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
        /* @SVD:field=BUSY,bitOffset=31,bitWidth=1,description="For debug purpose only" */
        volatile uint32_t BUSY: 1;  /* For debug purpose only */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_SR_Type;

/* @SVD:register=SCR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Status Clear Register" */
/* SCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCFC,bitOffset=0,bitWidth=1,description="Write 1 to clear TCF" */
        volatile uint32_t TCFC: 1;  /* Write 1 to clear TCF */
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SMFC,bitOffset=3,bitWidth=1,description="Write 1 to clear SMF" */
        volatile uint32_t SMFC: 1;  /* Write 1 to clear SMF */
        /* @SVD:field=CSVFC,bitOffset=4,bitWidth=1,description="Write 1 to clear CSVF" */
        volatile uint32_t CSVFC: 1;  /* Write 1 to clear CSVF */
        /* @SVD:field=RBXFC,bitOffset=5,bitWidth=1,description="Write 1 to clear RBXF" */
        volatile uint32_t RBXFC: 1;  /* Write 1 to clear RBXF */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_SCR_Type;

/* @SVD:register=CMDR1,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="Command Register" */
/* CMDR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMD,bitOffset=0,bitWidth=8,description="Command. Write to this register will trigger the sequence specified in CCR1" */
        volatile uint32_t CMD: 8;  /* Command. Write to this register will trigger the sequence sp */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CMDR1_Type;

/* @SVD:register=AR1,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="Address Register" */
/* AR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="Address" */
        volatile uint32_t ADDR: 32;  /* Address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_AR1_Type;

/* @SVD:register=ABR1,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Alternate Byte Register" */
/* ABR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ABYTE,bitOffset=0,bitWidth=32,description="Alternate byte" */
        volatile uint32_t ABYTE: 32;  /* Alternate byte */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_ABR1_Type;

/* @SVD:register=DLR1,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Data Length Register" */
/* DLR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DLEN,bitOffset=0,bitWidth=20,description="Data length\n              0: one byte\n              1: two bytes\n              …\n              n: (n+1) bytes" */
        volatile uint32_t DLEN: 20;  /* Data length               0: one byte               1: two b */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_DLR1_Type;

/* @SVD:register=CCR1,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Communication Configuration Register" */
/* CCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IMODE,bitOffset=0,bitWidth=3,description="Instruction mode\n              0: no instruction phase\n              1: single line\n              2: dual lines\n              3: quad lines\n              4/5/6 - reserved\n              7 - quad lines DDR" */
        volatile uint32_t IMODE: 3;  /* Instruction mode               0: no instruction phase       */
        /* @SVD:field=ADMODE,bitOffset=3,bitWidth=3,description="Address mode\n              0: no address phase\n              1: single line\n              2: dual line\n              3: quad line\n              4/5/6: reserved\n              7: quad line DDR" */
        volatile uint32_t ADMODE: 3;  /* Address mode               0: no address phase               */
        /* @SVD:field=ADSIZE,bitOffset=6,bitWidth=2,description="Address size\n              0: one byte\n              1: two bytes\n              2: three bytes\n              3: four bytes" */
        volatile uint32_t ADSIZE: 2;  /* Address size               0: one byte               1: two  */
        /* @SVD:field=ABMODE,bitOffset=8,bitWidth=3,description="Alternate byte mode\n              0: no alternate byte\n              1: single line\n              2: dual lines\n              3: quad lines\n              4/5/6: reserved\n              7: quad lines DDR" */
        volatile uint32_t ABMODE: 3;  /* Alternate byte mode               0: no alternate byte       */
        /* @SVD:field=ABSIZE,bitOffset=11,bitWidth=2,description="Alternate byte size\n              0: one byte\n              1: two bytes\n              2: three bytes\n              3: four bytes" */
        volatile uint32_t ABSIZE: 2;  /* Alternate byte size               0: one byte                */
        /* @SVD:field=DCYC,bitOffset=13,bitWidth=5,description="Number of dummy cycles\n              0: no dummy cycle\n              1: one dummy cycle\n              2: two dummy cycles" */
        volatile uint32_t DCYC: 5;  /* Number of dummy cycles               0: no dummy cycle       */
        /* @SVD:field=DMODE,bitOffset=18,bitWidth=3,description="Data Mode\n              0: no data phase\n              1: single line\n              2: dual lines\n              3: quad lines\n              4/5/6: reserved\n              7: quad lines DDR" */
        volatile uint32_t DMODE: 3;  /* Data Mode               0: no data phase               1: si */
        /* @SVD:field=FMODE,bitOffset=21,bitWidth=1,description="Function Mode\n              0: read mode\n              1: write mode" */
        volatile uint32_t FMODE: 1;  /* Function Mode               0: read mode               1: wr */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CCR1_Type;

/* @SVD:register=CMDR2,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Command Register" */
/* CMDR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMD,bitOffset=0,bitWidth=8,description="Command 2. If CMD2E is enabled, the CMD2 sequence will be issued after CMD1 as specified in CCR2\n              Note: CMD2 sequence cannot be issue individually" */
        volatile uint32_t CMD: 8;  /* Command 2. If CMD2E is enabled, the CMD2 sequence will be is */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CMDR2_Type;

/* @SVD:register=AR2,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Address Register" */
/* AR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="Address byte in CMD2 sequence" */
        volatile uint32_t ADDR: 32;  /* Address byte in CMD2 sequence */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_AR2_Type;

/* @SVD:register=ABR2,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Alternate Byte Register" */
/* ABR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ABYTE,bitOffset=0,bitWidth=32,description="Alternate byte in CMD2 sequence" */
        volatile uint32_t ABYTE: 32;  /* Alternate byte in CMD2 sequence */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_ABR2_Type;

/* @SVD:register=DLR2,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="Data Length Register" */
/* DLR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DLEN,bitOffset=0,bitWidth=20,description="Data length in CMD2 sequence" */
        volatile uint32_t DLEN: 20;  /* Data length in CMD2 sequence */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_DLR2_Type;

/* @SVD:register=CCR2,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="Communication Configuration Register" */
/* CCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IMODE,bitOffset=0,bitWidth=3,description="This register specifies the format of CMD2 sequence. Refer to CCR1 description" */
        volatile uint32_t IMODE: 3;  /* This register specifies the format of CMD2 sequence. Refer t */
        /* @SVD:field=ADMODE,bitOffset=3,bitWidth=3,description="" */
        volatile uint32_t ADMODE: 3;
        /* @SVD:field=ADSIZE,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t ADSIZE: 2;
        /* @SVD:field=ABMODE,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t ABMODE: 3;
        /* @SVD:field=ABSIZE,bitOffset=11,bitWidth=2,description="" */
        volatile uint32_t ABSIZE: 2;
        /* @SVD:field=DCYC,bitOffset=13,bitWidth=5,description="" */
        volatile uint32_t DCYC: 5;
        /* @SVD:field=DMODE,bitOffset=18,bitWidth=3,description="" */
        volatile uint32_t DMODE: 3;
        /* @SVD:field=FMODE,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t FMODE: 1;
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CCR2_Type;

/* @SVD:register=HCMDR,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="AHB Command Register" */
/* HCMDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RCMD,bitOffset=0,bitWidth=8,description="AHB read command. During XIP, the AHB read transaction will be translated into this Read Command on memory interface" */
        volatile uint32_t RCMD: 8;  /* AHB read command. During XIP, the AHB read transaction will  */
        /* @SVD:field=WCMD,bitOffset=8,bitWidth=8,description="AHB write command. During XIP, the AHB write transaction will be translated into this Write Command on memory interface" */
        volatile uint32_t WCMD: 8;  /* AHB write command. During XIP, the AHB write transaction wil */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_HCMDR_Type;

/* @SVD:register=HRABR,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="AHB Read Alternate Byte Register" */
/* HRABR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ABYTE,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t ABYTE: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_HRABR_Type;

/* @SVD:register=HRCCR,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="AHB Read Communication Configuration Register" */
/* HRCCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IMODE,bitOffset=0,bitWidth=3,description="This register specifies the format of AHB read command sequence. Refer to CCR1 description" */
        volatile uint32_t IMODE: 3;  /* This register specifies the format of AHB read command seque */
        /* @SVD:field=ADMODE,bitOffset=3,bitWidth=3,description="" */
        volatile uint32_t ADMODE: 3;
        /* @SVD:field=ADSIZE,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t ADSIZE: 2;
        /* @SVD:field=ABMODE,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t ABMODE: 3;
        /* @SVD:field=ABSIZE,bitOffset=11,bitWidth=2,description="" */
        volatile uint32_t ABSIZE: 2;
        /* @SVD:field=DCYC,bitOffset=13,bitWidth=5,description="" */
        volatile uint32_t DCYC: 5;
        /* @SVD:field=DMODE,bitOffset=18,bitWidth=3,description="" */
        volatile uint32_t DMODE: 3;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_HRCCR_Type;

/* @SVD:register=HWABR,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="AHB Write Alternate Byte Register" */
/* HWABR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ABYTE,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t ABYTE: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_HWABR_Type;

/* @SVD:register=HWCCR,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="AHB Write Communication Configuration Register" */
/* HWCCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IMODE,bitOffset=0,bitWidth=3,description="This register specifies the format of AHB write command sequence. Refer to CCR1 description" */
        volatile uint32_t IMODE: 3;  /* This register specifies the format of AHB write command sequ */
        /* @SVD:field=ADMODE,bitOffset=3,bitWidth=3,description="" */
        volatile uint32_t ADMODE: 3;
        /* @SVD:field=ADSIZE,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t ADSIZE: 2;
        /* @SVD:field=ABMODE,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t ABMODE: 3;
        /* @SVD:field=ABSIZE,bitOffset=11,bitWidth=2,description="" */
        volatile uint32_t ABSIZE: 2;
        /* @SVD:field=DCYC,bitOffset=13,bitWidth=5,description="" */
        volatile uint32_t DCYC: 5;
        /* @SVD:field=DMODE,bitOffset=18,bitWidth=3,description="" */
        volatile uint32_t DMODE: 3;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_HWCCR_Type;

/* @SVD:register=FIFOCR,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="FIFO Control Register" */
/* FIFOCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RXCLR,bitOffset=0,bitWidth=1,description="write 1 to clear Rx FIFO" */
        volatile uint32_t RXCLR: 1;  /* write 1 to clear Rx FIFO */
        /* @SVD:field=RXE,bitOffset=1,bitWidth=1,description="Rx FIFO empty" */
        volatile uint32_t RXE: 1;  /* Rx FIFO empty */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=TXCLR,bitOffset=8,bitWidth=1,description="write 1 to clear Tx FIFO" */
        volatile uint32_t TXCLR: 1;  /* write 1 to clear Tx FIFO */
        /* @SVD:field=TXF,bitOffset=9,bitWidth=1,description="Tx FIFO full flag" */
        volatile uint32_t TXF: 1;  /* Tx FIFO full flag */
        /* @SVD:field=TXSLOTS,bitOffset=10,bitWidth=5,description="When DMA enabled, asserts DMA reqeust if TXFIFO vacant slots is greater than or equal to TXSLOTS.\n              Note: this field should be set in accordance to the burst length in DMA. For example, if DMA employs BURST8 transction, then this filed is set to 8" */
        volatile uint32_t TXSLOTS: 5;  /* When DMA enabled, asserts DMA reqeust if TXFIFO vacant slots */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_FIFOCR_Type;

/* @SVD:register=MISCR,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="Miscelaneous Register" */
/* MISCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RXCLKDLY,bitOffset=0,bitWidth=8,description="Add delay on internal Rx clock  to fine tune the sampling position. Note: effective 5-bit" */
        volatile uint32_t RXCLKDLY: 8;  /* Add delay on internal Rx clock  to fine tune the sampling po */
        /* @SVD:field=SCKDLY,bitOffset=8,bitWidth=8,description="Add delay on output clock to fine tune the clock position. Note: effective 7-bit" */
        volatile uint32_t SCKDLY: 8;  /* Add delay on output clock to fine tune the clock position. N */
        /* @SVD:field=DQSDLY,bitOffset=16,bitWidth=8,description="Delay the input DQS signal to the appropriate sampling position.  For device w/ DQS signal only. Note: effective 7-bit" */
        volatile uint32_t DQSDLY: 8;  /* Delay the input DQS signal to the appropriate sampling posit */
        /* @SVD:field=RXCLKINV,bitOffset=24,bitWidth=1,description="Invert internal Rx clock to add half-cycle delay (coarse tune) when sampling data. It is usually used for FLASH device w/ higher frequency." */
        volatile uint32_t RXCLKINV: 1;  /* Invert internal Rx clock to add half-cycle delay (coarse tun */
        /* @SVD:field=SCKINV,bitOffset=25,bitWidth=1,description="Invert output clock. This bit is used to align (coarse tune) the output clock to the center of output data." */
        volatile uint32_t SCKINV: 1;  /* Invert output clock. This bit is used to align (coarse tune) */
        /* @SVD:field=DTRPRE,bitOffset=26,bitWidth=1,description="Enable pre-sampling for DTRReserved-Do not modify" */
        volatile uint32_t DTRPRE: 1;  /* Enable pre-sampling for DTRReserved-Do not modify */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
        /* @SVD:field=DBGSEL,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t DBGSEL: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_MISCR_Type;

/* @SVD:register=CTRSAR,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="CTR Starting Address Register" */
/* CTRSAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=SA,bitOffset=10,bitWidth=22,description="Starting address of the AES decryption area. Since the lowest 10 bits are zero, the address is always 1KB aligned.\n              Together with CTREAR, the total area is [CTRSAR, CTREAR)\n              For example, CTRSAR = 32'h0, CTREAR = 32'h200000, then the on-the-fly decryption area is 0x0 - 0x1FFFFF" */
        volatile uint32_t SA: 22;  /* Starting address of the AES decryption area. Since the lowes */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CTRSAR_Type;

/* @SVD:register=CTREAR,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="CTR Ending Address Register" */
/* CTREAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=EA,bitOffset=10,bitWidth=22,description="Ending address of the AES decryption area" */
        volatile uint32_t EA: 22;  /* Ending address of the AES decryption area */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CTREAR_Type;

/* @SVD:register=NONCEA,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="Nonce A Register" */
/* NONCEA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NONCEA,bitOffset=0,bitWidth=32,description="Used for on-the-fly decryption" */
        volatile uint32_t NONCEA: 32;  /* Used for on-the-fly decryption */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_NONCEA_Type;

/* @SVD:register=NONCEB,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="Nonce B Register" */
/* NONCEB 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NONCEB,bitOffset=0,bitWidth=32,description="Used for on-the-fly decryption" */
        volatile uint32_t NONCEB: 32;  /* Used for on-the-fly decryption */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_NONCEB_Type;

/* @SVD:register=AASAR,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="Address Aliasing Start Address Register" */
/* AASAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=SA,bitOffset=10,bitWidth=22,description="Starting address of the address aliasing area. Always 1KB aligned.Together with AAEAR, the aliasing area is [AASAR, AAEAR). \n              If the address falls into this area, an offset AAOAR is added and the aliased address will be used to access external memory" */
        volatile uint32_t SA: 22;  /* Starting address of the address aliasing area. Always 1KB al */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_AASAR_Type;

/* @SVD:register=AAEAR,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="Address Aliasing Ending Address Register" */
/* AAEAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=EA,bitOffset=10,bitWidth=22,description="Ending address of the address aliasing area" */
        volatile uint32_t EA: 22;  /* Ending address of the address aliasing area */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_AAEAR_Type;

/* @SVD:register=AAOAR,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="Address Aliasing Offset Address Register" */
/* AAOAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=OA,bitOffset=10,bitWidth=22,description="The offset to be added to the original address" */
        volatile uint32_t OA: 22;  /* The offset to be added to the original address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_AAOAR_Type;

/* @SVD:register=CIR,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="Command Interval Register" */
/* CIR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INTERVAL1,bitOffset=0,bitWidth=16,description="The interval between CMD1 itself. The unit is in MCLK cycles" */
        volatile uint32_t INTERVAL1: 16;  /* The interval between CMD1 itself. The unit is in MCLK cycles */
        /* @SVD:field=INTERVAL2,bitOffset=16,bitWidth=16,description="The interval between CMD1 and CMD2 (or between CMD2 itself) if CMD2E is enabled. The unit is in MCLK cycles" */
        volatile uint32_t INTERVAL2: 16;  /* The interval between CMD1 and CMD2 (or between CMD2 itself)  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CIR_Type;

/* @SVD:register=SMR,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="Status Match Register" */
/* SMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=STATUS,bitOffset=0,bitWidth=32,description="If status match is enabled, this register is compared with the data read from external memory.\n              Together with SMKR, only the bits with mask=1 will be considered to compare in AND or OR mode as configured in SMM field." */
        volatile uint32_t STATUS: 32;  /* If status match is enabled, this register is compared with t */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_SMR_Type;

/* @SVD:register=SMKR,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="Status Mask Register" */
/* SMKR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MASK,bitOffset=0,bitWidth=32,description="Status mask\n              0: the corresponding bit is not considered to compare\n              1: the corresponding bit is considered to compare" */
        volatile uint32_t MASK: 32;  /* Status mask               0: the corresponding bit is not co */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_SMKR_Type;

/* @SVD:register=TIMR,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="Timer Register" */
/* TIMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TIMEOUT,bitOffset=0,bitWidth=16,description="After the transaction is complete, CS remains low for multiple cycles of MCLK as specified by this register.\n              For example if TIMEOUT=n, CS remains active for n cycles, during which if a new transaction occurs and the address is consecutive, the memory access can be resumed w/o sending the command and address again." */
        volatile uint32_t TIMEOUT: 16;  /* After the transaction is complete, CS remains low for multip */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_TIMR_Type;

/* @SVD:register=WDTR,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="WDT Register" */
/* WDTR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TIMEOUT,bitOffset=0,bitWidth=16,description="Set timeout value in number of clk_wdt cycles" */
        volatile uint32_t TIMEOUT: 16;  /* Set timeout value in number of clk_wdt cycles */
        /* @SVD:field=EN,bitOffset=16,bitWidth=1,description="WDT enable. This watchdog is on AHB side such that bus access will not hang in exceptional cases" */
        volatile uint32_t EN: 1;  /* WDT enable. This watchdog is on AHB side such that bus acces */
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
        /* @SVD:field=TOF,bitOffset=31,bitWidth=1,description="Timeout flag. Self cleared when HREADYOUT becomes ready" */
        volatile uint32_t TOF: 1;  /* Timeout flag. Self cleared when HREADYOUT becomes ready */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_WDTR_Type;

/* @SVD:register=PRSAR,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="Prefetch Starting Address Register" */
/* PRSAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=SA,bitOffset=10,bitWidth=22,description="Starting address of the prefetch area\n              If prefetch is enabled and the read address falls into [PRSAR, PREAR), controller will prefetch the following data" */
        volatile uint32_t SA: 22;  /* Starting address of the prefetch area               If prefe */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_PRSAR_Type;

/* @SVD:register=PREAR,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="Prefetch Ending Address Register" */
/* PREAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD,bitOffset=0,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
        /* @SVD:field=EA,bitOffset=10,bitWidth=22,description="Ending address of the prefetch area" */
        volatile uint32_t EA: 22;  /* Ending address of the prefetch area */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_PREAR_Type;

/* @SVD:register=CALCR,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="Calibration Clock Register" */
/* CALCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DELAY,bitOffset=0,bitWidth=8,description="calibration delay result" */
        volatile uint32_t DELAY: 8;  /* calibration delay result */
        /* @SVD:field=DONE,bitOffset=8,bitWidth=1,description="calibration done flag" */
        volatile uint32_t DONE: 1;  /* calibration done flag */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
        /* @SVD:field=EN,bitOffset=31,bitWidth=1,description="calibration enable" */
        volatile uint32_t EN: 1;  /* calibration enable */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CALCR_Type;

/* @SVD:register=APM32CR,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="APM32 Control Register" */
/* APM32CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCPHR,bitOffset=0,bitWidth=4,description="For special use by AP 32Mb PSRAM.Reserved-Do not modify" */
        volatile uint32_t TCPHR: 4;  /* For special use by AP 32Mb PSRAM.Reserved-Do not modify */
        /* @SVD:field=TCPHW,bitOffset=4,bitWidth=4,description="For special use by AP 32Mb PSRAM.Reserved-Do not modify" */
        volatile uint32_t TCPHW: 4;  /* For special use by AP 32Mb PSRAM.Reserved-Do not modify */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_APM32CR_Type;

/* @SVD:register=CR2,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="Control Register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LOOP,bitOffset=0,bitWidth=8,description="Repeat CMD1->CMD2 sequence for n times. This filed is only valid when CMD2E=1 and SME2=0.\n              For example if LOOP=0, then the sequence is CMD1 -> CMD2.\n              If LOOP=2, then the sequence is (CMD1->CMD2) -> (CMD1->CMD2) -> (CMD1->CMD2)" */
        volatile uint32_t LOOP: 8;  /* Repeat CMD1->CMD2 sequence for n times. This filed is only v */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} MPI2_CR2_Type;

/* MPI2 寄存器结构体 */
typedef struct
{
    MPI2_CR_Type CR;  /* 0x0000 - Control Register */
    MPI2_DR_Type DR;  /* 0x0004 - Data Register */
    MPI2_DCR_Type DCR;  /* 0x0008 - Device Control Register */
    MPI2_PSCLR_Type PSCLR;  /* 0x000C - Prescaler Register */
    MPI2_SR_Type SR;  /* 0x0010 - Status Register */
    MPI2_SCR_Type SCR;  /* 0x0014 - Status Clear Register */
    MPI2_CMDR1_Type CMDR1;  /* 0x0018 - Command Register */
    MPI2_AR1_Type AR1;  /* 0x001C - Address Register */
    MPI2_ABR1_Type ABR1;  /* 0x0020 - Alternate Byte Register */
    MPI2_DLR1_Type DLR1;  /* 0x0024 - Data Length Register */
    MPI2_CCR1_Type CCR1;  /* 0x0028 - Communication Configuration Register */
    MPI2_CMDR2_Type CMDR2;  /* 0x002C - Command Register */
    MPI2_AR2_Type AR2;  /* 0x0030 - Address Register */
    MPI2_ABR2_Type ABR2;  /* 0x0034 - Alternate Byte Register */
    MPI2_DLR2_Type DLR2;  /* 0x0038 - Data Length Register */
    MPI2_CCR2_Type CCR2;  /* 0x003C - Communication Configuration Register */
    MPI2_HCMDR_Type HCMDR;  /* 0x0040 - AHB Command Register */
    MPI2_HRABR_Type HRABR;  /* 0x0044 - AHB Read Alternate Byte Register */
    MPI2_HRCCR_Type HRCCR;  /* 0x0048 - AHB Read Communication Configuration Reg */
    MPI2_HWABR_Type HWABR;  /* 0x004C - AHB Write Alternate Byte Register */
    MPI2_HWCCR_Type HWCCR;  /* 0x0050 - AHB Write Communication Configuration Re */
    MPI2_FIFOCR_Type FIFOCR;  /* 0x0054 - FIFO Control Register */
    MPI2_MISCR_Type MISCR;  /* 0x0058 - Miscelaneous Register */
    MPI2_CTRSAR_Type CTRSAR;  /* 0x005C - CTR Starting Address Register */
    MPI2_CTREAR_Type CTREAR;  /* 0x0060 - CTR Ending Address Register */
    MPI2_NONCEA_Type NONCEA;  /* 0x0064 - Nonce A Register */
    MPI2_NONCEB_Type NONCEB;  /* 0x0068 - Nonce B Register */
    MPI2_AASAR_Type AASAR;  /* 0x006C - Address Aliasing Start Address Register */
    MPI2_AAEAR_Type AAEAR;  /* 0x0070 - Address Aliasing Ending Address Register */
    MPI2_AAOAR_Type AAOAR;  /* 0x0074 - Address Aliasing Offset Address Register */
    MPI2_CIR_Type CIR;  /* 0x0078 - Command Interval Register */
    MPI2_SMR_Type SMR;  /* 0x007C - Status Match Register */
    MPI2_SMKR_Type SMKR;  /* 0x0080 - Status Mask Register */
    MPI2_TIMR_Type TIMR;  /* 0x0084 - Timer Register */
    MPI2_WDTR_Type WDTR;  /* 0x0088 - WDT Register */
    MPI2_PRSAR_Type PRSAR;  /* 0x008C - Prefetch Starting Address Register */
    MPI2_PREAR_Type PREAR;  /* 0x0090 - Prefetch Ending Address Register */
    MPI2_CALCR_Type CALCR;  /* 0x0094 - Calibration Clock Register */
    /* @SVD:register=RSVD1,offset=0x0098,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0098 - RSVD1 */
    MPI2_APM32CR_Type APM32CR;  /* 0x009C - APM32 Control Register */
    MPI2_CR2_Type CR2;  /* 0x00A0 - Control Register 2 */
} MPI2_TypeDef;

/* ============================================================================ */
/* SDMMC1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=SDMMC1,base=0x50045000,description="",groupName=SDMMC */

#define SDMMC1_BASE                                        (0x50045000UL)

/* @SVD:register=SR,offset=0x0000,size=32,access=read-write,reset=0x0,description="command and data status register " */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMD_BUSY,bitOffset=0,bitWidth=1,description="Command busy\n              1: busy, and when busy, start TX command bit is no usage and should not modify the relative register\n              0: command idle" */
        volatile uint32_t CMD_BUSY: 1;  /* Command busy               1: busy, and when busy, start TX  */
        /* @SVD:field=CMD_DONE,bitOffset=1,bitWidth=1,description="Command done \n              Read 1: transfer command done, and start a new transfer will take the bit into 0\n              Read 0: command transferring or idle\n              Write 1: clear the bit \n              Write 0: no any influence to the bit" */
        volatile uint32_t CMD_DONE: 1;  /* Command done                Read 1: transfer command done, a */
        /* @SVD:field=CMD_RSP_CRC,bitOffset=2,bitWidth=1,description="Command response CRC error status \n              Read 1: response CRC error\n              Read 0: response CRC right\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t CMD_RSP_CRC: 1;  /* Command response CRC error status                Read 1: res */
        /* @SVD:field=CMD_TIMEOUT,bitOffset=3,bitWidth=1,description="Command timeout (response timeout) \n              Read 1: timeout\n              Read 0: no timeout\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t CMD_TIMEOUT: 1;  /* Command timeout (response timeout)                Read 1: ti */
        /* @SVD:field=DATA_BUSY,bitOffset=4,bitWidth=1,description="Transfer Data busy \n              1: busy, and when busy, start transfer data bit is no usage and you should not modify the relative register. If want to do this, first disable transfer data enable bit, then the busy bit will be back to 0, and this transfer will also be cancelled.\n              0: data idle" */
        volatile uint32_t DATA_BUSY: 1;  /* Transfer Data busy                1: busy, and when busy, st */
        /* @SVD:field=DATA_DONE,bitOffset=5,bitWidth=1,description="Data transfer done \n              Read 1: transfer data done, and start a new transfer will take the bit into 0\n              Read 0: data transferring or idle\n              Write 1: clear the bit \n              Write 0: no any influence to the bit" */
        volatile uint32_t DATA_DONE: 1;  /* Data transfer done                Read 1: transfer data done */
        /* @SVD:field=DATA_CRC,bitOffset=6,bitWidth=1,description="Data CRC error \n              Read 1: data CRC error\n              Read 0: data CRC right\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t DATA_CRC: 1;  /* Data CRC error                Read 1: data CRC error         */
        /* @SVD:field=DATA_TIMEOUT,bitOffset=7,bitWidth=1,description="Data timeout \n              Read 1: timeout\n              Read 0: no timeout\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t DATA_TIMEOUT: 1;  /* Data timeout                Read 1: timeout               Re */
        /* @SVD:field=STARTBIT_ERROR,bitOffset=8,bitWidth=1,description="Wide bus start bits error \n              Didn't detect all start bits in data bus\n              Read 1: start bits error\n              Read 0: no start bits error\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t STARTBIT_ERROR: 1;  /* Wide bus start bits error                Didn't detect all s */
        /* @SVD:field=FIFO_UNDERRUN,bitOffset=9,bitWidth=1,description="FIFO underrun \n              Read 1: FIFO underrun error\n              Read 0: no FIFO underrun error\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t FIFO_UNDERRUN: 1;  /* FIFO underrun                Read 1: FIFO underrun error     */
        /* @SVD:field=FIFO_OVERRUN,bitOffset=10,bitWidth=1,description="FIFO overrun \n              Read 1: FIFO overrun error\n              Read 0: no FIFO overrun error\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t FIFO_OVERRUN: 1;  /* FIFO overrun                Read 1: FIFO overrun error       */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CMD_SENT,bitOffset=12,bitWidth=1,description="Command sent (perhaps no response back yet) \n              Read 1: command sent. When command start bit is set, the bit will also be back to 0\n              Read 0: command transferring or others\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t CMD_SENT: 1;  /* Command sent (perhaps no response back yet)                R */
        /* @SVD:field=CARD_INSERT,bitOffset=13,bitWidth=1,description="Detect card inserted \n              Read 1: detect card inserted. When detect card removed bit is set, the bit will also be back to 0\n              Read 0:  no meaning\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t CARD_INSERT: 1;  /* Detect card inserted                Read 1: detect card inse */
        /* @SVD:field=CARD_REMOVE,bitOffset=14,bitWidth=1,description="Detect card removed\n              Read 1: detect card removed. When detect card inserted bit is set, the bit will also be back to 0\n              Read 0:  no meaning\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t CARD_REMOVE: 1;  /* Detect card removed               Read 1: detect card remove */
        /* @SVD:field=CARD_EXIST,bitOffset=15,bitWidth=1,description="Card exist status\n              Read 1: card exist\n              Read 0: no card exist\n              This bit will be valid after enable detect card." */
        volatile uint32_t CARD_EXIST: 1;  /* Card exist status               Read 1: card exist           */
        /* @SVD:field=SDIO,bitOffset=16,bitWidth=1,description="Detect SDIO Card Interrupt\n              Read 1: detect sdio card generating interrupt\n              Read 0: no interrupt\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t SDIO: 1;  /* Detect SDIO Card Interrupt               Read 1: detect sdio */
        /* @SVD:field=CACHE_ERR,bitOffset=17,bitWidth=1,description="Detect cache error\n              Read 1: cache error occur\n              Read 0: no cache error\n              Write 1: clear the bit\n              Write 0: no any influence to the bit" */
        volatile uint32_t CACHE_ERR: 1;  /* Detect cache error               Read 1: cache error occur   */
        /* @SVD:field=RSVD,bitOffset=18,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_SR_Type;

/* @SVD:register=CCR,offset=0x0004,size=32,access=read-write,reset=0x0,description="command control register" */
/* CCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMD_START,bitOffset=0,bitWidth=1,description="Command start\n              write 1 to start command TX, and when begin to TX command, the bit will return into 0." */
        volatile uint32_t CMD_START: 1;  /* Command start               write 1 to start command TX, and */
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD3: 7;
        /* @SVD:field=CMD_TX_EN,bitOffset=8,bitWidth=1,description="TX command enable\n              1: enable TX command\n              0: disable TX command" */
        volatile uint32_t CMD_TX_EN: 1;  /* TX command enable               1: enable TX command         */
        /* @SVD:field=CMD_PEND,bitOffset=9,bitWidth=1,description="Command pending enable\n              When prepare to send stop command, this bit should be set. Controller will calculate a proper time point to send out the command to guarantee all the data have been transferred. And this is mainly used in stream mode.\n              Recommend using set_block_count (SD/MMC basis command) to control transferring data for block mode.\n              If send stop command for canceling this transfer (such as CRC error in multi-block), no need to set the bit." */
        volatile uint32_t CMD_PEND: 1;  /* Command pending enable               When prepare to send st */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=CMD_HAS_RSP,bitOffset=16,bitWidth=1,description="1: Response expected after command\n              0: No response expected after command" */
        volatile uint32_t CMD_HAS_RSP: 1;  /* 1: Response expected after command               0: No respo */
        /* @SVD:field=CMD_LONG_RSP,bitOffset=17,bitWidth=1,description="1: Response will be 136-bit, long response\n              0: Response will be 48-bit, normal response" */
        volatile uint32_t CMD_LONG_RSP: 1;  /* 1: Response will be 136-bit, long response               0:  */
        /* @SVD:field=CMD_INDEX,bitOffset=18,bitWidth=6,description="Command index" */
        volatile uint32_t CMD_INDEX: 6;  /* Command index */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CCR_Type;

/* @SVD:register=CAR,offset=0x0008,size=32,access=read-write,reset=0x0,description="command argument register" */
/* CAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMD_ARG,bitOffset=0,bitWidth=32,description="Command argument" */
        volatile uint32_t CMD_ARG: 32;  /* Command argument */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CAR_Type;

/* @SVD:register=RIR,offset=0x000C,size=32,access=read-write,reset=0x0,description="response command index register" */
/* RIR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSP_INDEX,bitOffset=0,bitWidth=6,description="Response command index" */
        volatile uint32_t RSP_INDEX: 6;  /* Response command index */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_RIR_Type;

/* @SVD:register=RAR1,offset=0x0010,size=32,access=read-write,reset=0x0,description="response command argument1 register" */
/* RAR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSP_ARG1,bitOffset=0,bitWidth=32,description="Response command content\n              If long response, it is rsp_arg[39:8]" */
        volatile uint32_t RSP_ARG1: 32;  /* Response command content               If long response, it  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_RAR1_Type;

/* @SVD:register=RAR2,offset=0x0014,size=32,access=read-write,reset=0x0,description="response command argument2 register" */
/* RAR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSP_ARG2,bitOffset=0,bitWidth=32,description="Long response, it is rsp_arg[71:40]" */
        volatile uint32_t RSP_ARG2: 32;  /* Long response, it is rsp_arg[71:40] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_RAR2_Type;

/* @SVD:register=RAR3,offset=0x0018,size=32,access=read-write,reset=0x0,description="response command argument3 register" */
/* RAR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSP_ARG3,bitOffset=0,bitWidth=32,description="Long response, it is rsp_arg[103:72]" */
        volatile uint32_t RSP_ARG3: 32;  /* Long response, it is rsp_arg[103:72] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_RAR3_Type;

/* @SVD:register=RAR4,offset=0x001C,size=32,access=read-write,reset=0x0,description="response command argument4 register" */
/* RAR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSP_ARG4,bitOffset=0,bitWidth=24,description="Long response, it is rsp_arg[127:104]" */
        volatile uint32_t RSP_ARG4: 24;  /* Long response, it is rsp_arg[127:104] */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_RAR4_Type;

/* @SVD:register=TOR,offset=0x0020,size=32,access=read-write,reset=0x0,description="timeout count register" */
/* TOR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TIMEOUT_CNT,bitOffset=0,bitWidth=32,description="Used to determine how much time waiting response or data bus busy is timeout, and decreased under card clock.\n              Set to 400000 for 1s timeout if interface clock is 400KHz." */
        volatile uint32_t TIMEOUT_CNT: 32;  /* Used to determine how much time waiting response or data bus */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_TOR_Type;

/* @SVD:register=DCR,offset=0x0024,size=32,access=read-write,reset=0x0,description="data control register" */
/* DCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA_START,bitOffset=0,bitWidth=1,description="Start transfer data\n              set 1 to let the controller begin to transfer data (in fact, go into wait write or wait read state). After begin to transfer, this bit will be back to 0." */
        volatile uint32_t DATA_START: 1;  /* Start transfer data               set 1 to let the controlle */
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD3: 7;
        /* @SVD:field=TRAN_DATA_EN,bitOffset=8,bitWidth=1,description="Transfer data enable\n              0: disable transfer data. After disable data transfer, stop command should be sent to card\n              1: enable data transfer" */
        volatile uint32_t TRAN_DATA_EN: 1;  /* Transfer data enable               0: disable transfer data. */
        /* @SVD:field=R_WN,bitOffset=9,bitWidth=1,description="Write or read\n              0: write data into card\n              1: read data from card" */
        volatile uint32_t R_WN: 1;  /* Write or read               0: write data into card          */
        /* @SVD:field=STREAM_MODE,bitOffset=10,bitWidth=1,description="Data transfer mode\n              0: block\n              1: stream" */
        volatile uint32_t STREAM_MODE: 1;  /* Data transfer mode               0: block               1: s */
        /* @SVD:field=WIRE_MODE,bitOffset=11,bitWidth=2,description="Wide data bus mode\n              00: 1 wire bus\n              01: 4 wires wide bus\n              1X: reserved" */
        volatile uint32_t WIRE_MODE: 2;  /* Wide data bus mode               00: 1 wire bus              */
        /* @SVD:field=RSVD2,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=BLOCK_SIZE,bitOffset=16,bitWidth=11,description="Data block size is block_size+1 (max 2048 bytes)\n              0: 1 byte\n              0x1ff: 512 bytes" */
        volatile uint32_t BLOCK_SIZE: 11;  /* Data block size is block_size+1 (max 2048 bytes)             */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_DCR_Type;

/* @SVD:register=DLR,offset=0x0028,size=32,access=read-write,reset=0x0,description="data length register" */
/* DLR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA_LEN,bitOffset=0,bitWidth=16,description="Data length value. The number of data bytes is data_len+1.\n              The number of data bytes should be a multiple of data block size.\n              0 is 1 byte. 0x1ff is 512 bytes. Max is 63.5KB." */
        volatile uint32_t DATA_LEN: 16;  /* Data length value. The number of data bytes is data_len+1.   */
        /* @SVD:field=BLOCK_TRAN_NUM,bitOffset=16,bitWidth=16,description="The number of blocks which have been transferred successfully\n              1 = 1 block transferred\n              It is cleared when start transfer data bit is set." */
        volatile uint32_t BLOCK_TRAN_NUM: 16;  /* The number of blocks which have been transferred successfull */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_DLR_Type;

/* @SVD:register=IER,offset=0x002C,size=32,access=read-write,reset=0x0,description="command and data interrupt mask register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD5,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=CMD_DONE_MASK,bitOffset=1,bitWidth=1,description="Command done bit mask for interrupt" */
        volatile uint32_t CMD_DONE_MASK: 1;  /* Command done bit mask for interrupt */
        /* @SVD:field=CMD_RSP_CRC_MASK,bitOffset=2,bitWidth=1,description="Command CRC error bit mask for interrupt" */
        volatile uint32_t CMD_RSP_CRC_MASK: 1;  /* Command CRC error bit mask for interrupt */
        /* @SVD:field=CMD_TIMEOUT_MASK,bitOffset=3,bitWidth=1,description="Command timeout bit mask for interrupt" */
        volatile uint32_t CMD_TIMEOUT_MASK: 1;  /* Command timeout bit mask for interrupt */
        /* @SVD:field=RSVD4,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=DATA_DONE_MASK,bitOffset=5,bitWidth=1,description="Data transfer done bit mask for interrupt" */
        volatile uint32_t DATA_DONE_MASK: 1;  /* Data transfer done bit mask for interrupt */
        /* @SVD:field=DATA_CRC_MASK,bitOffset=6,bitWidth=1,description="Data CRC error bit mask for interrupt" */
        volatile uint32_t DATA_CRC_MASK: 1;  /* Data CRC error bit mask for interrupt */
        /* @SVD:field=DATA_TIMEOUT_MASK,bitOffset=7,bitWidth=1,description="Data timeout bit mask for interrupt" */
        volatile uint32_t DATA_TIMEOUT_MASK: 1;  /* Data timeout bit mask for interrupt */
        /* @SVD:field=STARTBIT_ERROR_MASK,bitOffset=8,bitWidth=1,description="Wide bus start bits error bit mask for interrupt" */
        volatile uint32_t STARTBIT_ERROR_MASK: 1;  /* Wide bus start bits error bit mask for interrupt */
        /* @SVD:field=FIFO_UNDERRUN_MASK,bitOffset=9,bitWidth=1,description="FIFO underrun bit mask for interrupt" */
        volatile uint32_t FIFO_UNDERRUN_MASK: 1;  /* FIFO underrun bit mask for interrupt */
        /* @SVD:field=FIFO_OVERRUN_MASK,bitOffset=10,bitWidth=1,description="FIFO overrun bit mask for interrupt" */
        volatile uint32_t FIFO_OVERRUN_MASK: 1;  /* FIFO overrun bit mask for interrupt */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=CMD_SENT_MASK,bitOffset=12,bitWidth=1,description="Command sent mask for interrupt" */
        volatile uint32_t CMD_SENT_MASK: 1;  /* Command sent mask for interrupt */
        /* @SVD:field=CARD_INSERT_MASK,bitOffset=13,bitWidth=1,description="Detect card insert mask for interrupt" */
        volatile uint32_t CARD_INSERT_MASK: 1;  /* Detect card insert mask for interrupt */
        /* @SVD:field=CARD_REMOVE_MASK,bitOffset=14,bitWidth=1,description="Detect card remove mask for interrupt" */
        volatile uint32_t CARD_REMOVE_MASK: 1;  /* Detect card remove mask for interrupt */
        /* @SVD:field=RSVD2,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SDIO_MASK,bitOffset=16,bitWidth=1,description="Detect SDIO interrupt(data[1]) mask for interrupt" */
        volatile uint32_t SDIO_MASK: 1;  /* Detect SDIO interrupt(data[1]) mask for interrupt */
        /* @SVD:field=CACHE_ERR_MASK,bitOffset=17,bitWidth=1,description="cache error mask for interrupt" */
        volatile uint32_t CACHE_ERR_MASK: 1;  /* cache error mask for interrupt */
        /* @SVD:field=RSVD,bitOffset=18,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_IER_Type;

/* @SVD:register=CLKCR,offset=0x0030,size=32,access=read-write,reset=0x0,description="clock control register" */
/* CLKCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=STOP_CLK,bitOffset=0,bitWidth=1,description="Disable SD card clock\n              1: stop SD card clock\n              0: SD card clock generated" */
        volatile uint32_t STOP_CLK: 1;  /* Disable SD card clock               1: stop SD card clock    */
        /* @SVD:field=VOID_FIFO_ERROR,bitOffset=1,bitWidth=1,description="Void FIFO error\n              0: close the function\n              1: open the function\n              If open it, when FIFO will be overrun or underrun soon, the SD_CLK and the clock enable of this module will be closed, and wait to host to read or write FIFO.\n              Note: this function needs to be supported by card." */
        volatile uint32_t VOID_FIFO_ERROR: 1;  /* Void FIFO error               0: close the function          */
        /* @SVD:field=CLK_TUNE_SEL,bitOffset=2,bitWidth=2,description="select clock delay for rx sample \n              0: no delay\n              1: delay level 1 (~1.5ns typical)\n              2: delay level 2 (~3ns typical)\n              3: delay level 3 (~5ns typical)" */
        volatile uint32_t CLK_TUNE_SEL: 2;  /* select clock delay for rx sample                0: no delay  */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=DIV,bitOffset=8,bitWidth=13,description="Divide card clock counter. \n              0 is illegal.\n              sd_clock = hclk/(div + 1)\n              If hclk is 240M and div is 599, 400KHz SD clock will be generated." */
        volatile uint32_t DIV: 13;  /* Divide card clock counter.                0 is illegal.      */
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CLKCR_Type;

/* @SVD:register=CDR,offset=0x003C,size=32,access=read-write,reset=0x0,description="card interface control and card detect register" */
/* CDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SD_DATA3_CD,bitOffset=0,bitWidth=1,description="Use sd_data[3] to do card detect \n              0: use special pin to do card detect / write protect. (Currently not supported)\n              1: use sd_data[3] to do card detect (default)" */
        volatile uint32_t SD_DATA3_CD: 1;  /* Use sd_data[3] to do card detect                0: use speci */
        /* @SVD:field=ITIMING_SEL,bitOffset=1,bitWidth=1,description="select input sample timing (according to itiming config)" */
        volatile uint32_t ITIMING_SEL: 1;  /* select input sample timing (according to itiming config) */
        /* @SVD:field=OTIMING_SEL,bitOffset=2,bitWidth=1,description="select output timing (according to otiming config)" */
        volatile uint32_t OTIMING_SEL: 1;  /* select output timing (according to otiming config) */
        /* @SVD:field=EN_CD,bitOffset=3,bitWidth=1,description="Enable card detect \n              Only when the bit is valid, controller does card detect.\n              If use sd_data[3] to do card detect, the bit should be cleared when transfer valid data." */
        volatile uint32_t EN_CD: 1;  /* Enable card detect                Only when the bit is valid */
        /* @SVD:field=CD_HVALID,bitOffset=4,bitWidth=1,description="Card detect high level valid\n              0: detect low level means card exist\n              1: detect high level means card exist (default)" */
        volatile uint32_t CD_HVALID: 1;  /* Card detect high level valid               0: detect low lev */
        /* @SVD:field=CMD_OD,bitOffset=5,bitWidth=1,description="Open Drain mode for cmd line (for eMMC identification mode)\n              0: cmd line is push-pull\n              1: cmd line is open-drain" */
        volatile uint32_t CMD_OD: 1;  /* Open Drain mode for cmd line (for eMMC identification mode)  */
        /* @SVD:field=ITIMING,bitOffset=6,bitWidth=13,description="define input timing" */
        volatile uint32_t ITIMING: 13;  /* define input timing */
        /* @SVD:field=OTIMING,bitOffset=19,bitWidth=13,description="define output timing" */
        volatile uint32_t OTIMING: 13;  /* define output timing */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CDR_Type;

/* @SVD:register=DBGR1,offset=0x0040,size=32,access=read-write,reset=0x0,description="card debug port1 register" */
/* DBGR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMD_ST,bitOffset=0,bitWidth=16,description="command state for debug only" */
        volatile uint32_t CMD_ST: 16;  /* command state for debug only */
        /* @SVD:field=DATA_ST,bitOffset=16,bitWidth=15,description="data state for debug only" */
        volatile uint32_t DATA_ST: 15;  /* data state for debug only */
        /* @SVD:field=RSVD,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_DBGR1_Type;

/* @SVD:register=DBGR2,offset=0x0044,size=32,access=read-write,reset=0x0,description="card debug port2 register" */
/* DBGR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HOST_WORD_COUNTER,bitOffset=0,bitWidth=14,description="for debug only" */
        volatile uint32_t HOST_WORD_COUNTER: 14;  /* for debug only */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=VALID_DATA_COU,bitOffset=16,bitWidth=10,description="for debug only" */
        volatile uint32_t VALID_DATA_COU: 10;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
        /* @SVD:field=DBG_SEL,bitOffset=30,bitWidth=2,description="for debug only" */
        volatile uint32_t DBG_SEL: 2;  /* for debug only */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_DBGR2_Type;

/* @SVD:register=CEATA,offset=0x0048,size=32,access=read-write,reset=0x0,description="CE-ATA/SDIO mode register" */
/* CEATA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ATA_MODE,bitOffset=0,bitWidth=1,description="Select the card type, default is sd card\n              0: sd card mode\n              1: CE-ATA device mode" */
        volatile uint32_t ATA_MODE: 1;  /* Select the card type, default is sd card               0: sd */
        /* @SVD:field=ENABLE_SDIO_IRQ,bitOffset=1,bitWidth=1,description="Select the sdio card mode, default is sd card\n              0: sd card mode , no sdio card interrupt \n              1: sdio card mode , enable sdio card interrupt" */
        volatile uint32_t ENABLE_SDIO_IRQ: 1;  /* Select the sdio card mode, default is sd card                */
        /* @SVD:field=SDIO_4WIRES_IRQ,bitOffset=2,bitWidth=1,description="Select the sdio host 4 wires interrupt support\n              0: host not support 4 wires interrupt on single-block data transfers\n              1: host support 4 wires interrupt on single-block data transfers" */
        volatile uint32_t SDIO_4WIRES_IRQ: 1;  /* Select the sdio host 4 wires interrupt support               */
        /* @SVD:field=SDIO_4WIRES_MULTI_IRQ,bitOffset=3,bitWidth=1,description="Select the sdio host 4 wires interrupt on multi-block support\n              0: host not support 4 wires interrupt on multi-block data transfers\n              1: host support 4 wires interrupt on multi-block data transfers" */
        volatile uint32_t SDIO_4WIRES_MULTI_IRQ: 1;  /* Select the sdio host 4 wires interrupt on multi-block suppor */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CEATA_Type;

/* @SVD:register=DSR,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="data status register" */
/* DSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SD_DATA_I_LL,bitOffset=0,bitWidth=8,description="The status of each sd data pad status" */
        volatile uint32_t SD_DATA_I_LL: 8;  /* The status of each sd data pad status */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_DSR_Type;

/* @SVD:register=CDCR,offset=0x0058,size=32,access=read-write,reset=0x00000001,description="clock duty cycle register" */
/* CDCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLK_CONFIG,bitOffset=0,bitWidth=1,description="1: the sd clock is 50% duty cycle\n              0: the high level of the sd clock is 1 hclk cycle" */
        volatile uint32_t CLK_CONFIG: 1;  /* 1: the sd clock is 50% duty cycle               0: the high  */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CDCR_Type;

/* @SVD:register=CASR,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="cache status register" */
/* CASR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SD_REQ,bitOffset=0,bitWidth=1,description="Set 1 to request sd normal access. sd_req will be cleared automatically after sd_busy asserted" */
        volatile uint32_t SD_REQ: 1;  /* Set 1 to request sd normal access. sd_req will be cleared au */
        /* @SVD:field=SD_BUSY,bitOffset=1,bitWidth=1,description="Read 1 indicates sd is ready for normal access. Ahb access will be hold during sd_busy asserted.\n              After sd normal access done, write 1 to clear, and ahb access will continue" */
        volatile uint32_t SD_BUSY: 1;  /* Read 1 indicates sd is ready for normal access. Ahb access w */
        /* @SVD:field=CACHE_BUSY,bitOffset=2,bitWidth=1,description="Indicates cache is working" */
        volatile uint32_t CACHE_BUSY: 1;  /* Indicates cache is working */
        /* @SVD:field=CACHE_FLUSH,bitOffset=3,bitWidth=1,description="Set 1 to flush cache. Should set when cache not busy." */
        volatile uint32_t CACHE_FLUSH: 1;  /* Set 1 to flush cache. Should set when cache not busy. */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CASR_Type;

/* @SVD:register=CACR,offset=0x0060,size=32,access=read-write,reset=0xd0844c52,description="cache control register" */
/* CACR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=READ_INDEX,bitOffset=0,bitWidth=6,description="Command index for cache read. CMD18 by default" */
        volatile uint32_t READ_INDEX: 6;  /* Command index for cache read. CMD18 by default */
        /* @SVD:field=READ_HAS_RSP,bitOffset=6,bitWidth=1,description="Read command have a response" */
        volatile uint32_t READ_HAS_RSP: 1;  /* Read command have a response */
        /* @SVD:field=READ_LONG_RSP,bitOffset=7,bitWidth=1,description="Read response is 136-bit, long response" */
        volatile uint32_t READ_LONG_RSP: 1;  /* Read response is 136-bit, long response */
        /* @SVD:field=STOP_INDEX,bitOffset=8,bitWidth=6,description="Command index for stop. CMD12 by default" */
        volatile uint32_t STOP_INDEX: 6;  /* Command index for stop. CMD12 by default */
        /* @SVD:field=STOP_HAS_RSP,bitOffset=14,bitWidth=1,description="Stop command have a response" */
        volatile uint32_t STOP_HAS_RSP: 1;  /* Stop command have a response */
        /* @SVD:field=STOP_LONG_RSP,bitOffset=15,bitWidth=1,description="Stop response is 136-bit, long response" */
        volatile uint32_t STOP_LONG_RSP: 1;  /* Stop response is 136-bit, long response */
        /* @SVD:field=CACHE_BLOCK,bitOffset=16,bitWidth=3,description="cache depth is cache_block blocks" */
        volatile uint32_t CACHE_BLOCK: 3;  /* cache depth is cache_block blocks */
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CACHE_PREF_BLOCK,bitOffset=20,bitWidth=4,description="cache prefetch depth is cache_pref_block blocks. Should be no less than cache_block" */
        volatile uint32_t CACHE_PREF_BLOCK: 4;  /* cache prefetch depth is cache_pref_block blocks. Should be n */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=CACHE_HRESP,bitOffset=26,bitWidth=1,description="1: generate ahb error response when error occur\n              0: no ahb error response generated. Could check cache_err interrupt" */
        volatile uint32_t CACHE_HRESP: 1;  /* 1: generate ahb error response when error occur              */
        /* @SVD:field=CACHE_NOCRC,bitOffset=27,bitWidth=1,description="1: return ahb data without crc check\n              0: return ahb data after block crc pass" */
        volatile uint32_t CACHE_NOCRC: 1;  /* 1: return ahb data without crc check               0: return */
        /* @SVD:field=CACHE_SDSC,bitOffset=28,bitWidth=1,description="select card version\n              1: card size (=2GB, address of cmd18 is in byte\n              0: card size >2GB, address of cmd18 is in block" */
        volatile uint32_t CACHE_SDSC: 1;  /* select card version               1: card size (=2GB, addres */
        /* @SVD:field=CACHE_FORCE_READ,bitOffset=29,bitWidth=1,description="force cache read done\n              1: start new fetch for miss access only after cache read done\n              0: start new fetch for miss access even when cache is still filling (read will be breaked by cmd12)" */
        volatile uint32_t CACHE_FORCE_READ: 1;  /* force cache read done               1: start new fetch for m */
        /* @SVD:field=CACHE_TO_EN,bitOffset=30,bitWidth=1,description="enable ahb read timeout recover" */
        volatile uint32_t CACHE_TO_EN: 1;  /* enable ahb read timeout recover */
        /* @SVD:field=CACHE_EN,bitOffset=31,bitWidth=1,description="enable cache\n              1: ahb read will return cached data\n              0: ahb read always return dummy data with no error response" */
        volatile uint32_t CACHE_EN: 1;  /* enable cache               1: ahb read will return cached da */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CACR_Type;

/* @SVD:register=CACNT,offset=0x0064,size=32,access=read-write,reset=0xffff0020,description="cache counter register" */
/* CACNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CACHE_NCC,bitOffset=0,bitWidth=8,description="cmd-cmd interval counter in hclk cycles" */
        volatile uint32_t CACHE_NCC: 8;  /* cmd-cmd interval counter in hclk cycles */
        /* @SVD:field=CACHE_NDC,bitOffset=8,bitWidth=8,description="data-cmd interval counter in hclk cycles" */
        volatile uint32_t CACHE_NDC: 8;  /* data-cmd interval counter in hclk cycles */
        /* @SVD:field=CACHE_TOR,bitOffset=16,bitWidth=16,description="timeout count register for ahb read" */
        volatile uint32_t CACHE_TOR: 16;  /* timeout count register for ahb read */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CACNT_Type;

/* @SVD:register=CAOFF,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="cache offset register" */
/* CAOFF 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CACHE_OFFSET,bitOffset=0,bitWidth=32,description="offset to map ahb address to sd address for ahb access" */
        volatile uint32_t CACHE_OFFSET: 32;  /* offset to map ahb address to sd address for ahb access */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_CAOFF_Type;

/* @SVD:register=FIFO,offset=0x0200,size=32,access=read-write,reset=0x00000000,description="FIFO entry" */
/* FIFO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="Entry to access internal FIFO. Access should be word-aligned, ranging from 0x200 to 0x3fc. Inside the range, write to any address will push the data into the FIFO, and read any address will pop a word from the FIFO." */
        volatile uint32_t DATA: 32;  /* Entry to access internal FIFO. Access should be word-aligned */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SDMMC1_FIFO_Type;

/* SDMMC1 寄存器结构体 */
typedef struct
{
    SDMMC1_SR_Type SR;  /* 0x0000 - command and data status register */
    SDMMC1_CCR_Type CCR;  /* 0x0004 - command control register */
    SDMMC1_CAR_Type CAR;  /* 0x0008 - command argument register */
    SDMMC1_RIR_Type RIR;  /* 0x000C - response command index register */
    SDMMC1_RAR1_Type RAR1;  /* 0x0010 - response command argument1 register */
    SDMMC1_RAR2_Type RAR2;  /* 0x0014 - response command argument2 register */
    SDMMC1_RAR3_Type RAR3;  /* 0x0018 - response command argument3 register */
    SDMMC1_RAR4_Type RAR4;  /* 0x001C - response command argument4 register */
    SDMMC1_TOR_Type TOR;  /* 0x0020 - timeout count register */
    SDMMC1_DCR_Type DCR;  /* 0x0024 - data control register */
    SDMMC1_DLR_Type DLR;  /* 0x0028 - data length register */
    SDMMC1_IER_Type IER;  /* 0x002C - command and data interrupt mask register */
    SDMMC1_CLKCR_Type CLKCR;  /* 0x0030 - clock control register */
    /* @SVD:register=RSVD3,offset=0x0034,size=32,access=read-write,reset=0x0,description="RSVD3" */
    volatile uint32_t RSVD3;  /* 0x0034 - RSVD3 */
    volatile uint32_t RESERVED14[1];  /* 0x0038 - 0x003B */
    SDMMC1_CDR_Type CDR;  /* 0x003C - card interface control and card detect r */
    SDMMC1_DBGR1_Type DBGR1;  /* 0x0040 - card debug port1 register */
    SDMMC1_DBGR2_Type DBGR2;  /* 0x0044 - card debug port2 register */
    SDMMC1_CEATA_Type CEATA;  /* 0x0048 - CE-ATA/SDIO mode register */
    /* @SVD:register=RSVD2,offset=0x004C,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x004C - RSVD2 */
    volatile uint32_t RESERVED20[1];  /* 0x0050 - 0x0053 */
    SDMMC1_DSR_Type DSR;  /* 0x0054 - data status register */
    SDMMC1_CDCR_Type CDCR;  /* 0x0058 - clock duty cycle register */
    SDMMC1_CASR_Type CASR;  /* 0x005C - cache status register */
    SDMMC1_CACR_Type CACR;  /* 0x0060 - cache control register */
    SDMMC1_CACNT_Type CACNT;  /* 0x0064 - cache counter register */
    SDMMC1_CAOFF_Type CAOFF;  /* 0x0068 - cache offset register */
    /* @SVD:register=RSVD1,offset=0x006C,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x006C - RSVD1 */
    volatile uint32_t RESERVED28[100];  /* 0x0070 - 0x01FF */
    SDMMC1_FIFO_Type FIFO;  /* 0x0200 - FIFO entry */
} SDMMC1_TypeDef;

/* ============================================================================ */
/* CRC1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=CRC1,base=0x50048000,description="",groupName=CRC */

#define CRC1_BASE                                          (0x50048000UL)

/* @SVD:register=DR,offset=0x0000,size=32,access=read-write,reset=0x0,description="Data register" */
/* DR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DR,bitOffset=0,bitWidth=32,description="Data register bits.\n              This register is used to write new data to the CRC calculator.\n              It holds the previous CRC calculation result when it is read.\n              If the data size is less than 32 bits, the least significant bits are used to write/read the correct value." */
        volatile uint32_t DR: 32;  /* Data register bits.               This register is used to w */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC1_DR_Type;

/* @SVD:register=SR,offset=0x0004,size=32,access=read-write,reset=0x0,description="Status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DONE,bitOffset=0,bitWidth=1,description="Done flag. When DR written, done flag will be cleared automatically. The flag will assert after CRC operation of current DR finished." */
        volatile uint32_t DONE: 1;  /* Done flag. When DR written, done flag will be cleared automa */
        /* @SVD:field=OVERFLOW,bitOffset=1,bitWidth=1,description="Overflow when new data arrive while last calculation not done yet" */
        volatile uint32_t OVERFLOW: 1;  /* Overflow when new data arrive while last calculation not don */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC1_SR_Type;

/* @SVD:register=CR,offset=0x0008,size=32,access=read-write,reset=0x0,description="Control register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESET,bitOffset=0,bitWidth=1,description="This bit is set by software to reset the CRC calculation unit and set the data register to the value stored in the CRC_INIT register. This bit can only be set, it is automatically cleared by hardware" */
        volatile uint32_t RESET: 1;  /* This bit is set by software to reset the CRC calculation uni */
        /* @SVD:field=DATASIZE,bitOffset=1,bitWidth=2,description="Valid input data size\n              These bits control the valid size of the input data.\n              00: lower 8-bit\n              01: lower 16-bit\n              10: lower 24-bit\n              11: all 32-bit" */
        volatile uint32_t DATASIZE: 2;  /* Valid input data size               These bits control the v */
        /* @SVD:field=POLYSIZE,bitOffset=3,bitWidth=2,description="Polynomial size\n              These bits control the size of the polynomial.\n              00: 32 bit polynomial\n              01: 16 bit polynomial\n              10: 8 bit polynomial\n              11: 7 bit polynomial" */
        volatile uint32_t POLYSIZE: 2;  /* Polynomial size               These bits control the size of */
        /* @SVD:field=REV_IN,bitOffset=5,bitWidth=2,description="Reverse input data\n              These bits control the reversal of the bit order of the input data\n              00: Bit order not affected\n              01: Bit reversal done by byte\n              10: Bit reversal done by half-word\n              11: Bit reversal done by word" */
        volatile uint32_t REV_IN: 2;  /* Reverse input data               These bits control the reve */
        /* @SVD:field=REV_OUT,bitOffset=7,bitWidth=1,description="Reverse output data\n              This bit controls the reversal of the bit order of the output data.\n              0: Bit order not affected\n              1: Bit-reversed output format" */
        volatile uint32_t REV_OUT: 1;  /* Reverse output data               This bit controls the reve */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC1_CR_Type;

/* @SVD:register=INIT,offset=0x0010,size=32,access=read-write,reset=0x0,description="Initial CRC value" */
/* INIT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INIT,bitOffset=0,bitWidth=32,description="Programmable initial CRC value" */
        volatile uint32_t INIT: 32;  /* Programmable initial CRC value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC1_INIT_Type;

/* @SVD:register=POL,offset=0x0014,size=32,access=read-write,reset=0x0,description="CRC polynomial" */
/* POL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=POL,bitOffset=0,bitWidth=32,description="Programmable polynomial \n              This register is used to write the coefficients of the polynomial to be used for CRC calculation.\n              If the polynomial size is less than 32 bits, the least significant bits have to be used to program the correct value." */
        volatile uint32_t POL: 32;  /* Programmable polynomial                This register is used */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC1_POL_Type;

/* CRC1 寄存器结构体 */
typedef struct
{
    CRC1_DR_Type DR;  /* 0x0000 - Data register */
    CRC1_SR_Type SR;  /* 0x0004 - Status register */
    CRC1_CR_Type CR;  /* 0x0008 - Control register */
    /* @SVD:register=RSVD1,offset=0x000C,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x000C - RSVD1 */
    CRC1_INIT_Type INIT;  /* 0x0010 - Initial CRC value */
    CRC1_POL_Type POL;  /* 0x0014 - CRC polynomial */
} CRC1_TypeDef;

/* ============================================================================ */
/* PTC1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=PTC1,base=0x50080000,description="",groupName=PTC */

#define PTC1_BASE                                          (0x50080000UL)

/* @SVD:register=ISR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="interrupt status register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCIF1,bitOffset=0,bitWidth=1,description="task complete interrupt flag for task 1" */
        volatile uint32_t TCIF1: 1;  /* task complete interrupt flag for task 1 */
        /* @SVD:field=TCIF2,bitOffset=1,bitWidth=1,description="task complete interrupt flag for task 2" */
        volatile uint32_t TCIF2: 1;  /* task complete interrupt flag for task 2 */
        /* @SVD:field=TCIF3,bitOffset=2,bitWidth=1,description="task complete interrupt flag for task 3" */
        volatile uint32_t TCIF3: 1;  /* task complete interrupt flag for task 3 */
        /* @SVD:field=TCIF4,bitOffset=3,bitWidth=1,description="task complete interrupt flag for task 4" */
        volatile uint32_t TCIF4: 1;  /* task complete interrupt flag for task 4 */
        /* @SVD:field=TCIF5,bitOffset=4,bitWidth=1,description="task complete interrupt flag for task 5" */
        volatile uint32_t TCIF5: 1;  /* task complete interrupt flag for task 5 */
        /* @SVD:field=TCIF6,bitOffset=5,bitWidth=1,description="task complete interrupt flag for task 6" */
        volatile uint32_t TCIF6: 1;  /* task complete interrupt flag for task 6 */
        /* @SVD:field=TCIF7,bitOffset=6,bitWidth=1,description="task complete interrupt flag for task 7" */
        volatile uint32_t TCIF7: 1;  /* task complete interrupt flag for task 7 */
        /* @SVD:field=TCIF8,bitOffset=7,bitWidth=1,description="task complete interrupt flag for task 8" */
        volatile uint32_t TCIF8: 1;  /* task complete interrupt flag for task 8 */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=TEIF1,bitOffset=16,bitWidth=1,description="transfer error flag for task 1" */
        volatile uint32_t TEIF1: 1;  /* transfer error flag for task 1 */
        /* @SVD:field=TEIF2,bitOffset=17,bitWidth=1,description="transfer error flag for task 2" */
        volatile uint32_t TEIF2: 1;  /* transfer error flag for task 2 */
        /* @SVD:field=TEIF3,bitOffset=18,bitWidth=1,description="transfer error flag for task 3" */
        volatile uint32_t TEIF3: 1;  /* transfer error flag for task 3 */
        /* @SVD:field=TEIF4,bitOffset=19,bitWidth=1,description="transfer error flag for task 4" */
        volatile uint32_t TEIF4: 1;  /* transfer error flag for task 4 */
        /* @SVD:field=TEIF5,bitOffset=20,bitWidth=1,description="transfer error flag for task 5" */
        volatile uint32_t TEIF5: 1;  /* transfer error flag for task 5 */
        /* @SVD:field=TEIF6,bitOffset=21,bitWidth=1,description="transfer error flag for task 6" */
        volatile uint32_t TEIF6: 1;  /* transfer error flag for task 6 */
        /* @SVD:field=TEIF7,bitOffset=22,bitWidth=1,description="transfer error flag for task 7" */
        volatile uint32_t TEIF7: 1;  /* transfer error flag for task 7 */
        /* @SVD:field=TEIF8,bitOffset=23,bitWidth=1,description="transfer error flag for task 8" */
        volatile uint32_t TEIF8: 1;  /* transfer error flag for task 8 */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_ISR_Type;

/* @SVD:register=ICR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="interrupt clear register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CTCIF1,bitOffset=0,bitWidth=1,description="clear task complete interrupt flag for task 1" */
        volatile uint32_t CTCIF1: 1;  /* clear task complete interrupt flag for task 1 */
        /* @SVD:field=CTCIF2,bitOffset=1,bitWidth=1,description="clear task complete interrupt flag for task 2" */
        volatile uint32_t CTCIF2: 1;  /* clear task complete interrupt flag for task 2 */
        /* @SVD:field=CTCIF3,bitOffset=2,bitWidth=1,description="clear task complete interrupt flag for task 3" */
        volatile uint32_t CTCIF3: 1;  /* clear task complete interrupt flag for task 3 */
        /* @SVD:field=CTCIF4,bitOffset=3,bitWidth=1,description="clear task complete interrupt flag for task 4" */
        volatile uint32_t CTCIF4: 1;  /* clear task complete interrupt flag for task 4 */
        /* @SVD:field=CTCIF5,bitOffset=4,bitWidth=1,description="clear task complete interrupt flag for task 5" */
        volatile uint32_t CTCIF5: 1;  /* clear task complete interrupt flag for task 5 */
        /* @SVD:field=CTCIF6,bitOffset=5,bitWidth=1,description="clear task complete interrupt flag for task 6" */
        volatile uint32_t CTCIF6: 1;  /* clear task complete interrupt flag for task 6 */
        /* @SVD:field=CTCIF7,bitOffset=6,bitWidth=1,description="clear task complete interrupt flag for task 7" */
        volatile uint32_t CTCIF7: 1;  /* clear task complete interrupt flag for task 7 */
        /* @SVD:field=CTCIF8,bitOffset=7,bitWidth=1,description="clear task complete interrupt flag for task 8" */
        volatile uint32_t CTCIF8: 1;  /* clear task complete interrupt flag for task 8 */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=CTEIF,bitOffset=16,bitWidth=1,description="clear transfer error flag" */
        volatile uint32_t CTEIF: 1;  /* clear transfer error flag */
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_ICR_Type;

/* @SVD:register=IER,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="interrupt enable register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCIE1,bitOffset=0,bitWidth=1,description="enable task complete interrupt for task 1" */
        volatile uint32_t TCIE1: 1;  /* enable task complete interrupt for task 1 */
        /* @SVD:field=TCIE2,bitOffset=1,bitWidth=1,description="enable task complete interrupt for task 2" */
        volatile uint32_t TCIE2: 1;  /* enable task complete interrupt for task 2 */
        /* @SVD:field=TCIE3,bitOffset=2,bitWidth=1,description="enable task complete interrupt for task 3" */
        volatile uint32_t TCIE3: 1;  /* enable task complete interrupt for task 3 */
        /* @SVD:field=TCIE4,bitOffset=3,bitWidth=1,description="enable task complete interrupt for task 4" */
        volatile uint32_t TCIE4: 1;  /* enable task complete interrupt for task 4 */
        /* @SVD:field=TCIE5,bitOffset=4,bitWidth=1,description="enable task complete interrupt for task 5" */
        volatile uint32_t TCIE5: 1;  /* enable task complete interrupt for task 5 */
        /* @SVD:field=TCIE6,bitOffset=5,bitWidth=1,description="enable task complete interrupt for task 6" */
        volatile uint32_t TCIE6: 1;  /* enable task complete interrupt for task 6 */
        /* @SVD:field=TCIE7,bitOffset=6,bitWidth=1,description="enable task complete interrupt for task 7" */
        volatile uint32_t TCIE7: 1;  /* enable task complete interrupt for task 7 */
        /* @SVD:field=TCIE8,bitOffset=7,bitWidth=1,description="enable task complete interrupt for task 8" */
        volatile uint32_t TCIE8: 1;  /* enable task complete interrupt for task 8 */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=TEIE,bitOffset=16,bitWidth=1,description="enable transfer error flag" */
        volatile uint32_t TEIE: 1;  /* enable transfer error flag */
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_IER_Type;

/* @SVD:register=TCR1,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="task 1 control register" */
/* TCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source\n              0: task will only be triggered by SWTRIG\n              others: task will be triggered by selected source or SWTRIG" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source               0: task will only be tri */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TCR1_Type;

/* @SVD:register=TAR1,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="task 1 address register" */
/* TAR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TAR1_Type;

/* @SVD:register=TDR1,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="task 1 data register" */
/* TDR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TDR1_Type;

/* @SVD:register=RCR1,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="task 1 repetition and delay counter register" */
/* RCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=DLY,bitOffset=16,bitWidth=16,description="Delay time before task operation after triggered\n              0: no delay\n              others: delay DLY HCLK cycles before task operation\n              DLY is read as left delay time. DLY will be reloaded automatically after each operation." */
        volatile uint32_t DLY: 16;  /* Delay time before task operation after triggered             */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_RCR1_Type;

/* @SVD:register=TCR2,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="TCR2" */
/* TCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TCR2_Type;

/* @SVD:register=TAR2,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="TAR2" */
/* TAR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TAR2_Type;

/* @SVD:register=TDR2,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="TDR2" */
/* TDR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TDR2_Type;

/* @SVD:register=RCR2,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="task 2 repetition and delay counter register" */
/* RCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=DLY,bitOffset=16,bitWidth=16,description="Delay time before task operation after triggered\n              0: no delay\n              others: delay DLY HCLK cycles before task operation\n              DLY is read as left delay time. DLY will be reloaded automatically after each operation." */
        volatile uint32_t DLY: 16;  /* Delay time before task operation after triggered             */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_RCR2_Type;

/* @SVD:register=TCR3,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="TCR3" */
/* TCR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TCR3_Type;

/* @SVD:register=TAR3,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="TAR3" */
/* TAR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TAR3_Type;

/* @SVD:register=TDR3,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="TDR3" */
/* TDR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TDR3_Type;

/* @SVD:register=RCR3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="task 3 repetition and delay counter register" */
/* RCR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=DLY,bitOffset=16,bitWidth=16,description="Delay time before task operation after triggered\n              0: no delay\n              others: delay DLY HCLK cycles before task operation\n              DLY is read as left delay time. DLY will be reloaded automatically after each operation." */
        volatile uint32_t DLY: 16;  /* Delay time before task operation after triggered             */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_RCR3_Type;

/* @SVD:register=TCR4,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="TCR4" */
/* TCR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TCR4_Type;

/* @SVD:register=TAR4,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="TAR4" */
/* TAR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TAR4_Type;

/* @SVD:register=TDR4,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="TDR4" */
/* TDR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TDR4_Type;

/* @SVD:register=RCR4,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="task 4 repetition and delay counter register" */
/* RCR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=DLY,bitOffset=16,bitWidth=16,description="Delay time before task operation after triggered\n              0: no delay\n              others: delay DLY HCLK cycles before task operation\n              DLY is read as left delay time. DLY will be reloaded automatically after each operation." */
        volatile uint32_t DLY: 16;  /* Delay time before task operation after triggered             */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_RCR4_Type;

/* @SVD:register=TCR5,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="TCR5" */
/* TCR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TCR5_Type;

/* @SVD:register=TAR5,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="TAR5" */
/* TAR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TAR5_Type;

/* @SVD:register=TDR5,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="TDR5" */
/* TDR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TDR5_Type;

/* @SVD:register=RCR5,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="task 5 repetition counter register" */
/* RCR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_RCR5_Type;

/* @SVD:register=TCR6,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="TCR6" */
/* TCR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TCR6_Type;

/* @SVD:register=TAR6,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="TAR6" */
/* TAR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TAR6_Type;

/* @SVD:register=TDR6,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="TDR6" */
/* TDR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TDR6_Type;

/* @SVD:register=RCR6,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="task 6 repetition counter register" */
/* RCR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_RCR6_Type;

/* @SVD:register=TCR7,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="TCR7" */
/* TCR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TCR7_Type;

/* @SVD:register=TAR7,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="TAR7" */
/* TAR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TAR7_Type;

/* @SVD:register=TDR7,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="TDR7" */
/* TDR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TDR7_Type;

/* @SVD:register=RCR7,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="task 7 repetition counter register" */
/* RCR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_RCR7_Type;

/* @SVD:register=TCR8,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="TCR8" */
/* TCR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TCR8_Type;

/* @SVD:register=TAR8,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="TAR8" */
/* TAR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TAR8_Type;

/* @SVD:register=TDR8,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="TDR8" */
/* TDR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_TDR8_Type;

/* @SVD:register=RCR8,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="task 8 repetition counter register" */
/* RCR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_RCR8_Type;

/* @SVD:register=MEM1,offset=0x00D0,size=32,access=read-write,reset=0x00000000,description="temporary memory 1" */
/* MEM1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="memory to store temporary variables" */
        volatile uint32_t DATA: 32;  /* memory to store temporary variables */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_MEM1_Type;

/* @SVD:register=MEM2,offset=0x00D4,size=32,access=read-write,reset=0x00000000,description="temporary memory 2" */
/* MEM2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="memory to store temporary variables" */
        volatile uint32_t DATA: 32;  /* memory to store temporary variables */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_MEM2_Type;

/* @SVD:register=MEM3,offset=0x00D8,size=32,access=read-write,reset=0x00000000,description="temporary memory 3" */
/* MEM3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="memory to store temporary variables" */
        volatile uint32_t DATA: 32;  /* memory to store temporary variables */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_MEM3_Type;

/* @SVD:register=MEM4,offset=0x00DC,size=32,access=read-write,reset=0x00000000,description="temporary memory 4" */
/* MEM4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="memory to store temporary variables" */
        volatile uint32_t DATA: 32;  /* memory to store temporary variables */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_MEM4_Type;

/* @SVD:register=GPIO31_0,offset=0x00E0,size=32,access=read-write,reset=0x00000000,description="GPIO31_0" */
/* GPIO31_0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SELA,bitOffset=0,bitWidth=5,description="select trigger A of GPIO 31~0\n              0: select GPIO 0\n              1: select GPIO 1\n              ......\n              31: select GPIO 31" */
        volatile uint32_t SELA: 5;  /* select trigger A of GPIO 31~0               0: select GPIO 0 */
        /* @SVD:field=RSVD4,bitOffset=5,bitWidth=3,description="" */
        volatile uint32_t RSVD4: 3;
        /* @SVD:field=SELB,bitOffset=8,bitWidth=5,description="select trigger B of GPIO 31~0" */
        volatile uint32_t SELB: 5;  /* select trigger B of GPIO 31~0 */
        /* @SVD:field=RSVD3,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t RSVD3: 3;
        /* @SVD:field=SELC,bitOffset=16,bitWidth=5,description="select trigger C of GPIO 31~0" */
        volatile uint32_t SELC: 5;  /* select trigger C of GPIO 31~0 */
        /* @SVD:field=RSVD2,bitOffset=21,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=SELD,bitOffset=24,bitWidth=5,description="select trigger D of GPIO 31~0" */
        volatile uint32_t SELD: 5;  /* select trigger D of GPIO 31~0 */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_GPIO31_0_Type;

/* @SVD:register=GPIO63_32,offset=0x00E4,size=32,access=read-write,reset=0x00000000,description="GPIO63_32" */
/* GPIO63_32 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SELA,bitOffset=0,bitWidth=5,description="select trigger A of GPIO 63~32\n              0: select GPIO 32\n              1: select GPIO 33\n              ......\n              31: select GPIO 63" */
        volatile uint32_t SELA: 5;  /* select trigger A of GPIO 63~32               0: select GPIO  */
        /* @SVD:field=RSVD4,bitOffset=5,bitWidth=3,description="" */
        volatile uint32_t RSVD4: 3;
        /* @SVD:field=SELB,bitOffset=8,bitWidth=5,description="select trigger B of GPIO 63~32" */
        volatile uint32_t SELB: 5;  /* select trigger B of GPIO 63~32 */
        /* @SVD:field=RSVD3,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t RSVD3: 3;
        /* @SVD:field=SELC,bitOffset=16,bitWidth=5,description="select trigger C of GPIO 63~32" */
        volatile uint32_t SELC: 5;  /* select trigger C of GPIO 63~32 */
        /* @SVD:field=RSVD2,bitOffset=21,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=SELD,bitOffset=24,bitWidth=5,description="select trigger D of GPIO 63~32" */
        volatile uint32_t SELD: 5;  /* select trigger D of GPIO 63~32 */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_GPIO63_32_Type;

/* @SVD:register=GPIO95_64,offset=0x00E8,size=32,access=read-write,reset=0x00000000,description="GPIO95_64" */
/* GPIO95_64 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SELA,bitOffset=0,bitWidth=5,description="select trigger A of GPIO 95~64\n              0: select GPIO 64\n              1: select GPIO 65\n              ......\n              31: select GPIO 95" */
        volatile uint32_t SELA: 5;  /* select trigger A of GPIO 95~64               0: select GPIO  */
        /* @SVD:field=RSVD4,bitOffset=5,bitWidth=3,description="" */
        volatile uint32_t RSVD4: 3;
        /* @SVD:field=SELB,bitOffset=8,bitWidth=5,description="select trigger B of GPIO 95~64" */
        volatile uint32_t SELB: 5;  /* select trigger B of GPIO 95~64 */
        /* @SVD:field=RSVD3,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t RSVD3: 3;
        /* @SVD:field=SELC,bitOffset=16,bitWidth=5,description="select trigger C of GPIO 95~64" */
        volatile uint32_t SELC: 5;  /* select trigger C of GPIO 95~64 */
        /* @SVD:field=RSVD2,bitOffset=21,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=SELD,bitOffset=24,bitWidth=5,description="select trigger D of GPIO 95~64" */
        volatile uint32_t SELD: 5;  /* select trigger D of GPIO 95~64 */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC1_GPIO95_64_Type;

/* PTC1 寄存器结构体 */
typedef struct
{
    PTC1_ISR_Type ISR;  /* 0x0000 - interrupt status register */
    PTC1_ICR_Type ICR;  /* 0x0004 - interrupt clear register */
    PTC1_IER_Type IER;  /* 0x0008 - interrupt enable register */
    /* @SVD:register=RSVD2,offset=0x000C,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x000C - RSVD2 */
    PTC1_TCR1_Type TCR1;  /* 0x0010 - task 1 control register */
    PTC1_TAR1_Type TAR1;  /* 0x0014 - task 1 address register */
    PTC1_TDR1_Type TDR1;  /* 0x0018 - task 1 data register */
    PTC1_RCR1_Type RCR1;  /* 0x001C - task 1 repetition and delay counter regi */
    PTC1_TCR2_Type TCR2;  /* 0x0020 - TCR2 */
    PTC1_TAR2_Type TAR2;  /* 0x0024 - TAR2 */
    PTC1_TDR2_Type TDR2;  /* 0x0028 - TDR2 */
    PTC1_RCR2_Type RCR2;  /* 0x002C - task 2 repetition and delay counter regi */
    PTC1_TCR3_Type TCR3;  /* 0x0030 - TCR3 */
    PTC1_TAR3_Type TAR3;  /* 0x0034 - TAR3 */
    PTC1_TDR3_Type TDR3;  /* 0x0038 - TDR3 */
    PTC1_RCR3_Type RCR3;  /* 0x003C - task 3 repetition and delay counter regi */
    PTC1_TCR4_Type TCR4;  /* 0x0040 - TCR4 */
    PTC1_TAR4_Type TAR4;  /* 0x0044 - TAR4 */
    PTC1_TDR4_Type TDR4;  /* 0x0048 - TDR4 */
    PTC1_RCR4_Type RCR4;  /* 0x004C - task 4 repetition and delay counter regi */
    PTC1_TCR5_Type TCR5;  /* 0x0050 - TCR5 */
    PTC1_TAR5_Type TAR5;  /* 0x0054 - TAR5 */
    PTC1_TDR5_Type TDR5;  /* 0x0058 - TDR5 */
    PTC1_RCR5_Type RCR5;  /* 0x005C - task 5 repetition counter register */
    PTC1_TCR6_Type TCR6;  /* 0x0060 - TCR6 */
    PTC1_TAR6_Type TAR6;  /* 0x0064 - TAR6 */
    PTC1_TDR6_Type TDR6;  /* 0x0068 - TDR6 */
    PTC1_RCR6_Type RCR6;  /* 0x006C - task 6 repetition counter register */
    PTC1_TCR7_Type TCR7;  /* 0x0070 - TCR7 */
    PTC1_TAR7_Type TAR7;  /* 0x0074 - TAR7 */
    PTC1_TDR7_Type TDR7;  /* 0x0078 - TDR7 */
    PTC1_RCR7_Type RCR7;  /* 0x007C - task 7 repetition counter register */
    PTC1_TCR8_Type TCR8;  /* 0x0080 - TCR8 */
    PTC1_TAR8_Type TAR8;  /* 0x0084 - TAR8 */
    PTC1_TDR8_Type TDR8;  /* 0x0088 - TDR8 */
    PTC1_RCR8_Type RCR8;  /* 0x008C - task 8 repetition counter register */
    /* @SVD:register=RSVD1,offset=0x0090,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0090 - RSVD1 */
    volatile uint32_t RESERVED37[15];  /* 0x0094 - 0x00CF */
    PTC1_MEM1_Type MEM1;  /* 0x00D0 - temporary memory 1 */
    PTC1_MEM2_Type MEM2;  /* 0x00D4 - temporary memory 2 */
    PTC1_MEM3_Type MEM3;  /* 0x00D8 - temporary memory 3 */
    PTC1_MEM4_Type MEM4;  /* 0x00DC - temporary memory 4 */
    PTC1_GPIO31_0_Type GPIO31_0;  /* 0x00E0 - GPIO31_0 */
    PTC1_GPIO63_32_Type GPIO63_32;  /* 0x00E4 - GPIO63_32 */
    PTC1_GPIO95_64_Type GPIO95_64;  /* 0x00E8 - GPIO95_64 */
} PTC1_TypeDef;

/* ============================================================================ */
/* DMAC1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=DMAC1,base=0x50081000,description="",groupName=DMAC */

#define DMAC1_BASE                                         (0x50081000UL)

/* @SVD:register=ISR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="ISR" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GIF1,bitOffset=0,bitWidth=1,description="channel global interrupt flag. Set when any of TEIF/HTIF/TCIF asserted. Cleared when TEIF/HTIF/TCIF all cleared." */
        volatile uint32_t GIF1: 1;  /* channel global interrupt flag. Set when any of TEIF/HTIF/TCI */
        /* @SVD:field=TCIF1,bitOffset=1,bitWidth=1,description="channel transfer complete flag. Set when all NDT are transferred.  Cleared when write 1 to CTCIF or CGIF." */
        volatile uint32_t TCIF1: 1;  /* channel transfer complete flag. Set when all NDT are transfe */
        /* @SVD:field=HTIF1,bitOffset=2,bitWidth=1,description="channel half transfer flag. Set when half NDT are transferred. Cleared when write 1 to CHTIF or CGIF." */
        volatile uint32_t HTIF1: 1;  /* channel half transfer flag. Set when half NDT are transferre */
        /* @SVD:field=TEIF1,bitOffset=3,bitWidth=1,description="channel transfer error flag. Set when bus error detected. Cleared when write 1 to CTEIF or CGIF." */
        volatile uint32_t TEIF1: 1;  /* channel transfer error flag. Set when bus error detected. Cl */
        /* @SVD:field=GIF2,bitOffset=4,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF2: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF2,bitOffset=5,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF2: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF2,bitOffset=6,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF2: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF2,bitOffset=7,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF2: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF3,bitOffset=8,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF3: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF3,bitOffset=9,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF3: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF3,bitOffset=10,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF3: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF3,bitOffset=11,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF3: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF4,bitOffset=12,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF4: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF4,bitOffset=13,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF4: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF4,bitOffset=14,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF4: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF4,bitOffset=15,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF4: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF5,bitOffset=16,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF5: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF5,bitOffset=17,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF5: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF5,bitOffset=18,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF5: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF5,bitOffset=19,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF5: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF6,bitOffset=20,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF6: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF6,bitOffset=21,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF6: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF6,bitOffset=22,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF6: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF6,bitOffset=23,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF6: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF7,bitOffset=24,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF7: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF7,bitOffset=25,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF7: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF7,bitOffset=26,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF7: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF7,bitOffset=27,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF7: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF8,bitOffset=28,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF8: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF8,bitOffset=29,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF8: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF8,bitOffset=30,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF8: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF8,bitOffset=31,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF8: 1;  /* channel transfer error flag */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_ISR_Type;

/* @SVD:register=IFCR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="IFCR" */
/* IFCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CGIF1,bitOffset=0,bitWidth=1,description="CGIF,  global interrupt flag clear. Write 1 to clear all TEIF/HTIF/TCIF." */
        volatile uint32_t CGIF1: 1;  /* CGIF,  global interrupt flag clear. Write 1 to clear all TEI */
        /* @SVD:field=CTCIF1,bitOffset=1,bitWidth=1,description="CTCIF, transfer complete flag clear. Write 1 to clear TCIF." */
        volatile uint32_t CTCIF1: 1;  /* CTCIF, transfer complete flag clear. Write 1 to clear TCIF. */
        /* @SVD:field=CHTIF1,bitOffset=2,bitWidth=1,description="CHTIF, half transfer flag clear. Write 1 to clear HTIF." */
        volatile uint32_t CHTIF1: 1;  /* CHTIF, half transfer flag clear. Write 1 to clear HTIF. */
        /* @SVD:field=CTEIF1,bitOffset=3,bitWidth=1,description="CTEIF, transfer error flag clear. Write 1 to clear TEIF." */
        volatile uint32_t CTEIF1: 1;  /* CTEIF, transfer error flag clear. Write 1 to clear TEIF. */
        /* @SVD:field=CGIF2,bitOffset=4,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF2: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF2,bitOffset=5,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF2: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF2,bitOffset=6,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF2: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF2,bitOffset=7,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF2: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF3,bitOffset=8,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF3: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF3,bitOffset=9,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF3: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF3,bitOffset=10,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF3: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF3,bitOffset=11,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF3: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF4,bitOffset=12,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF4: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF4,bitOffset=13,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF4: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF4,bitOffset=14,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF4: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF4,bitOffset=15,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF4: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF5,bitOffset=16,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF5: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF5,bitOffset=17,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF5: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF5,bitOffset=18,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF5: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF5,bitOffset=19,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF5: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF6,bitOffset=20,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF6: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF6,bitOffset=21,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF6: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF6,bitOffset=22,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF6: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF6,bitOffset=23,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF6: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF7,bitOffset=24,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF7: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF7,bitOffset=25,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF7: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF7,bitOffset=26,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF7: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF7,bitOffset=27,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF7: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF8,bitOffset=28,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF8: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF8,bitOffset=29,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF8: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF8,bitOffset=30,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF8: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF8,bitOffset=31,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF8: 1;  /* CTEIF, transfer error flag clear */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_IFCR_Type;

/* @SVD:register=CCR1,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="CCR1" */
/* CCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CCR1_Type;

/* @SVD:register=CNDTR1,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="CNDTR1" */
/* CNDTR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CNDTR1_Type;

/* @SVD:register=CPAR1,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="CPAR1" */
/* CPAR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CPAR1_Type;

/* @SVD:register=CM0AR1,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="CM0AR1" */
/* CM0AR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="memory address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* memory address               It contains the base address of */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CM0AR1_Type;

/* @SVD:register=CBSR1,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="CBSR1" */
/* CBSR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CBSR1_Type;

/* @SVD:register=CCR2,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="CCR2" */
/* CCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CCR2_Type;

/* @SVD:register=CNDTR2,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="CNDTR2" */
/* CNDTR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CNDTR2_Type;

/* @SVD:register=CPAR2,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="CPAR2" */
/* CPAR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CPAR2_Type;

/* @SVD:register=CM0AR2,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="CM0AR2" */
/* CM0AR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CM0AR2_Type;

/* @SVD:register=CBSR2,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="CBSR2" */
/* CBSR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CBSR2_Type;

/* @SVD:register=CCR3,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="CCR3" */
/* CCR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CCR3_Type;

/* @SVD:register=CNDTR3,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="CNDTR3" */
/* CNDTR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CNDTR3_Type;

/* @SVD:register=CPAR3,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="CPAR3" */
/* CPAR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CPAR3_Type;

/* @SVD:register=CM0AR3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="CM0AR3" */
/* CM0AR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CM0AR3_Type;

/* @SVD:register=CBSR3,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="CBSR3" */
/* CBSR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CBSR3_Type;

/* @SVD:register=CCR4,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="CCR4" */
/* CCR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CCR4_Type;

/* @SVD:register=CNDTR4,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="CNDTR4" */
/* CNDTR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CNDTR4_Type;

/* @SVD:register=CPAR4,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="CPAR4" */
/* CPAR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CPAR4_Type;

/* @SVD:register=CM0AR4,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="CM0AR4" */
/* CM0AR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CM0AR4_Type;

/* @SVD:register=CBSR4,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="CBSR4" */
/* CBSR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CBSR4_Type;

/* @SVD:register=CCR5,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="CCR5" */
/* CCR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CCR5_Type;

/* @SVD:register=CNDTR5,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="CNDTR5" */
/* CNDTR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CNDTR5_Type;

/* @SVD:register=CPAR5,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="CPAR5" */
/* CPAR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CPAR5_Type;

/* @SVD:register=CM0AR5,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="CM0AR5" */
/* CM0AR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CM0AR5_Type;

/* @SVD:register=CBSR5,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="CBSR5" */
/* CBSR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CBSR5_Type;

/* @SVD:register=CCR6,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="CCR6" */
/* CCR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CCR6_Type;

/* @SVD:register=CNDTR6,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="CNDTR6" */
/* CNDTR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CNDTR6_Type;

/* @SVD:register=CPAR6,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="CPAR6" */
/* CPAR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CPAR6_Type;

/* @SVD:register=CM0AR6,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="CM0AR6" */
/* CM0AR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CM0AR6_Type;

/* @SVD:register=CBSR6,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="CBSR6" */
/* CBSR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CBSR6_Type;

/* @SVD:register=CCR7,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="CCR7" */
/* CCR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CCR7_Type;

/* @SVD:register=CNDTR7,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="CNDTR7" */
/* CNDTR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CNDTR7_Type;

/* @SVD:register=CPAR7,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="CPAR7" */
/* CPAR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CPAR7_Type;

/* @SVD:register=CM0AR7,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="CM0AR7" */
/* CM0AR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CM0AR7_Type;

/* @SVD:register=CBSR7,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="CBSR7" */
/* CBSR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non memory-to-memory mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non memory-to-memory mode               When B */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CBSR7_Type;

/* @SVD:register=CCR8,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="CCR8" */
/* CCR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CCR8_Type;

/* @SVD:register=CNDTR8,offset=0x0098,size=32,access=read-write,reset=0x00000000,description="CNDTR8" */
/* CNDTR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CNDTR8_Type;

/* @SVD:register=CPAR8,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="CPAR8" */
/* CPAR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CPAR8_Type;

/* @SVD:register=CM0AR8,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="CM0AR8" */
/* CM0AR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CM0AR8_Type;

/* @SVD:register=CBSR8,offset=0x00A4,size=32,access=read-write,reset=0x00000000,description="CBSR8" */
/* CBSR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CBSR8_Type;

/* @SVD:register=CSELR1,offset=0x00A8,size=32,access=read-write,reset=0x00000000,description="CSELR1" */
/* CSELR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=C1S,bitOffset=0,bitWidth=6,description="DMA channel 1 selection" */
        volatile uint32_t C1S: 6;  /* DMA channel 1 selection */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=C2S,bitOffset=8,bitWidth=6,description="DMA channel 2 selection" */
        volatile uint32_t C2S: 6;  /* DMA channel 2 selection */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=C3S,bitOffset=16,bitWidth=6,description="DMA channel 3 selection" */
        volatile uint32_t C3S: 6;  /* DMA channel 3 selection */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=C4S,bitOffset=24,bitWidth=6,description="DMA channel 4 selection" */
        volatile uint32_t C4S: 6;  /* DMA channel 4 selection */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CSELR1_Type;

/* @SVD:register=CSELR2,offset=0x00AC,size=32,access=read-write,reset=0x00000000,description="CSELR2" */
/* CSELR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=C5S,bitOffset=0,bitWidth=6,description="DMA channel 5 selection" */
        volatile uint32_t C5S: 6;  /* DMA channel 5 selection */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=C6S,bitOffset=8,bitWidth=6,description="DMA channel 6 selection" */
        volatile uint32_t C6S: 6;  /* DMA channel 6 selection */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=C7S,bitOffset=16,bitWidth=6,description="DMA channel 7 selection" */
        volatile uint32_t C7S: 6;  /* DMA channel 7 selection */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=C8S,bitOffset=24,bitWidth=6,description="DMA channel 8 selection" */
        volatile uint32_t C8S: 6;  /* DMA channel 8 selection */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC1_CSELR2_Type;

/* DMAC1 寄存器结构体 */
typedef struct
{
    DMAC1_ISR_Type ISR;  /* 0x0000 - ISR */
    DMAC1_IFCR_Type IFCR;  /* 0x0004 - IFCR */
    DMAC1_CCR1_Type CCR1;  /* 0x0008 - CCR1 */
    DMAC1_CNDTR1_Type CNDTR1;  /* 0x000C - CNDTR1 */
    DMAC1_CPAR1_Type CPAR1;  /* 0x0010 - CPAR1 */
    DMAC1_CM0AR1_Type CM0AR1;  /* 0x0014 - CM0AR1 */
    DMAC1_CBSR1_Type CBSR1;  /* 0x0018 - CBSR1 */
    DMAC1_CCR2_Type CCR2;  /* 0x001C - CCR2 */
    DMAC1_CNDTR2_Type CNDTR2;  /* 0x0020 - CNDTR2 */
    DMAC1_CPAR2_Type CPAR2;  /* 0x0024 - CPAR2 */
    DMAC1_CM0AR2_Type CM0AR2;  /* 0x0028 - CM0AR2 */
    DMAC1_CBSR2_Type CBSR2;  /* 0x002C - CBSR2 */
    DMAC1_CCR3_Type CCR3;  /* 0x0030 - CCR3 */
    DMAC1_CNDTR3_Type CNDTR3;  /* 0x0034 - CNDTR3 */
    DMAC1_CPAR3_Type CPAR3;  /* 0x0038 - CPAR3 */
    DMAC1_CM0AR3_Type CM0AR3;  /* 0x003C - CM0AR3 */
    DMAC1_CBSR3_Type CBSR3;  /* 0x0040 - CBSR3 */
    DMAC1_CCR4_Type CCR4;  /* 0x0044 - CCR4 */
    DMAC1_CNDTR4_Type CNDTR4;  /* 0x0048 - CNDTR4 */
    DMAC1_CPAR4_Type CPAR4;  /* 0x004C - CPAR4 */
    DMAC1_CM0AR4_Type CM0AR4;  /* 0x0050 - CM0AR4 */
    DMAC1_CBSR4_Type CBSR4;  /* 0x0054 - CBSR4 */
    DMAC1_CCR5_Type CCR5;  /* 0x0058 - CCR5 */
    DMAC1_CNDTR5_Type CNDTR5;  /* 0x005C - CNDTR5 */
    DMAC1_CPAR5_Type CPAR5;  /* 0x0060 - CPAR5 */
    DMAC1_CM0AR5_Type CM0AR5;  /* 0x0064 - CM0AR5 */
    DMAC1_CBSR5_Type CBSR5;  /* 0x0068 - CBSR5 */
    DMAC1_CCR6_Type CCR6;  /* 0x006C - CCR6 */
    DMAC1_CNDTR6_Type CNDTR6;  /* 0x0070 - CNDTR6 */
    DMAC1_CPAR6_Type CPAR6;  /* 0x0074 - CPAR6 */
    DMAC1_CM0AR6_Type CM0AR6;  /* 0x0078 - CM0AR6 */
    DMAC1_CBSR6_Type CBSR6;  /* 0x007C - CBSR6 */
    DMAC1_CCR7_Type CCR7;  /* 0x0080 - CCR7 */
    DMAC1_CNDTR7_Type CNDTR7;  /* 0x0084 - CNDTR7 */
    DMAC1_CPAR7_Type CPAR7;  /* 0x0088 - CPAR7 */
    DMAC1_CM0AR7_Type CM0AR7;  /* 0x008C - CM0AR7 */
    DMAC1_CBSR7_Type CBSR7;  /* 0x0090 - CBSR7 */
    DMAC1_CCR8_Type CCR8;  /* 0x0094 - CCR8 */
    DMAC1_CNDTR8_Type CNDTR8;  /* 0x0098 - CNDTR8 */
    DMAC1_CPAR8_Type CPAR8;  /* 0x009C - CPAR8 */
    DMAC1_CM0AR8_Type CM0AR8;  /* 0x00A0 - CM0AR8 */
    DMAC1_CBSR8_Type CBSR8;  /* 0x00A4 - CBSR8 */
    DMAC1_CSELR1_Type CSELR1;  /* 0x00A8 - CSELR1 */
    DMAC1_CSELR2_Type CSELR2;  /* 0x00AC - CSELR2 */
} DMAC1_TypeDef;

/* ============================================================================ */
/* USART1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=USART1,base=0x50084000,description="",groupName=USART */

#define USART1_BASE                                        (0x50084000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control Register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UE,bitOffset=0,bitWidth=1,description="USART enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t UE: 1;  /* USART enable               0: disabled               1: enab */
        /* @SVD:field=RSVD9,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RE,bitOffset=2,bitWidth=1,description="Receiver enable\n              0: receiver is disabled\n              1: receiver is enabled" */
        volatile uint32_t RE: 1;  /* Receiver enable               0: receiver is disabled        */
        /* @SVD:field=TE,bitOffset=3,bitWidth=1,description="Transmitter enable\n              0: transmitter is disabled\n              1: transmitter is enabled" */
        volatile uint32_t TE: 1;  /* Transmitter enable               0: transmitter is disabled  */
        /* @SVD:field=IDLEIE,bitOffset=4,bitWidth=1,description="Idle line interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever IDLE=1 in the ISR  register" */
        volatile uint32_t IDLEIE: 1;  /* Idle line interrupt enable               0: interrupt disabl */
        /* @SVD:field=RXNEIE,bitOffset=5,bitWidth=1,description="Rx not empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever RXNE=1 in the ISR register" */
        volatile uint32_t RXNEIE: 1;  /* Rx not empty interrupt enable               0: interrupt dis */
        /* @SVD:field=TCIE,bitOffset=6,bitWidth=1,description="Transfer compelete interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever TC=1 in the ISR register" */
        volatile uint32_t TCIE: 1;  /* Transfer compelete interrupt enable               0: interru */
        /* @SVD:field=TXEIE,bitOffset=7,bitWidth=1,description="Tx empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenver TXE=1 in the ISR register" */
        volatile uint32_t TXEIE: 1;  /* Tx empty interrupt enable               0: interrupt disable */
        /* @SVD:field=PEIE,bitOffset=8,bitWidth=1,description="Parity error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever PE=1 in the ISR register" */
        volatile uint32_t PEIE: 1;  /* Parity error interrupt enable               0: interrupt dis */
        /* @SVD:field=PS,bitOffset=9,bitWidth=1,description="Parity select\n              0: even parity\n              1: odd parity" */
        volatile uint32_t PS: 1;  /* Parity select               0: even parity               1:  */
        /* @SVD:field=PCE,bitOffset=10,bitWidth=1,description="Parity check enable. If enabled, parity bit is inserted at the MSB position \n              0: parity check disabled\n              1: parity check enabled" */
        volatile uint32_t PCE: 1;  /* Parity check enable. If enabled, parity bit is inserted at t */
        /* @SVD:field=RSVD8,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=OVER8,bitOffset=14,bitWidth=1,description="Oversampling mode\n              0: Oversampling by 16\n              1: Oversampling by 8" */
        volatile uint32_t OVER8: 1;  /* Oversampling mode               0: Oversampling by 16        */
        /* @SVD:field=RSVD5,bitOffset=15,bitWidth=5,description="" */
        volatile uint32_t RSVD5: 5;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=5,description="" */
        volatile uint32_t RSVD4: 5;
        /* @SVD:field=RSVD3,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=M,bitOffset=27,bitWidth=2,description="Mode bit indicates the length of the packet, including data bits and parity. Stop bits not included.\n              0: 6 bits (e.g. 6 data bits + no parity bit)\n              1: 7 bits (e.g. 6 data bits + 1 parity bit)\n              2: 8 bits (e.g. 7 data bits + 1 parity bit, or 6 data bits + 2 parity bits)\n              3: 9 bits (e.g. 8 data bits + 1 parity bit, or 7 data bits + 2 parity bits)" */
        volatile uint32_t M: 2;  /* Mode bit indicates the length of the packet, including data  */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Control Register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD19,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD19: 4;
        /* @SVD:field=RSVD18,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD18: 1;
        /* @SVD:field=RSVD17,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD17: 1;
        /* @SVD:field=RSVD16,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=RSVD15,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=STOP,bitOffset=12,bitWidth=2,description="Stop bits\n              0/1: 1 stop bit\n              2/3: 2 stop bits" */
        volatile uint32_t STOP: 2;  /* Stop bits               0/1: 1 stop bit               2/3: 2 */
        /* @SVD:field=RSVD10,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=21,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_CR2_Type;

/* @SVD:register=CR3,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Control Register 3" */
/* CR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EIE,bitOffset=0,bitWidth=1,description="Error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever FE=1 or ORE=1 or NF=1 in the ISR register" */
        volatile uint32_t EIE: 1;  /* Error interrupt enable               0: interrupt disabled   */
        /* @SVD:field=RSVD15,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=DMAR,bitOffset=6,bitWidth=1,description="Receiver DMA enable\n              0: DMA mode disabled for reception\n              1: DMA mode enabled for reception" */
        volatile uint32_t DMAR: 1;  /* Receiver DMA enable               0: DMA mode disabled for r */
        /* @SVD:field=DMAT,bitOffset=7,bitWidth=1,description="Transmitter DMA enable\n              0: DMA mode disabled for transmission\n              1: DMA mode enabled for transmission" */
        volatile uint32_t DMAT: 1;  /* Transmitter DMA enable               0: DMA mode disabled fo */
        /* @SVD:field=RTSE,bitOffset=8,bitWidth=1,description="RTS enable\n              0: RTS hardware flow control disabled\n              1: RTS hardware flow control enabled, RTS output is asserted low when new data can be received" */
        volatile uint32_t RTSE: 1;  /* RTS enable               0: RTS hardware flow control disabl */
        /* @SVD:field=CTSE,bitOffset=9,bitWidth=1,description="CTS enable\n              0: CTS hardware flow control disabled\n              1: CTS hardware flow control enabled, data is transmitted only when CTS input is asserted low" */
        volatile uint32_t CTSE: 1;  /* CTS enable               0: CTS hardware flow control disabl */
        /* @SVD:field=CTSIE,bitOffset=10,bitWidth=1,description="CTS interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever CTSIF=1 in the ISR register" */
        volatile uint32_t CTSIE: 1;  /* CTS interrupt enable               0: interrupt disabled     */
        /* @SVD:field=ONEBIT,bitOffset=11,bitWidth=1,description="One bit sampling mode\n              0: 3-bit sampling mode, the sampling value is determined by the voted result out of 3 bits\n              1: 1-bit sampling mode" */
        volatile uint32_t ONEBIT: 1;  /* One bit sampling mode               0: 3-bit sampling mode,  */
        /* @SVD:field=OVRDIS,bitOffset=12,bitWidth=1,description="Overrun disable\n              0: overrun error flag (ORE) will be set if new data received but previous data not read. New data will not overwrite the content in RDR register.\n              1: overrun disabled. If new data is received before previous data is read, the new data will overwrite the content in RDR register and ORE flag remains unset." */
        volatile uint32_t OVRDIS: 1;  /* Overrun disable               0: overrun error flag (ORE) wi */
        /* @SVD:field=RSVD10,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=17,bitWidth=3,description="" */
        volatile uint32_t RSVD6: 3;
        /* @SVD:field=RSVD5,bitOffset=20,bitWidth=2,description="" */
        volatile uint32_t RSVD5: 2;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=7,description="" */
        volatile uint32_t RSVD: 7;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_CR3_Type;

/* @SVD:register=BRR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Baud Rate Register" */
/* BRR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FRAC,bitOffset=0,bitWidth=4,description="Fractional part of baud rate prescaler" */
        volatile uint32_t FRAC: 4;  /* Fractional part of baud rate prescaler */
        /* @SVD:field=INT,bitOffset=4,bitWidth=12,description="Integer part of baud rate prescaler\n              If OVER8 = 0, Baud Rate = 48000000 / (INT + FRAC/16) / 16\n              If OVER8 = 1, Baud Rate = 48000000 / (INT + FRAC/16) / 8\n              For example:\n              OVER=0, INT=3, FRAC=0, Baud Rate = 48000000/(3+0)/16 = 1Mbps\n              OVER=0, INT=3, FRAC=4, Baud Rate = 48000000/(3+4/16)/16 = 923077 = 921600 + 1.6‰\n              OVER=1, INT=52, FRAC=1, Baud Rate = 48000000/(52+1/16)/8 = 115246 = 115200 + 0.4‰" */
        volatile uint32_t INT: 12;  /* Integer part of baud rate prescaler               If OVER8 = */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_BRR_Type;

/* @SVD:register=RQR,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="Request Register" */
/* RQR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RXFRQ,bitOffset=3,bitWidth=1,description="Rx data flush request. Write 1 to clear the RXNE flag and discard the current data in RDR" */
        volatile uint32_t RXFRQ: 1;  /* Rx data flush request. Write 1 to clear the RXNE flag and di */
        /* @SVD:field=TXFRQ,bitOffset=4,bitWidth=1,description="Tx data flush requestReserved-Do not modify" */
        volatile uint32_t TXFRQ: 1;  /* Tx data flush requestReserved-Do not modify */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_RQR_Type;

/* @SVD:register=ISR,offset=0x001C,size=32,access=read-write,reset=0x020000C0,description="Interrupt and Status Register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PE,bitOffset=0,bitWidth=1,description="Parity error. This bit is set when a parity error is detected in the received packet.\n              0: no parity error\n              1: parity error detected" */
        volatile uint32_t PE: 1;  /* Parity error. This bit is set when a parity error is detecte */
        /* @SVD:field=FE,bitOffset=1,bitWidth=1,description="Framing error. This bit is set by hardware when stop bit is not correctly received\n              0: no framing error is detected\n              1: framing error is detected" */
        volatile uint32_t FE: 1;  /* Framing error. This bit is set by hardware when stop bit is  */
        /* @SVD:field=NF,bitOffset=2,bitWidth=1,description="Noise flag. Noise means the samping values in the 3-bit sampling mode are not the same.\n              0: no noise is detected\n              1: noise is detected" */
        volatile uint32_t NF: 1;  /* Noise flag. Noise means the samping values in the 3-bit samp */
        /* @SVD:field=ORE,bitOffset=3,bitWidth=1,description="Overrun error. When new data is received but Rx buffer is not empty (i.e. previous data is not read yet), ORE is asserted and current RDR content is not lost. This feature can be disabled by set CR3_OVRDIS to 1.\n              0: no overrun error\n              1: overrun error is detected" */
        volatile uint32_t ORE: 1;  /* Overrun error. When new data is received but Rx buffer is no */
        /* @SVD:field=IDLE,bitOffset=4,bitWidth=1,description="Idle line detected\n              0: no idle line is detected\n              1: idle line is detected" */
        volatile uint32_t IDLE: 1;  /* Idle line detected               0: no idle line is detected */
        /* @SVD:field=RXNE,bitOffset=5,bitWidth=1,description="Rx data not empty. This bit is set by hardware when the received data is transferred into RDR register.\n              0: data is not received\n              1: data is ready in RDR to be read" */
        volatile uint32_t RXNE: 1;  /* Rx data not empty. This bit is set by hardware when the rece */
        /* @SVD:field=TC,bitOffset=6,bitWidth=1,description="transmission complete. This bit is set by hardware if the transmission is complete\n              0: transmission is not complete\n              1: transmission is complete" */
        volatile uint32_t TC: 1;  /* transmission complete. This bit is set by hardware if the tr */
        /* @SVD:field=TXE,bitOffset=7,bitWidth=1,description="Tx data empty\n              0: data is ready in TDR\n              1: data is already transferred to shift register, i.e. transmission is in progress or complete" */
        volatile uint32_t TXE: 1;  /* Tx data empty               0: data is ready in TDR          */
        /* @SVD:field=RSVD16,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=CTSIF,bitOffset=9,bitWidth=1,description="CTS interrupt flag. This bit is set by hardware whenever CTS input toggles.\n              0: no change on the CTS line\n              1: there is a change on the CTS line" */
        volatile uint32_t CTSIF: 1;  /* CTS interrupt flag. This bit is set by hardware whenever CTS */
        /* @SVD:field=CTS,bitOffset=10,bitWidth=1,description="CTS input. Read this bit to get the raw status of the CTS line." */
        volatile uint32_t CTS: 1;  /* CTS input. Read this bit to get the raw status of the CTS li */
        /* @SVD:field=RSVD15,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=RSVD10,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_ISR_Type;

/* @SVD:register=ICR,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Interrupt flag Clear Register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PECF,bitOffset=0,bitWidth=1,description="Parity error clear flag. Wriring 1 to this bit clears the PE flag in the ISR register." */
        volatile uint32_t PECF: 1;  /* Parity error clear flag. Wriring 1 to this bit clears the PE */
        /* @SVD:field=FECF,bitOffset=1,bitWidth=1,description="Framing error clear flag. Writing 1 to this bit clears the FE flag in the ISR register." */
        volatile uint32_t FECF: 1;  /* Framing error clear flag. Writing 1 to this bit clears the F */
        /* @SVD:field=NCF,bitOffset=2,bitWidth=1,description="Noise detected clear flag. Writing 1 to this bit clears the NF flag in the ISR register." */
        volatile uint32_t NCF: 1;  /* Noise detected clear flag. Writing 1 to this bit clears the  */
        /* @SVD:field=ORECF,bitOffset=3,bitWidth=1,description="Overrun error clear flag. Writing 1 to this bit clears the ORE flag in the ISR register." */
        volatile uint32_t ORECF: 1;  /* Overrun error clear flag. Writing 1 to this bit clears the O */
        /* @SVD:field=IDLECF,bitOffset=4,bitWidth=1,description="Idle line detected clear flag. Writing 1 to this bit clears the IDLECF flag in the ISR register." */
        volatile uint32_t IDLECF: 1;  /* Idle line detected clear flag. Writing 1 to this bit clears  */
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=TCCF,bitOffset=6,bitWidth=1,description="Transmission complete clear flag. Writing 1 to this bit clears the TC flag in the ISR register." */
        volatile uint32_t TCCF: 1;  /* Transmission complete clear flag. Writing 1 to this bit clea */
        /* @SVD:field=RSVD10,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=CTSCF,bitOffset=9,bitWidth=1,description="CTS clear flag. Writing 1 to this bit clears the CTSIF flag in the ISR register." */
        volatile uint32_t CTSCF: 1;  /* CTS clear flag. Writing 1 to this bit clears the CTSIF flag  */
        /* @SVD:field=RSVD8,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=13,bitWidth=4,description="" */
        volatile uint32_t RSVD5: 4;
        /* @SVD:field=RSVD4,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=18,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_ICR_Type;

/* @SVD:register=RDR,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Receive Data Register" */
/* RDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RDR,bitOffset=0,bitWidth=9,description="Received data" */
        volatile uint32_t RDR: 9;  /* Received data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_RDR_Type;

/* @SVD:register=TDR,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Transmit Data Register" */
/* TDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TDR,bitOffset=0,bitWidth=9,description="Transmit data" */
        volatile uint32_t TDR: 9;  /* Transmit data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_TDR_Type;

/* @SVD:register=MISCR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Miscellaneous Register" */
/* MISCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SMPLINI,bitOffset=0,bitWidth=4,description="initial sample count, count down from this value to zero to reach the middle of the start bit in RxReserved-Do not modify" */
        volatile uint32_t SMPLINI: 4;  /* initial sample count, count down from this value to zero to  */
        /* @SVD:field=RTSBIT,bitOffset=4,bitWidth=4,description="assert RTS ahead of the frame completion (in number of bits)Reserved-Do not modify" */
        volatile uint32_t RTSBIT: 4;  /* assert RTS ahead of the frame completion (in number of bits) */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
        /* @SVD:field=AUTOCAL,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t AUTOCAL: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_MISCR_Type;

/* @SVD:register=DRDR,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DRDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_DRDR_Type;

/* @SVD:register=DTDR,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DTDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_DTDR_Type;

/* @SVD:register=EXR,offset=0x0038,size=32,access=read-write,reset=0x00000001,description="Mutual Exclusive Register" */
/* EXR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BUSY,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t BUSY: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=ID,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t ID: 1;
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART1_EXR_Type;

/* USART1 寄存器结构体 */
typedef struct
{
    USART1_CR1_Type CR1;  /* 0x0000 - Control Register 1 */
    USART1_CR2_Type CR2;  /* 0x0004 - Control Register 2 */
    USART1_CR3_Type CR3;  /* 0x0008 - Control Register 3 */
    USART1_BRR_Type BRR;  /* 0x000C - Baud Rate Register */
    /* @SVD:register=RSVD1,offset=0x0010,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0010 - RSVD1 */
    volatile uint32_t RESERVED5[1];  /* 0x0014 - 0x0017 */
    USART1_RQR_Type RQR;  /* 0x0018 - Request Register */
    USART1_ISR_Type ISR;  /* 0x001C - Interrupt and Status Register */
    USART1_ICR_Type ICR;  /* 0x0020 - Interrupt flag Clear Register */
    USART1_RDR_Type RDR;  /* 0x0024 - Receive Data Register */
    USART1_TDR_Type TDR;  /* 0x0028 - Transmit Data Register */
    USART1_MISCR_Type MISCR;  /* 0x002C - Miscellaneous Register */
    USART1_DRDR_Type DRDR;  /* 0x0030 - Debug Receive Data Register */
    USART1_DTDR_Type DTDR;  /* 0x0034 - Debug Receive Data Register */
    USART1_EXR_Type EXR;  /* 0x0038 - Mutual Exclusive Register */
} USART1_TypeDef;

/* ============================================================================ */
/* USART2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=USART2,base=0x50085000,description="",groupName=USART */

#define USART2_BASE                                        (0x50085000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control Register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UE,bitOffset=0,bitWidth=1,description="USART enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t UE: 1;  /* USART enable               0: disabled               1: enab */
        /* @SVD:field=RSVD9,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RE,bitOffset=2,bitWidth=1,description="Receiver enable\n              0: receiver is disabled\n              1: receiver is enabled" */
        volatile uint32_t RE: 1;  /* Receiver enable               0: receiver is disabled        */
        /* @SVD:field=TE,bitOffset=3,bitWidth=1,description="Transmitter enable\n              0: transmitter is disabled\n              1: transmitter is enabled" */
        volatile uint32_t TE: 1;  /* Transmitter enable               0: transmitter is disabled  */
        /* @SVD:field=IDLEIE,bitOffset=4,bitWidth=1,description="Idle line interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever IDLE=1 in the ISR  register" */
        volatile uint32_t IDLEIE: 1;  /* Idle line interrupt enable               0: interrupt disabl */
        /* @SVD:field=RXNEIE,bitOffset=5,bitWidth=1,description="Rx not empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever RXNE=1 in the ISR register" */
        volatile uint32_t RXNEIE: 1;  /* Rx not empty interrupt enable               0: interrupt dis */
        /* @SVD:field=TCIE,bitOffset=6,bitWidth=1,description="Transfer compelete interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever TC=1 in the ISR register" */
        volatile uint32_t TCIE: 1;  /* Transfer compelete interrupt enable               0: interru */
        /* @SVD:field=TXEIE,bitOffset=7,bitWidth=1,description="Tx empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenver TXE=1 in the ISR register" */
        volatile uint32_t TXEIE: 1;  /* Tx empty interrupt enable               0: interrupt disable */
        /* @SVD:field=PEIE,bitOffset=8,bitWidth=1,description="Parity error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever PE=1 in the ISR register" */
        volatile uint32_t PEIE: 1;  /* Parity error interrupt enable               0: interrupt dis */
        /* @SVD:field=PS,bitOffset=9,bitWidth=1,description="Parity select\n              0: even parity\n              1: odd parity" */
        volatile uint32_t PS: 1;  /* Parity select               0: even parity               1:  */
        /* @SVD:field=PCE,bitOffset=10,bitWidth=1,description="Parity check enable. If enabled, parity bit is inserted at the MSB position \n              0: parity check disabled\n              1: parity check enabled" */
        volatile uint32_t PCE: 1;  /* Parity check enable. If enabled, parity bit is inserted at t */
        /* @SVD:field=RSVD8,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=OVER8,bitOffset=14,bitWidth=1,description="Oversampling mode\n              0: Oversampling by 16\n              1: Oversampling by 8" */
        volatile uint32_t OVER8: 1;  /* Oversampling mode               0: Oversampling by 16        */
        /* @SVD:field=RSVD5,bitOffset=15,bitWidth=5,description="" */
        volatile uint32_t RSVD5: 5;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=5,description="" */
        volatile uint32_t RSVD4: 5;
        /* @SVD:field=RSVD3,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=M,bitOffset=27,bitWidth=2,description="Mode bit indicates the length of the packet, including data bits and parity. Stop bits not included.\n              0: 6 bits (e.g. 6 data bits + no parity bit)\n              1: 7 bits (e.g. 6 data bits + 1 parity bit)\n              2: 8 bits (e.g. 7 data bits + 1 parity bit, or 6 data bits + 2 parity bits)\n              3: 9 bits (e.g. 8 data bits + 1 parity bit, or 7 data bits + 2 parity bits)" */
        volatile uint32_t M: 2;  /* Mode bit indicates the length of the packet, including data  */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Control Register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD19,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD19: 4;
        /* @SVD:field=RSVD18,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD18: 1;
        /* @SVD:field=RSVD17,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD17: 1;
        /* @SVD:field=RSVD16,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=RSVD15,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=STOP,bitOffset=12,bitWidth=2,description="Stop bits\n              0/1: 1 stop bit\n              2/3: 2 stop bits" */
        volatile uint32_t STOP: 2;  /* Stop bits               0/1: 1 stop bit               2/3: 2 */
        /* @SVD:field=RSVD10,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=21,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_CR2_Type;

/* @SVD:register=CR3,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Control Register 3" */
/* CR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EIE,bitOffset=0,bitWidth=1,description="Error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever FE=1 or ORE=1 or NF=1 in the ISR register" */
        volatile uint32_t EIE: 1;  /* Error interrupt enable               0: interrupt disabled   */
        /* @SVD:field=RSVD15,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=DMAR,bitOffset=6,bitWidth=1,description="Receiver DMA enable\n              0: DMA mode disabled for reception\n              1: DMA mode enabled for reception" */
        volatile uint32_t DMAR: 1;  /* Receiver DMA enable               0: DMA mode disabled for r */
        /* @SVD:field=DMAT,bitOffset=7,bitWidth=1,description="Transmitter DMA enable\n              0: DMA mode disabled for transmission\n              1: DMA mode enabled for transmission" */
        volatile uint32_t DMAT: 1;  /* Transmitter DMA enable               0: DMA mode disabled fo */
        /* @SVD:field=RTSE,bitOffset=8,bitWidth=1,description="RTS enable\n              0: RTS hardware flow control disabled\n              1: RTS hardware flow control enabled, RTS output is asserted low when new data can be received" */
        volatile uint32_t RTSE: 1;  /* RTS enable               0: RTS hardware flow control disabl */
        /* @SVD:field=CTSE,bitOffset=9,bitWidth=1,description="CTS enable\n              0: CTS hardware flow control disabled\n              1: CTS hardware flow control enabled, data is transmitted only when CTS input is asserted low" */
        volatile uint32_t CTSE: 1;  /* CTS enable               0: CTS hardware flow control disabl */
        /* @SVD:field=CTSIE,bitOffset=10,bitWidth=1,description="CTS interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever CTSIF=1 in the ISR register" */
        volatile uint32_t CTSIE: 1;  /* CTS interrupt enable               0: interrupt disabled     */
        /* @SVD:field=ONEBIT,bitOffset=11,bitWidth=1,description="One bit sampling mode\n              0: 3-bit sampling mode, the sampling value is determined by the voted result out of 3 bits\n              1: 1-bit sampling mode" */
        volatile uint32_t ONEBIT: 1;  /* One bit sampling mode               0: 3-bit sampling mode,  */
        /* @SVD:field=OVRDIS,bitOffset=12,bitWidth=1,description="Overrun disable\n              0: overrun error flag (ORE) will be set if new data received but previous data not read. New data will not overwrite the content in RDR register.\n              1: overrun disabled. If new data is received before previous data is read, the new data will overwrite the content in RDR register and ORE flag remains unset." */
        volatile uint32_t OVRDIS: 1;  /* Overrun disable               0: overrun error flag (ORE) wi */
        /* @SVD:field=RSVD10,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=17,bitWidth=3,description="" */
        volatile uint32_t RSVD6: 3;
        /* @SVD:field=RSVD5,bitOffset=20,bitWidth=2,description="" */
        volatile uint32_t RSVD5: 2;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=7,description="" */
        volatile uint32_t RSVD: 7;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_CR3_Type;

/* @SVD:register=BRR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Baud Rate Register" */
/* BRR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FRAC,bitOffset=0,bitWidth=4,description="Fractional part of baud rate prescaler" */
        volatile uint32_t FRAC: 4;  /* Fractional part of baud rate prescaler */
        /* @SVD:field=INT,bitOffset=4,bitWidth=12,description="Integer part of baud rate prescaler\n              If OVER8 = 0, Baud Rate = 48000000 / (INT + FRAC/16) / 16\n              If OVER8 = 1, Baud Rate = 48000000 / (INT + FRAC/16) / 8\n              For example:\n              OVER=0, INT=3, FRAC=0, Baud Rate = 48000000/(3+0)/16 = 1Mbps\n              OVER=0, INT=3, FRAC=4, Baud Rate = 48000000/(3+4/16)/16 = 923077 = 921600 + 1.6‰\n              OVER=1, INT=52, FRAC=1, Baud Rate = 48000000/(52+1/16)/8 = 115246 = 115200 + 0.4‰" */
        volatile uint32_t INT: 12;  /* Integer part of baud rate prescaler               If OVER8 = */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_BRR_Type;

/* @SVD:register=RQR,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="Request Register" */
/* RQR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RXFRQ,bitOffset=3,bitWidth=1,description="Rx data flush request. Write 1 to clear the RXNE flag and discard the current data in RDR" */
        volatile uint32_t RXFRQ: 1;  /* Rx data flush request. Write 1 to clear the RXNE flag and di */
        /* @SVD:field=TXFRQ,bitOffset=4,bitWidth=1,description="Tx data flush requestReserved-Do not modify" */
        volatile uint32_t TXFRQ: 1;  /* Tx data flush requestReserved-Do not modify */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_RQR_Type;

/* @SVD:register=ISR,offset=0x001C,size=32,access=read-write,reset=0x020000C0,description="Interrupt and Status Register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PE,bitOffset=0,bitWidth=1,description="Parity error. This bit is set when a parity error is detected in the received packet.\n              0: no parity error\n              1: parity error detected" */
        volatile uint32_t PE: 1;  /* Parity error. This bit is set when a parity error is detecte */
        /* @SVD:field=FE,bitOffset=1,bitWidth=1,description="Framing error. This bit is set by hardware when stop bit is not correctly received\n              0: no framing error is detected\n              1: framing error is detected" */
        volatile uint32_t FE: 1;  /* Framing error. This bit is set by hardware when stop bit is  */
        /* @SVD:field=NF,bitOffset=2,bitWidth=1,description="Noise flag. Noise means the samping values in the 3-bit sampling mode are not the same.\n              0: no noise is detected\n              1: noise is detected" */
        volatile uint32_t NF: 1;  /* Noise flag. Noise means the samping values in the 3-bit samp */
        /* @SVD:field=ORE,bitOffset=3,bitWidth=1,description="Overrun error. When new data is received but Rx buffer is not empty (i.e. previous data is not read yet), ORE is asserted and current RDR content is not lost. This feature can be disabled by set CR3_OVRDIS to 1.\n              0: no overrun error\n              1: overrun error is detected" */
        volatile uint32_t ORE: 1;  /* Overrun error. When new data is received but Rx buffer is no */
        /* @SVD:field=IDLE,bitOffset=4,bitWidth=1,description="Idle line detected\n              0: no idle line is detected\n              1: idle line is detected" */
        volatile uint32_t IDLE: 1;  /* Idle line detected               0: no idle line is detected */
        /* @SVD:field=RXNE,bitOffset=5,bitWidth=1,description="Rx data not empty. This bit is set by hardware when the received data is transferred into RDR register.\n              0: data is not received\n              1: data is ready in RDR to be read" */
        volatile uint32_t RXNE: 1;  /* Rx data not empty. This bit is set by hardware when the rece */
        /* @SVD:field=TC,bitOffset=6,bitWidth=1,description="transmission complete. This bit is set by hardware if the transmission is complete\n              0: transmission is not complete\n              1: transmission is complete" */
        volatile uint32_t TC: 1;  /* transmission complete. This bit is set by hardware if the tr */
        /* @SVD:field=TXE,bitOffset=7,bitWidth=1,description="Tx data empty\n              0: data is ready in TDR\n              1: data is already transferred to shift register, i.e. transmission is in progress or complete" */
        volatile uint32_t TXE: 1;  /* Tx data empty               0: data is ready in TDR          */
        /* @SVD:field=RSVD16,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=CTSIF,bitOffset=9,bitWidth=1,description="CTS interrupt flag. This bit is set by hardware whenever CTS input toggles.\n              0: no change on the CTS line\n              1: there is a change on the CTS line" */
        volatile uint32_t CTSIF: 1;  /* CTS interrupt flag. This bit is set by hardware whenever CTS */
        /* @SVD:field=CTS,bitOffset=10,bitWidth=1,description="CTS input. Read this bit to get the raw status of the CTS line." */
        volatile uint32_t CTS: 1;  /* CTS input. Read this bit to get the raw status of the CTS li */
        /* @SVD:field=RSVD15,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=RSVD10,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_ISR_Type;

/* @SVD:register=ICR,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Interrupt flag Clear Register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PECF,bitOffset=0,bitWidth=1,description="Parity error clear flag. Wriring 1 to this bit clears the PE flag in the ISR register." */
        volatile uint32_t PECF: 1;  /* Parity error clear flag. Wriring 1 to this bit clears the PE */
        /* @SVD:field=FECF,bitOffset=1,bitWidth=1,description="Framing error clear flag. Writing 1 to this bit clears the FE flag in the ISR register." */
        volatile uint32_t FECF: 1;  /* Framing error clear flag. Writing 1 to this bit clears the F */
        /* @SVD:field=NCF,bitOffset=2,bitWidth=1,description="Noise detected clear flag. Writing 1 to this bit clears the NF flag in the ISR register." */
        volatile uint32_t NCF: 1;  /* Noise detected clear flag. Writing 1 to this bit clears the  */
        /* @SVD:field=ORECF,bitOffset=3,bitWidth=1,description="Overrun error clear flag. Writing 1 to this bit clears the ORE flag in the ISR register." */
        volatile uint32_t ORECF: 1;  /* Overrun error clear flag. Writing 1 to this bit clears the O */
        /* @SVD:field=IDLECF,bitOffset=4,bitWidth=1,description="Idle line detected clear flag. Writing 1 to this bit clears the IDLECF flag in the ISR register." */
        volatile uint32_t IDLECF: 1;  /* Idle line detected clear flag. Writing 1 to this bit clears  */
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=TCCF,bitOffset=6,bitWidth=1,description="Transmission complete clear flag. Writing 1 to this bit clears the TC flag in the ISR register." */
        volatile uint32_t TCCF: 1;  /* Transmission complete clear flag. Writing 1 to this bit clea */
        /* @SVD:field=RSVD10,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=CTSCF,bitOffset=9,bitWidth=1,description="CTS clear flag. Writing 1 to this bit clears the CTSIF flag in the ISR register." */
        volatile uint32_t CTSCF: 1;  /* CTS clear flag. Writing 1 to this bit clears the CTSIF flag  */
        /* @SVD:field=RSVD8,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=13,bitWidth=4,description="" */
        volatile uint32_t RSVD5: 4;
        /* @SVD:field=RSVD4,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=18,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_ICR_Type;

/* @SVD:register=RDR,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Receive Data Register" */
/* RDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RDR,bitOffset=0,bitWidth=9,description="Received data" */
        volatile uint32_t RDR: 9;  /* Received data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_RDR_Type;

/* @SVD:register=TDR,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Transmit Data Register" */
/* TDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TDR,bitOffset=0,bitWidth=9,description="Transmit data" */
        volatile uint32_t TDR: 9;  /* Transmit data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_TDR_Type;

/* @SVD:register=MISCR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Miscellaneous Register" */
/* MISCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SMPLINI,bitOffset=0,bitWidth=4,description="initial sample count, count down from this value to zero to reach the middle of the start bit in RxReserved-Do not modify" */
        volatile uint32_t SMPLINI: 4;  /* initial sample count, count down from this value to zero to  */
        /* @SVD:field=RTSBIT,bitOffset=4,bitWidth=4,description="assert RTS ahead of the frame completion (in number of bits)Reserved-Do not modify" */
        volatile uint32_t RTSBIT: 4;  /* assert RTS ahead of the frame completion (in number of bits) */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
        /* @SVD:field=AUTOCAL,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t AUTOCAL: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_MISCR_Type;

/* @SVD:register=DRDR,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DRDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_DRDR_Type;

/* @SVD:register=DTDR,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DTDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_DTDR_Type;

/* @SVD:register=EXR,offset=0x0038,size=32,access=read-write,reset=0x00000001,description="Mutual Exclusive Register" */
/* EXR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BUSY,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t BUSY: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=ID,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t ID: 1;
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART2_EXR_Type;

/* USART2 寄存器结构体 */
typedef struct
{
    USART2_CR1_Type CR1;  /* 0x0000 - Control Register 1 */
    USART2_CR2_Type CR2;  /* 0x0004 - Control Register 2 */
    USART2_CR3_Type CR3;  /* 0x0008 - Control Register 3 */
    USART2_BRR_Type BRR;  /* 0x000C - Baud Rate Register */
    /* @SVD:register=RSVD1,offset=0x0010,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0010 - RSVD1 */
    volatile uint32_t RESERVED5[1];  /* 0x0014 - 0x0017 */
    USART2_RQR_Type RQR;  /* 0x0018 - Request Register */
    USART2_ISR_Type ISR;  /* 0x001C - Interrupt and Status Register */
    USART2_ICR_Type ICR;  /* 0x0020 - Interrupt flag Clear Register */
    USART2_RDR_Type RDR;  /* 0x0024 - Receive Data Register */
    USART2_TDR_Type TDR;  /* 0x0028 - Transmit Data Register */
    USART2_MISCR_Type MISCR;  /* 0x002C - Miscellaneous Register */
    USART2_DRDR_Type DRDR;  /* 0x0030 - Debug Receive Data Register */
    USART2_DTDR_Type DTDR;  /* 0x0034 - Debug Receive Data Register */
    USART2_EXR_Type EXR;  /* 0x0038 - Mutual Exclusive Register */
} USART2_TypeDef;

/* ============================================================================ */
/* USART3 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=USART3,base=0x50086000,description="",groupName=USART */

#define USART3_BASE                                        (0x50086000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control Register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UE,bitOffset=0,bitWidth=1,description="USART enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t UE: 1;  /* USART enable               0: disabled               1: enab */
        /* @SVD:field=RSVD9,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RE,bitOffset=2,bitWidth=1,description="Receiver enable\n              0: receiver is disabled\n              1: receiver is enabled" */
        volatile uint32_t RE: 1;  /* Receiver enable               0: receiver is disabled        */
        /* @SVD:field=TE,bitOffset=3,bitWidth=1,description="Transmitter enable\n              0: transmitter is disabled\n              1: transmitter is enabled" */
        volatile uint32_t TE: 1;  /* Transmitter enable               0: transmitter is disabled  */
        /* @SVD:field=IDLEIE,bitOffset=4,bitWidth=1,description="Idle line interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever IDLE=1 in the ISR  register" */
        volatile uint32_t IDLEIE: 1;  /* Idle line interrupt enable               0: interrupt disabl */
        /* @SVD:field=RXNEIE,bitOffset=5,bitWidth=1,description="Rx not empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever RXNE=1 in the ISR register" */
        volatile uint32_t RXNEIE: 1;  /* Rx not empty interrupt enable               0: interrupt dis */
        /* @SVD:field=TCIE,bitOffset=6,bitWidth=1,description="Transfer compelete interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever TC=1 in the ISR register" */
        volatile uint32_t TCIE: 1;  /* Transfer compelete interrupt enable               0: interru */
        /* @SVD:field=TXEIE,bitOffset=7,bitWidth=1,description="Tx empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenver TXE=1 in the ISR register" */
        volatile uint32_t TXEIE: 1;  /* Tx empty interrupt enable               0: interrupt disable */
        /* @SVD:field=PEIE,bitOffset=8,bitWidth=1,description="Parity error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever PE=1 in the ISR register" */
        volatile uint32_t PEIE: 1;  /* Parity error interrupt enable               0: interrupt dis */
        /* @SVD:field=PS,bitOffset=9,bitWidth=1,description="Parity select\n              0: even parity\n              1: odd parity" */
        volatile uint32_t PS: 1;  /* Parity select               0: even parity               1:  */
        /* @SVD:field=PCE,bitOffset=10,bitWidth=1,description="Parity check enable. If enabled, parity bit is inserted at the MSB position \n              0: parity check disabled\n              1: parity check enabled" */
        volatile uint32_t PCE: 1;  /* Parity check enable. If enabled, parity bit is inserted at t */
        /* @SVD:field=RSVD8,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=OVER8,bitOffset=14,bitWidth=1,description="Oversampling mode\n              0: Oversampling by 16\n              1: Oversampling by 8" */
        volatile uint32_t OVER8: 1;  /* Oversampling mode               0: Oversampling by 16        */
        /* @SVD:field=RSVD5,bitOffset=15,bitWidth=5,description="" */
        volatile uint32_t RSVD5: 5;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=5,description="" */
        volatile uint32_t RSVD4: 5;
        /* @SVD:field=RSVD3,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=M,bitOffset=27,bitWidth=2,description="Mode bit indicates the length of the packet, including data bits and parity. Stop bits not included.\n              0: 6 bits (e.g. 6 data bits + no parity bit)\n              1: 7 bits (e.g. 6 data bits + 1 parity bit)\n              2: 8 bits (e.g. 7 data bits + 1 parity bit, or 6 data bits + 2 parity bits)\n              3: 9 bits (e.g. 8 data bits + 1 parity bit, or 7 data bits + 2 parity bits)" */
        volatile uint32_t M: 2;  /* Mode bit indicates the length of the packet, including data  */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Control Register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD19,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD19: 4;
        /* @SVD:field=RSVD18,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD18: 1;
        /* @SVD:field=RSVD17,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD17: 1;
        /* @SVD:field=RSVD16,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=RSVD15,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=STOP,bitOffset=12,bitWidth=2,description="Stop bits\n              0/1: 1 stop bit\n              2/3: 2 stop bits" */
        volatile uint32_t STOP: 2;  /* Stop bits               0/1: 1 stop bit               2/3: 2 */
        /* @SVD:field=RSVD10,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=21,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_CR2_Type;

/* @SVD:register=CR3,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Control Register 3" */
/* CR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EIE,bitOffset=0,bitWidth=1,description="Error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever FE=1 or ORE=1 or NF=1 in the ISR register" */
        volatile uint32_t EIE: 1;  /* Error interrupt enable               0: interrupt disabled   */
        /* @SVD:field=RSVD15,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=DMAR,bitOffset=6,bitWidth=1,description="Receiver DMA enable\n              0: DMA mode disabled for reception\n              1: DMA mode enabled for reception" */
        volatile uint32_t DMAR: 1;  /* Receiver DMA enable               0: DMA mode disabled for r */
        /* @SVD:field=DMAT,bitOffset=7,bitWidth=1,description="Transmitter DMA enable\n              0: DMA mode disabled for transmission\n              1: DMA mode enabled for transmission" */
        volatile uint32_t DMAT: 1;  /* Transmitter DMA enable               0: DMA mode disabled fo */
        /* @SVD:field=RTSE,bitOffset=8,bitWidth=1,description="RTS enable\n              0: RTS hardware flow control disabled\n              1: RTS hardware flow control enabled, RTS output is asserted low when new data can be received" */
        volatile uint32_t RTSE: 1;  /* RTS enable               0: RTS hardware flow control disabl */
        /* @SVD:field=CTSE,bitOffset=9,bitWidth=1,description="CTS enable\n              0: CTS hardware flow control disabled\n              1: CTS hardware flow control enabled, data is transmitted only when CTS input is asserted low" */
        volatile uint32_t CTSE: 1;  /* CTS enable               0: CTS hardware flow control disabl */
        /* @SVD:field=CTSIE,bitOffset=10,bitWidth=1,description="CTS interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever CTSIF=1 in the ISR register" */
        volatile uint32_t CTSIE: 1;  /* CTS interrupt enable               0: interrupt disabled     */
        /* @SVD:field=ONEBIT,bitOffset=11,bitWidth=1,description="One bit sampling mode\n              0: 3-bit sampling mode, the sampling value is determined by the voted result out of 3 bits\n              1: 1-bit sampling mode" */
        volatile uint32_t ONEBIT: 1;  /* One bit sampling mode               0: 3-bit sampling mode,  */
        /* @SVD:field=OVRDIS,bitOffset=12,bitWidth=1,description="Overrun disable\n              0: overrun error flag (ORE) will be set if new data received but previous data not read. New data will not overwrite the content in RDR register.\n              1: overrun disabled. If new data is received before previous data is read, the new data will overwrite the content in RDR register and ORE flag remains unset." */
        volatile uint32_t OVRDIS: 1;  /* Overrun disable               0: overrun error flag (ORE) wi */
        /* @SVD:field=RSVD10,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=17,bitWidth=3,description="" */
        volatile uint32_t RSVD6: 3;
        /* @SVD:field=RSVD5,bitOffset=20,bitWidth=2,description="" */
        volatile uint32_t RSVD5: 2;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=7,description="" */
        volatile uint32_t RSVD: 7;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_CR3_Type;

/* @SVD:register=BRR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Baud Rate Register" */
/* BRR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FRAC,bitOffset=0,bitWidth=4,description="Fractional part of baud rate prescaler" */
        volatile uint32_t FRAC: 4;  /* Fractional part of baud rate prescaler */
        /* @SVD:field=INT,bitOffset=4,bitWidth=12,description="Integer part of baud rate prescaler\n              If OVER8 = 0, Baud Rate = 48000000 / (INT + FRAC/16) / 16\n              If OVER8 = 1, Baud Rate = 48000000 / (INT + FRAC/16) / 8\n              For example:\n              OVER=0, INT=3, FRAC=0, Baud Rate = 48000000/(3+0)/16 = 1Mbps\n              OVER=0, INT=3, FRAC=4, Baud Rate = 48000000/(3+4/16)/16 = 923077 = 921600 + 1.6‰\n              OVER=1, INT=52, FRAC=1, Baud Rate = 48000000/(52+1/16)/8 = 115246 = 115200 + 0.4‰" */
        volatile uint32_t INT: 12;  /* Integer part of baud rate prescaler               If OVER8 = */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_BRR_Type;

/* @SVD:register=RQR,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="Request Register" */
/* RQR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RXFRQ,bitOffset=3,bitWidth=1,description="Rx data flush request. Write 1 to clear the RXNE flag and discard the current data in RDR" */
        volatile uint32_t RXFRQ: 1;  /* Rx data flush request. Write 1 to clear the RXNE flag and di */
        /* @SVD:field=TXFRQ,bitOffset=4,bitWidth=1,description="Tx data flush requestReserved-Do not modify" */
        volatile uint32_t TXFRQ: 1;  /* Tx data flush requestReserved-Do not modify */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_RQR_Type;

/* @SVD:register=ISR,offset=0x001C,size=32,access=read-write,reset=0x020000C0,description="Interrupt and Status Register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PE,bitOffset=0,bitWidth=1,description="Parity error. This bit is set when a parity error is detected in the received packet.\n              0: no parity error\n              1: parity error detected" */
        volatile uint32_t PE: 1;  /* Parity error. This bit is set when a parity error is detecte */
        /* @SVD:field=FE,bitOffset=1,bitWidth=1,description="Framing error. This bit is set by hardware when stop bit is not correctly received\n              0: no framing error is detected\n              1: framing error is detected" */
        volatile uint32_t FE: 1;  /* Framing error. This bit is set by hardware when stop bit is  */
        /* @SVD:field=NF,bitOffset=2,bitWidth=1,description="Noise flag. Noise means the samping values in the 3-bit sampling mode are not the same.\n              0: no noise is detected\n              1: noise is detected" */
        volatile uint32_t NF: 1;  /* Noise flag. Noise means the samping values in the 3-bit samp */
        /* @SVD:field=ORE,bitOffset=3,bitWidth=1,description="Overrun error. When new data is received but Rx buffer is not empty (i.e. previous data is not read yet), ORE is asserted and current RDR content is not lost. This feature can be disabled by set CR3_OVRDIS to 1.\n              0: no overrun error\n              1: overrun error is detected" */
        volatile uint32_t ORE: 1;  /* Overrun error. When new data is received but Rx buffer is no */
        /* @SVD:field=IDLE,bitOffset=4,bitWidth=1,description="Idle line detected\n              0: no idle line is detected\n              1: idle line is detected" */
        volatile uint32_t IDLE: 1;  /* Idle line detected               0: no idle line is detected */
        /* @SVD:field=RXNE,bitOffset=5,bitWidth=1,description="Rx data not empty. This bit is set by hardware when the received data is transferred into RDR register.\n              0: data is not received\n              1: data is ready in RDR to be read" */
        volatile uint32_t RXNE: 1;  /* Rx data not empty. This bit is set by hardware when the rece */
        /* @SVD:field=TC,bitOffset=6,bitWidth=1,description="transmission complete. This bit is set by hardware if the transmission is complete\n              0: transmission is not complete\n              1: transmission is complete" */
        volatile uint32_t TC: 1;  /* transmission complete. This bit is set by hardware if the tr */
        /* @SVD:field=TXE,bitOffset=7,bitWidth=1,description="Tx data empty\n              0: data is ready in TDR\n              1: data is already transferred to shift register, i.e. transmission is in progress or complete" */
        volatile uint32_t TXE: 1;  /* Tx data empty               0: data is ready in TDR          */
        /* @SVD:field=RSVD16,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=CTSIF,bitOffset=9,bitWidth=1,description="CTS interrupt flag. This bit is set by hardware whenever CTS input toggles.\n              0: no change on the CTS line\n              1: there is a change on the CTS line" */
        volatile uint32_t CTSIF: 1;  /* CTS interrupt flag. This bit is set by hardware whenever CTS */
        /* @SVD:field=CTS,bitOffset=10,bitWidth=1,description="CTS input. Read this bit to get the raw status of the CTS line." */
        volatile uint32_t CTS: 1;  /* CTS input. Read this bit to get the raw status of the CTS li */
        /* @SVD:field=RSVD15,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=RSVD10,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_ISR_Type;

/* @SVD:register=ICR,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Interrupt flag Clear Register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PECF,bitOffset=0,bitWidth=1,description="Parity error clear flag. Wriring 1 to this bit clears the PE flag in the ISR register." */
        volatile uint32_t PECF: 1;  /* Parity error clear flag. Wriring 1 to this bit clears the PE */
        /* @SVD:field=FECF,bitOffset=1,bitWidth=1,description="Framing error clear flag. Writing 1 to this bit clears the FE flag in the ISR register." */
        volatile uint32_t FECF: 1;  /* Framing error clear flag. Writing 1 to this bit clears the F */
        /* @SVD:field=NCF,bitOffset=2,bitWidth=1,description="Noise detected clear flag. Writing 1 to this bit clears the NF flag in the ISR register." */
        volatile uint32_t NCF: 1;  /* Noise detected clear flag. Writing 1 to this bit clears the  */
        /* @SVD:field=ORECF,bitOffset=3,bitWidth=1,description="Overrun error clear flag. Writing 1 to this bit clears the ORE flag in the ISR register." */
        volatile uint32_t ORECF: 1;  /* Overrun error clear flag. Writing 1 to this bit clears the O */
        /* @SVD:field=IDLECF,bitOffset=4,bitWidth=1,description="Idle line detected clear flag. Writing 1 to this bit clears the IDLECF flag in the ISR register." */
        volatile uint32_t IDLECF: 1;  /* Idle line detected clear flag. Writing 1 to this bit clears  */
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=TCCF,bitOffset=6,bitWidth=1,description="Transmission complete clear flag. Writing 1 to this bit clears the TC flag in the ISR register." */
        volatile uint32_t TCCF: 1;  /* Transmission complete clear flag. Writing 1 to this bit clea */
        /* @SVD:field=RSVD10,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=CTSCF,bitOffset=9,bitWidth=1,description="CTS clear flag. Writing 1 to this bit clears the CTSIF flag in the ISR register." */
        volatile uint32_t CTSCF: 1;  /* CTS clear flag. Writing 1 to this bit clears the CTSIF flag  */
        /* @SVD:field=RSVD8,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=13,bitWidth=4,description="" */
        volatile uint32_t RSVD5: 4;
        /* @SVD:field=RSVD4,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=18,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_ICR_Type;

/* @SVD:register=RDR,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Receive Data Register" */
/* RDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RDR,bitOffset=0,bitWidth=9,description="Received data" */
        volatile uint32_t RDR: 9;  /* Received data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_RDR_Type;

/* @SVD:register=TDR,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Transmit Data Register" */
/* TDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TDR,bitOffset=0,bitWidth=9,description="Transmit data" */
        volatile uint32_t TDR: 9;  /* Transmit data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_TDR_Type;

/* @SVD:register=MISCR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Miscellaneous Register" */
/* MISCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SMPLINI,bitOffset=0,bitWidth=4,description="initial sample count, count down from this value to zero to reach the middle of the start bit in RxReserved-Do not modify" */
        volatile uint32_t SMPLINI: 4;  /* initial sample count, count down from this value to zero to  */
        /* @SVD:field=RTSBIT,bitOffset=4,bitWidth=4,description="assert RTS ahead of the frame completion (in number of bits)Reserved-Do not modify" */
        volatile uint32_t RTSBIT: 4;  /* assert RTS ahead of the frame completion (in number of bits) */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
        /* @SVD:field=AUTOCAL,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t AUTOCAL: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_MISCR_Type;

/* @SVD:register=DRDR,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DRDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_DRDR_Type;

/* @SVD:register=DTDR,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DTDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_DTDR_Type;

/* @SVD:register=EXR,offset=0x0038,size=32,access=read-write,reset=0x00000001,description="Mutual Exclusive Register" */
/* EXR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BUSY,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t BUSY: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=ID,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t ID: 1;
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART3_EXR_Type;

/* USART3 寄存器结构体 */
typedef struct
{
    USART3_CR1_Type CR1;  /* 0x0000 - Control Register 1 */
    USART3_CR2_Type CR2;  /* 0x0004 - Control Register 2 */
    USART3_CR3_Type CR3;  /* 0x0008 - Control Register 3 */
    USART3_BRR_Type BRR;  /* 0x000C - Baud Rate Register */
    /* @SVD:register=RSVD1,offset=0x0010,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0010 - RSVD1 */
    volatile uint32_t RESERVED5[1];  /* 0x0014 - 0x0017 */
    USART3_RQR_Type RQR;  /* 0x0018 - Request Register */
    USART3_ISR_Type ISR;  /* 0x001C - Interrupt and Status Register */
    USART3_ICR_Type ICR;  /* 0x0020 - Interrupt flag Clear Register */
    USART3_RDR_Type RDR;  /* 0x0024 - Receive Data Register */
    USART3_TDR_Type TDR;  /* 0x0028 - Transmit Data Register */
    USART3_MISCR_Type MISCR;  /* 0x002C - Miscellaneous Register */
    USART3_DRDR_Type DRDR;  /* 0x0030 - Debug Receive Data Register */
    USART3_DTDR_Type DTDR;  /* 0x0034 - Debug Receive Data Register */
    USART3_EXR_Type EXR;  /* 0x0038 - Mutual Exclusive Register */
} USART3_TypeDef;

/* ============================================================================ */
/* GPADC 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=GPADC,base=0x50087000,description="",groupName=GPADC */

#define GPADC_BASE                                         (0x50087000UL)

/* @SVD:register=ADC_CFG_REG1,offset=0x0000,size=32,access=read-write,reset=0x04714A44,description="ADC Analog Config Register 1" */
/* ADC_CFG_REG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ANAU_GPADC_CMREF_FAST_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t ANAU_GPADC_CMREF_FAST_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=ANAU_GPADC_P_INT_EN,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t ANAU_GPADC_P_INT_EN: 1;
        /* @SVD:field=ANAU_GPADC_CL_DLY,bitOffset=3,bitWidth=3,description="" */
        volatile uint32_t ANAU_GPADC_CL_DLY: 3;
        /* @SVD:field=ANAU_GPADC_EN_V18,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t ANAU_GPADC_EN_V18: 1;
        /* @SVD:field=ANAU_GPADC_SE,bitOffset=7,bitWidth=1,description="Set GPADC in single-ended mode, signal range at P-input: 0 ~ VREF" */
        volatile uint32_t ANAU_GPADC_SE: 1;  /* Set GPADC in single-ended mode, signal range at P-input: 0 ~ */
        /* @SVD:field=ANAU_GPADC_MUTE,bitOffset=8,bitWidth=1,description="Short GPADC P  and  N input to CMREF, i.e., VREF/2" */
        volatile uint32_t ANAU_GPADC_MUTE: 1;  /* Short GPADC P  and  N input to CMREF, i.e., VREF/2 */
        /* @SVD:field=ANAU_GPADC_SEL_NCH,bitOffset=9,bitWidth=3,description="Select N-side input channel for GPADC, 0 for channel 0, 7 for channel 7, effective when force on" */
        volatile uint32_t ANAU_GPADC_SEL_NCH: 3;  /* Select N-side input channel for GPADC, 0 for channel 0, 7 fo */
        /* @SVD:field=ANAU_GPADC_SEL_PCH,bitOffset=12,bitWidth=3,description="Select P-side input channel for GPADC, 0 for channel 0, 7 for channel 7, effective when force on" */
        volatile uint32_t ANAU_GPADC_SEL_PCH: 3;  /* Select P-side input channel for GPADC, 0 for channel 0, 7 fo */
        /* @SVD:field=ANAU_GPADC_LDOVREF_SEL,bitOffset=15,bitWidth=4,description="Set reference voltage for LDOREF, range = 0.35V(0) ~ 0.65V(15), step = 20mV" */
        volatile uint32_t ANAU_GPADC_LDOVREF_SEL: 4;  /* Set reference voltage for LDOREF, range = 0.35V(0) ~ 0.65V(1 */
        /* @SVD:field=ANAU_GPADC_LDOREF_EN,bitOffset=19,bitWidth=1,description="Enable LDORF for ADC VREF" */
        volatile uint32_t ANAU_GPADC_LDOREF_EN: 1;  /* Enable LDORF for ADC VREF */
        /* @SVD:field=ANAU_GPADC_VSP,bitOffset=20,bitWidth=2,description="Set comparator input CM in sampling phase, 0.539V (0) / 0.578V (1) / 0.642V (2) / 0.784V (3)" */
        volatile uint32_t ANAU_GPADC_VSP: 2;  /* Set comparator input CM in sampling phase, 0.539V (0) / 0.57 */
        /* @SVD:field=ANAU_GPADC_CMPCL,bitOffset=22,bitWidth=3,description="Tune ADC comparator CL= 3: 40f, range: 10fF (0) ~ 80fF (7) / 10fF step" */
        volatile uint32_t ANAU_GPADC_CMPCL: 3;  /* Tune ADC comparator CL= 3: 40f, range: 10fF (0) ~ 80fF (7) / */
        /* @SVD:field=ANAU_GPADC_CMM,bitOffset=25,bitWidth=5,description="Tune CDAC CM voltage 375mV range (increasing) / 25mV step, 8: for 0.5V Vcm,in" */
        volatile uint32_t ANAU_GPADC_CMM: 5;  /* Tune CDAC CM voltage 375mV range (increasing) / 25mV step, 8 */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_CFG_REG1_Type;

/* @SVD:register=ADC_SLOT0_REG,offset=0x0004,size=32,access=read-write,reset=0x00020801,description="ADC Slot0 Config Register " */
/* ADC_SLOT0_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t SLOT_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=PCHNL_SEL,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PCHNL_SEL: 3;
        /* @SVD:field=NCHNL_SEL,bitOffset=11,bitWidth=3,description="" */
        volatile uint32_t NCHNL_SEL: 3;
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_SLOT0_REG_Type;

/* @SVD:register=ADC_SLOT1_REG,offset=0x0008,size=32,access=read-write,reset=0x00020801,description="ADC Slot1 Config Register " */
/* ADC_SLOT1_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t SLOT_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=PCHNL_SEL,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PCHNL_SEL: 3;
        /* @SVD:field=NCHNL_SEL,bitOffset=11,bitWidth=3,description="" */
        volatile uint32_t NCHNL_SEL: 3;
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_SLOT1_REG_Type;

/* @SVD:register=ADC_SLOT2_REG,offset=0x000C,size=32,access=read-write,reset=0x00020801,description="ADC Slot2 Config Register " */
/* ADC_SLOT2_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t SLOT_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=PCHNL_SEL,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PCHNL_SEL: 3;
        /* @SVD:field=NCHNL_SEL,bitOffset=11,bitWidth=3,description="" */
        volatile uint32_t NCHNL_SEL: 3;
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_SLOT2_REG_Type;

/* @SVD:register=ADC_SLOT3_REG,offset=0x0010,size=32,access=read-write,reset=0x00020801,description="ADC Slot3 Config Register " */
/* ADC_SLOT3_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t SLOT_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=PCHNL_SEL,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PCHNL_SEL: 3;
        /* @SVD:field=NCHNL_SEL,bitOffset=11,bitWidth=3,description="" */
        volatile uint32_t NCHNL_SEL: 3;
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_SLOT3_REG_Type;

/* @SVD:register=ADC_SLOT4_REG,offset=0x0014,size=32,access=read-write,reset=0x00020801,description="ADC Slot4 Config Register " */
/* ADC_SLOT4_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t SLOT_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=PCHNL_SEL,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PCHNL_SEL: 3;
        /* @SVD:field=NCHNL_SEL,bitOffset=11,bitWidth=3,description="" */
        volatile uint32_t NCHNL_SEL: 3;
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_SLOT4_REG_Type;

/* @SVD:register=ADC_SLOT5_REG,offset=0x0018,size=32,access=read-write,reset=0x00020801,description="ADC Slot5 Config Register " */
/* ADC_SLOT5_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t SLOT_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=PCHNL_SEL,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PCHNL_SEL: 3;
        /* @SVD:field=NCHNL_SEL,bitOffset=11,bitWidth=3,description="" */
        volatile uint32_t NCHNL_SEL: 3;
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_SLOT5_REG_Type;

/* @SVD:register=ADC_SLOT6_REG,offset=0x001C,size=32,access=read-write,reset=0x00020801,description="ADC Slot6 Config Register " */
/* ADC_SLOT6_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t SLOT_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=PCHNL_SEL,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PCHNL_SEL: 3;
        /* @SVD:field=NCHNL_SEL,bitOffset=11,bitWidth=3,description="" */
        volatile uint32_t NCHNL_SEL: 3;
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_SLOT6_REG_Type;

/* @SVD:register=ADC_SLOT7_REG,offset=0x0020,size=32,access=read-write,reset=0x00020801,description="ADC Slot7 Config Register " */
/* ADC_SLOT7_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t SLOT_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=PCHNL_SEL,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t PCHNL_SEL: 3;
        /* @SVD:field=NCHNL_SEL,bitOffset=11,bitWidth=3,description="" */
        volatile uint32_t NCHNL_SEL: 3;
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_SLOT7_REG_Type;

/* @SVD:register=ADC_RDATA0,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="ADC Read Data0" */
/* ADC_RDATA0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT0_RDATA,bitOffset=0,bitWidth=12,description="" */
        volatile uint32_t SLOT0_RDATA: 12;
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=SLOT1_RDATA,bitOffset=16,bitWidth=12,description="" */
        volatile uint32_t SLOT1_RDATA: 12;
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_RDATA0_Type;

/* @SVD:register=ADC_RDATA1,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="ADC Read Data1" */
/* ADC_RDATA1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT2_RDATA,bitOffset=0,bitWidth=12,description="" */
        volatile uint32_t SLOT2_RDATA: 12;
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=SLOT3_RDATA,bitOffset=16,bitWidth=12,description="" */
        volatile uint32_t SLOT3_RDATA: 12;
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_RDATA1_Type;

/* @SVD:register=ADC_RDATA2,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="ADC Read Data2" */
/* ADC_RDATA2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT4_RDATA,bitOffset=0,bitWidth=12,description="" */
        volatile uint32_t SLOT4_RDATA: 12;
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=SLOT5_RDATA,bitOffset=16,bitWidth=12,description="" */
        volatile uint32_t SLOT5_RDATA: 12;
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_RDATA2_Type;

/* @SVD:register=ADC_RDATA3,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="ADC Read Data3" */
/* ADC_RDATA3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLOT6_RDATA,bitOffset=0,bitWidth=12,description="" */
        volatile uint32_t SLOT6_RDATA: 12;
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=SLOT7_RDATA,bitOffset=16,bitWidth=12,description="" */
        volatile uint32_t SLOT7_RDATA: 12;
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_RDATA3_Type;

/* @SVD:register=ADC_DMA_RDATA,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="ADC Read Data For DMA" */
/* ADC_DMA_RDATA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DMA_RDATA,bitOffset=0,bitWidth=13,description="" */
        volatile uint32_t DMA_RDATA: 13;
        /* @SVD:field=RSVD2,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=DMA_RDATA_RAW,bitOffset=16,bitWidth=13,description="" */
        volatile uint32_t DMA_RDATA_RAW: 13;
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_DMA_RDATA_Type;

/* @SVD:register=ADC_CTRL_REG,offset=0x0038,size=32,access=read-write,reset=0x00000130,description="ADC Control Register" */
/* ADC_CTRL_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADC_OP_MODE,bitOffset=0,bitWidth=1,description="0: single conversion mode 1: continuous conversion mode" */
        volatile uint32_t ADC_OP_MODE: 1;  /* 0: single conversion mode 1: continuous conversion mode */
        /* @SVD:field=ADC_START,bitOffset=1,bitWidth=1,description="Write 1 to start GPADC,(don't need clear )" */
        volatile uint32_t ADC_START: 1;  /* Write 1 to start GPADC,(don't need clear ) */
        /* @SVD:field=ADC_STOP,bitOffset=2,bitWidth=1,description="Write 1 to stop GPADC in continuous mode(need write 0 to clear)" */
        volatile uint32_t ADC_STOP: 1;  /* Write 1 to stop GPADC in continuous mode(need write 0 to cle */
        /* @SVD:field=INIT_TIME,bitOffset=3,bitWidth=4,description="GPADC will wait INIT_TIME ADCCLK cycles to start sample/conversion after being trigged" */
        volatile uint32_t INIT_TIME: 4;  /* GPADC will wait INIT_TIME ADCCLK cycles to start sample/conv */
        /* @SVD:field=DMA_EN,bitOffset=7,bitWidth=1,description="Enable DMA interface" */
        volatile uint32_t DMA_EN: 1;  /* Enable DMA interface */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TIMER_TRIG_EN,bitOffset=9,bitWidth=1,description="Enable timer trigger function" */
        volatile uint32_t TIMER_TRIG_EN: 1;  /* Enable timer trigger function */
        /* @SVD:field=CHNL_SEL_FRC_EN,bitOffset=10,bitWidth=1,description="Enable input channel setting in ADC_CFG_REG1" */
        volatile uint32_t CHNL_SEL_FRC_EN: 1;  /* Enable input channel setting in ADC_CFG_REG1 */
        /* @SVD:field=FRC_EN_ADC,bitOffset=11,bitWidth=1,description="Enable GPADC core" */
        volatile uint32_t FRC_EN_ADC: 1;  /* Enable GPADC core */
        /* @SVD:field=TIMER_TRIG_SRC_SEL,bitOffset=12,bitWidth=3,description="Timer trigger source select" */
        volatile uint32_t TIMER_TRIG_SRC_SEL: 3;  /* Timer trigger source select */
        /* @SVD:field=TIMER_TRIG_TYP,bitOffset=15,bitWidth=1,description="0: pulse no edge detect needed  1: level,need edge detect" */
        volatile uint32_t TIMER_TRIG_TYP: 1;  /* 0: pulse no edge detect needed  1: level,need edge detect */
        /* @SVD:field=DMA_DATA_SEL,bitOffset=16,bitWidth=1,description="0: combined data 1: raw data" */
        volatile uint32_t DMA_DATA_SEL: 1;  /* 0: combined data 1: raw data */
        /* @SVD:field=DATA_SAMP_DLY,bitOffset=17,bitWidth=4,description="" */
        volatile uint32_t DATA_SAMP_DLY: 4;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_CTRL_REG_Type;

/* @SVD:register=ADC_CTRL_REG2,offset=0x003C,size=32,access=read-write,reset=0x00000130,description="ADC Control Register2" */
/* ADC_CTRL_REG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SAMP_WIDTH,bitOffset=0,bitWidth=24,description="" */
        volatile uint32_t SAMP_WIDTH: 24;
        /* @SVD:field=CONV_WIDTH,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t CONV_WIDTH: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_ADC_CTRL_REG2_Type;

/* @SVD:register=GPADC_STATUS,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="GPADC Status Register" */
/* GPADC_STATUS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADC_DONE,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t ADC_DONE: 1;
        /* @SVD:field=SLOT_DONE,bitOffset=1,bitWidth=8,description="" */
        volatile uint32_t SLOT_DONE: 8;
        /* @SVD:field=CUR_SLOT,bitOffset=9,bitWidth=3,description="" */
        volatile uint32_t CUR_SLOT: 3;
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_GPADC_STATUS_Type;

/* @SVD:register=GPADC_IRQ,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="GPADC IRQ Register" */
/* GPADC_IRQ 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GPADC_ICR,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t GPADC_ICR: 1;
        /* @SVD:field=GPADC_IMR,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t GPADC_IMR: 1;
        /* @SVD:field=GPADC_IRSR,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t GPADC_IRSR: 1;
        /* @SVD:field=GPADC_ISR,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t GPADC_ISR: 1;
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPADC_GPADC_IRQ_Type;

/* GPADC 寄存器结构体 */
typedef struct
{
    GPADC_ADC_CFG_REG1_Type ADC_CFG_REG1;  /* 0x0000 - ADC Analog Config Register 1 */
    GPADC_ADC_SLOT0_REG_Type ADC_SLOT0_REG;  /* 0x0004 - ADC Slot0 Config Register */
    GPADC_ADC_SLOT1_REG_Type ADC_SLOT1_REG;  /* 0x0008 - ADC Slot1 Config Register */
    GPADC_ADC_SLOT2_REG_Type ADC_SLOT2_REG;  /* 0x000C - ADC Slot2 Config Register */
    GPADC_ADC_SLOT3_REG_Type ADC_SLOT3_REG;  /* 0x0010 - ADC Slot3 Config Register */
    GPADC_ADC_SLOT4_REG_Type ADC_SLOT4_REG;  /* 0x0014 - ADC Slot4 Config Register */
    GPADC_ADC_SLOT5_REG_Type ADC_SLOT5_REG;  /* 0x0018 - ADC Slot5 Config Register */
    GPADC_ADC_SLOT6_REG_Type ADC_SLOT6_REG;  /* 0x001C - ADC Slot6 Config Register */
    GPADC_ADC_SLOT7_REG_Type ADC_SLOT7_REG;  /* 0x0020 - ADC Slot7 Config Register */
    GPADC_ADC_RDATA0_Type ADC_RDATA0;  /* 0x0024 - ADC Read Data0 */
    GPADC_ADC_RDATA1_Type ADC_RDATA1;  /* 0x0028 - ADC Read Data1 */
    GPADC_ADC_RDATA2_Type ADC_RDATA2;  /* 0x002C - ADC Read Data2 */
    GPADC_ADC_RDATA3_Type ADC_RDATA3;  /* 0x0030 - ADC Read Data3 */
    GPADC_ADC_DMA_RDATA_Type ADC_DMA_RDATA;  /* 0x0034 - ADC Read Data For DMA */
    GPADC_ADC_CTRL_REG_Type ADC_CTRL_REG;  /* 0x0038 - ADC Control Register */
    GPADC_ADC_CTRL_REG2_Type ADC_CTRL_REG2;  /* 0x003C - ADC Control Register2 */
    GPADC_GPADC_STATUS_Type GPADC_STATUS;  /* 0x0040 - GPADC Status Register */
    GPADC_GPADC_IRQ_Type GPADC_IRQ;  /* 0x0044 - GPADC IRQ Register */
} GPADC_TypeDef;

/* ============================================================================ */
/* AUDCODEC 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=AUDCODEC,base=0x50088000,description="",groupName=AUDCODEC */

#define AUDCODEC_BASE                                      (0x50088000UL)

/* @SVD:register=ID,offset=0x0000,size=32,access=read-write,reset=0xC0DEC000,description="ID" */
/* ID 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FUNC,bitOffset=0,bitWidth=32,description="function id" */
        volatile uint32_t FUNC: 32;  /* function id */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ID_Type;

/* @SVD:register=CFG,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="CFG" */
/* CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADC_ENABLE,bitOffset=0,bitWidth=1,description="adc codec enable" */
        volatile uint32_t ADC_ENABLE: 1;  /* adc codec enable */
        /* @SVD:field=DAC_ENABLE,bitOffset=1,bitWidth=1,description="dac codec enable" */
        volatile uint32_t DAC_ENABLE: 1;  /* dac codec enable */
        /* @SVD:field=DAC_1K_MODE,bitOffset=2,bitWidth=1,description="codec dac sine 1k mode" */
        volatile uint32_t DAC_1K_MODE: 1;  /* codec dac sine 1k mode */
        /* @SVD:field=ADC_EN_DLY_SEL,bitOffset=3,bitWidth=2,description="codec adc enable delay count\n              0: no delay\n              1: 32 pclk\n              2: 64 pclk\n              3: 128 pclk" */
        volatile uint32_t ADC_EN_DLY_SEL: 2;  /* codec adc enable delay count               0: no delay       */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_CFG_Type;

/* @SVD:register=IRQ,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="IRQ" */
/* IRQ 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DAC_CH0_APB_OF,bitOffset=0,bitWidth=1,description="dac ch0 apb fifo overflow interrupt status. Write 1 to clear." */
        volatile uint32_t DAC_CH0_APB_OF: 1;  /* dac ch0 apb fifo overflow interrupt status. Write 1 to clear */
        /* @SVD:field=DAC_CH0_OUT_UF,bitOffset=1,bitWidth=1,description="dac ch0 output fifo underflow interrupt status. Write 1 to clear." */
        volatile uint32_t DAC_CH0_OUT_UF: 1;  /* dac ch0 output fifo underflow interrupt status. Write 1 to c */
        /* @SVD:field=DAC_CH0_STB_OF,bitOffset=2,bitWidth=1,description="dac ch0 input stb fifo overflow interrupt status. Write 1 to clear." */
        volatile uint32_t DAC_CH0_STB_OF: 1;  /* dac ch0 input stb fifo overflow interrupt status. Write 1 to */
        /* @SVD:field=DAC_CH1_APB_OF,bitOffset=3,bitWidth=1,description="dac ch1 apb fifo overflow interrupt status. Write 1 to clear." */
        volatile uint32_t DAC_CH1_APB_OF: 1;  /* dac ch1 apb fifo overflow interrupt status. Write 1 to clear */
        /* @SVD:field=DAC_CH1_OUT_UF,bitOffset=4,bitWidth=1,description="dac ch1 output fifo underflow interrupt status. Write 1 to clear." */
        volatile uint32_t DAC_CH1_OUT_UF: 1;  /* dac ch1 output fifo underflow interrupt status. Write 1 to c */
        /* @SVD:field=DAC_CH1_STB_OF,bitOffset=5,bitWidth=1,description="dac ch1 input stb fifo overflow interrupt status. Write 1 to clear." */
        volatile uint32_t DAC_CH1_STB_OF: 1;  /* dac ch1 input stb fifo overflow interrupt status. Write 1 to */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=10,description="" */
        volatile uint32_t RSVD2: 10;
        /* @SVD:field=ADC_CH0_APB_OF,bitOffset=16,bitWidth=1,description="adc ch0 apb fifo overflow interrupt status. Write 1 to clear." */
        volatile uint32_t ADC_CH0_APB_OF: 1;  /* adc ch0 apb fifo overflow interrupt status. Write 1 to clear */
        /* @SVD:field=ADC_CH0_APB_UF,bitOffset=17,bitWidth=1,description="adc ch0 apb fifo underflow interrupt status. Write 1 to clear." */
        volatile uint32_t ADC_CH0_APB_UF: 1;  /* adc ch0 apb fifo underflow interrupt status. Write 1 to clea */
        /* @SVD:field=ADC_CH0_SAT,bitOffset=18,bitWidth=1,description="adc ch0 saturation interrupt" */
        volatile uint32_t ADC_CH0_SAT: 1;  /* adc ch0 saturation interrupt */
        /* @SVD:field=ADC_CH1_APB_OF,bitOffset=19,bitWidth=1,description="adc ch1 apb fifo overflow interrupt status. Write 1 to clear." */
        volatile uint32_t ADC_CH1_APB_OF: 1;  /* adc ch1 apb fifo overflow interrupt status. Write 1 to clear */
        /* @SVD:field=ADC_CH1_APB_UF,bitOffset=20,bitWidth=1,description="adc ch1 apb fifo underflow interrupt status. Write 1 to clear." */
        volatile uint32_t ADC_CH1_APB_UF: 1;  /* adc ch1 apb fifo underflow interrupt status. Write 1 to clea */
        /* @SVD:field=ADC_CH1_SAT,bitOffset=21,bitWidth=1,description="adc ch1 saturation interrupt" */
        volatile uint32_t ADC_CH1_SAT: 1;  /* adc ch1 saturation interrupt */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_IRQ_Type;

/* @SVD:register=IRQ_MSK,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="IRQ_MSK" */
/* IRQ_MSK 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DAC_CH0_APB_OF,bitOffset=0,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t DAC_CH0_APB_OF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=DAC_CH0_OUT_UF,bitOffset=1,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t DAC_CH0_OUT_UF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=DAC_CH0_STB_OF,bitOffset=2,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t DAC_CH0_STB_OF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=DAC_CH1_APB_OF,bitOffset=3,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t DAC_CH1_APB_OF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=DAC_CH1_OUT_UF,bitOffset=4,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t DAC_CH1_OUT_UF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=DAC_CH1_STB_OF,bitOffset=5,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t DAC_CH1_STB_OF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=10,description="" */
        volatile uint32_t RSVD2: 10;
        /* @SVD:field=ADC_CH0_APB_OF,bitOffset=16,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t ADC_CH0_APB_OF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=ADC_CH0_APB_UF,bitOffset=17,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t ADC_CH0_APB_UF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=ADC_CH0_SAT,bitOffset=18,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t ADC_CH0_SAT: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=ADC_CH1_APB_OF,bitOffset=19,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t ADC_CH1_APB_OF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=ADC_CH1_APB_UF,bitOffset=20,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t ADC_CH1_APB_UF: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=ADC_CH1_SAT,bitOffset=21,bitWidth=1,description="interrupt mask. 0: mask the interrupt." */
        volatile uint32_t ADC_CH1_SAT: 1;  /* interrupt mask. 0: mask the interrupt. */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_IRQ_MSK_Type;

/* @SVD:register=DAC_CFG,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="DAC_CFG" */
/* DAC_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OSR_SEL,bitOffset=0,bitWidth=4,description="DAC oversample rate\n              4'b0000: 100\n              4'b0001: 150\n              4'b0010: 200\n              4'b0011: 300(sdm osr = 150)\n              4'b0100: 300(sdm osr = 300)\n              4'b0101: 400\n              4'b0110: 600\n              4'b0111: 800\n              4'b1000: 1200\n              4'b1001: 256\n              4'b1010: 512\n              4'b1011: 1024\n              other: reserved" */
        volatile uint32_t OSR_SEL: 4;  /* DAC oversample rate               4'b0000: 100               */
        /* @SVD:field=OP_MODE,bitOffset=4,bitWidth=2,description="dac operation mode\n              2'h0: normal mode: send dac data through tx interface\n              2'h1: apb mode: send dac data out through apb interface\n              2'h2, 2'h3: reserved" */
        volatile uint32_t OP_MODE: 2;  /* dac operation mode               2'h0: normal mode: send dac */
        /* @SVD:field=PATH_RESET,bitOffset=6,bitWidth=1,description="dac path reset, set 1 to reset dac path" */
        volatile uint32_t PATH_RESET: 1;  /* dac path reset, set 1 to reset dac path */
        /* @SVD:field=CLK_SRC_SEL,bitOffset=7,bitWidth=1,description="dac clock source select\n              1: pll\n              0: xtal" */
        volatile uint32_t CLK_SRC_SEL: 1;  /* dac clock source select               1: pll               0 */
        /* @SVD:field=CLK_DIV,bitOffset=8,bitWidth=8,description="dac clock divider" */
        volatile uint32_t CLK_DIV: 8;  /* dac clock divider */
        /* @SVD:field=MANUAL_OSR_MODE,bitOffset=16,bitWidth=1,description="set 1 to manually set hbf, interp3, sinc and sdm module" */
        volatile uint32_t MANUAL_OSR_MODE: 1;  /* set 1 to manually set hbf, interp3, sinc and sdm module */
        /* @SVD:field=HBF1_BYPASS_M,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t HBF1_BYPASS_M: 1;
        /* @SVD:field=HBF2_BYPASS_M,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t HBF2_BYPASS_M: 1;
        /* @SVD:field=HBF3_BYPASS_M,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t HBF3_BYPASS_M: 1;
        /* @SVD:field=HBF4_BYPASS_M,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t HBF4_BYPASS_M: 1;
        /* @SVD:field=INTERP3_BYPASS_M,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t INTERP3_BYPASS_M: 1;
        /* @SVD:field=SINC_RATE_SEL_M,bitOffset=22,bitWidth=3,description="0:25  1:50 2:16 3:32 4:64" */
        volatile uint32_t SINC_RATE_SEL_M: 3;  /* 0:25  1:50 2:16 3:32 4:64 */
        /* @SVD:field=SDM_OSR_SEL_M,bitOffset=25,bitWidth=2,description="0:100 1:150 2:300 3:256" */
        volatile uint32_t SDM_OSR_SEL_M: 2;  /* 0:100 1:150 2:300 3:256 */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CFG_Type;

/* @SVD:register=ADC_CFG,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="ADC_CFG" */
/* ADC_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OSR_SEL,bitOffset=0,bitWidth=3,description="ADC oversample rate\n              3'b000: 200\n              3'b001: 300\n              3'b010: 400\n              3'b011: 600\n              other: reserved" */
        volatile uint32_t OSR_SEL: 3;  /* ADC oversample rate               3'b000: 200                */
        /* @SVD:field=OP_MODE,bitOffset=3,bitWidth=2,description="adc operation mode\n              2'h0: normal mode: send adc data out through rx interface\n              2'h1: apb mode: send adc data out through apb interface\n              2'h2: raw data apb mode: send adc raw data out through apb interface\n              2'h3: reserved" */
        volatile uint32_t OP_MODE: 2;  /* adc operation mode               2'h0: normal mode: send adc */
        /* @SVD:field=PATH_RESET,bitOffset=5,bitWidth=1,description="adc path reset, set 1 to reset adc path" */
        volatile uint32_t PATH_RESET: 1;  /* adc path reset, set 1 to reset adc path */
        /* @SVD:field=CLK_SRC_SEL,bitOffset=6,bitWidth=1,description="adc clock source select\n              1: pll\n              0: xtal" */
        volatile uint32_t CLK_SRC_SEL: 1;  /* adc clock source select               1: pll               0 */
        /* @SVD:field=RSVD2,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CLK_DIV,bitOffset=8,bitWidth=8,description="adc clock divider" */
        volatile uint32_t CLK_DIV: 8;  /* adc clock divider */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC_CFG_Type;

/* @SVD:register=APB_STAT,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="APB_STAT" */
/* APB_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DAC_CH0_FIFO_CNT,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t DAC_CH0_FIFO_CNT: 4;
        /* @SVD:field=DAC_CH1_FIFO_CNT,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t DAC_CH1_FIFO_CNT: 4;
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=ADC_CH0_FIFO_CNT,bitOffset=16,bitWidth=4,description="" */
        volatile uint32_t ADC_CH0_FIFO_CNT: 4;
        /* @SVD:field=ADC_CH1_FIFO_CNT,bitOffset=20,bitWidth=4,description="" */
        volatile uint32_t ADC_CH1_FIFO_CNT: 4;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_APB_STAT_Type;

/* @SVD:register=ADC_CH0_CFG,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="ADC_CH0_CFG" */
/* ADC_CH0_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="adc channel enable" */
        volatile uint32_t ENABLE: 1;  /* adc channel enable */
        /* @SVD:field=HPF_BYPASS,bitOffset=1,bitWidth=1,description="high-pass filter bypass" */
        volatile uint32_t HPF_BYPASS: 1;  /* high-pass filter bypass */
        /* @SVD:field=HPF_COEF,bitOffset=2,bitWidth=4,description="high-pass filter coefficient" */
        volatile uint32_t HPF_COEF: 4;  /* high-pass filter coefficient */
        /* @SVD:field=STB_INV,bitOffset=6,bitWidth=1,description="adc strobe inverter" */
        volatile uint32_t STB_INV: 1;  /* adc strobe inverter */
        /* @SVD:field=DMA_EN,bitOffset=7,bitWidth=1,description="dma interface enable in apb mode and raw data apb mode\n              1: enable adc ch0 dma request interface\n              0: disable adc ch0 dma request interface" */
        volatile uint32_t DMA_EN: 1;  /* dma interface enable in apb mode and raw data apb mode       */
        /* @SVD:field=ROUGH_VOL,bitOffset=8,bitWidth=4,description="adc rough volume control\n              range from -60dB to 30dB\n              step is 6dB\n              4'h0: -60dB\n              4'h1: -54dB\n              ......\n              4'ha: 0dB\n              ......\n              4'he: 24dB\n              4'hf: 30dB" */
        volatile uint32_t ROUGH_VOL: 4;  /* adc rough volume control               range from -60dB to 3 */
        /* @SVD:field=FINE_VOL,bitOffset=12,bitWidth=4,description="adc fine volume control\n              range from 0dB to 6dB\n              step is 0.5dB\n              4'h0: 0dB\n              4'h1: 0.5dB\n              ......\n              4'hb: 5.5dB\n              4'hc, 4'hd, 4'he, 4'hf: mute" */
        volatile uint32_t FINE_VOL: 4;  /* adc fine volume control               range from 0dB to 6dB  */
        /* @SVD:field=DATA_FORMAT,bitOffset=16,bitWidth=1,description="adc data format\n              1: 16-bit\n              0: 24-bit\n              this bit only affect the data format accessed by apb interface. For 24-bit, every 24-bit data occupied 32-bit word. Bit[31:24] are zeros. For 16-bit mode, every 32-bit word contains two 16-bit audio data{D1, D0}" */
        volatile uint32_t DATA_FORMAT: 1;  /* adc data format               1: 16-bit               0: 24- */
        /* @SVD:field=SAT_DET_EN,bitOffset=17,bitWidth=1,description="adc saturation detect" */
        volatile uint32_t SAT_DET_EN: 1;  /* adc saturation detect */
        /* @SVD:field=SAT_DET_LEN,bitOffset=18,bitWidth=2,description="adc saturation detect pattern length\n              2'b00: 16\n              2'b01: 24\n              2'b10: 32\n              2'b11: 48" */
        volatile uint32_t SAT_DET_LEN: 2;  /* adc saturation detect pattern length               2'b00: 16 */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC_CH0_CFG_Type;

/* @SVD:register=ADC_CH1_CFG,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="ADC_CH1_CFG" */
/* ADC_CH1_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="adc channel enable" */
        volatile uint32_t ENABLE: 1;  /* adc channel enable */
        /* @SVD:field=HPF_BYPASS,bitOffset=1,bitWidth=1,description="high-pass filter bypass" */
        volatile uint32_t HPF_BYPASS: 1;  /* high-pass filter bypass */
        /* @SVD:field=HPF_COEF,bitOffset=2,bitWidth=4,description="high-pass filter coefficient" */
        volatile uint32_t HPF_COEF: 4;  /* high-pass filter coefficient */
        /* @SVD:field=STB_INV,bitOffset=6,bitWidth=1,description="adc strobe inverter" */
        volatile uint32_t STB_INV: 1;  /* adc strobe inverter */
        /* @SVD:field=DMA_EN,bitOffset=7,bitWidth=1,description="dma interface enable in apb mode and raw data apb mode\n              1: enable adc ch0 dma request interface\n              0: disable adc ch0 dma request interface" */
        volatile uint32_t DMA_EN: 1;  /* dma interface enable in apb mode and raw data apb mode       */
        /* @SVD:field=ROUGH_VOL,bitOffset=8,bitWidth=4,description="adc rough volume control\n              range from -60dB to 30dB\n              step is 6dB\n              4'h0: -60dB\n              4'h1: -54dB\n              ......\n              4'ha: 0dB\n              ......\n              4'he: 24dB\n              4'hf: 30dB" */
        volatile uint32_t ROUGH_VOL: 4;  /* adc rough volume control               range from -60dB to 3 */
        /* @SVD:field=FINE_VOL,bitOffset=12,bitWidth=4,description="adc fine volume control\n              range from 0dB to 6dB\n              step is 0.5dB\n              4'h0: 0dB\n              4'h1: 0.5dB\n              ......\n              4'hb: 5.5dB\n              4'hc, 4'hd, 4'he, 4'hf: mute" */
        volatile uint32_t FINE_VOL: 4;  /* adc fine volume control               range from 0dB to 6dB  */
        /* @SVD:field=DATA_FORMAT,bitOffset=16,bitWidth=1,description="adc data format\n              1: 16-bit\n              0: 24-bit\n              this bit only affect the data format accessed by apb interface. For 24-bit, every 24-bit data occupied 32-bit word. Bit[31:24] are zeros. For 16-bit mode, every 32-bit word contains two 16-bit audio data{D1, D0}" */
        volatile uint32_t DATA_FORMAT: 1;  /* adc data format               1: 16-bit               0: 24- */
        /* @SVD:field=SAT_DET_EN,bitOffset=17,bitWidth=1,description="adc saturation detect" */
        volatile uint32_t SAT_DET_EN: 1;  /* adc saturation detect */
        /* @SVD:field=SAT_DET_LEN,bitOffset=18,bitWidth=2,description="adc saturation detect pattern length\n              2'b00: 16\n              2'b01: 24\n              2'b10: 32\n              2'b11: 48" */
        volatile uint32_t SAT_DET_LEN: 2;  /* adc saturation detect pattern length               2'b00: 16 */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC_CH1_CFG_Type;

/* @SVD:register=DAC_CH0_CFG,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="DAC_CH0_CFG" */
/* DAC_CH0_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="dac channel enable" */
        volatile uint32_t ENABLE: 1;  /* dac channel enable */
        /* @SVD:field=DOUT_MUTE,bitOffset=1,bitWidth=1,description="dac output mute, set 1 to mute the output" */
        volatile uint32_t DOUT_MUTE: 1;  /* dac output mute, set 1 to mute the output */
        /* @SVD:field=DEM_MODE,bitOffset=2,bitWidth=2,description="dem output mode\n              2'h0: no shift for dem output\n              2'h1: shift dem output incrementally\n              2'h2: shift dem output according to input\n              2'h3: reserved" */
        volatile uint32_t DEM_MODE: 2;  /* dem output mode               2'h0: no shift for dem output  */
        /* @SVD:field=STB_FIFO_CNT,bitOffset=4,bitWidth=3,description="dac input stb fifo cnt" */
        volatile uint32_t STB_FIFO_CNT: 3;  /* dac input stb fifo cnt */
        /* @SVD:field=DMA_EN,bitOffset=7,bitWidth=1,description="dma interface enable in apb mode\n              1: enable dac ch0 dma request interface\n              0: disable dac ch0 dma request interface" */
        volatile uint32_t DMA_EN: 1;  /* dma interface enable in apb mode               1: enable dac */
        /* @SVD:field=ROUGH_VOL,bitOffset=8,bitWidth=4,description="dac rough volume control\n              range from -36dB to 54dB\n              step is 6dB\n              4'h0: -36dB\n              4'h1: -30dB\n              ......\n              4'h6: 0dB\n              ......\n              4'he: 48dB\n              4'hf: 54dB" */
        volatile uint32_t ROUGH_VOL: 4;  /* dac rough volume control               range from -36dB to 5 */
        /* @SVD:field=FINE_VOL,bitOffset=12,bitWidth=4,description="dac fine volume control\n              range from 0dB to 6dB\n              step is 0.5dB\n              4'h0: 0dB\n              4'h1: 0.5dB\n              ......\n              4'hb: 5.5dB\n              4'hc, 4'hd, 4'he, 4'hf: mute" */
        volatile uint32_t FINE_VOL: 4;  /* dac fine volume control               range from 0dB to 6dB  */
        /* @SVD:field=DATA_FORMAT,bitOffset=16,bitWidth=1,description="dac data format\n              1: 16-bit\n              0: 24-bit\n              this bit only affect the data format accessed by apb interface. For 24-bit, every 24-bit data occupied 32-bit word. Bit[31:24] are zeros. For 16-bit mode, every 32-bit word contains two 16-bit audio data{D1, D0}" */
        volatile uint32_t DATA_FORMAT: 1;  /* dac data format               1: 16-bit               0: 24- */
        /* @SVD:field=SINC_GAIN,bitOffset=17,bitWidth=9,description="dac sinc filter gain" */
        volatile uint32_t SINC_GAIN: 9;  /* dac sinc filter gain */
        /* @SVD:field=DITHER_GAIN,bitOffset=26,bitWidth=3,description="sdm dither gain" */
        volatile uint32_t DITHER_GAIN: 3;  /* sdm dither gain */
        /* @SVD:field=DITHER_EN,bitOffset=29,bitWidth=1,description="sdm dither enable" */
        volatile uint32_t DITHER_EN: 1;  /* sdm dither enable */
        /* @SVD:field=CLK_ANA_POL,bitOffset=30,bitWidth=1,description="analog dac clock polarity" */
        volatile uint32_t CLK_ANA_POL: 1;  /* analog dac clock polarity */
        /* @SVD:field=RSVD,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH0_CFG_Type;

/* @SVD:register=DAC_CH0_CFG_EXT,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="DAC_CH0_CFG_EXT" */
/* DAC_CH0_CFG_EXT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RAMP_EN,bitOffset=0,bitWidth=1,description="volume ramp enable" */
        volatile uint32_t RAMP_EN: 1;  /* volume ramp enable */
        /* @SVD:field=RAMP_MODE,bitOffset=1,bitWidth=1,description="volume ramp mode:\n              1: slowly ramp to target volume. Step is 0.5db\n              0: directly ramp to target volume." */
        volatile uint32_t RAMP_MODE: 1;  /* volume ramp mode:               1: slowly ramp to target vol */
        /* @SVD:field=ZERO_ADJUST_EN,bitOffset=2,bitWidth=1,description="enable volume adjustment during 0 volume cross." */
        volatile uint32_t ZERO_ADJUST_EN: 1;  /* enable volume adjustment during 0 volume cross. */
        /* @SVD:field=RAMP_INTERVAL,bitOffset=3,bitWidth=4,description="volume ramp interval." */
        volatile uint32_t RAMP_INTERVAL: 4;  /* volume ramp interval. */
        /* @SVD:field=RAMP_STAT,bitOffset=7,bitWidth=2,description="ramp module status" */
        volatile uint32_t RAMP_STAT: 2;  /* ramp module status */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH0_CFG_EXT_Type;

/* @SVD:register=DAC_CH1_CFG,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="DAC_CH1_CFG" */
/* DAC_CH1_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="dac channel enable" */
        volatile uint32_t ENABLE: 1;  /* dac channel enable */
        /* @SVD:field=DOUT_MUTE,bitOffset=1,bitWidth=1,description="dac output mute, set 1 to mute the output" */
        volatile uint32_t DOUT_MUTE: 1;  /* dac output mute, set 1 to mute the output */
        /* @SVD:field=DEM_MODE,bitOffset=2,bitWidth=2,description="dem output mode\n              2'h0: no shift for dem output\n              2'h1: shift dem output incrementally\n              2'h2: shift dem output according to input\n              2'h3: reserved" */
        volatile uint32_t DEM_MODE: 2;  /* dem output mode               2'h0: no shift for dem output  */
        /* @SVD:field=STB_FIFO_CNT,bitOffset=4,bitWidth=3,description="dac input stb fifo cnt" */
        volatile uint32_t STB_FIFO_CNT: 3;  /* dac input stb fifo cnt */
        /* @SVD:field=DMA_EN,bitOffset=7,bitWidth=1,description="dma interface enable in apb mode\n              1: enable dac ch0 dma request interface\n              0: disable dac ch0 dma request interface" */
        volatile uint32_t DMA_EN: 1;  /* dma interface enable in apb mode               1: enable dac */
        /* @SVD:field=ROUGH_VOL,bitOffset=8,bitWidth=4,description="dac rough volume control\n              range from -36dB to 54dB\n              step is 6dB\n              4'h0: -36dB\n              4'h1: -30dB\n              ......\n              4'h6: 0dB\n              ......\n              4'he: 48dB\n              4'hf: 54dB" */
        volatile uint32_t ROUGH_VOL: 4;  /* dac rough volume control               range from -36dB to 5 */
        /* @SVD:field=FINE_VOL,bitOffset=12,bitWidth=4,description="dac fine volume control\n              range from 0dB to 6dB\n              step is 0.5dB\n              4'h0: 0dB\n              4'h1: 0.5dB\n              ......\n              4'hb: 5.5dB\n              4'hc, 4'hd, 4'he, 4'hf: mute" */
        volatile uint32_t FINE_VOL: 4;  /* dac fine volume control               range from 0dB to 6dB  */
        /* @SVD:field=DATA_FORMAT,bitOffset=16,bitWidth=1,description="dac data format\n              1: 16-bit\n              0: 24-bit\n              this bit only affect the data format accessed by apb interface. For 24-bit, every 24-bit data occupied 32-bit word. Bit[31:24] are zeros. For 16-bit mode, every 32-bit word contains two 16-bit audio data{D1, D0}" */
        volatile uint32_t DATA_FORMAT: 1;  /* dac data format               1: 16-bit               0: 24- */
        /* @SVD:field=SINC_GAIN,bitOffset=17,bitWidth=9,description="dac sinc filter gain" */
        volatile uint32_t SINC_GAIN: 9;  /* dac sinc filter gain */
        /* @SVD:field=DITHER_GAIN,bitOffset=26,bitWidth=3,description="sdm dither gain" */
        volatile uint32_t DITHER_GAIN: 3;  /* sdm dither gain */
        /* @SVD:field=DITHER_EN,bitOffset=29,bitWidth=1,description="sdm dither enable" */
        volatile uint32_t DITHER_EN: 1;  /* sdm dither enable */
        /* @SVD:field=CLK_ANA_POL,bitOffset=30,bitWidth=1,description="analog dac clock polarity" */
        volatile uint32_t CLK_ANA_POL: 1;  /* analog dac clock polarity */
        /* @SVD:field=RSVD,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH1_CFG_Type;

/* @SVD:register=DAC_CH1_CFG_EXT,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="DAC_CH1_CFG_EXT" */
/* DAC_CH1_CFG_EXT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RAMP_EN,bitOffset=0,bitWidth=1,description="volume ramp enable" */
        volatile uint32_t RAMP_EN: 1;  /* volume ramp enable */
        /* @SVD:field=RAMP_MODE,bitOffset=1,bitWidth=1,description="volume ramp mode:\n              1: slowly ramp to target volume. Step is 0.5db\n              0: directly ramp to target volume." */
        volatile uint32_t RAMP_MODE: 1;  /* volume ramp mode:               1: slowly ramp to target vol */
        /* @SVD:field=ZERO_ADJUST_EN,bitOffset=2,bitWidth=1,description="enable volume adjustment during 0 volume cross." */
        volatile uint32_t ZERO_ADJUST_EN: 1;  /* enable volume adjustment during 0 volume cross. */
        /* @SVD:field=RAMP_INTERVAL,bitOffset=3,bitWidth=4,description="volume ramp interval." */
        volatile uint32_t RAMP_INTERVAL: 4;  /* volume ramp interval. */
        /* @SVD:field=RAMP_STAT,bitOffset=7,bitWidth=2,description="ramp module status" */
        volatile uint32_t RAMP_STAT: 2;  /* ramp module status */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH1_CFG_EXT_Type;

/* @SVD:register=ADC_CH0_ENTRY,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="ADC_CH0_ENTRY" */
/* ADC_CH0_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="adc channel0 data output" */
        volatile uint32_t DATA: 32;  /* adc channel0 data output */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC_CH0_ENTRY_Type;

/* @SVD:register=ADC_CH1_ENTRY,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="ADC_CH1_ENTRY" */
/* ADC_CH1_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="adc channel1 data output" */
        volatile uint32_t DATA: 32;  /* adc channel1 data output */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC_CH1_ENTRY_Type;

/* @SVD:register=DAC_CH0_ENTRY,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="DAC_CH0_ENTRY" */
/* DAC_CH0_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="dac channel0 data input" */
        volatile uint32_t DATA: 32;  /* dac channel0 data input */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH0_ENTRY_Type;

/* @SVD:register=DAC_CH1_ENTRY,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="DAC_CH1_ENTRY" */
/* DAC_CH1_ENTRY 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="dac channel0 data input" */
        volatile uint32_t DATA: 32;  /* dac channel0 data input */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH1_ENTRY_Type;

/* @SVD:register=DAC_CH0_DEBUG,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="DAC_CH0_DEBUG" */
/* DAC_CH0_DEBUG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA_OUT,bitOffset=0,bitWidth=16,description="debug dac output" */
        volatile uint32_t DATA_OUT: 16;  /* debug dac output */
        /* @SVD:field=BYPASS,bitOffset=16,bitWidth=1,description="debug bypass mode" */
        volatile uint32_t BYPASS: 1;  /* debug bypass mode */
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH0_DEBUG_Type;

/* @SVD:register=DAC_CH1_DEBUG,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="DAC_CH1_DEBUG" */
/* DAC_CH1_DEBUG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA_OUT,bitOffset=0,bitWidth=16,description="debug dac output" */
        volatile uint32_t DATA_OUT: 16;  /* debug dac output */
        /* @SVD:field=BYPASS,bitOffset=16,bitWidth=1,description="debug bypass mode" */
        volatile uint32_t BYPASS: 1;  /* debug bypass mode */
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH1_DEBUG_Type;

/* @SVD:register=DAC_CH0_DC,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="DAC_CH0_DC" */
/* DAC_CH0_DC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OFFSET,bitOffset=0,bitWidth=24,description="dac ch0 dc offset" */
        volatile uint32_t OFFSET: 24;  /* dac ch0 dc offset */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH0_DC_Type;

/* @SVD:register=DAC_CH1_DC,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="DAC_CH1_DC" */
/* DAC_CH1_DC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OFFSET,bitOffset=0,bitWidth=24,description="dac ch1 dc offset" */
        volatile uint32_t OFFSET: 24;  /* dac ch1 dc offset */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC_CH1_DC_Type;

/* @SVD:register=COMMON_CFG,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="COMMON_CFG" */
/* COMMON_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DC_TR,bitOffset=0,bitWidth=3,description="DC test point select" */
        volatile uint32_t DC_TR: 3;  /* DC test point select */
        /* @SVD:field=DC_BR,bitOffset=3,bitWidth=3,description="DC test Block select" */
        volatile uint32_t DC_BR: 3;  /* DC test Block select */
        /* @SVD:field=DC_MR,bitOffset=6,bitWidth=3,description="DC test Macro select" */
        volatile uint32_t DC_MR: 3;  /* DC test Macro select */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_COMMON_CFG_Type;

/* @SVD:register=BG_CFG0,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="BG_CFG0" */
/* BG_CFG0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="enable bandgap" */
        volatile uint32_t EN: 1;  /* enable bandgap */
        /* @SVD:field=LP_MODE,bitOffset=1,bitWidth=1,description="1: bandgap lp mode" */
        volatile uint32_t LP_MODE: 1;  /* 1: bandgap lp mode */
        /* @SVD:field=VREF_SEL,bitOffset=2,bitWidth=4,description="set vref, 12: 2.2V" */
        volatile uint32_t VREF_SEL: 4;  /* set vref, 12: 2.2V */
        /* @SVD:field=EN_CHOP,bitOffset=6,bitWidth=1,description="enable bandgap chop" */
        volatile uint32_t EN_CHOP: 1;  /* enable bandgap chop */
        /* @SVD:field=EN_SMPL,bitOffset=7,bitWidth=1,description="enable bandgap sample" */
        volatile uint32_t EN_SMPL: 1;  /* enable bandgap sample */
        /* @SVD:field=EN_RCFLT,bitOffset=8,bitWidth=1,description="enable bandgap rc filter" */
        volatile uint32_t EN_RCFLT: 1;  /* enable bandgap rc filter */
        /* @SVD:field=MIC_VREF_SEL,bitOffset=9,bitWidth=3,description="select mic vref" */
        volatile uint32_t MIC_VREF_SEL: 3;  /* select mic vref */
        /* @SVD:field=EN_AMP,bitOffset=12,bitWidth=1,description="enable bg opamp" */
        volatile uint32_t EN_AMP: 1;  /* enable bg opamp */
        /* @SVD:field=SET_VC,bitOffset=13,bitWidth=1,description="set vc" */
        volatile uint32_t SET_VC: 1;  /* set vc */
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_BG_CFG0_Type;

/* @SVD:register=BG_CFG1,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="BG_CFG1" */
/* BG_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SAMPCLK_HI,bitOffset=0,bitWidth=32,description="bg sample clock high cycle width, based on  0: stop bg sample clock" */
        volatile uint32_t SAMPCLK_HI: 32;  /* bg sample clock high cycle width, based on  0: stop bg sampl */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_BG_CFG1_Type;

/* @SVD:register=BG_CFG2,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="BG_CFG2" */
/* BG_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SAMPCLK_LO,bitOffset=0,bitWidth=32,description="bg sample clock low cycle width. 0: stop bg sample clock" */
        volatile uint32_t SAMPCLK_LO: 32;  /* bg sample clock low cycle width. 0: stop bg sample clock */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_BG_CFG2_Type;

/* @SVD:register=REFGEN_CFG,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="REFGEN_CFG" */
/* REFGEN_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="enable ref gen" */
        volatile uint32_t EN: 1;  /* enable ref gen */
        /* @SVD:field=EN_CHOP,bitOffset=1,bitWidth=1,description="enable ref gen chop" */
        volatile uint32_t EN_CHOP: 1;  /* enable ref gen chop */
        /* @SVD:field=BM,bitOffset=2,bitWidth=2,description="bias mode" */
        volatile uint32_t BM: 2;  /* bias mode */
        /* @SVD:field=LP_MODE,bitOffset=4,bitWidth=1,description="1: lpmode(adc), 0:dac" */
        volatile uint32_t LP_MODE: 1;  /* 1: lpmode(adc), 0:dac */
        /* @SVD:field=LV_MODE,bitOffset=5,bitWidth=1,description="low vol mode" */
        volatile uint32_t LV_MODE: 1;  /* low vol mode */
        /* @SVD:field=RZSEL,bitOffset=6,bitWidth=2,description="sel Rz, 0: 1uF cap" */
        volatile uint32_t RZSEL: 2;  /* sel Rz, 0: 1uF cap */
        /* @SVD:field=DISCHG,bitOffset=8,bitWidth=1,description="discharge vref" */
        volatile uint32_t DISCHG: 1;  /* discharge vref */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_REFGEN_CFG_Type;

/* @SVD:register=PLL_CFG0,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="PLL_CFG0" */
/* PLL_CFG0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ICP_OS_SEL,bitOffset=0,bitWidth=5,description="Icp os" */
        volatile uint32_t ICP_OS_SEL: 5;  /* Icp os */
        /* @SVD:field=OPEN,bitOffset=5,bitWidth=1,description="1: pll open" */
        volatile uint32_t OPEN: 1;  /* 1: pll open */
        /* @SVD:field=ICP_SEL,bitOffset=6,bitWidth=5,description="select Icp, 1:1.25u" */
        volatile uint32_t ICP_SEL: 5;  /* select Icp, 1:1.25u */
        /* @SVD:field=SEL_VREF_ANA,bitOffset=11,bitWidth=4,description="ldo vref, 7:1.1V" */
        volatile uint32_t SEL_VREF_ANA: 4;  /* ldo vref, 7:1.1V */
        /* @SVD:field=EN_ANA,bitOffset=15,bitWidth=1,description="enable ana block" */
        volatile uint32_t EN_ANA: 1;  /* enable ana block */
        /* @SVD:field=VCO_LP_MODE,bitOffset=16,bitWidth=1,description="1: lp mode" */
        volatile uint32_t VCO_LP_MODE: 1;  /* 1: lp mode */
        /* @SVD:field=FC_VCO,bitOffset=17,bitWidth=5,description="VCO Fcode" */
        volatile uint32_t FC_VCO: 5;  /* VCO Fcode */
        /* @SVD:field=EN_VCO_FLT,bitOffset=22,bitWidth=1,description="vco bais filter" */
        volatile uint32_t EN_VCO_FLT: 1;  /* vco bais filter */
        /* @SVD:field=SEL_VREF_VCO,bitOffset=23,bitWidth=4,description="ldo vref, 7:1.1V" */
        volatile uint32_t SEL_VREF_VCO: 4;  /* ldo vref, 7:1.1V */
        /* @SVD:field=EN_VCO,bitOffset=27,bitWidth=1,description="enable vco" */
        volatile uint32_t EN_VCO: 1;  /* enable vco */
        /* @SVD:field=EN_IARY,bitOffset=28,bitWidth=1,description="enable I array" */
        volatile uint32_t EN_IARY: 1;  /* enable I array */
        /* @SVD:field=SEL_CKREF,bitOffset=29,bitWidth=2,description="select ref clock, 2: 24MHz" */
        volatile uint32_t SEL_CKREF: 2;  /* select ref clock, 2: 24MHz */
        /* @SVD:field=RSVD,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_CFG0_Type;

/* @SVD:register=PLL_CFG1,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="PLL_CFG1" */
/* PLL_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=R3_SEL,bitOffset=0,bitWidth=4,description="select R3" */
        volatile uint32_t R3_SEL: 4;  /* select R3 */
        /* @SVD:field=RZ_SEL,bitOffset=4,bitWidth=4,description="select Rz" */
        volatile uint32_t RZ_SEL: 4;  /* select Rz */
        /* @SVD:field=C2_SEL,bitOffset=8,bitWidth=3,description="select C2" */
        volatile uint32_t C2_SEL: 3;  /* select C2 */
        /* @SVD:field=CZ_SEL,bitOffset=11,bitWidth=3,description="select Cz" */
        volatile uint32_t CZ_SEL: 3;  /* select Cz */
        /* @SVD:field=CSD_RST,bitOffset=14,bitWidth=1,description="reset CSD, high active" */
        volatile uint32_t CSD_RST: 1;  /* reset CSD, high active */
        /* @SVD:field=CSD_EN,bitOffset=15,bitWidth=1,description="enable CSD" */
        volatile uint32_t CSD_EN: 1;  /* enable CSD */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_CFG1_Type;

/* @SVD:register=PLL_CFG2,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="PLL_CFG2" */
/* PLL_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MMD_STG,bitOffset=0,bitWidth=2,description="mmd stg" */
        volatile uint32_t MMD_STG: 2;  /* mmd stg */
        /* @SVD:field=TR_DTEST,bitOffset=2,bitWidth=4,description="select dtest" */
        volatile uint32_t TR_DTEST: 4;  /* select dtest */
        /* @SVD:field=TE_DTEST,bitOffset=6,bitWidth=1,description="enable dtest" */
        volatile uint32_t TE_DTEST: 1;  /* enable dtest */
        /* @SVD:field=RSTB_SYNC_EN,bitOffset=7,bitWidth=1,description="resetb sync" */
        volatile uint32_t RSTB_SYNC_EN: 1;  /* resetb sync */
        /* @SVD:field=RSTB,bitOffset=8,bitWidth=1,description="resetb" */
        volatile uint32_t RSTB: 1;  /* resetb */
        /* @SVD:field=SEL_VREF_DIG,bitOffset=9,bitWidth=4,description="ldo vref, 7:1.1V" */
        volatile uint32_t SEL_VREF_DIG: 4;  /* ldo vref, 7:1.1V */
        /* @SVD:field=EN_DIG,bitOffset=13,bitWidth=1,description="enable dig block" */
        volatile uint32_t EN_DIG: 1;  /* enable dig block */
        /* @SVD:field=EN_LF_TSTBUF,bitOffset=14,bitWidth=1,description="enable vctrl buf" */
        volatile uint32_t EN_LF_TSTBUF: 1;  /* enable vctrl buf */
        /* @SVD:field=SEL_LF_VCIN,bitOffset=15,bitWidth=3,description="select vcin, 4: 550mV" */
        volatile uint32_t SEL_LF_VCIN: 3;  /* select vcin, 4: 550mV */
        /* @SVD:field=EN_LF_VCIN,bitOffset=18,bitWidth=1,description="enable vcin for vco" */
        volatile uint32_t EN_LF_VCIN: 1;  /* enable vcin for vco */
        /* @SVD:field=RSVD,bitOffset=19,bitWidth=13,description="" */
        volatile uint32_t RSVD: 13;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_CFG2_Type;

/* @SVD:register=PLL_CFG3,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="PLL_CFG3" */
/* PLL_CFG3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SDIN,bitOffset=0,bitWidth=20,description="sdm input" */
        volatile uint32_t SDIN: 20;  /* sdm input */
        /* @SVD:field=FCW,bitOffset=20,bitWidth=5,description="FCW" */
        volatile uint32_t FCW: 5;  /* FCW */
        /* @SVD:field=SDM_UPDATE,bitOffset=25,bitWidth=1,description="write 1 to update FCW and SDIN value" */
        volatile uint32_t SDM_UPDATE: 1;  /* write 1 to update FCW and SDIN value */
        /* @SVD:field=SDMIN_BYPASS,bitOffset=26,bitWidth=1,description="1: bypass FCW and SDIN sdm control signal" */
        volatile uint32_t SDMIN_BYPASS: 1;  /* 1: bypass FCW and SDIN sdm control signal */
        /* @SVD:field=SDM_MODE,bitOffset=27,bitWidth=1,description="sdm mode" */
        volatile uint32_t SDM_MODE: 1;  /* sdm mode */
        /* @SVD:field=EN_SDM_DITHER,bitOffset=28,bitWidth=1,description="enable sdm dither" */
        volatile uint32_t EN_SDM_DITHER: 1;  /* enable sdm dither */
        /* @SVD:field=SDM_DITHER,bitOffset=29,bitWidth=1,description="sdm dither" */
        volatile uint32_t SDM_DITHER: 1;  /* sdm dither */
        /* @SVD:field=EN_SDM,bitOffset=30,bitWidth=1,description="enable sdm" */
        volatile uint32_t EN_SDM: 1;  /* enable sdm */
        /* @SVD:field=SDMCLK_POL,bitOffset=31,bitWidth=1,description="sdm dig clk polarity" */
        volatile uint32_t SDMCLK_POL: 1;  /* sdm dig clk polarity */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_CFG3_Type;

/* @SVD:register=PLL_CFG4,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="PLL_CFG4" */
/* PLL_CFG4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DIVB_CLK_CHOP_DAC,bitOffset=0,bitWidth=2,description="DIVB dac chop clk" */
        volatile uint32_t DIVB_CLK_CHOP_DAC: 2;  /* DIVB dac chop clk */
        /* @SVD:field=DIVA_CLK_CHOP_DAC,bitOffset=2,bitWidth=3,description="DIVA dac chop clk" */
        volatile uint32_t DIVA_CLK_CHOP_DAC: 3;  /* DIVA dac chop clk */
        /* @SVD:field=EN_CLK_CHOP_DAC,bitOffset=5,bitWidth=1,description="enable dac chop clk" */
        volatile uint32_t EN_CLK_CHOP_DAC: 1;  /* enable dac chop clk */
        /* @SVD:field=DIVA_CLK_DAC,bitOffset=6,bitWidth=5,description="DIVA dac clk" */
        volatile uint32_t DIVA_CLK_DAC: 5;  /* DIVA dac clk */
        /* @SVD:field=EN_CLK_DAC,bitOffset=11,bitWidth=1,description="enable dac clk" */
        volatile uint32_t EN_CLK_DAC: 1;  /* enable dac clk */
        /* @SVD:field=SEL_CLK_DAC,bitOffset=12,bitWidth=1,description="1: select 9.6MHz as DAC clock" */
        volatile uint32_t SEL_CLK_DAC: 1;  /* 1: select 9.6MHz as DAC clock */
        /* @SVD:field=SEL_CLK_DAC_SOURCE,bitOffset=13,bitWidth=2,description="0: xtal 1: pll" */
        volatile uint32_t SEL_CLK_DAC_SOURCE: 2;  /* 0: xtal 1: pll */
        /* @SVD:field=SEL_CLK_DIG,bitOffset=15,bitWidth=1,description="select dig clk 0: pll  1: 24MHz from xtal" */
        volatile uint32_t SEL_CLK_DIG: 1;  /* select dig clk 0: pll  1: 24MHz from xtal */
        /* @SVD:field=CLK_DIG_STR,bitOffset=16,bitWidth=2,description="strength" */
        volatile uint32_t CLK_DIG_STR: 2;  /* strength */
        /* @SVD:field=DIVA_CLK_DIG,bitOffset=18,bitWidth=5,description="DIVA dig clk" */
        volatile uint32_t DIVA_CLK_DIG: 5;  /* DIVA dig clk */
        /* @SVD:field=EN_CLK_DIG,bitOffset=23,bitWidth=1,description="enable dig clk" */
        volatile uint32_t EN_CLK_DIG: 1;  /* enable dig clk */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_CFG4_Type;

/* @SVD:register=PLL_CFG5,offset=0x0098,size=32,access=read-write,reset=0x00000000,description="PLL_CFG5" */
/* PLL_CFG5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DIVB_CLK_CHOP_BG,bitOffset=0,bitWidth=2,description="DIVB bg chop clk" */
        volatile uint32_t DIVB_CLK_CHOP_BG: 2;  /* DIVB bg chop clk */
        /* @SVD:field=DIVA_CLK_CHOP_BG,bitOffset=2,bitWidth=5,description="DIVA bg chop clk" */
        volatile uint32_t DIVA_CLK_CHOP_BG: 5;  /* DIVA bg chop clk */
        /* @SVD:field=EN_CLK_CHOP_BG,bitOffset=7,bitWidth=1,description="enable bg chop clk" */
        volatile uint32_t EN_CLK_CHOP_BG: 1;  /* enable bg chop clk */
        /* @SVD:field=DIVB_CLK_CHOP_REFGEN,bitOffset=8,bitWidth=2,description="DIVB ref chop clk" */
        volatile uint32_t DIVB_CLK_CHOP_REFGEN: 2;  /* DIVB ref chop clk */
        /* @SVD:field=DIVA_CLK_CHOP_REFGEN,bitOffset=10,bitWidth=5,description="DIVA ref chop clk" */
        volatile uint32_t DIVA_CLK_CHOP_REFGEN: 5;  /* DIVA ref chop clk */
        /* @SVD:field=EN_CLK_CHOP_REFGEN,bitOffset=15,bitWidth=1,description="enable ref chop clk" */
        volatile uint32_t EN_CLK_CHOP_REFGEN: 1;  /* enable ref chop clk */
        /* @SVD:field=DIVB_CLK_CHOP_DAC2,bitOffset=16,bitWidth=2,description="DIVB dac2 chop clk" */
        volatile uint32_t DIVB_CLK_CHOP_DAC2: 2;  /* DIVB dac2 chop clk */
        /* @SVD:field=DIVA_CLK_CHOP_DAC2,bitOffset=18,bitWidth=3,description="DIVA dac2 chop clk" */
        volatile uint32_t DIVA_CLK_CHOP_DAC2: 3;  /* DIVA dac2 chop clk */
        /* @SVD:field=EN_CLK_CHOP_DAC2,bitOffset=21,bitWidth=1,description="enable dac2 chop clk" */
        volatile uint32_t EN_CLK_CHOP_DAC2: 1;  /* enable dac2 chop clk */
        /* @SVD:field=DIVA_CLK_DAC2,bitOffset=22,bitWidth=5,description="DIVA dac2 clk" */
        volatile uint32_t DIVA_CLK_DAC2: 5;  /* DIVA dac2 clk */
        /* @SVD:field=EN_CLK_DAC2,bitOffset=27,bitWidth=1,description="enable dac2 clk" */
        volatile uint32_t EN_CLK_DAC2: 1;  /* enable dac2 clk */
        /* @SVD:field=SEL_CLK_DAC2,bitOffset=28,bitWidth=1,description="1: select 9.6MHz as DAC clock" */
        volatile uint32_t SEL_CLK_DAC2: 1;  /* 1: select 9.6MHz as DAC clock */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_CFG5_Type;

/* @SVD:register=PLL_CFG6,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="PLL_CFG6" */
/* PLL_CFG6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEL_TST_CLK,bitOffset=0,bitWidth=4,description="select clk to test" */
        volatile uint32_t SEL_TST_CLK: 4;  /* select clk to test */
        /* @SVD:field=EN_TST_CLK,bitOffset=4,bitWidth=1,description="enable test clk" */
        volatile uint32_t EN_TST_CLK: 1;  /* enable test clk */
        /* @SVD:field=EN_CLK_RCCAL,bitOffset=5,bitWidth=1,description="enable RC CAL clk" */
        volatile uint32_t EN_CLK_RCCAL: 1;  /* enable RC CAL clk */
        /* @SVD:field=SEL_CLK_CHOP_MICBIAS,bitOffset=6,bitWidth=2,description="select micbias chop clk" */
        volatile uint32_t SEL_CLK_CHOP_MICBIAS: 2;  /* select micbias chop clk */
        /* @SVD:field=EN_CLK_CHOP_MICBIAS,bitOffset=8,bitWidth=1,description="enable micbias chop clk" */
        volatile uint32_t EN_CLK_CHOP_MICBIAS: 1;  /* enable micbias chop clk */
        /* @SVD:field=SEL_CLK_ADC2,bitOffset=9,bitWidth=1,description="select adc2 clk" */
        volatile uint32_t SEL_CLK_ADC2: 1;  /* select adc2 clk */
        /* @SVD:field=DIVA_CLK_ADC2,bitOffset=10,bitWidth=3,description="DIVA adc2 clk" */
        volatile uint32_t DIVA_CLK_ADC2: 3;  /* DIVA adc2 clk */
        /* @SVD:field=EN_CLK_ADC2,bitOffset=13,bitWidth=1,description="enable adc2 clk" */
        volatile uint32_t EN_CLK_ADC2: 1;  /* enable adc2 clk */
        /* @SVD:field=SEL_CLK_ADC1,bitOffset=14,bitWidth=1,description="select adc1 clk" */
        volatile uint32_t SEL_CLK_ADC1: 1;  /* select adc1 clk */
        /* @SVD:field=DIVA_CLK_ADC1,bitOffset=15,bitWidth=3,description="DIVA adc1 clk" */
        volatile uint32_t DIVA_CLK_ADC1: 3;  /* DIVA adc1 clk */
        /* @SVD:field=EN_CLK_ADC1,bitOffset=18,bitWidth=1,description="enable adc1 clk" */
        volatile uint32_t EN_CLK_ADC1: 1;  /* enable adc1 clk */
        /* @SVD:field=SEL_CLK_ADC0,bitOffset=19,bitWidth=1,description="select adc0 clk" */
        volatile uint32_t SEL_CLK_ADC0: 1;  /* select adc0 clk */
        /* @SVD:field=DIVA_CLK_ADC0,bitOffset=20,bitWidth=3,description="DIVA adc0 clk" */
        volatile uint32_t DIVA_CLK_ADC0: 3;  /* DIVA adc0 clk */
        /* @SVD:field=EN_CLK_ADC0,bitOffset=23,bitWidth=1,description="enable adc0 clk" */
        volatile uint32_t EN_CLK_ADC0: 1;  /* enable adc0 clk */
        /* @SVD:field=SEL_CLK_ADC_SOURCE,bitOffset=24,bitWidth=2,description="0: xtal, 1: pll" */
        volatile uint32_t SEL_CLK_ADC_SOURCE: 2;  /* 0: xtal, 1: pll */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_CFG6_Type;

/* @SVD:register=PLL_STAT,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="PLL_STAT" */
/* PLL_STAT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UNLOCK,bitOffset=0,bitWidth=1,description="1:pll unlock" */
        volatile uint32_t UNLOCK: 1;  /* 1:pll unlock */
        /* @SVD:field=SLIPPED_UP,bitOffset=1,bitWidth=1,description="slip up" */
        volatile uint32_t SLIPPED_UP: 1;  /* slip up */
        /* @SVD:field=SLIPPED_DN,bitOffset=2,bitWidth=1,description="slip dn" */
        volatile uint32_t SLIPPED_DN: 1;  /* slip dn */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=29,description="" */
        volatile uint32_t RSVD: 29;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_STAT_Type;

/* @SVD:register=PLL_CAL_CFG,offset=0x00A4,size=32,access=read-write,reset=0x00000000,description="PLL_CAL_CFG" */
/* PLL_CAL_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="calibration enable" */
        volatile uint32_t EN: 1;  /* calibration enable */
        /* @SVD:field=DONE,bitOffset=1,bitWidth=1,description="calibration done" */
        volatile uint32_t DONE: 1;  /* calibration done */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
        /* @SVD:field=LEN,bitOffset=16,bitWidth=16,description="calibration length" */
        volatile uint32_t LEN: 16;  /* calibration length */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_CAL_CFG_Type;

/* @SVD:register=PLL_CAL_RESULT,offset=0x00A8,size=32,access=read-write,reset=0x00000000,description="PLL_CAL_RESULT" */
/* PLL_CAL_RESULT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=XTAL_CNT,bitOffset=0,bitWidth=16,description="xtal calibration counter result" */
        volatile uint32_t XTAL_CNT: 16;  /* xtal calibration counter result */
        /* @SVD:field=PLL_CNT,bitOffset=16,bitWidth=16,description="pll calibration counter result" */
        volatile uint32_t PLL_CNT: 16;  /* pll calibration counter result */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_PLL_CAL_RESULT_Type;

/* @SVD:register=ADC_ANA_CFG,offset=0x00AC,size=32,access=read-write,reset=0x00000000,description="ADC_ANA_CFG" */
/* ADC_ANA_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MICBIAS_CHOP_EN,bitOffset=0,bitWidth=1,description="micbias chopping enable" */
        volatile uint32_t MICBIAS_CHOP_EN: 1;  /* micbias chopping enable */
        /* @SVD:field=MICBIAS_EN,bitOffset=1,bitWidth=1,description="micbias enable" */
        volatile uint32_t MICBIAS_EN: 1;  /* micbias enable */
        /* @SVD:field=CAPCODE,bitOffset=2,bitWidth=5,description="ADC cap code" */
        volatile uint32_t CAPCODE: 5;  /* ADC cap code */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC_ANA_CFG_Type;

/* @SVD:register=ADC1_CFG1,offset=0x00B0,size=32,access=read-write,reset=0x00000000,description="ADC1_CFG1" */
/* ADC1_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PERI_BM,bitOffset=0,bitWidth=2,description="peripheral circuits biasmode" */
        volatile uint32_t PERI_BM: 2;  /* peripheral circuits biasmode */
        /* @SVD:field=CLKOUT_INV,bitOffset=2,bitWidth=1,description="inverse output clock" */
        volatile uint32_t CLKOUT_INV: 1;  /* inverse output clock */
        /* @SVD:field=VCMST,bitOffset=3,bitWidth=1,description="VCM quick settling" */
        volatile uint32_t VCMST: 1;  /* VCM quick settling */
        /* @SVD:field=FCHOP_SEL,bitOffset=4,bitWidth=2,description="chopping frequncy\n              0x0:÷8\n              0x1:÷16\n              0x2:÷32\n              0x3:÷64" */
        volatile uint32_t FCHOP_SEL: 2;  /* chopping frequncy               0x0:÷8               0x1:÷16 */
        /* @SVD:field=VREF_SEL,bitOffset=6,bitWidth=3,description="vref code from proper vcm in flash7\n              0x0:1.2V\n              0x1:1.4V\n              0x7:2.6V" */
        volatile uint32_t VREF_SEL: 3;  /* vref code from proper vcm in flash7               0x0:1.2V   */
        /* @SVD:field=BM_INT2,bitOffset=9,bitWidth=3,description="bias mode of 2nd and 3rd opamp" */
        volatile uint32_t BM_INT2: 3;  /* bias mode of 2nd and 3rd opamp */
        /* @SVD:field=BM_INT1,bitOffset=12,bitWidth=3,description="bias mode of first opamp" */
        volatile uint32_t BM_INT1: 3;  /* bias mode of first opamp */
        /* @SVD:field=VST_SEL,bitOffset=15,bitWidth=3,description="start voltage\n              0x0:VCM+200mV\n              0x7:VCM+550mV" */
        volatile uint32_t VST_SEL: 3;  /* start voltage               0x0:VCM+200mV               0x7: */
        /* @SVD:field=GC,bitOffset=18,bitWidth=3,description="gaincode:\n              0x0:-6dB\n              0x1:0dB\n              ...\n              0x4:18dB" */
        volatile uint32_t GC: 3;  /* gaincode:               0x0:-6dB               0x1:0dB       */
        /* @SVD:field=DACN_EN,bitOffset=21,bitWidth=1,description="enable negative DAC1" */
        volatile uint32_t DACN_EN: 1;  /* enable negative DAC1 */
        /* @SVD:field=DIFF_EN,bitOffset=22,bitWidth=1,description="enable differential input mode" */
        volatile uint32_t DIFF_EN: 1;  /* enable differential input mode */
        /* @SVD:field=FSP,bitOffset=23,bitWidth=2,description="sampling frequency:\n              0x0:9.6M\n              0x1:8.82M\n              0x2:4.8M\n              0x3:4.41M" */
        volatile uint32_t FSP: 2;  /* sampling frequency:               0x0:9.6M               0x1 */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=7,description="" */
        volatile uint32_t RSVD: 7;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC1_CFG1_Type;

/* @SVD:register=ADC1_CFG2,offset=0x00B4,size=32,access=read-write,reset=0x00000000,description="ADC1_CFG2" */
/* ADC1_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLEAR,bitOffset=0,bitWidth=1,description="clear adc" */
        volatile uint32_t CLEAR: 1;  /* clear adc */
        /* @SVD:field=CHOP_EN,bitOffset=1,bitWidth=1,description="chopping enable" */
        volatile uint32_t CHOP_EN: 1;  /* chopping enable */
        /* @SVD:field=RSTB,bitOffset=2,bitWidth=1,description="reset adc" */
        volatile uint32_t RSTB: 1;  /* reset adc */
        /* @SVD:field=EN,bitOffset=3,bitWidth=1,description="enable adc" */
        volatile uint32_t EN: 1;  /* enable adc */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC1_CFG2_Type;

/* @SVD:register=ADC2_CFG1,offset=0x00B8,size=32,access=read-write,reset=0x00000000,description="ADC2_CFG1" */
/* ADC2_CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PERI_BM,bitOffset=0,bitWidth=2,description="peripheral circuits biasmode" */
        volatile uint32_t PERI_BM: 2;  /* peripheral circuits biasmode */
        /* @SVD:field=CLKOUT_INV,bitOffset=2,bitWidth=1,description="inverse output clock" */
        volatile uint32_t CLKOUT_INV: 1;  /* inverse output clock */
        /* @SVD:field=VCMST,bitOffset=3,bitWidth=1,description="VCM quick settling" */
        volatile uint32_t VCMST: 1;  /* VCM quick settling */
        /* @SVD:field=FCHOP_SEL,bitOffset=4,bitWidth=2,description="chopping frequncy\n              0x0:÷8\n              0x1:÷16\n              0x2:÷32\n              0x3:÷64" */
        volatile uint32_t FCHOP_SEL: 2;  /* chopping frequncy               0x0:÷8               0x1:÷16 */
        /* @SVD:field=VREF_SEL,bitOffset=6,bitWidth=3,description="vref code from proper vcm in flash7\n              0x0:1.2V\n              0x1:1.4V\n              0x7:2.6V" */
        volatile uint32_t VREF_SEL: 3;  /* vref code from proper vcm in flash7               0x0:1.2V   */
        /* @SVD:field=BM_INT2,bitOffset=9,bitWidth=3,description="bias mode of 2nd and 3rd opamp" */
        volatile uint32_t BM_INT2: 3;  /* bias mode of 2nd and 3rd opamp */
        /* @SVD:field=BM_INT1,bitOffset=12,bitWidth=3,description="bias mode of first opamp" */
        volatile uint32_t BM_INT1: 3;  /* bias mode of first opamp */
        /* @SVD:field=VST_SEL,bitOffset=15,bitWidth=3,description="start voltage\n              0x0:VCM+200mV\n              0x7:VCM+550mV" */
        volatile uint32_t VST_SEL: 3;  /* start voltage               0x0:VCM+200mV               0x7: */
        /* @SVD:field=GC,bitOffset=18,bitWidth=5,description="gaincode:\n              0x0:-10dB\n              0xa:0dB\n              0x1e:20dB" */
        volatile uint32_t GC: 5;  /* gaincode:               0x0:-10dB               0xa:0dB      */
        /* @SVD:field=FSP,bitOffset=23,bitWidth=2,description="sampling frequency:\n              0x0:9.6M\n              0x1:8.82M\n              0x2:4.8M\n              0x3:4.41M" */
        volatile uint32_t FSP: 2;  /* sampling frequency:               0x0:9.6M               0x1 */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=7,description="" */
        volatile uint32_t RSVD: 7;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC2_CFG1_Type;

/* @SVD:register=ADC2_CFG2,offset=0x00BC,size=32,access=read-write,reset=0x00000000,description="ADC2_CFG2" */
/* ADC2_CFG2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLEAR,bitOffset=0,bitWidth=1,description="clear adc" */
        volatile uint32_t CLEAR: 1;  /* clear adc */
        /* @SVD:field=CHOP_EN,bitOffset=1,bitWidth=1,description="chopping enable" */
        volatile uint32_t CHOP_EN: 1;  /* chopping enable */
        /* @SVD:field=RSTB,bitOffset=2,bitWidth=1,description="reset adc" */
        volatile uint32_t RSTB: 1;  /* reset adc */
        /* @SVD:field=EN,bitOffset=3,bitWidth=1,description="enable adc" */
        volatile uint32_t EN: 1;  /* enable adc */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_ADC2_CFG2_Type;

/* @SVD:register=DAC1_CFG,offset=0x00C0,size=32,access=read-write,reset=0x00000000,description="DAC1_CFG" */
/* DAC1_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN_OS_DAC,bitOffset=0,bitWidth=1,description="enable os dac" */
        volatile uint32_t EN_OS_DAC: 1;  /* enable os dac */
        /* @SVD:field=OS_DAC,bitOffset=1,bitWidth=7,description="os dac" */
        volatile uint32_t OS_DAC: 7;  /* os dac */
        /* @SVD:field=GAIN,bitOffset=8,bitWidth=4,description="dac gain" */
        volatile uint32_t GAIN: 4;  /* dac gain */
        /* @SVD:field=SR,bitOffset=12,bitWidth=1,description="dac short switch" */
        volatile uint32_t SR: 1;  /* dac short switch */
        /* @SVD:field=POL_CLK,bitOffset=13,bitWidth=1,description="dac clk polarity" */
        volatile uint32_t POL_CLK: 1;  /* dac clk polarity */
        /* @SVD:field=LP_MODE,bitOffset=14,bitWidth=1,description="0: 3.3V sup, 1: 1.8V supply" */
        volatile uint32_t LP_MODE: 1;  /* 0: 3.3V sup, 1: 1.8V supply */
        /* @SVD:field=SEL_VCM,bitOffset=15,bitWidth=3,description="select vcm" */
        volatile uint32_t SEL_VCM: 3;  /* select vcm */
        /* @SVD:field=BM,bitOffset=18,bitWidth=2,description="bias mode" */
        volatile uint32_t BM: 2;  /* bias mode */
        /* @SVD:field=EN_CHOP,bitOffset=20,bitWidth=1,description="enable chop" */
        volatile uint32_t EN_CHOP: 1;  /* enable chop */
        /* @SVD:field=EN_AMP,bitOffset=21,bitWidth=1,description="enable amp" */
        volatile uint32_t EN_AMP: 1;  /* enable amp */
        /* @SVD:field=EN_VCM,bitOffset=22,bitWidth=1,description="enable vcm" */
        volatile uint32_t EN_VCM: 1;  /* enable vcm */
        /* @SVD:field=EN_DAC,bitOffset=23,bitWidth=1,description="enable dac" */
        volatile uint32_t EN_DAC: 1;  /* enable dac */
        /* @SVD:field=SEL_VSTART,bitOffset=24,bitWidth=2,description="select Vstart" */
        volatile uint32_t SEL_VSTART: 2;  /* select Vstart */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC1_CFG_Type;

/* @SVD:register=DAC2_CFG,offset=0x00C4,size=32,access=read-write,reset=0x00000000,description="DAC2_CFG" */
/* DAC2_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN_OS_DAC,bitOffset=0,bitWidth=1,description="enable os dac" */
        volatile uint32_t EN_OS_DAC: 1;  /* enable os dac */
        /* @SVD:field=OS_DAC,bitOffset=1,bitWidth=7,description="os dac" */
        volatile uint32_t OS_DAC: 7;  /* os dac */
        /* @SVD:field=GAIN,bitOffset=8,bitWidth=4,description="dac gain" */
        volatile uint32_t GAIN: 4;  /* dac gain */
        /* @SVD:field=SR,bitOffset=12,bitWidth=1,description="dac short switch" */
        volatile uint32_t SR: 1;  /* dac short switch */
        /* @SVD:field=POL_CLK,bitOffset=13,bitWidth=1,description="dac clk polarity" */
        volatile uint32_t POL_CLK: 1;  /* dac clk polarity */
        /* @SVD:field=LP_MODE,bitOffset=14,bitWidth=1,description="0: 3.3V sup, 1: 1.8V supply" */
        volatile uint32_t LP_MODE: 1;  /* 0: 3.3V sup, 1: 1.8V supply */
        /* @SVD:field=SEL_VCM,bitOffset=15,bitWidth=3,description="select vcm" */
        volatile uint32_t SEL_VCM: 3;  /* select vcm */
        /* @SVD:field=BM,bitOffset=18,bitWidth=2,description="bias mode" */
        volatile uint32_t BM: 2;  /* bias mode */
        /* @SVD:field=EN_CHOP,bitOffset=20,bitWidth=1,description="enable chop" */
        volatile uint32_t EN_CHOP: 1;  /* enable chop */
        /* @SVD:field=EN_AMP,bitOffset=21,bitWidth=1,description="enable amp" */
        volatile uint32_t EN_AMP: 1;  /* enable amp */
        /* @SVD:field=EN_VCM,bitOffset=22,bitWidth=1,description="enable vcm" */
        volatile uint32_t EN_VCM: 1;  /* enable vcm */
        /* @SVD:field=EN_DAC,bitOffset=23,bitWidth=1,description="enable dac" */
        volatile uint32_t EN_DAC: 1;  /* enable dac */
        /* @SVD:field=SEL_VSTART,bitOffset=24,bitWidth=2,description="select Vstart" */
        volatile uint32_t SEL_VSTART: 2;  /* select Vstart */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_DAC2_CFG_Type;

/* @SVD:register=RESERVED_IN0,offset=0x00D0,size=32,access=read-write,reset=0x00000000,description="RESERVED_IN0" */
/* RESERVED_IN0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CTRL0,bitOffset=0,bitWidth=8,description="reserved control 0" */
        volatile uint32_t CTRL0: 8;  /* reserved control 0 */
        /* @SVD:field=CTRL1,bitOffset=8,bitWidth=8,description="reserved control 1" */
        volatile uint32_t CTRL1: 8;  /* reserved control 1 */
        /* @SVD:field=CTRL2,bitOffset=16,bitWidth=8,description="reserved control 2" */
        volatile uint32_t CTRL2: 8;  /* reserved control 2 */
        /* @SVD:field=CTRL3,bitOffset=24,bitWidth=8,description="reserved control 3" */
        volatile uint32_t CTRL3: 8;  /* reserved control 3 */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_RESERVED_IN0_Type;

/* @SVD:register=RESERVED_IN1,offset=0x00D4,size=32,access=read-write,reset=0x00000000,description="RESERVED_IN1" */
/* RESERVED_IN1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CTRL4,bitOffset=0,bitWidth=8,description="reserved control 4" */
        volatile uint32_t CTRL4: 8;  /* reserved control 4 */
        /* @SVD:field=CTRL5,bitOffset=8,bitWidth=8,description="reserved control 5" */
        volatile uint32_t CTRL5: 8;  /* reserved control 5 */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_RESERVED_IN1_Type;

/* @SVD:register=RESERVED_OUT,offset=0x00D8,size=32,access=read-write,reset=0x00000000,description="RESERVED_OUT" */
/* RESERVED_OUT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=STAT0,bitOffset=0,bitWidth=8,description="reserved status0" */
        volatile uint32_t STAT0: 8;  /* reserved status0 */
        /* @SVD:field=STAT1,bitOffset=8,bitWidth=8,description="reserved status1" */
        volatile uint32_t STAT1: 8;  /* reserved status1 */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} AUDCODEC_RESERVED_OUT_Type;

/* AUDCODEC 寄存器结构体 */
typedef struct
{
    AUDCODEC_ID_Type ID;  /* 0x0000 - ID */
    AUDCODEC_CFG_Type CFG;  /* 0x0004 - CFG */
    AUDCODEC_IRQ_Type IRQ;  /* 0x0008 - IRQ */
    AUDCODEC_IRQ_MSK_Type IRQ_MSK;  /* 0x000C - IRQ_MSK */
    AUDCODEC_DAC_CFG_Type DAC_CFG;  /* 0x0010 - DAC_CFG */
    AUDCODEC_ADC_CFG_Type ADC_CFG;  /* 0x0014 - ADC_CFG */
    AUDCODEC_APB_STAT_Type APB_STAT;  /* 0x0018 - APB_STAT */
    /* @SVD:register=RSVD5,offset=0x001C,size=32,access=read-write,reset=0x0,description="RSVD5" */
    volatile uint32_t RSVD5;  /* 0x001C - RSVD5 */
    AUDCODEC_ADC_CH0_CFG_Type ADC_CH0_CFG;  /* 0x0020 - ADC_CH0_CFG */
    AUDCODEC_ADC_CH1_CFG_Type ADC_CH1_CFG;  /* 0x0024 - ADC_CH1_CFG */
    /* @SVD:register=RSVD4,offset=0x0028,size=32,access=read-write,reset=0x0,description="RSVD4" */
    volatile uint32_t RSVD4;  /* 0x0028 - RSVD4 */
    volatile uint32_t RESERVED11[1];  /* 0x002C - 0x002F */
    AUDCODEC_DAC_CH0_CFG_Type DAC_CH0_CFG;  /* 0x0030 - DAC_CH0_CFG */
    AUDCODEC_DAC_CH0_CFG_EXT_Type DAC_CH0_CFG_EXT;  /* 0x0034 - DAC_CH0_CFG_EXT */
    AUDCODEC_DAC_CH1_CFG_Type DAC_CH1_CFG;  /* 0x0038 - DAC_CH1_CFG */
    AUDCODEC_DAC_CH1_CFG_EXT_Type DAC_CH1_CFG_EXT;  /* 0x003C - DAC_CH1_CFG_EXT */
    AUDCODEC_ADC_CH0_ENTRY_Type ADC_CH0_ENTRY;  /* 0x0040 - ADC_CH0_ENTRY */
    AUDCODEC_ADC_CH1_ENTRY_Type ADC_CH1_ENTRY;  /* 0x0044 - ADC_CH1_ENTRY */
    /* @SVD:register=RSVD3,offset=0x0048,size=32,access=read-write,reset=0x0,description="RSVD3" */
    volatile uint32_t RSVD3;  /* 0x0048 - RSVD3 */
    volatile uint32_t RESERVED19[1];  /* 0x004C - 0x004F */
    AUDCODEC_DAC_CH0_ENTRY_Type DAC_CH0_ENTRY;  /* 0x0050 - DAC_CH0_ENTRY */
    AUDCODEC_DAC_CH1_ENTRY_Type DAC_CH1_ENTRY;  /* 0x0054 - DAC_CH1_ENTRY */
    AUDCODEC_DAC_CH0_DEBUG_Type DAC_CH0_DEBUG;  /* 0x0058 - DAC_CH0_DEBUG */
    AUDCODEC_DAC_CH1_DEBUG_Type DAC_CH1_DEBUG;  /* 0x005C - DAC_CH1_DEBUG */
    AUDCODEC_DAC_CH0_DC_Type DAC_CH0_DC;  /* 0x0060 - DAC_CH0_DC */
    AUDCODEC_DAC_CH1_DC_Type DAC_CH1_DC;  /* 0x0064 - DAC_CH1_DC */
    /* @SVD:register=RSVD2,offset=0x0068,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x0068 - RSVD2 */
    volatile uint32_t RESERVED27[1];  /* 0x006C - 0x006F */
    AUDCODEC_COMMON_CFG_Type COMMON_CFG;  /* 0x0070 - COMMON_CFG */
    AUDCODEC_BG_CFG0_Type BG_CFG0;  /* 0x0074 - BG_CFG0 */
    AUDCODEC_BG_CFG1_Type BG_CFG1;  /* 0x0078 - BG_CFG1 */
    AUDCODEC_BG_CFG2_Type BG_CFG2;  /* 0x007C - BG_CFG2 */
    AUDCODEC_REFGEN_CFG_Type REFGEN_CFG;  /* 0x0080 - REFGEN_CFG */
    AUDCODEC_PLL_CFG0_Type PLL_CFG0;  /* 0x0084 - PLL_CFG0 */
    AUDCODEC_PLL_CFG1_Type PLL_CFG1;  /* 0x0088 - PLL_CFG1 */
    AUDCODEC_PLL_CFG2_Type PLL_CFG2;  /* 0x008C - PLL_CFG2 */
    AUDCODEC_PLL_CFG3_Type PLL_CFG3;  /* 0x0090 - PLL_CFG3 */
    AUDCODEC_PLL_CFG4_Type PLL_CFG4;  /* 0x0094 - PLL_CFG4 */
    AUDCODEC_PLL_CFG5_Type PLL_CFG5;  /* 0x0098 - PLL_CFG5 */
    AUDCODEC_PLL_CFG6_Type PLL_CFG6;  /* 0x009C - PLL_CFG6 */
    AUDCODEC_PLL_STAT_Type PLL_STAT;  /* 0x00A0 - PLL_STAT */
    AUDCODEC_PLL_CAL_CFG_Type PLL_CAL_CFG;  /* 0x00A4 - PLL_CAL_CFG */
    AUDCODEC_PLL_CAL_RESULT_Type PLL_CAL_RESULT;  /* 0x00A8 - PLL_CAL_RESULT */
    AUDCODEC_ADC_ANA_CFG_Type ADC_ANA_CFG;  /* 0x00AC - ADC_ANA_CFG */
    AUDCODEC_ADC1_CFG1_Type ADC1_CFG1;  /* 0x00B0 - ADC1_CFG1 */
    AUDCODEC_ADC1_CFG2_Type ADC1_CFG2;  /* 0x00B4 - ADC1_CFG2 */
    AUDCODEC_ADC2_CFG1_Type ADC2_CFG1;  /* 0x00B8 - ADC2_CFG1 */
    AUDCODEC_ADC2_CFG2_Type ADC2_CFG2;  /* 0x00BC - ADC2_CFG2 */
    AUDCODEC_DAC1_CFG_Type DAC1_CFG;  /* 0x00C0 - DAC1_CFG */
    AUDCODEC_DAC2_CFG_Type DAC2_CFG;  /* 0x00C4 - DAC2_CFG */
    /* @SVD:register=RSVD1,offset=0x00C8,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x00C8 - RSVD1 */
    volatile uint32_t RESERVED51[1];  /* 0x00CC - 0x00CF */
    AUDCODEC_RESERVED_IN0_Type RESERVED_IN0;  /* 0x00D0 - RESERVED_IN0 */
    AUDCODEC_RESERVED_IN1_Type RESERVED_IN1;  /* 0x00D4 - RESERVED_IN1 */
    AUDCODEC_RESERVED_OUT_Type RESERVED_OUT;  /* 0x00D8 - RESERVED_OUT */
} AUDCODEC_TypeDef;

/* ============================================================================ */
/* TSEN 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=TSEN,base=0x50089000,description="",groupName=TSEN */

#define TSEN_BASE                                          (0x50089000UL)

/* @SVD:register=TSEN_CTRL_REG,offset=0x0000,size=32,access=read-write,reset=0x04714A44,description="TSEN Analog Control Register " */
/* TSEN_CTRL_REG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ANAU_TSEN_PU,bitOffset=0,bitWidth=1,description="power up tsen" */
        volatile uint32_t ANAU_TSEN_PU: 1;  /* power up tsen */
        /* @SVD:field=ANAU_TSEN_RSTB,bitOffset=1,bitWidth=1,description="resetb for tsen" */
        volatile uint32_t ANAU_TSEN_RSTB: 1;  /* resetb for tsen */
        /* @SVD:field=ANAU_TSEN_RUN,bitOffset=2,bitWidth=1,description="enable tsen run" */
        volatile uint32_t ANAU_TSEN_RUN: 1;  /* enable tsen run */
        /* @SVD:field=ANAU_TSEN_IG_VBE,bitOffset=3,bitWidth=3,description="bias current selection to tune vba" */
        volatile uint32_t ANAU_TSEN_IG_VBE: 3;  /* bias current selection to tune vba */
        /* @SVD:field=ANAU_TSEN_FCK_SEL,bitOffset=6,bitWidth=2,description="select internal clock frequency" */
        volatile uint32_t ANAU_TSEN_FCK_SEL: 2;  /* select internal clock frequency */
        /* @SVD:field=ANAU_TSEN_SGN_EN,bitOffset=8,bitWidth=1,description="signature-mode enable" */
        volatile uint32_t ANAU_TSEN_SGN_EN: 1;  /* signature-mode enable */
        /* @SVD:field=ANAU_TSEN_SER_PAR_SEL,bitOffset=9,bitWidth=1,description="serial-parallel output selection" */
        volatile uint32_t ANAU_TSEN_SER_PAR_SEL: 1;  /* serial-parallel output selection */
        /* @SVD:field=ANAU_TSEN_RDY,bitOffset=10,bitWidth=1,description="tsen ready" */
        volatile uint32_t ANAU_TSEN_RDY: 1;  /* tsen ready */
        /* @SVD:field=ANAU_TSEN_EN,bitOffset=11,bitWidth=1,description="Enable tsen digital module" */
        volatile uint32_t ANAU_TSEN_EN: 1;  /* Enable tsen digital module */
        /* @SVD:field=ANAU_TSEN_CLK_DIV,bitOffset=12,bitWidth=6,description="gen tsen clk by divide hclk by anau_tsen_clk_div" */
        volatile uint32_t ANAU_TSEN_CLK_DIV: 6;  /* gen tsen clk by divide hclk by anau_tsen_clk_div */
        /* @SVD:field=RSVD,bitOffset=18,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TSEN_TSEN_CTRL_REG_Type;

/* @SVD:register=TSEN_RDATA,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Tsen Read Data" */
/* TSEN_RDATA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TSEN_RDATA,bitOffset=0,bitWidth=12,description="" */
        volatile uint32_t TSEN_RDATA: 12;
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TSEN_TSEN_RDATA_Type;

/* @SVD:register=TSEN_IRQ,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Tsen IRQ Register" */
/* TSEN_IRQ 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TSEN_ICR,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t TSEN_ICR: 1;
        /* @SVD:field=TSEN_IMR,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t TSEN_IMR: 1;
        /* @SVD:field=TSEN_IRSR,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t TSEN_IRSR: 1;
        /* @SVD:field=TSEN_ISR,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t TSEN_ISR: 1;
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} TSEN_TSEN_IRQ_Type;

/* TSEN 寄存器结构体 */
typedef struct
{
    TSEN_TSEN_CTRL_REG_Type TSEN_CTRL_REG;  /* 0x0000 - TSEN Analog Control Register */
    TSEN_TSEN_RDATA_Type TSEN_RDATA;  /* 0x0004 - Tsen Read Data */
    TSEN_TSEN_IRQ_Type TSEN_IRQ;  /* 0x0008 - Tsen IRQ Register */
} TSEN_TypeDef;

/* ============================================================================ */
/* GPTIM1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=GPTIM1,base=0x50090000,description="",groupName=GPTIM */

#define GPTIM1_BASE                                        (0x50090000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="TIM control register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CEN,bitOffset=0,bitWidth=1,description="Counter enable\n              0: Counter disabled\n              1: Counter enabled\n              Note: External clock, gated mode and encoder mode can work only if the CEN bit has been previously set by software. However trigger mode can set the CEN bit automatically by hardware. \n              CEN is cleared automatically in one-pulse mode, when an update event occurs." */
        volatile uint32_t CEN: 1;  /* Counter enable               0: Counter disabled             */
        /* @SVD:field=UDIS,bitOffset=1,bitWidth=1,description="Update disable\n              This bit is set and cleared by software to enable/disable UEV event generation.\n              0: UEV enabled. The Update (UEV) event is generated by one of the following events:\n                 Counter overflow/underflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              Buffered registers are then loaded with their preload values.\n              1: UEV disabled. The Update event is not generated, shadow registers keep their value (ARR, PSC, CCRx). However the counter and the prescaler are reinitialized if the UG bit is set or if a hardware reset is received from the slave mode controller." */
        volatile uint32_t UDIS: 1;  /* Update disable               This bit is set and cleared by  */
        /* @SVD:field=URS,bitOffset=2,bitWidth=1,description="Update request source\n              This bit is set and cleared by software to select the UEV event sources.\n              0: Any of the following events generate an update interrupt or DMA request if enabled. \n              These events can be: \n                 Counter overflow/underflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              1: Only counter overflow/underflow generates an update interrupt or DMA request if enabled." */
        volatile uint32_t URS: 1;  /* Update request source               This bit is set and clea */
        /* @SVD:field=OPM,bitOffset=3,bitWidth=1,description="One-pulse mode\n              0: Counter is not stopped at update event\n              1: Counter stops counting at the next update event (clearing the bit CEN)" */
        volatile uint32_t OPM: 1;  /* One-pulse mode               0: Counter is not stopped at up */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="Direction\n              0: Counter used as upcounter\n              1: Counter used as downcounter" */
        volatile uint32_t DIR: 1;  /* Direction               0: Counter used as upcounter         */
        /* @SVD:field=CMS,bitOffset=5,bitWidth=2,description="Center-aligned mode selection\n              00: Edge-aligned mode. The counter counts up or down depending on the direction bit (DIR).\n              01: Center-aligned mode 1. The counter counts up and down alternatively. Output compare interrupt flags of channels configured in output (CCxS=00 in CCMRx register) are set only when the counter is counting down.\n              10: Center-aligned mode 2. The counter counts up and down alternatively. Output compare interrupt flags of channels configured in output (CCxS=00 in CCMRx register) are set only when the counter is counting up.\n              11: Center-aligned mode 3. The counter counts up and down alternatively. Output compare interrupt flags of channels configured in output (CCxS=00 in CCMRx register) are set both when the counter is counting up or down." */
        volatile uint32_t CMS: 2;  /* Center-aligned mode selection               00: Edge-aligned */
        /* @SVD:field=ARPE,bitOffset=7,bitWidth=1,description="Auto-reload preload enable\n              0: ARR register is not buffered\n              1: ARR register is buffered" */
        volatile uint32_t ARPE: 1;  /* Auto-reload preload enable               0: ARR register is  */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=UIFREMAP,bitOffset=11,bitWidth=1,description="UIF status bit remapping\n              0: No remapping. UIF status bit is not copied to CNT register bit 31\n              1: Remapping enabled. UIF status bit is copied to CNT register bit 31" */
        volatile uint32_t UIFREMAP: 1;  /* UIF status bit remapping               0: No remapping. UIF  */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="TIM control register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=CCDS,bitOffset=3,bitWidth=1,description="Capture/compare DMA selection\n              0: CCx DMA request sent when CCx event occurs\n              1: CCx DMA requests sent when update event occurs" */
        volatile uint32_t CCDS: 1;  /* Capture/compare DMA selection               0: CCx DMA reque */
        /* @SVD:field=MMS,bitOffset=4,bitWidth=3,description="Master mode selection\n              These bits allow to select the information to be sent in master mode to slave timers for synchronization (TRGO). The combination is as follows:\n              000: Reset - the UG bit from the EGR register is used as trigger output (TRGO). If the reset is generated by the trigger input (slave mode controller configured in reset mode) then the signal on TRGO is delayed compared to the actual reset.\n              001: Enable - the Counter enable signal is used as trigger output (TRGO). It is useful to start several timers at the same time or to control a window in which a slave timer is enabled. The Counter Enable signal is generated by a logic OR between CEN control bit and the trigger input when configured in gated mode. \n              When the Counter Enable signal is controlled by the trigger input, there is a delay on TRGO, except if the master/slave mode is selected.\n              010: Update - The update event is selected as trigger output (TRGO). For instance a master timer can then be used as a prescaler for a slave timer.\n              011: Compare Pulse - The trigger output send a positive pulse when the CC1IF flag is to be set (even if it was already high), as soon as a capture or a compare match occurred. (TRGO)\n              100: Compare - OC1REF signal is used as trigger output (TRGO)\n              101: Compare - OC2REF signal is used as trigger output (TRGO)\n              110: Compare - OC3REF signal is used as trigger output (TRGO)\n              111: Compare - OC4REF signal is used as trigger output (TRGO)" */
        volatile uint32_t MMS: 3;  /* Master mode selection               These bits allow to sele */
        /* @SVD:field=TI1S,bitOffset=7,bitWidth=1,description="TI1 selection\n              0: The CH1 pin is connected to TI1 input\n              1: The CH1, CH2 and CH3 pins are connected to the TI1 input (XOR combination)" */
        volatile uint32_t TI1S: 1;  /* TI1 selection               0: The CH1 pin is connected to T */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CR2_Type;

/* @SVD:register=SMCR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="TIM slave mode control register" */
/* SMCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=TS,bitOffset=4,bitWidth=3,description="Trigger selection\n              This bit-field selects the trigger input to be used to synchronize the counter.\n              000: Internal Trigger 0 (ITR0)\n              001: Internal Trigger 1 (ITR1) \n              010: Internal Trigger 2 (ITR2)\n              011: Internal Trigger 3 (ITR3)\n              100: TI1 Edge Detector (TI1F_ED)\n              101: Filtered Timer Input 1 (TI1FP1)\n              110: Filtered Timer Input 2 (TI2FP2)\n              111: External Trigger input (ETRF)" */
        volatile uint32_t TS: 3;  /* Trigger selection               This bit-field selects the t */
        /* @SVD:field=MSM,bitOffset=7,bitWidth=1,description="Master/Slave mode\n              0: No action\n              1: The effect of an event on the trigger input (TRGI) is delayed to allow a perfect synchronization between the current timer and its slaves (through TRGO). It is useful if we want to synchronize several timers on a single external event." */
        volatile uint32_t MSM: 1;  /* Master/Slave mode               0: No action               1 */
        /* @SVD:field=ETF,bitOffset=8,bitWidth=4,description="External trigger filter\n              This bit-field then defines the frequency used to sample ETRP signal and the length of the digital filter applied to ETRP. The digital filter is made of an event counter in which N \n              consecutive events are needed to validate a transition on the output:\n              0000: No filter\n              0001:   fSAMPLING=fCLK, N=2\n              0010:   fSAMPLING=fCLK, N=4\n              0011:   fSAMPLING=fCLK, N=8\n              0100:   fSAMPLING=fCLK/2, N=6\n              0101:   fSAMPLING=fCLK/2, N=8\n              0110:   fSAMPLING=fCLK/4, N=6\n              0111:   fSAMPLING=fCLK/4, N=8\n              1000:   fSAMPLING=fCLK/8, N=6\n              1001:   fSAMPLING=fCLK/8, N=8\n              1010:   fSAMPLING=fCLK/16, N=5\n              1011:   fSAMPLING=fCLK/16, N=6\n              1100:   fSAMPLING=fCLK/16, N=8\n              1101:   fSAMPLING=fCLK/32, N=5\n              1110:   fSAMPLING=fCLK/32, N=6\n              1111:   fSAMPLING=fCLK/32, N=8" */
        volatile uint32_t ETF: 4;  /* External trigger filter               This bit-field then de */
        /* @SVD:field=ETPS,bitOffset=12,bitWidth=2,description="External trigger prescaler\n              External trigger signal ETRP frequency must be at most 1/4 of CK_INT frequency. A prescaler can be enabled to reduce ETRP frequency. It is useful when inputting fast external clocks.\n              00: Prescaler OFF\n              01: ETRP frequency divided by 2\n              10: ETRP frequency divided by 4\n              11: ETRP frequency divided by 8" */
        volatile uint32_t ETPS: 2;  /* External trigger prescaler               External trigger si */
        /* @SVD:field=ECE,bitOffset=14,bitWidth=1,description="External clock enable\n              This bit enables External clock mode 2.\n              0: External clock mode 2 disabled\n              1: External clock mode 2 enabled. The counter is clocked by any active edge on the ETRF signal." */
        volatile uint32_t ECE: 1;  /* External clock enable               This bit enables Externa */
        /* @SVD:field=ETP,bitOffset=15,bitWidth=1,description="External trigger polarity\n              0: ETR is non-inverted, active at high level or rising edge\n              1: ETR is inverted, active at low level or falling edge" */
        volatile uint32_t ETP: 1;  /* External trigger polarity               0: ETR is non-invert */
        /* @SVD:field=SMS,bitOffset=16,bitWidth=4,description="Slave mode selection\n              When external signals are selected the active edge of the trigger signal (TRGI) is linked to the polarity selected on the external input.\n              0000: Slave mode disabled.\n              0001: Encoder mode 1 - Counter counts up/down on TI1FP1 edge depending on TI2FP2 level.\n              0010: Encoder mode 2 - Counter counts up/down on TI2FP2 edge depending on TI1FP1 level.\n              0011: Encoder mode 3 - Counter counts up/down on both TI1FP1 and TI2FP2 edges depending on the level of the other input.\n              0100: Reset Mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter and generates an update of the registers.\n              0101: Gated Mode - The counter clock is enabled when the trigger input (TRGI) is high. The counter stops (but is not reset) as soon as the trigger becomes low. Both start and stop of the counter are controlled.\n              0110: Trigger Mode - The counter starts at a rising edge of the trigger TRGI (but it is not reset). Only the start of the counter is controlled.\n              0111: External Clock Mode 1 - Rising edges of the selected trigger (TRGI) clock the counter.\n              1000: Combined reset + trigger mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter, generates an update of the registers and starts the counter." */
        volatile uint32_t SMS: 4;  /* Slave mode selection               When external signals are */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_SMCR_Type;

/* @SVD:register=DIER,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="TIM DMA/Interrupt enable register" */
/* DIER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIE,bitOffset=0,bitWidth=1,description="Update interrupt enable\n              0: Update interrupt disabled.\n              1: Update interrupt enabled" */
        volatile uint32_t UIE: 1;  /* Update interrupt enable               0: Update interrupt di */
        /* @SVD:field=CC1IE,bitOffset=1,bitWidth=1,description="Capture/Compare 1 interrupt enable\n              0: CC1 interrupt disabled.\n              1: CC1 interrupt enabled" */
        volatile uint32_t CC1IE: 1;  /* Capture/Compare 1 interrupt enable               0: CC1 inte */
        /* @SVD:field=CC2IE,bitOffset=2,bitWidth=1,description="Capture/Compare 2 interrupt enable\n              0: CC2 interrupt disabled.\n              1: CC2 interrupt enabled." */
        volatile uint32_t CC2IE: 1;  /* Capture/Compare 2 interrupt enable               0: CC2 inte */
        /* @SVD:field=CC3IE,bitOffset=3,bitWidth=1,description="Capture/Compare 3 interrupt enable\n              0: CC3 interrupt disabled.\n              1: CC3 interrupt enabled" */
        volatile uint32_t CC3IE: 1;  /* Capture/Compare 3 interrupt enable               0: CC3 inte */
        /* @SVD:field=CC4IE,bitOffset=4,bitWidth=1,description="Capture/Compare 4 interrupt enable\n              0: CC4 interrupt disabled.\n              1: CC4 interrupt enabled" */
        volatile uint32_t CC4IE: 1;  /* Capture/Compare 4 interrupt enable               0: CC4 inte */
        /* @SVD:field=RSVD4,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=TIE,bitOffset=6,bitWidth=1,description="Trigger interrupt enable\n              0: Trigger interrupt disabled.\n              1: Trigger interrupt enabled" */
        volatile uint32_t TIE: 1;  /* Trigger interrupt enable               0: Trigger interrupt  */
        /* @SVD:field=RSVD3,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=UDE,bitOffset=8,bitWidth=1,description="Update DMA request enable\n              0: Update DMA request disabled.\n              1: Update DMA request enabled" */
        volatile uint32_t UDE: 1;  /* Update DMA request enable               0: Update DMA reques */
        /* @SVD:field=CC1DE,bitOffset=9,bitWidth=1,description="Capture/Compare 1 DMA request enable\n              0: CC1 DMA request disabled.\n              1: CC1 DMA request enabled." */
        volatile uint32_t CC1DE: 1;  /* Capture/Compare 1 DMA request enable               0: CC1 DM */
        /* @SVD:field=CC2DE,bitOffset=10,bitWidth=1,description="Capture/Compare 2 DMA request enable\n              0: CC2 DMA request disabled.\n              1: CC2 DMA request enabled." */
        volatile uint32_t CC2DE: 1;  /* Capture/Compare 2 DMA request enable               0: CC2 DM */
        /* @SVD:field=CC3DE,bitOffset=11,bitWidth=1,description="Capture/Compare 3 DMA request enable\n              0: CC3 DMA request disabled.\n              1: CC3 DMA request enabled." */
        volatile uint32_t CC3DE: 1;  /* Capture/Compare 3 DMA request enable               0: CC3 DM */
        /* @SVD:field=CC4DE,bitOffset=12,bitWidth=1,description="Capture/Compare 4 DMA request enable\n              0: CC4 DMA request disabled.\n              1: CC4 DMA request enabled" */
        volatile uint32_t CC4DE: 1;  /* Capture/Compare 4 DMA request enable               0: CC4 DM */
        /* @SVD:field=RSVD2,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TDE,bitOffset=14,bitWidth=1,description="Trigger DMA request enable\n              0: Trigger DMA request disabled.\n              1: Trigger DMA request enabled." */
        volatile uint32_t TDE: 1;  /* Trigger DMA request enable               0: Trigger DMA requ */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_DIER_Type;

/* @SVD:register=SR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="TIM status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIF,bitOffset=0,bitWidth=1,description="Update interrupt flag\n              This bit is set by hardware on an update event. It is cleared by software.\n              0: No update occurred\n              1: Update interrupt pending. This bit is set by hardware when the registers are updated:\n              At overflow or underflow and if UDIS=0 in the CR1 register.\n              When CNT is reinitialized by software using the UG bit in EGR register, if URS=0 and UDIS=0 in the CR1 register.\n              When CNT is reinitialized by a trigger event, if URS=0 and UDIS=0 in the CR1 register." */
        volatile uint32_t UIF: 1;  /* Update interrupt flag               This bit is set by hardw */
        /* @SVD:field=CC1IF,bitOffset=1,bitWidth=1,description="Capture/Compare 1 interrupt flag\n              If channel CC1 is configured as output: This flag is set by hardware when the counter matches the compare value. It is cleared by software.\n              0: No match.\n              1: The content of the counter CNT has matched the content of the CCR1 register.\n              If channel CC1 is configured as input: This bit is set by hardware on a capture. It is cleared by software or by reading the CCR1 register.\n              0: No input capture occurred.\n              1: The counter value has been captured in CCR1 register (An edge has been detected on IC1 which matches the selected polarity)." */
        volatile uint32_t CC1IF: 1;  /* Capture/Compare 1 interrupt flag               If channel CC */
        /* @SVD:field=CC2IF,bitOffset=2,bitWidth=1,description="Capture/Compare 2 interrupt flag" */
        volatile uint32_t CC2IF: 1;  /* Capture/Compare 2 interrupt flag */
        /* @SVD:field=CC3IF,bitOffset=3,bitWidth=1,description="Capture/Compare 3 interrupt flag" */
        volatile uint32_t CC3IF: 1;  /* Capture/Compare 3 interrupt flag */
        /* @SVD:field=CC4IF,bitOffset=4,bitWidth=1,description="Capture/Compare 4 interrupt flag" */
        volatile uint32_t CC4IF: 1;  /* Capture/Compare 4 interrupt flag */
        /* @SVD:field=RSVD3,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=TIF,bitOffset=6,bitWidth=1,description="Trigger interrupt flag\n              This flag is set by hardware on trigger event (active edge detected on TRGI input when the slave mode controller is enabled in all modes but gated mode). It is set when the counter starts or stops when gated mode is selected. It is cleared by software.\n              0: No trigger event occurred.\n              1: Trigger interrupt pending." */
        volatile uint32_t TIF: 1;  /* Trigger interrupt flag               This flag is set by har */
        /* @SVD:field=RSVD2,bitOffset=7,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CC1OF,bitOffset=9,bitWidth=1,description="Capture/Compare 1 overcapture flag\n              This flag is set by hardware only when the corresponding channel is configured in input capture mode. It is cleared by software by writing it to '0'.\n              0: No overcapture has been detected.\n              1: The counter value has been captured in CCR1 register while CC1IF flag was already set" */
        volatile uint32_t CC1OF: 1;  /* Capture/Compare 1 overcapture flag               This flag i */
        /* @SVD:field=CC2OF,bitOffset=10,bitWidth=1,description="Capture/Compare 2 overcapture flag" */
        volatile uint32_t CC2OF: 1;  /* Capture/Compare 2 overcapture flag */
        /* @SVD:field=CC3OF,bitOffset=11,bitWidth=1,description="Capture/Compare 3 overcapture flag" */
        volatile uint32_t CC3OF: 1;  /* Capture/Compare 3 overcapture flag */
        /* @SVD:field=CC4OF,bitOffset=12,bitWidth=1,description="Capture/Compare 4 overcapture flag" */
        volatile uint32_t CC4OF: 1;  /* Capture/Compare 4 overcapture flag */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_SR_Type;

/* @SVD:register=EGR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Event generation register" */
/* EGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UG,bitOffset=0,bitWidth=1,description="Update generation\n              This bit can be set by software, it is automatically cleared by hardware.\n              0: No action\n              1: Re-initialize the counter and generates an update of the registers. Note that the prescaler counter is cleared too (anyway the prescaler ratio is not affected). The counter is cleared if the center-aligned mode is selected or if DIR=0 (upcounting), else it takes the auto-reload value (ARR) if DIR=1 (downcounting)." */
        volatile uint32_t UG: 1;  /* Update generation               This bit can be set by softw */
        /* @SVD:field=CC1G,bitOffset=1,bitWidth=1,description="Capture/compare 1 generation\n              This bit is set by software in order to generate an event, it is automatically cleared by hardware.\n              0: No action\n              1: A capture/compare event is generated on channel 1:\n              If channel CC1 is configured as output:\n              CC1IF flag is set, Corresponding interrupt or DMA request is sent if enabled.\n              If channel CC1 is configured as input:\n              The current value of the counter is captured in CCR1 register. The CC1IF flag is set, the corresponding interrupt or DMA request is sent if enabled. The CC1OF flag is set if the CC1IF flag was already high." */
        volatile uint32_t CC1G: 1;  /* Capture/compare 1 generation               This bit is set b */
        /* @SVD:field=CC2G,bitOffset=2,bitWidth=1,description="Capture/compare 2 generation" */
        volatile uint32_t CC2G: 1;  /* Capture/compare 2 generation */
        /* @SVD:field=CC3G,bitOffset=3,bitWidth=1,description="Capture/compare 3 generation" */
        volatile uint32_t CC3G: 1;  /* Capture/compare 3 generation */
        /* @SVD:field=CC4G,bitOffset=4,bitWidth=1,description="Capture/compare 4 generation" */
        volatile uint32_t CC4G: 1;  /* Capture/compare 4 generation */
        /* @SVD:field=RSVD2,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TG,bitOffset=6,bitWidth=1,description="Trigger generation\n              This bit is set by software in order to generate an event, it is automatically cleared by hardware.\n              0: No action\n              1: The TIF flag is set in SR register. Related interrupt or DMA transfer can occur if enabled." */
        volatile uint32_t TG: 1;  /* Trigger generation               This bit is set by software */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_EGR_Type;

/* @SVD:register=CCMR1,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="TIM capture/compare mode register 1" */
/* CCMR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CC1S,bitOffset=0,bitWidth=2,description="Capture/Compare 1 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC1 channel is configured as output\n              01: CC1 channel is configured as input, IC1 is mapped on TI1\n              10: CC1 channel is configured as input, IC1 is mapped on TI2\n              11: CC1 channel is configured as input, IC1 is mapped on TRC. This mode is working only if an internal trigger input is selected through TS bit (SMCR register)" */
        volatile uint32_t CC1S: 2;  /* Capture/Compare 1 selection               This bit-field def */
        /* @SVD:field=IC1PSC,bitOffset=2,bitWidth=2,description="Input capture 1 prescaler\n              This bit-field defines the ratio of the prescaler acting on CC1 input (IC1). The prescaler is reset as soon as CC1E=0.\n              00: no prescaler, capture is done each time an edge is detected on the capture input\n              01: capture is done once every 2 events\n              10: capture is done once every 4 events\n              11: capture is done once every 8 events" */
        volatile uint32_t IC1PSC: 2;  /* Input capture 1 prescaler               This bit-field defin */
        /* @SVD:field=IC1F,bitOffset=4,bitWidth=4,description="Input capture 1 filter\n              This bit-field defines the frequency used to sample TI1 input and the length of the digital filter applied to TI1. The digital filter is made of an event counter in which N consecutive events are needed to validate a transition on the output:\n              0000: No filter, sampling is done at fCLK\n              0001: fSAMPLING=fCLK, N=2\n              0010: fSAMPLING=fCLK, N=4\n              0011: fSAMPLING=fCLK, N=8\n              0100: fSAMPLING=fCLK/2, N=6\n              0101: fSAMPLING=fCLK/2, N=8\n              0110: fSAMPLING=fCLK/4, N=6\n              0111: fSAMPLING=fCLK/4, N=8\n              1000: fSAMPLING=fCLK/8, N=6\n              1001: fSAMPLING=fCLK/8, N=8\n              1010: fSAMPLING=fCLK/16, N=5\n              1011: fSAMPLING=fCLK/16, N=6\n              1100: fSAMPLING=fCLK/16, N=8\n              1101: fSAMPLING=fCLK/32, N=5\n              1110: fSAMPLING=fCLK/32, N=6\n              1111: fSAMPLING=fCLK/32, N=8" */
        volatile uint32_t IC1F: 4;  /* Input capture 1 filter               This bit-field defines  */
        /* @SVD:field=CC2S,bitOffset=8,bitWidth=2,description="Capture/Compare 2 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC2 channel is configured as output\n              01: CC2 channel is configured as input, IC2 is mapped on TI2\n              10: CC2 channel is configured as input, IC2 is mapped on TI1\n              11: CC2 channel is configured as input, IC2 is mapped on TRC. This mode is working only if an internal trigger input is selected through the TS bit (SMCR register)" */
        volatile uint32_t CC2S: 2;  /* Capture/Compare 2 selection               This bit-field def */
        /* @SVD:field=IC2PSC,bitOffset=10,bitWidth=2,description="Input capture 2 prescaler" */
        volatile uint32_t IC2PSC: 2;  /* Input capture 2 prescaler */
        /* @SVD:field=IC2F,bitOffset=12,bitWidth=4,description="Input capture 2 filter" */
        volatile uint32_t IC2F: 4;  /* Input capture 2 filter */
        /* @SVD:field=OC1CE,bitOffset=16,bitWidth=1,description="Output compare 1 clear enable\n              0: OC1Ref is not affected by the ETRF input\n              1: OC1Ref is cleared as soon as a High level is detected on ETRF input" */
        volatile uint32_t OC1CE: 1;  /* Output compare 1 clear enable               0: OC1Ref is not */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=OC1PE,bitOffset=19,bitWidth=1,description="Output compare 1 preload enable\n              0: Preload register on CCR1 disabled. CCR1 can be written at anytime, the new value is taken in account immediately.\n              1: Preload register on CCR1 enabled. Read/Write operations access the preload register. CCR1 preload value is loaded in the active register at each update event." */
        volatile uint32_t OC1PE: 1;  /* Output compare 1 preload enable               0: Preload reg */
        /* @SVD:field=OC1M,bitOffset=20,bitWidth=4,description="Output compare 1 mode\n              These bits define the behavior of the output reference signal OC1REF from which OC1 and OC1N are derived. OC1REF is active high whereas OC1 and OC1N active level depends on CC1P and CC1NP bits.\n              0000: Frozen - The comparison between the output compare register CCR1 and the counter CNT has no effect on the outputs.(this mode is used to generate a timing base).\n              0001: Set channel 1 to active level on match. OC1REF signal is forced high when the counter CNT matches the capture/compare register 1 (CCR1).\n              0010: Set channel 1 to inactive level on match. OC1REF signal is forced low when the counter CNT matches the capture/compare register 1 (CCR1).\n              0011: Toggle - OC1REF toggles when CNT=CCR1.\n              0100: Force inactive level - OC1REF is forced low.\n              0101: Force active level - OC1REF is forced high.\n              0110: PWM mode 1 - In upcounting, channel 1 is active as long as CNT(CCR1 else inactive. In downcounting, channel 1 is inactive (OC1REF='0) as long as CNT>CCR1 else active (OC1REF=1).\n              0111: PWM mode 2 - In upcounting, channel 1 is inactive as long as CNT(CCR1 else active. In downcounting, channel 1 is active as long as CNT>CCR1 else inactive.\n              1000: Retriggerable OPM mode 1 - In up-counting mode, the channel is active until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 1 and the channels becomes inactive again at the next update. In down-counting mode, the channel is inactive until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 1 and the channels becomes inactive again at the next update.\n              1001: Retriggerable OPM mode 2 - In up-counting mode, the channel is inactive until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 2 and the channels becomes inactive again at the next update. In down-counting mode, the channel is active until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 1 and the channels becomes active again at the next update.\n              1010: Reserved,\n              1011: Reserved,\n              1100: Combined PWM mode 1 - OC1REF has the same behavior as in PWM mode 1. OC1REFC is the logical OR between OC1REF and OC2REF.\n              1101: Combined PWM mode 2 - OC1REF has the same behavior as in PWM mode 2. OC1REFC is the logical AND between OC1REF and OC2REF.\n              1110: Asymmetric PWM mode 1 - OC1REF has the same behavior as in PWM mode 1. OC1REFC outputs OC1REF when the counter is counting up, OC2REF when it is counting down.\n              1111: Asymmetric PWM mode 2 - OC1REF has the same behavior as in PWM mode 2. OC1REFC outputs OC1REF when the counter is counting up, OC2REF when it is counting down." */
        volatile uint32_t OC1M: 4;  /* Output compare 1 mode               These bits define the be */
        /* @SVD:field=OC2CE,bitOffset=24,bitWidth=1,description="Output compare 2 clear enable" */
        volatile uint32_t OC2CE: 1;  /* Output compare 2 clear enable */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=OC2PE,bitOffset=27,bitWidth=1,description="Output compare 2 preload enable" */
        volatile uint32_t OC2PE: 1;  /* Output compare 2 preload enable */
        /* @SVD:field=OC2M,bitOffset=28,bitWidth=4,description="Output compare 2 mode" */
        volatile uint32_t OC2M: 4;  /* Output compare 2 mode */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CCMR1_Type;

/* @SVD:register=CCMR2,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="TIM capture/compare mode register 2" */
/* CCMR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CC3S,bitOffset=0,bitWidth=2,description="Capture/Compare 3 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC3 channel is configured as output\n              01: CC3 channel is configured as input, IC3 is mapped on TI3\n              10: CC3 channel is configured as input, IC3 is mapped on TI4\n              11: CC3 channel is configured as input, IC3 is mapped on TRC. This mode is working only if an internal trigger input is selected through TS bit (SMCR register)" */
        volatile uint32_t CC3S: 2;  /* Capture/Compare 3 selection               This bit-field def */
        /* @SVD:field=IC3PSC,bitOffset=2,bitWidth=2,description="Input capture 3 prescaler" */
        volatile uint32_t IC3PSC: 2;  /* Input capture 3 prescaler */
        /* @SVD:field=IC3F,bitOffset=4,bitWidth=4,description="Input capture 3 filter" */
        volatile uint32_t IC3F: 4;  /* Input capture 3 filter */
        /* @SVD:field=CC4S,bitOffset=8,bitWidth=2,description="Capture/Compare 4 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC4 channel is configured as output\n              01: CC4 channel is configured as input, IC4 is mapped on TI4\n              10: CC4 channel is configured as input, IC4 is mapped on TI3\n              11: CC4 channel is configured as input, IC4 is mapped on TRC. This mode is working only if an internal trigger input is selected through TS bit (SMCR register)" */
        volatile uint32_t CC4S: 2;  /* Capture/Compare 4 selection               This bit-field def */
        /* @SVD:field=IC4PSC,bitOffset=10,bitWidth=2,description="Input capture 4 prescaler" */
        volatile uint32_t IC4PSC: 2;  /* Input capture 4 prescaler */
        /* @SVD:field=IC4F,bitOffset=12,bitWidth=4,description="Input capture 4 filter" */
        volatile uint32_t IC4F: 4;  /* Input capture 4 filter */
        /* @SVD:field=OC3CE,bitOffset=16,bitWidth=1,description="Output compare 3 clear enable" */
        volatile uint32_t OC3CE: 1;  /* Output compare 3 clear enable */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=OC3PE,bitOffset=19,bitWidth=1,description="Output compare 3 preload enable" */
        volatile uint32_t OC3PE: 1;  /* Output compare 3 preload enable */
        /* @SVD:field=OC3M,bitOffset=20,bitWidth=4,description="Output compare 3 mode" */
        volatile uint32_t OC3M: 4;  /* Output compare 3 mode */
        /* @SVD:field=OC4CE,bitOffset=24,bitWidth=1,description="Output compare 4 clear enable" */
        volatile uint32_t OC4CE: 1;  /* Output compare 4 clear enable */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=OC4PE,bitOffset=27,bitWidth=1,description="Output compare 4 preload enable" */
        volatile uint32_t OC4PE: 1;  /* Output compare 4 preload enable */
        /* @SVD:field=OC4M,bitOffset=28,bitWidth=4,description="Output compare 4 mode" */
        volatile uint32_t OC4M: 4;  /* Output compare 4 mode */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CCMR2_Type;

/* @SVD:register=CCER,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Capture/Compare enable register" */
/* CCER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CC1E,bitOffset=0,bitWidth=1,description="Capture/Compare 1 output enable.\n              CC1 channel configured as output:\n              0: Off - OC1 is not active\n              1: On - OC1 signal is output on the corresponding output pin\n              CC1 channel configured as input: This bit determines if a capture of the counter value can actually be done into the input capture/compare register 1 (CCR1) or not.\n              0: Capture disabled\n              1: Capture enabled" */
        volatile uint32_t CC1E: 1;  /* Capture/Compare 1 output enable.               CC1 channel c */
        /* @SVD:field=CC1P,bitOffset=1,bitWidth=1,description="Capture/Compare 1 output Polarity.\n              CC1 channel configured as output:\n              0: OC1 active high\n              1: OC1 active low\n              CC1 channel configured as input: CC1NP/CC1P bits select TI1FP1 and TI2FP1 polarity for trigger or capture operations.\n              00: noninverted/rising edge\n              Circuit is sensitive to TIxFP1 rising edge (capture, trigger in reset, external clock or trigger mode), TIxFP1 is not inverted (trigger in gated mode, encoder mode).\n              01: inverted/falling edge\n              Circuit is sensitive to TIxFP1 falling edge (capture, trigger in reset, external clock or trigger mode), TIxFP1 is inverted (trigger in gated mode, encoder mode).\n              10: reserved, do not use this configuration.\n              11: noninverted/both edges\n              Circuit is sensitive to both TIxFP1 rising and falling edges (capture, trigger in reset, external clock or trigger mode), TIxFP1 is not inverted (trigger in gated mode). This configuration must not be used for encoder mode." */
        volatile uint32_t CC1P: 1;  /* Capture/Compare 1 output Polarity.               CC1 channel */
        /* @SVD:field=RSVD5,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=CC1NP,bitOffset=3,bitWidth=1,description="Capture/Compare 1 output Polarity.\n              CC1 channel configured as output: CC1NP must be kept cleared in this case.\n              CC1 channel configured as input: This bit is used in conjunction with CC1P to define TI1FP1/TI2FP1 polarity. refer to CC1P description." */
        volatile uint32_t CC1NP: 1;  /* Capture/Compare 1 output Polarity.               CC1 channel */
        /* @SVD:field=CC2E,bitOffset=4,bitWidth=1,description="Capture/Compare 2 output enable." */
        volatile uint32_t CC2E: 1;  /* Capture/Compare 2 output enable. */
        /* @SVD:field=CC2P,bitOffset=5,bitWidth=1,description="Capture/Compare 2 output Polarity." */
        volatile uint32_t CC2P: 1;  /* Capture/Compare 2 output Polarity. */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=CC2NP,bitOffset=7,bitWidth=1,description="Capture/Compare 2 output Polarity." */
        volatile uint32_t CC2NP: 1;  /* Capture/Compare 2 output Polarity. */
        /* @SVD:field=CC3E,bitOffset=8,bitWidth=1,description="Capture/Compare 3 output enable." */
        volatile uint32_t CC3E: 1;  /* Capture/Compare 3 output enable. */
        /* @SVD:field=CC3P,bitOffset=9,bitWidth=1,description="Capture/Compare 3 output Polarity." */
        volatile uint32_t CC3P: 1;  /* Capture/Compare 3 output Polarity. */
        /* @SVD:field=RSVD3,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=CC3NP,bitOffset=11,bitWidth=1,description="Capture/Compare 3 output Polarity." */
        volatile uint32_t CC3NP: 1;  /* Capture/Compare 3 output Polarity. */
        /* @SVD:field=CC4E,bitOffset=12,bitWidth=1,description="Capture/Compare 4 output enable." */
        volatile uint32_t CC4E: 1;  /* Capture/Compare 4 output enable. */
        /* @SVD:field=CC4P,bitOffset=13,bitWidth=1,description="Capture/Compare 4 output Polarity." */
        volatile uint32_t CC4P: 1;  /* Capture/Compare 4 output Polarity. */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CC4NP,bitOffset=15,bitWidth=1,description="Capture/Compare 4 output Polarity." */
        volatile uint32_t CC4NP: 1;  /* Capture/Compare 4 output Polarity. */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CCER_Type;

/* @SVD:register=CNT,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Counter" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=16,description="counter value" */
        volatile uint32_t CNT: 16;  /* counter value */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
        /* @SVD:field=UIFCPY,bitOffset=31,bitWidth=1,description="Value depends on IUFREMAP in CR1.\n              If UIFREMAP = 1\n              UIFCPY: UIF Copy\n              This bit is a read-only copy of the UIF bit of the ISR register" */
        volatile uint32_t UIFCPY: 1;  /* Value depends on IUFREMAP in CR1.               If UIFREMAP  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CNT_Type;

/* @SVD:register=PSC,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Prescaler" */
/* PSC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PSC,bitOffset=0,bitWidth=16,description="Prescaler value\n              The counter clock frequency is equal to fCLK / (PSC[15:0] + 1).\n              PSC contains the value to be loaded in the active prescaler register at each update event (including when the counter is cleared through UG bit of EGR register or through trigger controller when configured in 'reset mode')." */
        volatile uint32_t PSC: 16;  /* Prescaler value               The counter clock frequency is */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_PSC_Type;

/* @SVD:register=ARR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Auto-reload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=16,description="Auto-reload value\n              ARR is the value to be loaded in the actual auto-reload register." */
        volatile uint32_t ARR: 16;  /* Auto-reload value               ARR is the value to be loade */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_ARR_Type;

/* @SVD:register=RCR,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Repetition counter register" */
/* RCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=8,description="Repetition counter value\n              These bits allow the user to set-up the update rate of the compare registers (i.e. periodic transfers from preload to active registers) when preload registers are enable, as well as the update interrupt generation rate, if this interrupt is enable.\n              Each time the REP_CNT related downcounter reaches zero, an update event is generated and it restarts counting from REP value. As REP_CNT is reloaded with REP value only at the repetition update event, any write to the RCR register is not taken in account until the next repetition update event.\n              It means in PWM mode (REP+1) corresponds to the number of PWM periods in edge-aligned mode." */
        volatile uint32_t REP: 8;  /* Repetition counter value               These bits allow the  */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_RCR_Type;

/* @SVD:register=CCR1,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 1" */
/* CCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR1,bitOffset=0,bitWidth=16,description="Capture/Compare 1 value\n              If channel CC1 is configured as output:\n              CCR1 is the value to be loaded in the actual capture/compare 1 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR1 register (bit OC1PE). Else the preload value is copied in the active capture/compare 1 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signaled on OC1 output.\n              If channel CC1is configured as input:\n              CCR1 is the counter value transferred by the last input capture 1 event (IC1). The CCR1 register is read-only and cannot be programmed." */
        volatile uint32_t CCR1: 16;  /* Capture/Compare 1 value               If channel CC1 is conf */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CCR1_Type;

/* @SVD:register=CCR2,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 2" */
/* CCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR2,bitOffset=0,bitWidth=16,description="Capture/Compare 2 value\n              If channel CC2 is configured as output:\n              CCR2 is the value to be loaded in the actual capture/compare 2 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR1 register (bit OC2PE). Else the preload value is copied in the active capture/compare 2 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signalled on OC2 output.\n              If channel CC2 is configured as input:\n              CCR2 is the counter value transferred by the last input capture 2 event (IC2). The CCR2 register is read-only and cannot be programmed." */
        volatile uint32_t CCR2: 16;  /* Capture/Compare 2 value               If channel CC2 is conf */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CCR2_Type;

/* @SVD:register=CCR3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 3" */
/* CCR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR3,bitOffset=0,bitWidth=16,description="Capture/Compare value\n              If channel CC3 is configured as output:\n              CCR3 is the value to be loaded in the actual capture/compare 3 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR2 register (bit OC3PE). Else the preload value is copied in the active capture/compare 3 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signalled on OC3 output.\n              If channel CC3is configured as input:\n              CCR3 is the counter value transferred by the last input capture 3 event (IC3). The CCR3 register is read-only and cannot be programmed." */
        volatile uint32_t CCR3: 16;  /* Capture/Compare value               If channel CC3 is config */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CCR3_Type;

/* @SVD:register=CCR4,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 4" */
/* CCR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR4,bitOffset=0,bitWidth=16,description="Capture/Compare value\n              1. if CC4 channel is configured as output:\n              CCR4 is the value to be loaded in the actual capture/compare 4 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR2 register (bit OC4PE). Else the preload value is copied in the active capture/compare 4 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signalled on OC4 output.\n              2.  if CC4 channel is configured as input:\n              CCR4 is the counter value transferred by the last input capture 4 event (IC4). The CCR4 register is read-only and cannot be programmed." */
        volatile uint32_t CCR4: 16;  /* Capture/Compare value               1. if CC4 channel is con */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM1_CCR4_Type;

/* GPTIM1 寄存器结构体 */
typedef struct
{
    GPTIM1_CR1_Type CR1;  /* 0x0000 - TIM control register 1 */
    GPTIM1_CR2_Type CR2;  /* 0x0004 - TIM control register 2 */
    GPTIM1_SMCR_Type SMCR;  /* 0x0008 - TIM slave mode control register */
    GPTIM1_DIER_Type DIER;  /* 0x000C - TIM DMA/Interrupt enable register */
    GPTIM1_SR_Type SR;  /* 0x0010 - TIM status register */
    GPTIM1_EGR_Type EGR;  /* 0x0014 - Event generation register */
    GPTIM1_CCMR1_Type CCMR1;  /* 0x0018 - TIM capture/compare mode register 1 */
    GPTIM1_CCMR2_Type CCMR2;  /* 0x001C - TIM capture/compare mode register 2 */
    GPTIM1_CCER_Type CCER;  /* 0x0020 - Capture/Compare enable register */
    GPTIM1_CNT_Type CNT;  /* 0x0024 - Counter */
    GPTIM1_PSC_Type PSC;  /* 0x0028 - Prescaler */
    GPTIM1_ARR_Type ARR;  /* 0x002C - Auto-reload register */
    GPTIM1_RCR_Type RCR;  /* 0x0030 - Repetition counter register */
    GPTIM1_CCR1_Type CCR1;  /* 0x0034 - Capture/Compare register 1 */
    GPTIM1_CCR2_Type CCR2;  /* 0x0038 - Capture/Compare register 2 */
    GPTIM1_CCR3_Type CCR3;  /* 0x003C - Capture/Compare register 3 */
    GPTIM1_CCR4_Type CCR4;  /* 0x0040 - Capture/Compare register 4 */
} GPTIM1_TypeDef;

/* ============================================================================ */
/* BTIM1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=BTIM1,base=0x50092000,description="",groupName=BTIM */

#define BTIM1_BASE                                         (0x50092000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="TIM control register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CEN,bitOffset=0,bitWidth=1,description="Counter enable\n              0: Counter disabled\n              1: Counter enabled\n              Note: Gated mode can work only if the CEN bit has been previously set by software. However trigger mode can set the CEN bit automatically by hardware. \n              CEN is cleared automatically in one-pulse mode, when an update event occurs." */
        volatile uint32_t CEN: 1;  /* Counter enable               0: Counter disabled             */
        /* @SVD:field=UDIS,bitOffset=1,bitWidth=1,description="Update disable\n              This bit is set and cleared by software to enable/disable UEV event generation.\n              0: UEV enabled. The Update (UEV) event is generated by one of the following events:\n                 Counter overflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              Buffered registers are then loaded with their preload values.\n              1: UEV disabled. The Update event is not generated, shadow registers keep their value (ARR, PSC). However the counter and the prescaler are reinitialized if the UG bit is set or if a hardware reset is received from the slave mode controller." */
        volatile uint32_t UDIS: 1;  /* Update disable               This bit is set and cleared by  */
        /* @SVD:field=URS,bitOffset=2,bitWidth=1,description="Update request source\n              This bit is set and cleared by software to select the UEV event sources.\n              0: Any of the following events generate an update interrupt or DMA request if enabled. \n              These events can be: \n                 Counter overflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              1: Only counter overflow generates an update interrupt or DMA request if enabled." */
        volatile uint32_t URS: 1;  /* Update request source               This bit is set and clea */
        /* @SVD:field=OPM,bitOffset=3,bitWidth=1,description="One-pulse mode\n              0: Counter is not stopped at update event\n              1: Counter stops counting at the next update event (clearing the bit CEN)" */
        volatile uint32_t OPM: 1;  /* One-pulse mode               0: Counter is not stopped at up */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=ARPE,bitOffset=7,bitWidth=1,description="Auto-reload preload enable\n              0: ARR register is not buffered\n              1: ARR register is buffered" */
        volatile uint32_t ARPE: 1;  /* Auto-reload preload enable               0: ARR register is  */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM1_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="TIM control register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=MMS,bitOffset=4,bitWidth=2,description="Master mode selection\n              These bits allow to select the information to be sent in master mode to slave timers for synchronization (TRGO). The combination is as follows:\n              00: Reset:the UG bit from the EGR register is used as trigger output (TRGO). If the reset is generated by the trigger input (slave mode controller configured in reset mode) then the signal on TRGO is delayed compared to the actual reset.\n              01: Enable :the Counter enable signal, CNT_EN, is used as trigger output (TRGO). It is useful to start several timers at the same time or to control a window in which a slave timer is enabled. The Counter Enable signal is generated by a logic OR between CEN control bit and the trigger input when configured in gated mode. \n              When the Counter Enable signal is controlled by the trigger input, there is a delay on TRGO, except if the master/slave mode is selected (see the MSM bit description in SMCR register).\n              10: Update:The update event is selected as trigger output (TRGO). For instance a master timer can then be used as a prescaler for a slave timer.\n              11: Gating:The delayed gating trigger is selected as trigger output (TRGO)." */
        volatile uint32_t MMS: 2;  /* Master mode selection               These bits allow to sele */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM1_CR2_Type;

/* @SVD:register=SMCR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="TIM slave mode control register" */
/* SMCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD5,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD5: 4;
        /* @SVD:field=TS,bitOffset=4,bitWidth=2,description="Trigger selection\n              This bit-field selects the trigger input to be used to synchronize the counter.\n              00: Internal Trigger 0 (ITR0)\n              01: Internal Trigger 1 (ITR1) \n              10: Internal Trigger 2 (ITR2)\n              11: Internal Trigger 3 (ITR3)" */
        volatile uint32_t TS: 2;  /* Trigger selection               This bit-field selects the t */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=MSM,bitOffset=7,bitWidth=1,description="Master/Slave mode. This bit should be asserted on master timer if synchronization if needed.\n              0: No action\n              1: The effect of an event on the trigger input (TRGI) is delayed to allow a perfect synchronization between the current timer and its slaves (through TRGO). It is useful if we want to synchronize several timers on a single external event." */
        volatile uint32_t MSM: 1;  /* Master/Slave mode. This bit should be asserted on master tim */
        /* @SVD:field=RSVD3,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD3: 8;
        /* @SVD:field=SMS,bitOffset=16,bitWidth=3,description="Slave mode selection\n              When external signals are selected the active edge of the trigger signal (TRGI) is linked to the polarity selected on the external input.\n              000: Slave mode disabled.\n              001: Reset Mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter and generates an update of the registers.\n              010: Trigger Mode - The counter starts at a rising edge of the trigger TRGI (but it is not reset). Only the start of the counter is controlled.\n              011: Combined reset + trigger mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter, generates an update of the registers and starts the counter.\n              100: External Clock Mode - Rising edges of the selected trigger (TRGI) clock the counter." */
        volatile uint32_t SMS: 3;  /* Slave mode selection               When external signals are */
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=GTS,bitOffset=20,bitWidth=2,description="Gating trigger selection in gated mode\n              This bit-field selects the trigger input to be used to enable the counter gating.\n              00: Internal Trigger 0 (ITR0)\n              01: Internal Trigger 1 (ITR1) \n              10: Internal Trigger 2 (ITR2)\n              11: Internal Trigger 3 (ITR3)" */
        volatile uint32_t GTS: 2;  /* Gating trigger selection in gated mode               This bi */
        /* @SVD:field=GTP,bitOffset=22,bitWidth=1,description="Gating trigger polarity invert\n              0: active at high level\n              1: active at low level" */
        volatile uint32_t GTP: 1;  /* Gating trigger polarity invert               0: active at hi */
        /* @SVD:field=GM,bitOffset=23,bitWidth=1,description="Gated Mode. The counter clock is enabled when the selected trigger input (TRGI) is active (according to gating trigger polarity). The counter stops (but is not reset) as soon as the trigger becomes inactive. Both start and stop of the counter are controlled. Gated mode and slave mode can be enabled simutanuously with different trigger selection." */
        volatile uint32_t GM: 1;  /* Gated Mode. The counter clock is enabled when the selected t */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM1_SMCR_Type;

/* @SVD:register=DIER,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="TIM DMA/Interrupt enable register" */
/* DIER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIE,bitOffset=0,bitWidth=1,description="Update interrupt enable\n              0: Update interrupt disabled.\n              1: Update interrupt enabled" */
        volatile uint32_t UIE: 1;  /* Update interrupt enable               0: Update interrupt di */
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=UDE,bitOffset=8,bitWidth=1,description="Update DMA request enable\n              0: Update DMA request disabled.\n              1: Update DMA request enabled" */
        volatile uint32_t UDE: 1;  /* Update DMA request enable               0: Update DMA reques */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM1_DIER_Type;

/* @SVD:register=SR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="TIM status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIF,bitOffset=0,bitWidth=1,description="Update interrupt flag\n              This bit is set by hardware on an update event. It is cleared by software.\n              0: No update occurred\n              1: Update interrupt pending. This bit is set by hardware when the registers are updated:\n              At overflow and if UDIS=0 in the CR1 register.\n              When CNT is reinitialized by software using the UG bit in EGR register, if URS=0 and UDIS=0 in the CR1 register.\n              When CNT is reinitialized by a trigger event (refer to the synchro control register description), if URS=0 and UDIS=0 in the CR1 register." */
        volatile uint32_t UIF: 1;  /* Update interrupt flag               This bit is set by hardw */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM1_SR_Type;

/* @SVD:register=EGR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Event generation register" */
/* EGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UG,bitOffset=0,bitWidth=1,description="Update generation\n              This bit can be set by software, it is automatically cleared by hardware.\n              0: No action\n              1: Re-initialize the counter and generates an update of the registers. Note that the prescaler counter is cleared too (anyway the prescaler ratio is not affected). The counter is cleared if the center-aligned mode is selected or if DIR=0 (upcounting), else it takes the auto-reload value (ARR) if DIR=1 (downcounting)." */
        volatile uint32_t UG: 1;  /* Update generation               This bit can be set by softw */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM1_EGR_Type;

/* @SVD:register=CNT,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Counter" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=32,description="counter value" */
        volatile uint32_t CNT: 32;  /* counter value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM1_CNT_Type;

/* @SVD:register=PSC,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Prescaler" */
/* PSC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PSC,bitOffset=0,bitWidth=16,description="Prescaler value\n              The counter clock frequency is equal to fCLK / (PSC[15:0] + 1).\n              PSC contains the value to be loaded in the active prescaler register at each update event (including when the counter is cleared through UG bit of EGR register or through trigger controller when configured in \"reset mode\")." */
        volatile uint32_t PSC: 16;  /* Prescaler value               The counter clock frequency is */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM1_PSC_Type;

/* @SVD:register=ARR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Auto-reload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=32,description="Auto-reload value\n              ARR is the value to be loaded in the actual auto-reload register. The counter is blocked while the auto-reload value is null." */
        volatile uint32_t ARR: 32;  /* Auto-reload value               ARR is the value to be loade */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM1_ARR_Type;

/* BTIM1 寄存器结构体 */
typedef struct
{
    BTIM1_CR1_Type CR1;  /* 0x0000 - TIM control register 1 */
    BTIM1_CR2_Type CR2;  /* 0x0004 - TIM control register 2 */
    BTIM1_SMCR_Type SMCR;  /* 0x0008 - TIM slave mode control register */
    BTIM1_DIER_Type DIER;  /* 0x000C - TIM DMA/Interrupt enable register */
    BTIM1_SR_Type SR;  /* 0x0010 - TIM status register */
    BTIM1_EGR_Type EGR;  /* 0x0014 - Event generation register */
    /* @SVD:register=RSVD1,offset=0x0018,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0018 - RSVD1 */
    volatile uint32_t RESERVED7[2];  /* 0x001C - 0x0023 */
    BTIM1_CNT_Type CNT;  /* 0x0024 - Counter */
    BTIM1_PSC_Type PSC;  /* 0x0028 - Prescaler */
    BTIM1_ARR_Type ARR;  /* 0x002C - Auto-reload register */
} BTIM1_TypeDef;

/* ============================================================================ */
/* WDT1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=WDT1,base=0x50094000,description="",groupName=WDT */

#define WDT1_BASE                                          (0x50094000UL)

/* @SVD:register=WDT_CVR0,offset=0x0000,size=32,access=read-write,reset=0x0,description="WatchDog Counter Value 0" */
/* WDT_CVR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COUNT_VALUE_0,bitOffset=0,bitWidth=24,description="Count Value for 1st TimeOut" */
        volatile uint32_t COUNT_VALUE_0: 24;  /* Count Value for 1st TimeOut */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT1_WDT_CVR0_Type;

/* @SVD:register=WDT_CVR1,offset=0x0004,size=32,access=read-write,reset=0x0,description="WatchDog Counter Value 1" */
/* WDT_CVR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COUNT_VALUE_1,bitOffset=0,bitWidth=24,description="Count Value for 2nd TimeOut" */
        volatile uint32_t COUNT_VALUE_1: 24;  /* Count Value for 2nd TimeOut */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT1_WDT_CVR1_Type;

/* @SVD:register=WDT_CR,offset=0x0008,size=32,access=read-write,reset=0x0,description="WatchDog Control Register" */
/* WDT_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESET_LENGTH,bitOffset=0,bitWidth=3,description="reset pulse length in number of wdt clock cycles" */
        volatile uint32_t RESET_LENGTH: 3;  /* reset pulse length in number of wdt clock cycles */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RESPONSE_MODE,bitOffset=4,bitWidth=1,description="0:reset only, 1:interrupt and reset" */
        volatile uint32_t RESPONSE_MODE: 1;  /* 0:reset only, 1:interrupt and reset */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT1_WDT_CR_Type;

/* @SVD:register=WDT_CCR,offset=0x000C,size=32,access=read-write,reset=0x0,description="WatchDog Counter Control Register" */
/* WDT_CCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COUNTER_CONTROL,bitOffset=0,bitWidth=8,description="SinglePulse /Write 8'h76 to restart, write8'h34 to stop, else do nothing" */
        volatile uint32_t COUNTER_CONTROL: 8;  /* SinglePulse /Write 8'h76 to restart, write8'h34 to stop, els */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT1_WDT_CCR_Type;

/* @SVD:register=WDT_ICR,offset=0x0010,size=32,access=read-write,reset=0x0,description="WatchDog Interrupt Clear Register" */
/* WDT_ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INT_CLR,bitOffset=0,bitWidth=1,description="SinglePulse /A pulse to clear interrupt" */
        volatile uint32_t INT_CLR: 1;  /* SinglePulse /A pulse to clear interrupt */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT1_WDT_ICR_Type;

/* @SVD:register=WDT_SR,offset=0x0014,size=32,access=read-write,reset=0x0,description="WatchDog Status Register" */
/* WDT_SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INT_ASSERT,bitOffset=0,bitWidth=1,description="Interrupt assert when 1" */
        volatile uint32_t INT_ASSERT: 1;  /* Interrupt assert when 1 */
        /* @SVD:field=WDT_ACTIVE,bitOffset=1,bitWidth=1,description="Watchdog runs when 1, else 0" */
        volatile uint32_t WDT_ACTIVE: 1;  /* Watchdog runs when 1, else 0 */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT1_WDT_SR_Type;

/* @SVD:register=WDT_WP,offset=0x0018,size=32,access=read-write,reset=0x0,description="WatchDog Write Protect Register" */
/* WDT_WP 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WRPT,bitOffset=0,bitWidth=31,description="write 0x58ab99fc generate write_protect, write 0x51ff8621 to release" */
        volatile uint32_t WRPT: 31;  /* write 0x58ab99fc generate write_protect, write 0x51ff8621 to */
        /* @SVD:field=WRPT_ST,bitOffset=31,bitWidth=1,description="1 indicates write protect is active" */
        volatile uint32_t WRPT_ST: 1;  /* 1 indicates write protect is active */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT1_WDT_WP_Type;

/* @SVD:register=WDT_FG,offset=0x001C,size=32,access=read-write,reset=0x0,description="WatchDog Flag Register" */
/* WDT_FG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RST_FG_CLR,bitOffset=0,bitWidth=1,description="SinglePulse/A pulse to clear reset flag" */
        volatile uint32_t RST_FG_CLR: 1;  /* SinglePulse/A pulse to clear reset flag */
        /* @SVD:field=RST_FG,bitOffset=1,bitWidth=1,description="1 indicates wdt has already reset system" */
        volatile uint32_t RST_FG: 1;  /* 1 indicates wdt has already reset system */
        /* @SVD:field=SYNC_FG_CLR,bitOffset=2,bitWidth=1,description="SinglePulse/A pulse to clear sync flag" */
        volatile uint32_t SYNC_FG_CLR: 1;  /* SinglePulse/A pulse to clear sync flag */
        /* @SVD:field=SYNC_FG,bitOffset=3,bitWidth=1,description="1 indicates one transition from system clk to wdt clk has complicated" */
        volatile uint32_t SYNC_FG: 1;  /* 1 indicates one transition from system clk to wdt clk has co */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT1_WDT_FG_Type;

/* WDT1 寄存器结构体 */
typedef struct
{
    WDT1_WDT_CVR0_Type WDT_CVR0;  /* 0x0000 - WatchDog Counter Value 0 */
    WDT1_WDT_CVR1_Type WDT_CVR1;  /* 0x0004 - WatchDog Counter Value 1 */
    WDT1_WDT_CR_Type WDT_CR;  /* 0x0008 - WatchDog Control Register */
    WDT1_WDT_CCR_Type WDT_CCR;  /* 0x000C - WatchDog Counter Control Register */
    WDT1_WDT_ICR_Type WDT_ICR;  /* 0x0010 - WatchDog Interrupt Clear Register */
    WDT1_WDT_SR_Type WDT_SR;  /* 0x0014 - WatchDog Status Register */
    WDT1_WDT_WP_Type WDT_WP;  /* 0x0018 - WatchDog Write Protect Register */
    WDT1_WDT_FG_Type WDT_FG;  /* 0x001C - WatchDog Flag Register */
} WDT1_TypeDef;

/* ============================================================================ */
/* SPI1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=SPI1,base=0x50095000,description="",groupName=SPI */

#define SPI1_BASE                                          (0x50095000UL)

/* @SVD:register=TOP_CTRL,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Top Control Register" */
/* TOP_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SSE,bitOffset=0,bitWidth=1,description="SPI controller Enable \n              0: SPI controller is disabled  \n              1: SPI controller is enabled" */
        volatile uint32_t SSE: 1;  /* SPI controller Enable                0: SPI controller is di */
        /* @SVD:field=FRF,bitOffset=1,bitWidth=2,description="Frame Format \n              0x0: Motorola* Serial Peripheral Interface (SPI)  \n              0x1: Texas Instruments* Synchronous Serial Protocol (SSP) \n              0x2: National Semiconductor Microwire* \n              0x3: RSVD" */
        volatile uint32_t FRF: 2;  /* Frame Format                0x0: Motorola* Serial Peripheral */
        /* @SVD:field=SCLKDIR,bitOffset=3,bitWidth=1,description="SPI_CLK Direction \n              0: Master mode, SPI controller drives SPI_CLK \n              1: Slave mode, SPI controller receives SPI_CLK" */
        volatile uint32_t SCLKDIR: 1;  /* SPI_CLK Direction                0: Master mode, SPI control */
        /* @SVD:field=SFRMDIR,bitOffset=4,bitWidth=1,description="SPI_CS Direction \n              0: Master mode, SPI controller drives SPI_CS \n              1: Slave mode, SPI controller receives SPI_CS" */
        volatile uint32_t SFRMDIR: 1;  /* SPI_CS Direction                0: Master mode, SPI controll */
        /* @SVD:field=DSS,bitOffset=5,bitWidth=5,description="SPI controller Work data size,  register bits value 0~31 indicated data size 1~32 bits,  usually use data size 8bits, 16bits, 24bits, 32bits" */
        volatile uint32_t DSS: 5;  /* SPI controller Work data size,  register bits value 0~31 ind */
        /* @SVD:field=SPO,bitOffset=10,bitWidth=1,description="Motorola SPI SPI_CLK Polarity Setting \n              0: The inactive or idle state of SPI_CLK is low \n              1: The inactive or idle state of SPI_CLK is high" */
        volatile uint32_t SPO: 1;  /* Motorola SPI SPI_CLK Polarity Setting                0: The  */
        /* @SVD:field=SPH,bitOffset=11,bitWidth=1,description="Motorola SPI SPI_CLK phase setting \n              0: SPI_CLK is inactive until one cycle after the start of a frame and active until 1/2 cycle before the end of a frame \n              1: SPI_CLK is inactive until 1/2 cycle after the start of a frame and active until one cycle before the end of a frame" */
        volatile uint32_t SPH: 1;  /* Motorola SPI SPI_CLK phase setting                0: SPI_CLK */
        /* @SVD:field=RSVD3,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=TRAIL,bitOffset=13,bitWidth=1,description="Trailing Byte \n              0: Trailing bytes are handled by CPU \n              1: Trailing bytes are handled by DMA bursts" */
        volatile uint32_t TRAIL: 1;  /* Trailing Byte                0: Trailing bytes are handled b */
        /* @SVD:field=HOLD_FRAME_LOW,bitOffset=14,bitWidth=1,description="Hold Frame Low Control \n              0:After this field is set to 1 and the SPI controller is operating in master mode,the output frame signal SPI_CS will be determined by control FSM.\n              1:After this field is set to 1 and the SPI controller is operating in master mode, the output frame signal SPI_CS will hold low." */
        volatile uint32_t HOLD_FRAME_LOW: 1;  /* Hold Frame Low Control                0:After this field is  */
        /* @SVD:field=IFS,bitOffset=15,bitWidth=1,description="Invert Frame Signal \n              0: SPI_CS polarity is as defined in protocol\n              1: SPI_CS will be inverted from normal-SPI_CS" */
        volatile uint32_t IFS: 1;  /* Invert Frame Signal                0: SPI_CS polarity is as  */
        /* @SVD:field=RSVD2,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TTE,bitOffset=17,bitWidth=1,description="SPI_DO Three-State Enable \n              0: SPI_DO output signal is not three-stated \n              1: SPI_DO is three-stated when not transmitting data" */
        volatile uint32_t TTE: 1;  /* SPI_DO Three-State Enable                0: SPI_DO output si */
        /* @SVD:field=TTELP,bitOffset=18,bitWidth=1,description="SPI_DO Three-state Enable On Last Phase (can be set only when TI-SSP)\n              0: SPI_DO is three-stated 1/2 clock cycle after the beginning of the LSB \n              1: SPI_DO output signal is three-stated on the clock edge that ends the LSB" */
        volatile uint32_t TTELP: 1;  /* SPI_DO Three-state Enable On Last Phase (can be set only whe */
        /* @SVD:field=RSVD,bitOffset=19,bitWidth=13,description="" */
        volatile uint32_t RSVD: 13;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_TOP_CTRL_Type;

/* @SVD:register=FIFO_CTRL,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="FIFO Control Register" */
/* FIFO_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TFT,bitOffset=0,bitWidth=5,description="TXFIFO Trigger Threshold This field sets the threshold level at which TXFIFO asserts interrupt. The level should be set to the preferred threshold value minus 1." */
        volatile uint32_t TFT: 5;  /* TXFIFO Trigger Threshold This field sets the threshold level */
        /* @SVD:field=RFT,bitOffset=5,bitWidth=5,description="RXFIFO Trigger Threshold This field sets the threshold level at which RXFIFO asserts interrupt. The level should be set to the preferred threshold value minus 1." */
        volatile uint32_t RFT: 5;  /* RXFIFO Trigger Threshold This field sets the threshold level */
        /* @SVD:field=TSRE,bitOffset=10,bitWidth=1,description="Transmit Service Request Enable \n              0: TxFIFO DMA service request is disabled \n              1: TxFIFO DMA service request is enabled" */
        volatile uint32_t TSRE: 1;  /* Transmit Service Request Enable                0: TxFIFO DMA */
        /* @SVD:field=RSRE,bitOffset=11,bitWidth=1,description="Receive Service Request Enable \n              0: RxFIFO DMA service request is disabled \n              1: RxFIFO DMA service request is enabled" */
        volatile uint32_t RSRE: 1;  /* Receive Service Request Enable                0: RxFIFO DMA  */
        /* @SVD:field=RXFIFO_RD_ENDIAN,bitOffset=12,bitWidth=2,description="apb_prdata Read from Rx FIFO Endian \n              0x0 = apb_prdata[31:0] = rxfifo_wdata[31:0] \n              0x1 = apb_prdata[31:0] = {rxfifo_wdata[15:0], rxfifo_wdata[31:16]} \n              0x2 = apb_prdata[31:0]= {rxfifo_wdata[7:0], rxfifo_wdata[15:8], rxfifo_wdata[23:16], rxfifo_wdata[31:24]} \n              0x3 = apb_prdata[31:0]= {rxfifo_wdata[23:16], rxfifo_wdata[31:24], rxfifo_wdata[7:0], rxfifo_wdata[15:8]}" */
        volatile uint32_t RXFIFO_RD_ENDIAN: 2;  /* apb_prdata Read from Rx FIFO Endian                0x0 = apb */
        /* @SVD:field=TXFIFO_WR_ENDIAN,bitOffset=14,bitWidth=2,description="apb_pwdata Write to TxFIFO Endian \n              0x0: txfifo_wdata[31:0] = apb_pwdata[31:0] \n              0x1: fifo_wdata[31:0] = {apb_pwdata[15:0], apb_pwdata[31:16]} \n              0x2: txfifo_wdata[31:0] = {apb_pwdata[7:0], apb_pwdata[15:8], apb_pwdata[23:16], apb_pwdata[31:24]} \n              0x3: txfifo_wdata[31:0] = {apb_pwdata[23:16], apb_pwdata[31:24], apb_pwdata[7:0], apb_pwdata[15:8]}" */
        volatile uint32_t TXFIFO_WR_ENDIAN: 2;  /* apb_pwdata Write to TxFIFO Endian                0x0: txfifo */
        /* @SVD:field=FPCKE,bitOffset=16,bitWidth=1,description="FIFO Packing Enable \n              0: FIFO packing mode disabled \n              1: FIFO packing mode enabled" */
        volatile uint32_t FPCKE: 1;  /* FIFO Packing Enable                0: FIFO packing mode disa */
        /* @SVD:field=RXFIFO_AUTO_FULL_CTRL,bitOffset=17,bitWidth=1,description="Rx FIFO Auto Full Control: After this field is set to 1 and the SPI controller is operating in master mode, the controller FSM returns to IDLE state and stops the SPI_CLK. \n              When Rx FIFO is full, the controller FSM continues transferring data after the RxFIFO is not full. \n              This field is used to avoid an RxFIFO overrun issue. \n              1: Enable Rx FIFO auto full control" */
        volatile uint32_t RXFIFO_AUTO_FULL_CTRL: 1;  /* Rx FIFO Auto Full Control: After this field is set to 1 and  */
        /* @SVD:field=RSVD,bitOffset=18,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_FIFO_CTRL_Type;

/* @SVD:register=INTE,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Interrupt Enable Register" */
/* INTE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TINTE,bitOffset=1,bitWidth=1,description="Receiver Time-out Interrupt Enable \n              0: Receiver time-out interrupt is disabled \n              1: Receiver time-out interrupt is enabled" */
        volatile uint32_t TINTE: 1;  /* Receiver Time-out Interrupt Enable                0: Receive */
        /* @SVD:field=RIE,bitOffset=2,bitWidth=1,description="Receive FIFO Interrupt Enable \n              0: RxFIFO threshold-level-reached interrupt is disabled \n              1: RxFIFO threshold-level-reached interrupt is enabled" */
        volatile uint32_t RIE: 1;  /* Receive FIFO Interrupt Enable                0: RxFIFO thres */
        /* @SVD:field=TIE,bitOffset=3,bitWidth=1,description="Transmit FIFO Interrupt Enable \n              0: TxFIFO threshold-level-reached interrupt is disabled \n              1: TxFIFO threshold-level-reached interrupt is enabled" */
        volatile uint32_t TIE: 1;  /* Transmit FIFO Interrupt Enable                0: TxFIFO thre */
        /* @SVD:field=RIM,bitOffset=4,bitWidth=1,description="Receive FIFO Overrun Interrupt Mask \n              0: ROR events generate an SPI interrupt \n              1: ROR events do NOT generate an SPI interrupt" */
        volatile uint32_t RIM: 1;  /* Receive FIFO Overrun Interrupt Mask                0: ROR ev */
        /* @SVD:field=TIM,bitOffset=5,bitWidth=1,description="Transmit FIFO Underrun Interrupt Mask\n              0 : TUR events generate an SPI interrupt \n              1 : TUR events do NOT generate an SPI interrupt" */
        volatile uint32_t TIM: 1;  /* Transmit FIFO Underrun Interrupt Mask               0 : TUR  */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_INTE_Type;

/* @SVD:register=TO,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="SPI Time Out Register" */
/* TO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TIMEOUT,bitOffset=0,bitWidth=24,description="Timeout Value TIMEOUT value is the value (0 to 2^24-1) that defines the time-out interval. The time-out interval is given by the equation shown in the TIMEOUT Interval Equation." */
        volatile uint32_t TIMEOUT: 24;  /* Timeout Value TIMEOUT value is the value (0 to 2^24-1) that  */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_TO_Type;

/* @SVD:register=DATA,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="SPI DATA Register" */
/* DATA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="DATA This field is used for data to be written to the TXFIFO read from the RXFIFO." */
        volatile uint32_t DATA: 32;  /* DATA This field is used for data to be written to the TXFIFO */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_DATA_Type;

/* @SVD:register=STATUS,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Status Register" */
/* STATUS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BSY,bitOffset=0,bitWidth=1,description="SPI controller Busy \n              0: SPI controller is idle or disabled \n              1: SPI controller is currently transmitting or receiving framed data" */
        volatile uint32_t BSY: 1;  /* SPI controller Busy                0: SPI controller is idle */
        /* @SVD:field=CSS,bitOffset=1,bitWidth=1,description="Clock Synchronization Status \n              0: SPI controller is ready for slave clock operations \n              1: SPI controller is currently busy synchronizing slave mode signals" */
        volatile uint32_t CSS: 1;  /* Clock Synchronization Status                0: SPI controlle */
        /* @SVD:field=RSVD6,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=TINT,bitOffset=3,bitWidth=1,description="Receiver Time-out Interrupt \n              0: No receiver time-out is pending \n              1: Receiver time-out pending, causes an interrupt request" */
        volatile uint32_t TINT: 1;  /* Receiver Time-out Interrupt                0: No receiver ti */
        /* @SVD:field=RSVD5,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=TFS,bitOffset=5,bitWidth=1,description="Transmit FIFO Service Request \n              0: TX FIFO level exceeds the TFT threshold (TFT + 1) or SPI controller is disabled \n              1: TXFIFO level is at or below TFT threshold (TFT + 1), causes an interrupt request" */
        volatile uint32_t TFS: 1;  /* Transmit FIFO Service Request                0: TX FIFO leve */
        /* @SVD:field=TNF,bitOffset=6,bitWidth=1,description="Transmit FIFO Not Full \n              0: TXFIFO is full \n              1: TXFIFO is not full" */
        volatile uint32_t TNF: 1;  /* Transmit FIFO Not Full                0: TXFIFO is full      */
        /* @SVD:field=TFL,bitOffset=7,bitWidth=4,description="Transmit FIFO Level This field is the number of entries in TXFIFO.When the value 0x0 is read, the TXFIFO is either empty or full, and software should read the [Transmit FIFO Not Full] field." */
        volatile uint32_t TFL: 4;  /* Transmit FIFO Level This field is the number of entries in T */
        /* @SVD:field=RSVD4,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=TUR,bitOffset=12,bitWidth=1,description="Transmit FIFO Underrun \n              0: The TXFIFO has not experienced an underrun \n              1: A read from the TXFIFO was attempted when the TXFIFO was empty, causes an interrupt if it is enabled ([Transmit FIFO Underrun Interrupt Mask] in the INT EN Register  is 0)" */
        volatile uint32_t TUR: 1;  /* Transmit FIFO Underrun                0: The TXFIFO has not  */
        /* @SVD:field=RFS,bitOffset=13,bitWidth=1,description="Receive FIFO Service Request \n              0: RXFIFO level is at or below RFT threshold (RFT) or SPI controller is disabled \n              1: RXFIFO level exceeds RFT threshold (RFT), causes an interrupt request" */
        volatile uint32_t RFS: 1;  /* Receive FIFO Service Request                0: RXFIFO level  */
        /* @SVD:field=RNE,bitOffset=14,bitWidth=1,description="Receive FIFO Not Empty \n              0: RXFIFO is empty \n              1: RXFIFO is not empty" */
        volatile uint32_t RNE: 1;  /* Receive FIFO Not Empty                0: RXFIFO is empty     */
        /* @SVD:field=RFL,bitOffset=15,bitWidth=4,description="Receive FIFO Level This field is the number of entries minus one in RXFIFO. When the value 0xF is read, the RXFIFO is either empty or full, and software should read the [Receive FIFO Not Empty] field." */
        volatile uint32_t RFL: 4;  /* Receive FIFO Level This field is the number of entries minus */
        /* @SVD:field=RSVD3,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=ROR,bitOffset=20,bitWidth=1,description="Receive FIFO Overrun \n              0: RXFIFO has not experienced an overrun \n              1: Attempted data write to full RXFIFO, causes an interrupt request" */
        volatile uint32_t ROR: 1;  /* Receive FIFO Overrun                0: RXFIFO has not experi */
        /* @SVD:field=RSVD2,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TX_OSS,bitOffset=22,bitWidth=1,description="TX FIFO Odd Sample Status When SPI controller is in packed mode, \n              the number of samples in the TX FIFO is:  ([Transmit FIFO Level]*2 + this field), when [Transmit FIFO Not Full] = 1 32, when [Transmit FIFO Not Full] = 0.\n              The TX FIFO cannot accept new data when [Transmit FIFO Not Full] = 1 and [Transmit FIFO Level] = 15 and this field = 1. (The TX FIFO has 31 samples).  \n              0: TxFIFO entry has an even number of samples \n              1: TxFIFO entry has an odd number of samples Note that this bit needs to be read only when FIFO Packing is enabled ([FIFO Packing Enable] in the FIFO Control Register is set). Otherwise, this bit is zero." */
        volatile uint32_t TX_OSS: 1;  /* TX FIFO Odd Sample Status When SPI controller is in packed m */
        /* @SVD:field=OSS,bitOffset=23,bitWidth=1,description="Odd Sample Status \n              0: RxFIFO entry has two samples \n              1: RxFIFO entry has one sample  \n              Note that this bit needs to be looked at only when FIFO Packing is enabled (FPCKE field in FIFO Control Register is set). Otherwise, this bit is zero. When SPI controller is in Packed mode and the CPU is used instead of DMA to read the RxFIFO, the CPU should make sure that [Receive FIFO Not Empty] = 1 AND this field = 0 before it attempts to read the RxFIFO." */
        volatile uint32_t OSS: 1;  /* Odd Sample Status                0: RxFIFO entry has two sam */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_STATUS_Type;

/* @SVD:register=RWOT_CTRL,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="RWOT Control Register" */
/* RWOT_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWOT,bitOffset=0,bitWidth=1,description="Receive Without Transmit \n              0: Transmit/receive mode \n              1: Receive without transmit mode" */
        volatile uint32_t RWOT: 1;  /* Receive Without Transmit                0: Transmit/receive  */
        /* @SVD:field=CYCLE_RWOT_EN,bitOffset=1,bitWidth=1,description="Enable RWOT Cycle Counter Mode \n              1: Enable" */
        volatile uint32_t CYCLE_RWOT_EN: 1;  /* Enable RWOT Cycle Counter Mode                1: Enable */
        /* @SVD:field=SET_RWOT_CYCLE,bitOffset=2,bitWidth=1,description="Set RWOT Cycle This field is used to set the value of the RWOT_CCM register to the internal rwot_counter. This field is self-cleared after SSE = 1. \n              1: Set rwot_counter" */
        volatile uint32_t SET_RWOT_CYCLE: 1;  /* Set RWOT Cycle This field is used to set the value of the RW */
        /* @SVD:field=CLR_RWOT_CYCLE,bitOffset=3,bitWidth=1,description="Clear Internal rwot_counter This field clears the rwot_counter to 0. This field is self cleared after SSE = 1. \n              1: Clear rwot_counter" */
        volatile uint32_t CLR_RWOT_CYCLE: 1;  /* Clear Internal rwot_counter This field clears the rwot_count */
        /* @SVD:field=MASK_RWOT_LAST_SAMPLE,bitOffset=4,bitWidth=1,description="Mask last_sample_flag in RWOT Mode \n              1: Mask \n              0: Unmask" */
        volatile uint32_t MASK_RWOT_LAST_SAMPLE: 1;  /* Mask last_sample_flag in RWOT Mode                1: Mask    */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_RWOT_CTRL_Type;

/* @SVD:register=RWOT_CCM,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="RWOT Counter Cycles Match Register" */
/* RWOT_CCM 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWOTCCM,bitOffset=0,bitWidth=32,description="It's just total SPI_CLK Cycles. \n              The value of this register defines the total number of SPI_CLK cycles when SPI controller works in master and RWOT mode. \n              When the rwot_counter matches this value, SPI controller returns to IDLE state and does not output SPI_CLK anymore." */
        volatile uint32_t RWOTCCM: 32;  /* It's just total SPI_CLK Cycles.                The value of  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_RWOT_CCM_Type;

/* @SVD:register=RWOT_CVWRN,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="RWOT Counter Value Write for Red Request Register" */
/* RWOT_CVWRN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWOTCVWR,bitOffset=0,bitWidth=32,description="RWOTCVWR This register prevents the risk of instability on rwot_counter value reading, it's only valid after SPI controller has been enabled Write 0 = No effect Write 1 = Capture value of rwot_counter Read: Returns the captured value of rwot_counter" */
        volatile uint32_t RWOTCVWR: 32;  /* RWOTCVWR This register prevents the risk of instability on r */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_RWOT_CVWRN_Type;

/* @SVD:register=CLK_CTRL,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="CLK  Control Register" */
/* CLK_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLK_DIV,bitOffset=0,bitWidth=7,description="div ratio from clk_sys" */
        volatile uint32_t CLK_DIV: 7;  /* div ratio from clk_sys */
        /* @SVD:field=CLK_SEL,bitOffset=7,bitWidth=1,description="0: select clk_div as clk for SPI controller\n              1: select clk_sys as clk for SPI controller" */
        volatile uint32_t CLK_SEL: 1;  /* 0: select clk_div as clk for SPI controller               1: */
        /* @SVD:field=CLK_EN,bitOffset=8,bitWidth=1,description="enable clk for internal logic" */
        volatile uint32_t CLK_EN: 1;  /* enable clk for internal logic */
        /* @SVD:field=SPI_DI_SEL,bitOffset=9,bitWidth=1,description="Select spi_di source.\n              0: from port SPI_DI.\n              1: from port SPI_DIO." */
        volatile uint32_t SPI_DI_SEL: 1;  /* Select spi_di source.               0: from port SPI_DI.     */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_CLK_CTRL_Type;

/* @SVD:register=TRIWIRE_CTRL,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="Three Wire Mode  Control Register" */
/* TRIWIRE_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SPI_TRI_WIRE_EN,bitOffset=0,bitWidth=1,description="SPI_THREE_WIRE_MODE_EN \n              0: normal mode\n              1: enable TRI-WIRE mode" */
        volatile uint32_t SPI_TRI_WIRE_EN: 1;  /* SPI_THREE_WIRE_MODE_EN                0: normal mode         */
        /* @SVD:field=TXD_OEN,bitOffset=1,bitWidth=1,description="TXD_OEN control when TRI-WIRE mode\n              1: SPI_DIO is input  \n              0: SPI_DIO is output" */
        volatile uint32_t TXD_OEN: 1;  /* TXD_OEN control when TRI-WIRE mode               1: SPI_DIO  */
        /* @SVD:field=WORK_WIDTH_DYN_CHANGE,bitOffset=2,bitWidth=1,description="WORK_WIDTH_DYN_CHNAGE \n              1: SW can dynamicly change TOP_CTRL[9:5] without disabling TOP_CTRL[0] and re-enabling TOP_CTRL[0]" */
        volatile uint32_t WORK_WIDTH_DYN_CHANGE: 1;  /* WORK_WIDTH_DYN_CHNAGE                1: SW can dynamicly cha */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=29,description="" */
        volatile uint32_t RSVD: 29;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI1_TRIWIRE_CTRL_Type;

/* SPI1 寄存器结构体 */
typedef struct
{
    SPI1_TOP_CTRL_Type TOP_CTRL;  /* 0x0000 - Top Control Register */
    SPI1_FIFO_CTRL_Type FIFO_CTRL;  /* 0x0004 - FIFO Control Register */
    SPI1_INTE_Type INTE;  /* 0x0008 - Interrupt Enable Register */
    SPI1_TO_Type TO;  /* 0x000C - SPI Time Out Register */
    SPI1_DATA_Type DATA;  /* 0x0010 - SPI DATA Register */
    SPI1_STATUS_Type STATUS;  /* 0x0014 - Status Register */
    /* @SVD:register=RSVD3,offset=0x0018,size=32,access=read-write,reset=0x0,description="RSVD3" */
    volatile uint32_t RSVD3;  /* 0x0018 - RSVD3 */
    volatile uint32_t RESERVED7[2];  /* 0x001C - 0x0023 */
    SPI1_RWOT_CTRL_Type RWOT_CTRL;  /* 0x0024 - RWOT Control Register */
    SPI1_RWOT_CCM_Type RWOT_CCM;  /* 0x0028 - RWOT Counter Cycles Match Register */
    SPI1_RWOT_CVWRN_Type RWOT_CVWRN;  /* 0x002C - RWOT Counter Value Write for Red Request */
    /* @SVD:register=RSVD2,offset=0x0030,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x0030 - RSVD2 */
    volatile uint32_t RESERVED13[2];  /* 0x0034 - 0x003B */
    SPI1_CLK_CTRL_Type CLK_CTRL;  /* 0x003C - CLK  Control Register */
    /* @SVD:register=RSVD1,offset=0x0040,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0040 - RSVD1 */
    volatile uint32_t RESERVED17[4];  /* 0x0044 - 0x0053 */
    SPI1_TRIWIRE_CTRL_Type TRIWIRE_CTRL;  /* 0x0054 - Three Wire Mode  Control Register */
} SPI1_TypeDef;

/* ============================================================================ */
/* SPI2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=SPI2,base=0x50096000,description="",groupName=SPI */

#define SPI2_BASE                                          (0x50096000UL)

/* @SVD:register=TOP_CTRL,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Top Control Register" */
/* TOP_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SSE,bitOffset=0,bitWidth=1,description="SPI controller Enable \n              0: SPI controller is disabled  \n              1: SPI controller is enabled" */
        volatile uint32_t SSE: 1;  /* SPI controller Enable                0: SPI controller is di */
        /* @SVD:field=FRF,bitOffset=1,bitWidth=2,description="Frame Format \n              0x0: Motorola* Serial Peripheral Interface (SPI)  \n              0x1: Texas Instruments* Synchronous Serial Protocol (SSP) \n              0x2: National Semiconductor Microwire* \n              0x3: RSVD" */
        volatile uint32_t FRF: 2;  /* Frame Format                0x0: Motorola* Serial Peripheral */
        /* @SVD:field=SCLKDIR,bitOffset=3,bitWidth=1,description="SPI_CLK Direction \n              0: Master mode, SPI controller drives SPI_CLK \n              1: Slave mode, SPI controller receives SPI_CLK" */
        volatile uint32_t SCLKDIR: 1;  /* SPI_CLK Direction                0: Master mode, SPI control */
        /* @SVD:field=SFRMDIR,bitOffset=4,bitWidth=1,description="SPI_CS Direction \n              0: Master mode, SPI controller drives SPI_CS \n              1: Slave mode, SPI controller receives SPI_CS" */
        volatile uint32_t SFRMDIR: 1;  /* SPI_CS Direction                0: Master mode, SPI controll */
        /* @SVD:field=DSS,bitOffset=5,bitWidth=5,description="SPI controller Work data size,  register bits value 0~31 indicated data size 1~32 bits,  usually use data size 8bits, 16bits, 24bits, 32bits" */
        volatile uint32_t DSS: 5;  /* SPI controller Work data size,  register bits value 0~31 ind */
        /* @SVD:field=SPO,bitOffset=10,bitWidth=1,description="Motorola SPI SPI_CLK Polarity Setting \n              0: The inactive or idle state of SPI_CLK is low \n              1: The inactive or idle state of SPI_CLK is high" */
        volatile uint32_t SPO: 1;  /* Motorola SPI SPI_CLK Polarity Setting                0: The  */
        /* @SVD:field=SPH,bitOffset=11,bitWidth=1,description="Motorola SPI SPI_CLK phase setting \n              0: SPI_CLK is inactive until one cycle after the start of a frame and active until 1/2 cycle before the end of a frame \n              1: SPI_CLK is inactive until 1/2 cycle after the start of a frame and active until one cycle before the end of a frame" */
        volatile uint32_t SPH: 1;  /* Motorola SPI SPI_CLK phase setting                0: SPI_CLK */
        /* @SVD:field=RSVD3,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=TRAIL,bitOffset=13,bitWidth=1,description="Trailing Byte \n              0: Trailing bytes are handled by CPU \n              1: Trailing bytes are handled by DMA bursts" */
        volatile uint32_t TRAIL: 1;  /* Trailing Byte                0: Trailing bytes are handled b */
        /* @SVD:field=HOLD_FRAME_LOW,bitOffset=14,bitWidth=1,description="Hold Frame Low Control \n              0:After this field is set to 1 and the SPI controller is operating in master mode,the output frame signal SPI_CS will be determined by control FSM.\n              1:After this field is set to 1 and the SPI controller is operating in master mode, the output frame signal SPI_CS will hold low." */
        volatile uint32_t HOLD_FRAME_LOW: 1;  /* Hold Frame Low Control                0:After this field is  */
        /* @SVD:field=IFS,bitOffset=15,bitWidth=1,description="Invert Frame Signal \n              0: SPI_CS polarity is as defined in protocol\n              1: SPI_CS will be inverted from normal-SPI_CS" */
        volatile uint32_t IFS: 1;  /* Invert Frame Signal                0: SPI_CS polarity is as  */
        /* @SVD:field=RSVD2,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TTE,bitOffset=17,bitWidth=1,description="SPI_DO Three-State Enable \n              0: SPI_DO output signal is not three-stated \n              1: SPI_DO is three-stated when not transmitting data" */
        volatile uint32_t TTE: 1;  /* SPI_DO Three-State Enable                0: SPI_DO output si */
        /* @SVD:field=TTELP,bitOffset=18,bitWidth=1,description="SPI_DO Three-state Enable On Last Phase (can be set only when TI-SSP)\n              0: SPI_DO is three-stated 1/2 clock cycle after the beginning of the LSB \n              1: SPI_DO output signal is three-stated on the clock edge that ends the LSB" */
        volatile uint32_t TTELP: 1;  /* SPI_DO Three-state Enable On Last Phase (can be set only whe */
        /* @SVD:field=RSVD,bitOffset=19,bitWidth=13,description="" */
        volatile uint32_t RSVD: 13;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_TOP_CTRL_Type;

/* @SVD:register=FIFO_CTRL,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="FIFO Control Register" */
/* FIFO_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TFT,bitOffset=0,bitWidth=5,description="TXFIFO Trigger Threshold This field sets the threshold level at which TXFIFO asserts interrupt. The level should be set to the preferred threshold value minus 1." */
        volatile uint32_t TFT: 5;  /* TXFIFO Trigger Threshold This field sets the threshold level */
        /* @SVD:field=RFT,bitOffset=5,bitWidth=5,description="RXFIFO Trigger Threshold This field sets the threshold level at which RXFIFO asserts interrupt. The level should be set to the preferred threshold value minus 1." */
        volatile uint32_t RFT: 5;  /* RXFIFO Trigger Threshold This field sets the threshold level */
        /* @SVD:field=TSRE,bitOffset=10,bitWidth=1,description="Transmit Service Request Enable \n              0: TxFIFO DMA service request is disabled \n              1: TxFIFO DMA service request is enabled" */
        volatile uint32_t TSRE: 1;  /* Transmit Service Request Enable                0: TxFIFO DMA */
        /* @SVD:field=RSRE,bitOffset=11,bitWidth=1,description="Receive Service Request Enable \n              0: RxFIFO DMA service request is disabled \n              1: RxFIFO DMA service request is enabled" */
        volatile uint32_t RSRE: 1;  /* Receive Service Request Enable                0: RxFIFO DMA  */
        /* @SVD:field=RXFIFO_RD_ENDIAN,bitOffset=12,bitWidth=2,description="apb_prdata Read from Rx FIFO Endian \n              0x0 = apb_prdata[31:0] = rxfifo_wdata[31:0] \n              0x1 = apb_prdata[31:0] = {rxfifo_wdata[15:0], rxfifo_wdata[31:16]} \n              0x2 = apb_prdata[31:0]= {rxfifo_wdata[7:0], rxfifo_wdata[15:8], rxfifo_wdata[23:16], rxfifo_wdata[31:24]} \n              0x3 = apb_prdata[31:0]= {rxfifo_wdata[23:16], rxfifo_wdata[31:24], rxfifo_wdata[7:0], rxfifo_wdata[15:8]}" */
        volatile uint32_t RXFIFO_RD_ENDIAN: 2;  /* apb_prdata Read from Rx FIFO Endian                0x0 = apb */
        /* @SVD:field=TXFIFO_WR_ENDIAN,bitOffset=14,bitWidth=2,description="apb_pwdata Write to TxFIFO Endian \n              0x0: txfifo_wdata[31:0] = apb_pwdata[31:0] \n              0x1: fifo_wdata[31:0] = {apb_pwdata[15:0], apb_pwdata[31:16]} \n              0x2: txfifo_wdata[31:0] = {apb_pwdata[7:0], apb_pwdata[15:8], apb_pwdata[23:16], apb_pwdata[31:24]} \n              0x3: txfifo_wdata[31:0] = {apb_pwdata[23:16], apb_pwdata[31:24], apb_pwdata[7:0], apb_pwdata[15:8]}" */
        volatile uint32_t TXFIFO_WR_ENDIAN: 2;  /* apb_pwdata Write to TxFIFO Endian                0x0: txfifo */
        /* @SVD:field=FPCKE,bitOffset=16,bitWidth=1,description="FIFO Packing Enable \n              0: FIFO packing mode disabled \n              1: FIFO packing mode enabled" */
        volatile uint32_t FPCKE: 1;  /* FIFO Packing Enable                0: FIFO packing mode disa */
        /* @SVD:field=RXFIFO_AUTO_FULL_CTRL,bitOffset=17,bitWidth=1,description="Rx FIFO Auto Full Control: After this field is set to 1 and the SPI controller is operating in master mode, the controller FSM returns to IDLE state and stops the SPI_CLK. \n              When Rx FIFO is full, the controller FSM continues transferring data after the RxFIFO is not full. \n              This field is used to avoid an RxFIFO overrun issue. \n              1: Enable Rx FIFO auto full control" */
        volatile uint32_t RXFIFO_AUTO_FULL_CTRL: 1;  /* Rx FIFO Auto Full Control: After this field is set to 1 and  */
        /* @SVD:field=RSVD,bitOffset=18,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_FIFO_CTRL_Type;

/* @SVD:register=INTE,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Interrupt Enable Register" */
/* INTE 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TINTE,bitOffset=1,bitWidth=1,description="Receiver Time-out Interrupt Enable \n              0: Receiver time-out interrupt is disabled \n              1: Receiver time-out interrupt is enabled" */
        volatile uint32_t TINTE: 1;  /* Receiver Time-out Interrupt Enable                0: Receive */
        /* @SVD:field=RIE,bitOffset=2,bitWidth=1,description="Receive FIFO Interrupt Enable \n              0: RxFIFO threshold-level-reached interrupt is disabled \n              1: RxFIFO threshold-level-reached interrupt is enabled" */
        volatile uint32_t RIE: 1;  /* Receive FIFO Interrupt Enable                0: RxFIFO thres */
        /* @SVD:field=TIE,bitOffset=3,bitWidth=1,description="Transmit FIFO Interrupt Enable \n              0: TxFIFO threshold-level-reached interrupt is disabled \n              1: TxFIFO threshold-level-reached interrupt is enabled" */
        volatile uint32_t TIE: 1;  /* Transmit FIFO Interrupt Enable                0: TxFIFO thre */
        /* @SVD:field=RIM,bitOffset=4,bitWidth=1,description="Receive FIFO Overrun Interrupt Mask \n              0: ROR events generate an SPI interrupt \n              1: ROR events do NOT generate an SPI interrupt" */
        volatile uint32_t RIM: 1;  /* Receive FIFO Overrun Interrupt Mask                0: ROR ev */
        /* @SVD:field=TIM,bitOffset=5,bitWidth=1,description="Transmit FIFO Underrun Interrupt Mask\n              0 : TUR events generate an SPI interrupt \n              1 : TUR events do NOT generate an SPI interrupt" */
        volatile uint32_t TIM: 1;  /* Transmit FIFO Underrun Interrupt Mask               0 : TUR  */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_INTE_Type;

/* @SVD:register=TO,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="SPI Time Out Register" */
/* TO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TIMEOUT,bitOffset=0,bitWidth=24,description="Timeout Value TIMEOUT value is the value (0 to 2^24-1) that defines the time-out interval. The time-out interval is given by the equation shown in the TIMEOUT Interval Equation." */
        volatile uint32_t TIMEOUT: 24;  /* Timeout Value TIMEOUT value is the value (0 to 2^24-1) that  */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_TO_Type;

/* @SVD:register=DATA,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="SPI DATA Register" */
/* DATA 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="DATA This field is used for data to be written to the TXFIFO read from the RXFIFO." */
        volatile uint32_t DATA: 32;  /* DATA This field is used for data to be written to the TXFIFO */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_DATA_Type;

/* @SVD:register=STATUS,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Status Register" */
/* STATUS 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BSY,bitOffset=0,bitWidth=1,description="SPI controller Busy \n              0: SPI controller is idle or disabled \n              1: SPI controller is currently transmitting or receiving framed data" */
        volatile uint32_t BSY: 1;  /* SPI controller Busy                0: SPI controller is idle */
        /* @SVD:field=CSS,bitOffset=1,bitWidth=1,description="Clock Synchronization Status \n              0: SPI controller is ready for slave clock operations \n              1: SPI controller is currently busy synchronizing slave mode signals" */
        volatile uint32_t CSS: 1;  /* Clock Synchronization Status                0: SPI controlle */
        /* @SVD:field=RSVD6,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=TINT,bitOffset=3,bitWidth=1,description="Receiver Time-out Interrupt \n              0: No receiver time-out is pending \n              1: Receiver time-out pending, causes an interrupt request" */
        volatile uint32_t TINT: 1;  /* Receiver Time-out Interrupt                0: No receiver ti */
        /* @SVD:field=RSVD5,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=TFS,bitOffset=5,bitWidth=1,description="Transmit FIFO Service Request \n              0: TX FIFO level exceeds the TFT threshold (TFT + 1) or SPI controller is disabled \n              1: TXFIFO level is at or below TFT threshold (TFT + 1), causes an interrupt request" */
        volatile uint32_t TFS: 1;  /* Transmit FIFO Service Request                0: TX FIFO leve */
        /* @SVD:field=TNF,bitOffset=6,bitWidth=1,description="Transmit FIFO Not Full \n              0: TXFIFO is full \n              1: TXFIFO is not full" */
        volatile uint32_t TNF: 1;  /* Transmit FIFO Not Full                0: TXFIFO is full      */
        /* @SVD:field=TFL,bitOffset=7,bitWidth=4,description="Transmit FIFO Level This field is the number of entries in TXFIFO.When the value 0x0 is read, the TXFIFO is either empty or full, and software should read the [Transmit FIFO Not Full] field." */
        volatile uint32_t TFL: 4;  /* Transmit FIFO Level This field is the number of entries in T */
        /* @SVD:field=RSVD4,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=TUR,bitOffset=12,bitWidth=1,description="Transmit FIFO Underrun \n              0: The TXFIFO has not experienced an underrun \n              1: A read from the TXFIFO was attempted when the TXFIFO was empty, causes an interrupt if it is enabled ([Transmit FIFO Underrun Interrupt Mask] in the INT EN Register  is 0)" */
        volatile uint32_t TUR: 1;  /* Transmit FIFO Underrun                0: The TXFIFO has not  */
        /* @SVD:field=RFS,bitOffset=13,bitWidth=1,description="Receive FIFO Service Request \n              0: RXFIFO level is at or below RFT threshold (RFT) or SPI controller is disabled \n              1: RXFIFO level exceeds RFT threshold (RFT), causes an interrupt request" */
        volatile uint32_t RFS: 1;  /* Receive FIFO Service Request                0: RXFIFO level  */
        /* @SVD:field=RNE,bitOffset=14,bitWidth=1,description="Receive FIFO Not Empty \n              0: RXFIFO is empty \n              1: RXFIFO is not empty" */
        volatile uint32_t RNE: 1;  /* Receive FIFO Not Empty                0: RXFIFO is empty     */
        /* @SVD:field=RFL,bitOffset=15,bitWidth=4,description="Receive FIFO Level This field is the number of entries minus one in RXFIFO. When the value 0xF is read, the RXFIFO is either empty or full, and software should read the [Receive FIFO Not Empty] field." */
        volatile uint32_t RFL: 4;  /* Receive FIFO Level This field is the number of entries minus */
        /* @SVD:field=RSVD3,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=ROR,bitOffset=20,bitWidth=1,description="Receive FIFO Overrun \n              0: RXFIFO has not experienced an overrun \n              1: Attempted data write to full RXFIFO, causes an interrupt request" */
        volatile uint32_t ROR: 1;  /* Receive FIFO Overrun                0: RXFIFO has not experi */
        /* @SVD:field=RSVD2,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TX_OSS,bitOffset=22,bitWidth=1,description="TX FIFO Odd Sample Status When SPI controller is in packed mode, \n              the number of samples in the TX FIFO is:  ([Transmit FIFO Level]*2 + this field), when [Transmit FIFO Not Full] = 1 32, when [Transmit FIFO Not Full] = 0.\n              The TX FIFO cannot accept new data when [Transmit FIFO Not Full] = 1 and [Transmit FIFO Level] = 15 and this field = 1. (The TX FIFO has 31 samples).  \n              0: TxFIFO entry has an even number of samples \n              1: TxFIFO entry has an odd number of samples Note that this bit needs to be read only when FIFO Packing is enabled ([FIFO Packing Enable] in the FIFO Control Register is set). Otherwise, this bit is zero." */
        volatile uint32_t TX_OSS: 1;  /* TX FIFO Odd Sample Status When SPI controller is in packed m */
        /* @SVD:field=OSS,bitOffset=23,bitWidth=1,description="Odd Sample Status \n              0: RxFIFO entry has two samples \n              1: RxFIFO entry has one sample  \n              Note that this bit needs to be looked at only when FIFO Packing is enabled (FPCKE field in FIFO Control Register is set). Otherwise, this bit is zero. When SPI controller is in Packed mode and the CPU is used instead of DMA to read the RxFIFO, the CPU should make sure that [Receive FIFO Not Empty] = 1 AND this field = 0 before it attempts to read the RxFIFO." */
        volatile uint32_t OSS: 1;  /* Odd Sample Status                0: RxFIFO entry has two sam */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_STATUS_Type;

/* @SVD:register=RWOT_CTRL,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="RWOT Control Register" */
/* RWOT_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWOT,bitOffset=0,bitWidth=1,description="Receive Without Transmit \n              0: Transmit/receive mode \n              1: Receive without transmit mode" */
        volatile uint32_t RWOT: 1;  /* Receive Without Transmit                0: Transmit/receive  */
        /* @SVD:field=CYCLE_RWOT_EN,bitOffset=1,bitWidth=1,description="Enable RWOT Cycle Counter Mode \n              1: Enable" */
        volatile uint32_t CYCLE_RWOT_EN: 1;  /* Enable RWOT Cycle Counter Mode                1: Enable */
        /* @SVD:field=SET_RWOT_CYCLE,bitOffset=2,bitWidth=1,description="Set RWOT Cycle This field is used to set the value of the RWOT_CCM register to the internal rwot_counter. This field is self-cleared after SSE = 1. \n              1: Set rwot_counter" */
        volatile uint32_t SET_RWOT_CYCLE: 1;  /* Set RWOT Cycle This field is used to set the value of the RW */
        /* @SVD:field=CLR_RWOT_CYCLE,bitOffset=3,bitWidth=1,description="Clear Internal rwot_counter This field clears the rwot_counter to 0. This field is self cleared after SSE = 1. \n              1: Clear rwot_counter" */
        volatile uint32_t CLR_RWOT_CYCLE: 1;  /* Clear Internal rwot_counter This field clears the rwot_count */
        /* @SVD:field=MASK_RWOT_LAST_SAMPLE,bitOffset=4,bitWidth=1,description="Mask last_sample_flag in RWOT Mode \n              1: Mask \n              0: Unmask" */
        volatile uint32_t MASK_RWOT_LAST_SAMPLE: 1;  /* Mask last_sample_flag in RWOT Mode                1: Mask    */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_RWOT_CTRL_Type;

/* @SVD:register=RWOT_CCM,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="RWOT Counter Cycles Match Register" */
/* RWOT_CCM 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWOTCCM,bitOffset=0,bitWidth=32,description="It's just total SPI_CLK Cycles. \n              The value of this register defines the total number of SPI_CLK cycles when SPI controller works in master and RWOT mode. \n              When the rwot_counter matches this value, SPI controller returns to IDLE state and does not output SPI_CLK anymore." */
        volatile uint32_t RWOTCCM: 32;  /* It's just total SPI_CLK Cycles.                The value of  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_RWOT_CCM_Type;

/* @SVD:register=RWOT_CVWRN,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="RWOT Counter Value Write for Red Request Register" */
/* RWOT_CVWRN 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWOTCVWR,bitOffset=0,bitWidth=32,description="RWOTCVWR This register prevents the risk of instability on rwot_counter value reading, it's only valid after SPI controller has been enabled Write 0 = No effect Write 1 = Capture value of rwot_counter Read: Returns the captured value of rwot_counter" */
        volatile uint32_t RWOTCVWR: 32;  /* RWOTCVWR This register prevents the risk of instability on r */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_RWOT_CVWRN_Type;

/* @SVD:register=CLK_CTRL,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="CLK  Control Register" */
/* CLK_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CLK_DIV,bitOffset=0,bitWidth=7,description="div ratio from clk_sys" */
        volatile uint32_t CLK_DIV: 7;  /* div ratio from clk_sys */
        /* @SVD:field=CLK_SEL,bitOffset=7,bitWidth=1,description="0: select clk_div as clk for SPI controller\n              1: select clk_sys as clk for SPI controller" */
        volatile uint32_t CLK_SEL: 1;  /* 0: select clk_div as clk for SPI controller               1: */
        /* @SVD:field=CLK_EN,bitOffset=8,bitWidth=1,description="enable clk for internal logic" */
        volatile uint32_t CLK_EN: 1;  /* enable clk for internal logic */
        /* @SVD:field=SPI_DI_SEL,bitOffset=9,bitWidth=1,description="Select spi_di source.\n              0: from port SPI_DI.\n              1: from port SPI_DIO." */
        volatile uint32_t SPI_DI_SEL: 1;  /* Select spi_di source.               0: from port SPI_DI.     */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_CLK_CTRL_Type;

/* @SVD:register=TRIWIRE_CTRL,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="Three Wire Mode  Control Register" */
/* TRIWIRE_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SPI_TRI_WIRE_EN,bitOffset=0,bitWidth=1,description="SPI_THREE_WIRE_MODE_EN \n              0: normal mode\n              1: enable TRI-WIRE mode" */
        volatile uint32_t SPI_TRI_WIRE_EN: 1;  /* SPI_THREE_WIRE_MODE_EN                0: normal mode         */
        /* @SVD:field=TXD_OEN,bitOffset=1,bitWidth=1,description="TXD_OEN control when TRI-WIRE mode\n              1: SPI_DIO is input  \n              0: SPI_DIO is output" */
        volatile uint32_t TXD_OEN: 1;  /* TXD_OEN control when TRI-WIRE mode               1: SPI_DIO  */
        /* @SVD:field=WORK_WIDTH_DYN_CHANGE,bitOffset=2,bitWidth=1,description="WORK_WIDTH_DYN_CHNAGE \n              1: SW can dynamicly change TOP_CTRL[9:5] without disabling TOP_CTRL[0] and re-enabling TOP_CTRL[0]" */
        volatile uint32_t WORK_WIDTH_DYN_CHANGE: 1;  /* WORK_WIDTH_DYN_CHNAGE                1: SW can dynamicly cha */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=29,description="" */
        volatile uint32_t RSVD: 29;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} SPI2_TRIWIRE_CTRL_Type;

/* SPI2 寄存器结构体 */
typedef struct
{
    SPI2_TOP_CTRL_Type TOP_CTRL;  /* 0x0000 - Top Control Register */
    SPI2_FIFO_CTRL_Type FIFO_CTRL;  /* 0x0004 - FIFO Control Register */
    SPI2_INTE_Type INTE;  /* 0x0008 - Interrupt Enable Register */
    SPI2_TO_Type TO;  /* 0x000C - SPI Time Out Register */
    SPI2_DATA_Type DATA;  /* 0x0010 - SPI DATA Register */
    SPI2_STATUS_Type STATUS;  /* 0x0014 - Status Register */
    /* @SVD:register=RSVD3,offset=0x0018,size=32,access=read-write,reset=0x0,description="RSVD3" */
    volatile uint32_t RSVD3;  /* 0x0018 - RSVD3 */
    volatile uint32_t RESERVED7[2];  /* 0x001C - 0x0023 */
    SPI2_RWOT_CTRL_Type RWOT_CTRL;  /* 0x0024 - RWOT Control Register */
    SPI2_RWOT_CCM_Type RWOT_CCM;  /* 0x0028 - RWOT Counter Cycles Match Register */
    SPI2_RWOT_CVWRN_Type RWOT_CVWRN;  /* 0x002C - RWOT Counter Value Write for Red Request */
    /* @SVD:register=RSVD2,offset=0x0030,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x0030 - RSVD2 */
    volatile uint32_t RESERVED13[2];  /* 0x0034 - 0x003B */
    SPI2_CLK_CTRL_Type CLK_CTRL;  /* 0x003C - CLK  Control Register */
    /* @SVD:register=RSVD1,offset=0x0040,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0040 - RSVD1 */
    volatile uint32_t RESERVED17[4];  /* 0x0044 - 0x0053 */
    SPI2_TRIWIRE_CTRL_Type TRIWIRE_CTRL;  /* 0x0054 - Three Wire Mode  Control Register */
} SPI2_TypeDef;

/* ============================================================================ */
/* PDM1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=PDM1,base=0x5009A000,description="",groupName=PDM */

#define PDM1_BASE                                          (0x5009A000UL)

/* @SVD:register=CFG0,offset=0x0000,size=32,access=read-write,reset=0x0,description="CFG0" */
/* CFG0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PDMCOREEN,bitOffset=0,bitWidth=1,description="1:Enable pdm module; 0: Disable pdm module" */
        volatile uint32_t PDMCOREEN: 1;  /* 1:Enable pdm module; 0: Disable pdm module */
        /* @SVD:field=CLK_SEL,bitOffset=1,bitWidth=1,description="1:Clk select dll 3.072MHz; 0: Clk selct xtal 9.6MHz" */
        volatile uint32_t CLK_SEL: 1;  /* 1:Clk select dll 3.072MHz; 0: Clk selct xtal 9.6MHz */
        /* @SVD:field=CLK_DIV,bitOffset=2,bitWidth=4,description="Clock frequency division ratio of 3.072MHz or 9.6MHz according to register clk_sel" */
        volatile uint32_t CLK_DIV: 4;  /* Clock frequency division ratio of 3.072MHz or 9.6MHz accordi */
        /* @SVD:field=LEFT_EN,bitOffset=6,bitWidth=1,description="1: Enable left channel  pdm data sampling; 0: Disable left channel  pdm data sampling" */
        volatile uint32_t LEFT_EN: 1;  /* 1: Enable left channel  pdm data sampling; 0: Disable left c */
        /* @SVD:field=RIGHT_EN,bitOffset=7,bitWidth=1,description="1: Enable right channel pdm data sampling; 0: Disable right channel pdm data sampling" */
        volatile uint32_t RIGHT_EN: 1;  /* 1: Enable right channel pdm data sampling; 0: Disable right  */
        /* @SVD:field=STEREO_EN,bitOffset=8,bitWidth=1,description="1:Enable double channels pdm data sampling; 0: Disable double channels pdm data sampling" */
        volatile uint32_t STEREO_EN: 1;  /* 1:Enable double channels pdm data sampling; 0: Disable doubl */
        /* @SVD:field=SWAP_EN,bitOffset=9,bitWidth=1,description="1: Swap right channel and left channel pdm data; 0: Not swap  right channel and left channel pdm data" */
        volatile uint32_t SWAP_EN: 1;  /* 1: Swap right channel and left channel pdm data; 0: Not swap */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_CFG0_Type;

/* @SVD:register=CFG1,offset=0x0004,size=32,access=read-write,reset=0x0,description="CFG1" */
/* CFG1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=SAMPLE_DLY_L,bitOffset=5,bitWidth=3,description="The number of delay dff before the left data stream in processing" */
        volatile uint32_t SAMPLE_DLY_L: 3;  /* The number of delay dff before the left data stream in proce */
        /* @SVD:field=SAMPLE_DLY_R,bitOffset=8,bitWidth=3,description="The number of delay dff before the right data stream in processing" */
        volatile uint32_t SAMPLE_DLY_R: 3;  /* The number of delay dff before the right data stream in proc */
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_CFG1_Type;

/* @SVD:register=SINC_CFG,offset=0x0008,size=32,access=read-write,reset=0x0,description="SINC_CFG" */
/* SINC_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SINC_RATE,bitOffset=0,bitWidth=8,description="dowmsampling rate of  sinc filter" */
        volatile uint32_t SINC_RATE: 8;  /* dowmsampling rate of  sinc filter */
        /* @SVD:field=SINC_ORDER_SEL,bitOffset=8,bitWidth=1,description="1:select four differentiators in sinc filter; 0:select three differentiators in sinc filter" */
        volatile uint32_t SINC_ORDER_SEL: 1;  /* 1:select four differentiators in sinc filter; 0:select three */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_SINC_CFG_Type;

/* @SVD:register=HPF_CFG,offset=0x0014,size=32,access=read-write,reset=0x0,description="HPF_CFG" */
/* HPF_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HPF_COEFF,bitOffset=0,bitWidth=4,description="coefficient of high-pass filter" */
        volatile uint32_t HPF_COEFF: 4;  /* coefficient of high-pass filter */
        /* @SVD:field=HPF_BYPASS,bitOffset=4,bitWidth=1,description="1:bypass-high pass filter ; 0: enable high-pass filter" */
        volatile uint32_t HPF_BYPASS: 1;  /* 1:bypass-high pass filter ; 0: enable high-pass filter */
        /* @SVD:field=HPF_RST,bitOffset=5,bitWidth=1,description="1:high-pass filter normal operation ; 0:reset high-pass filter" */
        volatile uint32_t HPF_RST: 1;  /* 1:high-pass filter normal operation ; 0:reset high-pass filt */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_HPF_CFG_Type;

/* @SVD:register=PGA_CFG,offset=0x0018,size=32,access=read-write,reset=0x0,description="PGA_CFG" */
/* PGA_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PGA_GAIN_L,bitOffset=0,bitWidth=7,description="left channel gain control , the range is -15dB~45dB. Resolution is 0.5dB/LSB" */
        volatile uint32_t PGA_GAIN_L: 7;  /* left channel gain control , the range is -15dB~45dB. Resolut */
        /* @SVD:field=PGA_GAIN_R,bitOffset=7,bitWidth=7,description="right channel gain control , the range is -15dB~45dB. Resolution is 0.5dB/LSB" */
        volatile uint32_t PGA_GAIN_R: 7;  /* right channel gain control , the range is -15dB~45dB. Resolu */
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_PGA_CFG_Type;

/* @SVD:register=LPF_CFG6,offset=0x0034,size=32,access=read-write,reset=0x0,description="LPF_CFG6" */
/* LPF_CFG6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=12,description="" */
        volatile uint32_t RSVD2: 12;
        /* @SVD:field=LPF_DS,bitOffset=12,bitWidth=1,description="1:downsampling rate of low pass filter is two;0:No downsampling of low pass filter" */
        volatile uint32_t LPF_DS: 1;  /* 1:downsampling rate of low pass filter is two;0:No downsampl */
        /* @SVD:field=LPF_BYPASS,bitOffset=13,bitWidth=1,description="1:bypass low-pass filter ; 0: enable low-pass filter" */
        volatile uint32_t LPF_BYPASS: 1;  /* 1:bypass low-pass filter ; 0: enable low-pass filter */
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_LPF_CFG6_Type;

/* @SVD:register=FIFO_CFG,offset=0x0038,size=32,access=read-write,reset=0x0,description="FIFO_CFG" */
/* FIFO_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BYTE_CON,bitOffset=0,bitWidth=1,description="1: combine left channel and right channel; 0: not combine left channel and right channel" */
        volatile uint32_t BYTE_CON: 1;  /* 1: combine left channel and right channel; 0: not combine le */
        /* @SVD:field=BYTE_TRUNC,bitOffset=1,bitWidth=2,description="1: 16bits output ; 0: 24bits output ;2:  8bits output ; 3: 32bits output" */
        volatile uint32_t BYTE_TRUNC: 2;  /* 1: 16bits output ; 0: 24bits output ;2:  8bits output ; 3: 3 */
        /* @SVD:field=PDM_SHIFT,bitOffset=3,bitWidth=3,description="the number of data left shift for higher data accuracy" */
        volatile uint32_t PDM_SHIFT: 3;  /* the number of data left shift for higher data accuracy */
        /* @SVD:field=RX_DMA_MSK_R,bitOffset=6,bitWidth=1,description="1:disable right channel dma request; 0: enable right channel dma request" */
        volatile uint32_t RX_DMA_MSK_R: 1;  /* 1:disable right channel dma request; 0: enable right channel */
        /* @SVD:field=RX_DMA_MSK_L,bitOffset=7,bitWidth=1,description="1:disable left channel dma request; 0: enable left channel dma request" */
        volatile uint32_t RX_DMA_MSK_L: 1;  /* 1:disable left channel dma request; 0: enable left channel d */
        /* @SVD:field=LR_CHG,bitOffset=8,bitWidth=1,description="1:exchange storage location of left and right channel; 0: don't exchange storage location of left and right channel" */
        volatile uint32_t LR_CHG: 1;  /* 1:exchange storage location of left and right channel; 0: do */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_FIFO_CFG_Type;

/* @SVD:register=FIFO_ST,offset=0x0044,size=32,access=read-write,reset=0x0,description="FIFO_ST" */
/* FIFO_ST 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ALMOST_EMPTY_R,bitOffset=0,bitWidth=1,description="1 indicates right channel fifo is less than two datas left" */
        volatile uint32_t ALMOST_EMPTY_R: 1;  /* 1 indicates right channel fifo is less than two datas left */
        /* @SVD:field=ALMOST_FULL_R,bitOffset=1,bitWidth=1,description="1 indicates right channel fifo is less than two full" */
        volatile uint32_t ALMOST_FULL_R: 1;  /* 1 indicates right channel fifo is less than two full */
        /* @SVD:field=EMPTY_R,bitOffset=2,bitWidth=1,description="1 indicates right channel fifo is empty" */
        volatile uint32_t EMPTY_R: 1;  /* 1 indicates right channel fifo is empty */
        /* @SVD:field=FULL_R,bitOffset=3,bitWidth=1,description="1 indicates right channel fifo is full" */
        volatile uint32_t FULL_R: 1;  /* 1 indicates right channel fifo is full */
        /* @SVD:field=ALMOST_EMPTY_L,bitOffset=4,bitWidth=1,description="1 indicates left channel fifo is less than two datas left" */
        volatile uint32_t ALMOST_EMPTY_L: 1;  /* 1 indicates left channel fifo is less than two datas left */
        /* @SVD:field=ALMOST_FULL_L,bitOffset=5,bitWidth=1,description="1 indicates left channel fifo is less than two full" */
        volatile uint32_t ALMOST_FULL_L: 1;  /* 1 indicates left channel fifo is less than two full */
        /* @SVD:field=EMPTY_L,bitOffset=6,bitWidth=1,description="1 indicates left channel fifo is empty" */
        volatile uint32_t EMPTY_L: 1;  /* 1 indicates left channel fifo is empty */
        /* @SVD:field=FULL_L,bitOffset=7,bitWidth=1,description="1 indicates left channel fifo is full" */
        volatile uint32_t FULL_L: 1;  /* 1 indicates left channel fifo is full */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_FIFO_ST_Type;

/* @SVD:register=INT_ST,offset=0x0048,size=32,access=read-write,reset=0x0,description="INT_ST" */
/* INT_ST 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OVERFLOW_R,bitOffset=0,bitWidth=1,description="1 indicates right channel fifo has already overflowed  and as irq at same time" */
        volatile uint32_t OVERFLOW_R: 1;  /* 1 indicates right channel fifo has already overflowed  and a */
        /* @SVD:field=OVERFLOW_L,bitOffset=1,bitWidth=1,description="1 indicates left channel fifo has already overflowed and as irq at same time" */
        volatile uint32_t OVERFLOW_L: 1;  /* 1 indicates left channel fifo has already overflowed and as  */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_INT_ST_Type;

/* @SVD:register=INT_MSK,offset=0x004C,size=32,access=read-write,reset=0x0,description="INT_MSK" */
/* INT_MSK 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INT_MASK_R,bitOffset=0,bitWidth=1,description="1:disable right channel irq to system; 0: enable right channel irq to system" */
        volatile uint32_t INT_MASK_R: 1;  /* 1:disable right channel irq to system; 0: enable right chann */
        /* @SVD:field=INT_MASK_L,bitOffset=1,bitWidth=1,description="1:disable left channel irq to system; 0: enable left channel irq to system" */
        volatile uint32_t INT_MASK_L: 1;  /* 1:disable left channel irq to system; 0: enable left channel */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_INT_MSK_Type;

/* @SVD:register=INT_CLR,offset=0x0050,size=32,access=read-write,reset=0x0,description="INT_CLR" */
/* INT_CLR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INT_CLR_R,bitOffset=0,bitWidth=1,description="clear right channel irq" */
        volatile uint32_t INT_CLR_R: 1;  /* clear right channel irq */
        /* @SVD:field=INT_CLR_L,bitOffset=1,bitWidth=1,description="clear left channel irq" */
        volatile uint32_t INT_CLR_L: 1;  /* clear left channel irq */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PDM1_INT_CLR_Type;

/* PDM1 寄存器结构体 */
typedef struct
{
    PDM1_CFG0_Type CFG0;  /* 0x0000 - CFG0 */
    PDM1_CFG1_Type CFG1;  /* 0x0004 - CFG1 */
    PDM1_SINC_CFG_Type SINC_CFG;  /* 0x0008 - SINC_CFG */
    /* @SVD:register=RSVD3,offset=0x000C,size=32,access=read-write,reset=0x0,description="RSVD3" */
    volatile uint32_t RSVD3;  /* 0x000C - RSVD3 */
    volatile uint32_t RESERVED4[1];  /* 0x0010 - 0x0013 */
    PDM1_HPF_CFG_Type HPF_CFG;  /* 0x0014 - HPF_CFG */
    PDM1_PGA_CFG_Type PGA_CFG;  /* 0x0018 - PGA_CFG */
    /* @SVD:register=RSVD2,offset=0x001C,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x001C - RSVD2 */
    volatile uint32_t RESERVED8[5];  /* 0x0020 - 0x0033 */
    PDM1_LPF_CFG6_Type LPF_CFG6;  /* 0x0034 - LPF_CFG6 */
    PDM1_FIFO_CFG_Type FIFO_CFG;  /* 0x0038 - FIFO_CFG */
    /* @SVD:register=RSVD1,offset=0x003C,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x003C - RSVD1 */
    volatile uint32_t RESERVED16[1];  /* 0x0040 - 0x0043 */
    PDM1_FIFO_ST_Type FIFO_ST;  /* 0x0044 - FIFO_ST */
    PDM1_INT_ST_Type INT_ST;  /* 0x0048 - INT_ST */
    PDM1_INT_MSK_Type INT_MSK;  /* 0x004C - INT_MSK */
    PDM1_INT_CLR_Type INT_CLR;  /* 0x0050 - INT_CLR */
} PDM1_TypeDef;

/* ============================================================================ */
/* I2C1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=I2C1,base=0x5009C000,description="",groupName=I2C */

#define I2C1_BASE                                          (0x5009C000UL)

/* @SVD:register=CR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MODE,bitOffset=0,bitWidth=2,description="Bus Mode (Master operation):\n              2'b00: standard-mode\n              2'b01: fast-mode and fast-mode plus\n              2'b10: HS-mode (standard mode when not doing a high speed transfer)\n              2'b11: HS-mode (fast mode when not doing a high speed transfer)\n              Bus Mode (Slave operation):\n              2'b0x: HS-mode is disabled. I2C unit uses Standard/Fast mode timing on the SDA pin.\n              2'b1x: HS-mode is enabled. I2C unit uses HS-mode timing on the SDA pin when a master code is received." */
        volatile uint32_t MODE: 2;  /* Bus Mode (Master operation):               2'b00: standard-m */
        /* @SVD:field=IUE,bitOffset=2,bitWidth=1,description="I2C Unit Enable:\n              0 = Disables the unit and does not master any transactions or respond to any slave transactions.\n              1 = Enables the I2C (defaults to slave-receive mode).\n              Software must guarantee the I2C bus is idle before setting this bit." */
        volatile uint32_t IUE: 1;  /* I2C Unit Enable:               0 = Disables the unit and doe */
        /* @SVD:field=SCLE,bitOffset=3,bitWidth=1,description="SCL Enable:\n              0 = Disables the I2C from driving the SCL line.\n              1 = Enables the I2C clock output for master-mode operation." */
        volatile uint32_t SCLE: 1;  /* SCL Enable:               0 = Disables the I2C from driving  */
        /* @SVD:field=DMAEN,bitOffset=4,bitWidth=1,description="DMA Enable for both TX and RX\n              0 = DMA mode is NOT enabled\n              1 = DMA mode enabled" */
        volatile uint32_t DMAEN: 1;  /* DMA Enable for both TX and RX               0 = DMA mode is  */
        /* @SVD:field=LASTNACK,bitOffset=5,bitWidth=1,description="Generate NACK for last DMA Read transfer" */
        volatile uint32_t LASTNACK: 1;  /* Generate NACK for last DMA Read transfer */
        /* @SVD:field=LASTSTOP,bitOffset=6,bitWidth=1,description="Generate STOP for last DMA transfer" */
        volatile uint32_t LASTSTOP: 1;  /* Generate STOP for last DMA transfer */
        /* @SVD:field=RSVD3,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=MSDE,bitOffset=8,bitWidth=1,description="Master Stop Detected Enable:\n              0 = Master Stop Detect (MSD) status is not enabled.\n              1 = Master Stop Detect (MSD) status is enabled." */
        volatile uint32_t MSDE: 1;  /* Master Stop Detected Enable:               0 = Master Stop D */
        /* @SVD:field=SCLPP,bitOffset=9,bitWidth=1,description="Push-pull mode Enable for SCL.\n              0 = open drain output for SCL.\n              1 = Push-pull output for SCL" */
        volatile uint32_t SCLPP: 1;  /* Push-pull mode Enable for SCL.               0 = open drain  */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SLVEN,bitOffset=11,bitWidth=1,description="Slave mode Enable for SCL.\n              0 = Disable slave mode. Will not monitor slave address on I2C bus.\n              1 = Enable slave mode. Will monitor slave address on I2C bus." */
        volatile uint32_t SLVEN: 1;  /* Slave mode Enable for SCL.               0 = Disable slave m */
        /* @SVD:field=DNF,bitOffset=12,bitWidth=3,description="Digital noise filter\n              These bits are used to configure the digital noise filter on SDA and SCL input. The digital filter will filter spikes with a length of up to DNF*Tfclk.\n              0: Digital filter disabled \n              1: Digital filter enabled and filtering capability up to 1 Tfclk\n              ...\n              7: digital filter enabled and filtering capability up to 7 Tfclk\n              Digital filter is added to analog filter. Digital filter will introduce delay on SCL and SDA processing, which is essential in hs-mode." */
        volatile uint32_t DNF: 3;  /* Digital noise filter               These bits are used to co */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
        /* @SVD:field=BRGRST,bitOffset=29,bitWidth=1,description="Reset bus related state machine and signals. Will be cleared by HW automatically\n              1 = request for reset\n              0 = reset finished" */
        volatile uint32_t BRGRST: 1;  /* Reset bus related state machine and signals. Will be cleared */
        /* @SVD:field=RSTREQ,bitOffset=30,bitWidth=1,description="I2C will do bus reset upon this bit set. Will be cleared by HW automatically after RSTCYC cycles of SCL generated.\n              1 = request for i2c bus reset\n              0 = bus reset finished" */
        volatile uint32_t RSTREQ: 1;  /* I2C will do bus reset upon this bit set. Will be cleared by  */
        /* @SVD:field=UR,bitOffset=31,bitWidth=1,description="Unit Reset. Software need first assert to reset then deassert to release.\n              0 = No reset.\n              1 = Reset I2C module." */
        volatile uint32_t UR: 1;  /* Unit Reset. Software need first assert to reset then deasser */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_CR_Type;

/* @SVD:register=TCR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Transfer Control register" */
/* TCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TB,bitOffset=0,bitWidth=1,description="Transfer Byte:\n              Used to send or receive a byte on the I2C bus:\n              0 = Cleared by I2C when the byte is sent/received.\n              1 = Send/receive a byte. \n              CPU can monitor this bit to determine when the byte transfer has completed. In master or slave mode, after each byte transfer including acknowledge pulse, the I2C holds the SCL line low (inserting wait states) until TB is set." */
        volatile uint32_t TB: 1;  /* Transfer Byte:               Used to send or receive a byte  */
        /* @SVD:field=START,bitOffset=1,bitWidth=1,description="Start:\n              Used to initiate a Start condition to the I2C unit when in master mode. \n              0 = Do not send a Start pulse.\n              1 = Send a Start pulse." */
        volatile uint32_t START: 1;  /* Start:               Used to initiate a Start condition to t */
        /* @SVD:field=STOP,bitOffset=2,bitWidth=1,description="Stop:\n              Used to initiate a Stop condition after transferring the next data byte on the I2C bus when in master mode. In master-receive mode, the NACK control bit must be set in conjunction with the STOP bit. \n              0 = Do not send a Stop.\n              1 = Send a Stop." */
        volatile uint32_t STOP: 1;  /* Stop:               Used to initiate a Stop condition after  */
        /* @SVD:field=NACK,bitOffset=3,bitWidth=1,description="The positive/negative acknowledge control bit, defines the type of acknowledge pulse sent by the I2C when in master receive mode:\n              0 = Send a positive acknowledge (ACK) pulse after receiving a data byte.\n              1 = Send a negative acknowledge (NACK) pulse after receiving a data byte.\n              The I2C automatically sends an ACK pulse when responding to its slave address or when responding in slave-receive mode, regardless of the NACK control-bit setting." */
        volatile uint32_t NACK: 1;  /* The positive/negative acknowledge control bit, defines the t */
        /* @SVD:field=MA,bitOffset=4,bitWidth=1,description="Master Abort:\n              Used by the I2C in master mode to generate a Stop without transmitting another data byte:\n              0 = The I2C transmits Stop on if TCR[STOP] is set.\n              1 = The I2C sends Stop without data transmission.\n              When in master-transmit mode, after transmitting a data byte, the TCR[TB] bit is cleared. When no more data bytes need to be sent, setting master abort bit sends the Stop. The TCR[TB] bit must remain clear.\n              In master-receive mode, when a NAK is sent without a Stop (TCR[STOP] bit was not set) and CPU does not send a repeated Start, setting this bit sends the Stop. Once again, the TCR[TB] bit must remain clear. Master Abort can be done immediately after the address phase (Master Transmit mode only)." */
        volatile uint32_t MA: 1;  /* Master Abort:               Used by the I2C in master mode t */
        /* @SVD:field=TXREQ,bitOffset=5,bitWidth=1,description="Request DMA TX. Will be cleared by HW automatically" */
        volatile uint32_t TXREQ: 1;  /* Request DMA TX. Will be cleared by HW automatically */
        /* @SVD:field=RXREQ,bitOffset=6,bitWidth=1,description="Request DMA RX. Will be cleared by HW automatically" */
        volatile uint32_t RXREQ: 1;  /* Request DMA RX. Will be cleared by HW automatically */
        /* @SVD:field=ABORTDMA,bitOffset=7,bitWidth=1,description="Abort DMA operation. Will be cleared by HW automatically" */
        volatile uint32_t ABORTDMA: 1;  /* Abort DMA operation. Will be cleared by HW automatically */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_TCR_Type;

/* @SVD:register=IER,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Interrupt Enable register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD4: 4;
        /* @SVD:field=SSDIE,bitOffset=4,bitWidth=1,description="Slave Stop Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt when it detects a Stop condition while in slave mode." */
        volatile uint32_t SSDIE: 1;  /* Slave Stop Detected Interrupt Enable:               0 = Disa */
        /* @SVD:field=ALDIE,bitOffset=5,bitWidth=1,description="Arbitration Loss Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt upon losing arbitration while in master mode." */
        volatile uint32_t ALDIE: 1;  /* Arbitration Loss Detected Interrupt Enable:               0  */
        /* @SVD:field=TEIE,bitOffset=6,bitWidth=1,description="DBR Transmit Empty Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt after transmitting a byte onto the I2C bus." */
        volatile uint32_t TEIE: 1;  /* DBR Transmit Empty Interrupt Enable:               0 = Disab */
        /* @SVD:field=RFIE,bitOffset=7,bitWidth=1,description="DBR Receive Full Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt when the DBR has received a data byte from the I2C bus." */
        volatile uint32_t RFIE: 1;  /* DBR Receive Full Interrupt Enable:               0 = Disable */
        /* @SVD:field=RSVD3,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=SADIE,bitOffset=9,bitWidth=1,description="Slave Address Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt upon detecting a slave address match or a general call address." */
        volatile uint32_t SADIE: 1;  /* Slave Address Detected Interrupt Enable:               0 = D */
        /* @SVD:field=BEDIE,bitOffset=10,bitWidth=1,description="Bus Error Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt for the following I2C bus errors:\n              As a master transmitter, no ACK was detected after a byte was sent.\n              As a slave receiver, the I2C generated a NACK pulse.\n              Software is responsible for guaranteeing that misplaced Start and Stop conditions do not occur." */
        volatile uint32_t BEDIE: 1;  /* Bus Error Detected Interrupt Enable:               0 = Disab */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=MSDIE,bitOffset=12,bitWidth=1,description="Master Stop Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C unit to interrupt upon detecting a Master Stop sent by the I2C unit." */
        volatile uint32_t MSDIE: 1;  /* Master Stop Detected Interrupt Enable:               0 = Dis */
        /* @SVD:field=DMADONEIE,bitOffset=13,bitWidth=1,description="DMA Transaction Done Interrupt Enable\n              0 = DMA Transaction done interrupt is not enabled.\n              1 = DMA Transaction done interrupt is enabled." */
        volatile uint32_t DMADONEIE: 1;  /* DMA Transaction Done Interrupt Enable               0 = DMA  */
        /* @SVD:field=OFIE,bitOffset=14,bitWidth=1,description="FIFO Overflow Interrupt Enable\n              0 = FIFO Overflow interrupt is not enabled\n              1 = FIFO Overflow interrupt is enabled" */
        volatile uint32_t OFIE: 1;  /* FIFO Overflow Interrupt Enable               0 = FIFO Overfl */
        /* @SVD:field=UFIE,bitOffset=15,bitWidth=1,description="FIFO Underflow Interrupt Enable\n              0 = FIFO Underflow interrupt is not enabled\n              1 = FIFO Underflow interrupt is enabled" */
        volatile uint32_t UFIE: 1;  /* FIFO Underflow Interrupt Enable               0 = FIFO Under */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_IER_Type;

/* @SVD:register=SR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWM,bitOffset=0,bitWidth=1,description="Read/write Mode:\n              0 = The I2C is in master-transmit or slave-receive mode. \n              1 = The I2C is in master-receive or slave-transmit mode. \n              This is the R/nW bit of the slave address. It is cleared automatically by hardware after a Stop state." */
        volatile uint32_t RWM: 1;  /* Read/write Mode:               0 = The I2C is in master-tran */
        /* @SVD:field=NACK,bitOffset=1,bitWidth=1,description="ACK/NACK Status:\n              0 = The I2C received or sent an ACK on the bus. \n              1 = The I2C received or sent a NACK.on the bus.\n              This bit is used in slave-transmit mode to determine when the byte transferred is the last one. This bit is updated after each byte and ACK/NACK information is received." */
        volatile uint32_t NACK: 1;  /* ACK/NACK Status:               0 = The I2C received or sent  */
        /* @SVD:field=UB,bitOffset=2,bitWidth=1,description="Unit Busy:\n              0 = I2C not busy.\n              1 = Set when local I2C is busy. This is defined as the time between the first Start and Stop." */
        volatile uint32_t UB: 1;  /* Unit Busy:               0 = I2C not busy.               1 = */
        /* @SVD:field=IBB,bitOffset=3,bitWidth=1,description="I2C Bus Busy:\n              0 = I2C bus is idle or the I2C is using the bus (that is, unit busy).\n              1 = Set when the I2C bus is busy but local I2C is not involved in the transaction." */
        volatile uint32_t IBB: 1;  /* I2C Bus Busy:               0 = I2C bus is idle or the I2C i */
        /* @SVD:field=SSD,bitOffset=4,bitWidth=1,description="Slave Stop Detected:\n              0 = No Stop detected.\n              1 = Set when the I2C detects a Stop while in slave-receive or slave-transmit mode. \n              Cleared if write 1" */
        volatile uint32_t SSD: 1;  /* Slave Stop Detected:               0 = No Stop detected.     */
        /* @SVD:field=ALD,bitOffset=5,bitWidth=1,description="Arbitration Loss Detected:\n              Used during multi-master operation:\n              0 = Cleared when arbitration is won or never took place.\n              1 = Set when the I2C loses arbitration. \n              Cleared if write 1" */
        volatile uint32_t ALD: 1;  /* Arbitration Loss Detected:               Used during multi-m */
        /* @SVD:field=TE,bitOffset=6,bitWidth=1,description="DBR Transmit Empty:\n              0 = The data byte is still being transmitted.\n              1 = The I2C has finished transmitting a data byte on the I2C bus. An interrupt is signalled when enabled in the CR.\n              Cleared if write 1" */
        volatile uint32_t TE: 1;  /* DBR Transmit Empty:               0 = The data byte is still */
        /* @SVD:field=RF,bitOffset=7,bitWidth=1,description="DBR Receive Full:\n              0 = The DBR has not received a new data byte or the I2C is idle.\n              1 = The DBR register received a new data byte from the I2C bus. An interrupt is signalled when enabled in the CR. \n              Cleared if write 1" */
        volatile uint32_t RF: 1;  /* DBR Receive Full:               0 = The DBR has not received */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SAD,bitOffset=9,bitWidth=1,description="Slave Address Detected:\n              0 = No slave address was detected.\n              1 = The I2C detected a seven-bit address that matches the general call address or SAR. An interrupt is signalled when enabled in the CR. \n              Cleared if write 1" */
        volatile uint32_t SAD: 1;  /* Slave Address Detected:               0 = No slave address w */
        /* @SVD:field=BED,bitOffset=10,bitWidth=1,description="Bus Error Detected:\n              0 = No error detected.\n              1 = The I2C sets this bit when it detects one of the following error conditions:\n              As a master transmitter, no ACK was detected on the interface after a byte was sent.\n              As a slave receiver, the I2C generates a NACK pulse.\n              When an error occurs, I2C bus transactions continue. Software must guarantee that misplaced Start and Stop conditions do not occur. \n              Cleared if write 1" */
        volatile uint32_t BED: 1;  /* Bus Error Detected:               0 = No error detected.     */
        /* @SVD:field=EBB,bitOffset=11,bitWidth=1,description="Early Bus Busy\n              0 = I2C bus is idle or the I2C is using the bus (that is, unit busy).\n              1 = Set when the unit detects that the SCL or SDA line is low without a START condition. Bit will remain set until the I2C unit detects the bus is idle by detecting a STOP condition. Bit will also be set whenever the IBB bit is set." */
        volatile uint32_t EBB: 1;  /* Early Bus Busy               0 = I2C bus is idle or the I2C  */
        /* @SVD:field=MSD,bitOffset=12,bitWidth=1,description="Master Stop Detected:\n              0 = No Master Stop Detected.\n              1 = This bit is set by the I2C unit when all of the following are true:\n              This bit is enabled (CR[MSDE] = 1);\n              I2C unit is configured as a master;\n              I2C transmits a STOP signal" */
        volatile uint32_t MSD: 1;  /* Master Stop Detected:               0 = No Master Stop Detec */
        /* @SVD:field=DMADONE,bitOffset=13,bitWidth=1,description="DMA Transaction Done. Asserted when both APB and I2C bus have finished transfer. Cleared if write 1" */
        volatile uint32_t DMADONE: 1;  /* DMA Transaction Done. Asserted when both APB and I2C bus hav */
        /* @SVD:field=OF,bitOffset=14,bitWidth=1,description="FIFO Overflow Flag. Asserted when FIFO is full and a PUSH request generated without a POP. Cleared if write 1" */
        volatile uint32_t OF: 1;  /* FIFO Overflow Flag. Asserted when FIFO is full and a PUSH re */
        /* @SVD:field=UF,bitOffset=15,bitWidth=1,description="FIFO Underflow Flag. Asserted when FIFO is empty and a POP request generated without a PUSH. Cleared if write 1" */
        volatile uint32_t UF: 1;  /* FIFO Underflow Flag. Asserted when FIFO is empty and a POP r */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_SR_Type;

/* @SVD:register=DBR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="Data Buffer register" */
/* DBR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=8,description="use the I2C Data Buffer register to transmit and receive data from the I2C bus. The DBR is accessed by software on one Side and by the I2C Shift register on the other. The DBR receives data coming into the I2C unit after a full byte is received and acknowledged. CPU writes data going out of the I2C to the DBR and sends it to the serial bus. \n              When the I2C is in transmit mode (master or slave), CPU writes data to the DBR over the internal bus. CPU write data to the DBR when a master transaction is initiated or when the DBR transmit-empty interrupt is signalled. Data moves from the DBR to the Shift register when the transfer byte bit is set. The DBR transmit-empty interrupt is signalled (if enabled) when a byte is transferred on the I2C bus and the acknowledge cycle is complete. If the DBR is not written, and a Stop condition is not in place before the I2C bus is ready to transfer the next byte packet, the I2C unit inserts wait states until CPU writes the DBR and sets the transfer byte bit.\n              When the I2C is in receive mode (master or slave), CPU reads DBR data over the internal bus. CPU reads data from the DBR when the DBR receive-full interrupt is signalled. The data moves from the Shift register to the DBR when the acknowledge cycle is complete. The I2C inserts wait states until the DBR is read. After the software reads the DBR, CR[NACK] are written by the software, allowing the next byte transfer to proceed to the I2C bus.\n              In DMA mode, DBR is automatically filled from FIFO in master transmit mode, or fetched and stored in FIFO in master receive mode until DMA done or aborted." */
        volatile uint32_t DATA: 8;  /* use the I2C Data Buffer register to transmit and receive dat */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_DBR_Type;

/* @SVD:register=SAR,offset=0x0014,size=32,access=read-write,reset=0x00000047,description="Slave Address Register" */
/* SAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=7,description="The seven-bit address to which the I2C responds when in slave-receive mode" */
        volatile uint32_t ADDR: 7;  /* The seven-bit address to which the I2C responds when in slav */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_SAR_Type;

/* @SVD:register=LCR,offset=0x0018,size=32,access=read-write,reset=0x081C72ED,description="Load Count Register" */
/* LCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLV,bitOffset=0,bitWidth=9,description="Decrementer Load value for Standard Mode SCL (master mode) for both high  and  low phase. \n              Data rate is generated as Ffclk/(SLV+max(SLV,CNT*2+6)+7+DNF) approximately. \n              100kbps data rate is generated by default if fclk is 48MHz.\n              SLV also controls setup time and hold time for START and STOP condition in Standard Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*SLV" */
        volatile uint32_t SLV: 9;  /* Decrementer Load value for Standard Mode SCL (master mode) f */
        /* @SVD:field=FLV,bitOffset=9,bitWidth=9,description="Decrementer Load value for Fast Mode (or Fast Mode Plus) SCL (master mode) for both high and low phase. \n              Data rate is generated as Ffclk/(FLV+max(FLV,CNT*2+6)+7+DNF) approximately. \n              400kbps data rate is generated by default if fclk is 48MHz.\n              FLV also controls setup time and hold time for START and STOP condition in Fast Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*FLV" */
        volatile uint32_t FLV: 9;  /* Decrementer Load value for Fast Mode (or Fast Mode Plus) SCL */
        /* @SVD:field=HLVL,bitOffset=18,bitWidth=9,description="Decrementer Load value for High Speed Mode SCL (master mode) for low phase. \n              Tlow=Tfclk*(HLVL+3+DNF). Data rate is generated as 1/(Thigh+Tlow), or Ffclk/(HLVH+HLVL+7+2*DNF). \n              3.2Mbps data rate is generated by default if fclk is 48MHz.\n              HLVL also controls setup time and hold time for START and STOP condition in High Speed Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*(HLVL+1)" */
        volatile uint32_t HLVL: 9;  /* Decrementer Load value for High Speed Mode SCL (master mode) */
        /* @SVD:field=HLVH,bitOffset=27,bitWidth=5,description="Decrementer Load value for High Speed Mode SCL (master mode) for high phase. \n              Thigh=Tfclk*(HLVH+4+DNF)" */
        volatile uint32_t HLVH: 5;  /* Decrementer Load value for High Speed Mode SCL (master mode) */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_LCR_Type;

/* @SVD:register=WCR,offset=0x001C,size=32,access=read-write,reset=0x0000000A,description="Wait Count Register" */
/* WCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=8,description="Controls the counter values defining the setup and hold times in standard and fast mode\n              Tvddat=Thddat=Tfclk*(CNT+2)\n              Tsudat=max(Tlow-Thddat,Thddat)\n              Lower counter values may violate setup and hold times." */
        volatile uint32_t CNT: 8;  /* Controls the counter values defining the setup and hold time */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_WCR_Type;

/* @SVD:register=RCCR,offset=0x0020,size=32,access=read-write,reset=0x00000009,description="Bus Reset Cycle Counter Register" */
/* RCCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSTCYC,bitOffset=0,bitWidth=4,description="The cycles of SCL during bus reset" */
        volatile uint32_t RSTCYC: 4;  /* The cycles of SCL during bus reset */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_RCCR_Type;

/* @SVD:register=BMR,offset=0x0024,size=32,access=read-write,reset=0x00000003,description="Bus Monitor Register" */
/* BMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SDA,bitOffset=0,bitWidth=1,description="value of the SDA pin." */
        volatile uint32_t SDA: 1;  /* value of the SDA pin. */
        /* @SVD:field=SCL,bitOffset=1,bitWidth=1,description="value of the SCL pin. Software can check bus level when the I2C bus is hung and the I2C unit must be reset." */
        volatile uint32_t SCL: 1;  /* value of the SCL pin. Software can check bus level when the  */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_BMR_Type;

/* @SVD:register=DNR,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="DMA number register" */
/* DNR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=9,description="Write as number of data to transfer in byte. Read as left data number to transfer" */
        volatile uint32_t NDT: 9;  /* Write as number of data to transfer in byte. Read as left da */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_DNR_Type;

/* @SVD:register=FIFO,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="FIFO Register" */
/* FIFO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=8,description="Write to push send data into FIFO. Read to pop received data from FIFO" */
        volatile uint32_t DATA: 8;  /* Write to push send data into FIFO. Read to pop received data */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C1_FIFO_Type;

/* I2C1 寄存器结构体 */
typedef struct
{
    I2C1_CR_Type CR;  /* 0x0000 - Control register */
    I2C1_TCR_Type TCR;  /* 0x0004 - Transfer Control register */
    I2C1_IER_Type IER;  /* 0x0008 - Interrupt Enable register */
    I2C1_SR_Type SR;  /* 0x000C - Status register */
    I2C1_DBR_Type DBR;  /* 0x0010 - Data Buffer register */
    I2C1_SAR_Type SAR;  /* 0x0014 - Slave Address Register */
    I2C1_LCR_Type LCR;  /* 0x0018 - Load Count Register */
    I2C1_WCR_Type WCR;  /* 0x001C - Wait Count Register */
    I2C1_RCCR_Type RCCR;  /* 0x0020 - Bus Reset Cycle Counter Register */
    I2C1_BMR_Type BMR;  /* 0x0024 - Bus Monitor Register */
    I2C1_DNR_Type DNR;  /* 0x0028 - DMA number register */
    /* @SVD:register=RSVD1,offset=0x002C,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x002C - RSVD1 */
    I2C1_FIFO_Type FIFO;  /* 0x0030 - FIFO Register */
} I2C1_TypeDef;

/* ============================================================================ */
/* I2C2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=I2C2,base=0x5009D000,description="",groupName=I2C */

#define I2C2_BASE                                          (0x5009D000UL)

/* @SVD:register=CR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MODE,bitOffset=0,bitWidth=2,description="Bus Mode (Master operation):\n              2'b00: standard-mode\n              2'b01: fast-mode and fast-mode plus\n              2'b10: HS-mode (standard mode when not doing a high speed transfer)\n              2'b11: HS-mode (fast mode when not doing a high speed transfer)\n              Bus Mode (Slave operation):\n              2'b0x: HS-mode is disabled. I2C unit uses Standard/Fast mode timing on the SDA pin.\n              2'b1x: HS-mode is enabled. I2C unit uses HS-mode timing on the SDA pin when a master code is received." */
        volatile uint32_t MODE: 2;  /* Bus Mode (Master operation):               2'b00: standard-m */
        /* @SVD:field=IUE,bitOffset=2,bitWidth=1,description="I2C Unit Enable:\n              0 = Disables the unit and does not master any transactions or respond to any slave transactions.\n              1 = Enables the I2C (defaults to slave-receive mode).\n              Software must guarantee the I2C bus is idle before setting this bit." */
        volatile uint32_t IUE: 1;  /* I2C Unit Enable:               0 = Disables the unit and doe */
        /* @SVD:field=SCLE,bitOffset=3,bitWidth=1,description="SCL Enable:\n              0 = Disables the I2C from driving the SCL line.\n              1 = Enables the I2C clock output for master-mode operation." */
        volatile uint32_t SCLE: 1;  /* SCL Enable:               0 = Disables the I2C from driving  */
        /* @SVD:field=DMAEN,bitOffset=4,bitWidth=1,description="DMA Enable for both TX and RX\n              0 = DMA mode is NOT enabled\n              1 = DMA mode enabled" */
        volatile uint32_t DMAEN: 1;  /* DMA Enable for both TX and RX               0 = DMA mode is  */
        /* @SVD:field=LASTNACK,bitOffset=5,bitWidth=1,description="Generate NACK for last DMA Read transfer" */
        volatile uint32_t LASTNACK: 1;  /* Generate NACK for last DMA Read transfer */
        /* @SVD:field=LASTSTOP,bitOffset=6,bitWidth=1,description="Generate STOP for last DMA transfer" */
        volatile uint32_t LASTSTOP: 1;  /* Generate STOP for last DMA transfer */
        /* @SVD:field=RSVD3,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=MSDE,bitOffset=8,bitWidth=1,description="Master Stop Detected Enable:\n              0 = Master Stop Detect (MSD) status is not enabled.\n              1 = Master Stop Detect (MSD) status is enabled." */
        volatile uint32_t MSDE: 1;  /* Master Stop Detected Enable:               0 = Master Stop D */
        /* @SVD:field=SCLPP,bitOffset=9,bitWidth=1,description="Push-pull mode Enable for SCL.\n              0 = open drain output for SCL.\n              1 = Push-pull output for SCL" */
        volatile uint32_t SCLPP: 1;  /* Push-pull mode Enable for SCL.               0 = open drain  */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SLVEN,bitOffset=11,bitWidth=1,description="Slave mode Enable for SCL.\n              0 = Disable slave mode. Will not monitor slave address on I2C bus.\n              1 = Enable slave mode. Will monitor slave address on I2C bus." */
        volatile uint32_t SLVEN: 1;  /* Slave mode Enable for SCL.               0 = Disable slave m */
        /* @SVD:field=DNF,bitOffset=12,bitWidth=3,description="Digital noise filter\n              These bits are used to configure the digital noise filter on SDA and SCL input. The digital filter will filter spikes with a length of up to DNF*Tfclk.\n              0: Digital filter disabled \n              1: Digital filter enabled and filtering capability up to 1 Tfclk\n              ...\n              7: digital filter enabled and filtering capability up to 7 Tfclk\n              Digital filter is added to analog filter. Digital filter will introduce delay on SCL and SDA processing, which is essential in hs-mode." */
        volatile uint32_t DNF: 3;  /* Digital noise filter               These bits are used to co */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
        /* @SVD:field=BRGRST,bitOffset=29,bitWidth=1,description="Reset bus related state machine and signals. Will be cleared by HW automatically\n              1 = request for reset\n              0 = reset finished" */
        volatile uint32_t BRGRST: 1;  /* Reset bus related state machine and signals. Will be cleared */
        /* @SVD:field=RSTREQ,bitOffset=30,bitWidth=1,description="I2C will do bus reset upon this bit set. Will be cleared by HW automatically after RSTCYC cycles of SCL generated.\n              1 = request for i2c bus reset\n              0 = bus reset finished" */
        volatile uint32_t RSTREQ: 1;  /* I2C will do bus reset upon this bit set. Will be cleared by  */
        /* @SVD:field=UR,bitOffset=31,bitWidth=1,description="Unit Reset. Software need first assert to reset then deassert to release.\n              0 = No reset.\n              1 = Reset I2C module." */
        volatile uint32_t UR: 1;  /* Unit Reset. Software need first assert to reset then deasser */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_CR_Type;

/* @SVD:register=TCR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Transfer Control register" */
/* TCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TB,bitOffset=0,bitWidth=1,description="Transfer Byte:\n              Used to send or receive a byte on the I2C bus:\n              0 = Cleared by I2C when the byte is sent/received.\n              1 = Send/receive a byte. \n              CPU can monitor this bit to determine when the byte transfer has completed. In master or slave mode, after each byte transfer including acknowledge pulse, the I2C holds the SCL line low (inserting wait states) until TB is set." */
        volatile uint32_t TB: 1;  /* Transfer Byte:               Used to send or receive a byte  */
        /* @SVD:field=START,bitOffset=1,bitWidth=1,description="Start:\n              Used to initiate a Start condition to the I2C unit when in master mode. \n              0 = Do not send a Start pulse.\n              1 = Send a Start pulse." */
        volatile uint32_t START: 1;  /* Start:               Used to initiate a Start condition to t */
        /* @SVD:field=STOP,bitOffset=2,bitWidth=1,description="Stop:\n              Used to initiate a Stop condition after transferring the next data byte on the I2C bus when in master mode. In master-receive mode, the NACK control bit must be set in conjunction with the STOP bit. \n              0 = Do not send a Stop.\n              1 = Send a Stop." */
        volatile uint32_t STOP: 1;  /* Stop:               Used to initiate a Stop condition after  */
        /* @SVD:field=NACK,bitOffset=3,bitWidth=1,description="The positive/negative acknowledge control bit, defines the type of acknowledge pulse sent by the I2C when in master receive mode:\n              0 = Send a positive acknowledge (ACK) pulse after receiving a data byte.\n              1 = Send a negative acknowledge (NACK) pulse after receiving a data byte.\n              The I2C automatically sends an ACK pulse when responding to its slave address or when responding in slave-receive mode, regardless of the NACK control-bit setting." */
        volatile uint32_t NACK: 1;  /* The positive/negative acknowledge control bit, defines the t */
        /* @SVD:field=MA,bitOffset=4,bitWidth=1,description="Master Abort:\n              Used by the I2C in master mode to generate a Stop without transmitting another data byte:\n              0 = The I2C transmits Stop on if TCR[STOP] is set.\n              1 = The I2C sends Stop without data transmission.\n              When in master-transmit mode, after transmitting a data byte, the TCR[TB] bit is cleared. When no more data bytes need to be sent, setting master abort bit sends the Stop. The TCR[TB] bit must remain clear.\n              In master-receive mode, when a NAK is sent without a Stop (TCR[STOP] bit was not set) and CPU does not send a repeated Start, setting this bit sends the Stop. Once again, the TCR[TB] bit must remain clear. Master Abort can be done immediately after the address phase (Master Transmit mode only)." */
        volatile uint32_t MA: 1;  /* Master Abort:               Used by the I2C in master mode t */
        /* @SVD:field=TXREQ,bitOffset=5,bitWidth=1,description="Request DMA TX. Will be cleared by HW automatically" */
        volatile uint32_t TXREQ: 1;  /* Request DMA TX. Will be cleared by HW automatically */
        /* @SVD:field=RXREQ,bitOffset=6,bitWidth=1,description="Request DMA RX. Will be cleared by HW automatically" */
        volatile uint32_t RXREQ: 1;  /* Request DMA RX. Will be cleared by HW automatically */
        /* @SVD:field=ABORTDMA,bitOffset=7,bitWidth=1,description="Abort DMA operation. Will be cleared by HW automatically" */
        volatile uint32_t ABORTDMA: 1;  /* Abort DMA operation. Will be cleared by HW automatically */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_TCR_Type;

/* @SVD:register=IER,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Interrupt Enable register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD4: 4;
        /* @SVD:field=SSDIE,bitOffset=4,bitWidth=1,description="Slave Stop Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt when it detects a Stop condition while in slave mode." */
        volatile uint32_t SSDIE: 1;  /* Slave Stop Detected Interrupt Enable:               0 = Disa */
        /* @SVD:field=ALDIE,bitOffset=5,bitWidth=1,description="Arbitration Loss Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt upon losing arbitration while in master mode." */
        volatile uint32_t ALDIE: 1;  /* Arbitration Loss Detected Interrupt Enable:               0  */
        /* @SVD:field=TEIE,bitOffset=6,bitWidth=1,description="DBR Transmit Empty Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt after transmitting a byte onto the I2C bus." */
        volatile uint32_t TEIE: 1;  /* DBR Transmit Empty Interrupt Enable:               0 = Disab */
        /* @SVD:field=RFIE,bitOffset=7,bitWidth=1,description="DBR Receive Full Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt when the DBR has received a data byte from the I2C bus." */
        volatile uint32_t RFIE: 1;  /* DBR Receive Full Interrupt Enable:               0 = Disable */
        /* @SVD:field=RSVD3,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=SADIE,bitOffset=9,bitWidth=1,description="Slave Address Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt upon detecting a slave address match or a general call address." */
        volatile uint32_t SADIE: 1;  /* Slave Address Detected Interrupt Enable:               0 = D */
        /* @SVD:field=BEDIE,bitOffset=10,bitWidth=1,description="Bus Error Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt for the following I2C bus errors:\n              As a master transmitter, no ACK was detected after a byte was sent.\n              As a slave receiver, the I2C generated a NACK pulse.\n              Software is responsible for guaranteeing that misplaced Start and Stop conditions do not occur." */
        volatile uint32_t BEDIE: 1;  /* Bus Error Detected Interrupt Enable:               0 = Disab */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=MSDIE,bitOffset=12,bitWidth=1,description="Master Stop Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C unit to interrupt upon detecting a Master Stop sent by the I2C unit." */
        volatile uint32_t MSDIE: 1;  /* Master Stop Detected Interrupt Enable:               0 = Dis */
        /* @SVD:field=DMADONEIE,bitOffset=13,bitWidth=1,description="DMA Transaction Done Interrupt Enable\n              0 = DMA Transaction done interrupt is not enabled.\n              1 = DMA Transaction done interrupt is enabled." */
        volatile uint32_t DMADONEIE: 1;  /* DMA Transaction Done Interrupt Enable               0 = DMA  */
        /* @SVD:field=OFIE,bitOffset=14,bitWidth=1,description="FIFO Overflow Interrupt Enable\n              0 = FIFO Overflow interrupt is not enabled\n              1 = FIFO Overflow interrupt is enabled" */
        volatile uint32_t OFIE: 1;  /* FIFO Overflow Interrupt Enable               0 = FIFO Overfl */
        /* @SVD:field=UFIE,bitOffset=15,bitWidth=1,description="FIFO Underflow Interrupt Enable\n              0 = FIFO Underflow interrupt is not enabled\n              1 = FIFO Underflow interrupt is enabled" */
        volatile uint32_t UFIE: 1;  /* FIFO Underflow Interrupt Enable               0 = FIFO Under */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_IER_Type;

/* @SVD:register=SR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWM,bitOffset=0,bitWidth=1,description="Read/write Mode:\n              0 = The I2C is in master-transmit or slave-receive mode. \n              1 = The I2C is in master-receive or slave-transmit mode. \n              This is the R/nW bit of the slave address. It is cleared automatically by hardware after a Stop state." */
        volatile uint32_t RWM: 1;  /* Read/write Mode:               0 = The I2C is in master-tran */
        /* @SVD:field=NACK,bitOffset=1,bitWidth=1,description="ACK/NACK Status:\n              0 = The I2C received or sent an ACK on the bus. \n              1 = The I2C received or sent a NACK.on the bus.\n              This bit is used in slave-transmit mode to determine when the byte transferred is the last one. This bit is updated after each byte and ACK/NACK information is received." */
        volatile uint32_t NACK: 1;  /* ACK/NACK Status:               0 = The I2C received or sent  */
        /* @SVD:field=UB,bitOffset=2,bitWidth=1,description="Unit Busy:\n              0 = I2C not busy.\n              1 = Set when local I2C is busy. This is defined as the time between the first Start and Stop." */
        volatile uint32_t UB: 1;  /* Unit Busy:               0 = I2C not busy.               1 = */
        /* @SVD:field=IBB,bitOffset=3,bitWidth=1,description="I2C Bus Busy:\n              0 = I2C bus is idle or the I2C is using the bus (that is, unit busy).\n              1 = Set when the I2C bus is busy but local I2C is not involved in the transaction." */
        volatile uint32_t IBB: 1;  /* I2C Bus Busy:               0 = I2C bus is idle or the I2C i */
        /* @SVD:field=SSD,bitOffset=4,bitWidth=1,description="Slave Stop Detected:\n              0 = No Stop detected.\n              1 = Set when the I2C detects a Stop while in slave-receive or slave-transmit mode. \n              Cleared if write 1" */
        volatile uint32_t SSD: 1;  /* Slave Stop Detected:               0 = No Stop detected.     */
        /* @SVD:field=ALD,bitOffset=5,bitWidth=1,description="Arbitration Loss Detected:\n              Used during multi-master operation:\n              0 = Cleared when arbitration is won or never took place.\n              1 = Set when the I2C loses arbitration. \n              Cleared if write 1" */
        volatile uint32_t ALD: 1;  /* Arbitration Loss Detected:               Used during multi-m */
        /* @SVD:field=TE,bitOffset=6,bitWidth=1,description="DBR Transmit Empty:\n              0 = The data byte is still being transmitted.\n              1 = The I2C has finished transmitting a data byte on the I2C bus. An interrupt is signalled when enabled in the CR.\n              Cleared if write 1" */
        volatile uint32_t TE: 1;  /* DBR Transmit Empty:               0 = The data byte is still */
        /* @SVD:field=RF,bitOffset=7,bitWidth=1,description="DBR Receive Full:\n              0 = The DBR has not received a new data byte or the I2C is idle.\n              1 = The DBR register received a new data byte from the I2C bus. An interrupt is signalled when enabled in the CR. \n              Cleared if write 1" */
        volatile uint32_t RF: 1;  /* DBR Receive Full:               0 = The DBR has not received */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SAD,bitOffset=9,bitWidth=1,description="Slave Address Detected:\n              0 = No slave address was detected.\n              1 = The I2C detected a seven-bit address that matches the general call address or SAR. An interrupt is signalled when enabled in the CR. \n              Cleared if write 1" */
        volatile uint32_t SAD: 1;  /* Slave Address Detected:               0 = No slave address w */
        /* @SVD:field=BED,bitOffset=10,bitWidth=1,description="Bus Error Detected:\n              0 = No error detected.\n              1 = The I2C sets this bit when it detects one of the following error conditions:\n              As a master transmitter, no ACK was detected on the interface after a byte was sent.\n              As a slave receiver, the I2C generates a NACK pulse.\n              When an error occurs, I2C bus transactions continue. Software must guarantee that misplaced Start and Stop conditions do not occur. \n              Cleared if write 1" */
        volatile uint32_t BED: 1;  /* Bus Error Detected:               0 = No error detected.     */
        /* @SVD:field=EBB,bitOffset=11,bitWidth=1,description="Early Bus Busy\n              0 = I2C bus is idle or the I2C is using the bus (that is, unit busy).\n              1 = Set when the unit detects that the SCL or SDA line is low without a START condition. Bit will remain set until the I2C unit detects the bus is idle by detecting a STOP condition. Bit will also be set whenever the IBB bit is set." */
        volatile uint32_t EBB: 1;  /* Early Bus Busy               0 = I2C bus is idle or the I2C  */
        /* @SVD:field=MSD,bitOffset=12,bitWidth=1,description="Master Stop Detected:\n              0 = No Master Stop Detected.\n              1 = This bit is set by the I2C unit when all of the following are true:\n              This bit is enabled (CR[MSDE] = 1);\n              I2C unit is configured as a master;\n              I2C transmits a STOP signal" */
        volatile uint32_t MSD: 1;  /* Master Stop Detected:               0 = No Master Stop Detec */
        /* @SVD:field=DMADONE,bitOffset=13,bitWidth=1,description="DMA Transaction Done. Asserted when both APB and I2C bus have finished transfer. Cleared if write 1" */
        volatile uint32_t DMADONE: 1;  /* DMA Transaction Done. Asserted when both APB and I2C bus hav */
        /* @SVD:field=OF,bitOffset=14,bitWidth=1,description="FIFO Overflow Flag. Asserted when FIFO is full and a PUSH request generated without a POP. Cleared if write 1" */
        volatile uint32_t OF: 1;  /* FIFO Overflow Flag. Asserted when FIFO is full and a PUSH re */
        /* @SVD:field=UF,bitOffset=15,bitWidth=1,description="FIFO Underflow Flag. Asserted when FIFO is empty and a POP request generated without a PUSH. Cleared if write 1" */
        volatile uint32_t UF: 1;  /* FIFO Underflow Flag. Asserted when FIFO is empty and a POP r */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_SR_Type;

/* @SVD:register=DBR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="Data Buffer register" */
/* DBR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=8,description="use the I2C Data Buffer register to transmit and receive data from the I2C bus. The DBR is accessed by software on one Side and by the I2C Shift register on the other. The DBR receives data coming into the I2C unit after a full byte is received and acknowledged. CPU writes data going out of the I2C to the DBR and sends it to the serial bus. \n              When the I2C is in transmit mode (master or slave), CPU writes data to the DBR over the internal bus. CPU write data to the DBR when a master transaction is initiated or when the DBR transmit-empty interrupt is signalled. Data moves from the DBR to the Shift register when the transfer byte bit is set. The DBR transmit-empty interrupt is signalled (if enabled) when a byte is transferred on the I2C bus and the acknowledge cycle is complete. If the DBR is not written, and a Stop condition is not in place before the I2C bus is ready to transfer the next byte packet, the I2C unit inserts wait states until CPU writes the DBR and sets the transfer byte bit.\n              When the I2C is in receive mode (master or slave), CPU reads DBR data over the internal bus. CPU reads data from the DBR when the DBR receive-full interrupt is signalled. The data moves from the Shift register to the DBR when the acknowledge cycle is complete. The I2C inserts wait states until the DBR is read. After the software reads the DBR, CR[NACK] are written by the software, allowing the next byte transfer to proceed to the I2C bus.\n              In DMA mode, DBR is automatically filled from FIFO in master transmit mode, or fetched and stored in FIFO in master receive mode until DMA done or aborted." */
        volatile uint32_t DATA: 8;  /* use the I2C Data Buffer register to transmit and receive dat */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_DBR_Type;

/* @SVD:register=SAR,offset=0x0014,size=32,access=read-write,reset=0x00000047,description="Slave Address Register" */
/* SAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=7,description="The seven-bit address to which the I2C responds when in slave-receive mode" */
        volatile uint32_t ADDR: 7;  /* The seven-bit address to which the I2C responds when in slav */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_SAR_Type;

/* @SVD:register=LCR,offset=0x0018,size=32,access=read-write,reset=0x081C72ED,description="Load Count Register" */
/* LCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLV,bitOffset=0,bitWidth=9,description="Decrementer Load value for Standard Mode SCL (master mode) for both high  and  low phase. \n              Data rate is generated as Ffclk/(SLV+max(SLV,CNT*2+6)+7+DNF) approximately. \n              100kbps data rate is generated by default if fclk is 48MHz.\n              SLV also controls setup time and hold time for START and STOP condition in Standard Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*SLV" */
        volatile uint32_t SLV: 9;  /* Decrementer Load value for Standard Mode SCL (master mode) f */
        /* @SVD:field=FLV,bitOffset=9,bitWidth=9,description="Decrementer Load value for Fast Mode (or Fast Mode Plus) SCL (master mode) for both high and low phase. \n              Data rate is generated as Ffclk/(FLV+max(FLV,CNT*2+6)+7+DNF) approximately. \n              400kbps data rate is generated by default if fclk is 48MHz.\n              FLV also controls setup time and hold time for START and STOP condition in Fast Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*FLV" */
        volatile uint32_t FLV: 9;  /* Decrementer Load value for Fast Mode (or Fast Mode Plus) SCL */
        /* @SVD:field=HLVL,bitOffset=18,bitWidth=9,description="Decrementer Load value for High Speed Mode SCL (master mode) for low phase. \n              Tlow=Tfclk*(HLVL+3+DNF). Data rate is generated as 1/(Thigh+Tlow), or Ffclk/(HLVH+HLVL+7+2*DNF). \n              3.2Mbps data rate is generated by default if fclk is 48MHz.\n              HLVL also controls setup time and hold time for START and STOP condition in High Speed Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*(HLVL+1)" */
        volatile uint32_t HLVL: 9;  /* Decrementer Load value for High Speed Mode SCL (master mode) */
        /* @SVD:field=HLVH,bitOffset=27,bitWidth=5,description="Decrementer Load value for High Speed Mode SCL (master mode) for high phase. \n              Thigh=Tfclk*(HLVH+4+DNF)" */
        volatile uint32_t HLVH: 5;  /* Decrementer Load value for High Speed Mode SCL (master mode) */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_LCR_Type;

/* @SVD:register=WCR,offset=0x001C,size=32,access=read-write,reset=0x0000000A,description="Wait Count Register" */
/* WCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=8,description="Controls the counter values defining the setup and hold times in standard and fast mode\n              Tvddat=Thddat=Tfclk*(CNT+2)\n              Tsudat=max(Tlow-Thddat,Thddat)\n              Lower counter values may violate setup and hold times." */
        volatile uint32_t CNT: 8;  /* Controls the counter values defining the setup and hold time */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_WCR_Type;

/* @SVD:register=RCCR,offset=0x0020,size=32,access=read-write,reset=0x00000009,description="Bus Reset Cycle Counter Register" */
/* RCCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSTCYC,bitOffset=0,bitWidth=4,description="The cycles of SCL during bus reset" */
        volatile uint32_t RSTCYC: 4;  /* The cycles of SCL during bus reset */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_RCCR_Type;

/* @SVD:register=BMR,offset=0x0024,size=32,access=read-write,reset=0x00000003,description="Bus Monitor Register" */
/* BMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SDA,bitOffset=0,bitWidth=1,description="value of the SDA pin." */
        volatile uint32_t SDA: 1;  /* value of the SDA pin. */
        /* @SVD:field=SCL,bitOffset=1,bitWidth=1,description="value of the SCL pin. Software can check bus level when the I2C bus is hung and the I2C unit must be reset." */
        volatile uint32_t SCL: 1;  /* value of the SCL pin. Software can check bus level when the  */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_BMR_Type;

/* @SVD:register=DNR,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="DMA number register" */
/* DNR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=9,description="Write as number of data to transfer in byte. Read as left data number to transfer" */
        volatile uint32_t NDT: 9;  /* Write as number of data to transfer in byte. Read as left da */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_DNR_Type;

/* @SVD:register=FIFO,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="FIFO Register" */
/* FIFO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=8,description="Write to push send data into FIFO. Read to pop received data from FIFO" */
        volatile uint32_t DATA: 8;  /* Write to push send data into FIFO. Read to pop received data */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C2_FIFO_Type;

/* I2C2 寄存器结构体 */
typedef struct
{
    I2C2_CR_Type CR;  /* 0x0000 - Control register */
    I2C2_TCR_Type TCR;  /* 0x0004 - Transfer Control register */
    I2C2_IER_Type IER;  /* 0x0008 - Interrupt Enable register */
    I2C2_SR_Type SR;  /* 0x000C - Status register */
    I2C2_DBR_Type DBR;  /* 0x0010 - Data Buffer register */
    I2C2_SAR_Type SAR;  /* 0x0014 - Slave Address Register */
    I2C2_LCR_Type LCR;  /* 0x0018 - Load Count Register */
    I2C2_WCR_Type WCR;  /* 0x001C - Wait Count Register */
    I2C2_RCCR_Type RCCR;  /* 0x0020 - Bus Reset Cycle Counter Register */
    I2C2_BMR_Type BMR;  /* 0x0024 - Bus Monitor Register */
    I2C2_DNR_Type DNR;  /* 0x0028 - DMA number register */
    /* @SVD:register=RSVD1,offset=0x002C,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x002C - RSVD1 */
    I2C2_FIFO_Type FIFO;  /* 0x0030 - FIFO Register */
} I2C2_TypeDef;

/* ============================================================================ */
/* I2C3 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=I2C3,base=0x5009E000,description="",groupName=I2C */

#define I2C3_BASE                                          (0x5009E000UL)

/* @SVD:register=CR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MODE,bitOffset=0,bitWidth=2,description="Bus Mode (Master operation):\n              2'b00: standard-mode\n              2'b01: fast-mode and fast-mode plus\n              2'b10: HS-mode (standard mode when not doing a high speed transfer)\n              2'b11: HS-mode (fast mode when not doing a high speed transfer)\n              Bus Mode (Slave operation):\n              2'b0x: HS-mode is disabled. I2C unit uses Standard/Fast mode timing on the SDA pin.\n              2'b1x: HS-mode is enabled. I2C unit uses HS-mode timing on the SDA pin when a master code is received." */
        volatile uint32_t MODE: 2;  /* Bus Mode (Master operation):               2'b00: standard-m */
        /* @SVD:field=IUE,bitOffset=2,bitWidth=1,description="I2C Unit Enable:\n              0 = Disables the unit and does not master any transactions or respond to any slave transactions.\n              1 = Enables the I2C (defaults to slave-receive mode).\n              Software must guarantee the I2C bus is idle before setting this bit." */
        volatile uint32_t IUE: 1;  /* I2C Unit Enable:               0 = Disables the unit and doe */
        /* @SVD:field=SCLE,bitOffset=3,bitWidth=1,description="SCL Enable:\n              0 = Disables the I2C from driving the SCL line.\n              1 = Enables the I2C clock output for master-mode operation." */
        volatile uint32_t SCLE: 1;  /* SCL Enable:               0 = Disables the I2C from driving  */
        /* @SVD:field=DMAEN,bitOffset=4,bitWidth=1,description="DMA Enable for both TX and RX\n              0 = DMA mode is NOT enabled\n              1 = DMA mode enabled" */
        volatile uint32_t DMAEN: 1;  /* DMA Enable for both TX and RX               0 = DMA mode is  */
        /* @SVD:field=LASTNACK,bitOffset=5,bitWidth=1,description="Generate NACK for last DMA Read transfer" */
        volatile uint32_t LASTNACK: 1;  /* Generate NACK for last DMA Read transfer */
        /* @SVD:field=LASTSTOP,bitOffset=6,bitWidth=1,description="Generate STOP for last DMA transfer" */
        volatile uint32_t LASTSTOP: 1;  /* Generate STOP for last DMA transfer */
        /* @SVD:field=RSVD3,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=MSDE,bitOffset=8,bitWidth=1,description="Master Stop Detected Enable:\n              0 = Master Stop Detect (MSD) status is not enabled.\n              1 = Master Stop Detect (MSD) status is enabled." */
        volatile uint32_t MSDE: 1;  /* Master Stop Detected Enable:               0 = Master Stop D */
        /* @SVD:field=SCLPP,bitOffset=9,bitWidth=1,description="Push-pull mode Enable for SCL.\n              0 = open drain output for SCL.\n              1 = Push-pull output for SCL" */
        volatile uint32_t SCLPP: 1;  /* Push-pull mode Enable for SCL.               0 = open drain  */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SLVEN,bitOffset=11,bitWidth=1,description="Slave mode Enable for SCL.\n              0 = Disable slave mode. Will not monitor slave address on I2C bus.\n              1 = Enable slave mode. Will monitor slave address on I2C bus." */
        volatile uint32_t SLVEN: 1;  /* Slave mode Enable for SCL.               0 = Disable slave m */
        /* @SVD:field=DNF,bitOffset=12,bitWidth=3,description="Digital noise filter\n              These bits are used to configure the digital noise filter on SDA and SCL input. The digital filter will filter spikes with a length of up to DNF*Tfclk.\n              0: Digital filter disabled \n              1: Digital filter enabled and filtering capability up to 1 Tfclk\n              ...\n              7: digital filter enabled and filtering capability up to 7 Tfclk\n              Digital filter is added to analog filter. Digital filter will introduce delay on SCL and SDA processing, which is essential in hs-mode." */
        volatile uint32_t DNF: 3;  /* Digital noise filter               These bits are used to co */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
        /* @SVD:field=BRGRST,bitOffset=29,bitWidth=1,description="Reset bus related state machine and signals. Will be cleared by HW automatically\n              1 = request for reset\n              0 = reset finished" */
        volatile uint32_t BRGRST: 1;  /* Reset bus related state machine and signals. Will be cleared */
        /* @SVD:field=RSTREQ,bitOffset=30,bitWidth=1,description="I2C will do bus reset upon this bit set. Will be cleared by HW automatically after RSTCYC cycles of SCL generated.\n              1 = request for i2c bus reset\n              0 = bus reset finished" */
        volatile uint32_t RSTREQ: 1;  /* I2C will do bus reset upon this bit set. Will be cleared by  */
        /* @SVD:field=UR,bitOffset=31,bitWidth=1,description="Unit Reset. Software need first assert to reset then deassert to release.\n              0 = No reset.\n              1 = Reset I2C module." */
        volatile uint32_t UR: 1;  /* Unit Reset. Software need first assert to reset then deasser */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_CR_Type;

/* @SVD:register=TCR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Transfer Control register" */
/* TCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TB,bitOffset=0,bitWidth=1,description="Transfer Byte:\n              Used to send or receive a byte on the I2C bus:\n              0 = Cleared by I2C when the byte is sent/received.\n              1 = Send/receive a byte. \n              CPU can monitor this bit to determine when the byte transfer has completed. In master or slave mode, after each byte transfer including acknowledge pulse, the I2C holds the SCL line low (inserting wait states) until TB is set." */
        volatile uint32_t TB: 1;  /* Transfer Byte:               Used to send or receive a byte  */
        /* @SVD:field=START,bitOffset=1,bitWidth=1,description="Start:\n              Used to initiate a Start condition to the I2C unit when in master mode. \n              0 = Do not send a Start pulse.\n              1 = Send a Start pulse." */
        volatile uint32_t START: 1;  /* Start:               Used to initiate a Start condition to t */
        /* @SVD:field=STOP,bitOffset=2,bitWidth=1,description="Stop:\n              Used to initiate a Stop condition after transferring the next data byte on the I2C bus when in master mode. In master-receive mode, the NACK control bit must be set in conjunction with the STOP bit. \n              0 = Do not send a Stop.\n              1 = Send a Stop." */
        volatile uint32_t STOP: 1;  /* Stop:               Used to initiate a Stop condition after  */
        /* @SVD:field=NACK,bitOffset=3,bitWidth=1,description="The positive/negative acknowledge control bit, defines the type of acknowledge pulse sent by the I2C when in master receive mode:\n              0 = Send a positive acknowledge (ACK) pulse after receiving a data byte.\n              1 = Send a negative acknowledge (NACK) pulse after receiving a data byte.\n              The I2C automatically sends an ACK pulse when responding to its slave address or when responding in slave-receive mode, regardless of the NACK control-bit setting." */
        volatile uint32_t NACK: 1;  /* The positive/negative acknowledge control bit, defines the t */
        /* @SVD:field=MA,bitOffset=4,bitWidth=1,description="Master Abort:\n              Used by the I2C in master mode to generate a Stop without transmitting another data byte:\n              0 = The I2C transmits Stop on if TCR[STOP] is set.\n              1 = The I2C sends Stop without data transmission.\n              When in master-transmit mode, after transmitting a data byte, the TCR[TB] bit is cleared. When no more data bytes need to be sent, setting master abort bit sends the Stop. The TCR[TB] bit must remain clear.\n              In master-receive mode, when a NAK is sent without a Stop (TCR[STOP] bit was not set) and CPU does not send a repeated Start, setting this bit sends the Stop. Once again, the TCR[TB] bit must remain clear. Master Abort can be done immediately after the address phase (Master Transmit mode only)." */
        volatile uint32_t MA: 1;  /* Master Abort:               Used by the I2C in master mode t */
        /* @SVD:field=TXREQ,bitOffset=5,bitWidth=1,description="Request DMA TX. Will be cleared by HW automatically" */
        volatile uint32_t TXREQ: 1;  /* Request DMA TX. Will be cleared by HW automatically */
        /* @SVD:field=RXREQ,bitOffset=6,bitWidth=1,description="Request DMA RX. Will be cleared by HW automatically" */
        volatile uint32_t RXREQ: 1;  /* Request DMA RX. Will be cleared by HW automatically */
        /* @SVD:field=ABORTDMA,bitOffset=7,bitWidth=1,description="Abort DMA operation. Will be cleared by HW automatically" */
        volatile uint32_t ABORTDMA: 1;  /* Abort DMA operation. Will be cleared by HW automatically */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_TCR_Type;

/* @SVD:register=IER,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Interrupt Enable register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD4: 4;
        /* @SVD:field=SSDIE,bitOffset=4,bitWidth=1,description="Slave Stop Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt when it detects a Stop condition while in slave mode." */
        volatile uint32_t SSDIE: 1;  /* Slave Stop Detected Interrupt Enable:               0 = Disa */
        /* @SVD:field=ALDIE,bitOffset=5,bitWidth=1,description="Arbitration Loss Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt upon losing arbitration while in master mode." */
        volatile uint32_t ALDIE: 1;  /* Arbitration Loss Detected Interrupt Enable:               0  */
        /* @SVD:field=TEIE,bitOffset=6,bitWidth=1,description="DBR Transmit Empty Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt after transmitting a byte onto the I2C bus." */
        volatile uint32_t TEIE: 1;  /* DBR Transmit Empty Interrupt Enable:               0 = Disab */
        /* @SVD:field=RFIE,bitOffset=7,bitWidth=1,description="DBR Receive Full Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt when the DBR has received a data byte from the I2C bus." */
        volatile uint32_t RFIE: 1;  /* DBR Receive Full Interrupt Enable:               0 = Disable */
        /* @SVD:field=RSVD3,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=SADIE,bitOffset=9,bitWidth=1,description="Slave Address Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt upon detecting a slave address match or a general call address." */
        volatile uint32_t SADIE: 1;  /* Slave Address Detected Interrupt Enable:               0 = D */
        /* @SVD:field=BEDIE,bitOffset=10,bitWidth=1,description="Bus Error Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt for the following I2C bus errors:\n              As a master transmitter, no ACK was detected after a byte was sent.\n              As a slave receiver, the I2C generated a NACK pulse.\n              Software is responsible for guaranteeing that misplaced Start and Stop conditions do not occur." */
        volatile uint32_t BEDIE: 1;  /* Bus Error Detected Interrupt Enable:               0 = Disab */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=MSDIE,bitOffset=12,bitWidth=1,description="Master Stop Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C unit to interrupt upon detecting a Master Stop sent by the I2C unit." */
        volatile uint32_t MSDIE: 1;  /* Master Stop Detected Interrupt Enable:               0 = Dis */
        /* @SVD:field=DMADONEIE,bitOffset=13,bitWidth=1,description="DMA Transaction Done Interrupt Enable\n              0 = DMA Transaction done interrupt is not enabled.\n              1 = DMA Transaction done interrupt is enabled." */
        volatile uint32_t DMADONEIE: 1;  /* DMA Transaction Done Interrupt Enable               0 = DMA  */
        /* @SVD:field=OFIE,bitOffset=14,bitWidth=1,description="FIFO Overflow Interrupt Enable\n              0 = FIFO Overflow interrupt is not enabled\n              1 = FIFO Overflow interrupt is enabled" */
        volatile uint32_t OFIE: 1;  /* FIFO Overflow Interrupt Enable               0 = FIFO Overfl */
        /* @SVD:field=UFIE,bitOffset=15,bitWidth=1,description="FIFO Underflow Interrupt Enable\n              0 = FIFO Underflow interrupt is not enabled\n              1 = FIFO Underflow interrupt is enabled" */
        volatile uint32_t UFIE: 1;  /* FIFO Underflow Interrupt Enable               0 = FIFO Under */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_IER_Type;

/* @SVD:register=SR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWM,bitOffset=0,bitWidth=1,description="Read/write Mode:\n              0 = The I2C is in master-transmit or slave-receive mode. \n              1 = The I2C is in master-receive or slave-transmit mode. \n              This is the R/nW bit of the slave address. It is cleared automatically by hardware after a Stop state." */
        volatile uint32_t RWM: 1;  /* Read/write Mode:               0 = The I2C is in master-tran */
        /* @SVD:field=NACK,bitOffset=1,bitWidth=1,description="ACK/NACK Status:\n              0 = The I2C received or sent an ACK on the bus. \n              1 = The I2C received or sent a NACK.on the bus.\n              This bit is used in slave-transmit mode to determine when the byte transferred is the last one. This bit is updated after each byte and ACK/NACK information is received." */
        volatile uint32_t NACK: 1;  /* ACK/NACK Status:               0 = The I2C received or sent  */
        /* @SVD:field=UB,bitOffset=2,bitWidth=1,description="Unit Busy:\n              0 = I2C not busy.\n              1 = Set when local I2C is busy. This is defined as the time between the first Start and Stop." */
        volatile uint32_t UB: 1;  /* Unit Busy:               0 = I2C not busy.               1 = */
        /* @SVD:field=IBB,bitOffset=3,bitWidth=1,description="I2C Bus Busy:\n              0 = I2C bus is idle or the I2C is using the bus (that is, unit busy).\n              1 = Set when the I2C bus is busy but local I2C is not involved in the transaction." */
        volatile uint32_t IBB: 1;  /* I2C Bus Busy:               0 = I2C bus is idle or the I2C i */
        /* @SVD:field=SSD,bitOffset=4,bitWidth=1,description="Slave Stop Detected:\n              0 = No Stop detected.\n              1 = Set when the I2C detects a Stop while in slave-receive or slave-transmit mode. \n              Cleared if write 1" */
        volatile uint32_t SSD: 1;  /* Slave Stop Detected:               0 = No Stop detected.     */
        /* @SVD:field=ALD,bitOffset=5,bitWidth=1,description="Arbitration Loss Detected:\n              Used during multi-master operation:\n              0 = Cleared when arbitration is won or never took place.\n              1 = Set when the I2C loses arbitration. \n              Cleared if write 1" */
        volatile uint32_t ALD: 1;  /* Arbitration Loss Detected:               Used during multi-m */
        /* @SVD:field=TE,bitOffset=6,bitWidth=1,description="DBR Transmit Empty:\n              0 = The data byte is still being transmitted.\n              1 = The I2C has finished transmitting a data byte on the I2C bus. An interrupt is signalled when enabled in the CR.\n              Cleared if write 1" */
        volatile uint32_t TE: 1;  /* DBR Transmit Empty:               0 = The data byte is still */
        /* @SVD:field=RF,bitOffset=7,bitWidth=1,description="DBR Receive Full:\n              0 = The DBR has not received a new data byte or the I2C is idle.\n              1 = The DBR register received a new data byte from the I2C bus. An interrupt is signalled when enabled in the CR. \n              Cleared if write 1" */
        volatile uint32_t RF: 1;  /* DBR Receive Full:               0 = The DBR has not received */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SAD,bitOffset=9,bitWidth=1,description="Slave Address Detected:\n              0 = No slave address was detected.\n              1 = The I2C detected a seven-bit address that matches the general call address or SAR. An interrupt is signalled when enabled in the CR. \n              Cleared if write 1" */
        volatile uint32_t SAD: 1;  /* Slave Address Detected:               0 = No slave address w */
        /* @SVD:field=BED,bitOffset=10,bitWidth=1,description="Bus Error Detected:\n              0 = No error detected.\n              1 = The I2C sets this bit when it detects one of the following error conditions:\n              As a master transmitter, no ACK was detected on the interface after a byte was sent.\n              As a slave receiver, the I2C generates a NACK pulse.\n              When an error occurs, I2C bus transactions continue. Software must guarantee that misplaced Start and Stop conditions do not occur. \n              Cleared if write 1" */
        volatile uint32_t BED: 1;  /* Bus Error Detected:               0 = No error detected.     */
        /* @SVD:field=EBB,bitOffset=11,bitWidth=1,description="Early Bus Busy\n              0 = I2C bus is idle or the I2C is using the bus (that is, unit busy).\n              1 = Set when the unit detects that the SCL or SDA line is low without a START condition. Bit will remain set until the I2C unit detects the bus is idle by detecting a STOP condition. Bit will also be set whenever the IBB bit is set." */
        volatile uint32_t EBB: 1;  /* Early Bus Busy               0 = I2C bus is idle or the I2C  */
        /* @SVD:field=MSD,bitOffset=12,bitWidth=1,description="Master Stop Detected:\n              0 = No Master Stop Detected.\n              1 = This bit is set by the I2C unit when all of the following are true:\n              This bit is enabled (CR[MSDE] = 1);\n              I2C unit is configured as a master;\n              I2C transmits a STOP signal" */
        volatile uint32_t MSD: 1;  /* Master Stop Detected:               0 = No Master Stop Detec */
        /* @SVD:field=DMADONE,bitOffset=13,bitWidth=1,description="DMA Transaction Done. Asserted when both APB and I2C bus have finished transfer. Cleared if write 1" */
        volatile uint32_t DMADONE: 1;  /* DMA Transaction Done. Asserted when both APB and I2C bus hav */
        /* @SVD:field=OF,bitOffset=14,bitWidth=1,description="FIFO Overflow Flag. Asserted when FIFO is full and a PUSH request generated without a POP. Cleared if write 1" */
        volatile uint32_t OF: 1;  /* FIFO Overflow Flag. Asserted when FIFO is full and a PUSH re */
        /* @SVD:field=UF,bitOffset=15,bitWidth=1,description="FIFO Underflow Flag. Asserted when FIFO is empty and a POP request generated without a PUSH. Cleared if write 1" */
        volatile uint32_t UF: 1;  /* FIFO Underflow Flag. Asserted when FIFO is empty and a POP r */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_SR_Type;

/* @SVD:register=DBR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="Data Buffer register" */
/* DBR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=8,description="use the I2C Data Buffer register to transmit and receive data from the I2C bus. The DBR is accessed by software on one Side and by the I2C Shift register on the other. The DBR receives data coming into the I2C unit after a full byte is received and acknowledged. CPU writes data going out of the I2C to the DBR and sends it to the serial bus. \n              When the I2C is in transmit mode (master or slave), CPU writes data to the DBR over the internal bus. CPU write data to the DBR when a master transaction is initiated or when the DBR transmit-empty interrupt is signalled. Data moves from the DBR to the Shift register when the transfer byte bit is set. The DBR transmit-empty interrupt is signalled (if enabled) when a byte is transferred on the I2C bus and the acknowledge cycle is complete. If the DBR is not written, and a Stop condition is not in place before the I2C bus is ready to transfer the next byte packet, the I2C unit inserts wait states until CPU writes the DBR and sets the transfer byte bit.\n              When the I2C is in receive mode (master or slave), CPU reads DBR data over the internal bus. CPU reads data from the DBR when the DBR receive-full interrupt is signalled. The data moves from the Shift register to the DBR when the acknowledge cycle is complete. The I2C inserts wait states until the DBR is read. After the software reads the DBR, CR[NACK] are written by the software, allowing the next byte transfer to proceed to the I2C bus.\n              In DMA mode, DBR is automatically filled from FIFO in master transmit mode, or fetched and stored in FIFO in master receive mode until DMA done or aborted." */
        volatile uint32_t DATA: 8;  /* use the I2C Data Buffer register to transmit and receive dat */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_DBR_Type;

/* @SVD:register=SAR,offset=0x0014,size=32,access=read-write,reset=0x00000047,description="Slave Address Register" */
/* SAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=7,description="The seven-bit address to which the I2C responds when in slave-receive mode" */
        volatile uint32_t ADDR: 7;  /* The seven-bit address to which the I2C responds when in slav */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_SAR_Type;

/* @SVD:register=LCR,offset=0x0018,size=32,access=read-write,reset=0x081C72ED,description="Load Count Register" */
/* LCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLV,bitOffset=0,bitWidth=9,description="Decrementer Load value for Standard Mode SCL (master mode) for both high  and  low phase. \n              Data rate is generated as Ffclk/(SLV+max(SLV,CNT*2+6)+7+DNF) approximately. \n              100kbps data rate is generated by default if fclk is 48MHz.\n              SLV also controls setup time and hold time for START and STOP condition in Standard Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*SLV" */
        volatile uint32_t SLV: 9;  /* Decrementer Load value for Standard Mode SCL (master mode) f */
        /* @SVD:field=FLV,bitOffset=9,bitWidth=9,description="Decrementer Load value for Fast Mode (or Fast Mode Plus) SCL (master mode) for both high and low phase. \n              Data rate is generated as Ffclk/(FLV+max(FLV,CNT*2+6)+7+DNF) approximately. \n              400kbps data rate is generated by default if fclk is 48MHz.\n              FLV also controls setup time and hold time for START and STOP condition in Fast Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*FLV" */
        volatile uint32_t FLV: 9;  /* Decrementer Load value for Fast Mode (or Fast Mode Plus) SCL */
        /* @SVD:field=HLVL,bitOffset=18,bitWidth=9,description="Decrementer Load value for High Speed Mode SCL (master mode) for low phase. \n              Tlow=Tfclk*(HLVL+3+DNF). Data rate is generated as 1/(Thigh+Tlow), or Ffclk/(HLVH+HLVL+7+2*DNF). \n              3.2Mbps data rate is generated by default if fclk is 48MHz.\n              HLVL also controls setup time and hold time for START and STOP condition in High Speed Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*(HLVL+1)" */
        volatile uint32_t HLVL: 9;  /* Decrementer Load value for High Speed Mode SCL (master mode) */
        /* @SVD:field=HLVH,bitOffset=27,bitWidth=5,description="Decrementer Load value for High Speed Mode SCL (master mode) for high phase. \n              Thigh=Tfclk*(HLVH+4+DNF)" */
        volatile uint32_t HLVH: 5;  /* Decrementer Load value for High Speed Mode SCL (master mode) */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_LCR_Type;

/* @SVD:register=WCR,offset=0x001C,size=32,access=read-write,reset=0x0000000A,description="Wait Count Register" */
/* WCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=8,description="Controls the counter values defining the setup and hold times in standard and fast mode\n              Tvddat=Thddat=Tfclk*(CNT+2)\n              Tsudat=max(Tlow-Thddat,Thddat)\n              Lower counter values may violate setup and hold times." */
        volatile uint32_t CNT: 8;  /* Controls the counter values defining the setup and hold time */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_WCR_Type;

/* @SVD:register=RCCR,offset=0x0020,size=32,access=read-write,reset=0x00000009,description="Bus Reset Cycle Counter Register" */
/* RCCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSTCYC,bitOffset=0,bitWidth=4,description="The cycles of SCL during bus reset" */
        volatile uint32_t RSTCYC: 4;  /* The cycles of SCL during bus reset */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_RCCR_Type;

/* @SVD:register=BMR,offset=0x0024,size=32,access=read-write,reset=0x00000003,description="Bus Monitor Register" */
/* BMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SDA,bitOffset=0,bitWidth=1,description="value of the SDA pin." */
        volatile uint32_t SDA: 1;  /* value of the SDA pin. */
        /* @SVD:field=SCL,bitOffset=1,bitWidth=1,description="value of the SCL pin. Software can check bus level when the I2C bus is hung and the I2C unit must be reset." */
        volatile uint32_t SCL: 1;  /* value of the SCL pin. Software can check bus level when the  */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_BMR_Type;

/* @SVD:register=DNR,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="DMA number register" */
/* DNR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=9,description="Write as number of data to transfer in byte. Read as left data number to transfer" */
        volatile uint32_t NDT: 9;  /* Write as number of data to transfer in byte. Read as left da */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_DNR_Type;

/* @SVD:register=FIFO,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="FIFO Register" */
/* FIFO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=8,description="Write to push send data into FIFO. Read to pop received data from FIFO" */
        volatile uint32_t DATA: 8;  /* Write to push send data into FIFO. Read to pop received data */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C3_FIFO_Type;

/* I2C3 寄存器结构体 */
typedef struct
{
    I2C3_CR_Type CR;  /* 0x0000 - Control register */
    I2C3_TCR_Type TCR;  /* 0x0004 - Transfer Control register */
    I2C3_IER_Type IER;  /* 0x0008 - Interrupt Enable register */
    I2C3_SR_Type SR;  /* 0x000C - Status register */
    I2C3_DBR_Type DBR;  /* 0x0010 - Data Buffer register */
    I2C3_SAR_Type SAR;  /* 0x0014 - Slave Address Register */
    I2C3_LCR_Type LCR;  /* 0x0018 - Load Count Register */
    I2C3_WCR_Type WCR;  /* 0x001C - Wait Count Register */
    I2C3_RCCR_Type RCCR;  /* 0x0020 - Bus Reset Cycle Counter Register */
    I2C3_BMR_Type BMR;  /* 0x0024 - Bus Monitor Register */
    I2C3_DNR_Type DNR;  /* 0x0028 - DMA number register */
    /* @SVD:register=RSVD1,offset=0x002C,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x002C - RSVD1 */
    I2C3_FIFO_Type FIFO;  /* 0x0030 - FIFO Register */
} I2C3_TypeDef;

/* ============================================================================ */
/* I2C4 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=I2C4,base=0x5009F000,description="",groupName=I2C */

#define I2C4_BASE                                          (0x5009F000UL)

/* @SVD:register=CR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MODE,bitOffset=0,bitWidth=2,description="Bus Mode (Master operation):\n              2'b00: standard-mode\n              2'b01: fast-mode and fast-mode plus\n              2'b10: HS-mode (standard mode when not doing a high speed transfer)\n              2'b11: HS-mode (fast mode when not doing a high speed transfer)\n              Bus Mode (Slave operation):\n              2'b0x: HS-mode is disabled. I2C unit uses Standard/Fast mode timing on the SDA pin.\n              2'b1x: HS-mode is enabled. I2C unit uses HS-mode timing on the SDA pin when a master code is received." */
        volatile uint32_t MODE: 2;  /* Bus Mode (Master operation):               2'b00: standard-m */
        /* @SVD:field=IUE,bitOffset=2,bitWidth=1,description="I2C Unit Enable:\n              0 = Disables the unit and does not master any transactions or respond to any slave transactions.\n              1 = Enables the I2C (defaults to slave-receive mode).\n              Software must guarantee the I2C bus is idle before setting this bit." */
        volatile uint32_t IUE: 1;  /* I2C Unit Enable:               0 = Disables the unit and doe */
        /* @SVD:field=SCLE,bitOffset=3,bitWidth=1,description="SCL Enable:\n              0 = Disables the I2C from driving the SCL line.\n              1 = Enables the I2C clock output for master-mode operation." */
        volatile uint32_t SCLE: 1;  /* SCL Enable:               0 = Disables the I2C from driving  */
        /* @SVD:field=DMAEN,bitOffset=4,bitWidth=1,description="DMA Enable for both TX and RX\n              0 = DMA mode is NOT enabled\n              1 = DMA mode enabled" */
        volatile uint32_t DMAEN: 1;  /* DMA Enable for both TX and RX               0 = DMA mode is  */
        /* @SVD:field=LASTNACK,bitOffset=5,bitWidth=1,description="Generate NACK for last DMA Read transfer" */
        volatile uint32_t LASTNACK: 1;  /* Generate NACK for last DMA Read transfer */
        /* @SVD:field=LASTSTOP,bitOffset=6,bitWidth=1,description="Generate STOP for last DMA transfer" */
        volatile uint32_t LASTSTOP: 1;  /* Generate STOP for last DMA transfer */
        /* @SVD:field=RSVD3,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=MSDE,bitOffset=8,bitWidth=1,description="Master Stop Detected Enable:\n              0 = Master Stop Detect (MSD) status is not enabled.\n              1 = Master Stop Detect (MSD) status is enabled." */
        volatile uint32_t MSDE: 1;  /* Master Stop Detected Enable:               0 = Master Stop D */
        /* @SVD:field=SCLPP,bitOffset=9,bitWidth=1,description="Push-pull mode Enable for SCL.\n              0 = open drain output for SCL.\n              1 = Push-pull output for SCL" */
        volatile uint32_t SCLPP: 1;  /* Push-pull mode Enable for SCL.               0 = open drain  */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SLVEN,bitOffset=11,bitWidth=1,description="Slave mode Enable for SCL.\n              0 = Disable slave mode. Will not monitor slave address on I2C bus.\n              1 = Enable slave mode. Will monitor slave address on I2C bus." */
        volatile uint32_t SLVEN: 1;  /* Slave mode Enable for SCL.               0 = Disable slave m */
        /* @SVD:field=DNF,bitOffset=12,bitWidth=3,description="Digital noise filter\n              These bits are used to configure the digital noise filter on SDA and SCL input. The digital filter will filter spikes with a length of up to DNF*Tfclk.\n              0: Digital filter disabled \n              1: Digital filter enabled and filtering capability up to 1 Tfclk\n              ...\n              7: digital filter enabled and filtering capability up to 7 Tfclk\n              Digital filter is added to analog filter. Digital filter will introduce delay on SCL and SDA processing, which is essential in hs-mode." */
        volatile uint32_t DNF: 3;  /* Digital noise filter               These bits are used to co */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=14,description="" */
        volatile uint32_t RSVD: 14;
        /* @SVD:field=BRGRST,bitOffset=29,bitWidth=1,description="Reset bus related state machine and signals. Will be cleared by HW automatically\n              1 = request for reset\n              0 = reset finished" */
        volatile uint32_t BRGRST: 1;  /* Reset bus related state machine and signals. Will be cleared */
        /* @SVD:field=RSTREQ,bitOffset=30,bitWidth=1,description="I2C will do bus reset upon this bit set. Will be cleared by HW automatically after RSTCYC cycles of SCL generated.\n              1 = request for i2c bus reset\n              0 = bus reset finished" */
        volatile uint32_t RSTREQ: 1;  /* I2C will do bus reset upon this bit set. Will be cleared by  */
        /* @SVD:field=UR,bitOffset=31,bitWidth=1,description="Unit Reset. Software need first assert to reset then deassert to release.\n              0 = No reset.\n              1 = Reset I2C module." */
        volatile uint32_t UR: 1;  /* Unit Reset. Software need first assert to reset then deasser */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_CR_Type;

/* @SVD:register=TCR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Transfer Control register" */
/* TCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TB,bitOffset=0,bitWidth=1,description="Transfer Byte:\n              Used to send or receive a byte on the I2C bus:\n              0 = Cleared by I2C when the byte is sent/received.\n              1 = Send/receive a byte. \n              CPU can monitor this bit to determine when the byte transfer has completed. In master or slave mode, after each byte transfer including acknowledge pulse, the I2C holds the SCL line low (inserting wait states) until TB is set." */
        volatile uint32_t TB: 1;  /* Transfer Byte:               Used to send or receive a byte  */
        /* @SVD:field=START,bitOffset=1,bitWidth=1,description="Start:\n              Used to initiate a Start condition to the I2C unit when in master mode. \n              0 = Do not send a Start pulse.\n              1 = Send a Start pulse." */
        volatile uint32_t START: 1;  /* Start:               Used to initiate a Start condition to t */
        /* @SVD:field=STOP,bitOffset=2,bitWidth=1,description="Stop:\n              Used to initiate a Stop condition after transferring the next data byte on the I2C bus when in master mode. In master-receive mode, the NACK control bit must be set in conjunction with the STOP bit. \n              0 = Do not send a Stop.\n              1 = Send a Stop." */
        volatile uint32_t STOP: 1;  /* Stop:               Used to initiate a Stop condition after  */
        /* @SVD:field=NACK,bitOffset=3,bitWidth=1,description="The positive/negative acknowledge control bit, defines the type of acknowledge pulse sent by the I2C when in master receive mode:\n              0 = Send a positive acknowledge (ACK) pulse after receiving a data byte.\n              1 = Send a negative acknowledge (NACK) pulse after receiving a data byte.\n              The I2C automatically sends an ACK pulse when responding to its slave address or when responding in slave-receive mode, regardless of the NACK control-bit setting." */
        volatile uint32_t NACK: 1;  /* The positive/negative acknowledge control bit, defines the t */
        /* @SVD:field=MA,bitOffset=4,bitWidth=1,description="Master Abort:\n              Used by the I2C in master mode to generate a Stop without transmitting another data byte:\n              0 = The I2C transmits Stop on if TCR[STOP] is set.\n              1 = The I2C sends Stop without data transmission.\n              When in master-transmit mode, after transmitting a data byte, the TCR[TB] bit is cleared. When no more data bytes need to be sent, setting master abort bit sends the Stop. The TCR[TB] bit must remain clear.\n              In master-receive mode, when a NAK is sent without a Stop (TCR[STOP] bit was not set) and CPU does not send a repeated Start, setting this bit sends the Stop. Once again, the TCR[TB] bit must remain clear. Master Abort can be done immediately after the address phase (Master Transmit mode only)." */
        volatile uint32_t MA: 1;  /* Master Abort:               Used by the I2C in master mode t */
        /* @SVD:field=TXREQ,bitOffset=5,bitWidth=1,description="Request DMA TX. Will be cleared by HW automatically" */
        volatile uint32_t TXREQ: 1;  /* Request DMA TX. Will be cleared by HW automatically */
        /* @SVD:field=RXREQ,bitOffset=6,bitWidth=1,description="Request DMA RX. Will be cleared by HW automatically" */
        volatile uint32_t RXREQ: 1;  /* Request DMA RX. Will be cleared by HW automatically */
        /* @SVD:field=ABORTDMA,bitOffset=7,bitWidth=1,description="Abort DMA operation. Will be cleared by HW automatically" */
        volatile uint32_t ABORTDMA: 1;  /* Abort DMA operation. Will be cleared by HW automatically */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_TCR_Type;

/* @SVD:register=IER,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Interrupt Enable register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD4: 4;
        /* @SVD:field=SSDIE,bitOffset=4,bitWidth=1,description="Slave Stop Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt when it detects a Stop condition while in slave mode." */
        volatile uint32_t SSDIE: 1;  /* Slave Stop Detected Interrupt Enable:               0 = Disa */
        /* @SVD:field=ALDIE,bitOffset=5,bitWidth=1,description="Arbitration Loss Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt upon losing arbitration while in master mode." */
        volatile uint32_t ALDIE: 1;  /* Arbitration Loss Detected Interrupt Enable:               0  */
        /* @SVD:field=TEIE,bitOffset=6,bitWidth=1,description="DBR Transmit Empty Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt after transmitting a byte onto the I2C bus." */
        volatile uint32_t TEIE: 1;  /* DBR Transmit Empty Interrupt Enable:               0 = Disab */
        /* @SVD:field=RFIE,bitOffset=7,bitWidth=1,description="DBR Receive Full Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt when the DBR has received a data byte from the I2C bus." */
        volatile uint32_t RFIE: 1;  /* DBR Receive Full Interrupt Enable:               0 = Disable */
        /* @SVD:field=RSVD3,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=SADIE,bitOffset=9,bitWidth=1,description="Slave Address Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt upon detecting a slave address match or a general call address." */
        volatile uint32_t SADIE: 1;  /* Slave Address Detected Interrupt Enable:               0 = D */
        /* @SVD:field=BEDIE,bitOffset=10,bitWidth=1,description="Bus Error Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C to interrupt for the following I2C bus errors:\n              As a master transmitter, no ACK was detected after a byte was sent.\n              As a slave receiver, the I2C generated a NACK pulse.\n              Software is responsible for guaranteeing that misplaced Start and Stop conditions do not occur." */
        volatile uint32_t BEDIE: 1;  /* Bus Error Detected Interrupt Enable:               0 = Disab */
        /* @SVD:field=RSVD2,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=MSDIE,bitOffset=12,bitWidth=1,description="Master Stop Detected Interrupt Enable:\n              0 = Disable interrupt.\n              1 = Enables the I2C unit to interrupt upon detecting a Master Stop sent by the I2C unit." */
        volatile uint32_t MSDIE: 1;  /* Master Stop Detected Interrupt Enable:               0 = Dis */
        /* @SVD:field=DMADONEIE,bitOffset=13,bitWidth=1,description="DMA Transaction Done Interrupt Enable\n              0 = DMA Transaction done interrupt is not enabled.\n              1 = DMA Transaction done interrupt is enabled." */
        volatile uint32_t DMADONEIE: 1;  /* DMA Transaction Done Interrupt Enable               0 = DMA  */
        /* @SVD:field=OFIE,bitOffset=14,bitWidth=1,description="FIFO Overflow Interrupt Enable\n              0 = FIFO Overflow interrupt is not enabled\n              1 = FIFO Overflow interrupt is enabled" */
        volatile uint32_t OFIE: 1;  /* FIFO Overflow Interrupt Enable               0 = FIFO Overfl */
        /* @SVD:field=UFIE,bitOffset=15,bitWidth=1,description="FIFO Underflow Interrupt Enable\n              0 = FIFO Underflow interrupt is not enabled\n              1 = FIFO Underflow interrupt is enabled" */
        volatile uint32_t UFIE: 1;  /* FIFO Underflow Interrupt Enable               0 = FIFO Under */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_IER_Type;

/* @SVD:register=SR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RWM,bitOffset=0,bitWidth=1,description="Read/write Mode:\n              0 = The I2C is in master-transmit or slave-receive mode. \n              1 = The I2C is in master-receive or slave-transmit mode. \n              This is the R/nW bit of the slave address. It is cleared automatically by hardware after a Stop state." */
        volatile uint32_t RWM: 1;  /* Read/write Mode:               0 = The I2C is in master-tran */
        /* @SVD:field=NACK,bitOffset=1,bitWidth=1,description="ACK/NACK Status:\n              0 = The I2C received or sent an ACK on the bus. \n              1 = The I2C received or sent a NACK.on the bus.\n              This bit is used in slave-transmit mode to determine when the byte transferred is the last one. This bit is updated after each byte and ACK/NACK information is received." */
        volatile uint32_t NACK: 1;  /* ACK/NACK Status:               0 = The I2C received or sent  */
        /* @SVD:field=UB,bitOffset=2,bitWidth=1,description="Unit Busy:\n              0 = I2C not busy.\n              1 = Set when local I2C is busy. This is defined as the time between the first Start and Stop." */
        volatile uint32_t UB: 1;  /* Unit Busy:               0 = I2C not busy.               1 = */
        /* @SVD:field=IBB,bitOffset=3,bitWidth=1,description="I2C Bus Busy:\n              0 = I2C bus is idle or the I2C is using the bus (that is, unit busy).\n              1 = Set when the I2C bus is busy but local I2C is not involved in the transaction." */
        volatile uint32_t IBB: 1;  /* I2C Bus Busy:               0 = I2C bus is idle or the I2C i */
        /* @SVD:field=SSD,bitOffset=4,bitWidth=1,description="Slave Stop Detected:\n              0 = No Stop detected.\n              1 = Set when the I2C detects a Stop while in slave-receive or slave-transmit mode. \n              Cleared if write 1" */
        volatile uint32_t SSD: 1;  /* Slave Stop Detected:               0 = No Stop detected.     */
        /* @SVD:field=ALD,bitOffset=5,bitWidth=1,description="Arbitration Loss Detected:\n              Used during multi-master operation:\n              0 = Cleared when arbitration is won or never took place.\n              1 = Set when the I2C loses arbitration. \n              Cleared if write 1" */
        volatile uint32_t ALD: 1;  /* Arbitration Loss Detected:               Used during multi-m */
        /* @SVD:field=TE,bitOffset=6,bitWidth=1,description="DBR Transmit Empty:\n              0 = The data byte is still being transmitted.\n              1 = The I2C has finished transmitting a data byte on the I2C bus. An interrupt is signalled when enabled in the CR.\n              Cleared if write 1" */
        volatile uint32_t TE: 1;  /* DBR Transmit Empty:               0 = The data byte is still */
        /* @SVD:field=RF,bitOffset=7,bitWidth=1,description="DBR Receive Full:\n              0 = The DBR has not received a new data byte or the I2C is idle.\n              1 = The DBR register received a new data byte from the I2C bus. An interrupt is signalled when enabled in the CR. \n              Cleared if write 1" */
        volatile uint32_t RF: 1;  /* DBR Receive Full:               0 = The DBR has not received */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SAD,bitOffset=9,bitWidth=1,description="Slave Address Detected:\n              0 = No slave address was detected.\n              1 = The I2C detected a seven-bit address that matches the general call address or SAR. An interrupt is signalled when enabled in the CR. \n              Cleared if write 1" */
        volatile uint32_t SAD: 1;  /* Slave Address Detected:               0 = No slave address w */
        /* @SVD:field=BED,bitOffset=10,bitWidth=1,description="Bus Error Detected:\n              0 = No error detected.\n              1 = The I2C sets this bit when it detects one of the following error conditions:\n              As a master transmitter, no ACK was detected on the interface after a byte was sent.\n              As a slave receiver, the I2C generates a NACK pulse.\n              When an error occurs, I2C bus transactions continue. Software must guarantee that misplaced Start and Stop conditions do not occur. \n              Cleared if write 1" */
        volatile uint32_t BED: 1;  /* Bus Error Detected:               0 = No error detected.     */
        /* @SVD:field=EBB,bitOffset=11,bitWidth=1,description="Early Bus Busy\n              0 = I2C bus is idle or the I2C is using the bus (that is, unit busy).\n              1 = Set when the unit detects that the SCL or SDA line is low without a START condition. Bit will remain set until the I2C unit detects the bus is idle by detecting a STOP condition. Bit will also be set whenever the IBB bit is set." */
        volatile uint32_t EBB: 1;  /* Early Bus Busy               0 = I2C bus is idle or the I2C  */
        /* @SVD:field=MSD,bitOffset=12,bitWidth=1,description="Master Stop Detected:\n              0 = No Master Stop Detected.\n              1 = This bit is set by the I2C unit when all of the following are true:\n              This bit is enabled (CR[MSDE] = 1);\n              I2C unit is configured as a master;\n              I2C transmits a STOP signal" */
        volatile uint32_t MSD: 1;  /* Master Stop Detected:               0 = No Master Stop Detec */
        /* @SVD:field=DMADONE,bitOffset=13,bitWidth=1,description="DMA Transaction Done. Asserted when both APB and I2C bus have finished transfer. Cleared if write 1" */
        volatile uint32_t DMADONE: 1;  /* DMA Transaction Done. Asserted when both APB and I2C bus hav */
        /* @SVD:field=OF,bitOffset=14,bitWidth=1,description="FIFO Overflow Flag. Asserted when FIFO is full and a PUSH request generated without a POP. Cleared if write 1" */
        volatile uint32_t OF: 1;  /* FIFO Overflow Flag. Asserted when FIFO is full and a PUSH re */
        /* @SVD:field=UF,bitOffset=15,bitWidth=1,description="FIFO Underflow Flag. Asserted when FIFO is empty and a POP request generated without a PUSH. Cleared if write 1" */
        volatile uint32_t UF: 1;  /* FIFO Underflow Flag. Asserted when FIFO is empty and a POP r */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_SR_Type;

/* @SVD:register=DBR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="Data Buffer register" */
/* DBR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=8,description="use the I2C Data Buffer register to transmit and receive data from the I2C bus. The DBR is accessed by software on one Side and by the I2C Shift register on the other. The DBR receives data coming into the I2C unit after a full byte is received and acknowledged. CPU writes data going out of the I2C to the DBR and sends it to the serial bus. \n              When the I2C is in transmit mode (master or slave), CPU writes data to the DBR over the internal bus. CPU write data to the DBR when a master transaction is initiated or when the DBR transmit-empty interrupt is signalled. Data moves from the DBR to the Shift register when the transfer byte bit is set. The DBR transmit-empty interrupt is signalled (if enabled) when a byte is transferred on the I2C bus and the acknowledge cycle is complete. If the DBR is not written, and a Stop condition is not in place before the I2C bus is ready to transfer the next byte packet, the I2C unit inserts wait states until CPU writes the DBR and sets the transfer byte bit.\n              When the I2C is in receive mode (master or slave), CPU reads DBR data over the internal bus. CPU reads data from the DBR when the DBR receive-full interrupt is signalled. The data moves from the Shift register to the DBR when the acknowledge cycle is complete. The I2C inserts wait states until the DBR is read. After the software reads the DBR, CR[NACK] are written by the software, allowing the next byte transfer to proceed to the I2C bus.\n              In DMA mode, DBR is automatically filled from FIFO in master transmit mode, or fetched and stored in FIFO in master receive mode until DMA done or aborted." */
        volatile uint32_t DATA: 8;  /* use the I2C Data Buffer register to transmit and receive dat */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_DBR_Type;

/* @SVD:register=SAR,offset=0x0014,size=32,access=read-write,reset=0x00000047,description="Slave Address Register" */
/* SAR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=7,description="The seven-bit address to which the I2C responds when in slave-receive mode" */
        volatile uint32_t ADDR: 7;  /* The seven-bit address to which the I2C responds when in slav */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_SAR_Type;

/* @SVD:register=LCR,offset=0x0018,size=32,access=read-write,reset=0x081C72ED,description="Load Count Register" */
/* LCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLV,bitOffset=0,bitWidth=9,description="Decrementer Load value for Standard Mode SCL (master mode) for both high  and  low phase. \n              Data rate is generated as Ffclk/(SLV+max(SLV,CNT*2+6)+7+DNF) approximately. \n              100kbps data rate is generated by default if fclk is 48MHz.\n              SLV also controls setup time and hold time for START and STOP condition in Standard Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*SLV" */
        volatile uint32_t SLV: 9;  /* Decrementer Load value for Standard Mode SCL (master mode) f */
        /* @SVD:field=FLV,bitOffset=9,bitWidth=9,description="Decrementer Load value for Fast Mode (or Fast Mode Plus) SCL (master mode) for both high and low phase. \n              Data rate is generated as Ffclk/(FLV+max(FLV,CNT*2+6)+7+DNF) approximately. \n              400kbps data rate is generated by default if fclk is 48MHz.\n              FLV also controls setup time and hold time for START and STOP condition in Fast Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*FLV" */
        volatile uint32_t FLV: 9;  /* Decrementer Load value for Fast Mode (or Fast Mode Plus) SCL */
        /* @SVD:field=HLVL,bitOffset=18,bitWidth=9,description="Decrementer Load value for High Speed Mode SCL (master mode) for low phase. \n              Tlow=Tfclk*(HLVL+3+DNF). Data rate is generated as 1/(Thigh+Tlow), or Ffclk/(HLVH+HLVL+7+2*DNF). \n              3.2Mbps data rate is generated by default if fclk is 48MHz.\n              HLVL also controls setup time and hold time for START and STOP condition in High Speed Mode(master mode). \n              Thdsta=Tsusta=Tsusto=Tfclk*(HLVL+1)" */
        volatile uint32_t HLVL: 9;  /* Decrementer Load value for High Speed Mode SCL (master mode) */
        /* @SVD:field=HLVH,bitOffset=27,bitWidth=5,description="Decrementer Load value for High Speed Mode SCL (master mode) for high phase. \n              Thigh=Tfclk*(HLVH+4+DNF)" */
        volatile uint32_t HLVH: 5;  /* Decrementer Load value for High Speed Mode SCL (master mode) */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_LCR_Type;

/* @SVD:register=WCR,offset=0x001C,size=32,access=read-write,reset=0x0000000A,description="Wait Count Register" */
/* WCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=8,description="Controls the counter values defining the setup and hold times in standard and fast mode\n              Tvddat=Thddat=Tfclk*(CNT+2)\n              Tsudat=max(Tlow-Thddat,Thddat)\n              Lower counter values may violate setup and hold times." */
        volatile uint32_t CNT: 8;  /* Controls the counter values defining the setup and hold time */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_WCR_Type;

/* @SVD:register=RCCR,offset=0x0020,size=32,access=read-write,reset=0x00000009,description="Bus Reset Cycle Counter Register" */
/* RCCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSTCYC,bitOffset=0,bitWidth=4,description="The cycles of SCL during bus reset" */
        volatile uint32_t RSTCYC: 4;  /* The cycles of SCL during bus reset */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_RCCR_Type;

/* @SVD:register=BMR,offset=0x0024,size=32,access=read-write,reset=0x00000003,description="Bus Monitor Register" */
/* BMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SDA,bitOffset=0,bitWidth=1,description="value of the SDA pin." */
        volatile uint32_t SDA: 1;  /* value of the SDA pin. */
        /* @SVD:field=SCL,bitOffset=1,bitWidth=1,description="value of the SCL pin. Software can check bus level when the I2C bus is hung and the I2C unit must be reset." */
        volatile uint32_t SCL: 1;  /* value of the SCL pin. Software can check bus level when the  */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_BMR_Type;

/* @SVD:register=DNR,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="DMA number register" */
/* DNR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=9,description="Write as number of data to transfer in byte. Read as left data number to transfer" */
        volatile uint32_t NDT: 9;  /* Write as number of data to transfer in byte. Read as left da */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_DNR_Type;

/* @SVD:register=FIFO,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="FIFO Register" */
/* FIFO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=8,description="Write to push send data into FIFO. Read to pop received data from FIFO" */
        volatile uint32_t DATA: 8;  /* Write to push send data into FIFO. Read to pop received data */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} I2C4_FIFO_Type;

/* I2C4 寄存器结构体 */
typedef struct
{
    I2C4_CR_Type CR;  /* 0x0000 - Control register */
    I2C4_TCR_Type TCR;  /* 0x0004 - Transfer Control register */
    I2C4_IER_Type IER;  /* 0x0008 - Interrupt Enable register */
    I2C4_SR_Type SR;  /* 0x000C - Status register */
    I2C4_DBR_Type DBR;  /* 0x0010 - Data Buffer register */
    I2C4_SAR_Type SAR;  /* 0x0014 - Slave Address Register */
    I2C4_LCR_Type LCR;  /* 0x0018 - Load Count Register */
    I2C4_WCR_Type WCR;  /* 0x001C - Wait Count Register */
    I2C4_RCCR_Type RCCR;  /* 0x0020 - Bus Reset Cycle Counter Register */
    I2C4_BMR_Type BMR;  /* 0x0024 - Bus Monitor Register */
    I2C4_DNR_Type DNR;  /* 0x0028 - DMA number register */
    /* @SVD:register=RSVD1,offset=0x002C,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x002C - RSVD1 */
    I2C4_FIFO_Type FIFO;  /* 0x0030 - FIFO Register */
} I2C4_TypeDef;

/* ============================================================================ */
/* HPSYS_GPIO 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=HPSYS_GPIO,base=0x500A0000,description="",groupName=HPSYS_GPIO */

#define HPSYS_GPIO_BASE                                    (0x500A0000UL)

/* @SVD:register=DIR0,offset=0x0000,size=32,access=read-write,reset=0x0,description="Data Input Register" */
/* DIR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IN,bitOffset=0,bitWidth=32,description="GPIO[31:0] input value" */
        volatile uint32_t IN: 32;  /* GPIO[31:0] input value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DIR0_Type;

/* @SVD:register=DOR0,offset=0x0004,size=32,access=read-write,reset=0x0,description="Data Output Register" */
/* DOR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OUT,bitOffset=0,bitWidth=32,description="GPIO[31:0] output value if output enabled" */
        volatile uint32_t OUT: 32;  /* GPIO[31:0] output value if output enabled */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOR0_Type;

/* @SVD:register=DOSR0,offset=0x0008,size=32,access=read-write,reset=0x0,description="Data Output  Set Register" */
/* DOSR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOS,bitOffset=0,bitWidth=32,description="set 1 to pull up output of corresponding GPIO[31:0]" */
        volatile uint32_t DOS: 32;  /* set 1 to pull up output of corresponding GPIO[31:0] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOSR0_Type;

/* @SVD:register=DOCR0,offset=0x000C,size=32,access=read-write,reset=0x0,description="Data Output  Clear Register" */
/* DOCR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOC,bitOffset=0,bitWidth=32,description="set 1 to  pull down output of corresponding GPIO[31:0]" */
        volatile uint32_t DOC: 32;  /* set 1 to  pull down output of corresponding GPIO[31:0] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOCR0_Type;

/* @SVD:register=DOER0,offset=0x0010,size=32,access=read-write,reset=0x0,description="Data Output Enable Register" */
/* DOER0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOE,bitOffset=0,bitWidth=32,description="GPIO[31:0] output enable" */
        volatile uint32_t DOE: 32;  /* GPIO[31:0] output enable */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOER0_Type;

/* @SVD:register=DOESR0,offset=0x0014,size=32,access=read-write,reset=0x0,description="Data Output Enable Set Register" */
/* DOESR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOES,bitOffset=0,bitWidth=32,description="set 1 to enable output of corresponding GPIO[31:0]" */
        volatile uint32_t DOES: 32;  /* set 1 to enable output of corresponding GPIO[31:0] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOESR0_Type;

/* @SVD:register=DOECR0,offset=0x0018,size=32,access=read-write,reset=0x0,description="Data Output Enable Clear Register" */
/* DOECR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOEC,bitOffset=0,bitWidth=32,description="set 1 to disable output of corresponding GPIO[31:0]" */
        volatile uint32_t DOEC: 32;  /* set 1 to disable output of corresponding GPIO[31:0] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOECR0_Type;

/* @SVD:register=IER0,offset=0x001C,size=32,access=read-write,reset=0x0,description="Interrupt Enable Register" */
/* IER0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IER,bitOffset=0,bitWidth=32,description="GPIO[31:0]  interrupt  enable" */
        volatile uint32_t IER: 32;  /* GPIO[31:0]  interrupt  enable */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IER0_Type;

/* @SVD:register=IESR0,offset=0x0020,size=32,access=read-write,reset=0x0,description="Interrupt Enable Set Register" */
/* IESR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IES,bitOffset=0,bitWidth=32,description="set 1 to enable interrupt of corresponding GPIO[31:0]" */
        volatile uint32_t IES: 32;  /* set 1 to enable interrupt of corresponding GPIO[31:0] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IESR0_Type;

/* @SVD:register=IECR0,offset=0x0024,size=32,access=read-write,reset=0x0,description="Interrupt Enable Clear Register" */
/* IECR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IEC,bitOffset=0,bitWidth=32,description="set 1 to disable interrupt of corresponding GPIO[31:0]" */
        volatile uint32_t IEC: 32;  /* set 1 to disable interrupt of corresponding GPIO[31:0] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IECR0_Type;

/* @SVD:register=ITR0,offset=0x0028,size=32,access=read-write,reset=0x0,description="Interrupt Type Register" */
/* ITR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ITR,bitOffset=0,bitWidth=32,description="GPIO[31:0]  interrupt  type" */
        volatile uint32_t ITR: 32;  /* GPIO[31:0]  interrupt  type */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_ITR0_Type;

/* @SVD:register=ITSR0,offset=0x002C,size=32,access=read-write,reset=0x0,description="Interrupt Type Set Register" */
/* ITSR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ITS,bitOffset=0,bitWidth=32,description="set 1 for edge-sensitive interrupt mode of corresponding GPIO[31:0]" */
        volatile uint32_t ITS: 32;  /* set 1 for edge-sensitive interrupt mode of corresponding GPI */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_ITSR0_Type;

/* @SVD:register=ITCR0,offset=0x0030,size=32,access=read-write,reset=0x0,description="Interrupt Type Clear Register" */
/* ITCR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ITC,bitOffset=0,bitWidth=32,description="set 1 for level-sensitive interrupt mode of corresponding GPIO[31:0]" */
        volatile uint32_t ITC: 32;  /* set 1 for level-sensitive interrupt mode of corresponding GP */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_ITCR0_Type;

/* @SVD:register=IPHR0,offset=0x0034,size=32,access=read-write,reset=0x0,description="Interrupt Polarity High Register" */
/* IPHR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPH,bitOffset=0,bitWidth=32,description="rising edge in edge mode, or high level in level mode of corresponding GPIO[31:0]" */
        volatile uint32_t IPH: 32;  /* rising edge in edge mode, or high level in level mode of cor */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPHR0_Type;

/* @SVD:register=IPHSR0,offset=0x0038,size=32,access=read-write,reset=0x0,description="Interrupt Polarity  High Set Register" */
/* IPHSR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPHS,bitOffset=0,bitWidth=32,description="set 1 for rising edge in edge mode, or high level in level mode of corresponding GPIO[31:0]" */
        volatile uint32_t IPHS: 32;  /* set 1 for rising edge in edge mode, or high level in level m */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPHSR0_Type;

/* @SVD:register=IPHCR0,offset=0x003C,size=32,access=read-write,reset=0x0,description="Interrupt Polarity  High Clear  Register" */
/* IPHCR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPHC,bitOffset=0,bitWidth=32,description="set 1 for disable rising edge in edge mode, or high level in level mode of corresponding GPIO[31:0]" */
        volatile uint32_t IPHC: 32;  /* set 1 for disable rising edge in edge mode, or high level in */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPHCR0_Type;

/* @SVD:register=IPLR0,offset=0x0040,size=32,access=read-write,reset=0x0,description="Interrupt Polarity Low Register" */
/* IPLR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPL,bitOffset=0,bitWidth=32,description="falling edge in edge mode, or low level in level mode of corresponding GPIO[31:0]" */
        volatile uint32_t IPL: 32;  /* falling edge in edge mode, or low level in level mode of cor */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPLR0_Type;

/* @SVD:register=IPLSR0,offset=0x0044,size=32,access=read-write,reset=0x0,description="Interrupt Polarity Low Set Register" */
/* IPLSR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPLS,bitOffset=0,bitWidth=32,description="set 1 for falling edge in edge mode, or low level in level mode of corresponding GPIO[31:0]" */
        volatile uint32_t IPLS: 32;  /* set 1 for falling edge in edge mode, or low level in level m */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPLSR0_Type;

/* @SVD:register=IPLCR0,offset=0x0048,size=32,access=read-write,reset=0x0,description="Interrupt Polarity Low Clear Register" */
/* IPLCR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPLC,bitOffset=0,bitWidth=32,description="set 1 for disable falling edge in edge mode, or low level in level mode of corresponding GPIO[31:0]" */
        volatile uint32_t IPLC: 32;  /* set 1 for disable falling edge in edge mode, or low level in */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPLCR0_Type;

/* @SVD:register=ISR0,offset=0x004C,size=32,access=read-write,reset=0x0,description="Interrupt Status Register" */
/* ISR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IS,bitOffset=0,bitWidth=32,description="Interrupt status. Write 1 will clear interrupt status of corresponding GPIO[31:0]" */
        volatile uint32_t IS: 32;  /* Interrupt status. Write 1 will clear interrupt status of cor */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_ISR0_Type;

/* @SVD:register=OEMR0,offset=0x0060,size=32,access=read-write,reset=0x0,description="output mode Register" */
/* OEMR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OEM,bitOffset=0,bitWidth=32,description="output mode of corresponding GPIO[31:0]" */
        volatile uint32_t OEM: 32;  /* output mode of corresponding GPIO[31:0] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_OEMR0_Type;

/* @SVD:register=OEMSR0,offset=0x0064,size=32,access=read-write,reset=0x0,description="output mode Set Register" */
/* OEMSR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OEMS,bitOffset=0,bitWidth=32,description="output mode Set of corresponding GPIO[31:0]" */
        volatile uint32_t OEMS: 32;  /* output mode Set of corresponding GPIO[31:0] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_OEMSR0_Type;

/* @SVD:register=OEMCR0,offset=0x0068,size=32,access=read-write,reset=0x0,description="output mode Clear Register" */
/* OEMCR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OEMC,bitOffset=0,bitWidth=32,description="output mode Clear of corresponding GPIO[31:0]" */
        volatile uint32_t OEMC: 32;  /* output mode Clear of corresponding GPIO[31:0] */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_OEMCR0_Type;

/* @SVD:register=DIR1,offset=0x0080,size=32,access=read-write,reset=0x0,description="Data Input Register" */
/* DIR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IN,bitOffset=0,bitWidth=13,description="GPIO[44:32] input value" */
        volatile uint32_t IN: 13;  /* GPIO[44:32] input value */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DIR1_Type;

/* @SVD:register=DOR1,offset=0x0084,size=32,access=read-write,reset=0x0,description="Data Output Register" */
/* DOR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OUT,bitOffset=0,bitWidth=13,description="GPIO[44:32] output value if output enabled" */
        volatile uint32_t OUT: 13;  /* GPIO[44:32] output value if output enabled */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOR1_Type;

/* @SVD:register=DOSR1,offset=0x0088,size=32,access=read-write,reset=0x0,description="Data Output  Set Register" */
/* DOSR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOS,bitOffset=0,bitWidth=13,description="set 1 to pull up output of corresponding GPIO[44:32]" */
        volatile uint32_t DOS: 13;  /* set 1 to pull up output of corresponding GPIO[44:32] */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOSR1_Type;

/* @SVD:register=DOCR1,offset=0x008C,size=32,access=read-write,reset=0x0,description="Data Output  Clear Register" */
/* DOCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOC,bitOffset=0,bitWidth=13,description="set 1 to  pull down output of corresponding GPIO[44:32]" */
        volatile uint32_t DOC: 13;  /* set 1 to  pull down output of corresponding GPIO[44:32] */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOCR1_Type;

/* @SVD:register=DOER1,offset=0x0090,size=32,access=read-write,reset=0x0,description="Data Output Enable Register" */
/* DOER1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOE,bitOffset=0,bitWidth=13,description="GPIO[44:32] output enable" */
        volatile uint32_t DOE: 13;  /* GPIO[44:32] output enable */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOER1_Type;

/* @SVD:register=DOESR1,offset=0x0094,size=32,access=read-write,reset=0x0,description="Data Output Enable Set Register" */
/* DOESR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOES,bitOffset=0,bitWidth=13,description="set 1 to enable output of corresponding GPIO[44:32]" */
        volatile uint32_t DOES: 13;  /* set 1 to enable output of corresponding GPIO[44:32] */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOESR1_Type;

/* @SVD:register=DOECR1,offset=0x0098,size=32,access=read-write,reset=0x0,description="Data Output Enable Clear Register" */
/* DOECR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DOEC,bitOffset=0,bitWidth=13,description="set 1 to disable output of corresponding GPIO[44:32]" */
        volatile uint32_t DOEC: 13;  /* set 1 to disable output of corresponding GPIO[44:32] */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_DOECR1_Type;

/* @SVD:register=IER1,offset=0x009C,size=32,access=read-write,reset=0x0,description="Interrupt Enable Register" */
/* IER1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IER,bitOffset=0,bitWidth=13,description="GPIO[44:32]  interrupt  enable" */
        volatile uint32_t IER: 13;  /* GPIO[44:32]  interrupt  enable */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IER1_Type;

/* @SVD:register=IESR1,offset=0x00A0,size=32,access=read-write,reset=0x0,description="Interrupt Enable Set Register" */
/* IESR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IES,bitOffset=0,bitWidth=13,description="set 1 to enable interrupt of corresponding GPIO[44:32]" */
        volatile uint32_t IES: 13;  /* set 1 to enable interrupt of corresponding GPIO[44:32] */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IESR1_Type;

/* @SVD:register=IECR1,offset=0x00A4,size=32,access=read-write,reset=0x0,description="Interrupt Enable Clear Register" */
/* IECR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IEC,bitOffset=0,bitWidth=13,description="set 1 to disable interrupt of corresponding GPIO[44:32]" */
        volatile uint32_t IEC: 13;  /* set 1 to disable interrupt of corresponding GPIO[44:32] */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IECR1_Type;

/* @SVD:register=ITR1,offset=0x00A8,size=32,access=read-write,reset=0x0,description="Interrupt Type Register" */
/* ITR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ITR,bitOffset=0,bitWidth=13,description="GPIO[44:32]  interrupt  type" */
        volatile uint32_t ITR: 13;  /* GPIO[44:32]  interrupt  type */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_ITR1_Type;

/* @SVD:register=ITSR1,offset=0x00AC,size=32,access=read-write,reset=0x0,description="Interrupt Type Set Register" */
/* ITSR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ITS,bitOffset=0,bitWidth=13,description="set 1 for edge-sensitive interrupt mode of corresponding GPIO[44:32]" */
        volatile uint32_t ITS: 13;  /* set 1 for edge-sensitive interrupt mode of corresponding GPI */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_ITSR1_Type;

/* @SVD:register=ITCR1,offset=0x00B0,size=32,access=read-write,reset=0x0,description="Interrupt Type Clear Register" */
/* ITCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ITC,bitOffset=0,bitWidth=13,description="set 1 for level-sensitive interrupt mode of corresponding GPIO[44:32]" */
        volatile uint32_t ITC: 13;  /* set 1 for level-sensitive interrupt mode of corresponding GP */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_ITCR1_Type;

/* @SVD:register=IPHR1,offset=0x00B4,size=32,access=read-write,reset=0x0,description="Interrupt Polarity High Register" */
/* IPHR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPH,bitOffset=0,bitWidth=13,description="rising edge in edge mode, or high level in level mode of corresponding GPIO[44:32]" */
        volatile uint32_t IPH: 13;  /* rising edge in edge mode, or high level in level mode of cor */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPHR1_Type;

/* @SVD:register=IPHSR1,offset=0x00B8,size=32,access=read-write,reset=0x0,description="Interrupt Polarity  High Set Register" */
/* IPHSR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPHS,bitOffset=0,bitWidth=13,description="set 1 for rising edge in edge mode, or high level in level mode of corresponding GPIO[44:32]" */
        volatile uint32_t IPHS: 13;  /* set 1 for rising edge in edge mode, or high level in level m */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPHSR1_Type;

/* @SVD:register=IPHCR1,offset=0x00BC,size=32,access=read-write,reset=0x0,description="Interrupt Polarity  High Clear  Register" */
/* IPHCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPHC,bitOffset=0,bitWidth=13,description="set 1 for disable rising edge in edge mode, or high level in level mode of corresponding GPIO[44:32]" */
        volatile uint32_t IPHC: 13;  /* set 1 for disable rising edge in edge mode, or high level in */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPHCR1_Type;

/* @SVD:register=IPLR1,offset=0x00C0,size=32,access=read-write,reset=0x0,description="Interrupt Polarity Low Register" */
/* IPLR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPL,bitOffset=0,bitWidth=13,description="falling edge in edge mode, or low level in level mode of corresponding GPIO[44:32]" */
        volatile uint32_t IPL: 13;  /* falling edge in edge mode, or low level in level mode of cor */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPLR1_Type;

/* @SVD:register=IPLSR1,offset=0x00C4,size=32,access=read-write,reset=0x0,description="Interrupt Polarity Low Set Register" */
/* IPLSR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPLS,bitOffset=0,bitWidth=13,description="set 1 for falling edge in edge mode, or low level in level mode of corresponding GPIO[44:32]" */
        volatile uint32_t IPLS: 13;  /* set 1 for falling edge in edge mode, or low level in level m */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPLSR1_Type;

/* @SVD:register=IPLCR1,offset=0x00C8,size=32,access=read-write,reset=0x0,description="Interrupt Polarity Low Clear Register" */
/* IPLCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IPLC,bitOffset=0,bitWidth=13,description="set 1 for disable falling edge in edge mode, or low level in level mode of corresponding GPIO[44:32]" */
        volatile uint32_t IPLC: 13;  /* set 1 for disable falling edge in edge mode, or low level in */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_IPLCR1_Type;

/* @SVD:register=ISR1,offset=0x00CC,size=32,access=read-write,reset=0x0,description="Interrupt Status Register" */
/* ISR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IS,bitOffset=0,bitWidth=13,description="Interrupt status. Write 1 will clear interrupt status of corresponding GPIO[44:32]" */
        volatile uint32_t IS: 13;  /* Interrupt status. Write 1 will clear interrupt status of cor */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_ISR1_Type;

/* @SVD:register=OEMR1,offset=0x00E0,size=32,access=read-write,reset=0x0,description="output mode Register" */
/* OEMR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OEM,bitOffset=0,bitWidth=13,description="output mode of corresponding GPIO[44:32]" */
        volatile uint32_t OEM: 13;  /* output mode of corresponding GPIO[44:32] */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_OEMR1_Type;

/* @SVD:register=OEMSR1,offset=0x00E4,size=32,access=read-write,reset=0x0,description="output mode Set Register" */
/* OEMSR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OEMS,bitOffset=0,bitWidth=13,description="output mode Set of corresponding GPIO[44:32]" */
        volatile uint32_t OEMS: 13;  /* output mode Set of corresponding GPIO[44:32] */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_OEMSR1_Type;

/* @SVD:register=OEMCR1,offset=0x00E8,size=32,access=read-write,reset=0x0,description="output mode Clear Register" */
/* OEMCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=OEMC,bitOffset=0,bitWidth=13,description="output mode Clear of corresponding GPIO[44:32]" */
        volatile uint32_t OEMC: 13;  /* output mode Clear of corresponding GPIO[44:32] */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_GPIO_OEMCR1_Type;

/* HPSYS_GPIO 寄存器结构体 */
typedef struct
{
    HPSYS_GPIO_DIR0_Type DIR0;  /* 0x0000 - Data Input Register */
    HPSYS_GPIO_DOR0_Type DOR0;  /* 0x0004 - Data Output Register */
    HPSYS_GPIO_DOSR0_Type DOSR0;  /* 0x0008 - Data Output  Set Register */
    HPSYS_GPIO_DOCR0_Type DOCR0;  /* 0x000C - Data Output  Clear Register */
    HPSYS_GPIO_DOER0_Type DOER0;  /* 0x0010 - Data Output Enable Register */
    HPSYS_GPIO_DOESR0_Type DOESR0;  /* 0x0014 - Data Output Enable Set Register */
    HPSYS_GPIO_DOECR0_Type DOECR0;  /* 0x0018 - Data Output Enable Clear Register */
    HPSYS_GPIO_IER0_Type IER0;  /* 0x001C - Interrupt Enable Register */
    HPSYS_GPIO_IESR0_Type IESR0;  /* 0x0020 - Interrupt Enable Set Register */
    HPSYS_GPIO_IECR0_Type IECR0;  /* 0x0024 - Interrupt Enable Clear Register */
    HPSYS_GPIO_ITR0_Type ITR0;  /* 0x0028 - Interrupt Type Register */
    HPSYS_GPIO_ITSR0_Type ITSR0;  /* 0x002C - Interrupt Type Set Register */
    HPSYS_GPIO_ITCR0_Type ITCR0;  /* 0x0030 - Interrupt Type Clear Register */
    HPSYS_GPIO_IPHR0_Type IPHR0;  /* 0x0034 - Interrupt Polarity High Register */
    HPSYS_GPIO_IPHSR0_Type IPHSR0;  /* 0x0038 - Interrupt Polarity  High Set Register */
    HPSYS_GPIO_IPHCR0_Type IPHCR0;  /* 0x003C - Interrupt Polarity  High Clear  Register */
    HPSYS_GPIO_IPLR0_Type IPLR0;  /* 0x0040 - Interrupt Polarity Low Register */
    HPSYS_GPIO_IPLSR0_Type IPLSR0;  /* 0x0044 - Interrupt Polarity Low Set Register */
    HPSYS_GPIO_IPLCR0_Type IPLCR0;  /* 0x0048 - Interrupt Polarity Low Clear Register */
    HPSYS_GPIO_ISR0_Type ISR0;  /* 0x004C - Interrupt Status Register */
    /* @SVD:register=RSVD3,offset=0x0050,size=32,access=read-write,reset=0x0,description="RSVD3" */
    volatile uint32_t RSVD3;  /* 0x0050 - RSVD3 */
    volatile uint32_t RESERVED21[3];  /* 0x0054 - 0x005F */
    HPSYS_GPIO_OEMR0_Type OEMR0;  /* 0x0060 - output mode Register */
    HPSYS_GPIO_OEMSR0_Type OEMSR0;  /* 0x0064 - output mode Set Register */
    HPSYS_GPIO_OEMCR0_Type OEMCR0;  /* 0x0068 - output mode Clear Register */
    /* @SVD:register=RSVD2,offset=0x006C,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x006C - RSVD2 */
    volatile uint32_t RESERVED28[4];  /* 0x0070 - 0x007F */
    HPSYS_GPIO_DIR1_Type DIR1;  /* 0x0080 - Data Input Register */
    HPSYS_GPIO_DOR1_Type DOR1;  /* 0x0084 - Data Output Register */
    HPSYS_GPIO_DOSR1_Type DOSR1;  /* 0x0088 - Data Output  Set Register */
    HPSYS_GPIO_DOCR1_Type DOCR1;  /* 0x008C - Data Output  Clear Register */
    HPSYS_GPIO_DOER1_Type DOER1;  /* 0x0090 - Data Output Enable Register */
    HPSYS_GPIO_DOESR1_Type DOESR1;  /* 0x0094 - Data Output Enable Set Register */
    HPSYS_GPIO_DOECR1_Type DOECR1;  /* 0x0098 - Data Output Enable Clear Register */
    HPSYS_GPIO_IER1_Type IER1;  /* 0x009C - Interrupt Enable Register */
    HPSYS_GPIO_IESR1_Type IESR1;  /* 0x00A0 - Interrupt Enable Set Register */
    HPSYS_GPIO_IECR1_Type IECR1;  /* 0x00A4 - Interrupt Enable Clear Register */
    HPSYS_GPIO_ITR1_Type ITR1;  /* 0x00A8 - Interrupt Type Register */
    HPSYS_GPIO_ITSR1_Type ITSR1;  /* 0x00AC - Interrupt Type Set Register */
    HPSYS_GPIO_ITCR1_Type ITCR1;  /* 0x00B0 - Interrupt Type Clear Register */
    HPSYS_GPIO_IPHR1_Type IPHR1;  /* 0x00B4 - Interrupt Polarity High Register */
    HPSYS_GPIO_IPHSR1_Type IPHSR1;  /* 0x00B8 - Interrupt Polarity  High Set Register */
    HPSYS_GPIO_IPHCR1_Type IPHCR1;  /* 0x00BC - Interrupt Polarity  High Clear  Register */
    HPSYS_GPIO_IPLR1_Type IPLR1;  /* 0x00C0 - Interrupt Polarity Low Register */
    HPSYS_GPIO_IPLSR1_Type IPLSR1;  /* 0x00C4 - Interrupt Polarity Low Set Register */
    HPSYS_GPIO_IPLCR1_Type IPLCR1;  /* 0x00C8 - Interrupt Polarity Low Clear Register */
    HPSYS_GPIO_ISR1_Type ISR1;  /* 0x00CC - Interrupt Status Register */
    /* @SVD:register=RSVD1,offset=0x00D0,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x00D0 - RSVD1 */
    volatile uint32_t RESERVED53[3];  /* 0x00D4 - 0x00DF */
    HPSYS_GPIO_OEMR1_Type OEMR1;  /* 0x00E0 - output mode Register */
    HPSYS_GPIO_OEMSR1_Type OEMSR1;  /* 0x00E4 - output mode Set Register */
    HPSYS_GPIO_OEMCR1_Type OEMCR1;  /* 0x00E8 - output mode Clear Register */
} HPSYS_GPIO_TypeDef;

/* ============================================================================ */
/* GPTIM2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=GPTIM2,base=0x500B0000,description="",groupName=GPTIM */

#define GPTIM2_BASE                                        (0x500B0000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="TIM control register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CEN,bitOffset=0,bitWidth=1,description="Counter enable\n              0: Counter disabled\n              1: Counter enabled\n              Note: External clock, gated mode and encoder mode can work only if the CEN bit has been previously set by software. However trigger mode can set the CEN bit automatically by hardware. \n              CEN is cleared automatically in one-pulse mode, when an update event occurs." */
        volatile uint32_t CEN: 1;  /* Counter enable               0: Counter disabled             */
        /* @SVD:field=UDIS,bitOffset=1,bitWidth=1,description="Update disable\n              This bit is set and cleared by software to enable/disable UEV event generation.\n              0: UEV enabled. The Update (UEV) event is generated by one of the following events:\n                 Counter overflow/underflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              Buffered registers are then loaded with their preload values.\n              1: UEV disabled. The Update event is not generated, shadow registers keep their value (ARR, PSC, CCRx). However the counter and the prescaler are reinitialized if the UG bit is set or if a hardware reset is received from the slave mode controller." */
        volatile uint32_t UDIS: 1;  /* Update disable               This bit is set and cleared by  */
        /* @SVD:field=URS,bitOffset=2,bitWidth=1,description="Update request source\n              This bit is set and cleared by software to select the UEV event sources.\n              0: Any of the following events generate an update interrupt or DMA request if enabled. \n              These events can be: \n                 Counter overflow/underflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              1: Only counter overflow/underflow generates an update interrupt or DMA request if enabled." */
        volatile uint32_t URS: 1;  /* Update request source               This bit is set and clea */
        /* @SVD:field=OPM,bitOffset=3,bitWidth=1,description="One-pulse mode\n              0: Counter is not stopped at update event\n              1: Counter stops counting at the next update event (clearing the bit CEN)" */
        volatile uint32_t OPM: 1;  /* One-pulse mode               0: Counter is not stopped at up */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="Direction\n              0: Counter used as upcounter\n              1: Counter used as downcounter" */
        volatile uint32_t DIR: 1;  /* Direction               0: Counter used as upcounter         */
        /* @SVD:field=CMS,bitOffset=5,bitWidth=2,description="Center-aligned mode selection\n              00: Edge-aligned mode. The counter counts up or down depending on the direction bit (DIR).\n              01: Center-aligned mode 1. The counter counts up and down alternatively. Output compare interrupt flags of channels configured in output (CCxS=00 in CCMRx register) are set only when the counter is counting down.\n              10: Center-aligned mode 2. The counter counts up and down alternatively. Output compare interrupt flags of channels configured in output (CCxS=00 in CCMRx register) are set only when the counter is counting up.\n              11: Center-aligned mode 3. The counter counts up and down alternatively. Output compare interrupt flags of channels configured in output (CCxS=00 in CCMRx register) are set both when the counter is counting up or down." */
        volatile uint32_t CMS: 2;  /* Center-aligned mode selection               00: Edge-aligned */
        /* @SVD:field=ARPE,bitOffset=7,bitWidth=1,description="Auto-reload preload enable\n              0: ARR register is not buffered\n              1: ARR register is buffered" */
        volatile uint32_t ARPE: 1;  /* Auto-reload preload enable               0: ARR register is  */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=UIFREMAP,bitOffset=11,bitWidth=1,description="UIF status bit remapping\n              0: No remapping. UIF status bit is not copied to CNT register bit 31\n              1: Remapping enabled. UIF status bit is copied to CNT register bit 31" */
        volatile uint32_t UIFREMAP: 1;  /* UIF status bit remapping               0: No remapping. UIF  */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="TIM control register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=CCDS,bitOffset=3,bitWidth=1,description="Capture/compare DMA selection\n              0: CCx DMA request sent when CCx event occurs\n              1: CCx DMA requests sent when update event occurs" */
        volatile uint32_t CCDS: 1;  /* Capture/compare DMA selection               0: CCx DMA reque */
        /* @SVD:field=MMS,bitOffset=4,bitWidth=3,description="Master mode selection\n              These bits allow to select the information to be sent in master mode to slave timers for synchronization (TRGO). The combination is as follows:\n              000: Reset - the UG bit from the EGR register is used as trigger output (TRGO). If the reset is generated by the trigger input (slave mode controller configured in reset mode) then the signal on TRGO is delayed compared to the actual reset.\n              001: Enable - the Counter enable signal is used as trigger output (TRGO). It is useful to start several timers at the same time or to control a window in which a slave timer is enabled. The Counter Enable signal is generated by a logic OR between CEN control bit and the trigger input when configured in gated mode. \n              When the Counter Enable signal is controlled by the trigger input, there is a delay on TRGO, except if the master/slave mode is selected.\n              010: Update - The update event is selected as trigger output (TRGO). For instance a master timer can then be used as a prescaler for a slave timer.\n              011: Compare Pulse - The trigger output send a positive pulse when the CC1IF flag is to be set (even if it was already high), as soon as a capture or a compare match occurred. (TRGO)\n              100: Compare - OC1REF signal is used as trigger output (TRGO)\n              101: Compare - OC2REF signal is used as trigger output (TRGO)\n              110: Compare - OC3REF signal is used as trigger output (TRGO)\n              111: Compare - OC4REF signal is used as trigger output (TRGO)" */
        volatile uint32_t MMS: 3;  /* Master mode selection               These bits allow to sele */
        /* @SVD:field=TI1S,bitOffset=7,bitWidth=1,description="TI1 selection\n              0: The CH1 pin is connected to TI1 input\n              1: The CH1, CH2 and CH3 pins are connected to the TI1 input (XOR combination)" */
        volatile uint32_t TI1S: 1;  /* TI1 selection               0: The CH1 pin is connected to T */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CR2_Type;

/* @SVD:register=SMCR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="TIM slave mode control register" */
/* SMCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=TS,bitOffset=4,bitWidth=3,description="Trigger selection\n              This bit-field selects the trigger input to be used to synchronize the counter.\n              000: Internal Trigger 0 (ITR0)\n              001: Internal Trigger 1 (ITR1) \n              010: Internal Trigger 2 (ITR2)\n              011: Internal Trigger 3 (ITR3)\n              100: TI1 Edge Detector (TI1F_ED)\n              101: Filtered Timer Input 1 (TI1FP1)\n              110: Filtered Timer Input 2 (TI2FP2)\n              111: External Trigger input (ETRF)" */
        volatile uint32_t TS: 3;  /* Trigger selection               This bit-field selects the t */
        /* @SVD:field=MSM,bitOffset=7,bitWidth=1,description="Master/Slave mode\n              0: No action\n              1: The effect of an event on the trigger input (TRGI) is delayed to allow a perfect synchronization between the current timer and its slaves (through TRGO). It is useful if we want to synchronize several timers on a single external event." */
        volatile uint32_t MSM: 1;  /* Master/Slave mode               0: No action               1 */
        /* @SVD:field=ETF,bitOffset=8,bitWidth=4,description="External trigger filter\n              This bit-field then defines the frequency used to sample ETRP signal and the length of the digital filter applied to ETRP. The digital filter is made of an event counter in which N \n              consecutive events are needed to validate a transition on the output:\n              0000: No filter\n              0001:   fSAMPLING=fCLK, N=2\n              0010:   fSAMPLING=fCLK, N=4\n              0011:   fSAMPLING=fCLK, N=8\n              0100:   fSAMPLING=fCLK/2, N=6\n              0101:   fSAMPLING=fCLK/2, N=8\n              0110:   fSAMPLING=fCLK/4, N=6\n              0111:   fSAMPLING=fCLK/4, N=8\n              1000:   fSAMPLING=fCLK/8, N=6\n              1001:   fSAMPLING=fCLK/8, N=8\n              1010:   fSAMPLING=fCLK/16, N=5\n              1011:   fSAMPLING=fCLK/16, N=6\n              1100:   fSAMPLING=fCLK/16, N=8\n              1101:   fSAMPLING=fCLK/32, N=5\n              1110:   fSAMPLING=fCLK/32, N=6\n              1111:   fSAMPLING=fCLK/32, N=8" */
        volatile uint32_t ETF: 4;  /* External trigger filter               This bit-field then de */
        /* @SVD:field=ETPS,bitOffset=12,bitWidth=2,description="External trigger prescaler\n              External trigger signal ETRP frequency must be at most 1/4 of CK_INT frequency. A prescaler can be enabled to reduce ETRP frequency. It is useful when inputting fast external clocks.\n              00: Prescaler OFF\n              01: ETRP frequency divided by 2\n              10: ETRP frequency divided by 4\n              11: ETRP frequency divided by 8" */
        volatile uint32_t ETPS: 2;  /* External trigger prescaler               External trigger si */
        /* @SVD:field=ECE,bitOffset=14,bitWidth=1,description="External clock enable\n              This bit enables External clock mode 2.\n              0: External clock mode 2 disabled\n              1: External clock mode 2 enabled. The counter is clocked by any active edge on the ETRF signal." */
        volatile uint32_t ECE: 1;  /* External clock enable               This bit enables Externa */
        /* @SVD:field=ETP,bitOffset=15,bitWidth=1,description="External trigger polarity\n              0: ETR is non-inverted, active at high level or rising edge\n              1: ETR is inverted, active at low level or falling edge" */
        volatile uint32_t ETP: 1;  /* External trigger polarity               0: ETR is non-invert */
        /* @SVD:field=SMS,bitOffset=16,bitWidth=4,description="Slave mode selection\n              When external signals are selected the active edge of the trigger signal (TRGI) is linked to the polarity selected on the external input.\n              0000: Slave mode disabled.\n              0001: Encoder mode 1 - Counter counts up/down on TI1FP1 edge depending on TI2FP2 level.\n              0010: Encoder mode 2 - Counter counts up/down on TI2FP2 edge depending on TI1FP1 level.\n              0011: Encoder mode 3 - Counter counts up/down on both TI1FP1 and TI2FP2 edges depending on the level of the other input.\n              0100: Reset Mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter and generates an update of the registers.\n              0101: Gated Mode - The counter clock is enabled when the trigger input (TRGI) is high. The counter stops (but is not reset) as soon as the trigger becomes low. Both start and stop of the counter are controlled.\n              0110: Trigger Mode - The counter starts at a rising edge of the trigger TRGI (but it is not reset). Only the start of the counter is controlled.\n              0111: External Clock Mode 1 - Rising edges of the selected trigger (TRGI) clock the counter.\n              1000: Combined reset + trigger mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter, generates an update of the registers and starts the counter." */
        volatile uint32_t SMS: 4;  /* Slave mode selection               When external signals are */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_SMCR_Type;

/* @SVD:register=DIER,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="TIM DMA/Interrupt enable register" */
/* DIER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIE,bitOffset=0,bitWidth=1,description="Update interrupt enable\n              0: Update interrupt disabled.\n              1: Update interrupt enabled" */
        volatile uint32_t UIE: 1;  /* Update interrupt enable               0: Update interrupt di */
        /* @SVD:field=CC1IE,bitOffset=1,bitWidth=1,description="Capture/Compare 1 interrupt enable\n              0: CC1 interrupt disabled.\n              1: CC1 interrupt enabled" */
        volatile uint32_t CC1IE: 1;  /* Capture/Compare 1 interrupt enable               0: CC1 inte */
        /* @SVD:field=CC2IE,bitOffset=2,bitWidth=1,description="Capture/Compare 2 interrupt enable\n              0: CC2 interrupt disabled.\n              1: CC2 interrupt enabled." */
        volatile uint32_t CC2IE: 1;  /* Capture/Compare 2 interrupt enable               0: CC2 inte */
        /* @SVD:field=CC3IE,bitOffset=3,bitWidth=1,description="Capture/Compare 3 interrupt enable\n              0: CC3 interrupt disabled.\n              1: CC3 interrupt enabled" */
        volatile uint32_t CC3IE: 1;  /* Capture/Compare 3 interrupt enable               0: CC3 inte */
        /* @SVD:field=CC4IE,bitOffset=4,bitWidth=1,description="Capture/Compare 4 interrupt enable\n              0: CC4 interrupt disabled.\n              1: CC4 interrupt enabled" */
        volatile uint32_t CC4IE: 1;  /* Capture/Compare 4 interrupt enable               0: CC4 inte */
        /* @SVD:field=RSVD4,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=TIE,bitOffset=6,bitWidth=1,description="Trigger interrupt enable\n              0: Trigger interrupt disabled.\n              1: Trigger interrupt enabled" */
        volatile uint32_t TIE: 1;  /* Trigger interrupt enable               0: Trigger interrupt  */
        /* @SVD:field=RSVD3,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=UDE,bitOffset=8,bitWidth=1,description="Update DMA request enable\n              0: Update DMA request disabled.\n              1: Update DMA request enabled" */
        volatile uint32_t UDE: 1;  /* Update DMA request enable               0: Update DMA reques */
        /* @SVD:field=CC1DE,bitOffset=9,bitWidth=1,description="Capture/Compare 1 DMA request enable\n              0: CC1 DMA request disabled.\n              1: CC1 DMA request enabled." */
        volatile uint32_t CC1DE: 1;  /* Capture/Compare 1 DMA request enable               0: CC1 DM */
        /* @SVD:field=CC2DE,bitOffset=10,bitWidth=1,description="Capture/Compare 2 DMA request enable\n              0: CC2 DMA request disabled.\n              1: CC2 DMA request enabled." */
        volatile uint32_t CC2DE: 1;  /* Capture/Compare 2 DMA request enable               0: CC2 DM */
        /* @SVD:field=CC3DE,bitOffset=11,bitWidth=1,description="Capture/Compare 3 DMA request enable\n              0: CC3 DMA request disabled.\n              1: CC3 DMA request enabled." */
        volatile uint32_t CC3DE: 1;  /* Capture/Compare 3 DMA request enable               0: CC3 DM */
        /* @SVD:field=CC4DE,bitOffset=12,bitWidth=1,description="Capture/Compare 4 DMA request enable\n              0: CC4 DMA request disabled.\n              1: CC4 DMA request enabled" */
        volatile uint32_t CC4DE: 1;  /* Capture/Compare 4 DMA request enable               0: CC4 DM */
        /* @SVD:field=RSVD2,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TDE,bitOffset=14,bitWidth=1,description="Trigger DMA request enable\n              0: Trigger DMA request disabled.\n              1: Trigger DMA request enabled." */
        volatile uint32_t TDE: 1;  /* Trigger DMA request enable               0: Trigger DMA requ */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_DIER_Type;

/* @SVD:register=SR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="TIM status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIF,bitOffset=0,bitWidth=1,description="Update interrupt flag\n              This bit is set by hardware on an update event. It is cleared by software.\n              0: No update occurred\n              1: Update interrupt pending. This bit is set by hardware when the registers are updated:\n              At overflow or underflow and if UDIS=0 in the CR1 register.\n              When CNT is reinitialized by software using the UG bit in EGR register, if URS=0 and UDIS=0 in the CR1 register.\n              When CNT is reinitialized by a trigger event, if URS=0 and UDIS=0 in the CR1 register." */
        volatile uint32_t UIF: 1;  /* Update interrupt flag               This bit is set by hardw */
        /* @SVD:field=CC1IF,bitOffset=1,bitWidth=1,description="Capture/Compare 1 interrupt flag\n              If channel CC1 is configured as output: This flag is set by hardware when the counter matches the compare value. It is cleared by software.\n              0: No match.\n              1: The content of the counter CNT has matched the content of the CCR1 register.\n              If channel CC1 is configured as input: This bit is set by hardware on a capture. It is cleared by software or by reading the CCR1 register.\n              0: No input capture occurred.\n              1: The counter value has been captured in CCR1 register (An edge has been detected on IC1 which matches the selected polarity)." */
        volatile uint32_t CC1IF: 1;  /* Capture/Compare 1 interrupt flag               If channel CC */
        /* @SVD:field=CC2IF,bitOffset=2,bitWidth=1,description="Capture/Compare 2 interrupt flag" */
        volatile uint32_t CC2IF: 1;  /* Capture/Compare 2 interrupt flag */
        /* @SVD:field=CC3IF,bitOffset=3,bitWidth=1,description="Capture/Compare 3 interrupt flag" */
        volatile uint32_t CC3IF: 1;  /* Capture/Compare 3 interrupt flag */
        /* @SVD:field=CC4IF,bitOffset=4,bitWidth=1,description="Capture/Compare 4 interrupt flag" */
        volatile uint32_t CC4IF: 1;  /* Capture/Compare 4 interrupt flag */
        /* @SVD:field=RSVD3,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=TIF,bitOffset=6,bitWidth=1,description="Trigger interrupt flag\n              This flag is set by hardware on trigger event (active edge detected on TRGI input when the slave mode controller is enabled in all modes but gated mode). It is set when the counter starts or stops when gated mode is selected. It is cleared by software.\n              0: No trigger event occurred.\n              1: Trigger interrupt pending." */
        volatile uint32_t TIF: 1;  /* Trigger interrupt flag               This flag is set by har */
        /* @SVD:field=RSVD2,bitOffset=7,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=CC1OF,bitOffset=9,bitWidth=1,description="Capture/Compare 1 overcapture flag\n              This flag is set by hardware only when the corresponding channel is configured in input capture mode. It is cleared by software by writing it to '0'.\n              0: No overcapture has been detected.\n              1: The counter value has been captured in CCR1 register while CC1IF flag was already set" */
        volatile uint32_t CC1OF: 1;  /* Capture/Compare 1 overcapture flag               This flag i */
        /* @SVD:field=CC2OF,bitOffset=10,bitWidth=1,description="Capture/Compare 2 overcapture flag" */
        volatile uint32_t CC2OF: 1;  /* Capture/Compare 2 overcapture flag */
        /* @SVD:field=CC3OF,bitOffset=11,bitWidth=1,description="Capture/Compare 3 overcapture flag" */
        volatile uint32_t CC3OF: 1;  /* Capture/Compare 3 overcapture flag */
        /* @SVD:field=CC4OF,bitOffset=12,bitWidth=1,description="Capture/Compare 4 overcapture flag" */
        volatile uint32_t CC4OF: 1;  /* Capture/Compare 4 overcapture flag */
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_SR_Type;

/* @SVD:register=EGR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Event generation register" */
/* EGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UG,bitOffset=0,bitWidth=1,description="Update generation\n              This bit can be set by software, it is automatically cleared by hardware.\n              0: No action\n              1: Re-initialize the counter and generates an update of the registers. Note that the prescaler counter is cleared too (anyway the prescaler ratio is not affected). The counter is cleared if the center-aligned mode is selected or if DIR=0 (upcounting), else it takes the auto-reload value (ARR) if DIR=1 (downcounting)." */
        volatile uint32_t UG: 1;  /* Update generation               This bit can be set by softw */
        /* @SVD:field=CC1G,bitOffset=1,bitWidth=1,description="Capture/compare 1 generation\n              This bit is set by software in order to generate an event, it is automatically cleared by hardware.\n              0: No action\n              1: A capture/compare event is generated on channel 1:\n              If channel CC1 is configured as output:\n              CC1IF flag is set, Corresponding interrupt or DMA request is sent if enabled.\n              If channel CC1 is configured as input:\n              The current value of the counter is captured in CCR1 register. The CC1IF flag is set, the corresponding interrupt or DMA request is sent if enabled. The CC1OF flag is set if the CC1IF flag was already high." */
        volatile uint32_t CC1G: 1;  /* Capture/compare 1 generation               This bit is set b */
        /* @SVD:field=CC2G,bitOffset=2,bitWidth=1,description="Capture/compare 2 generation" */
        volatile uint32_t CC2G: 1;  /* Capture/compare 2 generation */
        /* @SVD:field=CC3G,bitOffset=3,bitWidth=1,description="Capture/compare 3 generation" */
        volatile uint32_t CC3G: 1;  /* Capture/compare 3 generation */
        /* @SVD:field=CC4G,bitOffset=4,bitWidth=1,description="Capture/compare 4 generation" */
        volatile uint32_t CC4G: 1;  /* Capture/compare 4 generation */
        /* @SVD:field=RSVD2,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=TG,bitOffset=6,bitWidth=1,description="Trigger generation\n              This bit is set by software in order to generate an event, it is automatically cleared by hardware.\n              0: No action\n              1: The TIF flag is set in SR register. Related interrupt or DMA transfer can occur if enabled." */
        volatile uint32_t TG: 1;  /* Trigger generation               This bit is set by software */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_EGR_Type;

/* @SVD:register=CCMR1,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="TIM capture/compare mode register 1" */
/* CCMR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CC1S,bitOffset=0,bitWidth=2,description="Capture/Compare 1 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC1 channel is configured as output\n              01: CC1 channel is configured as input, IC1 is mapped on TI1\n              10: CC1 channel is configured as input, IC1 is mapped on TI2\n              11: CC1 channel is configured as input, IC1 is mapped on TRC. This mode is working only if an internal trigger input is selected through TS bit (SMCR register)" */
        volatile uint32_t CC1S: 2;  /* Capture/Compare 1 selection               This bit-field def */
        /* @SVD:field=IC1PSC,bitOffset=2,bitWidth=2,description="Input capture 1 prescaler\n              This bit-field defines the ratio of the prescaler acting on CC1 input (IC1). The prescaler is reset as soon as CC1E=0.\n              00: no prescaler, capture is done each time an edge is detected on the capture input\n              01: capture is done once every 2 events\n              10: capture is done once every 4 events\n              11: capture is done once every 8 events" */
        volatile uint32_t IC1PSC: 2;  /* Input capture 1 prescaler               This bit-field defin */
        /* @SVD:field=IC1F,bitOffset=4,bitWidth=4,description="Input capture 1 filter\n              This bit-field defines the frequency used to sample TI1 input and the length of the digital filter applied to TI1. The digital filter is made of an event counter in which N consecutive events are needed to validate a transition on the output:\n              0000: No filter, sampling is done at fCLK\n              0001: fSAMPLING=fCLK, N=2\n              0010: fSAMPLING=fCLK, N=4\n              0011: fSAMPLING=fCLK, N=8\n              0100: fSAMPLING=fCLK/2, N=6\n              0101: fSAMPLING=fCLK/2, N=8\n              0110: fSAMPLING=fCLK/4, N=6\n              0111: fSAMPLING=fCLK/4, N=8\n              1000: fSAMPLING=fCLK/8, N=6\n              1001: fSAMPLING=fCLK/8, N=8\n              1010: fSAMPLING=fCLK/16, N=5\n              1011: fSAMPLING=fCLK/16, N=6\n              1100: fSAMPLING=fCLK/16, N=8\n              1101: fSAMPLING=fCLK/32, N=5\n              1110: fSAMPLING=fCLK/32, N=6\n              1111: fSAMPLING=fCLK/32, N=8" */
        volatile uint32_t IC1F: 4;  /* Input capture 1 filter               This bit-field defines  */
        /* @SVD:field=CC2S,bitOffset=8,bitWidth=2,description="Capture/Compare 2 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC2 channel is configured as output\n              01: CC2 channel is configured as input, IC2 is mapped on TI2\n              10: CC2 channel is configured as input, IC2 is mapped on TI1\n              11: CC2 channel is configured as input, IC2 is mapped on TRC. This mode is working only if an internal trigger input is selected through the TS bit (SMCR register)" */
        volatile uint32_t CC2S: 2;  /* Capture/Compare 2 selection               This bit-field def */
        /* @SVD:field=IC2PSC,bitOffset=10,bitWidth=2,description="Input capture 2 prescaler" */
        volatile uint32_t IC2PSC: 2;  /* Input capture 2 prescaler */
        /* @SVD:field=IC2F,bitOffset=12,bitWidth=4,description="Input capture 2 filter" */
        volatile uint32_t IC2F: 4;  /* Input capture 2 filter */
        /* @SVD:field=OC1CE,bitOffset=16,bitWidth=1,description="Output compare 1 clear enable\n              0: OC1Ref is not affected by the ETRF input\n              1: OC1Ref is cleared as soon as a High level is detected on ETRF input" */
        volatile uint32_t OC1CE: 1;  /* Output compare 1 clear enable               0: OC1Ref is not */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=OC1PE,bitOffset=19,bitWidth=1,description="Output compare 1 preload enable\n              0: Preload register on CCR1 disabled. CCR1 can be written at anytime, the new value is taken in account immediately.\n              1: Preload register on CCR1 enabled. Read/Write operations access the preload register. CCR1 preload value is loaded in the active register at each update event." */
        volatile uint32_t OC1PE: 1;  /* Output compare 1 preload enable               0: Preload reg */
        /* @SVD:field=OC1M,bitOffset=20,bitWidth=4,description="Output compare 1 mode\n              These bits define the behavior of the output reference signal OC1REF from which OC1 and OC1N are derived. OC1REF is active high whereas OC1 and OC1N active level depends on CC1P and CC1NP bits.\n              0000: Frozen - The comparison between the output compare register CCR1 and the counter CNT has no effect on the outputs.(this mode is used to generate a timing base).\n              0001: Set channel 1 to active level on match. OC1REF signal is forced high when the counter CNT matches the capture/compare register 1 (CCR1).\n              0010: Set channel 1 to inactive level on match. OC1REF signal is forced low when the counter CNT matches the capture/compare register 1 (CCR1).\n              0011: Toggle - OC1REF toggles when CNT=CCR1.\n              0100: Force inactive level - OC1REF is forced low.\n              0101: Force active level - OC1REF is forced high.\n              0110: PWM mode 1 - In upcounting, channel 1 is active as long as CNT(CCR1 else inactive. In downcounting, channel 1 is inactive (OC1REF='0) as long as CNT>CCR1 else active (OC1REF=1).\n              0111: PWM mode 2 - In upcounting, channel 1 is inactive as long as CNT(CCR1 else active. In downcounting, channel 1 is active as long as CNT>CCR1 else inactive.\n              1000: Retriggerable OPM mode 1 - In up-counting mode, the channel is active until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 1 and the channels becomes inactive again at the next update. In down-counting mode, the channel is inactive until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 1 and the channels becomes inactive again at the next update.\n              1001: Retriggerable OPM mode 2 - In up-counting mode, the channel is inactive until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 2 and the channels becomes inactive again at the next update. In down-counting mode, the channel is active until a trigger event is detected (on TRGI signal). Then, a comparison is performed as in PWM mode 1 and the channels becomes active again at the next update.\n              1010: Reserved,\n              1011: Reserved,\n              1100: Combined PWM mode 1 - OC1REF has the same behavior as in PWM mode 1. OC1REFC is the logical OR between OC1REF and OC2REF.\n              1101: Combined PWM mode 2 - OC1REF has the same behavior as in PWM mode 2. OC1REFC is the logical AND between OC1REF and OC2REF.\n              1110: Asymmetric PWM mode 1 - OC1REF has the same behavior as in PWM mode 1. OC1REFC outputs OC1REF when the counter is counting up, OC2REF when it is counting down.\n              1111: Asymmetric PWM mode 2 - OC1REF has the same behavior as in PWM mode 2. OC1REFC outputs OC1REF when the counter is counting up, OC2REF when it is counting down." */
        volatile uint32_t OC1M: 4;  /* Output compare 1 mode               These bits define the be */
        /* @SVD:field=OC2CE,bitOffset=24,bitWidth=1,description="Output compare 2 clear enable" */
        volatile uint32_t OC2CE: 1;  /* Output compare 2 clear enable */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=OC2PE,bitOffset=27,bitWidth=1,description="Output compare 2 preload enable" */
        volatile uint32_t OC2PE: 1;  /* Output compare 2 preload enable */
        /* @SVD:field=OC2M,bitOffset=28,bitWidth=4,description="Output compare 2 mode" */
        volatile uint32_t OC2M: 4;  /* Output compare 2 mode */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CCMR1_Type;

/* @SVD:register=CCMR2,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="TIM capture/compare mode register 2" */
/* CCMR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CC3S,bitOffset=0,bitWidth=2,description="Capture/Compare 3 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC3 channel is configured as output\n              01: CC3 channel is configured as input, IC3 is mapped on TI3\n              10: CC3 channel is configured as input, IC3 is mapped on TI4\n              11: CC3 channel is configured as input, IC3 is mapped on TRC. This mode is working only if an internal trigger input is selected through TS bit (SMCR register)" */
        volatile uint32_t CC3S: 2;  /* Capture/Compare 3 selection               This bit-field def */
        /* @SVD:field=IC3PSC,bitOffset=2,bitWidth=2,description="Input capture 3 prescaler" */
        volatile uint32_t IC3PSC: 2;  /* Input capture 3 prescaler */
        /* @SVD:field=IC3F,bitOffset=4,bitWidth=4,description="Input capture 3 filter" */
        volatile uint32_t IC3F: 4;  /* Input capture 3 filter */
        /* @SVD:field=CC4S,bitOffset=8,bitWidth=2,description="Capture/Compare 4 selection\n              This bit-field defines the direction of the channel (input/output) as well as the used input. \n              00: CC4 channel is configured as output\n              01: CC4 channel is configured as input, IC4 is mapped on TI4\n              10: CC4 channel is configured as input, IC4 is mapped on TI3\n              11: CC4 channel is configured as input, IC4 is mapped on TRC. This mode is working only if an internal trigger input is selected through TS bit (SMCR register)" */
        volatile uint32_t CC4S: 2;  /* Capture/Compare 4 selection               This bit-field def */
        /* @SVD:field=IC4PSC,bitOffset=10,bitWidth=2,description="Input capture 4 prescaler" */
        volatile uint32_t IC4PSC: 2;  /* Input capture 4 prescaler */
        /* @SVD:field=IC4F,bitOffset=12,bitWidth=4,description="Input capture 4 filter" */
        volatile uint32_t IC4F: 4;  /* Input capture 4 filter */
        /* @SVD:field=OC3CE,bitOffset=16,bitWidth=1,description="Output compare 3 clear enable" */
        volatile uint32_t OC3CE: 1;  /* Output compare 3 clear enable */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=OC3PE,bitOffset=19,bitWidth=1,description="Output compare 3 preload enable" */
        volatile uint32_t OC3PE: 1;  /* Output compare 3 preload enable */
        /* @SVD:field=OC3M,bitOffset=20,bitWidth=4,description="Output compare 3 mode" */
        volatile uint32_t OC3M: 4;  /* Output compare 3 mode */
        /* @SVD:field=OC4CE,bitOffset=24,bitWidth=1,description="Output compare 4 clear enable" */
        volatile uint32_t OC4CE: 1;  /* Output compare 4 clear enable */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=OC4PE,bitOffset=27,bitWidth=1,description="Output compare 4 preload enable" */
        volatile uint32_t OC4PE: 1;  /* Output compare 4 preload enable */
        /* @SVD:field=OC4M,bitOffset=28,bitWidth=4,description="Output compare 4 mode" */
        volatile uint32_t OC4M: 4;  /* Output compare 4 mode */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CCMR2_Type;

/* @SVD:register=CCER,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Capture/Compare enable register" */
/* CCER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CC1E,bitOffset=0,bitWidth=1,description="Capture/Compare 1 output enable.\n              CC1 channel configured as output:\n              0: Off - OC1 is not active\n              1: On - OC1 signal is output on the corresponding output pin\n              CC1 channel configured as input: This bit determines if a capture of the counter value can actually be done into the input capture/compare register 1 (CCR1) or not.\n              0: Capture disabled\n              1: Capture enabled" */
        volatile uint32_t CC1E: 1;  /* Capture/Compare 1 output enable.               CC1 channel c */
        /* @SVD:field=CC1P,bitOffset=1,bitWidth=1,description="Capture/Compare 1 output Polarity.\n              CC1 channel configured as output:\n              0: OC1 active high\n              1: OC1 active low\n              CC1 channel configured as input: CC1NP/CC1P bits select TI1FP1 and TI2FP1 polarity for trigger or capture operations.\n              00: noninverted/rising edge\n              Circuit is sensitive to TIxFP1 rising edge (capture, trigger in reset, external clock or trigger mode), TIxFP1 is not inverted (trigger in gated mode, encoder mode).\n              01: inverted/falling edge\n              Circuit is sensitive to TIxFP1 falling edge (capture, trigger in reset, external clock or trigger mode), TIxFP1 is inverted (trigger in gated mode, encoder mode).\n              10: reserved, do not use this configuration.\n              11: noninverted/both edges\n              Circuit is sensitive to both TIxFP1 rising and falling edges (capture, trigger in reset, external clock or trigger mode), TIxFP1 is not inverted (trigger in gated mode). This configuration must not be used for encoder mode." */
        volatile uint32_t CC1P: 1;  /* Capture/Compare 1 output Polarity.               CC1 channel */
        /* @SVD:field=RSVD5,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=CC1NP,bitOffset=3,bitWidth=1,description="Capture/Compare 1 output Polarity.\n              CC1 channel configured as output: CC1NP must be kept cleared in this case.\n              CC1 channel configured as input: This bit is used in conjunction with CC1P to define TI1FP1/TI2FP1 polarity. refer to CC1P description." */
        volatile uint32_t CC1NP: 1;  /* Capture/Compare 1 output Polarity.               CC1 channel */
        /* @SVD:field=CC2E,bitOffset=4,bitWidth=1,description="Capture/Compare 2 output enable." */
        volatile uint32_t CC2E: 1;  /* Capture/Compare 2 output enable. */
        /* @SVD:field=CC2P,bitOffset=5,bitWidth=1,description="Capture/Compare 2 output Polarity." */
        volatile uint32_t CC2P: 1;  /* Capture/Compare 2 output Polarity. */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=CC2NP,bitOffset=7,bitWidth=1,description="Capture/Compare 2 output Polarity." */
        volatile uint32_t CC2NP: 1;  /* Capture/Compare 2 output Polarity. */
        /* @SVD:field=CC3E,bitOffset=8,bitWidth=1,description="Capture/Compare 3 output enable." */
        volatile uint32_t CC3E: 1;  /* Capture/Compare 3 output enable. */
        /* @SVD:field=CC3P,bitOffset=9,bitWidth=1,description="Capture/Compare 3 output Polarity." */
        volatile uint32_t CC3P: 1;  /* Capture/Compare 3 output Polarity. */
        /* @SVD:field=RSVD3,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=CC3NP,bitOffset=11,bitWidth=1,description="Capture/Compare 3 output Polarity." */
        volatile uint32_t CC3NP: 1;  /* Capture/Compare 3 output Polarity. */
        /* @SVD:field=CC4E,bitOffset=12,bitWidth=1,description="Capture/Compare 4 output enable." */
        volatile uint32_t CC4E: 1;  /* Capture/Compare 4 output enable. */
        /* @SVD:field=CC4P,bitOffset=13,bitWidth=1,description="Capture/Compare 4 output Polarity." */
        volatile uint32_t CC4P: 1;  /* Capture/Compare 4 output Polarity. */
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CC4NP,bitOffset=15,bitWidth=1,description="Capture/Compare 4 output Polarity." */
        volatile uint32_t CC4NP: 1;  /* Capture/Compare 4 output Polarity. */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CCER_Type;

/* @SVD:register=CNT,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Counter" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=16,description="counter value" */
        volatile uint32_t CNT: 16;  /* counter value */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
        /* @SVD:field=UIFCPY,bitOffset=31,bitWidth=1,description="Value depends on IUFREMAP in CR1.\n              If UIFREMAP = 1\n              UIFCPY: UIF Copy\n              This bit is a read-only copy of the UIF bit of the ISR register" */
        volatile uint32_t UIFCPY: 1;  /* Value depends on IUFREMAP in CR1.               If UIFREMAP  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CNT_Type;

/* @SVD:register=PSC,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Prescaler" */
/* PSC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PSC,bitOffset=0,bitWidth=16,description="Prescaler value\n              The counter clock frequency is equal to fCLK / (PSC[15:0] + 1).\n              PSC contains the value to be loaded in the active prescaler register at each update event (including when the counter is cleared through UG bit of EGR register or through trigger controller when configured in 'reset mode')." */
        volatile uint32_t PSC: 16;  /* Prescaler value               The counter clock frequency is */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_PSC_Type;

/* @SVD:register=ARR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Auto-reload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=16,description="Auto-reload value\n              ARR is the value to be loaded in the actual auto-reload register." */
        volatile uint32_t ARR: 16;  /* Auto-reload value               ARR is the value to be loade */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_ARR_Type;

/* @SVD:register=RCR,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Repetition counter register" */
/* RCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=8,description="Repetition counter value\n              These bits allow the user to set-up the update rate of the compare registers (i.e. periodic transfers from preload to active registers) when preload registers are enable, as well as the update interrupt generation rate, if this interrupt is enable.\n              Each time the REP_CNT related downcounter reaches zero, an update event is generated and it restarts counting from REP value. As REP_CNT is reloaded with REP value only at the repetition update event, any write to the RCR register is not taken in account until the next repetition update event.\n              It means in PWM mode (REP+1) corresponds to the number of PWM periods in edge-aligned mode." */
        volatile uint32_t REP: 8;  /* Repetition counter value               These bits allow the  */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_RCR_Type;

/* @SVD:register=CCR1,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 1" */
/* CCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR1,bitOffset=0,bitWidth=16,description="Capture/Compare 1 value\n              If channel CC1 is configured as output:\n              CCR1 is the value to be loaded in the actual capture/compare 1 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR1 register (bit OC1PE). Else the preload value is copied in the active capture/compare 1 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signaled on OC1 output.\n              If channel CC1is configured as input:\n              CCR1 is the counter value transferred by the last input capture 1 event (IC1). The CCR1 register is read-only and cannot be programmed." */
        volatile uint32_t CCR1: 16;  /* Capture/Compare 1 value               If channel CC1 is conf */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CCR1_Type;

/* @SVD:register=CCR2,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 2" */
/* CCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR2,bitOffset=0,bitWidth=16,description="Capture/Compare 2 value\n              If channel CC2 is configured as output:\n              CCR2 is the value to be loaded in the actual capture/compare 2 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR1 register (bit OC2PE). Else the preload value is copied in the active capture/compare 2 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signalled on OC2 output.\n              If channel CC2 is configured as input:\n              CCR2 is the counter value transferred by the last input capture 2 event (IC2). The CCR2 register is read-only and cannot be programmed." */
        volatile uint32_t CCR2: 16;  /* Capture/Compare 2 value               If channel CC2 is conf */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CCR2_Type;

/* @SVD:register=CCR3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 3" */
/* CCR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR3,bitOffset=0,bitWidth=16,description="Capture/Compare value\n              If channel CC3 is configured as output:\n              CCR3 is the value to be loaded in the actual capture/compare 3 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR2 register (bit OC3PE). Else the preload value is copied in the active capture/compare 3 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signalled on OC3 output.\n              If channel CC3is configured as input:\n              CCR3 is the counter value transferred by the last input capture 3 event (IC3). The CCR3 register is read-only and cannot be programmed." */
        volatile uint32_t CCR3: 16;  /* Capture/Compare value               If channel CC3 is config */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CCR3_Type;

/* @SVD:register=CCR4,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="Capture/Compare register 4" */
/* CCR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CCR4,bitOffset=0,bitWidth=16,description="Capture/Compare value\n              1. if CC4 channel is configured as output:\n              CCR4 is the value to be loaded in the actual capture/compare 4 register (preload value).It is loaded permanently if the preload feature is not selected in the CCMR2 register (bit OC4PE). Else the preload value is copied in the active capture/compare 4 register when an update event occurs.\n              The active capture/compare register contains the value to be compared to the counter CNT and signalled on OC4 output.\n              2.  if CC4 channel is configured as input:\n              CCR4 is the counter value transferred by the last input capture 4 event (IC4). The CCR4 register is read-only and cannot be programmed." */
        volatile uint32_t CCR4: 16;  /* Capture/Compare value               1. if CC4 channel is con */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} GPTIM2_CCR4_Type;

/* GPTIM2 寄存器结构体 */
typedef struct
{
    GPTIM2_CR1_Type CR1;  /* 0x0000 - TIM control register 1 */
    GPTIM2_CR2_Type CR2;  /* 0x0004 - TIM control register 2 */
    GPTIM2_SMCR_Type SMCR;  /* 0x0008 - TIM slave mode control register */
    GPTIM2_DIER_Type DIER;  /* 0x000C - TIM DMA/Interrupt enable register */
    GPTIM2_SR_Type SR;  /* 0x0010 - TIM status register */
    GPTIM2_EGR_Type EGR;  /* 0x0014 - Event generation register */
    GPTIM2_CCMR1_Type CCMR1;  /* 0x0018 - TIM capture/compare mode register 1 */
    GPTIM2_CCMR2_Type CCMR2;  /* 0x001C - TIM capture/compare mode register 2 */
    GPTIM2_CCER_Type CCER;  /* 0x0020 - Capture/Compare enable register */
    GPTIM2_CNT_Type CNT;  /* 0x0024 - Counter */
    GPTIM2_PSC_Type PSC;  /* 0x0028 - Prescaler */
    GPTIM2_ARR_Type ARR;  /* 0x002C - Auto-reload register */
    GPTIM2_RCR_Type RCR;  /* 0x0030 - Repetition counter register */
    GPTIM2_CCR1_Type CCR1;  /* 0x0034 - Capture/Compare register 1 */
    GPTIM2_CCR2_Type CCR2;  /* 0x0038 - Capture/Compare register 2 */
    GPTIM2_CCR3_Type CCR3;  /* 0x003C - Capture/Compare register 3 */
    GPTIM2_CCR4_Type CCR4;  /* 0x0040 - Capture/Compare register 4 */
} GPTIM2_TypeDef;

/* ============================================================================ */
/* BTIM2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=BTIM2,base=0x500B1000,description="",groupName=BTIM */

#define BTIM2_BASE                                         (0x500B1000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="TIM control register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CEN,bitOffset=0,bitWidth=1,description="Counter enable\n              0: Counter disabled\n              1: Counter enabled\n              Note: Gated mode can work only if the CEN bit has been previously set by software. However trigger mode can set the CEN bit automatically by hardware. \n              CEN is cleared automatically in one-pulse mode, when an update event occurs." */
        volatile uint32_t CEN: 1;  /* Counter enable               0: Counter disabled             */
        /* @SVD:field=UDIS,bitOffset=1,bitWidth=1,description="Update disable\n              This bit is set and cleared by software to enable/disable UEV event generation.\n              0: UEV enabled. The Update (UEV) event is generated by one of the following events:\n                 Counter overflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              Buffered registers are then loaded with their preload values.\n              1: UEV disabled. The Update event is not generated, shadow registers keep their value (ARR, PSC). However the counter and the prescaler are reinitialized if the UG bit is set or if a hardware reset is received from the slave mode controller." */
        volatile uint32_t UDIS: 1;  /* Update disable               This bit is set and cleared by  */
        /* @SVD:field=URS,bitOffset=2,bitWidth=1,description="Update request source\n              This bit is set and cleared by software to select the UEV event sources.\n              0: Any of the following events generate an update interrupt or DMA request if enabled. \n              These events can be: \n                 Counter overflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              1: Only counter overflow generates an update interrupt or DMA request if enabled." */
        volatile uint32_t URS: 1;  /* Update request source               This bit is set and clea */
        /* @SVD:field=OPM,bitOffset=3,bitWidth=1,description="One-pulse mode\n              0: Counter is not stopped at update event\n              1: Counter stops counting at the next update event (clearing the bit CEN)" */
        volatile uint32_t OPM: 1;  /* One-pulse mode               0: Counter is not stopped at up */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=ARPE,bitOffset=7,bitWidth=1,description="Auto-reload preload enable\n              0: ARR register is not buffered\n              1: ARR register is buffered" */
        volatile uint32_t ARPE: 1;  /* Auto-reload preload enable               0: ARR register is  */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM2_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="TIM control register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=MMS,bitOffset=4,bitWidth=2,description="Master mode selection\n              These bits allow to select the information to be sent in master mode to slave timers for synchronization (TRGO). The combination is as follows:\n              00: Reset:the UG bit from the EGR register is used as trigger output (TRGO). If the reset is generated by the trigger input (slave mode controller configured in reset mode) then the signal on TRGO is delayed compared to the actual reset.\n              01: Enable :the Counter enable signal, CNT_EN, is used as trigger output (TRGO). It is useful to start several timers at the same time or to control a window in which a slave timer is enabled. The Counter Enable signal is generated by a logic OR between CEN control bit and the trigger input when configured in gated mode. \n              When the Counter Enable signal is controlled by the trigger input, there is a delay on TRGO, except if the master/slave mode is selected (see the MSM bit description in SMCR register).\n              10: Update:The update event is selected as trigger output (TRGO). For instance a master timer can then be used as a prescaler for a slave timer.\n              11: Gating:The delayed gating trigger is selected as trigger output (TRGO)." */
        volatile uint32_t MMS: 2;  /* Master mode selection               These bits allow to sele */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM2_CR2_Type;

/* @SVD:register=SMCR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="TIM slave mode control register" */
/* SMCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD5,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD5: 4;
        /* @SVD:field=TS,bitOffset=4,bitWidth=2,description="Trigger selection\n              This bit-field selects the trigger input to be used to synchronize the counter.\n              00: Internal Trigger 0 (ITR0)\n              01: Internal Trigger 1 (ITR1) \n              10: Internal Trigger 2 (ITR2)\n              11: Internal Trigger 3 (ITR3)" */
        volatile uint32_t TS: 2;  /* Trigger selection               This bit-field selects the t */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=MSM,bitOffset=7,bitWidth=1,description="Master/Slave mode. This bit should be asserted on master timer if synchronization if needed.\n              0: No action\n              1: The effect of an event on the trigger input (TRGI) is delayed to allow a perfect synchronization between the current timer and its slaves (through TRGO). It is useful if we want to synchronize several timers on a single external event." */
        volatile uint32_t MSM: 1;  /* Master/Slave mode. This bit should be asserted on master tim */
        /* @SVD:field=RSVD3,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD3: 8;
        /* @SVD:field=SMS,bitOffset=16,bitWidth=3,description="Slave mode selection\n              When external signals are selected the active edge of the trigger signal (TRGI) is linked to the polarity selected on the external input.\n              000: Slave mode disabled.\n              001: Reset Mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter and generates an update of the registers.\n              010: Trigger Mode - The counter starts at a rising edge of the trigger TRGI (but it is not reset). Only the start of the counter is controlled.\n              011: Combined reset + trigger mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter, generates an update of the registers and starts the counter.\n              100: External Clock Mode - Rising edges of the selected trigger (TRGI) clock the counter." */
        volatile uint32_t SMS: 3;  /* Slave mode selection               When external signals are */
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=GTS,bitOffset=20,bitWidth=2,description="Gating trigger selection in gated mode\n              This bit-field selects the trigger input to be used to enable the counter gating.\n              00: Internal Trigger 0 (ITR0)\n              01: Internal Trigger 1 (ITR1) \n              10: Internal Trigger 2 (ITR2)\n              11: Internal Trigger 3 (ITR3)" */
        volatile uint32_t GTS: 2;  /* Gating trigger selection in gated mode               This bi */
        /* @SVD:field=GTP,bitOffset=22,bitWidth=1,description="Gating trigger polarity invert\n              0: active at high level\n              1: active at low level" */
        volatile uint32_t GTP: 1;  /* Gating trigger polarity invert               0: active at hi */
        /* @SVD:field=GM,bitOffset=23,bitWidth=1,description="Gated Mode. The counter clock is enabled when the selected trigger input (TRGI) is active (according to gating trigger polarity). The counter stops (but is not reset) as soon as the trigger becomes inactive. Both start and stop of the counter are controlled. Gated mode and slave mode can be enabled simutanuously with different trigger selection." */
        volatile uint32_t GM: 1;  /* Gated Mode. The counter clock is enabled when the selected t */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM2_SMCR_Type;

/* @SVD:register=DIER,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="TIM DMA/Interrupt enable register" */
/* DIER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIE,bitOffset=0,bitWidth=1,description="Update interrupt enable\n              0: Update interrupt disabled.\n              1: Update interrupt enabled" */
        volatile uint32_t UIE: 1;  /* Update interrupt enable               0: Update interrupt di */
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=UDE,bitOffset=8,bitWidth=1,description="Update DMA request enable\n              0: Update DMA request disabled.\n              1: Update DMA request enabled" */
        volatile uint32_t UDE: 1;  /* Update DMA request enable               0: Update DMA reques */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM2_DIER_Type;

/* @SVD:register=SR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="TIM status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIF,bitOffset=0,bitWidth=1,description="Update interrupt flag\n              This bit is set by hardware on an update event. It is cleared by software.\n              0: No update occurred\n              1: Update interrupt pending. This bit is set by hardware when the registers are updated:\n              At overflow and if UDIS=0 in the CR1 register.\n              When CNT is reinitialized by software using the UG bit in EGR register, if URS=0 and UDIS=0 in the CR1 register.\n              When CNT is reinitialized by a trigger event (refer to the synchro control register description), if URS=0 and UDIS=0 in the CR1 register." */
        volatile uint32_t UIF: 1;  /* Update interrupt flag               This bit is set by hardw */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM2_SR_Type;

/* @SVD:register=EGR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Event generation register" */
/* EGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UG,bitOffset=0,bitWidth=1,description="Update generation\n              This bit can be set by software, it is automatically cleared by hardware.\n              0: No action\n              1: Re-initialize the counter and generates an update of the registers. Note that the prescaler counter is cleared too (anyway the prescaler ratio is not affected). The counter is cleared if the center-aligned mode is selected or if DIR=0 (upcounting), else it takes the auto-reload value (ARR) if DIR=1 (downcounting)." */
        volatile uint32_t UG: 1;  /* Update generation               This bit can be set by softw */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM2_EGR_Type;

/* @SVD:register=CNT,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Counter" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=32,description="counter value" */
        volatile uint32_t CNT: 32;  /* counter value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM2_CNT_Type;

/* @SVD:register=PSC,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Prescaler" */
/* PSC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PSC,bitOffset=0,bitWidth=16,description="Prescaler value\n              The counter clock frequency is equal to fCLK / (PSC[15:0] + 1).\n              PSC contains the value to be loaded in the active prescaler register at each update event (including when the counter is cleared through UG bit of EGR register or through trigger controller when configured in \"reset mode\")." */
        volatile uint32_t PSC: 16;  /* Prescaler value               The counter clock frequency is */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM2_PSC_Type;

/* @SVD:register=ARR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Auto-reload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=32,description="Auto-reload value\n              ARR is the value to be loaded in the actual auto-reload register. The counter is blocked while the auto-reload value is null." */
        volatile uint32_t ARR: 32;  /* Auto-reload value               ARR is the value to be loade */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM2_ARR_Type;

/* BTIM2 寄存器结构体 */
typedef struct
{
    BTIM2_CR1_Type CR1;  /* 0x0000 - TIM control register 1 */
    BTIM2_CR2_Type CR2;  /* 0x0004 - TIM control register 2 */
    BTIM2_SMCR_Type SMCR;  /* 0x0008 - TIM slave mode control register */
    BTIM2_DIER_Type DIER;  /* 0x000C - TIM DMA/Interrupt enable register */
    BTIM2_SR_Type SR;  /* 0x0010 - TIM status register */
    BTIM2_EGR_Type EGR;  /* 0x0014 - Event generation register */
    /* @SVD:register=RSVD1,offset=0x0018,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0018 - RSVD1 */
    volatile uint32_t RESERVED7[2];  /* 0x001C - 0x0023 */
    BTIM2_CNT_Type CNT;  /* 0x0024 - Counter */
    BTIM2_PSC_Type PSC;  /* 0x0028 - Prescaler */
    BTIM2_ARR_Type ARR;  /* 0x002C - Auto-reload register */
} BTIM2_TypeDef;

/* ============================================================================ */
/* HPSYS_AON 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=HPSYS_AON,base=0x500C0000,description="",groupName=HPSYS_AON */

#define HPSYS_AON_BASE                                     (0x500C0000UL)

/* @SVD:register=PMR,offset=0x0000,size=32,access=read-write,reset=0x0,description="Power Mode Register" */
/* PMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MODE,bitOffset=0,bitWidth=2,description="Power Mode: 2'h0 - active; 2'h1 - light sleep; 2'h2 - deep sleep; 2'h3 - standby" */
        volatile uint32_t MODE: 2;  /* Power Mode: 2'h0 - active; 2'h1 - light sleep; 2'h2 - deep s */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
        /* @SVD:field=FORCE_LCPU,bitOffset=30,bitWidth=1,description="for debug only" */
        volatile uint32_t FORCE_LCPU: 1;  /* for debug only */
        /* @SVD:field=FORCE_SLEEP,bitOffset=31,bitWidth=1,description="Set 1 to force enter low power mode. Will be cleared automatically" */
        volatile uint32_t FORCE_SLEEP: 1;  /* Set 1 to force enter low power mode. Will be cleared automat */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_PMR_Type;

/* @SVD:register=CR1,offset=0x0004,size=32,access=read-write,reset=0x0,description="Control Register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PIN0_MODE,bitOffset=0,bitWidth=3,description="mode for wakeup PIN0 (PA24)\n              0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge, 4/5/6/7: pos or neg edge" */
        volatile uint32_t PIN0_MODE: 3;  /* mode for wakeup PIN0 (PA24)               0 - high level, 1  */
        /* @SVD:field=PIN1_MODE,bitOffset=3,bitWidth=3,description="mode for wakeup PIN1 (PA25)" */
        volatile uint32_t PIN1_MODE: 3;  /* mode for wakeup PIN1 (PA25) */
        /* @SVD:field=PIN2_MODE,bitOffset=6,bitWidth=3,description="mode for wakeup PIN2 (PA26)" */
        volatile uint32_t PIN2_MODE: 3;  /* mode for wakeup PIN2 (PA26) */
        /* @SVD:field=PIN3_MODE,bitOffset=9,bitWidth=3,description="mode for wakeup PIN3 (PA27)" */
        volatile uint32_t PIN3_MODE: 3;  /* mode for wakeup PIN3 (PA27) */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=13,description="" */
        volatile uint32_t RSVD: 13;
        /* @SVD:field=PINOUT_SEL0,bitOffset=25,bitWidth=3,description="for debug only" */
        volatile uint32_t PINOUT_SEL0: 3;  /* for debug only */
        /* @SVD:field=PINOUT_SEL1,bitOffset=28,bitWidth=3,description="for debug only" */
        volatile uint32_t PINOUT_SEL1: 3;  /* for debug only */
        /* @SVD:field=GTIM_EN,bitOffset=31,bitWidth=1,description="Enable global timer" */
        volatile uint32_t GTIM_EN: 1;  /* Enable global timer */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_CR1_Type;

/* @SVD:register=CR2,offset=0x0008,size=32,access=read-write,reset=0x0,description="Control Register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=PIN10_MODE,bitOffset=6,bitWidth=3,description="mode for wakeup PIN10 (PA34)\n              0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge, 4/5/6/7: pos or neg edge" */
        volatile uint32_t PIN10_MODE: 3;  /* mode for wakeup PIN10 (PA34)               0 - high level, 1 */
        /* @SVD:field=PIN11_MODE,bitOffset=9,bitWidth=3,description="mode for wakeup PIN11 (PA35)" */
        volatile uint32_t PIN11_MODE: 3;  /* mode for wakeup PIN11 (PA35) */
        /* @SVD:field=PIN12_MODE,bitOffset=12,bitWidth=3,description="mode for wakeup PIN12 (PA36)" */
        volatile uint32_t PIN12_MODE: 3;  /* mode for wakeup PIN12 (PA36) */
        /* @SVD:field=PIN13_MODE,bitOffset=15,bitWidth=3,description="mode for wakeup PIN13 (PA37)" */
        volatile uint32_t PIN13_MODE: 3;  /* mode for wakeup PIN13 (PA37) */
        /* @SVD:field=PIN14_MODE,bitOffset=18,bitWidth=3,description="mode for wakeup PIN14 (PA38)" */
        volatile uint32_t PIN14_MODE: 3;  /* mode for wakeup PIN14 (PA38) */
        /* @SVD:field=PIN15_MODE,bitOffset=21,bitWidth=3,description="mode for wakeup PIN15 (PA39)" */
        volatile uint32_t PIN15_MODE: 3;  /* mode for wakeup PIN15 (PA39) */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_CR2_Type;

/* @SVD:register=CR3,offset=0x000C,size=32,access=read-write,reset=0x0,description="Control Register 3" */
/* CR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PIN16_MODE,bitOffset=0,bitWidth=3,description="mode for wakeup PIN16 (PA40)\n              0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge, 4/5/6/7: pos or neg edge" */
        volatile uint32_t PIN16_MODE: 3;  /* mode for wakeup PIN16 (PA40)               0 - high level, 1 */
        /* @SVD:field=PIN17_MODE,bitOffset=3,bitWidth=3,description="mode for wakeup PIN17 (PA41)" */
        volatile uint32_t PIN17_MODE: 3;  /* mode for wakeup PIN17 (PA41) */
        /* @SVD:field=PIN18_MODE,bitOffset=6,bitWidth=3,description="mode for wakeup PIN18 (PA42)" */
        volatile uint32_t PIN18_MODE: 3;  /* mode for wakeup PIN18 (PA42) */
        /* @SVD:field=PIN19_MODE,bitOffset=9,bitWidth=3,description="mode for wakeup PIN19 (PA43)" */
        volatile uint32_t PIN19_MODE: 3;  /* mode for wakeup PIN19 (PA43) */
        /* @SVD:field=PIN20_MODE,bitOffset=12,bitWidth=3,description="mode for wakeup PIN20 (PA44)" */
        volatile uint32_t PIN20_MODE: 3;  /* mode for wakeup PIN20 (PA44) */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_CR3_Type;

/* @SVD:register=ACR,offset=0x0010,size=32,access=read-write,reset=0x0,description="Active Mode Control register" */
/* ACR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HRC48_REQ,bitOffset=0,bitWidth=1,description="Request hrc48 in active mode" */
        volatile uint32_t HRC48_REQ: 1;  /* Request hrc48 in active mode */
        /* @SVD:field=HXT48_REQ,bitOffset=1,bitWidth=1,description="Request hxt48 in active mode" */
        volatile uint32_t HXT48_REQ: 1;  /* Request hxt48 in active mode */
        /* @SVD:field=PWR_REQ,bitOffset=2,bitWidth=1,description="Request power during Active mode" */
        volatile uint32_t PWR_REQ: 1;  /* Request power during Active mode */
        /* @SVD:field=EXTPWR_REQ,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t EXTPWR_REQ: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
        /* @SVD:field=HRC48_RDY,bitOffset=30,bitWidth=1,description="Indicate hrc48 is ready" */
        volatile uint32_t HRC48_RDY: 1;  /* Indicate hrc48 is ready */
        /* @SVD:field=HXT48_RDY,bitOffset=31,bitWidth=1,description="Indicate hxt48 is ready" */
        volatile uint32_t HXT48_RDY: 1;  /* Indicate hxt48 is ready */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_ACR_Type;

/* @SVD:register=LSCR,offset=0x0014,size=32,access=read-write,reset=0x0,description="Light Sleep Ctrl Register" */
/* LSCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HRC48_REQ,bitOffset=0,bitWidth=1,description="Request hrc48 in Light Sleep mode" */
        volatile uint32_t HRC48_REQ: 1;  /* Request hrc48 in Light Sleep mode */
        /* @SVD:field=HXT48_REQ,bitOffset=1,bitWidth=1,description="Request hxt48 in Light Sleep mode" */
        volatile uint32_t HXT48_REQ: 1;  /* Request hxt48 in Light Sleep mode */
        /* @SVD:field=PWR_REQ,bitOffset=2,bitWidth=1,description="Request power during Light Sleep mode" */
        volatile uint32_t PWR_REQ: 1;  /* Request power during Light Sleep mode */
        /* @SVD:field=EXTPWR_REQ,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t EXTPWR_REQ: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_LSCR_Type;

/* @SVD:register=DSCR,offset=0x0018,size=32,access=read-write,reset=0x0,description="Deep Sleep Ctrl Register" */
/* DSCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HRC48_REQ,bitOffset=0,bitWidth=1,description="Request hrc48 in Deep Sleep mode" */
        volatile uint32_t HRC48_REQ: 1;  /* Request hrc48 in Deep Sleep mode */
        /* @SVD:field=HXT48_REQ,bitOffset=1,bitWidth=1,description="Request hxt48 in Deep Sleep mode" */
        volatile uint32_t HXT48_REQ: 1;  /* Request hxt48 in Deep Sleep mode */
        /* @SVD:field=PWR_REQ,bitOffset=2,bitWidth=1,description="Request power during Deep Sleep mode" */
        volatile uint32_t PWR_REQ: 1;  /* Request power during Deep Sleep mode */
        /* @SVD:field=EXTPWR_REQ,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t EXTPWR_REQ: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_DSCR_Type;

/* @SVD:register=SBCR,offset=0x001C,size=32,access=read-write,reset=0x0,description="Standby Mode Ctrl Register" */
/* SBCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HRC48_REQ,bitOffset=0,bitWidth=1,description="Request hrc48 in Standby mode" */
        volatile uint32_t HRC48_REQ: 1;  /* Request hrc48 in Standby mode */
        /* @SVD:field=HXT48_REQ,bitOffset=1,bitWidth=1,description="Request hxt48 in Standby mode" */
        volatile uint32_t HXT48_REQ: 1;  /* Request hxt48 in Standby mode */
        /* @SVD:field=PWR_REQ,bitOffset=2,bitWidth=1,description="Request power during Standby mode" */
        volatile uint32_t PWR_REQ: 1;  /* Request power during Standby mode */
        /* @SVD:field=EXTPWR_REQ,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t EXTPWR_REQ: 1;  /* for debug only */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=PD_RAM0,bitOffset=6,bitWidth=1,description="for debug only" */
        volatile uint32_t PD_RAM0: 1;  /* for debug only */
        /* @SVD:field=PD_RAM1,bitOffset=7,bitWidth=1,description="for debug only" */
        volatile uint32_t PD_RAM1: 1;  /* for debug only */
        /* @SVD:field=PD_RAM2,bitOffset=8,bitWidth=1,description="for debug only" */
        volatile uint32_t PD_RAM2: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_SBCR_Type;

/* @SVD:register=WER,offset=0x0020,size=32,access=read-write,reset=0x0,description="Wakeup Enable register" */
/* WER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RTC,bitOffset=0,bitWidth=1,description="Set 1 to enable RTC as wakeup source" */
        volatile uint32_t RTC: 1;  /* Set 1 to enable RTC as wakeup source */
        /* @SVD:field=GPIO1,bitOffset=1,bitWidth=1,description="Set 1 to enable IO(PA) as wakeup source" */
        volatile uint32_t GPIO1: 1;  /* Set 1 to enable IO(PA) as wakeup source */
        /* @SVD:field=LPTIM1,bitOffset=2,bitWidth=1,description="Set 1 to enable LPTIM1 as wakeup source" */
        volatile uint32_t LPTIM1: 1;  /* Set 1 to enable LPTIM1 as wakeup source */
        /* @SVD:field=PMUC,bitOffset=3,bitWidth=1,description="Set 1 to enable PMUC as wakeup source" */
        volatile uint32_t PMUC: 1;  /* Set 1 to enable PMUC as wakeup source */
        /* @SVD:field=RSVD3,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=LP2HP_REQ,bitOffset=6,bitWidth=1,description="Set 1 to enable LPSYS request as wakeup source" */
        volatile uint32_t LP2HP_REQ: 1;  /* Set 1 to enable LPSYS request as wakeup source */
        /* @SVD:field=LP2HP_IRQ,bitOffset=7,bitWidth=1,description="Set 1 to enable MAILBOX2 as wakeup source" */
        volatile uint32_t LP2HP_IRQ: 1;  /* Set 1 to enable MAILBOX2 as wakeup source */
        /* @SVD:field=PIN0,bitOffset=8,bitWidth=1,description="Set 1 to enable PA24 as wakeup source" */
        volatile uint32_t PIN0: 1;  /* Set 1 to enable PA24 as wakeup source */
        /* @SVD:field=PIN1,bitOffset=9,bitWidth=1,description="Set 1 to enable PA25 as wakeup source" */
        volatile uint32_t PIN1: 1;  /* Set 1 to enable PA25 as wakeup source */
        /* @SVD:field=PIN2,bitOffset=10,bitWidth=1,description="Set 1 to enable PA26 as wakeup source" */
        volatile uint32_t PIN2: 1;  /* Set 1 to enable PA26 as wakeup source */
        /* @SVD:field=PIN3,bitOffset=11,bitWidth=1,description="Set 1 to enable PA27 as wakeup source" */
        volatile uint32_t PIN3: 1;  /* Set 1 to enable PA27 as wakeup source */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=PIN10,bitOffset=18,bitWidth=1,description="Set 1 to enable PA34 as wakeup source" */
        volatile uint32_t PIN10: 1;  /* Set 1 to enable PA34 as wakeup source */
        /* @SVD:field=PIN11,bitOffset=19,bitWidth=1,description="Set 1 to enable PA35 as wakeup source" */
        volatile uint32_t PIN11: 1;  /* Set 1 to enable PA35 as wakeup source */
        /* @SVD:field=PIN12,bitOffset=20,bitWidth=1,description="Set 1 to enable PA36 as wakeup source" */
        volatile uint32_t PIN12: 1;  /* Set 1 to enable PA36 as wakeup source */
        /* @SVD:field=PIN13,bitOffset=21,bitWidth=1,description="Set 1 to enable PA37 as wakeup source" */
        volatile uint32_t PIN13: 1;  /* Set 1 to enable PA37 as wakeup source */
        /* @SVD:field=PIN14,bitOffset=22,bitWidth=1,description="Set 1 to enable PA38 as wakeup source" */
        volatile uint32_t PIN14: 1;  /* Set 1 to enable PA38 as wakeup source */
        /* @SVD:field=PIN15,bitOffset=23,bitWidth=1,description="Set 1 to enable PA39 as wakeup source" */
        volatile uint32_t PIN15: 1;  /* Set 1 to enable PA39 as wakeup source */
        /* @SVD:field=PIN16,bitOffset=24,bitWidth=1,description="Set 1 to enable PA40 as wakeup source" */
        volatile uint32_t PIN16: 1;  /* Set 1 to enable PA40 as wakeup source */
        /* @SVD:field=PIN17,bitOffset=25,bitWidth=1,description="Set 1 to enable PA41 as wakeup source" */
        volatile uint32_t PIN17: 1;  /* Set 1 to enable PA41 as wakeup source */
        /* @SVD:field=PIN18,bitOffset=26,bitWidth=1,description="Set 1 to enable PA42 as wakeup source" */
        volatile uint32_t PIN18: 1;  /* Set 1 to enable PA42 as wakeup source */
        /* @SVD:field=PIN19,bitOffset=27,bitWidth=1,description="Set 1 to enable PA43 as wakeup source" */
        volatile uint32_t PIN19: 1;  /* Set 1 to enable PA43 as wakeup source */
        /* @SVD:field=PIN20,bitOffset=28,bitWidth=1,description="Set 1 to enable PA44 as wakeup source" */
        volatile uint32_t PIN20: 1;  /* Set 1 to enable PA44 as wakeup source */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_WER_Type;

/* @SVD:register=WSR,offset=0x0024,size=32,access=read-write,reset=0x0,description="Wakeup Status register" */
/* WSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RTC,bitOffset=0,bitWidth=1,description="Indicates the wakeup status from RTC. Note: the status is masked by WER" */
        volatile uint32_t RTC: 1;  /* Indicates the wakeup status from RTC. Note: the status is ma */
        /* @SVD:field=GPIO1,bitOffset=1,bitWidth=1,description="Indicates the wakeup status from IO(PA). Note: the status is masked by WER" */
        volatile uint32_t GPIO1: 1;  /* Indicates the wakeup status from IO(PA). Note: the status is */
        /* @SVD:field=LPTIM1,bitOffset=2,bitWidth=1,description="Indicates the wakeup status from LPTIM1. Note: the status is masked by WER" */
        volatile uint32_t LPTIM1: 1;  /* Indicates the wakeup status from LPTIM1. Note: the status is */
        /* @SVD:field=PMUC,bitOffset=3,bitWidth=1,description="Indicates the wakeup status from PMUC. Note: the status is masked by WER" */
        volatile uint32_t PMUC: 1;  /* Indicates the wakeup status from PMUC. Note: the status is m */
        /* @SVD:field=RSVD3,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=LP2HP_REQ,bitOffset=6,bitWidth=1,description="Indicates the wakeup status from LPSYS request. Note: the status is masked by WER" */
        volatile uint32_t LP2HP_REQ: 1;  /* Indicates the wakeup status from LPSYS request. Note: the st */
        /* @SVD:field=LP2HP_IRQ,bitOffset=7,bitWidth=1,description="Indicates the wakeup status from MAILBOX2. Note: the status is masked by WER" */
        volatile uint32_t LP2HP_IRQ: 1;  /* Indicates the wakeup status from MAILBOX2. Note: the status  */
        /* @SVD:field=PIN0,bitOffset=8,bitWidth=1,description="Indicates the wakeup status from PA24 request. Note: the status is masked by WER" */
        volatile uint32_t PIN0: 1;  /* Indicates the wakeup status from PA24 request. Note: the sta */
        /* @SVD:field=PIN1,bitOffset=9,bitWidth=1,description="Indicates the wakeup status from PA25 request. Note: the status is masked by WER" */
        volatile uint32_t PIN1: 1;  /* Indicates the wakeup status from PA25 request. Note: the sta */
        /* @SVD:field=PIN2,bitOffset=10,bitWidth=1,description="Indicates the wakeup status from PA26 request. Note: the status is masked by WER" */
        volatile uint32_t PIN2: 1;  /* Indicates the wakeup status from PA26 request. Note: the sta */
        /* @SVD:field=PIN3,bitOffset=11,bitWidth=1,description="Indicates the wakeup status from PA27 request. Note: the status is masked by WER" */
        volatile uint32_t PIN3: 1;  /* Indicates the wakeup status from PA27 request. Note: the sta */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=PIN10,bitOffset=18,bitWidth=1,description="Indicates the wakeup status from PA34 request. Note: the status is masked by WER" */
        volatile uint32_t PIN10: 1;  /* Indicates the wakeup status from PA34 request. Note: the sta */
        /* @SVD:field=PIN11,bitOffset=19,bitWidth=1,description="Indicates the wakeup status from PA35 request. Note: the status is masked by WER" */
        volatile uint32_t PIN11: 1;  /* Indicates the wakeup status from PA35 request. Note: the sta */
        /* @SVD:field=PIN12,bitOffset=20,bitWidth=1,description="Indicates the wakeup status from PA36 request. Note: the status is masked by WER" */
        volatile uint32_t PIN12: 1;  /* Indicates the wakeup status from PA36 request. Note: the sta */
        /* @SVD:field=PIN13,bitOffset=21,bitWidth=1,description="Indicates the wakeup status from PA37 request. Note: the status is masked by WER" */
        volatile uint32_t PIN13: 1;  /* Indicates the wakeup status from PA37 request. Note: the sta */
        /* @SVD:field=PIN14,bitOffset=22,bitWidth=1,description="Indicates the wakeup status from PA38 request. Note: the status is masked by WER" */
        volatile uint32_t PIN14: 1;  /* Indicates the wakeup status from PA38 request. Note: the sta */
        /* @SVD:field=PIN15,bitOffset=23,bitWidth=1,description="Indicates the wakeup status from PA39 request. Note: the status is masked by WER" */
        volatile uint32_t PIN15: 1;  /* Indicates the wakeup status from PA39 request. Note: the sta */
        /* @SVD:field=PIN16,bitOffset=24,bitWidth=1,description="Indicates the wakeup status from PA40 request. Note: the status is masked by WER" */
        volatile uint32_t PIN16: 1;  /* Indicates the wakeup status from PA40 request. Note: the sta */
        /* @SVD:field=PIN17,bitOffset=25,bitWidth=1,description="Indicates the wakeup status from PA41 request. Note: the status is masked by WER" */
        volatile uint32_t PIN17: 1;  /* Indicates the wakeup status from PA41 request. Note: the sta */
        /* @SVD:field=PIN18,bitOffset=26,bitWidth=1,description="Indicates the wakeup status from PA42 request. Note: the status is masked by WER" */
        volatile uint32_t PIN18: 1;  /* Indicates the wakeup status from PA42 request. Note: the sta */
        /* @SVD:field=PIN19,bitOffset=27,bitWidth=1,description="Indicates the wakeup status from PA43 request. Note: the status is masked by WER" */
        volatile uint32_t PIN19: 1;  /* Indicates the wakeup status from PA43 request. Note: the sta */
        /* @SVD:field=PIN20,bitOffset=28,bitWidth=1,description="Indicates the wakeup status from PA44 request. Note: the status is masked by WER" */
        volatile uint32_t PIN20: 1;  /* Indicates the wakeup status from PA44 request. Note: the sta */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_WSR_Type;

/* @SVD:register=WCR,offset=0x0028,size=32,access=read-write,reset=0x0,description="Wakeup Clear register" */
/* WCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD3,bitOffset=0,bitWidth=8,description="Note: for RTC/IO(PA)/LPTIM/PMUC, clear the wakeup status directly in the orignal module" */
        volatile uint32_t RSVD3: 8;  /* Note: for RTC/IO(PA)/LPTIM/PMUC, clear the wakeup status dir */
        /* @SVD:field=PIN0,bitOffset=8,bitWidth=1,description="Write 1 to clear PA24 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN0: 1;  /* Write 1 to clear PA24 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN1,bitOffset=9,bitWidth=1,description="Write 1 to clear PA25 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN1: 1;  /* Write 1 to clear PA25 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN2,bitOffset=10,bitWidth=1,description="Write 1 to clear PA26 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN2: 1;  /* Write 1 to clear PA26 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN3,bitOffset=11,bitWidth=1,description="Write 1 to clear PA27 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN3: 1;  /* Write 1 to clear PA27 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=PIN10,bitOffset=18,bitWidth=1,description="Write 1 to clear PA34 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN10: 1;  /* Write 1 to clear PA34 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN11,bitOffset=19,bitWidth=1,description="Write 1 to clear PA35 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN11: 1;  /* Write 1 to clear PA35 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN12,bitOffset=20,bitWidth=1,description="Write 1 to clear PA36 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN12: 1;  /* Write 1 to clear PA36 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN13,bitOffset=21,bitWidth=1,description="Write 1 to clear PA37 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN13: 1;  /* Write 1 to clear PA37 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN14,bitOffset=22,bitWidth=1,description="Write 1 to clear PA38 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN14: 1;  /* Write 1 to clear PA38 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN15,bitOffset=23,bitWidth=1,description="Write 1 to clear PA39 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN15: 1;  /* Write 1 to clear PA39 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN16,bitOffset=24,bitWidth=1,description="Write 1 to clear PA40 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN16: 1;  /* Write 1 to clear PA40 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN17,bitOffset=25,bitWidth=1,description="Write 1 to clear PA41 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN17: 1;  /* Write 1 to clear PA41 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN18,bitOffset=26,bitWidth=1,description="Write 1 to clear PA42 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN18: 1;  /* Write 1 to clear PA42 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN19,bitOffset=27,bitWidth=1,description="Write 1 to clear PA43 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN19: 1;  /* Write 1 to clear PA43 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN20,bitOffset=28,bitWidth=1,description="Write 1 to clear PA44 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN20: 1;  /* Write 1 to clear PA44 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=AON,bitOffset=31,bitWidth=1,description="Write 1 to clear the AON wakeup IRQ status" */
        volatile uint32_t AON: 1;  /* Write 1 to clear the AON wakeup IRQ status */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_WCR_Type;

/* @SVD:register=ISSR,offset=0x002C,size=32,access=read-write,reset=0x0,description="Inter System Wakeup Register" */
/* ISSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HP2LP_REQ,bitOffset=0,bitWidth=1,description="Write 1 to request LPSYS to stay in active mode" */
        volatile uint32_t HP2LP_REQ: 1;  /* Write 1 to request LPSYS to stay in active mode */
        /* @SVD:field=LP2HP_REQ,bitOffset=1,bitWidth=1,description="Indicate LPSYS request exists" */
        volatile uint32_t LP2HP_REQ: 1;  /* Indicate LPSYS request exists */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=HP_ACTIVE,bitOffset=4,bitWidth=1,description="Write 1 to indicates HPSYS is active" */
        volatile uint32_t HP_ACTIVE: 1;  /* Write 1 to indicates HPSYS is active */
        /* @SVD:field=LP_ACTIVE,bitOffset=5,bitWidth=1,description="Read 1 indicates LPSYS is active" */
        volatile uint32_t LP_ACTIVE: 1;  /* Read 1 indicates LPSYS is active */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_ISSR_Type;

/* @SVD:register=ANACR,offset=0x0030,size=32,access=read-write,reset=0x0,description="Analog Control Register" */
/* ANACR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA_ISO,bitOffset=0,bitWidth=1,description="Set 1 to force IO(PA) into retention mode" */
        volatile uint32_t PA_ISO: 1;  /* Set 1 to force IO(PA) into retention mode */
        /* @SVD:field=VHP_ISO,bitOffset=1,bitWidth=1,description="Set 1 to force off all HPSYS related analog modules" */
        volatile uint32_t VHP_ISO: 1;  /* Set 1 to force off all HPSYS related analog modules */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_ANACR_Type;

/* @SVD:register=GTIMR,offset=0x0034,size=32,access=read-write,reset=0x0,description="Global Timer Register" */
/* GTIMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=32,description="Global timer value" */
        volatile uint32_t CNT: 32;  /* Global timer value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_GTIMR_Type;

/* @SVD:register=RESERVE0,offset=0x0038,size=32,access=read-write,reset=0x0,description="Reserved Register 0" */
/* RESERVE0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="for debug only" */
        volatile uint32_t DATA: 32;  /* for debug only */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_RESERVE0_Type;

/* @SVD:register=RESERVE1,offset=0x003C,size=32,access=read-write,reset=0x0,description="Reserved Register 1" */
/* RESERVE1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="for debug only" */
        volatile uint32_t DATA: 32;  /* for debug only */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} HPSYS_AON_RESERVE1_Type;

/* HPSYS_AON 寄存器结构体 */
typedef struct
{
    HPSYS_AON_PMR_Type PMR;  /* 0x0000 - Power Mode Register */
    HPSYS_AON_CR1_Type CR1;  /* 0x0004 - Control Register 1 */
    HPSYS_AON_CR2_Type CR2;  /* 0x0008 - Control Register 2 */
    HPSYS_AON_CR3_Type CR3;  /* 0x000C - Control Register 3 */
    HPSYS_AON_ACR_Type ACR;  /* 0x0010 - Active Mode Control register */
    HPSYS_AON_LSCR_Type LSCR;  /* 0x0014 - Light Sleep Ctrl Register */
    HPSYS_AON_DSCR_Type DSCR;  /* 0x0018 - Deep Sleep Ctrl Register */
    HPSYS_AON_SBCR_Type SBCR;  /* 0x001C - Standby Mode Ctrl Register */
    HPSYS_AON_WER_Type WER;  /* 0x0020 - Wakeup Enable register */
    HPSYS_AON_WSR_Type WSR;  /* 0x0024 - Wakeup Status register */
    HPSYS_AON_WCR_Type WCR;  /* 0x0028 - Wakeup Clear register */
    HPSYS_AON_ISSR_Type ISSR;  /* 0x002C - Inter System Wakeup Register */
    HPSYS_AON_ANACR_Type ANACR;  /* 0x0030 - Analog Control Register */
    HPSYS_AON_GTIMR_Type GTIMR;  /* 0x0034 - Global Timer Register */
    HPSYS_AON_RESERVE0_Type RESERVE0;  /* 0x0038 - Reserved Register 0 */
    HPSYS_AON_RESERVE1_Type RESERVE1;  /* 0x003C - Reserved Register 1 */
} HPSYS_AON_TypeDef;

/* ============================================================================ */
/* LPTIM1 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=LPTIM1,base=0x500C1000,description="",groupName=LPTIM */

#define LPTIM1_BASE                                        (0x500C1000UL)

/* @SVD:register=ISR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="LPTIM interrupt and status register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UE,bitOffset=0,bitWidth=1,description="LPTIM update event occurred\n              UE is set by hardware to inform application that an update event was generated when overflow occurred while repetition counter reached zero. UE flag can be cleared by writing 1 to the UECLR bit in the LPTIM_ICR register." */
        volatile uint32_t UE: 1;  /* LPTIM update event occurred               UE is set by hardw */
        /* @SVD:field=OF,bitOffset=1,bitWidth=1,description="Overflow occurred\n              OF is set by hardware to inform application that LPTIM_CNT register’s value reached the LPTIM_ARR register’s value and count from zero again. OF flag can be cleared by writing 1 to the OFCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OF: 1;  /* Overflow occurred               OF is set by hardware to inf */
        /* @SVD:field=OC,bitOffset=2,bitWidth=1,description="Output compare match\n              The OC bit is set by hardware to inform application that LPTIM_CNT register value reached the LPTIM_CMP register’s value. OC flag can be cleared by writing 1 to the OCCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OC: 1;  /* Output compare match               The OC bit is set by hard */
        /* @SVD:field=ET,bitOffset=3,bitWidth=1,description="External trigger edge event\n              ET is set by hardware to inform application that a valid edge on the selected external trigger input has occurred. If the trigger is ignored because the timer has already started, then this flag is not set. ET flag can be cleared by writing 1 to the ETCLR bit in the LPTIM_ICR register." */
        volatile uint32_t ET: 1;  /* External trigger edge event               ET is set by hardw */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=UEWKUP,bitOffset=8,bitWidth=1,description="Indicates update event wakeup occurred\n              UEWKUP is set by hardware when an update event was generated (overflow occurred while repetition counter reached zero). To clear UEWKUP, first  write 0 to the UEWE bit in the LPTIM_IER register to disable, then write 1 to the WKUPCLR bit in the LPTIM_ICR register." */
        volatile uint32_t UEWKUP: 1;  /* Indicates update event wakeup occurred               UEWKUP  */
        /* @SVD:field=OFWKUP,bitOffset=9,bitWidth=1,description="Indicates overflow wakeup occurred\n              OFWKUP is set by hardware when LPTIM_CNT register’s value reached the LPTIM_ARR register’s value and count from zero again. To clear OFWKUP, first  write 0 to the OFWE bit in the LPTIM_IER register to disable, then write 1 to the WKUPCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OFWKUP: 1;  /* Indicates overflow wakeup occurred               OFWKUP is s */
        /* @SVD:field=OCWKUP,bitOffset=10,bitWidth=1,description="Indicates output compare wakeup occurred\n              The OCWKUP bit is set by hardware when LPTIM_CNT register value reached the LPTIM_CMP register’s value. To clear OCWKUP, first  write 0 to the OCWE bit in the LPTIM_IER register to disable, then write 1 to the WKUPCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OCWKUP: 1;  /* Indicates output compare wakeup occurred               The O */
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM1_ISR_Type;

/* @SVD:register=ICR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="LPTIM interrupt and status clear register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UECLR,bitOffset=0,bitWidth=1,description="Update event clear flag\n              Writing 1 to this bit clear the UE flag in the LPTIM_ISR register." */
        volatile uint32_t UECLR: 1;  /* Update event clear flag               Writing 1 to this bit  */
        /* @SVD:field=OFCLR,bitOffset=1,bitWidth=1,description="Overflow clear flag\n              Writing 1 to this bit clears the OF flag in the LPTIM_ISR register" */
        volatile uint32_t OFCLR: 1;  /* Overflow clear flag               Writing 1 to this bit clea */
        /* @SVD:field=OCCLR,bitOffset=2,bitWidth=1,description="Output compare clear flag\n              Writing 1 to this bit clears the OC flag in the LPTIM_ISR register" */
        volatile uint32_t OCCLR: 1;  /* Output compare clear flag               Writing 1 to this bi */
        /* @SVD:field=ETCLR,bitOffset=3,bitWidth=1,description="External trigger valid edge clear flag\n              Writing 1 to this bit clears the ET flag in the LPTIM_ISR register" */
        volatile uint32_t ETCLR: 1;  /* External trigger valid edge clear flag               Writing */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=WKUPCLR,bitOffset=8,bitWidth=1,description="wakeup status clear flag\n              Writing 1 to this bit clears all wakeup status flags in the LPTIM_ISR register." */
        volatile uint32_t WKUPCLR: 1;  /* wakeup status clear flag               Writing 1 to this bit */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM1_ICR_Type;

/* @SVD:register=IER,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="LPTIM interrupt and wakeup enable register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UEIE,bitOffset=0,bitWidth=1,description="Update event interrupt enable\n              0: Update event interrupt disabled\n              1: Update event interrupt enabled" */
        volatile uint32_t UEIE: 1;  /* Update event interrupt enable               0: Update event  */
        /* @SVD:field=OFIE,bitOffset=1,bitWidth=1,description="Overflow Interrupt Enable\n              0: Overflow interrupt disabled\n              1: Overflow interrupt enabled" */
        volatile uint32_t OFIE: 1;  /* Overflow Interrupt Enable               0: Overflow interrup */
        /* @SVD:field=OCIE,bitOffset=2,bitWidth=1,description="Output compare Interrupt Enable\n              0: Output compare interrupt disabled\n              1: Output compare interrupt enabled" */
        volatile uint32_t OCIE: 1;  /* Output compare Interrupt Enable               0: Output comp */
        /* @SVD:field=ETIE,bitOffset=3,bitWidth=1,description="External trigger valid edge Interrupt Enable\n              0: External trigger interrupt disabled\n              1: External trigger interrupt enabled" */
        volatile uint32_t ETIE: 1;  /* External trigger valid edge Interrupt Enable               0 */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=UEWE,bitOffset=8,bitWidth=1,description="Update event Wakeup enable\n              0: Update event Wakeup disabled\n              1: Update event Wakeup enabled" */
        volatile uint32_t UEWE: 1;  /* Update event Wakeup enable               0: Update event Wak */
        /* @SVD:field=OFWE,bitOffset=9,bitWidth=1,description="Overflow Wakeup Enable\n              0: Overflow Wakeup disabled\n              1: Overflow Wakeup enabled" */
        volatile uint32_t OFWE: 1;  /* Overflow Wakeup Enable               0: Overflow Wakeup disa */
        /* @SVD:field=OCWE,bitOffset=10,bitWidth=1,description="Output compare Wakeup Enable\n              0: Output compare wakeup disabled\n              1: Output compare wakeup enabled" */
        volatile uint32_t OCWE: 1;  /* Output compare Wakeup Enable               0: Output compare */
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM1_IER_Type;

/* @SVD:register=CFGR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="LPTIM configuration register" */
/* CFGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CKSEL,bitOffset=0,bitWidth=1,description="Clock selector\n              The CKSEL bit selects which clock source the LPTIM will use:\n              0: LPTIM is clocked by internal clock source, according to INTCKSEL\n              1: LPTIM is clocked by external clock source, according to EXTCKSEL" */
        volatile uint32_t CKSEL: 1;  /* Clock selector               The CKSEL bit selects which clo */
        /* @SVD:field=CKPOL,bitOffset=1,bitWidth=2,description="Clock Polarity\n              If LPTIM is clocked by an external clock source, CKPOL bits is used to configure the active edge or edges used by the counter:\n              00: the rising edge is the active edge used for counting\n              01: the falling edge is the active edge used for counting\n              10: both edges are active edges. When both external clock signal edges are considered active ones, the LPTIM must also be clocked by an internal clock source with a frequency equal to at least four time the external clock frequency.\n              11: not allowed" */
        volatile uint32_t CKPOL: 2;  /* Clock Polarity               If LPTIM is clocked by an exter */
        /* @SVD:field=CKFLT,bitOffset=3,bitWidth=2,description="Configurable digital filter for external clock\n              The CKFLT value sets the number of consecutive equal samples that should be detected when a level change occurs on an external clock signal before it is considered as a valid level transition. An internal clock source must be present to use this feature\n              00: any external clock signal level change is considered as a valid transition\n              01: external clock signal level change must be stable for at least 2 clock periods before it is considered as valid transition.\n              10: external clock signal level change must be stable for at least 4 clock periods before it is considered as valid transition.\n              11: external clock signal level change must be stable for at least 8 clock periods before it is considered as valid transition." */
        volatile uint32_t CKFLT: 2;  /* Configurable digital filter for external clock               */
        /* @SVD:field=INTCKSEL,bitOffset=5,bitWidth=1,description="Internal clock source selector\n              0: internal clock source is clk_lp\n              1: internal clock source is pclk2" */
        volatile uint32_t INTCKSEL: 1;  /* Internal clock source selector               0: internal clo */
        /* @SVD:field=TRGFLT,bitOffset=6,bitWidth=2,description="Configurable digital filter for trigger\n              The TRGFLT value sets the number of consecutive equal samples that should be detected when a level change occurs on an internal trigger before it is considered as a valid level transition. An internal clock source must be present to use this feature\n              00: any trigger active level change is considered as a valid trigger\n              01: trigger active level change must be stable for at least 2 clock periods before it is considered as valid trigger.\n              10: trigger active level change must be stable for at least 4 clock periods before it is considered as valid trigger.\n              11: trigger active level change must be stable for at least 8 clock periods before it is considered as valid trigger." */
        volatile uint32_t TRGFLT: 2;  /* Configurable digital filter for trigger               The TR */
        /* @SVD:field=EXTCKSEL,bitOffset=8,bitWidth=1,description="External clock source selector\n              0: external clock source is from lptim_in\n              1: external clock source is from LPCOMP (if LPCOMP integrated)" */
        volatile uint32_t EXTCKSEL: 1;  /* External clock source selector               0: external clo */
        /* @SVD:field=PRESC,bitOffset=9,bitWidth=3,description="Clock prescaler\n              The PRESC bits configure the prescaler division factor. It can be one among the following division factors:\n              000: /1\n              001: /2\n              010: /4\n              011: /8\n              100: /16\n              101: /32\n              110: /64\n              111: /128" */
        volatile uint32_t PRESC: 3;  /* Clock prescaler               The PRESC bits configure the p */
        /* @SVD:field=RSVD4,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=TRIGSEL,bitOffset=13,bitWidth=3,description="Trigger selector\n              The TRIGSEL bits select the trigger source that will serve as a trigger event for the LPTIM among the below 8 available sources:\n              000: lptim_ext0\n              001: lptim_ext1\n              010: lptim_ext2\n              011: lptim_ext3\n              100: lptim_ext4\n              101: lptim_ext5\n              110: lptim_ext6\n              111: lptim_ext7" */
        volatile uint32_t TRIGSEL: 3;  /* Trigger selector               The TRIGSEL bits select the t */
        /* @SVD:field=RSVD3,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=TRIGEN,bitOffset=17,bitWidth=2,description="Trigger enable and polarity\n              The TRIGEN bits controls whether the LPTIM counter is started by an external trigger or not. If the external trigger option is selected, three configurations are possible for the trigger active edge:\n              00: software trigger (counting start is initiated by software)\n              01: rising edge is the active edge\n              10: falling edge is the active edge\n              11: both edges are active edges" */
        volatile uint32_t TRIGEN: 2;  /* Trigger enable and polarity               The TRIGEN bits co */
        /* @SVD:field=TIMOUT,bitOffset=19,bitWidth=1,description="Timeout enable\n              The TIMOUT bit controls the Timeout feature\n              0: A trigger event arriving when the timer is already started will be ignored\n              1: A trigger event arriving when the timer is already started will reset and restart the LPTIM counter and the repetition counter" */
        volatile uint32_t TIMOUT: 1;  /* Timeout enable               The TIMOUT bit controls the Tim */
        /* @SVD:field=WAVE,bitOffset=20,bitWidth=1,description="Waveform shape\n              The WAVE bit controls the output shape\n              0: Deactivate Set-once mode\n              1: Activate the Set-once mode" */
        volatile uint32_t WAVE: 1;  /* Waveform shape               The WAVE bit controls the outpu */
        /* @SVD:field=WAVPOL,bitOffset=21,bitWidth=1,description="Waveform shape polarity\n              The WAVEPOL bit controls the output polarity\n              0: The LPTIM output reflects the compare results between LPTIM_ARR and LPTIM_CMP registers\n              1: The LPTIM output reflects the inverse of the compare results between LPTIM_ARR and LPTIM_CMP registers" */
        volatile uint32_t WAVPOL: 1;  /* Waveform shape polarity               The WAVEPOL bit contro */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=COUNTMODE,bitOffset=23,bitWidth=1,description="counter mode in internal clock source mode (CKSEL=0). If CKSEL=1, this bit has no effect.\n              0: the counter is incremented following each internal clock pulse\n              1: the counter is incremented following each valid pulse on the external clock" */
        volatile uint32_t COUNTMODE: 1;  /* counter mode in internal clock source mode (CKSEL=0). If CKS */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM1_CFGR_Type;

/* @SVD:register=CR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="LPTIM control register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="LPTIM enable\n              The ENABLE bit is set and cleared by software. \n              0:LPTIM is disabled\n              1:LPTIM is enabled" */
        volatile uint32_t ENABLE: 1;  /* LPTIM enable               The ENABLE bit is set and cleared */
        /* @SVD:field=SNGSTRT,bitOffset=1,bitWidth=1,description="LPTIM start in Single mode\n              This bit is set by software and cleared by hardware.\n              In case of software start (TRIGEN[1:0] = 00), setting this bit starts the LPTIM in single pulse mode.\n              If the software start is disabled (TRIGEN[1:0] different than 00), setting this bit starts the LPTIM in single pulse mode as soon as an external trigger is detected.\n              If this bit is set when the LPTIM is in continuous counting mode, then the LPTIM will stop at the following match between ARR and CNT registers.\n              If this bit is set simultaneously with CNTSTRT, then LPTIM will be in continuous counting mode." */
        volatile uint32_t SNGSTRT: 1;  /* LPTIM start in Single mode               This bit is set by  */
        /* @SVD:field=CNTSTRT,bitOffset=2,bitWidth=1,description="Timer start in Continuous mode\n              This bit is set by software and cleared by hardware.\n              In case of software start (TRIGEN[1:0] = 00), setting this bit starts the LPTIM in Continuous mode.\n              If the software start is disabled (TRIGEN[1:0] different than 00), setting this bit starts the timer in Continuous mode as soon as an external trigger is detected.\n              If this bit is set when a single pulse mode counting is ongoing, then the timer will not stop at the next match between ARR and CNT registers and the LPTIM counter keeps counting in Continuous mode." */
        volatile uint32_t CNTSTRT: 1;  /* Timer start in Continuous mode               This bit is set */
        /* @SVD:field=COUNTRST,bitOffset=3,bitWidth=1,description="Counter reset\n              This bit is set by software and cleared by hardware. When set to 1 this bit will trigger a synchronous reset of the CNT register. Due to the synchronous nature of this reset, it only takes place after a synchronization delay.\n              COUNTRST must never be set to 1 by software before it is already cleared to 0 by hardware. Software should consequently check that COUNTRST bit is already cleared to 0 before attempting to set it to 1." */
        volatile uint32_t COUNTRST: 1;  /* Counter reset               This bit is set by software and  */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM1_CR_Type;

/* @SVD:register=CMP,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="LPTIM compare register" */
/* CMP 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMP,bitOffset=0,bitWidth=24,description="Compare value\n              CMP is the compare value used by the LPTIM." */
        volatile uint32_t CMP: 24;  /* Compare value               CMP is the compare value used by */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM1_CMP_Type;

/* @SVD:register=ARR,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="LPTIM autoreload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=24,description="Auto reload value\n              ARR is the autoreload value for the LPTIM. This value must be strictly greater than the CMP[15:0] value." */
        volatile uint32_t ARR: 24;  /* Auto reload value               ARR is the autoreload value  */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM1_ARR_Type;

/* @SVD:register=CNT,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="LPTIM counter register" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=24,description="Counter value\n              When the LPTIM is running with an asynchronous clock, reading the CNT register may return unreliable values. So in this case it is necessary to perform two consecutive read accesses and verify that the two returned values are identical." */
        volatile uint32_t CNT: 24;  /* Counter value               When the LPTIM is running with a */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM1_CNT_Type;

/* @SVD:register=RCR,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="LPTIM repetition register" */
/* RCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=8,description="Repetition register value\n              REP is the repetition value for the LPTIM.\n              Read REP will return left repetition times. It should be noted that for a reliable REP register read access, two consecutive read accesses must be performed and compared. A read access can be considered reliable when the values of the two consecutive read accesses are equal." */
        volatile uint32_t REP: 8;  /* Repetition register value               REP is the repetitio */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM1_RCR_Type;

/* LPTIM1 寄存器结构体 */
typedef struct
{
    LPTIM1_ISR_Type ISR;  /* 0x0000 - LPTIM interrupt and status register */
    LPTIM1_ICR_Type ICR;  /* 0x0004 - LPTIM interrupt and status clear registe */
    LPTIM1_IER_Type IER;  /* 0x0008 - LPTIM interrupt and wakeup enable regist */
    LPTIM1_CFGR_Type CFGR;  /* 0x000C - LPTIM configuration register */
    LPTIM1_CR_Type CR;  /* 0x0010 - LPTIM control register */
    LPTIM1_CMP_Type CMP;  /* 0x0014 - LPTIM compare register */
    LPTIM1_ARR_Type ARR;  /* 0x0018 - LPTIM autoreload register */
    LPTIM1_CNT_Type CNT;  /* 0x001C - LPTIM counter register */
    LPTIM1_RCR_Type RCR;  /* 0x0020 - LPTIM repetition register */
} LPTIM1_TypeDef;

/* ============================================================================ */
/* LPTIM2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=LPTIM2,base=0x500C2000,description="",groupName=LPTIM */

#define LPTIM2_BASE                                        (0x500C2000UL)

/* @SVD:register=ISR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="LPTIM interrupt and status register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UE,bitOffset=0,bitWidth=1,description="LPTIM update event occurred\n              UE is set by hardware to inform application that an update event was generated when overflow occurred while repetition counter reached zero. UE flag can be cleared by writing 1 to the UECLR bit in the LPTIM_ICR register." */
        volatile uint32_t UE: 1;  /* LPTIM update event occurred               UE is set by hardw */
        /* @SVD:field=OF,bitOffset=1,bitWidth=1,description="Overflow occurred\n              OF is set by hardware to inform application that LPTIM_CNT register’s value reached the LPTIM_ARR register’s value and count from zero again. OF flag can be cleared by writing 1 to the OFCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OF: 1;  /* Overflow occurred               OF is set by hardware to inf */
        /* @SVD:field=OC,bitOffset=2,bitWidth=1,description="Output compare match\n              The OC bit is set by hardware to inform application that LPTIM_CNT register value reached the LPTIM_CMP register’s value. OC flag can be cleared by writing 1 to the OCCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OC: 1;  /* Output compare match               The OC bit is set by hard */
        /* @SVD:field=ET,bitOffset=3,bitWidth=1,description="External trigger edge event\n              ET is set by hardware to inform application that a valid edge on the selected external trigger input has occurred. If the trigger is ignored because the timer has already started, then this flag is not set. ET flag can be cleared by writing 1 to the ETCLR bit in the LPTIM_ICR register." */
        volatile uint32_t ET: 1;  /* External trigger edge event               ET is set by hardw */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=UEWKUP,bitOffset=8,bitWidth=1,description="Indicates update event wakeup occurred\n              UEWKUP is set by hardware when an update event was generated (overflow occurred while repetition counter reached zero). To clear UEWKUP, first  write 0 to the UEWE bit in the LPTIM_IER register to disable, then write 1 to the WKUPCLR bit in the LPTIM_ICR register." */
        volatile uint32_t UEWKUP: 1;  /* Indicates update event wakeup occurred               UEWKUP  */
        /* @SVD:field=OFWKUP,bitOffset=9,bitWidth=1,description="Indicates overflow wakeup occurred\n              OFWKUP is set by hardware when LPTIM_CNT register’s value reached the LPTIM_ARR register’s value and count from zero again. To clear OFWKUP, first  write 0 to the OFWE bit in the LPTIM_IER register to disable, then write 1 to the WKUPCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OFWKUP: 1;  /* Indicates overflow wakeup occurred               OFWKUP is s */
        /* @SVD:field=OCWKUP,bitOffset=10,bitWidth=1,description="Indicates output compare wakeup occurred\n              The OCWKUP bit is set by hardware when LPTIM_CNT register value reached the LPTIM_CMP register’s value. To clear OCWKUP, first  write 0 to the OCWE bit in the LPTIM_IER register to disable, then write 1 to the WKUPCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OCWKUP: 1;  /* Indicates output compare wakeup occurred               The O */
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM2_ISR_Type;

/* @SVD:register=ICR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="LPTIM interrupt and status clear register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UECLR,bitOffset=0,bitWidth=1,description="Update event clear flag\n              Writing 1 to this bit clear the UE flag in the LPTIM_ISR register." */
        volatile uint32_t UECLR: 1;  /* Update event clear flag               Writing 1 to this bit  */
        /* @SVD:field=OFCLR,bitOffset=1,bitWidth=1,description="Overflow clear flag\n              Writing 1 to this bit clears the OF flag in the LPTIM_ISR register" */
        volatile uint32_t OFCLR: 1;  /* Overflow clear flag               Writing 1 to this bit clea */
        /* @SVD:field=OCCLR,bitOffset=2,bitWidth=1,description="Output compare clear flag\n              Writing 1 to this bit clears the OC flag in the LPTIM_ISR register" */
        volatile uint32_t OCCLR: 1;  /* Output compare clear flag               Writing 1 to this bi */
        /* @SVD:field=ETCLR,bitOffset=3,bitWidth=1,description="External trigger valid edge clear flag\n              Writing 1 to this bit clears the ET flag in the LPTIM_ISR register" */
        volatile uint32_t ETCLR: 1;  /* External trigger valid edge clear flag               Writing */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=WKUPCLR,bitOffset=8,bitWidth=1,description="wakeup status clear flag\n              Writing 1 to this bit clears all wakeup status flags in the LPTIM_ISR register." */
        volatile uint32_t WKUPCLR: 1;  /* wakeup status clear flag               Writing 1 to this bit */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM2_ICR_Type;

/* @SVD:register=IER,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="LPTIM interrupt and wakeup enable register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UEIE,bitOffset=0,bitWidth=1,description="Update event interrupt enable\n              0: Update event interrupt disabled\n              1: Update event interrupt enabled" */
        volatile uint32_t UEIE: 1;  /* Update event interrupt enable               0: Update event  */
        /* @SVD:field=OFIE,bitOffset=1,bitWidth=1,description="Overflow Interrupt Enable\n              0: Overflow interrupt disabled\n              1: Overflow interrupt enabled" */
        volatile uint32_t OFIE: 1;  /* Overflow Interrupt Enable               0: Overflow interrup */
        /* @SVD:field=OCIE,bitOffset=2,bitWidth=1,description="Output compare Interrupt Enable\n              0: Output compare interrupt disabled\n              1: Output compare interrupt enabled" */
        volatile uint32_t OCIE: 1;  /* Output compare Interrupt Enable               0: Output comp */
        /* @SVD:field=ETIE,bitOffset=3,bitWidth=1,description="External trigger valid edge Interrupt Enable\n              0: External trigger interrupt disabled\n              1: External trigger interrupt enabled" */
        volatile uint32_t ETIE: 1;  /* External trigger valid edge Interrupt Enable               0 */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=UEWE,bitOffset=8,bitWidth=1,description="Update event Wakeup enable\n              0: Update event Wakeup disabled\n              1: Update event Wakeup enabled" */
        volatile uint32_t UEWE: 1;  /* Update event Wakeup enable               0: Update event Wak */
        /* @SVD:field=OFWE,bitOffset=9,bitWidth=1,description="Overflow Wakeup Enable\n              0: Overflow Wakeup disabled\n              1: Overflow Wakeup enabled" */
        volatile uint32_t OFWE: 1;  /* Overflow Wakeup Enable               0: Overflow Wakeup disa */
        /* @SVD:field=OCWE,bitOffset=10,bitWidth=1,description="Output compare Wakeup Enable\n              0: Output compare wakeup disabled\n              1: Output compare wakeup enabled" */
        volatile uint32_t OCWE: 1;  /* Output compare Wakeup Enable               0: Output compare */
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM2_IER_Type;

/* @SVD:register=CFGR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="LPTIM configuration register" */
/* CFGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CKSEL,bitOffset=0,bitWidth=1,description="Clock selector\n              The CKSEL bit selects which clock source the LPTIM will use:\n              0: LPTIM is clocked by internal clock source, according to INTCKSEL\n              1: LPTIM is clocked by external clock source, according to EXTCKSEL" */
        volatile uint32_t CKSEL: 1;  /* Clock selector               The CKSEL bit selects which clo */
        /* @SVD:field=CKPOL,bitOffset=1,bitWidth=2,description="Clock Polarity\n              If LPTIM is clocked by an external clock source, CKPOL bits is used to configure the active edge or edges used by the counter:\n              00: the rising edge is the active edge used for counting\n              01: the falling edge is the active edge used for counting\n              10: both edges are active edges. When both external clock signal edges are considered active ones, the LPTIM must also be clocked by an internal clock source with a frequency equal to at least four time the external clock frequency.\n              11: not allowed" */
        volatile uint32_t CKPOL: 2;  /* Clock Polarity               If LPTIM is clocked by an exter */
        /* @SVD:field=CKFLT,bitOffset=3,bitWidth=2,description="Configurable digital filter for external clock\n              The CKFLT value sets the number of consecutive equal samples that should be detected when a level change occurs on an external clock signal before it is considered as a valid level transition. An internal clock source must be present to use this feature\n              00: any external clock signal level change is considered as a valid transition\n              01: external clock signal level change must be stable for at least 2 clock periods before it is considered as valid transition.\n              10: external clock signal level change must be stable for at least 4 clock periods before it is considered as valid transition.\n              11: external clock signal level change must be stable for at least 8 clock periods before it is considered as valid transition." */
        volatile uint32_t CKFLT: 2;  /* Configurable digital filter for external clock               */
        /* @SVD:field=INTCKSEL,bitOffset=5,bitWidth=1,description="Internal clock source selector\n              0: internal clock source is clk_lp\n              1: internal clock source is pclk2" */
        volatile uint32_t INTCKSEL: 1;  /* Internal clock source selector               0: internal clo */
        /* @SVD:field=TRGFLT,bitOffset=6,bitWidth=2,description="Configurable digital filter for trigger\n              The TRGFLT value sets the number of consecutive equal samples that should be detected when a level change occurs on an internal trigger before it is considered as a valid level transition. An internal clock source must be present to use this feature\n              00: any trigger active level change is considered as a valid trigger\n              01: trigger active level change must be stable for at least 2 clock periods before it is considered as valid trigger.\n              10: trigger active level change must be stable for at least 4 clock periods before it is considered as valid trigger.\n              11: trigger active level change must be stable for at least 8 clock periods before it is considered as valid trigger." */
        volatile uint32_t TRGFLT: 2;  /* Configurable digital filter for trigger               The TR */
        /* @SVD:field=EXTCKSEL,bitOffset=8,bitWidth=1,description="External clock source selector\n              0: external clock source is from lptim_in\n              1: external clock source is from LPCOMP (if LPCOMP integrated)" */
        volatile uint32_t EXTCKSEL: 1;  /* External clock source selector               0: external clo */
        /* @SVD:field=PRESC,bitOffset=9,bitWidth=3,description="Clock prescaler\n              The PRESC bits configure the prescaler division factor. It can be one among the following division factors:\n              000: /1\n              001: /2\n              010: /4\n              011: /8\n              100: /16\n              101: /32\n              110: /64\n              111: /128" */
        volatile uint32_t PRESC: 3;  /* Clock prescaler               The PRESC bits configure the p */
        /* @SVD:field=RSVD4,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=TRIGSEL,bitOffset=13,bitWidth=3,description="Trigger selector\n              The TRIGSEL bits select the trigger source that will serve as a trigger event for the LPTIM among the below 8 available sources:\n              000: lptim_ext0\n              001: lptim_ext1\n              010: lptim_ext2\n              011: lptim_ext3\n              100: lptim_ext4\n              101: lptim_ext5\n              110: lptim_ext6\n              111: lptim_ext7" */
        volatile uint32_t TRIGSEL: 3;  /* Trigger selector               The TRIGSEL bits select the t */
        /* @SVD:field=RSVD3,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=TRIGEN,bitOffset=17,bitWidth=2,description="Trigger enable and polarity\n              The TRIGEN bits controls whether the LPTIM counter is started by an external trigger or not. If the external trigger option is selected, three configurations are possible for the trigger active edge:\n              00: software trigger (counting start is initiated by software)\n              01: rising edge is the active edge\n              10: falling edge is the active edge\n              11: both edges are active edges" */
        volatile uint32_t TRIGEN: 2;  /* Trigger enable and polarity               The TRIGEN bits co */
        /* @SVD:field=TIMOUT,bitOffset=19,bitWidth=1,description="Timeout enable\n              The TIMOUT bit controls the Timeout feature\n              0: A trigger event arriving when the timer is already started will be ignored\n              1: A trigger event arriving when the timer is already started will reset and restart the LPTIM counter and the repetition counter" */
        volatile uint32_t TIMOUT: 1;  /* Timeout enable               The TIMOUT bit controls the Tim */
        /* @SVD:field=WAVE,bitOffset=20,bitWidth=1,description="Waveform shape\n              The WAVE bit controls the output shape\n              0: Deactivate Set-once mode\n              1: Activate the Set-once mode" */
        volatile uint32_t WAVE: 1;  /* Waveform shape               The WAVE bit controls the outpu */
        /* @SVD:field=WAVPOL,bitOffset=21,bitWidth=1,description="Waveform shape polarity\n              The WAVEPOL bit controls the output polarity\n              0: The LPTIM output reflects the compare results between LPTIM_ARR and LPTIM_CMP registers\n              1: The LPTIM output reflects the inverse of the compare results between LPTIM_ARR and LPTIM_CMP registers" */
        volatile uint32_t WAVPOL: 1;  /* Waveform shape polarity               The WAVEPOL bit contro */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=COUNTMODE,bitOffset=23,bitWidth=1,description="counter mode in internal clock source mode (CKSEL=0). If CKSEL=1, this bit has no effect.\n              0: the counter is incremented following each internal clock pulse\n              1: the counter is incremented following each valid pulse on the external clock" */
        volatile uint32_t COUNTMODE: 1;  /* counter mode in internal clock source mode (CKSEL=0). If CKS */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM2_CFGR_Type;

/* @SVD:register=CR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="LPTIM control register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="LPTIM enable\n              The ENABLE bit is set and cleared by software. \n              0:LPTIM is disabled\n              1:LPTIM is enabled" */
        volatile uint32_t ENABLE: 1;  /* LPTIM enable               The ENABLE bit is set and cleared */
        /* @SVD:field=SNGSTRT,bitOffset=1,bitWidth=1,description="LPTIM start in Single mode\n              This bit is set by software and cleared by hardware.\n              In case of software start (TRIGEN[1:0] = 00), setting this bit starts the LPTIM in single pulse mode.\n              If the software start is disabled (TRIGEN[1:0] different than 00), setting this bit starts the LPTIM in single pulse mode as soon as an external trigger is detected.\n              If this bit is set when the LPTIM is in continuous counting mode, then the LPTIM will stop at the following match between ARR and CNT registers.\n              If this bit is set simultaneously with CNTSTRT, then LPTIM will be in continuous counting mode." */
        volatile uint32_t SNGSTRT: 1;  /* LPTIM start in Single mode               This bit is set by  */
        /* @SVD:field=CNTSTRT,bitOffset=2,bitWidth=1,description="Timer start in Continuous mode\n              This bit is set by software and cleared by hardware.\n              In case of software start (TRIGEN[1:0] = 00), setting this bit starts the LPTIM in Continuous mode.\n              If the software start is disabled (TRIGEN[1:0] different than 00), setting this bit starts the timer in Continuous mode as soon as an external trigger is detected.\n              If this bit is set when a single pulse mode counting is ongoing, then the timer will not stop at the next match between ARR and CNT registers and the LPTIM counter keeps counting in Continuous mode." */
        volatile uint32_t CNTSTRT: 1;  /* Timer start in Continuous mode               This bit is set */
        /* @SVD:field=COUNTRST,bitOffset=3,bitWidth=1,description="Counter reset\n              This bit is set by software and cleared by hardware. When set to 1 this bit will trigger a synchronous reset of the CNT register. Due to the synchronous nature of this reset, it only takes place after a synchronization delay.\n              COUNTRST must never be set to 1 by software before it is already cleared to 0 by hardware. Software should consequently check that COUNTRST bit is already cleared to 0 before attempting to set it to 1." */
        volatile uint32_t COUNTRST: 1;  /* Counter reset               This bit is set by software and  */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM2_CR_Type;

/* @SVD:register=CMP,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="LPTIM compare register" */
/* CMP 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMP,bitOffset=0,bitWidth=24,description="Compare value\n              CMP is the compare value used by the LPTIM." */
        volatile uint32_t CMP: 24;  /* Compare value               CMP is the compare value used by */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM2_CMP_Type;

/* @SVD:register=ARR,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="LPTIM autoreload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=24,description="Auto reload value\n              ARR is the autoreload value for the LPTIM. This value must be strictly greater than the CMP[15:0] value." */
        volatile uint32_t ARR: 24;  /* Auto reload value               ARR is the autoreload value  */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM2_ARR_Type;

/* @SVD:register=CNT,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="LPTIM counter register" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=24,description="Counter value\n              When the LPTIM is running with an asynchronous clock, reading the CNT register may return unreliable values. So in this case it is necessary to perform two consecutive read accesses and verify that the two returned values are identical." */
        volatile uint32_t CNT: 24;  /* Counter value               When the LPTIM is running with a */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM2_CNT_Type;

/* @SVD:register=RCR,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="LPTIM repetition register" */
/* RCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=8,description="Repetition register value\n              REP is the repetition value for the LPTIM.\n              Read REP will return left repetition times. It should be noted that for a reliable REP register read access, two consecutive read accesses must be performed and compared. A read access can be considered reliable when the values of the two consecutive read accesses are equal." */
        volatile uint32_t REP: 8;  /* Repetition register value               REP is the repetitio */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM2_RCR_Type;

/* LPTIM2 寄存器结构体 */
typedef struct
{
    LPTIM2_ISR_Type ISR;  /* 0x0000 - LPTIM interrupt and status register */
    LPTIM2_ICR_Type ICR;  /* 0x0004 - LPTIM interrupt and status clear registe */
    LPTIM2_IER_Type IER;  /* 0x0008 - LPTIM interrupt and wakeup enable regist */
    LPTIM2_CFGR_Type CFGR;  /* 0x000C - LPTIM configuration register */
    LPTIM2_CR_Type CR;  /* 0x0010 - LPTIM control register */
    LPTIM2_CMP_Type CMP;  /* 0x0014 - LPTIM compare register */
    LPTIM2_ARR_Type ARR;  /* 0x0018 - LPTIM autoreload register */
    LPTIM2_CNT_Type CNT;  /* 0x001C - LPTIM counter register */
    LPTIM2_RCR_Type RCR;  /* 0x0020 - LPTIM repetition register */
} LPTIM2_TypeDef;

/* ============================================================================ */
/* PMUC 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=PMUC,base=0x500CA000,description="",groupName=PMUC */

#define PMUC_BASE                                          (0x500CA000UL)

/* @SVD:register=CR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control Register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEL_LPCLK,bitOffset=0,bitWidth=1,description="LP clock for watchdog and FSM. 0 - LRC10, 1 - LRC32" */
        volatile uint32_t SEL_LPCLK: 1;  /* LP clock for watchdog and FSM. 0 - LRC10, 1 - LRC32 */
        /* @SVD:field=HIBER_EN,bitOffset=1,bitWidth=1,description="Write 1 to enter hibernate mode; write 0 to clear when exit from hibernate" */
        volatile uint32_t HIBER_EN: 1;  /* Write 1 to enter hibernate mode; write 0 to clear when exit  */
        /* @SVD:field=REBOOT,bitOffset=2,bitWidth=1,description="Write 1 to reboot; write 0 to clear after boot up" */
        volatile uint32_t REBOOT: 1;  /* Write 1 to reboot; write 0 to clear after boot up */
        /* @SVD:field=PIN_RET,bitOffset=3,bitWidth=1,description="If set to 1, IO retained during hibernate mode; otherwise, high-Z" */
        volatile uint32_t PIN_RET: 1;  /* If set to 1, IO retained during hibernate mode; otherwise, h */
        /* @SVD:field=PIN0_MODE,bitOffset=4,bitWidth=3,description="4/6 - both edge (high-active detection), 5/7 - both edge (low-active detection)" */
        volatile uint32_t PIN0_MODE: 3;  /* 4/6 - both edge (high-active detection), 5/7 - both edge (lo */
        /* @SVD:field=PIN1_MODE,bitOffset=7,bitWidth=3,description="0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge" */
        volatile uint32_t PIN1_MODE: 3;  /* 0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge */
        /* @SVD:field=PIN0_SEL,bitOffset=10,bitWidth=5,description="select one out of PA[44:24]. 0 - PA24, 1 - PA25, 20 - PA44, etc." */
        volatile uint32_t PIN0_SEL: 5;  /* select one out of PA[44:24]. 0 - PA24, 1 - PA25, 20 - PA44,  */
        /* @SVD:field=PIN1_SEL,bitOffset=15,bitWidth=5,description="" */
        volatile uint32_t PIN1_SEL: 5;
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CR_Type;

/* @SVD:register=WER,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Wakeup Enable register" */
/* WER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RTC,bitOffset=0,bitWidth=1,description="Set 1 to enable RTC as wakeup source" */
        volatile uint32_t RTC: 1;  /* Set 1 to enable RTC as wakeup source */
        /* @SVD:field=WDT1,bitOffset=1,bitWidth=1,description="Set 1 to enable WDT1 as reboot cause" */
        volatile uint32_t WDT1: 1;  /* Set 1 to enable WDT1 as reboot cause */
        /* @SVD:field=WDT2,bitOffset=2,bitWidth=1,description="Set 1 to enable WDT2 as reboot cause" */
        volatile uint32_t WDT2: 1;  /* Set 1 to enable WDT2 as reboot cause */
        /* @SVD:field=PIN0,bitOffset=3,bitWidth=1,description="Set 1 to enable PIN0 as wakeup source" */
        volatile uint32_t PIN0: 1;  /* Set 1 to enable PIN0 as wakeup source */
        /* @SVD:field=PIN1,bitOffset=4,bitWidth=1,description="Set 1 to enable PIN1 as wakeup source" */
        volatile uint32_t PIN1: 1;  /* Set 1 to enable PIN1 as wakeup source */
        /* @SVD:field=RSVD2,bitOffset=5,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=LOWBAT,bitOffset=7,bitWidth=1,description="If enabled, auto shut down upon battery low; and will power up if battery ready" */
        volatile uint32_t LOWBAT: 1;  /* If enabled, auto shut down upon battery low; and will power  */
        /* @SVD:field=CHG,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t CHG: 1;
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_WER_Type;

/* @SVD:register=WSR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Wakeup Status register" */
/* WSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RTC,bitOffset=0,bitWidth=1,description="Indicates the wakeup status from RTC. Note: the status is masked by WER" */
        volatile uint32_t RTC: 1;  /* Indicates the wakeup status from RTC. Note: the status is ma */
        /* @SVD:field=WDT1,bitOffset=1,bitWidth=1,description="Indicates reboot by WDT1" */
        volatile uint32_t WDT1: 1;  /* Indicates reboot by WDT1 */
        /* @SVD:field=WDT2,bitOffset=2,bitWidth=1,description="Indicates reboot by WDT2" */
        volatile uint32_t WDT2: 1;  /* Indicates reboot by WDT2 */
        /* @SVD:field=PIN0,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t PIN0: 1;
        /* @SVD:field=PIN1,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t PIN1: 1;
        /* @SVD:field=IWDT,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t IWDT: 1;
        /* @SVD:field=PWRKEY,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t PWRKEY: 1;
        /* @SVD:field=LOWBAT,bitOffset=7,bitWidth=1,description="Indicates auto reboot due to battery low" */
        volatile uint32_t LOWBAT: 1;  /* Indicates auto reboot due to battery low */
        /* @SVD:field=CHG,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t CHG: 1;
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_WSR_Type;

/* @SVD:register=WCR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Wakeup Clear register" */
/* WCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD3,bitOffset=0,bitWidth=1,description="Clear status in RTC" */
        volatile uint32_t RSVD3: 1;  /* Clear status in RTC */
        /* @SVD:field=WDT1,bitOffset=1,bitWidth=1,description="Write 1 to clear WDT1 reboot flag" */
        volatile uint32_t WDT1: 1;  /* Write 1 to clear WDT1 reboot flag */
        /* @SVD:field=WDT2,bitOffset=2,bitWidth=1,description="Write 1 to clear WDT2 reboot flag" */
        volatile uint32_t WDT2: 1;  /* Write 1 to clear WDT2 reboot flag */
        /* @SVD:field=PIN0,bitOffset=3,bitWidth=1,description="Write 1 to clear PIN0 wakeup flag. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN0: 1;  /* Write 1 to clear PIN0 wakeup flag. Only valid if PIN wakeup  */
        /* @SVD:field=PIN1,bitOffset=4,bitWidth=1,description="Write 1 to clear PIN1 wakeup flag." */
        volatile uint32_t PIN1: 1;  /* Write 1 to clear PIN1 wakeup flag. */
        /* @SVD:field=RSVD2,bitOffset=5,bitWidth=1,description="Clear status in IWDT" */
        volatile uint32_t RSVD2: 1;  /* Clear status in IWDT */
        /* @SVD:field=PWRKEY,bitOffset=6,bitWidth=1,description="Write 1 to clear PWRKEY reset flag" */
        volatile uint32_t PWRKEY: 1;  /* Write 1 to clear PWRKEY reset flag */
        /* @SVD:field=LOWBAT,bitOffset=7,bitWidth=1,description="Write 1 to clear LOWBAT flag" */
        volatile uint32_t LOWBAT: 1;  /* Write 1 to clear LOWBAT flag */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
        /* @SVD:field=AON,bitOffset=31,bitWidth=1,description="Write 1 to clear the AON wakeup IRQ status" */
        volatile uint32_t AON: 1;  /* Write 1 to clear the AON wakeup IRQ status */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_WCR_Type;

/* @SVD:register=VRTC_CR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="VRTC Control Register" */
/* VRTC_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VRTC_VBIT,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t VRTC_VBIT: 4;
        /* @SVD:field=VRTC_TRIM,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t VRTC_TRIM: 4;
        /* @SVD:field=BOR_EN,bitOffset=8,bitWidth=1,description="Brownout Reset Enable" */
        volatile uint32_t BOR_EN: 1;  /* Brownout Reset Enable */
        /* @SVD:field=BOR_VT_TRIM,bitOffset=9,bitWidth=4,description="" */
        volatile uint32_t BOR_VT_TRIM: 4;
        /* @SVD:field=RSVD,bitOffset=13,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_VRTC_CR_Type;

/* @SVD:register=VRET_CR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="VRET Control Register" */
/* VRET_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t EN: 1;
        /* @SVD:field=BM,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t BM: 1;
        /* @SVD:field=VBIT,bitOffset=2,bitWidth=4,description="" */
        volatile uint32_t VBIT: 4;
        /* @SVD:field=RSVD3,bitOffset=6,bitWidth=4,description="" */
        volatile uint32_t RSVD3: 4;
        /* @SVD:field=TRIM,bitOffset=10,bitWidth=4,description="" */
        volatile uint32_t TRIM: 4;
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=DLY,bitOffset=16,bitWidth=6,description="VRET_LDO power up delay in number of CLK_LP cycles" */
        volatile uint32_t DLY: 6;  /* VRET_LDO power up delay in number of CLK_LP cycles */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=9,description="" */
        volatile uint32_t RSVD: 9;
        /* @SVD:field=RDY,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RDY: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_VRET_CR_Type;

/* @SVD:register=LRC10_CR,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="RC10K Control Register" */
/* LRC10_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="Enabled by default" */
        volatile uint32_t EN: 1;  /* Enabled by default */
        /* @SVD:field=CMPBM1,bitOffset=1,bitWidth=2,description="" */
        volatile uint32_t CMPBM1: 2;
        /* @SVD:field=CMPBM2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t CMPBM2: 1;
        /* @SVD:field=CHGCRT,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t CHGCRT: 2;
        /* @SVD:field=CHGCAP,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t CHGCAP: 2;
        /* @SVD:field=REFRES,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t REFRES: 1;
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
        /* @SVD:field=RDY,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RDY: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_LRC10_CR_Type;

/* @SVD:register=LRC32_CR,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="RC32K Control Register" */
/* LRC32_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="Disabled by default" */
        volatile uint32_t EN: 1;  /* Disabled by default */
        /* @SVD:field=CMPBM1,bitOffset=1,bitWidth=2,description="" */
        volatile uint32_t CMPBM1: 2;
        /* @SVD:field=CMPBM2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t CMPBM2: 1;
        /* @SVD:field=CHGCRT,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t CHGCRT: 2;
        /* @SVD:field=RSEL,bitOffset=6,bitWidth=4,description="" */
        volatile uint32_t RSEL: 4;
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
        /* @SVD:field=RDY,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RDY: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_LRC32_CR_Type;

/* @SVD:register=LXT_CR,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="XTAL32K Control Register" */
/* LXT_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t EN: 1;
        /* @SVD:field=RSN,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSN: 1;
        /* @SVD:field=BM,bitOffset=2,bitWidth=4,description="" */
        volatile uint32_t BM: 4;
        /* @SVD:field=AMP_BM,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t AMP_BM: 2;
        /* @SVD:field=AMPCTRL_ENB,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t AMPCTRL_ENB: 1;
        /* @SVD:field=BMSEL,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t BMSEL: 1;
        /* @SVD:field=BMSTART,bitOffset=10,bitWidth=4,description="" */
        volatile uint32_t BMSTART: 4;
        /* @SVD:field=CAP_SEL,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t CAP_SEL: 1;
        /* @SVD:field=EXT_EN,bitOffset=15,bitWidth=1,description="use external 32K from Pin" */
        volatile uint32_t EXT_EN: 1;  /* use external 32K from Pin */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
        /* @SVD:field=RDY,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RDY: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_LXT_CR_Type;

/* @SVD:register=AON_BG,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="AON Bandgap Register" */
/* AON_BG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BUF_VOS_TRIM,bitOffset=0,bitWidth=3,description="" */
        volatile uint32_t BUF_VOS_TRIM: 3;
        /* @SVD:field=BUF_VOS_STEP,bitOffset=3,bitWidth=2,description="" */
        volatile uint32_t BUF_VOS_STEP: 2;
        /* @SVD:field=BUF_VOS_POLAR,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t BUF_VOS_POLAR: 1;
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_AON_BG_Type;

/* @SVD:register=AON_LDO,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="AON LDO Register" */
/* AON_LDO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VBAT_LDO_SET_VOUT,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t VBAT_LDO_SET_VOUT: 4;
        /* @SVD:field=VBAT_POR_TH,bitOffset=4,bitWidth=3,description="" */
        volatile uint32_t VBAT_POR_TH: 3;
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_AON_LDO_Type;

/* @SVD:register=BUCK_CR1,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="BUCK Control Register 1" */
/* BUCK_CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t EN: 1;
        /* @SVD:field=CTRL,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t CTRL: 1;
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
        /* @SVD:field=MOT_CTUNE,bitOffset=6,bitWidth=3,description="" */
        volatile uint32_t MOT_CTUNE: 3;
        /* @SVD:field=COT_CTUNE,bitOffset=9,bitWidth=3,description="" */
        volatile uint32_t COT_CTUNE: 3;
        /* @SVD:field=COMP_BM_AHI,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t COMP_BM_AHI: 1;
        /* @SVD:field=COMP_IQ_TUNE,bitOffset=13,bitWidth=2,description="" */
        volatile uint32_t COMP_IQ_TUNE: 2;
        /* @SVD:field=COMP_IDYN_TUNE,bitOffset=15,bitWidth=2,description="" */
        volatile uint32_t COMP_IDYN_TUNE: 2;
        /* @SVD:field=IOCP_TUNE,bitOffset=17,bitWidth=3,description="" */
        volatile uint32_t IOCP_TUNE: 3;
        /* @SVD:field=SEL_IOCP_HI,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t SEL_IOCP_HI: 1;
        /* @SVD:field=SEL_LX22,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t SEL_LX22: 1;
        /* @SVD:field=OCP_AON,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t OCP_AON: 1;
        /* @SVD:field=ZCD_AON,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t ZCD_AON: 1;
        /* @SVD:field=UVLO_X_BIAS,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t UVLO_X_BIAS: 1;
        /* @SVD:field=BG_BUF_VOS_TRIM,bitOffset=25,bitWidth=3,description="" */
        volatile uint32_t BG_BUF_VOS_TRIM: 3;
        /* @SVD:field=BG_BUF_VOS_STEP,bitOffset=28,bitWidth=2,description="" */
        volatile uint32_t BG_BUF_VOS_STEP: 2;
        /* @SVD:field=BG_BUF_VOS_POLAR,bitOffset=30,bitWidth=1,description="" */
        volatile uint32_t BG_BUF_VOS_POLAR: 1;
        /* @SVD:field=SS_DONE,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t SS_DONE: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_BUCK_CR1_Type;

/* @SVD:register=BUCK_CR2,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="BUCK Control Register 2" */
/* BUCK_CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=H2M_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t H2M_EN: 1;
        /* @SVD:field=H2L_EN,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t H2L_EN: 1;
        /* @SVD:field=M2L_EN,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t M2L_EN: 1;
        /* @SVD:field=L2M_EN,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t L2M_EN: 1;
        /* @SVD:field=M2H_CNT,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t M2H_CNT: 4;
        /* @SVD:field=L2H_CNT,bitOffset=8,bitWidth=4,description="" */
        volatile uint32_t L2H_CNT: 4;
        /* @SVD:field=L2M_CNT,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t L2M_CNT: 4;
        /* @SVD:field=BYPASS_PG,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t BYPASS_PG: 1;
        /* @SVD:field=BYPASS_OCP,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t BYPASS_OCP: 1;
        /* @SVD:field=BYPASS_UVLO,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t BYPASS_UVLO: 1;
        /* @SVD:field=FORCE_RDY,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t FORCE_RDY: 1;
        /* @SVD:field=SET_VOUT_M,bitOffset=20,bitWidth=4,description="1.1V" */
        volatile uint32_t SET_VOUT_M: 4;  /* 1.1V */
        /* @SVD:field=SET_VOUT_L,bitOffset=24,bitWidth=4,description="0.75V" */
        volatile uint32_t SET_VOUT_L: 4;  /* 0.75V */
        /* @SVD:field=TDIS,bitOffset=28,bitWidth=4,description="Discharge for TDIS*4 LP clock cycles during reboot" */
        volatile uint32_t TDIS: 4;  /* Discharge for TDIS*4 LP clock cycles during reboot */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_BUCK_CR2_Type;

/* @SVD:register=CHG_CR1,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Charger Control Register 1" */
/* CHG_CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="only available when CR3 FORCE_CTRL bit is set" */
        volatile uint32_t EN: 1;  /* only available when CR3 FORCE_CTRL bit is set */
        /* @SVD:field=LOOP_EN,bitOffset=1,bitWidth=1,description="only available when CR3 FORCE_CTRL bit is set" */
        volatile uint32_t LOOP_EN: 1;  /* only available when CR3 FORCE_CTRL bit is set */
        /* @SVD:field=CC_ICTRL,bitOffset=2,bitWidth=6,description="" */
        volatile uint32_t CC_ICTRL: 6;
        /* @SVD:field=CC_VCTRL,bitOffset=8,bitWidth=6,description="" */
        volatile uint32_t CC_VCTRL: 6;
        /* @SVD:field=CC_MP,bitOffset=14,bitWidth=5,description="" */
        volatile uint32_t CC_MP: 5;
        /* @SVD:field=CC_MN,bitOffset=19,bitWidth=5,description="" */
        volatile uint32_t CC_MN: 5;
        /* @SVD:field=CC_RANGE,bitOffset=24,bitWidth=2,description="" */
        volatile uint32_t CC_RANGE: 2;
        /* @SVD:field=CV_VCTRL,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t CV_VCTRL: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CHG_CR1_Type;

/* @SVD:register=CHG_CR2,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="Charger Control Register 2" */
/* CHG_CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BG_PROG_V1P2,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t BG_PROG_V1P2: 4;
        /* @SVD:field=PRECC_RANGE,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t PRECC_RANGE: 2;
        /* @SVD:field=PRECC_ICTRL,bitOffset=6,bitWidth=6,description="" */
        volatile uint32_t PRECC_ICTRL: 6;
        /* @SVD:field=REP_VCTRL,bitOffset=12,bitWidth=6,description="" */
        volatile uint32_t REP_VCTRL: 6;
        /* @SVD:field=HIGH_VCTRL,bitOffset=18,bitWidth=6,description="" */
        volatile uint32_t HIGH_VCTRL: 6;
        /* @SVD:field=BM_EOC,bitOffset=24,bitWidth=3,description="" */
        volatile uint32_t BM_EOC: 3;
        /* @SVD:field=RANGE_EOC,bitOffset=27,bitWidth=1,description="" */
        volatile uint32_t RANGE_EOC: 1;
        /* @SVD:field=VBAT_RANGE,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t VBAT_RANGE: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CHG_CR2_Type;

/* @SVD:register=CHG_CR3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="Charger Control Register 3" */
/* CHG_CR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DLY1,bitOffset=0,bitWidth=6,description="" */
        volatile uint32_t DLY1: 6;
        /* @SVD:field=DLY2,bitOffset=6,bitWidth=5,description="" */
        volatile uint32_t DLY2: 5;
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=19,description="" */
        volatile uint32_t RSVD: 19;
        /* @SVD:field=FORCE_RST,bitOffset=30,bitWidth=1,description="When charger plugged out, this bit will auto reset" */
        volatile uint32_t FORCE_RST: 1;  /* When charger plugged out, this bit will auto reset */
        /* @SVD:field=FORCE_CTRL,bitOffset=31,bitWidth=1,description="When charger plugged out, this bit will auto reset" */
        volatile uint32_t FORCE_CTRL: 1;  /* When charger plugged out, this bit will auto reset */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CHG_CR3_Type;

/* @SVD:register=CHG_CR4,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="Charger Control Register 4" */
/* CHG_CR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IE_VBUS_RDY,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t IE_VBUS_RDY: 1;
        /* @SVD:field=IE_VBAT_HIGH,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t IE_VBAT_HIGH: 1;
        /* @SVD:field=IE_ABOVE_REP,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t IE_ABOVE_REP: 1;
        /* @SVD:field=IE_ABOVE_CC,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t IE_ABOVE_CC: 1;
        /* @SVD:field=IE_CC_MODE,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t IE_CC_MODE: 1;
        /* @SVD:field=IE_CV_MODE,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t IE_CV_MODE: 1;
        /* @SVD:field=IE_EOC_MODE,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t IE_EOC_MODE: 1;
        /* @SVD:field=IE_EOC,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t IE_EOC: 1;
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
        /* @SVD:field=IM_VBUS_RDY,bitOffset=11,bitWidth=3,description="0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge, others - both edge" */
        volatile uint32_t IM_VBUS_RDY: 3;  /* 0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge, o */
        /* @SVD:field=IM_VBAT_HIGH,bitOffset=14,bitWidth=3,description="" */
        volatile uint32_t IM_VBAT_HIGH: 3;
        /* @SVD:field=IM_ABOVE_REP,bitOffset=17,bitWidth=3,description="" */
        volatile uint32_t IM_ABOVE_REP: 3;
        /* @SVD:field=IM_ABOVE_CC,bitOffset=20,bitWidth=3,description="" */
        volatile uint32_t IM_ABOVE_CC: 3;
        /* @SVD:field=IM_CC_MODE,bitOffset=23,bitWidth=3,description="" */
        volatile uint32_t IM_CC_MODE: 3;
        /* @SVD:field=IM_CV_MODE,bitOffset=26,bitWidth=3,description="" */
        volatile uint32_t IM_CV_MODE: 3;
        /* @SVD:field=IM_EOC_MODE,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t IM_EOC_MODE: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CHG_CR4_Type;

/* @SVD:register=CHG_CR5,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="Charger Control Register 5" */
/* CHG_CR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=IC_VBUS_RDY,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t IC_VBUS_RDY: 1;
        /* @SVD:field=IC_VBAT_HIGH,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t IC_VBAT_HIGH: 1;
        /* @SVD:field=IC_ABOVE_REP,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t IC_ABOVE_REP: 1;
        /* @SVD:field=IC_ABOVE_CC,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t IC_ABOVE_CC: 1;
        /* @SVD:field=IC_CC_MODE,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t IC_CC_MODE: 1;
        /* @SVD:field=IC_CV_MODE,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t IC_CV_MODE: 1;
        /* @SVD:field=IC_EOC_MODE,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t IC_EOC_MODE: 1;
        /* @SVD:field=IC_EOC,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t IC_EOC: 1;
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=IS_VBUS_RDY,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t IS_VBUS_RDY: 1;
        /* @SVD:field=IS_VBAT_HIGH,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t IS_VBAT_HIGH: 1;
        /* @SVD:field=IS_ABOVE_REP,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t IS_ABOVE_REP: 1;
        /* @SVD:field=IS_ABOVE_CC,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t IS_ABOVE_CC: 1;
        /* @SVD:field=IS_CC_MODE,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t IS_CC_MODE: 1;
        /* @SVD:field=IS_CV_MODE,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t IS_CV_MODE: 1;
        /* @SVD:field=IS_EOC_MODE,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t IS_EOC_MODE: 1;
        /* @SVD:field=IS_EOC,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t IS_EOC: 1;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CHG_CR5_Type;

/* @SVD:register=CHG_SR,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="Charger Status Register" */
/* CHG_SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VBUS_RDY_OUT,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t VBUS_RDY_OUT: 1;
        /* @SVD:field=VBAT_HIGH_OUT,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t VBAT_HIGH_OUT: 1;
        /* @SVD:field=VBAT_ABOVE_REP_OUT,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t VBAT_ABOVE_REP_OUT: 1;
        /* @SVD:field=VBAT_ABOVE_CC_OUT,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t VBAT_ABOVE_CC_OUT: 1;
        /* @SVD:field=CC_MODE,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t CC_MODE: 1;
        /* @SVD:field=CV_MODE,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t CV_MODE: 1;
        /* @SVD:field=EOC_MODE,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t EOC_MODE: 1;
        /* @SVD:field=RSVD2,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CHG_STATE,bitOffset=8,bitWidth=7,description="Charger finite state machine" */
        volatile uint32_t CHG_STATE: 7;  /* Charger finite state machine */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CHG_SR_Type;

/* @SVD:register=HPSYS_LDO,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="HPSYS LDO Control Register" */
/* HPSYS_LDO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t EN: 1;
        /* @SVD:field=BP,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t BP: 1;
        /* @SVD:field=VREF,bitOffset=2,bitWidth=4,description="optional voltage (0.9V)" */
        volatile uint32_t VREF: 4;  /* optional voltage (0.9V) */
        /* @SVD:field=VREF2,bitOffset=6,bitWidth=4,description="Lower voltage for deep sleep mode (0.6V)" */
        volatile uint32_t VREF2: 4;  /* Lower voltage for deep sleep mode (0.6V) */
        /* @SVD:field=DLY,bitOffset=10,bitWidth=6,description="HPSYS_LDO power up delay in CLK_LP cycles" */
        volatile uint32_t DLY: 6;  /* HPSYS_LDO power up delay in CLK_LP cycles */
        /* @SVD:field=RDY,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RDY: 1;
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_HPSYS_LDO_Type;

/* @SVD:register=LPSYS_LDO,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="LPSYS LDO Control Register" */
/* LPSYS_LDO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t EN: 1;
        /* @SVD:field=BP,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t BP: 1;
        /* @SVD:field=VREF,bitOffset=2,bitWidth=4,description="optional voltage (1.0V)" */
        volatile uint32_t VREF: 4;  /* optional voltage (1.0V) */
        /* @SVD:field=VREF2,bitOffset=6,bitWidth=4,description="Lower voltage for deep sleep mode (0.6V)" */
        volatile uint32_t VREF2: 4;  /* Lower voltage for deep sleep mode (0.6V) */
        /* @SVD:field=DLY,bitOffset=10,bitWidth=6,description="LPSYS_LDO power up delay in CLK_LP cycles" */
        volatile uint32_t DLY: 6;  /* LPSYS_LDO power up delay in CLK_LP cycles */
        /* @SVD:field=RDY,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RDY: 1;
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_LPSYS_LDO_Type;

/* @SVD:register=HPSYS_SWR,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="HPSYS Switch Register" */
/* HPSYS_SWR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PSW,bitOffset=0,bitWidth=2,description="[0] - RET_LDO; [1] - HPSYS_LDO" */
        volatile uint32_t PSW: 2;  /* [0] - RET_LDO; [1] - HPSYS_LDO */
        /* @SVD:field=PSW_RET,bitOffset=2,bitWidth=2,description="PSW value during DS/SB" */
        volatile uint32_t PSW_RET: 2;  /* PSW value during DS/SB */
        /* @SVD:field=DLY,bitOffset=4,bitWidth=3,description="wait for N cycles before asserting RDY" */
        volatile uint32_t DLY: 3;  /* wait for N cycles before asserting RDY */
        /* @SVD:field=NORET,bitOffset=7,bitWidth=1,description="Cut off VHPMEM entirely during standby. No retention" */
        volatile uint32_t NORET: 1;  /* Cut off VHPMEM entirely during standby. No retention */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
        /* @SVD:field=RDY,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RDY: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_HPSYS_SWR_Type;

/* @SVD:register=LPSYS_SWR,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="LPSYS Switch Register" */
/* LPSYS_SWR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PSW,bitOffset=0,bitWidth=2,description="[0] - RET_LDO; [1] - LPSYS_LDO" */
        volatile uint32_t PSW: 2;  /* [0] - RET_LDO; [1] - LPSYS_LDO */
        /* @SVD:field=PSW_RET,bitOffset=2,bitWidth=2,description="PSW value during DS/SB" */
        volatile uint32_t PSW_RET: 2;  /* PSW value during DS/SB */
        /* @SVD:field=DLY,bitOffset=4,bitWidth=3,description="wait for N cycles before asserting RDY" */
        volatile uint32_t DLY: 3;  /* wait for N cycles before asserting RDY */
        /* @SVD:field=NORET,bitOffset=7,bitWidth=1,description="Cut off VLPMEM entirely during standby. No retention" */
        volatile uint32_t NORET: 1;  /* Cut off VLPMEM entirely during standby. No retention */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
        /* @SVD:field=RDY,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RDY: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_LPSYS_SWR_Type;

/* @SVD:register=PERI_LDO,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="Peripherals LDO" */
/* PERI_LDO 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN_LDO18,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t EN_LDO18: 1;
        /* @SVD:field=LDO18_VREF_SEL,bitOffset=1,bitWidth=4,description="" */
        volatile uint32_t LDO18_VREF_SEL: 4;
        /* @SVD:field=LDO18_PD,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t LDO18_PD: 1;
        /* @SVD:field=RSVD3,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=EN_VDD33_LDO2,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t EN_VDD33_LDO2: 1;
        /* @SVD:field=VDD33_LDO2_SET_VOUT,bitOffset=9,bitWidth=4,description="" */
        volatile uint32_t VDD33_LDO2_SET_VOUT: 4;
        /* @SVD:field=VDD33_LDO2_PD,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t VDD33_LDO2_PD: 1;
        /* @SVD:field=RSVD2,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=EN_VDD33_LDO3,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t EN_VDD33_LDO3: 1;
        /* @SVD:field=VDD33_LDO3_SET_VOUT,bitOffset=17,bitWidth=4,description="" */
        volatile uint32_t VDD33_LDO3_SET_VOUT: 4;
        /* @SVD:field=VDD33_LDO3_PD,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t VDD33_LDO3_PD: 1;
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_PERI_LDO_Type;

/* @SVD:register=PMU_TR,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="PMU Test Register" */
/* PMU_TR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PMU_DC_TR,bitOffset=0,bitWidth=3,description="test point select" */
        volatile uint32_t PMU_DC_TR: 3;  /* test point select */
        /* @SVD:field=PMU_DC_MR,bitOffset=3,bitWidth=3,description="macro select" */
        volatile uint32_t PMU_DC_MR: 3;  /* macro select */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_PMU_TR_Type;

/* @SVD:register=PMU_RSVD,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="PMU Reserved Register" */
/* PMU_RSVD 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESERVE0,bitOffset=0,bitWidth=8,description="" */
        volatile uint32_t RESERVE0: 8;
        /* @SVD:field=RESERVE1,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RESERVE1: 8;
        /* @SVD:field=RESERVE2,bitOffset=16,bitWidth=8,description="" */
        volatile uint32_t RESERVE2: 8;
        /* @SVD:field=RESERVE3,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RESERVE3: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_PMU_RSVD_Type;

/* @SVD:register=HXT_CR1,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="HXT48 Control Register 1" */
/* HXT_CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t EN: 1;
        /* @SVD:field=BUF_EN,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t BUF_EN: 1;
        /* @SVD:field=BUF_DIG_EN,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t BUF_DIG_EN: 1;
        /* @SVD:field=BUF_DIG_STR,bitOffset=3,bitWidth=2,description="" */
        volatile uint32_t BUF_DIG_STR: 2;
        /* @SVD:field=BUF_DLL_EN,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t BUF_DLL_EN: 1;
        /* @SVD:field=BUF_DLL_STR,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t BUF_DLL_STR: 2;
        /* @SVD:field=BUF_AUD_EN,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t BUF_AUD_EN: 1;
        /* @SVD:field=BUF_AUD_STR,bitOffset=9,bitWidth=2,description="" */
        volatile uint32_t BUF_AUD_STR: 2;
        /* @SVD:field=BUF_RF_STR,bitOffset=11,bitWidth=2,description="" */
        volatile uint32_t BUF_RF_STR: 2;
        /* @SVD:field=LDO_VREF,bitOffset=13,bitWidth=4,description="" */
        volatile uint32_t LDO_VREF: 4;
        /* @SVD:field=LDO_FLT_RSEL,bitOffset=17,bitWidth=2,description="" */
        volatile uint32_t LDO_FLT_RSEL: 2;
        /* @SVD:field=GM_EN,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t GM_EN: 1;
        /* @SVD:field=CBANK_SEL,bitOffset=20,bitWidth=10,description="" */
        volatile uint32_t CBANK_SEL: 10;
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_HXT_CR1_Type;

/* @SVD:register=HXT_CR2,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="HXT48 Control Register 2" */
/* HXT_CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=AGC_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t AGC_EN: 1;
        /* @SVD:field=AGC_ISTART_SEL,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t AGC_ISTART_SEL: 1;
        /* @SVD:field=AGC_VTH,bitOffset=2,bitWidth=4,description="" */
        volatile uint32_t AGC_VTH: 4;
        /* @SVD:field=AGC_VINDC,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t AGC_VINDC: 2;
        /* @SVD:field=ACBUF_SEL,bitOffset=8,bitWidth=2,description="" */
        volatile uint32_t ACBUF_SEL: 2;
        /* @SVD:field=ACBUF_RSEL,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t ACBUF_RSEL: 1;
        /* @SVD:field=BUF_SEL2,bitOffset=11,bitWidth=2,description="" */
        volatile uint32_t BUF_SEL2: 2;
        /* @SVD:field=BUF_SEL3,bitOffset=13,bitWidth=2,description="" */
        volatile uint32_t BUF_SEL3: 2;
        /* @SVD:field=IDAC_EN,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t IDAC_EN: 1;
        /* @SVD:field=IDAC,bitOffset=16,bitWidth=10,description="" */
        volatile uint32_t IDAC: 10;
        /* @SVD:field=SDADC_CLKIN_EN,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t SDADC_CLKIN_EN: 1;
        /* @SVD:field=SDADC_CLKDIV1_SEL,bitOffset=27,bitWidth=2,description="" */
        volatile uint32_t SDADC_CLKDIV1_SEL: 2;
        /* @SVD:field=SDADC_CLKDIV2_SEL,bitOffset=29,bitWidth=2,description="" */
        volatile uint32_t SDADC_CLKDIV2_SEL: 2;
        /* @SVD:field=SLEEP_EN,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t SLEEP_EN: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_HXT_CR2_Type;

/* @SVD:register=HXT_CR3,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="HXT48 Control Register 3" */
/* HXT_CR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BUF_DAC_STR,bitOffset=0,bitWidth=2,description="" */
        volatile uint32_t BUF_DAC_STR: 2;
        /* @SVD:field=BUF_OSLO_STR,bitOffset=2,bitWidth=2,description="" */
        volatile uint32_t BUF_OSLO_STR: 2;
        /* @SVD:field=DLY,bitOffset=4,bitWidth=6,description="" */
        volatile uint32_t DLY: 6;
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_HXT_CR3_Type;

/* @SVD:register=HRC_CR,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="HRC48 Control Register" */
/* HRC_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t EN: 1;
        /* @SVD:field=LDO_VREF,bitOffset=1,bitWidth=4,description="" */
        volatile uint32_t LDO_VREF: 4;
        /* @SVD:field=FREQ_TRIM,bitOffset=5,bitWidth=10,description="" */
        volatile uint32_t FREQ_TRIM: 10;
        /* @SVD:field=TEMP_TRIM,bitOffset=15,bitWidth=3,description="" */
        volatile uint32_t TEMP_TRIM: 3;
        /* @SVD:field=CLK96M_EN,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t CLK96M_EN: 1;
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=CLKHP_EN,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t CLKHP_EN: 1;
        /* @SVD:field=CLKHP_SEL,bitOffset=21,bitWidth=2,description="" */
        volatile uint32_t CLKHP_SEL: 2;
        /* @SVD:field=CLKHP_STR,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t CLKHP_STR: 2;
        /* @SVD:field=CLKLP_EN,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t CLKLP_EN: 1;
        /* @SVD:field=CLKLP_SEL,bitOffset=26,bitWidth=2,description="" */
        volatile uint32_t CLKLP_SEL: 2;
        /* @SVD:field=CLKLP_STR,bitOffset=28,bitWidth=2,description="" */
        volatile uint32_t CLKLP_STR: 2;
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
        /* @SVD:field=DLY,bitOffset=31,bitWidth=1,description="number of cycles for BG ready. 0 - one cycle of CLK_LP; 1 - two cycles of CLK_LP" */
        volatile uint32_t DLY: 1;  /* number of cycles for BG ready. 0 - one cycle of CLK_LP; 1 -  */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_HRC_CR_Type;

/* @SVD:register=DBL96_CR,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="DBL96 Control Register" */
/* DBL96_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t EN: 1;
        /* @SVD:field=OUT_EN,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t OUT_EN: 1;
        /* @SVD:field=TODIG_EN,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t TODIG_EN: 1;
        /* @SVD:field=TODIG_STR,bitOffset=3,bitWidth=2,description="" */
        volatile uint32_t TODIG_STR: 2;
        /* @SVD:field=TORF_EN,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t TORF_EN: 1;
        /* @SVD:field=TOOSLO_EN,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t TOOSLO_EN: 1;
        /* @SVD:field=LOOP_RSTB,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t LOOP_RSTB: 1;
        /* @SVD:field=PH_EN,bitOffset=8,bitWidth=4,description="" */
        volatile uint32_t PH_EN: 4;
        /* @SVD:field=DLY_EN,bitOffset=12,bitWidth=4,description="" */
        volatile uint32_t DLY_EN: 4;
        /* @SVD:field=DLY_EXT_EN,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t DLY_EXT_EN: 1;
        /* @SVD:field=DLY_SEL_EXT_EN,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t DLY_SEL_EXT_EN: 1;
        /* @SVD:field=DLY_SEL_EXT,bitOffset=18,bitWidth=11,description="" */
        volatile uint32_t DLY_SEL_EXT: 11;
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_DBL96_CR_Type;

/* @SVD:register=DBL96_CALR,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="DBL96 Calibration Register" */
/* DBL96_CALR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CAL_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t CAL_EN: 1;
        /* @SVD:field=CAL_CLOSE_EXT_EN,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t CAL_CLOSE_EXT_EN: 1;
        /* @SVD:field=CAL_OP,bitOffset=2,bitWidth=11,description="" */
        volatile uint32_t CAL_OP: 11;
        /* @SVD:field=CAL_LOCK,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t CAL_LOCK: 1;
        /* @SVD:field=RSVD,bitOffset=14,bitWidth=18,description="" */
        volatile uint32_t RSVD: 18;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_DBL96_CALR_Type;

/* @SVD:register=CAU_BGR,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="CAU Bandgap Register" */
/* CAU_BGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HPBG_VDDPSW_EN,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t HPBG_VDDPSW_EN: 1;
        /* @SVD:field=HPBG_EN,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t HPBG_EN: 1;
        /* @SVD:field=LPBG_EN,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t LPBG_EN: 1;
        /* @SVD:field=LPBG_VREF06,bitOffset=3,bitWidth=4,description="" */
        volatile uint32_t LPBG_VREF06: 4;
        /* @SVD:field=LPBG_VREF12,bitOffset=7,bitWidth=4,description="" */
        volatile uint32_t LPBG_VREF12: 4;
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CAU_BGR_Type;

/* @SVD:register=CAU_TR,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="CAU Test Register" */
/* CAU_TR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CAU_DC_TR,bitOffset=0,bitWidth=3,description="" */
        volatile uint32_t CAU_DC_TR: 3;
        /* @SVD:field=CAU_DC_BR,bitOffset=3,bitWidth=3,description="" */
        volatile uint32_t CAU_DC_BR: 3;
        /* @SVD:field=CAU_DC_MR,bitOffset=6,bitWidth=3,description="" */
        volatile uint32_t CAU_DC_MR: 3;
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CAU_TR_Type;

/* @SVD:register=CAU_RSVD,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="CAU Reserved Register" */
/* CAU_RSVD 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESERVE0,bitOffset=0,bitWidth=8,description="" */
        volatile uint32_t RESERVE0: 8;
        /* @SVD:field=RESERVE1,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RESERVE1: 8;
        /* @SVD:field=RESERVE2,bitOffset=16,bitWidth=8,description="" */
        volatile uint32_t RESERVE2: 8;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_CAU_RSVD_Type;

/* @SVD:register=WKUP_CNT,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="Wakeup Count Register" */
/* WKUP_CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PIN0_CNT,bitOffset=0,bitWidth=16,description="" */
        volatile uint32_t PIN0_CNT: 16;
        /* @SVD:field=PIN1_CNT,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t PIN1_CNT: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_WKUP_CNT_Type;

/* @SVD:register=PWRKEY_CNT,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="PowerKey Count Register" */
/* PWRKEY_CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=RST_CNT,bitOffset=4,bitWidth=16,description="press high for RST_CNT*16 CLK_WDT cycles to reset the whole chip" */
        volatile uint32_t RST_CNT: 16;  /* press high for RST_CNT*16 CLK_WDT cycles to reset the whole  */
        /* @SVD:field=RSVD,bitOffset=20,bitWidth=12,description="" */
        volatile uint32_t RSVD: 12;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_PWRKEY_CNT_Type;

/* @SVD:register=HPSYS_VOUT,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="HPSYS_VOUT" */
/* HPSYS_VOUT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VOUT,bitOffset=0,bitWidth=4,description="0xD - 1.2V, 0xA - 1.1V, 0x8 - 1.0V, 0x5 - 0.9V" */
        volatile uint32_t VOUT: 4;  /* 0xD - 1.2V, 0xA - 1.1V, 0x8 - 1.0V, 0x5 - 0.9V */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_HPSYS_VOUT_Type;

/* @SVD:register=LPSYS_VOUT,offset=0x0098,size=32,access=read-write,reset=0x00000000,description="LPSYS_VOUT" */
/* LPSYS_VOUT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VOUT,bitOffset=0,bitWidth=4,description="0x8 - 1.0V, 0x5 - 0.9V" */
        volatile uint32_t VOUT: 4;  /* 0x8 - 1.0V, 0x5 - 0.9V */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_LPSYS_VOUT_Type;

/* @SVD:register=BUCK_VOUT,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="BUCK_VOUT" */
/* BUCK_VOUT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=VOUT,bitOffset=0,bitWidth=4,description="0xF - 1.35V, 0xD - 1.25V, 0x9 - 1.05V, 0x6 - 0.9V, 0x2 - 0.7V" */
        volatile uint32_t VOUT: 4;  /* 0xF - 1.35V, 0xD - 1.25V, 0x9 - 1.05V, 0x6 - 0.9V, 0x2 - 0.7 */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PMUC_BUCK_VOUT_Type;

/* PMUC 寄存器结构体 */
typedef struct
{
    PMUC_CR_Type CR;  /* 0x0000 - Control Register */
    PMUC_WER_Type WER;  /* 0x0004 - Wakeup Enable register */
    PMUC_WSR_Type WSR;  /* 0x0008 - Wakeup Status register */
    PMUC_WCR_Type WCR;  /* 0x000C - Wakeup Clear register */
    PMUC_VRTC_CR_Type VRTC_CR;  /* 0x0010 - VRTC Control Register */
    PMUC_VRET_CR_Type VRET_CR;  /* 0x0014 - VRET Control Register */
    PMUC_LRC10_CR_Type LRC10_CR;  /* 0x0018 - RC10K Control Register */
    PMUC_LRC32_CR_Type LRC32_CR;  /* 0x001C - RC32K Control Register */
    PMUC_LXT_CR_Type LXT_CR;  /* 0x0020 - XTAL32K Control Register */
    PMUC_AON_BG_Type AON_BG;  /* 0x0024 - AON Bandgap Register */
    PMUC_AON_LDO_Type AON_LDO;  /* 0x0028 - AON LDO Register */
    PMUC_BUCK_CR1_Type BUCK_CR1;  /* 0x002C - BUCK Control Register 1 */
    PMUC_BUCK_CR2_Type BUCK_CR2;  /* 0x0030 - BUCK Control Register 2 */
    PMUC_CHG_CR1_Type CHG_CR1;  /* 0x0034 - Charger Control Register 1 */
    PMUC_CHG_CR2_Type CHG_CR2;  /* 0x0038 - Charger Control Register 2 */
    PMUC_CHG_CR3_Type CHG_CR3;  /* 0x003C - Charger Control Register 3 */
    PMUC_CHG_CR4_Type CHG_CR4;  /* 0x0040 - Charger Control Register 4 */
    PMUC_CHG_CR5_Type CHG_CR5;  /* 0x0044 - Charger Control Register 5 */
    PMUC_CHG_SR_Type CHG_SR;  /* 0x0048 - Charger Status Register */
    PMUC_HPSYS_LDO_Type HPSYS_LDO;  /* 0x004C - HPSYS LDO Control Register */
    PMUC_LPSYS_LDO_Type LPSYS_LDO;  /* 0x0050 - LPSYS LDO Control Register */
    PMUC_HPSYS_SWR_Type HPSYS_SWR;  /* 0x0054 - HPSYS Switch Register */
    PMUC_LPSYS_SWR_Type LPSYS_SWR;  /* 0x0058 - LPSYS Switch Register */
    PMUC_PERI_LDO_Type PERI_LDO;  /* 0x005C - Peripherals LDO */
    PMUC_PMU_TR_Type PMU_TR;  /* 0x0060 - PMU Test Register */
    PMUC_PMU_RSVD_Type PMU_RSVD;  /* 0x0064 - PMU Reserved Register */
    PMUC_HXT_CR1_Type HXT_CR1;  /* 0x0068 - HXT48 Control Register 1 */
    PMUC_HXT_CR2_Type HXT_CR2;  /* 0x006C - HXT48 Control Register 2 */
    PMUC_HXT_CR3_Type HXT_CR3;  /* 0x0070 - HXT48 Control Register 3 */
    PMUC_HRC_CR_Type HRC_CR;  /* 0x0074 - HRC48 Control Register */
    PMUC_DBL96_CR_Type DBL96_CR;  /* 0x0078 - DBL96 Control Register */
    PMUC_DBL96_CALR_Type DBL96_CALR;  /* 0x007C - DBL96 Calibration Register */
    PMUC_CAU_BGR_Type CAU_BGR;  /* 0x0080 - CAU Bandgap Register */
    PMUC_CAU_TR_Type CAU_TR;  /* 0x0084 - CAU Test Register */
    PMUC_CAU_RSVD_Type CAU_RSVD;  /* 0x0088 - CAU Reserved Register */
    PMUC_WKUP_CNT_Type WKUP_CNT;  /* 0x008C - Wakeup Count Register */
    PMUC_PWRKEY_CNT_Type PWRKEY_CNT;  /* 0x0090 - PowerKey Count Register */
    PMUC_HPSYS_VOUT_Type HPSYS_VOUT;  /* 0x0094 - HPSYS_VOUT */
    PMUC_LPSYS_VOUT_Type LPSYS_VOUT;  /* 0x0098 - LPSYS_VOUT */
    PMUC_BUCK_VOUT_Type BUCK_VOUT;  /* 0x009C - BUCK_VOUT */
} PMUC_TypeDef;

/* ============================================================================ */
/* IWDT 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=IWDT,base=0x500CC000,description="",groupName=WDT */

#define IWDT_BASE                                          (0x500CC000UL)

/* @SVD:register=WDT_CVR0,offset=0x0000,size=32,access=read-write,reset=0x0,description="WatchDog Counter Value 0" */
/* WDT_CVR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COUNT_VALUE_0,bitOffset=0,bitWidth=24,description="Count Value for 1st TimeOut" */
        volatile uint32_t COUNT_VALUE_0: 24;  /* Count Value for 1st TimeOut */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} IWDT_WDT_CVR0_Type;

/* @SVD:register=WDT_CVR1,offset=0x0004,size=32,access=read-write,reset=0x0,description="WatchDog Counter Value 1" */
/* WDT_CVR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COUNT_VALUE_1,bitOffset=0,bitWidth=24,description="Count Value for 2nd TimeOut" */
        volatile uint32_t COUNT_VALUE_1: 24;  /* Count Value for 2nd TimeOut */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} IWDT_WDT_CVR1_Type;

/* @SVD:register=WDT_CR,offset=0x0008,size=32,access=read-write,reset=0x0,description="WatchDog Control Register" */
/* WDT_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESET_LENGTH,bitOffset=0,bitWidth=3,description="reset pulse length in number of wdt clock cycles" */
        volatile uint32_t RESET_LENGTH: 3;  /* reset pulse length in number of wdt clock cycles */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RESPONSE_MODE,bitOffset=4,bitWidth=1,description="0:reset only, 1:interrupt and reset" */
        volatile uint32_t RESPONSE_MODE: 1;  /* 0:reset only, 1:interrupt and reset */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} IWDT_WDT_CR_Type;

/* @SVD:register=WDT_CCR,offset=0x000C,size=32,access=read-write,reset=0x0,description="WatchDog Counter Control Register" */
/* WDT_CCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COUNTER_CONTROL,bitOffset=0,bitWidth=8,description="SinglePulse /Write 8'h76 to restart, write8'h34 to stop, else do nothing" */
        volatile uint32_t COUNTER_CONTROL: 8;  /* SinglePulse /Write 8'h76 to restart, write8'h34 to stop, els */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} IWDT_WDT_CCR_Type;

/* @SVD:register=WDT_ICR,offset=0x0010,size=32,access=read-write,reset=0x0,description="WatchDog Interrupt Clear Register" */
/* WDT_ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INT_CLR,bitOffset=0,bitWidth=1,description="SinglePulse /A pulse to clear interrupt" */
        volatile uint32_t INT_CLR: 1;  /* SinglePulse /A pulse to clear interrupt */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} IWDT_WDT_ICR_Type;

/* @SVD:register=WDT_SR,offset=0x0014,size=32,access=read-write,reset=0x0,description="WatchDog Status Register" */
/* WDT_SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INT_ASSERT,bitOffset=0,bitWidth=1,description="Interrupt assert when 1" */
        volatile uint32_t INT_ASSERT: 1;  /* Interrupt assert when 1 */
        /* @SVD:field=WDT_ACTIVE,bitOffset=1,bitWidth=1,description="Watchdog runs when 1, else 0" */
        volatile uint32_t WDT_ACTIVE: 1;  /* Watchdog runs when 1, else 0 */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} IWDT_WDT_SR_Type;

/* @SVD:register=WDT_WP,offset=0x0018,size=32,access=read-write,reset=0x0,description="WatchDog Write Protect Register" */
/* WDT_WP 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WRPT,bitOffset=0,bitWidth=31,description="write 0x58ab99fc generate write_protect, write 0x51ff8621 to release" */
        volatile uint32_t WRPT: 31;  /* write 0x58ab99fc generate write_protect, write 0x51ff8621 to */
        /* @SVD:field=WRPT_ST,bitOffset=31,bitWidth=1,description="1 indicates write protect is active" */
        volatile uint32_t WRPT_ST: 1;  /* 1 indicates write protect is active */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} IWDT_WDT_WP_Type;

/* @SVD:register=WDT_FG,offset=0x001C,size=32,access=read-write,reset=0x0,description="WatchDog Flag Register" */
/* WDT_FG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RST_FG_CLR,bitOffset=0,bitWidth=1,description="SinglePulse/A pulse to clear reset flag" */
        volatile uint32_t RST_FG_CLR: 1;  /* SinglePulse/A pulse to clear reset flag */
        /* @SVD:field=RST_FG,bitOffset=1,bitWidth=1,description="1 indicates wdt has already reset system" */
        volatile uint32_t RST_FG: 1;  /* 1 indicates wdt has already reset system */
        /* @SVD:field=SYNC_FG_CLR,bitOffset=2,bitWidth=1,description="SinglePulse/A pulse to clear sync flag" */
        volatile uint32_t SYNC_FG_CLR: 1;  /* SinglePulse/A pulse to clear sync flag */
        /* @SVD:field=SYNC_FG,bitOffset=3,bitWidth=1,description="1 indicates one transition from system clk to wdt clk has complicated" */
        volatile uint32_t SYNC_FG: 1;  /* 1 indicates one transition from system clk to wdt clk has co */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} IWDT_WDT_FG_Type;

/* IWDT 寄存器结构体 */
typedef struct
{
    IWDT_WDT_CVR0_Type WDT_CVR0;  /* 0x0000 - WatchDog Counter Value 0 */
    IWDT_WDT_CVR1_Type WDT_CVR1;  /* 0x0004 - WatchDog Counter Value 1 */
    IWDT_WDT_CR_Type WDT_CR;  /* 0x0008 - WatchDog Control Register */
    IWDT_WDT_CCR_Type WDT_CCR;  /* 0x000C - WatchDog Counter Control Register */
    IWDT_WDT_ICR_Type WDT_ICR;  /* 0x0010 - WatchDog Interrupt Clear Register */
    IWDT_WDT_SR_Type WDT_SR;  /* 0x0014 - WatchDog Status Register */
    IWDT_WDT_WP_Type WDT_WP;  /* 0x0018 - WatchDog Write Protect Register */
    IWDT_WDT_FG_Type WDT_FG;  /* 0x001C - WatchDog Flag Register */
} IWDT_TypeDef;

/* ============================================================================ */
/* LPSYS_RCC 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=LPSYS_RCC,base=0x40000000,description="",groupName=LPSYS_RCC */

#define LPSYS_RCC_BASE                                     (0x40000000UL)

/* @SVD:register=RSTR1,offset=0x0000,size=32,access=read-write,reset=0x0,description="Reset Register 1" */
/* RSTR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LCPU,bitOffset=0,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t LCPU: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=DMAC2,bitOffset=1,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t DMAC2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=MAILBOX2,bitOffset=2,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t MAILBOX2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=PINMUX2,bitOffset=3,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t PINMUX2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=PATCH,bitOffset=4,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t PATCH: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=USART4,bitOffset=5,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t USART4: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=USART5,bitOffset=6,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t USART5: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD4,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=PTC2,bitOffset=8,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t PTC2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=BTIM3,bitOffset=9,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t BTIM3: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=BTIM4,bitOffset=10,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t BTIM4: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=4,description="" */
        volatile uint32_t RSVD3: 4;
        /* @SVD:field=SYSCFG2,bitOffset=15,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t SYSCFG2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=GPIO2,bitOffset=16,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t GPIO2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RFC,bitOffset=18,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t RFC: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=PHY,bitOffset=19,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t PHY: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=MAC,bitOffset=20,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t MAC: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=CRC2,bitOffset=21,bitWidth=1,description="0 - no reset; 1 - reset" */
        volatile uint32_t CRC2: 1;  /* 0 - no reset; 1 - reset */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_RCC_RSTR1_Type;

/* @SVD:register=ENR1,offset=0x0004,size=32,access=read-write,reset=0x0,description="Enable Register 1" */
/* ENR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=DMAC2,bitOffset=1,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t DMAC2: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=MAILBOX2,bitOffset=2,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t MAILBOX2: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=PINMUX2,bitOffset=3,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t PINMUX2: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=PATCH,bitOffset=4,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t PATCH: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=USART4,bitOffset=5,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t USART4: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=USART5,bitOffset=6,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t USART5: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=SECU2,bitOffset=7,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t SECU2: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=PTC2,bitOffset=8,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t PTC2: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=BTIM3,bitOffset=9,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t BTIM3: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=BTIM4,bitOffset=10,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t BTIM4: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=4,description="" */
        volatile uint32_t RSVD3: 4;
        /* @SVD:field=SYSCFG2,bitOffset=15,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t SYSCFG2: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=GPIO2,bitOffset=16,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t GPIO2: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RFC,bitOffset=18,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t RFC: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=PHY,bitOffset=19,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t PHY: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=MAC,bitOffset=20,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t MAC: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=CRC2,bitOffset=21,bitWidth=1,description="0 - disabled; 1 - enabled" */
        volatile uint32_t CRC2: 1;  /* 0 - disabled; 1 - enabled */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_RCC_ENR1_Type;

/* @SVD:register=ESR1,offset=0x0008,size=32,access=read-write,reset=0x0,description="Enable Set Register 1" */
/* ESR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=DMAC2,bitOffset=1,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t DMAC2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=MAILBOX2,bitOffset=2,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t MAILBOX2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=PINMUX2,bitOffset=3,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t PINMUX2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=PATCH,bitOffset=4,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t PATCH: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=USART4,bitOffset=5,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t USART4: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=USART5,bitOffset=6,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t USART5: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=SECU2,bitOffset=7,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t SECU2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=PTC2,bitOffset=8,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t PTC2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=BTIM3,bitOffset=9,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t BTIM3: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=BTIM4,bitOffset=10,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t BTIM4: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=4,description="" */
        volatile uint32_t RSVD3: 4;
        /* @SVD:field=SYSCFG2,bitOffset=15,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t SYSCFG2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=GPIO2,bitOffset=16,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t GPIO2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RFC,bitOffset=18,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t RFC: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=PHY,bitOffset=19,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t PHY: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=MAC,bitOffset=20,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t MAC: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=CRC2,bitOffset=21,bitWidth=1,description="write 1 to set module enable, write 0 has no effect" */
        volatile uint32_t CRC2: 1;  /* write 1 to set module enable, write 0 has no effect */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_RCC_ESR1_Type;

/* @SVD:register=ECR1,offset=0x000C,size=32,access=read-write,reset=0x0,description="Enable Clear Register 1" */
/* ECR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=DMAC2,bitOffset=1,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t DMAC2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=MAILBOX2,bitOffset=2,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t MAILBOX2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=PINMUX2,bitOffset=3,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t PINMUX2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=PATCH,bitOffset=4,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t PATCH: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=USART4,bitOffset=5,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t USART4: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=USART5,bitOffset=6,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t USART5: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=SECU2,bitOffset=7,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t SECU2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=PTC2,bitOffset=8,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t PTC2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=BTIM3,bitOffset=9,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t BTIM3: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=BTIM4,bitOffset=10,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t BTIM4: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=4,description="" */
        volatile uint32_t RSVD3: 4;
        /* @SVD:field=SYSCFG2,bitOffset=15,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t SYSCFG2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=GPIO2,bitOffset=16,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t GPIO2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD2,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RFC,bitOffset=18,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t RFC: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=PHY,bitOffset=19,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t PHY: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=MAC,bitOffset=20,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t MAC: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=CRC2,bitOffset=21,bitWidth=1,description="write 1 to clear module enable, write 0 has no effect" */
        volatile uint32_t CRC2: 1;  /* write 1 to clear module enable, write 0 has no effect */
        /* @SVD:field=RSVD,bitOffset=22,bitWidth=10,description="" */
        volatile uint32_t RSVD: 10;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_RCC_ECR1_Type;

/* @SVD:register=CSR,offset=0x0010,size=32,access=read-write,reset=0x0,description="Clock Select Register" */
/* CSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEL_SYS,bitOffset=0,bitWidth=1,description="select clk_lpsys source\n              0 - clk_hrc48; 1 - clk_hxt48" */
        volatile uint32_t SEL_SYS: 1;  /* select clk_lpsys source               0 - clk_hrc48; 1 - clk */
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=SEL_SYS_LP,bitOffset=2,bitWidth=1,description="select clk_lpsys source\n              0 - selected by SEL_SYS; 1 - clk_wdt" */
        volatile uint32_t SEL_SYS_LP: 1;  /* select clk_lpsys source               0 - selected by SEL_SY */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=SEL_PERI,bitOffset=4,bitWidth=1,description="select clk_peri_lpsys source\n              0 - clk_hrc48; 1 - clk_hxt48" */
        volatile uint32_t SEL_PERI: 1;  /* select clk_peri_lpsys source               0 - clk_hrc48; 1  */
        /* @SVD:field=SEL_TICK,bitOffset=5,bitWidth=2,description="select clock source for systick reference\n              0 - clk_rtc; 1 - reserved; 2 - clk_hrc48; 3 - clk_hxt48" */
        volatile uint32_t SEL_TICK: 2;  /* select clock source for systick reference               0 -  */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_RCC_CSR_Type;

/* @SVD:register=CFGR,offset=0x0014,size=32,access=read-write,reset=0x0,description="Clock Configuration Register" */
/* CFGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HDIV1,bitOffset=0,bitWidth=6,description="hclk_lpsys = clk_lpsys / HDIV\n              if HDIV=0, hclk_lpsys = clk_lpsys" */
        volatile uint32_t HDIV1: 6;  /* hclk_lpsys = clk_lpsys / HDIV               if HDIV=0, hclk_ */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=PDIV1,bitOffset=8,bitWidth=3,description="pclk1_lpsys = hclk_lpsys / (2^PDIV1), by default divided by 2" */
        volatile uint32_t PDIV1: 3;  /* pclk1_lpsys = hclk_lpsys / (2^PDIV1), by default divided by  */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=PDIV2,bitOffset=12,bitWidth=3,description="pclk2_lpsys = hclk_lpsys / (2^PDIV2), by default divided by 32" */
        volatile uint32_t PDIV2: 3;  /* pclk2_lpsys = hclk_lpsys / (2^PDIV2), by default divided by  */
        /* @SVD:field=RSVD2,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=MACDIV,bitOffset=16,bitWidth=4,description="MAC clock divider\n              MACCLK = hclk_lpsys / MACDIV" */
        volatile uint32_t MACDIV: 4;  /* MAC clock divider               MACCLK = hclk_lpsys / MACDIV */
        /* @SVD:field=MACFREQ,bitOffset=20,bitWidth=5,description="clock frequency of MAC clock" */
        volatile uint32_t MACFREQ: 5;  /* clock frequency of MAC clock */
        /* @SVD:field=TICKDIV,bitOffset=25,bitWidth=6,description="systick reference clock is systick reference clock source (selected by SEL_TICK) devided by TICKDIV" */
        volatile uint32_t TICKDIV: 6;  /* systick reference clock is systick reference clock source (s */
        /* @SVD:field=RSVD,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_RCC_CFGR_Type;

/* @SVD:register=DBGR,offset=0x0018,size=32,access=read-write,reset=0x0,description="Debug Register" */
/* DBGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SYSCLK_AON,bitOffset=0,bitWidth=1,description="for debug only" */
        volatile uint32_t SYSCLK_AON: 1;  /* for debug only */
        /* @SVD:field=SYSCLK_SWLP,bitOffset=1,bitWidth=1,description="for debug only" */
        volatile uint32_t SYSCLK_SWLP: 1;  /* for debug only */
        /* @SVD:field=FORCE_BUS,bitOffset=2,bitWidth=1,description="for debug only" */
        volatile uint32_t FORCE_BUS: 1;  /* for debug only */
        /* @SVD:field=FORCE_MAC,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t FORCE_MAC: 1;  /* for debug only */
        /* @SVD:field=FORCE_GPIO,bitOffset=4,bitWidth=1,description="for debug only" */
        volatile uint32_t FORCE_GPIO: 1;  /* for debug only */
        /* @SVD:field=SYSCLK_SWBT,bitOffset=5,bitWidth=1,description="If set to 1, clk_lpsys will: \n              switch from clk_hrc48 to clk_hxt48 when MAC active; \n              switch from clk_hxt48 to clk_hrc48 when MAC sleep;" */
        volatile uint32_t SYSCLK_SWBT: 1;  /* If set to 1, clk_lpsys will:                switch from clk_ */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_RCC_DBGR_Type;

/* LPSYS_RCC 寄存器结构体 */
typedef struct
{
    LPSYS_RCC_RSTR1_Type RSTR1;  /* 0x0000 - Reset Register 1 */
    LPSYS_RCC_ENR1_Type ENR1;  /* 0x0004 - Enable Register 1 */
    LPSYS_RCC_ESR1_Type ESR1;  /* 0x0008 - Enable Set Register 1 */
    LPSYS_RCC_ECR1_Type ECR1;  /* 0x000C - Enable Clear Register 1 */
    LPSYS_RCC_CSR_Type CSR;  /* 0x0010 - Clock Select Register */
    LPSYS_RCC_CFGR_Type CFGR;  /* 0x0014 - Clock Configuration Register */
    LPSYS_RCC_DBGR_Type DBGR;  /* 0x0018 - Debug Register */
} LPSYS_RCC_TypeDef;

/* ============================================================================ */
/* DMAC2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=DMAC2,base=0x40001000,description="",groupName=DMAC */

#define DMAC2_BASE                                         (0x40001000UL)

/* @SVD:register=ISR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="ISR" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=GIF1,bitOffset=0,bitWidth=1,description="channel global interrupt flag. Set when any of TEIF/HTIF/TCIF asserted. Cleared when TEIF/HTIF/TCIF all cleared." */
        volatile uint32_t GIF1: 1;  /* channel global interrupt flag. Set when any of TEIF/HTIF/TCI */
        /* @SVD:field=TCIF1,bitOffset=1,bitWidth=1,description="channel transfer complete flag. Set when all NDT are transferred.  Cleared when write 1 to CTCIF or CGIF." */
        volatile uint32_t TCIF1: 1;  /* channel transfer complete flag. Set when all NDT are transfe */
        /* @SVD:field=HTIF1,bitOffset=2,bitWidth=1,description="channel half transfer flag. Set when half NDT are transferred. Cleared when write 1 to CHTIF or CGIF." */
        volatile uint32_t HTIF1: 1;  /* channel half transfer flag. Set when half NDT are transferre */
        /* @SVD:field=TEIF1,bitOffset=3,bitWidth=1,description="channel transfer error flag. Set when bus error detected. Cleared when write 1 to CTEIF or CGIF." */
        volatile uint32_t TEIF1: 1;  /* channel transfer error flag. Set when bus error detected. Cl */
        /* @SVD:field=GIF2,bitOffset=4,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF2: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF2,bitOffset=5,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF2: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF2,bitOffset=6,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF2: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF2,bitOffset=7,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF2: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF3,bitOffset=8,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF3: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF3,bitOffset=9,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF3: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF3,bitOffset=10,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF3: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF3,bitOffset=11,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF3: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF4,bitOffset=12,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF4: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF4,bitOffset=13,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF4: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF4,bitOffset=14,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF4: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF4,bitOffset=15,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF4: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF5,bitOffset=16,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF5: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF5,bitOffset=17,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF5: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF5,bitOffset=18,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF5: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF5,bitOffset=19,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF5: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF6,bitOffset=20,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF6: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF6,bitOffset=21,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF6: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF6,bitOffset=22,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF6: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF6,bitOffset=23,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF6: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF7,bitOffset=24,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF7: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF7,bitOffset=25,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF7: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF7,bitOffset=26,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF7: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF7,bitOffset=27,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF7: 1;  /* channel transfer error flag */
        /* @SVD:field=GIF8,bitOffset=28,bitWidth=1,description="channel global interrupt flag" */
        volatile uint32_t GIF8: 1;  /* channel global interrupt flag */
        /* @SVD:field=TCIF8,bitOffset=29,bitWidth=1,description="channel transfer complete flag" */
        volatile uint32_t TCIF8: 1;  /* channel transfer complete flag */
        /* @SVD:field=HTIF8,bitOffset=30,bitWidth=1,description="channel half transfer flag" */
        volatile uint32_t HTIF8: 1;  /* channel half transfer flag */
        /* @SVD:field=TEIF8,bitOffset=31,bitWidth=1,description="channel transfer error flag" */
        volatile uint32_t TEIF8: 1;  /* channel transfer error flag */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_ISR_Type;

/* @SVD:register=IFCR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="IFCR" */
/* IFCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CGIF1,bitOffset=0,bitWidth=1,description="CGIF,  global interrupt flag clear. Write 1 to clear all TEIF/HTIF/TCIF." */
        volatile uint32_t CGIF1: 1;  /* CGIF,  global interrupt flag clear. Write 1 to clear all TEI */
        /* @SVD:field=CTCIF1,bitOffset=1,bitWidth=1,description="CTCIF, transfer complete flag clear. Write 1 to clear TCIF." */
        volatile uint32_t CTCIF1: 1;  /* CTCIF, transfer complete flag clear. Write 1 to clear TCIF. */
        /* @SVD:field=CHTIF1,bitOffset=2,bitWidth=1,description="CHTIF, half transfer flag clear. Write 1 to clear HTIF." */
        volatile uint32_t CHTIF1: 1;  /* CHTIF, half transfer flag clear. Write 1 to clear HTIF. */
        /* @SVD:field=CTEIF1,bitOffset=3,bitWidth=1,description="CTEIF, transfer error flag clear. Write 1 to clear TEIF." */
        volatile uint32_t CTEIF1: 1;  /* CTEIF, transfer error flag clear. Write 1 to clear TEIF. */
        /* @SVD:field=CGIF2,bitOffset=4,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF2: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF2,bitOffset=5,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF2: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF2,bitOffset=6,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF2: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF2,bitOffset=7,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF2: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF3,bitOffset=8,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF3: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF3,bitOffset=9,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF3: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF3,bitOffset=10,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF3: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF3,bitOffset=11,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF3: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF4,bitOffset=12,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF4: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF4,bitOffset=13,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF4: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF4,bitOffset=14,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF4: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF4,bitOffset=15,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF4: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF5,bitOffset=16,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF5: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF5,bitOffset=17,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF5: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF5,bitOffset=18,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF5: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF5,bitOffset=19,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF5: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF6,bitOffset=20,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF6: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF6,bitOffset=21,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF6: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF6,bitOffset=22,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF6: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF6,bitOffset=23,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF6: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF7,bitOffset=24,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF7: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF7,bitOffset=25,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF7: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF7,bitOffset=26,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF7: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF7,bitOffset=27,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF7: 1;  /* CTEIF, transfer error flag clear */
        /* @SVD:field=CGIF8,bitOffset=28,bitWidth=1,description="CGIF,  global interrupt flag clear" */
        volatile uint32_t CGIF8: 1;  /* CGIF,  global interrupt flag clear */
        /* @SVD:field=CTCIF8,bitOffset=29,bitWidth=1,description="CTCIF, transfer complete flag clear" */
        volatile uint32_t CTCIF8: 1;  /* CTCIF, transfer complete flag clear */
        /* @SVD:field=CHTIF8,bitOffset=30,bitWidth=1,description="CHTIF, half transfer flag clear" */
        volatile uint32_t CHTIF8: 1;  /* CHTIF, half transfer flag clear */
        /* @SVD:field=CTEIF8,bitOffset=31,bitWidth=1,description="CTEIF, transfer error flag clear" */
        volatile uint32_t CTEIF8: 1;  /* CTEIF, transfer error flag clear */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_IFCR_Type;

/* @SVD:register=CCR1,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="CCR1" */
/* CCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CCR1_Type;

/* @SVD:register=CNDTR1,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="CNDTR1" */
/* CNDTR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CNDTR1_Type;

/* @SVD:register=CPAR1,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="CPAR1" */
/* CPAR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CPAR1_Type;

/* @SVD:register=CM0AR1,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="CM0AR1" */
/* CM0AR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="memory address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* memory address               It contains the base address of */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CM0AR1_Type;

/* @SVD:register=CBSR1,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="CBSR1" */
/* CBSR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CBSR1_Type;

/* @SVD:register=CCR2,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="CCR2" */
/* CCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CCR2_Type;

/* @SVD:register=CNDTR2,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="CNDTR2" */
/* CNDTR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CNDTR2_Type;

/* @SVD:register=CPAR2,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="CPAR2" */
/* CPAR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CPAR2_Type;

/* @SVD:register=CM0AR2,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="CM0AR2" */
/* CM0AR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CM0AR2_Type;

/* @SVD:register=CBSR2,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="CBSR2" */
/* CBSR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CBSR2_Type;

/* @SVD:register=CCR3,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="CCR3" */
/* CCR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CCR3_Type;

/* @SVD:register=CNDTR3,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="CNDTR3" */
/* CNDTR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CNDTR3_Type;

/* @SVD:register=CPAR3,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="CPAR3" */
/* CPAR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CPAR3_Type;

/* @SVD:register=CM0AR3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="CM0AR3" */
/* CM0AR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CM0AR3_Type;

/* @SVD:register=CBSR3,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="CBSR3" */
/* CBSR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CBSR3_Type;

/* @SVD:register=CCR4,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="CCR4" */
/* CCR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CCR4_Type;

/* @SVD:register=CNDTR4,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="CNDTR4" */
/* CNDTR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CNDTR4_Type;

/* @SVD:register=CPAR4,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="CPAR4" */
/* CPAR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CPAR4_Type;

/* @SVD:register=CM0AR4,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="CM0AR4" */
/* CM0AR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CM0AR4_Type;

/* @SVD:register=CBSR4,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="CBSR4" */
/* CBSR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CBSR4_Type;

/* @SVD:register=CCR5,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="CCR5" */
/* CCR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CCR5_Type;

/* @SVD:register=CNDTR5,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="CNDTR5" */
/* CNDTR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CNDTR5_Type;

/* @SVD:register=CPAR5,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="CPAR5" */
/* CPAR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CPAR5_Type;

/* @SVD:register=CM0AR5,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="CM0AR5" */
/* CM0AR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CM0AR5_Type;

/* @SVD:register=CBSR5,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="CBSR5" */
/* CBSR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CBSR5_Type;

/* @SVD:register=CCR6,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="CCR6" */
/* CCR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CCR6_Type;

/* @SVD:register=CNDTR6,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="CNDTR6" */
/* CNDTR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CNDTR6_Type;

/* @SVD:register=CPAR6,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="CPAR6" */
/* CPAR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CPAR6_Type;

/* @SVD:register=CM0AR6,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="CM0AR6" */
/* CM0AR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CM0AR6_Type;

/* @SVD:register=CBSR6,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="CBSR6" */
/* CBSR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CBSR6_Type;

/* @SVD:register=CCR7,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="CCR7" */
/* CCR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CCR7_Type;

/* @SVD:register=CNDTR7,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="CNDTR7" */
/* CNDTR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CNDTR7_Type;

/* @SVD:register=CPAR7,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="CPAR7" */
/* CPAR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CPAR7_Type;

/* @SVD:register=CM0AR7,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="CM0AR7" */
/* CM0AR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CM0AR7_Type;

/* @SVD:register=CBSR7,offset=0x0090,size=32,access=read-write,reset=0x00000000,description="CBSR7" */
/* CBSR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non memory-to-memory mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non memory-to-memory mode               When B */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CBSR7_Type;

/* @SVD:register=CCR8,offset=0x0094,size=32,access=read-write,reset=0x00000000,description="CCR8" */
/* CCR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EN,bitOffset=0,bitWidth=1,description="channel enable\n              When a channel transfer error occurs, this bit is cleared by hardware. It can not be set again by software (channel x re-activated) until the TEIFx bit of the ISR register is cleared (by setting the CTEIFx bit of the IFCR register).\n              0: disabled\n              1: enabled" */
        volatile uint32_t EN: 1;  /* channel enable               When a channel transfer error o */
        /* @SVD:field=TCIE,bitOffset=1,bitWidth=1,description="transfer complete interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TCIE: 1;  /* transfer complete interrupt enable               0: disabled */
        /* @SVD:field=HTIE,bitOffset=2,bitWidth=1,description="half transfer interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t HTIE: 1;  /* half transfer interrupt enable               0: disabled     */
        /* @SVD:field=TEIE,bitOffset=3,bitWidth=1,description="transfer error interrupt enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t TEIE: 1;  /* transfer error interrupt enable               0: disabled    */
        /* @SVD:field=DIR,bitOffset=4,bitWidth=1,description="data transfer direction\n              This bit must be set only in memory-to-peripheral and peripheral-to-memory modes.\n              0: read from peripheral\n                 Source attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Destination attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode.\n              1: read from memory\n                 Destination attributes are defined by PSIZE and PINC, plus the CPARx register. This is still valid in a memory-to-memory mode.\n                 Source attributes are defined by MSIZE and MINC, plus the CM0ARx register. This is still valid in a peripheral-to-peripheral mode." */
        volatile uint32_t DIR: 1;  /* data transfer direction               This bit must be set o */
        /* @SVD:field=CIRC,bitOffset=5,bitWidth=1,description="circular mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t CIRC: 1;  /* circular mode               0: disabled               1: ena */
        /* @SVD:field=PINC,bitOffset=6,bitWidth=1,description="peripheral increment mode\n              Defines the increment mode for each DMA transfer to the identified peripheral.\n              n memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the \n              memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and \n              the peripheral source if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t PINC: 1;  /* peripheral increment mode               Defines the incremen */
        /* @SVD:field=MINC,bitOffset=7,bitWidth=1,description="memory increment mode\n              Defines the increment mode for each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              0: disabled\n              1: enabled" */
        volatile uint32_t MINC: 1;  /* memory increment mode               Defines the increment mo */
        /* @SVD:field=PSIZE,bitOffset=8,bitWidth=2,description="peripheral size\n              Defines the data size of each DMA transfer to the identified peripheral.\n              In memory-to-memory mode, this field identifies the memory destination if DIR = 1 and the memory source if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral destination if DIR = 1 and the peripheral source if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t PSIZE: 2;  /* peripheral size               Defines the data size of each  */
        /* @SVD:field=MSIZE,bitOffset=10,bitWidth=2,description="memory size\n              Defines the data size of each DMA transfer to the identified memory.\n              In memory-to-memory mode, this field identifies the memory source if DIR = 1 and the memory destination if DIR = 0.\n              In peripheral-to-peripheral mode, this field identifies the peripheral source if DIR = 1 and the peripheral destination if DIR = 0.\n              00: 8 bits\n              01: 16 bits\n              10: 32 bits\n              11: reserved" */
        volatile uint32_t MSIZE: 2;  /* memory size               Defines the data size of each DMA  */
        /* @SVD:field=PL,bitOffset=12,bitWidth=2,description="priority level\n              00: low\n              01: medium\n              10: high\n              11: very high" */
        volatile uint32_t PL: 2;  /* priority level               00: low               01: mediu */
        /* @SVD:field=MEM2MEM,bitOffset=14,bitWidth=1,description="memory-to-memory mode\n              0: disabled\n              1: enabled" */
        volatile uint32_t MEM2MEM: 1;  /* memory-to-memory mode               0: disabled              */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CCR8_Type;

/* @SVD:register=CNDTR8,offset=0x0098,size=32,access=read-write,reset=0x00000000,description="CNDTR8" */
/* CNDTR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=NDT,bitOffset=0,bitWidth=16,description="number of data to transfer (0 to 2^16 - 1)\n              This field is updated by hardware when the channel is enabled:\n                 It is decremented after each single DMA 'read followed by write' transfer, indicating the remaining amount of data items to transfer.\n                 It is kept at zero when the programmed amount of data to transfer is reached, if the channel is not in circular mode (CIRC = 0 in the CCRx register).\n                 It is reloaded automatically by the previously programmed value, when the transfer is complete, if the channel is in circular mode (CIRC = 1).\n              If this field is zero, no transfer can be served whatever the channel status (enabled or not)." */
        volatile uint32_t NDT: 16;  /* number of data to transfer (0 to 2^16 - 1)               Thi */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CNDTR8_Type;

/* @SVD:register=CPAR8,offset=0x009C,size=32,access=read-write,reset=0x00000000,description="CPAR8" */
/* CPAR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the peripheral data register from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory destination address if DIR = 1 and the memory source address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral destination address DIR = 1 and the peripheral source address if DIR = 0." */
        volatile uint32_t PA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CPAR8_Type;

/* @SVD:register=CM0AR8,offset=0x00A0,size=32,access=read-write,reset=0x00000000,description="CM0AR8" */
/* CM0AR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MA,bitOffset=0,bitWidth=32,description="peripheral address\n              It contains the base address of the memory from/to which the data will be read/written.\n              In memory-to-memory mode, this register identifies the memory source address if DIR = 1 and the memory destination address if DIR = 0.\n              In peripheral-to-peripheral mode, this register identifies the peripheral source address DIR = 1 and the peripheral destination address if DIR = 0." */
        volatile uint32_t MA: 32;  /* peripheral address               It contains the base addres */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CM0AR8_Type;

/* @SVD:register=CBSR8,offset=0x00A4,size=32,access=read-write,reset=0x00000000,description="CBSR8" */
/* CBSR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BS,bitOffset=0,bitWidth=8,description="burst size in non-m2m mode\n              When BS>1, DMA will transfer for BS times for each request if left NDT is larger than BS, or else transfer for left NDT times.\n              When BS=0 or 1, DMA will always do single transfer for each request.\n              In memory-to-memory mode, BS is ignored." */
        volatile uint32_t BS: 8;  /* burst size in non-m2m mode               When BS>1, DMA will */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CBSR8_Type;

/* @SVD:register=CSELR1,offset=0x00A8,size=32,access=read-write,reset=0x00000000,description="CSELR1" */
/* CSELR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=C1S,bitOffset=0,bitWidth=6,description="DMA channel 1 selection" */
        volatile uint32_t C1S: 6;  /* DMA channel 1 selection */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=C2S,bitOffset=8,bitWidth=6,description="DMA channel 2 selection" */
        volatile uint32_t C2S: 6;  /* DMA channel 2 selection */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=C3S,bitOffset=16,bitWidth=6,description="DMA channel 3 selection" */
        volatile uint32_t C3S: 6;  /* DMA channel 3 selection */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=C4S,bitOffset=24,bitWidth=6,description="DMA channel 4 selection" */
        volatile uint32_t C4S: 6;  /* DMA channel 4 selection */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CSELR1_Type;

/* @SVD:register=CSELR2,offset=0x00AC,size=32,access=read-write,reset=0x00000000,description="CSELR2" */
/* CSELR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=C5S,bitOffset=0,bitWidth=6,description="DMA channel 5 selection" */
        volatile uint32_t C5S: 6;  /* DMA channel 5 selection */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=C6S,bitOffset=8,bitWidth=6,description="DMA channel 6 selection" */
        volatile uint32_t C6S: 6;  /* DMA channel 6 selection */
        /* @SVD:field=RSVD3,bitOffset=14,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=C7S,bitOffset=16,bitWidth=6,description="DMA channel 7 selection" */
        volatile uint32_t C7S: 6;  /* DMA channel 7 selection */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=C8S,bitOffset=24,bitWidth=6,description="DMA channel 8 selection" */
        volatile uint32_t C8S: 6;  /* DMA channel 8 selection */
        /* @SVD:field=RSVD,bitOffset=30,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} DMAC2_CSELR2_Type;

/* DMAC2 寄存器结构体 */
typedef struct
{
    DMAC2_ISR_Type ISR;  /* 0x0000 - ISR */
    DMAC2_IFCR_Type IFCR;  /* 0x0004 - IFCR */
    DMAC2_CCR1_Type CCR1;  /* 0x0008 - CCR1 */
    DMAC2_CNDTR1_Type CNDTR1;  /* 0x000C - CNDTR1 */
    DMAC2_CPAR1_Type CPAR1;  /* 0x0010 - CPAR1 */
    DMAC2_CM0AR1_Type CM0AR1;  /* 0x0014 - CM0AR1 */
    DMAC2_CBSR1_Type CBSR1;  /* 0x0018 - CBSR1 */
    DMAC2_CCR2_Type CCR2;  /* 0x001C - CCR2 */
    DMAC2_CNDTR2_Type CNDTR2;  /* 0x0020 - CNDTR2 */
    DMAC2_CPAR2_Type CPAR2;  /* 0x0024 - CPAR2 */
    DMAC2_CM0AR2_Type CM0AR2;  /* 0x0028 - CM0AR2 */
    DMAC2_CBSR2_Type CBSR2;  /* 0x002C - CBSR2 */
    DMAC2_CCR3_Type CCR3;  /* 0x0030 - CCR3 */
    DMAC2_CNDTR3_Type CNDTR3;  /* 0x0034 - CNDTR3 */
    DMAC2_CPAR3_Type CPAR3;  /* 0x0038 - CPAR3 */
    DMAC2_CM0AR3_Type CM0AR3;  /* 0x003C - CM0AR3 */
    DMAC2_CBSR3_Type CBSR3;  /* 0x0040 - CBSR3 */
    DMAC2_CCR4_Type CCR4;  /* 0x0044 - CCR4 */
    DMAC2_CNDTR4_Type CNDTR4;  /* 0x0048 - CNDTR4 */
    DMAC2_CPAR4_Type CPAR4;  /* 0x004C - CPAR4 */
    DMAC2_CM0AR4_Type CM0AR4;  /* 0x0050 - CM0AR4 */
    DMAC2_CBSR4_Type CBSR4;  /* 0x0054 - CBSR4 */
    DMAC2_CCR5_Type CCR5;  /* 0x0058 - CCR5 */
    DMAC2_CNDTR5_Type CNDTR5;  /* 0x005C - CNDTR5 */
    DMAC2_CPAR5_Type CPAR5;  /* 0x0060 - CPAR5 */
    DMAC2_CM0AR5_Type CM0AR5;  /* 0x0064 - CM0AR5 */
    DMAC2_CBSR5_Type CBSR5;  /* 0x0068 - CBSR5 */
    DMAC2_CCR6_Type CCR6;  /* 0x006C - CCR6 */
    DMAC2_CNDTR6_Type CNDTR6;  /* 0x0070 - CNDTR6 */
    DMAC2_CPAR6_Type CPAR6;  /* 0x0074 - CPAR6 */
    DMAC2_CM0AR6_Type CM0AR6;  /* 0x0078 - CM0AR6 */
    DMAC2_CBSR6_Type CBSR6;  /* 0x007C - CBSR6 */
    DMAC2_CCR7_Type CCR7;  /* 0x0080 - CCR7 */
    DMAC2_CNDTR7_Type CNDTR7;  /* 0x0084 - CNDTR7 */
    DMAC2_CPAR7_Type CPAR7;  /* 0x0088 - CPAR7 */
    DMAC2_CM0AR7_Type CM0AR7;  /* 0x008C - CM0AR7 */
    DMAC2_CBSR7_Type CBSR7;  /* 0x0090 - CBSR7 */
    DMAC2_CCR8_Type CCR8;  /* 0x0094 - CCR8 */
    DMAC2_CNDTR8_Type CNDTR8;  /* 0x0098 - CNDTR8 */
    DMAC2_CPAR8_Type CPAR8;  /* 0x009C - CPAR8 */
    DMAC2_CM0AR8_Type CM0AR8;  /* 0x00A0 - CM0AR8 */
    DMAC2_CBSR8_Type CBSR8;  /* 0x00A4 - CBSR8 */
    DMAC2_CSELR1_Type CSELR1;  /* 0x00A8 - CSELR1 */
    DMAC2_CSELR2_Type CSELR2;  /* 0x00AC - CSELR2 */
} DMAC2_TypeDef;

/* ============================================================================ */
/* LPSYS_PINMUX 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=LPSYS_PINMUX,base=0x40003000,description="",groupName=LPSYS_PINMUX */

#define LPSYS_PINMUX_BASE                                  (0x40003000UL)

/* @SVD:register=PAD_PB00,offset=0x0000,size=32,access=read-write,reset=0x0,description="PAD_PB00" */
/* PAD_PB00 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=3,description="Function Select" */
        volatile uint32_t FSEL: 3;  /* Function Select */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_PINMUX_PAD_PB00_Type;

/* @SVD:register=PAD_PB01,offset=0x0004,size=32,access=read-write,reset=0x0,description="PAD_PB01" */
/* PAD_PB01 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=3,description="Function Select" */
        volatile uint32_t FSEL: 3;  /* Function Select */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_PINMUX_PAD_PB01_Type;

/* @SVD:register=PAD_PB02,offset=0x0008,size=32,access=read-write,reset=0x0,description="PAD_PB02" */
/* PAD_PB02 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=3,description="Function Select" */
        volatile uint32_t FSEL: 3;  /* Function Select */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_PINMUX_PAD_PB02_Type;

/* @SVD:register=PAD_PB03,offset=0x000C,size=32,access=read-write,reset=0x0,description="PAD_PB03" */
/* PAD_PB03 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FSEL,bitOffset=0,bitWidth=3,description="Function Select" */
        volatile uint32_t FSEL: 3;  /* Function Select */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=PE,bitOffset=4,bitWidth=1,description="Pull Enable. Logic HIGH enables week pull device" */
        volatile uint32_t PE: 1;  /* Pull Enable. Logic HIGH enables week pull device */
        /* @SVD:field=PS,bitOffset=5,bitWidth=1,description="Pull Select. Logic HIGH selects pull-up, logic LOW select pull-down" */
        volatile uint32_t PS: 1;  /* Pull Select. Logic HIGH selects pull-up, logic LOW select pu */
        /* @SVD:field=IE,bitOffset=6,bitWidth=1,description="Input Enable. Logic HIGH enables the input buffer" */
        volatile uint32_t IE: 1;  /* Input Enable. Logic HIGH enables the input buffer */
        /* @SVD:field=IS,bitOffset=7,bitWidth=1,description="Input Select. Logic LOW selects CMOS input, logic HIGH selects Schmitt input" */
        volatile uint32_t IS: 1;  /* Input Select. Logic LOW selects CMOS input, logic HIGH selec */
        /* @SVD:field=SR,bitOffset=8,bitWidth=1,description="Slew Rate. Logic HIGH selects slow slew rate, logic LOW selects fast slew rate" */
        volatile uint32_t SR: 1;  /* Slew Rate. Logic HIGH selects slow slew rate, logic LOW sele */
        /* @SVD:field=DS0,bitOffset=9,bitWidth=1,description="Drive Select 0. Used to select output drive strength" */
        volatile uint32_t DS0: 1;  /* Drive Select 0. Used to select output drive strength */
        /* @SVD:field=DS1,bitOffset=10,bitWidth=1,description="Drive Select 1. Used to select output drive strength" */
        volatile uint32_t DS1: 1;  /* Drive Select 1. Used to select output drive strength */
        /* @SVD:field=POE,bitOffset=11,bitWidth=1,description="Reserved. Always set to logic LOW" */
        volatile uint32_t POE: 1;  /* Reserved. Always set to logic LOW */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=20,description="" */
        volatile uint32_t RSVD: 20;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_PINMUX_PAD_PB03_Type;

/* LPSYS_PINMUX 寄存器结构体 */
typedef struct
{
    LPSYS_PINMUX_PAD_PB00_Type PAD_PB00;  /* 0x0000 - PAD_PB00 */
    LPSYS_PINMUX_PAD_PB01_Type PAD_PB01;  /* 0x0004 - PAD_PB01 */
    LPSYS_PINMUX_PAD_PB02_Type PAD_PB02;  /* 0x0008 - PAD_PB02 */
    LPSYS_PINMUX_PAD_PB03_Type PAD_PB03;  /* 0x000C - PAD_PB03 */
} LPSYS_PINMUX_TypeDef;

/* ============================================================================ */
/* USART4 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=USART4,base=0x40005000,description="",groupName=USART */

#define USART4_BASE                                        (0x40005000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control Register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UE,bitOffset=0,bitWidth=1,description="USART enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t UE: 1;  /* USART enable               0: disabled               1: enab */
        /* @SVD:field=RSVD9,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RE,bitOffset=2,bitWidth=1,description="Receiver enable\n              0: receiver is disabled\n              1: receiver is enabled" */
        volatile uint32_t RE: 1;  /* Receiver enable               0: receiver is disabled        */
        /* @SVD:field=TE,bitOffset=3,bitWidth=1,description="Transmitter enable\n              0: transmitter is disabled\n              1: transmitter is enabled" */
        volatile uint32_t TE: 1;  /* Transmitter enable               0: transmitter is disabled  */
        /* @SVD:field=IDLEIE,bitOffset=4,bitWidth=1,description="Idle line interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever IDLE=1 in the ISR  register" */
        volatile uint32_t IDLEIE: 1;  /* Idle line interrupt enable               0: interrupt disabl */
        /* @SVD:field=RXNEIE,bitOffset=5,bitWidth=1,description="Rx not empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever RXNE=1 in the ISR register" */
        volatile uint32_t RXNEIE: 1;  /* Rx not empty interrupt enable               0: interrupt dis */
        /* @SVD:field=TCIE,bitOffset=6,bitWidth=1,description="Transfer compelete interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever TC=1 in the ISR register" */
        volatile uint32_t TCIE: 1;  /* Transfer compelete interrupt enable               0: interru */
        /* @SVD:field=TXEIE,bitOffset=7,bitWidth=1,description="Tx empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenver TXE=1 in the ISR register" */
        volatile uint32_t TXEIE: 1;  /* Tx empty interrupt enable               0: interrupt disable */
        /* @SVD:field=PEIE,bitOffset=8,bitWidth=1,description="Parity error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever PE=1 in the ISR register" */
        volatile uint32_t PEIE: 1;  /* Parity error interrupt enable               0: interrupt dis */
        /* @SVD:field=PS,bitOffset=9,bitWidth=1,description="Parity select\n              0: even parity\n              1: odd parity" */
        volatile uint32_t PS: 1;  /* Parity select               0: even parity               1:  */
        /* @SVD:field=PCE,bitOffset=10,bitWidth=1,description="Parity check enable. If enabled, parity bit is inserted at the MSB position \n              0: parity check disabled\n              1: parity check enabled" */
        volatile uint32_t PCE: 1;  /* Parity check enable. If enabled, parity bit is inserted at t */
        /* @SVD:field=RSVD8,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=OVER8,bitOffset=14,bitWidth=1,description="Oversampling mode\n              0: Oversampling by 16\n              1: Oversampling by 8" */
        volatile uint32_t OVER8: 1;  /* Oversampling mode               0: Oversampling by 16        */
        /* @SVD:field=RSVD5,bitOffset=15,bitWidth=5,description="" */
        volatile uint32_t RSVD5: 5;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=5,description="" */
        volatile uint32_t RSVD4: 5;
        /* @SVD:field=RSVD3,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=M,bitOffset=27,bitWidth=2,description="Mode bit indicates the length of the packet, including data bits and parity. Stop bits not included.\n              0: 6 bits (e.g. 6 data bits + no parity bit)\n              1: 7 bits (e.g. 6 data bits + 1 parity bit)\n              2: 8 bits (e.g. 7 data bits + 1 parity bit, or 6 data bits + 2 parity bits)\n              3: 9 bits (e.g. 8 data bits + 1 parity bit, or 7 data bits + 2 parity bits)" */
        volatile uint32_t M: 2;  /* Mode bit indicates the length of the packet, including data  */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Control Register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD19,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD19: 4;
        /* @SVD:field=RSVD18,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD18: 1;
        /* @SVD:field=RSVD17,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD17: 1;
        /* @SVD:field=RSVD16,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=RSVD15,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=STOP,bitOffset=12,bitWidth=2,description="Stop bits\n              0/1: 1 stop bit\n              2/3: 2 stop bits" */
        volatile uint32_t STOP: 2;  /* Stop bits               0/1: 1 stop bit               2/3: 2 */
        /* @SVD:field=RSVD10,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=21,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_CR2_Type;

/* @SVD:register=CR3,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Control Register 3" */
/* CR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EIE,bitOffset=0,bitWidth=1,description="Error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever FE=1 or ORE=1 or NF=1 in the ISR register" */
        volatile uint32_t EIE: 1;  /* Error interrupt enable               0: interrupt disabled   */
        /* @SVD:field=RSVD15,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=DMAR,bitOffset=6,bitWidth=1,description="Receiver DMA enable\n              0: DMA mode disabled for reception\n              1: DMA mode enabled for reception" */
        volatile uint32_t DMAR: 1;  /* Receiver DMA enable               0: DMA mode disabled for r */
        /* @SVD:field=DMAT,bitOffset=7,bitWidth=1,description="Transmitter DMA enable\n              0: DMA mode disabled for transmission\n              1: DMA mode enabled for transmission" */
        volatile uint32_t DMAT: 1;  /* Transmitter DMA enable               0: DMA mode disabled fo */
        /* @SVD:field=RTSE,bitOffset=8,bitWidth=1,description="RTS enable\n              0: RTS hardware flow control disabled\n              1: RTS hardware flow control enabled, RTS output is asserted low when new data can be received" */
        volatile uint32_t RTSE: 1;  /* RTS enable               0: RTS hardware flow control disabl */
        /* @SVD:field=CTSE,bitOffset=9,bitWidth=1,description="CTS enable\n              0: CTS hardware flow control disabled\n              1: CTS hardware flow control enabled, data is transmitted only when CTS input is asserted low" */
        volatile uint32_t CTSE: 1;  /* CTS enable               0: CTS hardware flow control disabl */
        /* @SVD:field=CTSIE,bitOffset=10,bitWidth=1,description="CTS interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever CTSIF=1 in the ISR register" */
        volatile uint32_t CTSIE: 1;  /* CTS interrupt enable               0: interrupt disabled     */
        /* @SVD:field=ONEBIT,bitOffset=11,bitWidth=1,description="One bit sampling mode\n              0: 3-bit sampling mode, the sampling value is determined by the voted result out of 3 bits\n              1: 1-bit sampling mode" */
        volatile uint32_t ONEBIT: 1;  /* One bit sampling mode               0: 3-bit sampling mode,  */
        /* @SVD:field=OVRDIS,bitOffset=12,bitWidth=1,description="Overrun disable\n              0: overrun error flag (ORE) will be set if new data received but previous data not read. New data will not overwrite the content in RDR register.\n              1: overrun disabled. If new data is received before previous data is read, the new data will overwrite the content in RDR register and ORE flag remains unset." */
        volatile uint32_t OVRDIS: 1;  /* Overrun disable               0: overrun error flag (ORE) wi */
        /* @SVD:field=RSVD10,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=17,bitWidth=3,description="" */
        volatile uint32_t RSVD6: 3;
        /* @SVD:field=RSVD5,bitOffset=20,bitWidth=2,description="" */
        volatile uint32_t RSVD5: 2;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=7,description="" */
        volatile uint32_t RSVD: 7;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_CR3_Type;

/* @SVD:register=BRR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Baud Rate Register" */
/* BRR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FRAC,bitOffset=0,bitWidth=4,description="Fractional part of baud rate prescaler" */
        volatile uint32_t FRAC: 4;  /* Fractional part of baud rate prescaler */
        /* @SVD:field=INT,bitOffset=4,bitWidth=12,description="Integer part of baud rate prescaler\n              If OVER8 = 0, Baud Rate = 48000000 / (INT + FRAC/16) / 16\n              If OVER8 = 1, Baud Rate = 48000000 / (INT + FRAC/16) / 8\n              For example:\n              OVER=0, INT=3, FRAC=0, Baud Rate = 48000000/(3+0)/16 = 1Mbps\n              OVER=0, INT=3, FRAC=4, Baud Rate = 48000000/(3+4/16)/16 = 923077 = 921600 + 1.6‰\n              OVER=1, INT=52, FRAC=1, Baud Rate = 48000000/(52+1/16)/8 = 115246 = 115200 + 0.4‰" */
        volatile uint32_t INT: 12;  /* Integer part of baud rate prescaler               If OVER8 = */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_BRR_Type;

/* @SVD:register=RQR,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="Request Register" */
/* RQR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RXFRQ,bitOffset=3,bitWidth=1,description="Rx data flush request. Write 1 to clear the RXNE flag and discard the current data in RDR" */
        volatile uint32_t RXFRQ: 1;  /* Rx data flush request. Write 1 to clear the RXNE flag and di */
        /* @SVD:field=TXFRQ,bitOffset=4,bitWidth=1,description="Tx data flush requestReserved-Do not modify" */
        volatile uint32_t TXFRQ: 1;  /* Tx data flush requestReserved-Do not modify */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_RQR_Type;

/* @SVD:register=ISR,offset=0x001C,size=32,access=read-write,reset=0x020000C0,description="Interrupt and Status Register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PE,bitOffset=0,bitWidth=1,description="Parity error. This bit is set when a parity error is detected in the received packet.\n              0: no parity error\n              1: parity error detected" */
        volatile uint32_t PE: 1;  /* Parity error. This bit is set when a parity error is detecte */
        /* @SVD:field=FE,bitOffset=1,bitWidth=1,description="Framing error. This bit is set by hardware when stop bit is not correctly received\n              0: no framing error is detected\n              1: framing error is detected" */
        volatile uint32_t FE: 1;  /* Framing error. This bit is set by hardware when stop bit is  */
        /* @SVD:field=NF,bitOffset=2,bitWidth=1,description="Noise flag. Noise means the samping values in the 3-bit sampling mode are not the same.\n              0: no noise is detected\n              1: noise is detected" */
        volatile uint32_t NF: 1;  /* Noise flag. Noise means the samping values in the 3-bit samp */
        /* @SVD:field=ORE,bitOffset=3,bitWidth=1,description="Overrun error. When new data is received but Rx buffer is not empty (i.e. previous data is not read yet), ORE is asserted and current RDR content is not lost. This feature can be disabled by set CR3_OVRDIS to 1.\n              0: no overrun error\n              1: overrun error is detected" */
        volatile uint32_t ORE: 1;  /* Overrun error. When new data is received but Rx buffer is no */
        /* @SVD:field=IDLE,bitOffset=4,bitWidth=1,description="Idle line detected\n              0: no idle line is detected\n              1: idle line is detected" */
        volatile uint32_t IDLE: 1;  /* Idle line detected               0: no idle line is detected */
        /* @SVD:field=RXNE,bitOffset=5,bitWidth=1,description="Rx data not empty. This bit is set by hardware when the received data is transferred into RDR register.\n              0: data is not received\n              1: data is ready in RDR to be read" */
        volatile uint32_t RXNE: 1;  /* Rx data not empty. This bit is set by hardware when the rece */
        /* @SVD:field=TC,bitOffset=6,bitWidth=1,description="transmission complete. This bit is set by hardware if the transmission is complete\n              0: transmission is not complete\n              1: transmission is complete" */
        volatile uint32_t TC: 1;  /* transmission complete. This bit is set by hardware if the tr */
        /* @SVD:field=TXE,bitOffset=7,bitWidth=1,description="Tx data empty\n              0: data is ready in TDR\n              1: data is already transferred to shift register, i.e. transmission is in progress or complete" */
        volatile uint32_t TXE: 1;  /* Tx data empty               0: data is ready in TDR          */
        /* @SVD:field=RSVD16,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=CTSIF,bitOffset=9,bitWidth=1,description="CTS interrupt flag. This bit is set by hardware whenever CTS input toggles.\n              0: no change on the CTS line\n              1: there is a change on the CTS line" */
        volatile uint32_t CTSIF: 1;  /* CTS interrupt flag. This bit is set by hardware whenever CTS */
        /* @SVD:field=CTS,bitOffset=10,bitWidth=1,description="CTS input. Read this bit to get the raw status of the CTS line." */
        volatile uint32_t CTS: 1;  /* CTS input. Read this bit to get the raw status of the CTS li */
        /* @SVD:field=RSVD15,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=RSVD10,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_ISR_Type;

/* @SVD:register=ICR,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Interrupt flag Clear Register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PECF,bitOffset=0,bitWidth=1,description="Parity error clear flag. Wriring 1 to this bit clears the PE flag in the ISR register." */
        volatile uint32_t PECF: 1;  /* Parity error clear flag. Wriring 1 to this bit clears the PE */
        /* @SVD:field=FECF,bitOffset=1,bitWidth=1,description="Framing error clear flag. Writing 1 to this bit clears the FE flag in the ISR register." */
        volatile uint32_t FECF: 1;  /* Framing error clear flag. Writing 1 to this bit clears the F */
        /* @SVD:field=NCF,bitOffset=2,bitWidth=1,description="Noise detected clear flag. Writing 1 to this bit clears the NF flag in the ISR register." */
        volatile uint32_t NCF: 1;  /* Noise detected clear flag. Writing 1 to this bit clears the  */
        /* @SVD:field=ORECF,bitOffset=3,bitWidth=1,description="Overrun error clear flag. Writing 1 to this bit clears the ORE flag in the ISR register." */
        volatile uint32_t ORECF: 1;  /* Overrun error clear flag. Writing 1 to this bit clears the O */
        /* @SVD:field=IDLECF,bitOffset=4,bitWidth=1,description="Idle line detected clear flag. Writing 1 to this bit clears the IDLECF flag in the ISR register." */
        volatile uint32_t IDLECF: 1;  /* Idle line detected clear flag. Writing 1 to this bit clears  */
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=TCCF,bitOffset=6,bitWidth=1,description="Transmission complete clear flag. Writing 1 to this bit clears the TC flag in the ISR register." */
        volatile uint32_t TCCF: 1;  /* Transmission complete clear flag. Writing 1 to this bit clea */
        /* @SVD:field=RSVD10,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=CTSCF,bitOffset=9,bitWidth=1,description="CTS clear flag. Writing 1 to this bit clears the CTSIF flag in the ISR register." */
        volatile uint32_t CTSCF: 1;  /* CTS clear flag. Writing 1 to this bit clears the CTSIF flag  */
        /* @SVD:field=RSVD8,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=13,bitWidth=4,description="" */
        volatile uint32_t RSVD5: 4;
        /* @SVD:field=RSVD4,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=18,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_ICR_Type;

/* @SVD:register=RDR,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Receive Data Register" */
/* RDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RDR,bitOffset=0,bitWidth=9,description="Received data" */
        volatile uint32_t RDR: 9;  /* Received data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_RDR_Type;

/* @SVD:register=TDR,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Transmit Data Register" */
/* TDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TDR,bitOffset=0,bitWidth=9,description="Transmit data" */
        volatile uint32_t TDR: 9;  /* Transmit data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_TDR_Type;

/* @SVD:register=MISCR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Miscellaneous Register" */
/* MISCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SMPLINI,bitOffset=0,bitWidth=4,description="initial sample count, count down from this value to zero to reach the middle of the start bit in RxReserved-Do not modify" */
        volatile uint32_t SMPLINI: 4;  /* initial sample count, count down from this value to zero to  */
        /* @SVD:field=RTSBIT,bitOffset=4,bitWidth=4,description="assert RTS ahead of the frame completion (in number of bits)Reserved-Do not modify" */
        volatile uint32_t RTSBIT: 4;  /* assert RTS ahead of the frame completion (in number of bits) */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
        /* @SVD:field=AUTOCAL,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t AUTOCAL: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_MISCR_Type;

/* @SVD:register=DRDR,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DRDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_DRDR_Type;

/* @SVD:register=DTDR,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DTDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_DTDR_Type;

/* @SVD:register=EXR,offset=0x0038,size=32,access=read-write,reset=0x00000001,description="Mutual Exclusive Register" */
/* EXR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BUSY,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t BUSY: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=ID,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t ID: 1;
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART4_EXR_Type;

/* USART4 寄存器结构体 */
typedef struct
{
    USART4_CR1_Type CR1;  /* 0x0000 - Control Register 1 */
    USART4_CR2_Type CR2;  /* 0x0004 - Control Register 2 */
    USART4_CR3_Type CR3;  /* 0x0008 - Control Register 3 */
    USART4_BRR_Type BRR;  /* 0x000C - Baud Rate Register */
    /* @SVD:register=RSVD1,offset=0x0010,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0010 - RSVD1 */
    volatile uint32_t RESERVED5[1];  /* 0x0014 - 0x0017 */
    USART4_RQR_Type RQR;  /* 0x0018 - Request Register */
    USART4_ISR_Type ISR;  /* 0x001C - Interrupt and Status Register */
    USART4_ICR_Type ICR;  /* 0x0020 - Interrupt flag Clear Register */
    USART4_RDR_Type RDR;  /* 0x0024 - Receive Data Register */
    USART4_TDR_Type TDR;  /* 0x0028 - Transmit Data Register */
    USART4_MISCR_Type MISCR;  /* 0x002C - Miscellaneous Register */
    USART4_DRDR_Type DRDR;  /* 0x0030 - Debug Receive Data Register */
    USART4_DTDR_Type DTDR;  /* 0x0034 - Debug Receive Data Register */
    USART4_EXR_Type EXR;  /* 0x0038 - Mutual Exclusive Register */
} USART4_TypeDef;

/* ============================================================================ */
/* USART5 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=USART5,base=0x40006000,description="",groupName=USART */

#define USART5_BASE                                        (0x40006000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="Control Register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UE,bitOffset=0,bitWidth=1,description="USART enable\n              0: disabled\n              1: enabled" */
        volatile uint32_t UE: 1;  /* USART enable               0: disabled               1: enab */
        /* @SVD:field=RSVD9,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RE,bitOffset=2,bitWidth=1,description="Receiver enable\n              0: receiver is disabled\n              1: receiver is enabled" */
        volatile uint32_t RE: 1;  /* Receiver enable               0: receiver is disabled        */
        /* @SVD:field=TE,bitOffset=3,bitWidth=1,description="Transmitter enable\n              0: transmitter is disabled\n              1: transmitter is enabled" */
        volatile uint32_t TE: 1;  /* Transmitter enable               0: transmitter is disabled  */
        /* @SVD:field=IDLEIE,bitOffset=4,bitWidth=1,description="Idle line interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever IDLE=1 in the ISR  register" */
        volatile uint32_t IDLEIE: 1;  /* Idle line interrupt enable               0: interrupt disabl */
        /* @SVD:field=RXNEIE,bitOffset=5,bitWidth=1,description="Rx not empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever RXNE=1 in the ISR register" */
        volatile uint32_t RXNEIE: 1;  /* Rx not empty interrupt enable               0: interrupt dis */
        /* @SVD:field=TCIE,bitOffset=6,bitWidth=1,description="Transfer compelete interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever TC=1 in the ISR register" */
        volatile uint32_t TCIE: 1;  /* Transfer compelete interrupt enable               0: interru */
        /* @SVD:field=TXEIE,bitOffset=7,bitWidth=1,description="Tx empty interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenver TXE=1 in the ISR register" */
        volatile uint32_t TXEIE: 1;  /* Tx empty interrupt enable               0: interrupt disable */
        /* @SVD:field=PEIE,bitOffset=8,bitWidth=1,description="Parity error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever PE=1 in the ISR register" */
        volatile uint32_t PEIE: 1;  /* Parity error interrupt enable               0: interrupt dis */
        /* @SVD:field=PS,bitOffset=9,bitWidth=1,description="Parity select\n              0: even parity\n              1: odd parity" */
        volatile uint32_t PS: 1;  /* Parity select               0: even parity               1:  */
        /* @SVD:field=PCE,bitOffset=10,bitWidth=1,description="Parity check enable. If enabled, parity bit is inserted at the MSB position \n              0: parity check disabled\n              1: parity check enabled" */
        volatile uint32_t PCE: 1;  /* Parity check enable. If enabled, parity bit is inserted at t */
        /* @SVD:field=RSVD8,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=OVER8,bitOffset=14,bitWidth=1,description="Oversampling mode\n              0: Oversampling by 16\n              1: Oversampling by 8" */
        volatile uint32_t OVER8: 1;  /* Oversampling mode               0: Oversampling by 16        */
        /* @SVD:field=RSVD5,bitOffset=15,bitWidth=5,description="" */
        volatile uint32_t RSVD5: 5;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=5,description="" */
        volatile uint32_t RSVD4: 5;
        /* @SVD:field=RSVD3,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=26,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=M,bitOffset=27,bitWidth=2,description="Mode bit indicates the length of the packet, including data bits and parity. Stop bits not included.\n              0: 6 bits (e.g. 6 data bits + no parity bit)\n              1: 7 bits (e.g. 6 data bits + 1 parity bit)\n              2: 8 bits (e.g. 7 data bits + 1 parity bit, or 6 data bits + 2 parity bits)\n              3: 9 bits (e.g. 8 data bits + 1 parity bit, or 7 data bits + 2 parity bits)" */
        volatile uint32_t M: 2;  /* Mode bit indicates the length of the packet, including data  */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="Control Register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD19,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD19: 4;
        /* @SVD:field=RSVD18,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD18: 1;
        /* @SVD:field=RSVD17,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD17: 1;
        /* @SVD:field=RSVD16,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=RSVD15,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=9,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=STOP,bitOffset=12,bitWidth=2,description="Stop bits\n              0/1: 1 stop bit\n              2/3: 2 stop bits" */
        volatile uint32_t STOP: 2;  /* Stop bits               0/1: 1 stop bit               2/3: 2 */
        /* @SVD:field=RSVD10,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=21,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_CR2_Type;

/* @SVD:register=CR3,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="Control Register 3" */
/* CR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=EIE,bitOffset=0,bitWidth=1,description="Error interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever FE=1 or ORE=1 or NF=1 in the ISR register" */
        volatile uint32_t EIE: 1;  /* Error interrupt enable               0: interrupt disabled   */
        /* @SVD:field=RSVD15,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=DMAR,bitOffset=6,bitWidth=1,description="Receiver DMA enable\n              0: DMA mode disabled for reception\n              1: DMA mode enabled for reception" */
        volatile uint32_t DMAR: 1;  /* Receiver DMA enable               0: DMA mode disabled for r */
        /* @SVD:field=DMAT,bitOffset=7,bitWidth=1,description="Transmitter DMA enable\n              0: DMA mode disabled for transmission\n              1: DMA mode enabled for transmission" */
        volatile uint32_t DMAT: 1;  /* Transmitter DMA enable               0: DMA mode disabled fo */
        /* @SVD:field=RTSE,bitOffset=8,bitWidth=1,description="RTS enable\n              0: RTS hardware flow control disabled\n              1: RTS hardware flow control enabled, RTS output is asserted low when new data can be received" */
        volatile uint32_t RTSE: 1;  /* RTS enable               0: RTS hardware flow control disabl */
        /* @SVD:field=CTSE,bitOffset=9,bitWidth=1,description="CTS enable\n              0: CTS hardware flow control disabled\n              1: CTS hardware flow control enabled, data is transmitted only when CTS input is asserted low" */
        volatile uint32_t CTSE: 1;  /* CTS enable               0: CTS hardware flow control disabl */
        /* @SVD:field=CTSIE,bitOffset=10,bitWidth=1,description="CTS interrupt enable\n              0: interrupt disabled\n              1: interrupt is generated whenever CTSIF=1 in the ISR register" */
        volatile uint32_t CTSIE: 1;  /* CTS interrupt enable               0: interrupt disabled     */
        /* @SVD:field=ONEBIT,bitOffset=11,bitWidth=1,description="One bit sampling mode\n              0: 3-bit sampling mode, the sampling value is determined by the voted result out of 3 bits\n              1: 1-bit sampling mode" */
        volatile uint32_t ONEBIT: 1;  /* One bit sampling mode               0: 3-bit sampling mode,  */
        /* @SVD:field=OVRDIS,bitOffset=12,bitWidth=1,description="Overrun disable\n              0: overrun error flag (ORE) will be set if new data received but previous data not read. New data will not overwrite the content in RDR register.\n              1: overrun disabled. If new data is received before previous data is read, the new data will overwrite the content in RDR register and ORE flag remains unset." */
        volatile uint32_t OVRDIS: 1;  /* Overrun disable               0: overrun error flag (ORE) wi */
        /* @SVD:field=RSVD10,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=17,bitWidth=3,description="" */
        volatile uint32_t RSVD6: 3;
        /* @SVD:field=RSVD5,bitOffset=20,bitWidth=2,description="" */
        volatile uint32_t RSVD5: 2;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=24,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=7,description="" */
        volatile uint32_t RSVD: 7;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_CR3_Type;

/* @SVD:register=BRR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="Baud Rate Register" */
/* BRR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=FRAC,bitOffset=0,bitWidth=4,description="Fractional part of baud rate prescaler" */
        volatile uint32_t FRAC: 4;  /* Fractional part of baud rate prescaler */
        /* @SVD:field=INT,bitOffset=4,bitWidth=12,description="Integer part of baud rate prescaler\n              If OVER8 = 0, Baud Rate = 48000000 / (INT + FRAC/16) / 16\n              If OVER8 = 1, Baud Rate = 48000000 / (INT + FRAC/16) / 8\n              For example:\n              OVER=0, INT=3, FRAC=0, Baud Rate = 48000000/(3+0)/16 = 1Mbps\n              OVER=0, INT=3, FRAC=4, Baud Rate = 48000000/(3+4/16)/16 = 923077 = 921600 + 1.6‰\n              OVER=1, INT=52, FRAC=1, Baud Rate = 48000000/(52+1/16)/8 = 115246 = 115200 + 0.4‰" */
        volatile uint32_t INT: 12;  /* Integer part of baud rate prescaler               If OVER8 = */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_BRR_Type;

/* @SVD:register=RQR,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="Request Register" */
/* RQR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD4,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=1,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RXFRQ,bitOffset=3,bitWidth=1,description="Rx data flush request. Write 1 to clear the RXNE flag and discard the current data in RDR" */
        volatile uint32_t RXFRQ: 1;  /* Rx data flush request. Write 1 to clear the RXNE flag and di */
        /* @SVD:field=TXFRQ,bitOffset=4,bitWidth=1,description="Tx data flush requestReserved-Do not modify" */
        volatile uint32_t TXFRQ: 1;  /* Tx data flush requestReserved-Do not modify */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_RQR_Type;

/* @SVD:register=ISR,offset=0x001C,size=32,access=read-write,reset=0x020000C0,description="Interrupt and Status Register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PE,bitOffset=0,bitWidth=1,description="Parity error. This bit is set when a parity error is detected in the received packet.\n              0: no parity error\n              1: parity error detected" */
        volatile uint32_t PE: 1;  /* Parity error. This bit is set when a parity error is detecte */
        /* @SVD:field=FE,bitOffset=1,bitWidth=1,description="Framing error. This bit is set by hardware when stop bit is not correctly received\n              0: no framing error is detected\n              1: framing error is detected" */
        volatile uint32_t FE: 1;  /* Framing error. This bit is set by hardware when stop bit is  */
        /* @SVD:field=NF,bitOffset=2,bitWidth=1,description="Noise flag. Noise means the samping values in the 3-bit sampling mode are not the same.\n              0: no noise is detected\n              1: noise is detected" */
        volatile uint32_t NF: 1;  /* Noise flag. Noise means the samping values in the 3-bit samp */
        /* @SVD:field=ORE,bitOffset=3,bitWidth=1,description="Overrun error. When new data is received but Rx buffer is not empty (i.e. previous data is not read yet), ORE is asserted and current RDR content is not lost. This feature can be disabled by set CR3_OVRDIS to 1.\n              0: no overrun error\n              1: overrun error is detected" */
        volatile uint32_t ORE: 1;  /* Overrun error. When new data is received but Rx buffer is no */
        /* @SVD:field=IDLE,bitOffset=4,bitWidth=1,description="Idle line detected\n              0: no idle line is detected\n              1: idle line is detected" */
        volatile uint32_t IDLE: 1;  /* Idle line detected               0: no idle line is detected */
        /* @SVD:field=RXNE,bitOffset=5,bitWidth=1,description="Rx data not empty. This bit is set by hardware when the received data is transferred into RDR register.\n              0: data is not received\n              1: data is ready in RDR to be read" */
        volatile uint32_t RXNE: 1;  /* Rx data not empty. This bit is set by hardware when the rece */
        /* @SVD:field=TC,bitOffset=6,bitWidth=1,description="transmission complete. This bit is set by hardware if the transmission is complete\n              0: transmission is not complete\n              1: transmission is complete" */
        volatile uint32_t TC: 1;  /* transmission complete. This bit is set by hardware if the tr */
        /* @SVD:field=TXE,bitOffset=7,bitWidth=1,description="Tx data empty\n              0: data is ready in TDR\n              1: data is already transferred to shift register, i.e. transmission is in progress or complete" */
        volatile uint32_t TXE: 1;  /* Tx data empty               0: data is ready in TDR          */
        /* @SVD:field=RSVD16,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD16: 1;
        /* @SVD:field=CTSIF,bitOffset=9,bitWidth=1,description="CTS interrupt flag. This bit is set by hardware whenever CTS input toggles.\n              0: no change on the CTS line\n              1: there is a change on the CTS line" */
        volatile uint32_t CTSIF: 1;  /* CTS interrupt flag. This bit is set by hardware whenever CTS */
        /* @SVD:field=CTS,bitOffset=10,bitWidth=1,description="CTS input. Read this bit to get the raw status of the CTS line." */
        volatile uint32_t CTS: 1;  /* CTS input. Read this bit to get the raw status of the CTS li */
        /* @SVD:field=RSVD15,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD15: 1;
        /* @SVD:field=RSVD14,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD14: 1;
        /* @SVD:field=RSVD13,bitOffset=13,bitWidth=1,description="" */
        volatile uint32_t RSVD13: 1;
        /* @SVD:field=RSVD12,bitOffset=14,bitWidth=1,description="" */
        volatile uint32_t RSVD12: 1;
        /* @SVD:field=RSVD11,bitOffset=15,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=RSVD10,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=RSVD8,bitOffset=18,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=21,bitWidth=1,description="" */
        volatile uint32_t RSVD5: 1;
        /* @SVD:field=RSVD4,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=23,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=25,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_ISR_Type;

/* @SVD:register=ICR,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="Interrupt flag Clear Register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PECF,bitOffset=0,bitWidth=1,description="Parity error clear flag. Wriring 1 to this bit clears the PE flag in the ISR register." */
        volatile uint32_t PECF: 1;  /* Parity error clear flag. Wriring 1 to this bit clears the PE */
        /* @SVD:field=FECF,bitOffset=1,bitWidth=1,description="Framing error clear flag. Writing 1 to this bit clears the FE flag in the ISR register." */
        volatile uint32_t FECF: 1;  /* Framing error clear flag. Writing 1 to this bit clears the F */
        /* @SVD:field=NCF,bitOffset=2,bitWidth=1,description="Noise detected clear flag. Writing 1 to this bit clears the NF flag in the ISR register." */
        volatile uint32_t NCF: 1;  /* Noise detected clear flag. Writing 1 to this bit clears the  */
        /* @SVD:field=ORECF,bitOffset=3,bitWidth=1,description="Overrun error clear flag. Writing 1 to this bit clears the ORE flag in the ISR register." */
        volatile uint32_t ORECF: 1;  /* Overrun error clear flag. Writing 1 to this bit clears the O */
        /* @SVD:field=IDLECF,bitOffset=4,bitWidth=1,description="Idle line detected clear flag. Writing 1 to this bit clears the IDLECF flag in the ISR register." */
        volatile uint32_t IDLECF: 1;  /* Idle line detected clear flag. Writing 1 to this bit clears  */
        /* @SVD:field=RSVD11,bitOffset=5,bitWidth=1,description="" */
        volatile uint32_t RSVD11: 1;
        /* @SVD:field=TCCF,bitOffset=6,bitWidth=1,description="Transmission complete clear flag. Writing 1 to this bit clears the TC flag in the ISR register." */
        volatile uint32_t TCCF: 1;  /* Transmission complete clear flag. Writing 1 to this bit clea */
        /* @SVD:field=RSVD10,bitOffset=7,bitWidth=1,description="" */
        volatile uint32_t RSVD10: 1;
        /* @SVD:field=RSVD9,bitOffset=8,bitWidth=1,description="" */
        volatile uint32_t RSVD9: 1;
        /* @SVD:field=CTSCF,bitOffset=9,bitWidth=1,description="CTS clear flag. Writing 1 to this bit clears the CTSIF flag in the ISR register." */
        volatile uint32_t CTSCF: 1;  /* CTS clear flag. Writing 1 to this bit clears the CTSIF flag  */
        /* @SVD:field=RSVD8,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD8: 1;
        /* @SVD:field=RSVD7,bitOffset=11,bitWidth=1,description="" */
        volatile uint32_t RSVD7: 1;
        /* @SVD:field=RSVD6,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD6: 1;
        /* @SVD:field=RSVD5,bitOffset=13,bitWidth=4,description="" */
        volatile uint32_t RSVD5: 4;
        /* @SVD:field=RSVD4,bitOffset=17,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=RSVD3,bitOffset=18,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RSVD2,bitOffset=20,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=11,description="" */
        volatile uint32_t RSVD: 11;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_ICR_Type;

/* @SVD:register=RDR,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Receive Data Register" */
/* RDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RDR,bitOffset=0,bitWidth=9,description="Received data" */
        volatile uint32_t RDR: 9;  /* Received data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_RDR_Type;

/* @SVD:register=TDR,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Transmit Data Register" */
/* TDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TDR,bitOffset=0,bitWidth=9,description="Transmit data" */
        volatile uint32_t TDR: 9;  /* Transmit data */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_TDR_Type;

/* @SVD:register=MISCR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Miscellaneous Register" */
/* MISCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SMPLINI,bitOffset=0,bitWidth=4,description="initial sample count, count down from this value to zero to reach the middle of the start bit in RxReserved-Do not modify" */
        volatile uint32_t SMPLINI: 4;  /* initial sample count, count down from this value to zero to  */
        /* @SVD:field=RTSBIT,bitOffset=4,bitWidth=4,description="assert RTS ahead of the frame completion (in number of bits)Reserved-Do not modify" */
        volatile uint32_t RTSBIT: 4;  /* assert RTS ahead of the frame completion (in number of bits) */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
        /* @SVD:field=AUTOCAL,bitOffset=31,bitWidth=1,description="" */
        volatile uint32_t AUTOCAL: 1;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_MISCR_Type;

/* @SVD:register=DRDR,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DRDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_DRDR_Type;

/* @SVD:register=DTDR,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="Debug Receive Data Register" */
/* DTDR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="" */
        volatile uint32_t DATA: 32;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_DTDR_Type;

/* @SVD:register=EXR,offset=0x0038,size=32,access=read-write,reset=0x00000001,description="Mutual Exclusive Register" */
/* EXR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BUSY,bitOffset=0,bitWidth=1,description="" */
        volatile uint32_t BUSY: 1;
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=ID,bitOffset=4,bitWidth=1,description="" */
        volatile uint32_t ID: 1;
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} USART5_EXR_Type;

/* USART5 寄存器结构体 */
typedef struct
{
    USART5_CR1_Type CR1;  /* 0x0000 - Control Register 1 */
    USART5_CR2_Type CR2;  /* 0x0004 - Control Register 2 */
    USART5_CR3_Type CR3;  /* 0x0008 - Control Register 3 */
    USART5_BRR_Type BRR;  /* 0x000C - Baud Rate Register */
    /* @SVD:register=RSVD1,offset=0x0010,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0010 - RSVD1 */
    volatile uint32_t RESERVED5[1];  /* 0x0014 - 0x0017 */
    USART5_RQR_Type RQR;  /* 0x0018 - Request Register */
    USART5_ISR_Type ISR;  /* 0x001C - Interrupt and Status Register */
    USART5_ICR_Type ICR;  /* 0x0020 - Interrupt flag Clear Register */
    USART5_RDR_Type RDR;  /* 0x0024 - Receive Data Register */
    USART5_TDR_Type TDR;  /* 0x0028 - Transmit Data Register */
    USART5_MISCR_Type MISCR;  /* 0x002C - Miscellaneous Register */
    USART5_DRDR_Type DRDR;  /* 0x0030 - Debug Receive Data Register */
    USART5_DTDR_Type DTDR;  /* 0x0034 - Debug Receive Data Register */
    USART5_EXR_Type EXR;  /* 0x0038 - Mutual Exclusive Register */
} USART5_TypeDef;

/* ============================================================================ */
/* BTIM3 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=BTIM3,base=0x40009000,description="",groupName=BTIM */

#define BTIM3_BASE                                         (0x40009000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="TIM control register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CEN,bitOffset=0,bitWidth=1,description="Counter enable\n              0: Counter disabled\n              1: Counter enabled\n              Note: Gated mode can work only if the CEN bit has been previously set by software. However trigger mode can set the CEN bit automatically by hardware. \n              CEN is cleared automatically in one-pulse mode, when an update event occurs." */
        volatile uint32_t CEN: 1;  /* Counter enable               0: Counter disabled             */
        /* @SVD:field=UDIS,bitOffset=1,bitWidth=1,description="Update disable\n              This bit is set and cleared by software to enable/disable UEV event generation.\n              0: UEV enabled. The Update (UEV) event is generated by one of the following events:\n                 Counter overflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              Buffered registers are then loaded with their preload values.\n              1: UEV disabled. The Update event is not generated, shadow registers keep their value (ARR, PSC). However the counter and the prescaler are reinitialized if the UG bit is set or if a hardware reset is received from the slave mode controller." */
        volatile uint32_t UDIS: 1;  /* Update disable               This bit is set and cleared by  */
        /* @SVD:field=URS,bitOffset=2,bitWidth=1,description="Update request source\n              This bit is set and cleared by software to select the UEV event sources.\n              0: Any of the following events generate an update interrupt or DMA request if enabled. \n              These events can be: \n                 Counter overflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              1: Only counter overflow generates an update interrupt or DMA request if enabled." */
        volatile uint32_t URS: 1;  /* Update request source               This bit is set and clea */
        /* @SVD:field=OPM,bitOffset=3,bitWidth=1,description="One-pulse mode\n              0: Counter is not stopped at update event\n              1: Counter stops counting at the next update event (clearing the bit CEN)" */
        volatile uint32_t OPM: 1;  /* One-pulse mode               0: Counter is not stopped at up */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=ARPE,bitOffset=7,bitWidth=1,description="Auto-reload preload enable\n              0: ARR register is not buffered\n              1: ARR register is buffered" */
        volatile uint32_t ARPE: 1;  /* Auto-reload preload enable               0: ARR register is  */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM3_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="TIM control register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=MMS,bitOffset=4,bitWidth=2,description="Master mode selection\n              These bits allow to select the information to be sent in master mode to slave timers for synchronization (TRGO). The combination is as follows:\n              00: Reset:the UG bit from the EGR register is used as trigger output (TRGO). If the reset is generated by the trigger input (slave mode controller configured in reset mode) then the signal on TRGO is delayed compared to the actual reset.\n              01: Enable :the Counter enable signal, CNT_EN, is used as trigger output (TRGO). It is useful to start several timers at the same time or to control a window in which a slave timer is enabled. The Counter Enable signal is generated by a logic OR between CEN control bit and the trigger input when configured in gated mode. \n              When the Counter Enable signal is controlled by the trigger input, there is a delay on TRGO, except if the master/slave mode is selected (see the MSM bit description in SMCR register).\n              10: Update:The update event is selected as trigger output (TRGO). For instance a master timer can then be used as a prescaler for a slave timer.\n              11: Gating:The delayed gating trigger is selected as trigger output (TRGO)." */
        volatile uint32_t MMS: 2;  /* Master mode selection               These bits allow to sele */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM3_CR2_Type;

/* @SVD:register=SMCR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="TIM slave mode control register" */
/* SMCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD5,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD5: 4;
        /* @SVD:field=TS,bitOffset=4,bitWidth=2,description="Trigger selection\n              This bit-field selects the trigger input to be used to synchronize the counter.\n              00: Internal Trigger 0 (ITR0)\n              01: Internal Trigger 1 (ITR1) \n              10: Internal Trigger 2 (ITR2)\n              11: Internal Trigger 3 (ITR3)" */
        volatile uint32_t TS: 2;  /* Trigger selection               This bit-field selects the t */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=MSM,bitOffset=7,bitWidth=1,description="Master/Slave mode. This bit should be asserted on master timer if synchronization if needed.\n              0: No action\n              1: The effect of an event on the trigger input (TRGI) is delayed to allow a perfect synchronization between the current timer and its slaves (through TRGO). It is useful if we want to synchronize several timers on a single external event." */
        volatile uint32_t MSM: 1;  /* Master/Slave mode. This bit should be asserted on master tim */
        /* @SVD:field=RSVD3,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD3: 8;
        /* @SVD:field=SMS,bitOffset=16,bitWidth=3,description="Slave mode selection\n              When external signals are selected the active edge of the trigger signal (TRGI) is linked to the polarity selected on the external input.\n              000: Slave mode disabled.\n              001: Reset Mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter and generates an update of the registers.\n              010: Trigger Mode - The counter starts at a rising edge of the trigger TRGI (but it is not reset). Only the start of the counter is controlled.\n              011: Combined reset + trigger mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter, generates an update of the registers and starts the counter.\n              100: External Clock Mode - Rising edges of the selected trigger (TRGI) clock the counter." */
        volatile uint32_t SMS: 3;  /* Slave mode selection               When external signals are */
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=GTS,bitOffset=20,bitWidth=2,description="Gating trigger selection in gated mode\n              This bit-field selects the trigger input to be used to enable the counter gating.\n              00: Internal Trigger 0 (ITR0)\n              01: Internal Trigger 1 (ITR1) \n              10: Internal Trigger 2 (ITR2)\n              11: Internal Trigger 3 (ITR3)" */
        volatile uint32_t GTS: 2;  /* Gating trigger selection in gated mode               This bi */
        /* @SVD:field=GTP,bitOffset=22,bitWidth=1,description="Gating trigger polarity invert\n              0: active at high level\n              1: active at low level" */
        volatile uint32_t GTP: 1;  /* Gating trigger polarity invert               0: active at hi */
        /* @SVD:field=GM,bitOffset=23,bitWidth=1,description="Gated Mode. The counter clock is enabled when the selected trigger input (TRGI) is active (according to gating trigger polarity). The counter stops (but is not reset) as soon as the trigger becomes inactive. Both start and stop of the counter are controlled. Gated mode and slave mode can be enabled simutanuously with different trigger selection." */
        volatile uint32_t GM: 1;  /* Gated Mode. The counter clock is enabled when the selected t */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM3_SMCR_Type;

/* @SVD:register=DIER,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="TIM DMA/Interrupt enable register" */
/* DIER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIE,bitOffset=0,bitWidth=1,description="Update interrupt enable\n              0: Update interrupt disabled.\n              1: Update interrupt enabled" */
        volatile uint32_t UIE: 1;  /* Update interrupt enable               0: Update interrupt di */
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=UDE,bitOffset=8,bitWidth=1,description="Update DMA request enable\n              0: Update DMA request disabled.\n              1: Update DMA request enabled" */
        volatile uint32_t UDE: 1;  /* Update DMA request enable               0: Update DMA reques */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM3_DIER_Type;

/* @SVD:register=SR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="TIM status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIF,bitOffset=0,bitWidth=1,description="Update interrupt flag\n              This bit is set by hardware on an update event. It is cleared by software.\n              0: No update occurred\n              1: Update interrupt pending. This bit is set by hardware when the registers are updated:\n              At overflow and if UDIS=0 in the CR1 register.\n              When CNT is reinitialized by software using the UG bit in EGR register, if URS=0 and UDIS=0 in the CR1 register.\n              When CNT is reinitialized by a trigger event (refer to the synchro control register description), if URS=0 and UDIS=0 in the CR1 register." */
        volatile uint32_t UIF: 1;  /* Update interrupt flag               This bit is set by hardw */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM3_SR_Type;

/* @SVD:register=EGR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Event generation register" */
/* EGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UG,bitOffset=0,bitWidth=1,description="Update generation\n              This bit can be set by software, it is automatically cleared by hardware.\n              0: No action\n              1: Re-initialize the counter and generates an update of the registers. Note that the prescaler counter is cleared too (anyway the prescaler ratio is not affected). The counter is cleared if the center-aligned mode is selected or if DIR=0 (upcounting), else it takes the auto-reload value (ARR) if DIR=1 (downcounting)." */
        volatile uint32_t UG: 1;  /* Update generation               This bit can be set by softw */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM3_EGR_Type;

/* @SVD:register=CNT,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Counter" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=32,description="counter value" */
        volatile uint32_t CNT: 32;  /* counter value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM3_CNT_Type;

/* @SVD:register=PSC,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Prescaler" */
/* PSC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PSC,bitOffset=0,bitWidth=16,description="Prescaler value\n              The counter clock frequency is equal to fCLK / (PSC[15:0] + 1).\n              PSC contains the value to be loaded in the active prescaler register at each update event (including when the counter is cleared through UG bit of EGR register or through trigger controller when configured in \"reset mode\")." */
        volatile uint32_t PSC: 16;  /* Prescaler value               The counter clock frequency is */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM3_PSC_Type;

/* @SVD:register=ARR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Auto-reload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=32,description="Auto-reload value\n              ARR is the value to be loaded in the actual auto-reload register. The counter is blocked while the auto-reload value is null." */
        volatile uint32_t ARR: 32;  /* Auto-reload value               ARR is the value to be loade */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM3_ARR_Type;

/* BTIM3 寄存器结构体 */
typedef struct
{
    BTIM3_CR1_Type CR1;  /* 0x0000 - TIM control register 1 */
    BTIM3_CR2_Type CR2;  /* 0x0004 - TIM control register 2 */
    BTIM3_SMCR_Type SMCR;  /* 0x0008 - TIM slave mode control register */
    BTIM3_DIER_Type DIER;  /* 0x000C - TIM DMA/Interrupt enable register */
    BTIM3_SR_Type SR;  /* 0x0010 - TIM status register */
    BTIM3_EGR_Type EGR;  /* 0x0014 - Event generation register */
    /* @SVD:register=RSVD1,offset=0x0018,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0018 - RSVD1 */
    volatile uint32_t RESERVED7[2];  /* 0x001C - 0x0023 */
    BTIM3_CNT_Type CNT;  /* 0x0024 - Counter */
    BTIM3_PSC_Type PSC;  /* 0x0028 - Prescaler */
    BTIM3_ARR_Type ARR;  /* 0x002C - Auto-reload register */
} BTIM3_TypeDef;

/* ============================================================================ */
/* BTIM4 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=BTIM4,base=0x4000A000,description="",groupName=BTIM */

#define BTIM4_BASE                                         (0x4000A000UL)

/* @SVD:register=CR1,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="TIM control register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CEN,bitOffset=0,bitWidth=1,description="Counter enable\n              0: Counter disabled\n              1: Counter enabled\n              Note: Gated mode can work only if the CEN bit has been previously set by software. However trigger mode can set the CEN bit automatically by hardware. \n              CEN is cleared automatically in one-pulse mode, when an update event occurs." */
        volatile uint32_t CEN: 1;  /* Counter enable               0: Counter disabled             */
        /* @SVD:field=UDIS,bitOffset=1,bitWidth=1,description="Update disable\n              This bit is set and cleared by software to enable/disable UEV event generation.\n              0: UEV enabled. The Update (UEV) event is generated by one of the following events:\n                 Counter overflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              Buffered registers are then loaded with their preload values.\n              1: UEV disabled. The Update event is not generated, shadow registers keep their value (ARR, PSC). However the counter and the prescaler are reinitialized if the UG bit is set or if a hardware reset is received from the slave mode controller." */
        volatile uint32_t UDIS: 1;  /* Update disable               This bit is set and cleared by  */
        /* @SVD:field=URS,bitOffset=2,bitWidth=1,description="Update request source\n              This bit is set and cleared by software to select the UEV event sources.\n              0: Any of the following events generate an update interrupt or DMA request if enabled. \n              These events can be: \n                 Counter overflow\n                 Setting the UG bit\n                 Update generation through the slave mode controller\n              1: Only counter overflow generates an update interrupt or DMA request if enabled." */
        volatile uint32_t URS: 1;  /* Update request source               This bit is set and clea */
        /* @SVD:field=OPM,bitOffset=3,bitWidth=1,description="One-pulse mode\n              0: Counter is not stopped at update event\n              1: Counter stops counting at the next update event (clearing the bit CEN)" */
        volatile uint32_t OPM: 1;  /* One-pulse mode               0: Counter is not stopped at up */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=ARPE,bitOffset=7,bitWidth=1,description="Auto-reload preload enable\n              0: ARR register is not buffered\n              1: ARR register is buffered" */
        volatile uint32_t ARPE: 1;  /* Auto-reload preload enable               0: ARR register is  */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM4_CR1_Type;

/* @SVD:register=CR2,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="TIM control register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=MMS,bitOffset=4,bitWidth=2,description="Master mode selection\n              These bits allow to select the information to be sent in master mode to slave timers for synchronization (TRGO). The combination is as follows:\n              00: Reset:the UG bit from the EGR register is used as trigger output (TRGO). If the reset is generated by the trigger input (slave mode controller configured in reset mode) then the signal on TRGO is delayed compared to the actual reset.\n              01: Enable :the Counter enable signal, CNT_EN, is used as trigger output (TRGO). It is useful to start several timers at the same time or to control a window in which a slave timer is enabled. The Counter Enable signal is generated by a logic OR between CEN control bit and the trigger input when configured in gated mode. \n              When the Counter Enable signal is controlled by the trigger input, there is a delay on TRGO, except if the master/slave mode is selected (see the MSM bit description in SMCR register).\n              10: Update:The update event is selected as trigger output (TRGO). For instance a master timer can then be used as a prescaler for a slave timer.\n              11: Gating:The delayed gating trigger is selected as trigger output (TRGO)." */
        volatile uint32_t MMS: 2;  /* Master mode selection               These bits allow to sele */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM4_CR2_Type;

/* @SVD:register=SMCR,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="TIM slave mode control register" */
/* SMCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD5,bitOffset=0,bitWidth=4,description="" */
        volatile uint32_t RSVD5: 4;
        /* @SVD:field=TS,bitOffset=4,bitWidth=2,description="Trigger selection\n              This bit-field selects the trigger input to be used to synchronize the counter.\n              00: Internal Trigger 0 (ITR0)\n              01: Internal Trigger 1 (ITR1) \n              10: Internal Trigger 2 (ITR2)\n              11: Internal Trigger 3 (ITR3)" */
        volatile uint32_t TS: 2;  /* Trigger selection               This bit-field selects the t */
        /* @SVD:field=RSVD4,bitOffset=6,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=MSM,bitOffset=7,bitWidth=1,description="Master/Slave mode. This bit should be asserted on master timer if synchronization if needed.\n              0: No action\n              1: The effect of an event on the trigger input (TRGI) is delayed to allow a perfect synchronization between the current timer and its slaves (through TRGO). It is useful if we want to synchronize several timers on a single external event." */
        volatile uint32_t MSM: 1;  /* Master/Slave mode. This bit should be asserted on master tim */
        /* @SVD:field=RSVD3,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD3: 8;
        /* @SVD:field=SMS,bitOffset=16,bitWidth=3,description="Slave mode selection\n              When external signals are selected the active edge of the trigger signal (TRGI) is linked to the polarity selected on the external input.\n              000: Slave mode disabled.\n              001: Reset Mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter and generates an update of the registers.\n              010: Trigger Mode - The counter starts at a rising edge of the trigger TRGI (but it is not reset). Only the start of the counter is controlled.\n              011: Combined reset + trigger mode - Rising edge of the selected trigger input (TRGI) reinitializes the counter, generates an update of the registers and starts the counter.\n              100: External Clock Mode - Rising edges of the selected trigger (TRGI) clock the counter." */
        volatile uint32_t SMS: 3;  /* Slave mode selection               When external signals are */
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=GTS,bitOffset=20,bitWidth=2,description="Gating trigger selection in gated mode\n              This bit-field selects the trigger input to be used to enable the counter gating.\n              00: Internal Trigger 0 (ITR0)\n              01: Internal Trigger 1 (ITR1) \n              10: Internal Trigger 2 (ITR2)\n              11: Internal Trigger 3 (ITR3)" */
        volatile uint32_t GTS: 2;  /* Gating trigger selection in gated mode               This bi */
        /* @SVD:field=GTP,bitOffset=22,bitWidth=1,description="Gating trigger polarity invert\n              0: active at high level\n              1: active at low level" */
        volatile uint32_t GTP: 1;  /* Gating trigger polarity invert               0: active at hi */
        /* @SVD:field=GM,bitOffset=23,bitWidth=1,description="Gated Mode. The counter clock is enabled when the selected trigger input (TRGI) is active (according to gating trigger polarity). The counter stops (but is not reset) as soon as the trigger becomes inactive. Both start and stop of the counter are controlled. Gated mode and slave mode can be enabled simutanuously with different trigger selection." */
        volatile uint32_t GM: 1;  /* Gated Mode. The counter clock is enabled when the selected t */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM4_SMCR_Type;

/* @SVD:register=DIER,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="TIM DMA/Interrupt enable register" */
/* DIER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIE,bitOffset=0,bitWidth=1,description="Update interrupt enable\n              0: Update interrupt disabled.\n              1: Update interrupt enabled" */
        volatile uint32_t UIE: 1;  /* Update interrupt enable               0: Update interrupt di */
        /* @SVD:field=RSVD2,bitOffset=1,bitWidth=7,description="" */
        volatile uint32_t RSVD2: 7;
        /* @SVD:field=UDE,bitOffset=8,bitWidth=1,description="Update DMA request enable\n              0: Update DMA request disabled.\n              1: Update DMA request enabled" */
        volatile uint32_t UDE: 1;  /* Update DMA request enable               0: Update DMA reques */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM4_DIER_Type;

/* @SVD:register=SR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="TIM status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UIF,bitOffset=0,bitWidth=1,description="Update interrupt flag\n              This bit is set by hardware on an update event. It is cleared by software.\n              0: No update occurred\n              1: Update interrupt pending. This bit is set by hardware when the registers are updated:\n              At overflow and if UDIS=0 in the CR1 register.\n              When CNT is reinitialized by software using the UG bit in EGR register, if URS=0 and UDIS=0 in the CR1 register.\n              When CNT is reinitialized by a trigger event (refer to the synchro control register description), if URS=0 and UDIS=0 in the CR1 register." */
        volatile uint32_t UIF: 1;  /* Update interrupt flag               This bit is set by hardw */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM4_SR_Type;

/* @SVD:register=EGR,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="Event generation register" */
/* EGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UG,bitOffset=0,bitWidth=1,description="Update generation\n              This bit can be set by software, it is automatically cleared by hardware.\n              0: No action\n              1: Re-initialize the counter and generates an update of the registers. Note that the prescaler counter is cleared too (anyway the prescaler ratio is not affected). The counter is cleared if the center-aligned mode is selected or if DIR=0 (upcounting), else it takes the auto-reload value (ARR) if DIR=1 (downcounting)." */
        volatile uint32_t UG: 1;  /* Update generation               This bit can be set by softw */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM4_EGR_Type;

/* @SVD:register=CNT,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="Counter" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=32,description="counter value" */
        volatile uint32_t CNT: 32;  /* counter value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM4_CNT_Type;

/* @SVD:register=PSC,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="Prescaler" */
/* PSC 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PSC,bitOffset=0,bitWidth=16,description="Prescaler value\n              The counter clock frequency is equal to fCLK / (PSC[15:0] + 1).\n              PSC contains the value to be loaded in the active prescaler register at each update event (including when the counter is cleared through UG bit of EGR register or through trigger controller when configured in \"reset mode\")." */
        volatile uint32_t PSC: 16;  /* Prescaler value               The counter clock frequency is */
        /* @SVD:field=RSVD,bitOffset=16,bitWidth=16,description="" */
        volatile uint32_t RSVD: 16;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM4_PSC_Type;

/* @SVD:register=ARR,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="Auto-reload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=32,description="Auto-reload value\n              ARR is the value to be loaded in the actual auto-reload register. The counter is blocked while the auto-reload value is null." */
        volatile uint32_t ARR: 32;  /* Auto-reload value               ARR is the value to be loade */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} BTIM4_ARR_Type;

/* BTIM4 寄存器结构体 */
typedef struct
{
    BTIM4_CR1_Type CR1;  /* 0x0000 - TIM control register 1 */
    BTIM4_CR2_Type CR2;  /* 0x0004 - TIM control register 2 */
    BTIM4_SMCR_Type SMCR;  /* 0x0008 - TIM slave mode control register */
    BTIM4_DIER_Type DIER;  /* 0x000C - TIM DMA/Interrupt enable register */
    BTIM4_SR_Type SR;  /* 0x0010 - TIM status register */
    BTIM4_EGR_Type EGR;  /* 0x0014 - Event generation register */
    /* @SVD:register=RSVD1,offset=0x0018,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0018 - RSVD1 */
    volatile uint32_t RESERVED7[2];  /* 0x001C - 0x0023 */
    BTIM4_CNT_Type CNT;  /* 0x0024 - Counter */
    BTIM4_PSC_Type PSC;  /* 0x0028 - Prescaler */
    BTIM4_ARR_Type ARR;  /* 0x002C - Auto-reload register */
} BTIM4_TypeDef;

/* ============================================================================ */
/* WDT2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=WDT2,base=0x4000B000,description="",groupName=WDT */

#define WDT2_BASE                                          (0x4000B000UL)

/* @SVD:register=WDT_CVR0,offset=0x0000,size=32,access=read-write,reset=0x0,description="WatchDog Counter Value 0" */
/* WDT_CVR0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COUNT_VALUE_0,bitOffset=0,bitWidth=24,description="Count Value for 1st TimeOut" */
        volatile uint32_t COUNT_VALUE_0: 24;  /* Count Value for 1st TimeOut */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT2_WDT_CVR0_Type;

/* @SVD:register=WDT_CVR1,offset=0x0004,size=32,access=read-write,reset=0x0,description="WatchDog Counter Value 1" */
/* WDT_CVR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COUNT_VALUE_1,bitOffset=0,bitWidth=24,description="Count Value for 2nd TimeOut" */
        volatile uint32_t COUNT_VALUE_1: 24;  /* Count Value for 2nd TimeOut */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT2_WDT_CVR1_Type;

/* @SVD:register=WDT_CR,offset=0x0008,size=32,access=read-write,reset=0x0,description="WatchDog Control Register" */
/* WDT_CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESET_LENGTH,bitOffset=0,bitWidth=3,description="reset pulse length in number of wdt clock cycles" */
        volatile uint32_t RESET_LENGTH: 3;  /* reset pulse length in number of wdt clock cycles */
        /* @SVD:field=RSVD2,bitOffset=3,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=RESPONSE_MODE,bitOffset=4,bitWidth=1,description="0:reset only, 1:interrupt and reset" */
        volatile uint32_t RESPONSE_MODE: 1;  /* 0:reset only, 1:interrupt and reset */
        /* @SVD:field=RSVD,bitOffset=5,bitWidth=27,description="" */
        volatile uint32_t RSVD: 27;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT2_WDT_CR_Type;

/* @SVD:register=WDT_CCR,offset=0x000C,size=32,access=read-write,reset=0x0,description="WatchDog Counter Control Register" */
/* WDT_CCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=COUNTER_CONTROL,bitOffset=0,bitWidth=8,description="SinglePulse /Write 8'h76 to restart, write8'h34 to stop, else do nothing" */
        volatile uint32_t COUNTER_CONTROL: 8;  /* SinglePulse /Write 8'h76 to restart, write8'h34 to stop, els */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT2_WDT_CCR_Type;

/* @SVD:register=WDT_ICR,offset=0x0010,size=32,access=read-write,reset=0x0,description="WatchDog Interrupt Clear Register" */
/* WDT_ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INT_CLR,bitOffset=0,bitWidth=1,description="SinglePulse /A pulse to clear interrupt" */
        volatile uint32_t INT_CLR: 1;  /* SinglePulse /A pulse to clear interrupt */
        /* @SVD:field=RSVD,bitOffset=1,bitWidth=31,description="" */
        volatile uint32_t RSVD: 31;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT2_WDT_ICR_Type;

/* @SVD:register=WDT_SR,offset=0x0014,size=32,access=read-write,reset=0x0,description="WatchDog Status Register" */
/* WDT_SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INT_ASSERT,bitOffset=0,bitWidth=1,description="Interrupt assert when 1" */
        volatile uint32_t INT_ASSERT: 1;  /* Interrupt assert when 1 */
        /* @SVD:field=WDT_ACTIVE,bitOffset=1,bitWidth=1,description="Watchdog runs when 1, else 0" */
        volatile uint32_t WDT_ACTIVE: 1;  /* Watchdog runs when 1, else 0 */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT2_WDT_SR_Type;

/* @SVD:register=WDT_WP,offset=0x0018,size=32,access=read-write,reset=0x0,description="WatchDog Write Protect Register" */
/* WDT_WP 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WRPT,bitOffset=0,bitWidth=31,description="write 0x58ab99fc generate write_protect, write 0x51ff8621 to release" */
        volatile uint32_t WRPT: 31;  /* write 0x58ab99fc generate write_protect, write 0x51ff8621 to */
        /* @SVD:field=WRPT_ST,bitOffset=31,bitWidth=1,description="1 indicates write protect is active" */
        volatile uint32_t WRPT_ST: 1;  /* 1 indicates write protect is active */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT2_WDT_WP_Type;

/* @SVD:register=WDT_FG,offset=0x001C,size=32,access=read-write,reset=0x0,description="WatchDog Flag Register" */
/* WDT_FG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RST_FG_CLR,bitOffset=0,bitWidth=1,description="SinglePulse/A pulse to clear reset flag" */
        volatile uint32_t RST_FG_CLR: 1;  /* SinglePulse/A pulse to clear reset flag */
        /* @SVD:field=RST_FG,bitOffset=1,bitWidth=1,description="1 indicates wdt has already reset system" */
        volatile uint32_t RST_FG: 1;  /* 1 indicates wdt has already reset system */
        /* @SVD:field=SYNC_FG_CLR,bitOffset=2,bitWidth=1,description="SinglePulse/A pulse to clear sync flag" */
        volatile uint32_t SYNC_FG_CLR: 1;  /* SinglePulse/A pulse to clear sync flag */
        /* @SVD:field=SYNC_FG,bitOffset=3,bitWidth=1,description="1 indicates one transition from system clk to wdt clk has complicated" */
        volatile uint32_t SYNC_FG: 1;  /* 1 indicates one transition from system clk to wdt clk has co */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} WDT2_WDT_FG_Type;

/* WDT2 寄存器结构体 */
typedef struct
{
    WDT2_WDT_CVR0_Type WDT_CVR0;  /* 0x0000 - WatchDog Counter Value 0 */
    WDT2_WDT_CVR1_Type WDT_CVR1;  /* 0x0004 - WatchDog Counter Value 1 */
    WDT2_WDT_CR_Type WDT_CR;  /* 0x0008 - WatchDog Control Register */
    WDT2_WDT_CCR_Type WDT_CCR;  /* 0x000C - WatchDog Counter Control Register */
    WDT2_WDT_ICR_Type WDT_ICR;  /* 0x0010 - WatchDog Interrupt Clear Register */
    WDT2_WDT_SR_Type WDT_SR;  /* 0x0014 - WatchDog Status Register */
    WDT2_WDT_WP_Type WDT_WP;  /* 0x0018 - WatchDog Write Protect Register */
    WDT2_WDT_FG_Type WDT_FG;  /* 0x001C - WatchDog Flag Register */
} WDT2_TypeDef;

/* ============================================================================ */
/* PTC2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=PTC2,base=0x4000C000,description="",groupName=PTC */

#define PTC2_BASE                                          (0x4000C000UL)

/* @SVD:register=ISR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="interrupt status register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCIF1,bitOffset=0,bitWidth=1,description="task complete interrupt flag for task 1" */
        volatile uint32_t TCIF1: 1;  /* task complete interrupt flag for task 1 */
        /* @SVD:field=TCIF2,bitOffset=1,bitWidth=1,description="task complete interrupt flag for task 2" */
        volatile uint32_t TCIF2: 1;  /* task complete interrupt flag for task 2 */
        /* @SVD:field=TCIF3,bitOffset=2,bitWidth=1,description="task complete interrupt flag for task 3" */
        volatile uint32_t TCIF3: 1;  /* task complete interrupt flag for task 3 */
        /* @SVD:field=TCIF4,bitOffset=3,bitWidth=1,description="task complete interrupt flag for task 4" */
        volatile uint32_t TCIF4: 1;  /* task complete interrupt flag for task 4 */
        /* @SVD:field=TCIF5,bitOffset=4,bitWidth=1,description="task complete interrupt flag for task 5" */
        volatile uint32_t TCIF5: 1;  /* task complete interrupt flag for task 5 */
        /* @SVD:field=TCIF6,bitOffset=5,bitWidth=1,description="task complete interrupt flag for task 6" */
        volatile uint32_t TCIF6: 1;  /* task complete interrupt flag for task 6 */
        /* @SVD:field=TCIF7,bitOffset=6,bitWidth=1,description="task complete interrupt flag for task 7" */
        volatile uint32_t TCIF7: 1;  /* task complete interrupt flag for task 7 */
        /* @SVD:field=TCIF8,bitOffset=7,bitWidth=1,description="task complete interrupt flag for task 8" */
        volatile uint32_t TCIF8: 1;  /* task complete interrupt flag for task 8 */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=TEIF1,bitOffset=16,bitWidth=1,description="transfer error flag for task 1" */
        volatile uint32_t TEIF1: 1;  /* transfer error flag for task 1 */
        /* @SVD:field=TEIF2,bitOffset=17,bitWidth=1,description="transfer error flag for task 2" */
        volatile uint32_t TEIF2: 1;  /* transfer error flag for task 2 */
        /* @SVD:field=TEIF3,bitOffset=18,bitWidth=1,description="transfer error flag for task 3" */
        volatile uint32_t TEIF3: 1;  /* transfer error flag for task 3 */
        /* @SVD:field=TEIF4,bitOffset=19,bitWidth=1,description="transfer error flag for task 4" */
        volatile uint32_t TEIF4: 1;  /* transfer error flag for task 4 */
        /* @SVD:field=TEIF5,bitOffset=20,bitWidth=1,description="transfer error flag for task 5" */
        volatile uint32_t TEIF5: 1;  /* transfer error flag for task 5 */
        /* @SVD:field=TEIF6,bitOffset=21,bitWidth=1,description="transfer error flag for task 6" */
        volatile uint32_t TEIF6: 1;  /* transfer error flag for task 6 */
        /* @SVD:field=TEIF7,bitOffset=22,bitWidth=1,description="transfer error flag for task 7" */
        volatile uint32_t TEIF7: 1;  /* transfer error flag for task 7 */
        /* @SVD:field=TEIF8,bitOffset=23,bitWidth=1,description="transfer error flag for task 8" */
        volatile uint32_t TEIF8: 1;  /* transfer error flag for task 8 */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_ISR_Type;

/* @SVD:register=ICR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="interrupt clear register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CTCIF1,bitOffset=0,bitWidth=1,description="clear task complete interrupt flag for task 1" */
        volatile uint32_t CTCIF1: 1;  /* clear task complete interrupt flag for task 1 */
        /* @SVD:field=CTCIF2,bitOffset=1,bitWidth=1,description="clear task complete interrupt flag for task 2" */
        volatile uint32_t CTCIF2: 1;  /* clear task complete interrupt flag for task 2 */
        /* @SVD:field=CTCIF3,bitOffset=2,bitWidth=1,description="clear task complete interrupt flag for task 3" */
        volatile uint32_t CTCIF3: 1;  /* clear task complete interrupt flag for task 3 */
        /* @SVD:field=CTCIF4,bitOffset=3,bitWidth=1,description="clear task complete interrupt flag for task 4" */
        volatile uint32_t CTCIF4: 1;  /* clear task complete interrupt flag for task 4 */
        /* @SVD:field=CTCIF5,bitOffset=4,bitWidth=1,description="clear task complete interrupt flag for task 5" */
        volatile uint32_t CTCIF5: 1;  /* clear task complete interrupt flag for task 5 */
        /* @SVD:field=CTCIF6,bitOffset=5,bitWidth=1,description="clear task complete interrupt flag for task 6" */
        volatile uint32_t CTCIF6: 1;  /* clear task complete interrupt flag for task 6 */
        /* @SVD:field=CTCIF7,bitOffset=6,bitWidth=1,description="clear task complete interrupt flag for task 7" */
        volatile uint32_t CTCIF7: 1;  /* clear task complete interrupt flag for task 7 */
        /* @SVD:field=CTCIF8,bitOffset=7,bitWidth=1,description="clear task complete interrupt flag for task 8" */
        volatile uint32_t CTCIF8: 1;  /* clear task complete interrupt flag for task 8 */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=CTEIF,bitOffset=16,bitWidth=1,description="clear transfer error flag" */
        volatile uint32_t CTEIF: 1;  /* clear transfer error flag */
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_ICR_Type;

/* @SVD:register=IER,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="interrupt enable register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TCIE1,bitOffset=0,bitWidth=1,description="enable task complete interrupt for task 1" */
        volatile uint32_t TCIE1: 1;  /* enable task complete interrupt for task 1 */
        /* @SVD:field=TCIE2,bitOffset=1,bitWidth=1,description="enable task complete interrupt for task 2" */
        volatile uint32_t TCIE2: 1;  /* enable task complete interrupt for task 2 */
        /* @SVD:field=TCIE3,bitOffset=2,bitWidth=1,description="enable task complete interrupt for task 3" */
        volatile uint32_t TCIE3: 1;  /* enable task complete interrupt for task 3 */
        /* @SVD:field=TCIE4,bitOffset=3,bitWidth=1,description="enable task complete interrupt for task 4" */
        volatile uint32_t TCIE4: 1;  /* enable task complete interrupt for task 4 */
        /* @SVD:field=TCIE5,bitOffset=4,bitWidth=1,description="enable task complete interrupt for task 5" */
        volatile uint32_t TCIE5: 1;  /* enable task complete interrupt for task 5 */
        /* @SVD:field=TCIE6,bitOffset=5,bitWidth=1,description="enable task complete interrupt for task 6" */
        volatile uint32_t TCIE6: 1;  /* enable task complete interrupt for task 6 */
        /* @SVD:field=TCIE7,bitOffset=6,bitWidth=1,description="enable task complete interrupt for task 7" */
        volatile uint32_t TCIE7: 1;  /* enable task complete interrupt for task 7 */
        /* @SVD:field=TCIE8,bitOffset=7,bitWidth=1,description="enable task complete interrupt for task 8" */
        volatile uint32_t TCIE8: 1;  /* enable task complete interrupt for task 8 */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=TEIE,bitOffset=16,bitWidth=1,description="enable transfer error flag" */
        volatile uint32_t TEIE: 1;  /* enable transfer error flag */
        /* @SVD:field=RSVD,bitOffset=17,bitWidth=15,description="" */
        volatile uint32_t RSVD: 15;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_IER_Type;

/* @SVD:register=TCR1,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="task 1 control register" */
/* TCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source\n              0: task will only be triggered by SWTRIG\n              others: task will be triggered by selected source or SWTRIG" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source               0: task will only be tri */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TCR1_Type;

/* @SVD:register=TAR1,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="task 1 address register" */
/* TAR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TAR1_Type;

/* @SVD:register=TDR1,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="task 1 data register" */
/* TDR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TDR1_Type;

/* @SVD:register=RCR1,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="task 1 repetition and delay counter register" */
/* RCR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=DLY,bitOffset=16,bitWidth=16,description="Delay time before task operation after triggered\n              0: no delay\n              others: delay DLY HCLK cycles before task operation\n              DLY is read as left delay time. DLY will be reloaded automatically after each operation." */
        volatile uint32_t DLY: 16;  /* Delay time before task operation after triggered             */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_RCR1_Type;

/* @SVD:register=TCR2,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="TCR2" */
/* TCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TCR2_Type;

/* @SVD:register=TAR2,offset=0x0024,size=32,access=read-write,reset=0x00000000,description="TAR2" */
/* TAR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TAR2_Type;

/* @SVD:register=TDR2,offset=0x0028,size=32,access=read-write,reset=0x00000000,description="TDR2" */
/* TDR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TDR2_Type;

/* @SVD:register=RCR2,offset=0x002C,size=32,access=read-write,reset=0x00000000,description="task 2 repetition and delay counter register" */
/* RCR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=DLY,bitOffset=16,bitWidth=16,description="Delay time before task operation after triggered\n              0: no delay\n              others: delay DLY HCLK cycles before task operation\n              DLY is read as left delay time. DLY will be reloaded automatically after each operation." */
        volatile uint32_t DLY: 16;  /* Delay time before task operation after triggered             */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_RCR2_Type;

/* @SVD:register=TCR3,offset=0x0030,size=32,access=read-write,reset=0x00000000,description="TCR3" */
/* TCR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TCR3_Type;

/* @SVD:register=TAR3,offset=0x0034,size=32,access=read-write,reset=0x00000000,description="TAR3" */
/* TAR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TAR3_Type;

/* @SVD:register=TDR3,offset=0x0038,size=32,access=read-write,reset=0x00000000,description="TDR3" */
/* TDR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TDR3_Type;

/* @SVD:register=RCR3,offset=0x003C,size=32,access=read-write,reset=0x00000000,description="task 3 repetition and delay counter register" */
/* RCR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=DLY,bitOffset=16,bitWidth=16,description="Delay time before task operation after triggered\n              0: no delay\n              others: delay DLY HCLK cycles before task operation\n              DLY is read as left delay time. DLY will be reloaded automatically after each operation." */
        volatile uint32_t DLY: 16;  /* Delay time before task operation after triggered             */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_RCR3_Type;

/* @SVD:register=TCR4,offset=0x0040,size=32,access=read-write,reset=0x00000000,description="TCR4" */
/* TCR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TCR4_Type;

/* @SVD:register=TAR4,offset=0x0044,size=32,access=read-write,reset=0x00000000,description="TAR4" */
/* TAR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TAR4_Type;

/* @SVD:register=TDR4,offset=0x0048,size=32,access=read-write,reset=0x00000000,description="TDR4" */
/* TDR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TDR4_Type;

/* @SVD:register=RCR4,offset=0x004C,size=32,access=read-write,reset=0x00000000,description="task 4 repetition and delay counter register" */
/* RCR4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=DLY,bitOffset=16,bitWidth=16,description="Delay time before task operation after triggered\n              0: no delay\n              others: delay DLY HCLK cycles before task operation\n              DLY is read as left delay time. DLY will be reloaded automatically after each operation." */
        volatile uint32_t DLY: 16;  /* Delay time before task operation after triggered             */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_RCR4_Type;

/* @SVD:register=TCR5,offset=0x0050,size=32,access=read-write,reset=0x00000000,description="TCR5" */
/* TCR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TCR5_Type;

/* @SVD:register=TAR5,offset=0x0054,size=32,access=read-write,reset=0x00000000,description="TAR5" */
/* TAR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TAR5_Type;

/* @SVD:register=TDR5,offset=0x0058,size=32,access=read-write,reset=0x00000000,description="TDR5" */
/* TDR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TDR5_Type;

/* @SVD:register=RCR5,offset=0x005C,size=32,access=read-write,reset=0x00000000,description="task 5 repetition counter register" */
/* RCR5 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_RCR5_Type;

/* @SVD:register=TCR6,offset=0x0060,size=32,access=read-write,reset=0x00000000,description="TCR6" */
/* TCR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TCR6_Type;

/* @SVD:register=TAR6,offset=0x0064,size=32,access=read-write,reset=0x00000000,description="TAR6" */
/* TAR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TAR6_Type;

/* @SVD:register=TDR6,offset=0x0068,size=32,access=read-write,reset=0x00000000,description="TDR6" */
/* TDR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TDR6_Type;

/* @SVD:register=RCR6,offset=0x006C,size=32,access=read-write,reset=0x00000000,description="task 6 repetition counter register" */
/* RCR6 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_RCR6_Type;

/* @SVD:register=TCR7,offset=0x0070,size=32,access=read-write,reset=0x00000000,description="TCR7" */
/* TCR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TCR7_Type;

/* @SVD:register=TAR7,offset=0x0074,size=32,access=read-write,reset=0x00000000,description="TAR7" */
/* TAR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TAR7_Type;

/* @SVD:register=TDR7,offset=0x0078,size=32,access=read-write,reset=0x00000000,description="TDR7" */
/* TDR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TDR7_Type;

/* @SVD:register=RCR7,offset=0x007C,size=32,access=read-write,reset=0x00000000,description="task 7 repetition counter register" */
/* RCR7 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_RCR7_Type;

/* @SVD:register=TCR8,offset=0x0080,size=32,access=read-write,reset=0x00000000,description="TCR8" */
/* TCR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TRIGSEL,bitOffset=0,bitWidth=8,description="select trigger source" */
        volatile uint32_t TRIGSEL: 8;  /* select trigger source */
        /* @SVD:field=RSVD2,bitOffset=8,bitWidth=8,description="" */
        volatile uint32_t RSVD2: 8;
        /* @SVD:field=OP,bitOffset=16,bitWidth=3,description="task operation\n              3'b000: direct write data\n              3'b100: read then XOR with data and write back\n              3'b101: read then OR with data and write back\n              3'b110: read then AND with data and write back\n              3'b111: read then add with data and write back" */
        volatile uint32_t OP: 3;  /* task operation               3'b000: direct write data       */
        /* @SVD:field=TRIGPOL,bitOffset=19,bitWidth=1,description="trigger polarity\n              0: select positive edge of trigger\n              1: select negative edge of trigger" */
        volatile uint32_t TRIGPOL: 1;  /* trigger polarity               0: select positive edge of tr */
        /* @SVD:field=SWTRIG,bitOffset=20,bitWidth=1,description="software trigger\n              task will be triggerd at once after SWTRIG set. SWTRIG will be cleared automatically." */
        volatile uint32_t SWTRIG: 1;  /* software trigger               task will be triggerd at once */
        /* @SVD:field=REPEN,bitOffset=21,bitWidth=1,description="repetition enable\n              0: task will be triggerd no matter what value REP is\n              1: task will only be triggerd when REP is not 0" */
        volatile uint32_t REPEN: 1;  /* repetition enable               0: task will be triggerd no  */
        /* @SVD:field=REPTRIG,bitOffset=22,bitWidth=1,description="repetition trigger\n              0: ptc trigger will be generated after each operation\n              1: ptc trigger will be generated after operation for REP times" */
        volatile uint32_t REPTRIG: 1;  /* repetition trigger               0: ptc trigger will be gene */
        /* @SVD:field=REPIRQ,bitOffset=23,bitWidth=1,description="repetition interrupt\n              0: interrupt will be generated after each operation\n              1: interrupt will be generated after operation for REP times" */
        volatile uint32_t REPIRQ: 1;  /* repetition interrupt               0: interrupt will be gene */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TCR8_Type;

/* @SVD:register=TAR8,offset=0x0084,size=32,access=read-write,reset=0x00000000,description="TAR8" */
/* TAR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ADDR,bitOffset=0,bitWidth=32,description="peripheral address to access to" */
        volatile uint32_t ADDR: 32;  /* peripheral address to access to */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TAR8_Type;

/* @SVD:register=TDR8,offset=0x0088,size=32,access=read-write,reset=0x00000000,description="TDR8" */
/* TDR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="data value for task operation" */
        volatile uint32_t DATA: 32;  /* data value for task operation */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_TDR8_Type;

/* @SVD:register=RCR8,offset=0x008C,size=32,access=read-write,reset=0x00000000,description="task 8 repetition counter register" */
/* RCR8 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=10,description="Repetition counter value\n              if REPEN is 1, task will only be triggerd when REP is not 0. \n              when REP is larger than 0, it will be decrease by 1 automatically each time task triggered." */
        volatile uint32_t REP: 10;  /* Repetition counter value               if REPEN is 1, task w */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=22,description="" */
        volatile uint32_t RSVD: 22;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_RCR8_Type;

/* @SVD:register=MEM1,offset=0x00D0,size=32,access=read-write,reset=0x00000000,description="temporary memory 1" */
/* MEM1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="memory to store temporary variables" */
        volatile uint32_t DATA: 32;  /* memory to store temporary variables */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_MEM1_Type;

/* @SVD:register=MEM2,offset=0x00D4,size=32,access=read-write,reset=0x00000000,description="temporary memory 2" */
/* MEM2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="memory to store temporary variables" */
        volatile uint32_t DATA: 32;  /* memory to store temporary variables */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_MEM2_Type;

/* @SVD:register=MEM3,offset=0x00D8,size=32,access=read-write,reset=0x00000000,description="temporary memory 3" */
/* MEM3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="memory to store temporary variables" */
        volatile uint32_t DATA: 32;  /* memory to store temporary variables */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_MEM3_Type;

/* @SVD:register=MEM4,offset=0x00DC,size=32,access=read-write,reset=0x00000000,description="temporary memory 4" */
/* MEM4 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="memory to store temporary variables" */
        volatile uint32_t DATA: 32;  /* memory to store temporary variables */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_MEM4_Type;

/* @SVD:register=GPIO31_0,offset=0x00E0,size=32,access=read-write,reset=0x00000000,description="GPIO31_0" */
/* GPIO31_0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SELA,bitOffset=0,bitWidth=5,description="select trigger A of GPIO 31~0\n              0: select GPIO 0\n              1: select GPIO 1\n              ......\n              31: select GPIO 31" */
        volatile uint32_t SELA: 5;  /* select trigger A of GPIO 31~0               0: select GPIO 0 */
        /* @SVD:field=RSVD4,bitOffset=5,bitWidth=3,description="" */
        volatile uint32_t RSVD4: 3;
        /* @SVD:field=SELB,bitOffset=8,bitWidth=5,description="select trigger B of GPIO 31~0" */
        volatile uint32_t SELB: 5;  /* select trigger B of GPIO 31~0 */
        /* @SVD:field=RSVD3,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t RSVD3: 3;
        /* @SVD:field=SELC,bitOffset=16,bitWidth=5,description="select trigger C of GPIO 31~0" */
        volatile uint32_t SELC: 5;  /* select trigger C of GPIO 31~0 */
        /* @SVD:field=RSVD2,bitOffset=21,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=SELD,bitOffset=24,bitWidth=5,description="select trigger D of GPIO 31~0" */
        volatile uint32_t SELD: 5;  /* select trigger D of GPIO 31~0 */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_GPIO31_0_Type;

/* @SVD:register=GPIO63_32,offset=0x00E4,size=32,access=read-write,reset=0x00000000,description="GPIO63_32" */
/* GPIO63_32 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SELA,bitOffset=0,bitWidth=5,description="select trigger A of GPIO 63~32\n              0: select GPIO 32\n              1: select GPIO 33\n              ......\n              31: select GPIO 63" */
        volatile uint32_t SELA: 5;  /* select trigger A of GPIO 63~32               0: select GPIO  */
        /* @SVD:field=RSVD4,bitOffset=5,bitWidth=3,description="" */
        volatile uint32_t RSVD4: 3;
        /* @SVD:field=SELB,bitOffset=8,bitWidth=5,description="select trigger B of GPIO 63~32" */
        volatile uint32_t SELB: 5;  /* select trigger B of GPIO 63~32 */
        /* @SVD:field=RSVD3,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t RSVD3: 3;
        /* @SVD:field=SELC,bitOffset=16,bitWidth=5,description="select trigger C of GPIO 63~32" */
        volatile uint32_t SELC: 5;  /* select trigger C of GPIO 63~32 */
        /* @SVD:field=RSVD2,bitOffset=21,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=SELD,bitOffset=24,bitWidth=5,description="select trigger D of GPIO 63~32" */
        volatile uint32_t SELD: 5;  /* select trigger D of GPIO 63~32 */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_GPIO63_32_Type;

/* @SVD:register=GPIO95_64,offset=0x00E8,size=32,access=read-write,reset=0x00000000,description="GPIO95_64" */
/* GPIO95_64 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SELA,bitOffset=0,bitWidth=5,description="select trigger A of GPIO 95~64\n              0: select GPIO 64\n              1: select GPIO 65\n              ......\n              31: select GPIO 95" */
        volatile uint32_t SELA: 5;  /* select trigger A of GPIO 95~64               0: select GPIO  */
        /* @SVD:field=RSVD4,bitOffset=5,bitWidth=3,description="" */
        volatile uint32_t RSVD4: 3;
        /* @SVD:field=SELB,bitOffset=8,bitWidth=5,description="select trigger B of GPIO 95~64" */
        volatile uint32_t SELB: 5;  /* select trigger B of GPIO 95~64 */
        /* @SVD:field=RSVD3,bitOffset=13,bitWidth=3,description="" */
        volatile uint32_t RSVD3: 3;
        /* @SVD:field=SELC,bitOffset=16,bitWidth=5,description="select trigger C of GPIO 95~64" */
        volatile uint32_t SELC: 5;  /* select trigger C of GPIO 95~64 */
        /* @SVD:field=RSVD2,bitOffset=21,bitWidth=3,description="" */
        volatile uint32_t RSVD2: 3;
        /* @SVD:field=SELD,bitOffset=24,bitWidth=5,description="select trigger D of GPIO 95~64" */
        volatile uint32_t SELD: 5;  /* select trigger D of GPIO 95~64 */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} PTC2_GPIO95_64_Type;

/* PTC2 寄存器结构体 */
typedef struct
{
    PTC2_ISR_Type ISR;  /* 0x0000 - interrupt status register */
    PTC2_ICR_Type ICR;  /* 0x0004 - interrupt clear register */
    PTC2_IER_Type IER;  /* 0x0008 - interrupt enable register */
    /* @SVD:register=RSVD2,offset=0x000C,size=32,access=read-write,reset=0x0,description="RSVD2" */
    volatile uint32_t RSVD2;  /* 0x000C - RSVD2 */
    PTC2_TCR1_Type TCR1;  /* 0x0010 - task 1 control register */
    PTC2_TAR1_Type TAR1;  /* 0x0014 - task 1 address register */
    PTC2_TDR1_Type TDR1;  /* 0x0018 - task 1 data register */
    PTC2_RCR1_Type RCR1;  /* 0x001C - task 1 repetition and delay counter regi */
    PTC2_TCR2_Type TCR2;  /* 0x0020 - TCR2 */
    PTC2_TAR2_Type TAR2;  /* 0x0024 - TAR2 */
    PTC2_TDR2_Type TDR2;  /* 0x0028 - TDR2 */
    PTC2_RCR2_Type RCR2;  /* 0x002C - task 2 repetition and delay counter regi */
    PTC2_TCR3_Type TCR3;  /* 0x0030 - TCR3 */
    PTC2_TAR3_Type TAR3;  /* 0x0034 - TAR3 */
    PTC2_TDR3_Type TDR3;  /* 0x0038 - TDR3 */
    PTC2_RCR3_Type RCR3;  /* 0x003C - task 3 repetition and delay counter regi */
    PTC2_TCR4_Type TCR4;  /* 0x0040 - TCR4 */
    PTC2_TAR4_Type TAR4;  /* 0x0044 - TAR4 */
    PTC2_TDR4_Type TDR4;  /* 0x0048 - TDR4 */
    PTC2_RCR4_Type RCR4;  /* 0x004C - task 4 repetition and delay counter regi */
    PTC2_TCR5_Type TCR5;  /* 0x0050 - TCR5 */
    PTC2_TAR5_Type TAR5;  /* 0x0054 - TAR5 */
    PTC2_TDR5_Type TDR5;  /* 0x0058 - TDR5 */
    PTC2_RCR5_Type RCR5;  /* 0x005C - task 5 repetition counter register */
    PTC2_TCR6_Type TCR6;  /* 0x0060 - TCR6 */
    PTC2_TAR6_Type TAR6;  /* 0x0064 - TAR6 */
    PTC2_TDR6_Type TDR6;  /* 0x0068 - TDR6 */
    PTC2_RCR6_Type RCR6;  /* 0x006C - task 6 repetition counter register */
    PTC2_TCR7_Type TCR7;  /* 0x0070 - TCR7 */
    PTC2_TAR7_Type TAR7;  /* 0x0074 - TAR7 */
    PTC2_TDR7_Type TDR7;  /* 0x0078 - TDR7 */
    PTC2_RCR7_Type RCR7;  /* 0x007C - task 7 repetition counter register */
    PTC2_TCR8_Type TCR8;  /* 0x0080 - TCR8 */
    PTC2_TAR8_Type TAR8;  /* 0x0084 - TAR8 */
    PTC2_TDR8_Type TDR8;  /* 0x0088 - TDR8 */
    PTC2_RCR8_Type RCR8;  /* 0x008C - task 8 repetition counter register */
    /* @SVD:register=RSVD1,offset=0x0090,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0090 - RSVD1 */
    volatile uint32_t RESERVED37[15];  /* 0x0094 - 0x00CF */
    PTC2_MEM1_Type MEM1;  /* 0x00D0 - temporary memory 1 */
    PTC2_MEM2_Type MEM2;  /* 0x00D4 - temporary memory 2 */
    PTC2_MEM3_Type MEM3;  /* 0x00D8 - temporary memory 3 */
    PTC2_MEM4_Type MEM4;  /* 0x00DC - temporary memory 4 */
    PTC2_GPIO31_0_Type GPIO31_0;  /* 0x00E0 - GPIO31_0 */
    PTC2_GPIO63_32_Type GPIO63_32;  /* 0x00E4 - GPIO63_32 */
    PTC2_GPIO95_64_Type GPIO95_64;  /* 0x00E8 - GPIO95_64 */
} PTC2_TypeDef;

/* ============================================================================ */
/* LPSYS_CFG 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=LPSYS_CFG,base=0x4000F000,description="",groupName=LPSYS_CFG */

#define LPSYS_CFG_BASE                                     (0x4000F000UL)

/* @SVD:register=SYSCR,offset=0x0000,size=32,access=read-write,reset=0x0,description="System Configure Register" */
/* SYSCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=WDT2_REBOOT,bitOffset=0,bitWidth=1,description="If set to 1, WDT2 reset will reboot the whole chip" */
        volatile uint32_t WDT2_REBOOT: 1;  /* If set to 1, WDT2 reset will reboot the whole chip */
        /* @SVD:field=DBG_SWAP,bitOffset=1,bitWidth=2,description="reserved for debug" */
        volatile uint32_t DBG_SWAP: 2;  /* reserved for debug */
        /* @SVD:field=LDO_VSEL,bitOffset=3,bitWidth=1,description="select work mode\n              0: D\n              1: S" */
        volatile uint32_t LDO_VSEL: 1;  /* select work mode               0: D               1: S */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_CFG_SYSCR_Type;

/* @SVD:register=RTC_TR,offset=0x0004,size=32,access=read-write,reset=0x0,description="Mirrored RTC Time Register" */
/* RTC_TR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SS,bitOffset=0,bitWidth=10,description="Sub-second counter" */
        volatile uint32_t SS: 10;  /* Sub-second counter */
        /* @SVD:field=RSVD,bitOffset=10,bitWidth=1,description="" */
        volatile uint32_t RSVD: 1;
        /* @SVD:field=SU,bitOffset=11,bitWidth=4,description="Second units in BCD format" */
        volatile uint32_t SU: 4;  /* Second units in BCD format */
        /* @SVD:field=ST,bitOffset=15,bitWidth=3,description="Second tens in BCD format" */
        volatile uint32_t ST: 3;  /* Second tens in BCD format */
        /* @SVD:field=MNU,bitOffset=18,bitWidth=4,description="Minute units in BCD format" */
        volatile uint32_t MNU: 4;  /* Minute units in BCD format */
        /* @SVD:field=MNT,bitOffset=22,bitWidth=3,description="Minute tens in BCD format" */
        volatile uint32_t MNT: 3;  /* Minute tens in BCD format */
        /* @SVD:field=HU,bitOffset=25,bitWidth=4,description="Hour units in BCD format" */
        volatile uint32_t HU: 4;  /* Hour units in BCD format */
        /* @SVD:field=HT,bitOffset=29,bitWidth=2,description="Hour tens in BCD format" */
        volatile uint32_t HT: 2;  /* Hour tens in BCD format */
        /* @SVD:field=PM,bitOffset=31,bitWidth=1,description="AM/PM notation\n              0: AM\n              1: PM" */
        volatile uint32_t PM: 1;  /* AM/PM notation               0: AM               1: PM */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_CFG_RTC_TR_Type;

/* @SVD:register=RTC_DR,offset=0x0008,size=32,access=read-write,reset=0x0,description="Mirrored RTC Date Register" */
/* RTC_DR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DU,bitOffset=0,bitWidth=4,description="Date units in BCD format" */
        volatile uint32_t DU: 4;  /* Date units in BCD format */
        /* @SVD:field=DT,bitOffset=4,bitWidth=2,description="Date tens in BCD format" */
        volatile uint32_t DT: 2;  /* Date tens in BCD format */
        /* @SVD:field=RSVD2,bitOffset=6,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=MU,bitOffset=8,bitWidth=4,description="Month units in BCD format" */
        volatile uint32_t MU: 4;  /* Month units in BCD format */
        /* @SVD:field=MT,bitOffset=12,bitWidth=1,description="Month tens in BCD format" */
        volatile uint32_t MT: 1;  /* Month tens in BCD format */
        /* @SVD:field=WD,bitOffset=13,bitWidth=3,description="Week day units\n              000: forbidden\n              001: Monday\n              ...\n              111: Sunday" */
        volatile uint32_t WD: 3;  /* Week day units               000: forbidden               00 */
        /* @SVD:field=YU,bitOffset=16,bitWidth=4,description="Year units in BCD format" */
        volatile uint32_t YU: 4;  /* Year units in BCD format */
        /* @SVD:field=YT,bitOffset=20,bitWidth=4,description="Year tens in BCD format" */
        volatile uint32_t YT: 4;  /* Year tens in BCD format */
        /* @SVD:field=CB,bitOffset=24,bitWidth=1,description="Century flag" */
        volatile uint32_t CB: 1;  /* Century flag */
        /* @SVD:field=RSVD,bitOffset=25,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
        /* @SVD:field=ERR,bitOffset=31,bitWidth=1,description="reserved for debug" */
        volatile uint32_t ERR: 1;  /* reserved for debug */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_CFG_RTC_DR_Type;

/* @SVD:register=ULPMCR,offset=0x000C,size=32,access=read-write,reset=0x0,description="ULP Memory Control register" */
/* ULPMCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RAM_RM,bitOffset=0,bitWidth=2,description="reserved for debug" */
        volatile uint32_t RAM_RM: 2;  /* reserved for debug */
        /* @SVD:field=RSVD4,bitOffset=2,bitWidth=2,description="" */
        volatile uint32_t RSVD4: 2;
        /* @SVD:field=RAM_RME,bitOffset=4,bitWidth=1,description="reserved for debug" */
        volatile uint32_t RAM_RME: 1;  /* reserved for debug */
        /* @SVD:field=RAM_RA,bitOffset=5,bitWidth=2,description="reserved for debug" */
        volatile uint32_t RAM_RA: 2;  /* reserved for debug */
        /* @SVD:field=RAM_WA,bitOffset=7,bitWidth=3,description="reserved for debug" */
        volatile uint32_t RAM_WA: 3;  /* reserved for debug */
        /* @SVD:field=RAM_WPULSE,bitOffset=10,bitWidth=3,description="reserved for debug" */
        volatile uint32_t RAM_WPULSE: 3;  /* reserved for debug */
        /* @SVD:field=RSVD3,bitOffset=13,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=RAM_RDY,bitOffset=15,bitWidth=1,description="reserved for debug" */
        volatile uint32_t RAM_RDY: 1;  /* reserved for debug */
        /* @SVD:field=ROM_RM,bitOffset=16,bitWidth=2,description="reserved for debug" */
        volatile uint32_t ROM_RM: 2;  /* reserved for debug */
        /* @SVD:field=RSVD2,bitOffset=18,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=ROM_RME,bitOffset=20,bitWidth=1,description="reserved for debug" */
        volatile uint32_t ROM_RME: 1;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=21,bitWidth=9,description="" */
        volatile uint32_t RSVD: 9;
        /* @SVD:field=ROM_DIS,bitOffset=30,bitWidth=1,description="reserved for debug" */
        volatile uint32_t ROM_DIS: 1;  /* reserved for debug */
        /* @SVD:field=FORCE_ON,bitOffset=31,bitWidth=1,description="reserved for debug" */
        volatile uint32_t FORCE_ON: 1;  /* reserved for debug */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_CFG_ULPMCR_Type;

/* @SVD:register=DBGR,offset=0x0010,size=32,access=read-write,reset=0x0,description="Debug Register" */
/* DBGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SEL_L,bitOffset=0,bitWidth=4,description="reserved for debug" */
        volatile uint32_t SEL_L: 4;  /* reserved for debug */
        /* @SVD:field=SEL_H,bitOffset=4,bitWidth=4,description="reserved for debug" */
        volatile uint32_t SEL_H: 4;  /* reserved for debug */
        /* @SVD:field=BITEN_L,bitOffset=8,bitWidth=8,description="reserved for debug" */
        volatile uint32_t BITEN_L: 8;  /* reserved for debug */
        /* @SVD:field=BITEN_H,bitOffset=16,bitWidth=8,description="reserved for debug" */
        volatile uint32_t BITEN_H: 8;  /* reserved for debug */
        /* @SVD:field=CLK_SEL,bitOffset=24,bitWidth=3,description="reserved for debug" */
        volatile uint32_t CLK_SEL: 3;  /* reserved for debug */
        /* @SVD:field=CLK_EN,bitOffset=27,bitWidth=1,description="reserved for debug" */
        volatile uint32_t CLK_EN: 1;  /* reserved for debug */
        /* @SVD:field=LP2HP_NMI,bitOffset=28,bitWidth=1,description="set 1 to send NMI interrupt to HCPU" */
        volatile uint32_t LP2HP_NMI: 1;  /* set 1 to send NMI interrupt to HCPU */
        /* @SVD:field=HP2LP_NMIE,bitOffset=29,bitWidth=1,description="HP2LP NMI interrupt enable" */
        volatile uint32_t HP2LP_NMIE: 1;  /* HP2LP NMI interrupt enable */
        /* @SVD:field=HP2LP_NMIF,bitOffset=30,bitWidth=1,description="HP2LP NMI interrupt flag" */
        volatile uint32_t HP2LP_NMIF: 1;  /* HP2LP NMI interrupt flag */
        /* @SVD:field=READY,bitOffset=31,bitWidth=1,description="reserved for debug" */
        volatile uint32_t READY: 1;  /* reserved for debug */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_CFG_DBGR_Type;

/* @SVD:register=MDBGR,offset=0x0014,size=32,access=read-write,reset=0x0,description="Memory Debug Register" */
/* MDBGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LS_RAM0,bitOffset=0,bitWidth=1,description="reserved for debug" */
        volatile uint32_t LS_RAM0: 1;  /* reserved for debug */
        /* @SVD:field=LS_RAM1,bitOffset=1,bitWidth=1,description="reserved for debug" */
        volatile uint32_t LS_RAM1: 1;  /* reserved for debug */
        /* @SVD:field=LS_ROM,bitOffset=2,bitWidth=1,description="reserved for debug" */
        volatile uint32_t LS_ROM: 1;  /* reserved for debug */
        /* @SVD:field=PD_ROM,bitOffset=3,bitWidth=1,description="reserved for debug" */
        volatile uint32_t PD_ROM: 1;  /* reserved for debug */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_CFG_MDBGR_Type;

/* @SVD:register=USART4_PINR,offset=0x0038,size=32,access=read-write,reset=0x0,description="USART4 Pin Register" */
/* USART4_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TXD_PIN,bitOffset=0,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t TXD_PIN: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD4,bitOffset=3,bitWidth=5,description="" */
        volatile uint32_t RSVD4: 5;
        /* @SVD:field=RXD_PIN,bitOffset=8,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t RXD_PIN: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD3: 5;
        /* @SVD:field=RTS_PIN,bitOffset=16,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t RTS_PIN: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=CTS_PIN,bitOffset=24,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t CTS_PIN: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_CFG_USART4_PINR_Type;

/* @SVD:register=USART5_PINR,offset=0x003C,size=32,access=read-write,reset=0x0,description="USART5 Pin Register" */
/* USART5_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=TXD_PIN,bitOffset=0,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t TXD_PIN: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD4,bitOffset=3,bitWidth=5,description="" */
        volatile uint32_t RSVD4: 5;
        /* @SVD:field=RXD_PIN,bitOffset=8,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t RXD_PIN: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD3: 5;
        /* @SVD:field=RTS_PIN,bitOffset=16,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t RTS_PIN: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=CTS_PIN,bitOffset=24,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t CTS_PIN: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_CFG_USART5_PINR_Type;

/* @SVD:register=PTA_PINR,offset=0x0040,size=32,access=read-write,reset=0x0,description="PTA Pin Register" */
/* PTA_PINR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=BT_ACTIVE,bitOffset=0,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t BT_ACTIVE: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD4,bitOffset=3,bitWidth=5,description="" */
        volatile uint32_t RSVD4: 5;
        /* @SVD:field=BT_COLLISION,bitOffset=8,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t BT_COLLISION: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD3,bitOffset=11,bitWidth=5,description="" */
        volatile uint32_t RSVD3: 5;
        /* @SVD:field=BT_PRIORITY,bitOffset=16,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t BT_PRIORITY: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD2,bitOffset=19,bitWidth=5,description="" */
        volatile uint32_t RSVD2: 5;
        /* @SVD:field=WLAN_ACTIVE,bitOffset=24,bitWidth=3,description="Connect function pin to selected IO(PB). \n              0 to 3 for PB00 to PB03.\n              Other values for floating." */
        volatile uint32_t WLAN_ACTIVE: 3;  /* Connect function pin to selected IO(PB).                0 to */
        /* @SVD:field=RSVD,bitOffset=27,bitWidth=5,description="" */
        volatile uint32_t RSVD: 5;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_CFG_PTA_PINR_Type;

/* LPSYS_CFG 寄存器结构体 */
typedef struct
{
    LPSYS_CFG_SYSCR_Type SYSCR;  /* 0x0000 - System Configure Register */
    LPSYS_CFG_RTC_TR_Type RTC_TR;  /* 0x0004 - Mirrored RTC Time Register */
    LPSYS_CFG_RTC_DR_Type RTC_DR;  /* 0x0008 - Mirrored RTC Date Register */
    LPSYS_CFG_ULPMCR_Type ULPMCR;  /* 0x000C - ULP Memory Control register */
    LPSYS_CFG_DBGR_Type DBGR;  /* 0x0010 - Debug Register */
    LPSYS_CFG_MDBGR_Type MDBGR;  /* 0x0014 - Memory Debug Register */
    /* @SVD:register=RSVD1,offset=0x0018,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0018 - RSVD1 */
    volatile uint32_t RESERVED7[7];  /* 0x001C - 0x0037 */
    LPSYS_CFG_USART4_PINR_Type USART4_PINR;  /* 0x0038 - USART4 Pin Register */
    LPSYS_CFG_USART5_PINR_Type USART5_PINR;  /* 0x003C - USART5 Pin Register */
    LPSYS_CFG_PTA_PINR_Type PTA_PINR;  /* 0x0040 - PTA Pin Register */
} LPSYS_CFG_TypeDef;

/* ============================================================================ */
/* LPSYS_AON 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=LPSYS_AON,base=0x40040000,description="",groupName=LPSYS_AON */

#define LPSYS_AON_BASE                                     (0x40040000UL)

/* @SVD:register=PMR,offset=0x0000,size=32,access=read-write,reset=0x0,description="Power Mode Register" */
/* PMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=MODE,bitOffset=0,bitWidth=2,description="Power Mode: 2'h0 - active/idle; 2'h1 - light sleep; 2'h2 - deep sleep; 2'h3 - standby" */
        volatile uint32_t MODE: 2;  /* Power Mode: 2'h0 - active/idle; 2'h1 - light sleep; 2'h2 - d */
        /* @SVD:field=CPUWAIT,bitOffset=2,bitWidth=1,description="Stall CPU out of reset. Should be cleared before LCPU run" */
        volatile uint32_t CPUWAIT: 1;  /* Stall CPU out of reset. Should be cleared before LCPU run */
        /* @SVD:field=RSVD,bitOffset=3,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
        /* @SVD:field=FORCE_SLEEP,bitOffset=31,bitWidth=1,description="Set 1 to force enter low power mode. Will be cleared automatically" */
        volatile uint32_t FORCE_SLEEP: 1;  /* Set 1 to force enter low power mode. Will be cleared automat */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_PMR_Type;

/* @SVD:register=CR1,offset=0x0004,size=32,access=read-write,reset=0x0,description="Control Register 1" */
/* CR1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PIN0_MODE,bitOffset=0,bitWidth=3,description="mode for wakeup PIN0 (PA24)\n              0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge, 4/5/6/7: pos or neg edge" */
        volatile uint32_t PIN0_MODE: 3;  /* mode for wakeup PIN0 (PA24)               0 - high level, 1  */
        /* @SVD:field=PIN1_MODE,bitOffset=3,bitWidth=3,description="mode for wakeup PIN1 (PA25)" */
        volatile uint32_t PIN1_MODE: 3;  /* mode for wakeup PIN1 (PA25) */
        /* @SVD:field=PIN2_MODE,bitOffset=6,bitWidth=3,description="mode for wakeup PIN2 (PA26)" */
        volatile uint32_t PIN2_MODE: 3;  /* mode for wakeup PIN2 (PA26) */
        /* @SVD:field=PIN3_MODE,bitOffset=9,bitWidth=3,description="mode for wakeup PIN3 (PA27)" */
        volatile uint32_t PIN3_MODE: 3;  /* mode for wakeup PIN3 (PA27) */
        /* @SVD:field=RSVD,bitOffset=12,bitWidth=13,description="" */
        volatile uint32_t RSVD: 13;
        /* @SVD:field=PINOUT_SEL0,bitOffset=25,bitWidth=3,description="for debug only" */
        volatile uint32_t PINOUT_SEL0: 3;  /* for debug only */
        /* @SVD:field=PINOUT_SEL1,bitOffset=28,bitWidth=3,description="for debug only" */
        volatile uint32_t PINOUT_SEL1: 3;  /* for debug only */
        /* @SVD:field=GTIM_EN,bitOffset=31,bitWidth=1,description="Enable global timer" */
        volatile uint32_t GTIM_EN: 1;  /* Enable global timer */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_CR1_Type;

/* @SVD:register=CR2,offset=0x0008,size=32,access=read-write,reset=0x0,description="Control Register 2" */
/* CR2 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=PIN10_MODE,bitOffset=6,bitWidth=3,description="mode for wakeup PIN10 (PA34)\n              0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge, 4/5/6/7: pos or neg edge" */
        volatile uint32_t PIN10_MODE: 3;  /* mode for wakeup PIN10 (PA34)               0 - high level, 1 */
        /* @SVD:field=PIN11_MODE,bitOffset=9,bitWidth=3,description="mode for wakeup PIN11 (PA35)" */
        volatile uint32_t PIN11_MODE: 3;  /* mode for wakeup PIN11 (PA35) */
        /* @SVD:field=PIN12_MODE,bitOffset=12,bitWidth=3,description="mode for wakeup PIN12 (PA36)" */
        volatile uint32_t PIN12_MODE: 3;  /* mode for wakeup PIN12 (PA36) */
        /* @SVD:field=PIN13_MODE,bitOffset=15,bitWidth=3,description="mode for wakeup PIN13 (PA37)" */
        volatile uint32_t PIN13_MODE: 3;  /* mode for wakeup PIN13 (PA37) */
        /* @SVD:field=PIN14_MODE,bitOffset=18,bitWidth=3,description="mode for wakeup PIN14 (PA38)" */
        volatile uint32_t PIN14_MODE: 3;  /* mode for wakeup PIN14 (PA38) */
        /* @SVD:field=PIN15_MODE,bitOffset=21,bitWidth=3,description="mode for wakeup PIN15 (PA39)" */
        volatile uint32_t PIN15_MODE: 3;  /* mode for wakeup PIN15 (PA39) */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_CR2_Type;

/* @SVD:register=CR3,offset=0x000C,size=32,access=read-write,reset=0x0,description="Control Register 3" */
/* CR3 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PIN16_MODE,bitOffset=0,bitWidth=3,description="mode for wakeup PIN16 (PA40)\n              0 - high level, 1 - low level, 2 - pos edge, 3 - neg edge, 4/5/6/7: pos or neg edge" */
        volatile uint32_t PIN16_MODE: 3;  /* mode for wakeup PIN16 (PA40)               0 - high level, 1 */
        /* @SVD:field=PIN17_MODE,bitOffset=3,bitWidth=3,description="mode for wakeup PIN17 (PA41)" */
        volatile uint32_t PIN17_MODE: 3;  /* mode for wakeup PIN17 (PA41) */
        /* @SVD:field=PIN18_MODE,bitOffset=6,bitWidth=3,description="mode for wakeup PIN18 (PA42)" */
        volatile uint32_t PIN18_MODE: 3;  /* mode for wakeup PIN18 (PA42) */
        /* @SVD:field=PIN19_MODE,bitOffset=9,bitWidth=3,description="mode for wakeup PIN19 (PA43)" */
        volatile uint32_t PIN19_MODE: 3;  /* mode for wakeup PIN19 (PA43) */
        /* @SVD:field=PIN20_MODE,bitOffset=12,bitWidth=3,description="mode for wakeup PIN20 (PA44)" */
        volatile uint32_t PIN20_MODE: 3;  /* mode for wakeup PIN20 (PA44) */
        /* @SVD:field=RSVD,bitOffset=15,bitWidth=17,description="" */
        volatile uint32_t RSVD: 17;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_CR3_Type;

/* @SVD:register=ACR,offset=0x0010,size=32,access=read-write,reset=0x0,description="Active Mode Control register" */
/* ACR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HRC48_REQ,bitOffset=0,bitWidth=1,description="Request hrc48 in active mode" */
        volatile uint32_t HRC48_REQ: 1;  /* Request hrc48 in active mode */
        /* @SVD:field=HXT48_REQ,bitOffset=1,bitWidth=1,description="Request hxt48 in active mode" */
        volatile uint32_t HXT48_REQ: 1;  /* Request hxt48 in active mode */
        /* @SVD:field=PWR_REQ,bitOffset=2,bitWidth=1,description="Request power during Active mode" */
        volatile uint32_t PWR_REQ: 1;  /* Request power during Active mode */
        /* @SVD:field=EXTPWR_REQ,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t EXTPWR_REQ: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
        /* @SVD:field=HRC48_RDY,bitOffset=30,bitWidth=1,description="Indicate hrc48 is ready" */
        volatile uint32_t HRC48_RDY: 1;  /* Indicate hrc48 is ready */
        /* @SVD:field=HXT48_RDY,bitOffset=31,bitWidth=1,description="Indicate hxt48 is ready" */
        volatile uint32_t HXT48_RDY: 1;  /* Indicate hxt48 is ready */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_ACR_Type;

/* @SVD:register=LSCR,offset=0x0014,size=32,access=read-write,reset=0x0,description="Light Sleep Ctrl Register" */
/* LSCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HRC48_REQ,bitOffset=0,bitWidth=1,description="Request hrc48 in Light Sleep mode" */
        volatile uint32_t HRC48_REQ: 1;  /* Request hrc48 in Light Sleep mode */
        /* @SVD:field=HXT48_REQ,bitOffset=1,bitWidth=1,description="Request hxt48 in Light Sleep mode" */
        volatile uint32_t HXT48_REQ: 1;  /* Request hxt48 in Light Sleep mode */
        /* @SVD:field=PWR_REQ,bitOffset=2,bitWidth=1,description="Request power during Light Sleep mode" */
        volatile uint32_t PWR_REQ: 1;  /* Request power during Light Sleep mode */
        /* @SVD:field=EXTPWR_REQ,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t EXTPWR_REQ: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_LSCR_Type;

/* @SVD:register=DSCR,offset=0x0018,size=32,access=read-write,reset=0x0,description="Deep Sleep Ctrl Register" */
/* DSCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HRC48_REQ,bitOffset=0,bitWidth=1,description="Request hrc48 in Deep Sleep mode" */
        volatile uint32_t HRC48_REQ: 1;  /* Request hrc48 in Deep Sleep mode */
        /* @SVD:field=HXT48_REQ,bitOffset=1,bitWidth=1,description="Request hxt48 in Deep Sleep mode" */
        volatile uint32_t HXT48_REQ: 1;  /* Request hxt48 in Deep Sleep mode */
        /* @SVD:field=PWR_REQ,bitOffset=2,bitWidth=1,description="Request power during Deep Sleep mode" */
        volatile uint32_t PWR_REQ: 1;  /* Request power during Deep Sleep mode */
        /* @SVD:field=EXTPWR_REQ,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t EXTPWR_REQ: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_DSCR_Type;

/* @SVD:register=SBCR,offset=0x001C,size=32,access=read-write,reset=0x0,description="Standby Mode Ctrl Register" */
/* SBCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=HRC48_REQ,bitOffset=0,bitWidth=1,description="Request hrc48 in Standby mode" */
        volatile uint32_t HRC48_REQ: 1;  /* Request hrc48 in Standby mode */
        /* @SVD:field=HXT48_REQ,bitOffset=1,bitWidth=1,description="Request hxt48 in Standby mode" */
        volatile uint32_t HXT48_REQ: 1;  /* Request hxt48 in Standby mode */
        /* @SVD:field=PWR_REQ,bitOffset=2,bitWidth=1,description="Request power during Standby mode" */
        volatile uint32_t PWR_REQ: 1;  /* Request power during Standby mode */
        /* @SVD:field=EXTPWR_REQ,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t EXTPWR_REQ: 1;  /* for debug only */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=PD_RAM0,bitOffset=6,bitWidth=1,description="for debug only" */
        volatile uint32_t PD_RAM0: 1;  /* for debug only */
        /* @SVD:field=PD_RAM1,bitOffset=7,bitWidth=1,description="for debug only" */
        volatile uint32_t PD_RAM1: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_SBCR_Type;

/* @SVD:register=WER,offset=0x0020,size=32,access=read-write,reset=0x0,description="Wakeup Enable register" */
/* WER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RTC,bitOffset=0,bitWidth=1,description="Set 1 to enable RTC as wakeup source" */
        volatile uint32_t RTC: 1;  /* Set 1 to enable RTC as wakeup source */
        /* @SVD:field=GPIO2,bitOffset=1,bitWidth=1,description="Set 1 to enable IO(PB) as wakeup source" */
        volatile uint32_t GPIO2: 1;  /* Set 1 to enable IO(PB) as wakeup source */
        /* @SVD:field=LPTIM3,bitOffset=2,bitWidth=1,description="Set 1 to enable LPTIM3 as wakeup source" */
        volatile uint32_t LPTIM3: 1;  /* Set 1 to enable LPTIM3 as wakeup source */
        /* @SVD:field=RSVD3,bitOffset=3,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=BT,bitOffset=5,bitWidth=1,description="Set 1 to enable BT as wakeup source" */
        volatile uint32_t BT: 1;  /* Set 1 to enable BT as wakeup source */
        /* @SVD:field=HP2LP_REQ,bitOffset=6,bitWidth=1,description="Set 1 to enable HPSYS request as wakeup source" */
        volatile uint32_t HP2LP_REQ: 1;  /* Set 1 to enable HPSYS request as wakeup source */
        /* @SVD:field=HP2LP_IRQ,bitOffset=7,bitWidth=1,description="Set 1 to enable MAILBOX1 as wakeup source" */
        volatile uint32_t HP2LP_IRQ: 1;  /* Set 1 to enable MAILBOX1 as wakeup source */
        /* @SVD:field=PIN0,bitOffset=8,bitWidth=1,description="Set 1 to enable PA24 as wakeup source" */
        volatile uint32_t PIN0: 1;  /* Set 1 to enable PA24 as wakeup source */
        /* @SVD:field=PIN1,bitOffset=9,bitWidth=1,description="Set 1 to enable PA25 as wakeup source" */
        volatile uint32_t PIN1: 1;  /* Set 1 to enable PA25 as wakeup source */
        /* @SVD:field=PIN2,bitOffset=10,bitWidth=1,description="Set 1 to enable PA26 as wakeup source" */
        volatile uint32_t PIN2: 1;  /* Set 1 to enable PA26 as wakeup source */
        /* @SVD:field=PIN3,bitOffset=11,bitWidth=1,description="Set 1 to enable PA27 as wakeup source" */
        volatile uint32_t PIN3: 1;  /* Set 1 to enable PA27 as wakeup source */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=PIN10,bitOffset=18,bitWidth=1,description="Set 1 to enable PA34 as wakeup source" */
        volatile uint32_t PIN10: 1;  /* Set 1 to enable PA34 as wakeup source */
        /* @SVD:field=PIN11,bitOffset=19,bitWidth=1,description="Set 1 to enable PA35 as wakeup source" */
        volatile uint32_t PIN11: 1;  /* Set 1 to enable PA35 as wakeup source */
        /* @SVD:field=PIN12,bitOffset=20,bitWidth=1,description="Set 1 to enable PA36 as wakeup source" */
        volatile uint32_t PIN12: 1;  /* Set 1 to enable PA36 as wakeup source */
        /* @SVD:field=PIN13,bitOffset=21,bitWidth=1,description="Set 1 to enable PA37 as wakeup source" */
        volatile uint32_t PIN13: 1;  /* Set 1 to enable PA37 as wakeup source */
        /* @SVD:field=PIN14,bitOffset=22,bitWidth=1,description="Set 1 to enable PA38 as wakeup source" */
        volatile uint32_t PIN14: 1;  /* Set 1 to enable PA38 as wakeup source */
        /* @SVD:field=PIN15,bitOffset=23,bitWidth=1,description="Set 1 to enable PA39 as wakeup source" */
        volatile uint32_t PIN15: 1;  /* Set 1 to enable PA39 as wakeup source */
        /* @SVD:field=PIN16,bitOffset=24,bitWidth=1,description="Set 1 to enable PA40 as wakeup source" */
        volatile uint32_t PIN16: 1;  /* Set 1 to enable PA40 as wakeup source */
        /* @SVD:field=PIN17,bitOffset=25,bitWidth=1,description="Set 1 to enable PA41 as wakeup source" */
        volatile uint32_t PIN17: 1;  /* Set 1 to enable PA41 as wakeup source */
        /* @SVD:field=PIN18,bitOffset=26,bitWidth=1,description="Set 1 to enable PA42 as wakeup source" */
        volatile uint32_t PIN18: 1;  /* Set 1 to enable PA42 as wakeup source */
        /* @SVD:field=PIN19,bitOffset=27,bitWidth=1,description="Set 1 to enable PA43 as wakeup source" */
        volatile uint32_t PIN19: 1;  /* Set 1 to enable PA43 as wakeup source */
        /* @SVD:field=PIN20,bitOffset=28,bitWidth=1,description="Set 1 to enable PA44 as wakeup source" */
        volatile uint32_t PIN20: 1;  /* Set 1 to enable PA44 as wakeup source */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_WER_Type;

/* @SVD:register=WSR,offset=0x0024,size=32,access=read-write,reset=0x0,description="Wakeup Status register" */
/* WSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RTC,bitOffset=0,bitWidth=1,description="Indicates the wakeup status from RTC. Note: the status is masked by WER" */
        volatile uint32_t RTC: 1;  /* Indicates the wakeup status from RTC. Note: the status is ma */
        /* @SVD:field=GPIO2,bitOffset=1,bitWidth=1,description="Indicates the wakeup status from IO(PB). Note: the status is masked by WER" */
        volatile uint32_t GPIO2: 1;  /* Indicates the wakeup status from IO(PB). Note: the status is */
        /* @SVD:field=LPTIM3,bitOffset=2,bitWidth=1,description="Indicates the wakeup status from LPTIM3. Note: the status is masked by WER" */
        volatile uint32_t LPTIM3: 1;  /* Indicates the wakeup status from LPTIM3. Note: the status is */
        /* @SVD:field=RSVD3,bitOffset=3,bitWidth=2,description="" */
        volatile uint32_t RSVD3: 2;
        /* @SVD:field=BT,bitOffset=5,bitWidth=1,description="Indicates the wakeup status from BT. Note: the status is masked by WER" */
        volatile uint32_t BT: 1;  /* Indicates the wakeup status from BT. Note: the status is mas */
        /* @SVD:field=HP2LP_REQ,bitOffset=6,bitWidth=1,description="Indicates the wakeup status from HPSYS request. Note: the status is masked by WER" */
        volatile uint32_t HP2LP_REQ: 1;  /* Indicates the wakeup status from HPSYS request. Note: the st */
        /* @SVD:field=HP2LP_IRQ,bitOffset=7,bitWidth=1,description="Indicates the wakeup status from MAILBOX1. Note: the status is masked by WER" */
        volatile uint32_t HP2LP_IRQ: 1;  /* Indicates the wakeup status from MAILBOX1. Note: the status  */
        /* @SVD:field=PIN0,bitOffset=8,bitWidth=1,description="Indicates the wakeup status from PA24 request. Note: the status is masked by WER" */
        volatile uint32_t PIN0: 1;  /* Indicates the wakeup status from PA24 request. Note: the sta */
        /* @SVD:field=PIN1,bitOffset=9,bitWidth=1,description="Indicates the wakeup status from PA25 request. Note: the status is masked by WER" */
        volatile uint32_t PIN1: 1;  /* Indicates the wakeup status from PA25 request. Note: the sta */
        /* @SVD:field=PIN2,bitOffset=10,bitWidth=1,description="Indicates the wakeup status from PA26 request. Note: the status is masked by WER" */
        volatile uint32_t PIN2: 1;  /* Indicates the wakeup status from PA26 request. Note: the sta */
        /* @SVD:field=PIN3,bitOffset=11,bitWidth=1,description="Indicates the wakeup status from PA27 request. Note: the status is masked by WER" */
        volatile uint32_t PIN3: 1;  /* Indicates the wakeup status from PA27 request. Note: the sta */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=PIN10,bitOffset=18,bitWidth=1,description="Indicates the wakeup status from PA34 request. Note: the status is masked by WER" */
        volatile uint32_t PIN10: 1;  /* Indicates the wakeup status from PA34 request. Note: the sta */
        /* @SVD:field=PIN11,bitOffset=19,bitWidth=1,description="Indicates the wakeup status from PA35 request. Note: the status is masked by WER" */
        volatile uint32_t PIN11: 1;  /* Indicates the wakeup status from PA35 request. Note: the sta */
        /* @SVD:field=PIN12,bitOffset=20,bitWidth=1,description="Indicates the wakeup status from PA36 request. Note: the status is masked by WER" */
        volatile uint32_t PIN12: 1;  /* Indicates the wakeup status from PA36 request. Note: the sta */
        /* @SVD:field=PIN13,bitOffset=21,bitWidth=1,description="Indicates the wakeup status from PA37 request. Note: the status is masked by WER" */
        volatile uint32_t PIN13: 1;  /* Indicates the wakeup status from PA37 request. Note: the sta */
        /* @SVD:field=PIN14,bitOffset=22,bitWidth=1,description="Indicates the wakeup status from PA38 request. Note: the status is masked by WER" */
        volatile uint32_t PIN14: 1;  /* Indicates the wakeup status from PA38 request. Note: the sta */
        /* @SVD:field=PIN15,bitOffset=23,bitWidth=1,description="Indicates the wakeup status from PA39 request. Note: the status is masked by WER" */
        volatile uint32_t PIN15: 1;  /* Indicates the wakeup status from PA39 request. Note: the sta */
        /* @SVD:field=PIN16,bitOffset=24,bitWidth=1,description="Indicates the wakeup status from PA40 request. Note: the status is masked by WER" */
        volatile uint32_t PIN16: 1;  /* Indicates the wakeup status from PA40 request. Note: the sta */
        /* @SVD:field=PIN17,bitOffset=25,bitWidth=1,description="Indicates the wakeup status from PA41 request. Note: the status is masked by WER" */
        volatile uint32_t PIN17: 1;  /* Indicates the wakeup status from PA41 request. Note: the sta */
        /* @SVD:field=PIN18,bitOffset=26,bitWidth=1,description="Indicates the wakeup status from PA42 request. Note: the status is masked by WER" */
        volatile uint32_t PIN18: 1;  /* Indicates the wakeup status from PA42 request. Note: the sta */
        /* @SVD:field=PIN19,bitOffset=27,bitWidth=1,description="Indicates the wakeup status from PA43 request. Note: the status is masked by WER" */
        volatile uint32_t PIN19: 1;  /* Indicates the wakeup status from PA43 request. Note: the sta */
        /* @SVD:field=PIN20,bitOffset=28,bitWidth=1,description="Indicates the wakeup status from PA44 request. Note: the status is masked by WER" */
        volatile uint32_t PIN20: 1;  /* Indicates the wakeup status from PA44 request. Note: the sta */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=3,description="" */
        volatile uint32_t RSVD: 3;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_WSR_Type;

/* @SVD:register=WCR,offset=0x0028,size=32,access=read-write,reset=0x0,description="Wakeup Clear register" */
/* WCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD3,bitOffset=0,bitWidth=8,description="Note: for RTC/GPIO/LPTIM/BT, clear the wakeup status directly in the corresponding module" */
        volatile uint32_t RSVD3: 8;  /* Note: for RTC/GPIO/LPTIM/BT, clear the wakeup status directl */
        /* @SVD:field=PIN0,bitOffset=8,bitWidth=1,description="Write 1 to clear PA24 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN0: 1;  /* Write 1 to clear PA24 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN1,bitOffset=9,bitWidth=1,description="Write 1 to clear PA25 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN1: 1;  /* Write 1 to clear PA25 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN2,bitOffset=10,bitWidth=1,description="Write 1 to clear PA26 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN2: 1;  /* Write 1 to clear PA26 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN3,bitOffset=11,bitWidth=1,description="Write 1 to clear PA27 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN3: 1;  /* Write 1 to clear PA27 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=RSVD2,bitOffset=12,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=PIN10,bitOffset=18,bitWidth=1,description="Write 1 to clear PA34 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN10: 1;  /* Write 1 to clear PA34 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN11,bitOffset=19,bitWidth=1,description="Write 1 to clear PA35 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN11: 1;  /* Write 1 to clear PA35 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN12,bitOffset=20,bitWidth=1,description="Write 1 to clear PA36 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN12: 1;  /* Write 1 to clear PA36 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN13,bitOffset=21,bitWidth=1,description="Write 1 to clear PA37 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN13: 1;  /* Write 1 to clear PA37 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN14,bitOffset=22,bitWidth=1,description="Write 1 to clear PA38 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN14: 1;  /* Write 1 to clear PA38 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN15,bitOffset=23,bitWidth=1,description="Write 1 to clear PA39 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN15: 1;  /* Write 1 to clear PA39 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN16,bitOffset=24,bitWidth=1,description="Write 1 to clear PA40 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN16: 1;  /* Write 1 to clear PA40 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN17,bitOffset=25,bitWidth=1,description="Write 1 to clear PA41 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN17: 1;  /* Write 1 to clear PA41 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN18,bitOffset=26,bitWidth=1,description="Write 1 to clear PA42 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN18: 1;  /* Write 1 to clear PA42 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN19,bitOffset=27,bitWidth=1,description="Write 1 to clear PA43 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN19: 1;  /* Write 1 to clear PA43 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=PIN20,bitOffset=28,bitWidth=1,description="Write 1 to clear PA44 wakeup source. Only valid if PIN wakeup is configured as edge trigger" */
        volatile uint32_t PIN20: 1;  /* Write 1 to clear PA44 wakeup source. Only valid if PIN wakeu */
        /* @SVD:field=RSVD,bitOffset=29,bitWidth=2,description="" */
        volatile uint32_t RSVD: 2;
        /* @SVD:field=AON,bitOffset=31,bitWidth=1,description="Write 1 to clear the AON wakeup IRQ status" */
        volatile uint32_t AON: 1;  /* Write 1 to clear the AON wakeup IRQ status */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_WCR_Type;

/* @SVD:register=ISSR,offset=0x002C,size=32,access=read-write,reset=0x0,description="Inter System Status Register" */
/* ISSR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=LP2HP_REQ,bitOffset=0,bitWidth=1,description="write 1 to request HPSYS to stay in active mode" */
        volatile uint32_t LP2HP_REQ: 1;  /* write 1 to request HPSYS to stay in active mode */
        /* @SVD:field=HP2LP_REQ,bitOffset=1,bitWidth=1,description="indicate HPSYS request exists" */
        volatile uint32_t HP2LP_REQ: 1;  /* indicate HPSYS request exists */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=LP_ACTIVE,bitOffset=4,bitWidth=1,description="write 1 to indicates LPSYS is active" */
        volatile uint32_t LP_ACTIVE: 1;  /* write 1 to indicates LPSYS is active */
        /* @SVD:field=HP_ACTIVE,bitOffset=5,bitWidth=1,description="read 1 indicates HPSYS is active" */
        volatile uint32_t HP_ACTIVE: 1;  /* read 1 indicates HPSYS is active */
        /* @SVD:field=RSVD,bitOffset=6,bitWidth=26,description="" */
        volatile uint32_t RSVD: 26;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_ISSR_Type;

/* @SVD:register=TARGET,offset=0x0030,size=32,access=read-write,reset=0x0,description="BT sleep time target" */
/* TARGET 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLEEP_TARGET,bitOffset=0,bitWidth=28,description="bt sleep time target in cycles of clk_rtc" */
        volatile uint32_t SLEEP_TARGET: 28;  /* bt sleep time target in cycles of clk_rtc */
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_TARGET_Type;

/* @SVD:register=ACTUAL,offset=0x0034,size=32,access=read-write,reset=0x0,description="BT actual sleep time" */
/* ACTUAL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLEEP_CNT,bitOffset=0,bitWidth=28,description="bt actual sleep time in cycles of clk_rtc. If not woken up by software or external interrupt, sleep_cnt counts up every clk_rtc cycle, until reaches sleep_target" */
        volatile uint32_t SLEEP_CNT: 28;  /* bt actual sleep time in cycles of clk_rtc. If not woken up b */
        /* @SVD:field=RSVD,bitOffset=28,bitWidth=4,description="" */
        volatile uint32_t RSVD: 4;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_ACTUAL_Type;

/* @SVD:register=PRE_WKUP,offset=0x0038,size=32,access=read-write,reset=0x0,description="time before bt awake" */
/* PRE_WKUP 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=XTAL_TIME,bitOffset=0,bitWidth=10,description="cycles of clk_rtc for hxt48 ready before bt awake." */
        volatile uint32_t XTAL_TIME: 10;  /* cycles of clk_rtc for hxt48 ready before bt awake. */
        /* @SVD:field=RSVD2,bitOffset=10,bitWidth=6,description="" */
        volatile uint32_t RSVD2: 6;
        /* @SVD:field=WKUP_TIME,bitOffset=16,bitWidth=10,description="cycles of clk_rtc for LPSYS ready before bt awake." */
        volatile uint32_t WKUP_TIME: 10;  /* cycles of clk_rtc for LPSYS ready before bt awake. */
        /* @SVD:field=RSVD,bitOffset=26,bitWidth=6,description="" */
        volatile uint32_t RSVD: 6;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_PRE_WKUP_Type;

/* @SVD:register=SLP_CFG,offset=0x003C,size=32,access=read-write,reset=0x0,description="BT sleep configuration" */
/* SLP_CFG 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RSVD2,bitOffset=0,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=XTAL_ALWAYS_ON,bitOffset=2,bitWidth=1,description="for debug only" */
        volatile uint32_t XTAL_ALWAYS_ON: 1;  /* for debug only */
        /* @SVD:field=XTAL_FORCE_OFF,bitOffset=3,bitWidth=1,description="for debug only" */
        volatile uint32_t XTAL_FORCE_OFF: 1;  /* for debug only */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_SLP_CFG_Type;

/* @SVD:register=SLP_CTRL,offset=0x0040,size=32,access=read-write,reset=0x0,description="BT sleep control" */
/* SLP_CTRL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SLEEP_REQ,bitOffset=0,bitWidth=1,description="bt sleep request. Will be cleared automatically" */
        volatile uint32_t SLEEP_REQ: 1;  /* bt sleep request. Will be cleared automatically */
        /* @SVD:field=WKUP_REQ,bitOffset=1,bitWidth=1,description="software request to wakeup bt. Will be cleared automatically" */
        volatile uint32_t WKUP_REQ: 1;  /* software request to wakeup bt. Will be cleared automatically */
        /* @SVD:field=RSVD2,bitOffset=2,bitWidth=2,description="" */
        volatile uint32_t RSVD2: 2;
        /* @SVD:field=SLEEP_STATUS,bitOffset=4,bitWidth=1,description="bt sleep status. 1 means bt is sleeping and sleep_cnt is counting up" */
        volatile uint32_t SLEEP_STATUS: 1;  /* bt sleep status. 1 means bt is sleeping and sleep_cnt is cou */
        /* @SVD:field=XTAL_REQ,bitOffset=5,bitWidth=1,description="xtal request status. 1 means bt is requiring xtal." */
        volatile uint32_t XTAL_REQ: 1;  /* xtal request status. 1 means bt is requiring xtal. */
        /* @SVD:field=BT_WKUP,bitOffset=6,bitWidth=1,description="bt wakeup source. 1 means bt has not enter sleep or has enter wakeup procedure" */
        volatile uint32_t BT_WKUP: 1;  /* bt wakeup source. 1 means bt has not enter sleep or has ente */
        /* @SVD:field=RSVD,bitOffset=7,bitWidth=25,description="" */
        volatile uint32_t RSVD: 25;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_SLP_CTRL_Type;

/* @SVD:register=ANACR,offset=0x0044,size=32,access=read-write,reset=0x0,description="Analog Control Register" */
/* ANACR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PB_ISO,bitOffset=0,bitWidth=1,description="Set 1 to force IO(PB) into retention mode" */
        volatile uint32_t PB_ISO: 1;  /* Set 1 to force IO(PB) into retention mode */
        /* @SVD:field=VLP_ISO,bitOffset=1,bitWidth=1,description="Set 1 to force off all LPSYS related analog modules" */
        volatile uint32_t VLP_ISO: 1;  /* Set 1 to force off all LPSYS related analog modules */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_ANACR_Type;

/* @SVD:register=GTIMR,offset=0x0048,size=32,access=read-write,reset=0x0,description="Global Timer Register" */
/* GTIMR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=32,description="Global timer value" */
        volatile uint32_t CNT: 32;  /* Global timer value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_GTIMR_Type;

/* @SVD:register=RESERVE0,offset=0x004C,size=32,access=read-write,reset=0x0,description="Reserved Register 0" */
/* RESERVE0 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="for debug only" */
        volatile uint32_t DATA: 32;  /* for debug only */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_RESERVE0_Type;

/* @SVD:register=RESERVE1,offset=0x0050,size=32,access=read-write,reset=0x0,description="Reserved Register 1" */
/* RESERVE1 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DATA,bitOffset=0,bitWidth=32,description="for debug only" */
        volatile uint32_t DATA: 32;  /* for debug only */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_RESERVE1_Type;

/* @SVD:register=SPR,offset=0x0100,size=32,access=read-write,reset=0x0,description="Stack Pointer Register" */
/* SPR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=SP,bitOffset=0,bitWidth=32,description="LCPU stack pointer address" */
        volatile uint32_t SP: 32;  /* LCPU stack pointer address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_SPR_Type;

/* @SVD:register=PCR,offset=0x0104,size=32,access=read-write,reset=0x0,description="Pointer Counter Register" */
/* PCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=PC,bitOffset=0,bitWidth=32,description="LCPU PC pointer address" */
        volatile uint32_t PC: 32;  /* LCPU PC pointer address */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPSYS_AON_PCR_Type;

/* LPSYS_AON 寄存器结构体 */
typedef struct
{
    LPSYS_AON_PMR_Type PMR;  /* 0x0000 - Power Mode Register */
    LPSYS_AON_CR1_Type CR1;  /* 0x0004 - Control Register 1 */
    LPSYS_AON_CR2_Type CR2;  /* 0x0008 - Control Register 2 */
    LPSYS_AON_CR3_Type CR3;  /* 0x000C - Control Register 3 */
    LPSYS_AON_ACR_Type ACR;  /* 0x0010 - Active Mode Control register */
    LPSYS_AON_LSCR_Type LSCR;  /* 0x0014 - Light Sleep Ctrl Register */
    LPSYS_AON_DSCR_Type DSCR;  /* 0x0018 - Deep Sleep Ctrl Register */
    LPSYS_AON_SBCR_Type SBCR;  /* 0x001C - Standby Mode Ctrl Register */
    LPSYS_AON_WER_Type WER;  /* 0x0020 - Wakeup Enable register */
    LPSYS_AON_WSR_Type WSR;  /* 0x0024 - Wakeup Status register */
    LPSYS_AON_WCR_Type WCR;  /* 0x0028 - Wakeup Clear register */
    LPSYS_AON_ISSR_Type ISSR;  /* 0x002C - Inter System Status Register */
    LPSYS_AON_TARGET_Type TARGET;  /* 0x0030 - BT sleep time target */
    LPSYS_AON_ACTUAL_Type ACTUAL;  /* 0x0034 - BT actual sleep time */
    LPSYS_AON_PRE_WKUP_Type PRE_WKUP;  /* 0x0038 - time before bt awake */
    LPSYS_AON_SLP_CFG_Type SLP_CFG;  /* 0x003C - BT sleep configuration */
    LPSYS_AON_SLP_CTRL_Type SLP_CTRL;  /* 0x0040 - BT sleep control */
    LPSYS_AON_ANACR_Type ANACR;  /* 0x0044 - Analog Control Register */
    LPSYS_AON_GTIMR_Type GTIMR;  /* 0x0048 - Global Timer Register */
    LPSYS_AON_RESERVE0_Type RESERVE0;  /* 0x004C - Reserved Register 0 */
    LPSYS_AON_RESERVE1_Type RESERVE1;  /* 0x0050 - Reserved Register 1 */
    /* @SVD:register=RSVD1,offset=0x0054,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x0054 - RSVD1 */
    volatile uint32_t RESERVED22[42];  /* 0x0058 - 0x00FF */
    LPSYS_AON_SPR_Type SPR;  /* 0x0100 - Stack Pointer Register */
    LPSYS_AON_PCR_Type PCR;  /* 0x0104 - Pointer Counter Register */
} LPSYS_AON_TypeDef;

/* ============================================================================ */
/* LPTIM3 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=LPTIM3,base=0x40042000,description="",groupName=LPTIM */

#define LPTIM3_BASE                                        (0x40042000UL)

/* @SVD:register=ISR,offset=0x0000,size=32,access=read-write,reset=0x00000000,description="LPTIM interrupt and status register" */
/* ISR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UE,bitOffset=0,bitWidth=1,description="LPTIM update event occurred\n              UE is set by hardware to inform application that an update event was generated when overflow occurred while repetition counter reached zero. UE flag can be cleared by writing 1 to the UECLR bit in the LPTIM_ICR register." */
        volatile uint32_t UE: 1;  /* LPTIM update event occurred               UE is set by hardw */
        /* @SVD:field=OF,bitOffset=1,bitWidth=1,description="Overflow occurred\n              OF is set by hardware to inform application that LPTIM_CNT register’s value reached the LPTIM_ARR register’s value and count from zero again. OF flag can be cleared by writing 1 to the OFCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OF: 1;  /* Overflow occurred               OF is set by hardware to inf */
        /* @SVD:field=OC,bitOffset=2,bitWidth=1,description="Output compare match\n              The OC bit is set by hardware to inform application that LPTIM_CNT register value reached the LPTIM_CMP register’s value. OC flag can be cleared by writing 1 to the OCCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OC: 1;  /* Output compare match               The OC bit is set by hard */
        /* @SVD:field=ET,bitOffset=3,bitWidth=1,description="External trigger edge event\n              ET is set by hardware to inform application that a valid edge on the selected external trigger input has occurred. If the trigger is ignored because the timer has already started, then this flag is not set. ET flag can be cleared by writing 1 to the ETCLR bit in the LPTIM_ICR register." */
        volatile uint32_t ET: 1;  /* External trigger edge event               ET is set by hardw */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=UEWKUP,bitOffset=8,bitWidth=1,description="Indicates update event wakeup occurred\n              UEWKUP is set by hardware when an update event was generated (overflow occurred while repetition counter reached zero). To clear UEWKUP, first  write 0 to the UEWE bit in the LPTIM_IER register to disable, then write 1 to the WKUPCLR bit in the LPTIM_ICR register." */
        volatile uint32_t UEWKUP: 1;  /* Indicates update event wakeup occurred               UEWKUP  */
        /* @SVD:field=OFWKUP,bitOffset=9,bitWidth=1,description="Indicates overflow wakeup occurred\n              OFWKUP is set by hardware when LPTIM_CNT register’s value reached the LPTIM_ARR register’s value and count from zero again. To clear OFWKUP, first  write 0 to the OFWE bit in the LPTIM_IER register to disable, then write 1 to the WKUPCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OFWKUP: 1;  /* Indicates overflow wakeup occurred               OFWKUP is s */
        /* @SVD:field=OCWKUP,bitOffset=10,bitWidth=1,description="Indicates output compare wakeup occurred\n              The OCWKUP bit is set by hardware when LPTIM_CNT register value reached the LPTIM_CMP register’s value. To clear OCWKUP, first  write 0 to the OCWE bit in the LPTIM_IER register to disable, then write 1 to the WKUPCLR bit in the LPTIM_ICR register." */
        volatile uint32_t OCWKUP: 1;  /* Indicates output compare wakeup occurred               The O */
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM3_ISR_Type;

/* @SVD:register=ICR,offset=0x0004,size=32,access=read-write,reset=0x00000000,description="LPTIM interrupt and status clear register" */
/* ICR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UECLR,bitOffset=0,bitWidth=1,description="Update event clear flag\n              Writing 1 to this bit clear the UE flag in the LPTIM_ISR register." */
        volatile uint32_t UECLR: 1;  /* Update event clear flag               Writing 1 to this bit  */
        /* @SVD:field=OFCLR,bitOffset=1,bitWidth=1,description="Overflow clear flag\n              Writing 1 to this bit clears the OF flag in the LPTIM_ISR register" */
        volatile uint32_t OFCLR: 1;  /* Overflow clear flag               Writing 1 to this bit clea */
        /* @SVD:field=OCCLR,bitOffset=2,bitWidth=1,description="Output compare clear flag\n              Writing 1 to this bit clears the OC flag in the LPTIM_ISR register" */
        volatile uint32_t OCCLR: 1;  /* Output compare clear flag               Writing 1 to this bi */
        /* @SVD:field=ETCLR,bitOffset=3,bitWidth=1,description="External trigger valid edge clear flag\n              Writing 1 to this bit clears the ET flag in the LPTIM_ISR register" */
        volatile uint32_t ETCLR: 1;  /* External trigger valid edge clear flag               Writing */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=WKUPCLR,bitOffset=8,bitWidth=1,description="wakeup status clear flag\n              Writing 1 to this bit clears all wakeup status flags in the LPTIM_ISR register." */
        volatile uint32_t WKUPCLR: 1;  /* wakeup status clear flag               Writing 1 to this bit */
        /* @SVD:field=RSVD,bitOffset=9,bitWidth=23,description="" */
        volatile uint32_t RSVD: 23;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM3_ICR_Type;

/* @SVD:register=IER,offset=0x0008,size=32,access=read-write,reset=0x00000000,description="LPTIM interrupt and wakeup enable register" */
/* IER 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=UEIE,bitOffset=0,bitWidth=1,description="Update event interrupt enable\n              0: Update event interrupt disabled\n              1: Update event interrupt enabled" */
        volatile uint32_t UEIE: 1;  /* Update event interrupt enable               0: Update event  */
        /* @SVD:field=OFIE,bitOffset=1,bitWidth=1,description="Overflow Interrupt Enable\n              0: Overflow interrupt disabled\n              1: Overflow interrupt enabled" */
        volatile uint32_t OFIE: 1;  /* Overflow Interrupt Enable               0: Overflow interrup */
        /* @SVD:field=OCIE,bitOffset=2,bitWidth=1,description="Output compare Interrupt Enable\n              0: Output compare interrupt disabled\n              1: Output compare interrupt enabled" */
        volatile uint32_t OCIE: 1;  /* Output compare Interrupt Enable               0: Output comp */
        /* @SVD:field=ETIE,bitOffset=3,bitWidth=1,description="External trigger valid edge Interrupt Enable\n              0: External trigger interrupt disabled\n              1: External trigger interrupt enabled" */
        volatile uint32_t ETIE: 1;  /* External trigger valid edge Interrupt Enable               0 */
        /* @SVD:field=RSVD2,bitOffset=4,bitWidth=4,description="" */
        volatile uint32_t RSVD2: 4;
        /* @SVD:field=UEWE,bitOffset=8,bitWidth=1,description="Update event Wakeup enable\n              0: Update event Wakeup disabled\n              1: Update event Wakeup enabled" */
        volatile uint32_t UEWE: 1;  /* Update event Wakeup enable               0: Update event Wak */
        /* @SVD:field=OFWE,bitOffset=9,bitWidth=1,description="Overflow Wakeup Enable\n              0: Overflow Wakeup disabled\n              1: Overflow Wakeup enabled" */
        volatile uint32_t OFWE: 1;  /* Overflow Wakeup Enable               0: Overflow Wakeup disa */
        /* @SVD:field=OCWE,bitOffset=10,bitWidth=1,description="Output compare Wakeup Enable\n              0: Output compare wakeup disabled\n              1: Output compare wakeup enabled" */
        volatile uint32_t OCWE: 1;  /* Output compare Wakeup Enable               0: Output compare */
        /* @SVD:field=RSVD,bitOffset=11,bitWidth=21,description="" */
        volatile uint32_t RSVD: 21;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM3_IER_Type;

/* @SVD:register=CFGR,offset=0x000C,size=32,access=read-write,reset=0x00000000,description="LPTIM configuration register" */
/* CFGR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CKSEL,bitOffset=0,bitWidth=1,description="Clock selector\n              The CKSEL bit selects which clock source the LPTIM will use:\n              0: LPTIM is clocked by internal clock source, according to INTCKSEL\n              1: LPTIM is clocked by external clock source, according to EXTCKSEL" */
        volatile uint32_t CKSEL: 1;  /* Clock selector               The CKSEL bit selects which clo */
        /* @SVD:field=CKPOL,bitOffset=1,bitWidth=2,description="Clock Polarity\n              If LPTIM is clocked by an external clock source, CKPOL bits is used to configure the active edge or edges used by the counter:\n              00: the rising edge is the active edge used for counting\n              01: the falling edge is the active edge used for counting\n              10: both edges are active edges. When both external clock signal edges are considered active ones, the LPTIM must also be clocked by an internal clock source with a frequency equal to at least four time the external clock frequency.\n              11: not allowed" */
        volatile uint32_t CKPOL: 2;  /* Clock Polarity               If LPTIM is clocked by an exter */
        /* @SVD:field=CKFLT,bitOffset=3,bitWidth=2,description="Configurable digital filter for external clock\n              The CKFLT value sets the number of consecutive equal samples that should be detected when a level change occurs on an external clock signal before it is considered as a valid level transition. An internal clock source must be present to use this feature\n              00: any external clock signal level change is considered as a valid transition\n              01: external clock signal level change must be stable for at least 2 clock periods before it is considered as valid transition.\n              10: external clock signal level change must be stable for at least 4 clock periods before it is considered as valid transition.\n              11: external clock signal level change must be stable for at least 8 clock periods before it is considered as valid transition." */
        volatile uint32_t CKFLT: 2;  /* Configurable digital filter for external clock               */
        /* @SVD:field=INTCKSEL,bitOffset=5,bitWidth=1,description="Internal clock source selector\n              0: internal clock source is clk_lp\n              1: internal clock source is pclk2" */
        volatile uint32_t INTCKSEL: 1;  /* Internal clock source selector               0: internal clo */
        /* @SVD:field=TRGFLT,bitOffset=6,bitWidth=2,description="Configurable digital filter for trigger\n              The TRGFLT value sets the number of consecutive equal samples that should be detected when a level change occurs on an internal trigger before it is considered as a valid level transition. An internal clock source must be present to use this feature\n              00: any trigger active level change is considered as a valid trigger\n              01: trigger active level change must be stable for at least 2 clock periods before it is considered as valid trigger.\n              10: trigger active level change must be stable for at least 4 clock periods before it is considered as valid trigger.\n              11: trigger active level change must be stable for at least 8 clock periods before it is considered as valid trigger." */
        volatile uint32_t TRGFLT: 2;  /* Configurable digital filter for trigger               The TR */
        /* @SVD:field=EXTCKSEL,bitOffset=8,bitWidth=1,description="External clock source selector\n              0: external clock source is from lptim_in\n              1: external clock source is from LPCOMP (if LPCOMP integrated)" */
        volatile uint32_t EXTCKSEL: 1;  /* External clock source selector               0: external clo */
        /* @SVD:field=PRESC,bitOffset=9,bitWidth=3,description="Clock prescaler\n              The PRESC bits configure the prescaler division factor. It can be one among the following division factors:\n              000: /1\n              001: /2\n              010: /4\n              011: /8\n              100: /16\n              101: /32\n              110: /64\n              111: /128" */
        volatile uint32_t PRESC: 3;  /* Clock prescaler               The PRESC bits configure the p */
        /* @SVD:field=RSVD4,bitOffset=12,bitWidth=1,description="" */
        volatile uint32_t RSVD4: 1;
        /* @SVD:field=TRIGSEL,bitOffset=13,bitWidth=3,description="Trigger selector\n              The TRIGSEL bits select the trigger source that will serve as a trigger event for the LPTIM among the below 8 available sources:\n              000: lptim_ext0\n              001: lptim_ext1\n              010: lptim_ext2\n              011: lptim_ext3\n              100: lptim_ext4\n              101: lptim_ext5\n              110: lptim_ext6\n              111: lptim_ext7" */
        volatile uint32_t TRIGSEL: 3;  /* Trigger selector               The TRIGSEL bits select the t */
        /* @SVD:field=RSVD3,bitOffset=16,bitWidth=1,description="" */
        volatile uint32_t RSVD3: 1;
        /* @SVD:field=TRIGEN,bitOffset=17,bitWidth=2,description="Trigger enable and polarity\n              The TRIGEN bits controls whether the LPTIM counter is started by an external trigger or not. If the external trigger option is selected, three configurations are possible for the trigger active edge:\n              00: software trigger (counting start is initiated by software)\n              01: rising edge is the active edge\n              10: falling edge is the active edge\n              11: both edges are active edges" */
        volatile uint32_t TRIGEN: 2;  /* Trigger enable and polarity               The TRIGEN bits co */
        /* @SVD:field=TIMOUT,bitOffset=19,bitWidth=1,description="Timeout enable\n              The TIMOUT bit controls the Timeout feature\n              0: A trigger event arriving when the timer is already started will be ignored\n              1: A trigger event arriving when the timer is already started will reset and restart the LPTIM counter and the repetition counter" */
        volatile uint32_t TIMOUT: 1;  /* Timeout enable               The TIMOUT bit controls the Tim */
        /* @SVD:field=WAVE,bitOffset=20,bitWidth=1,description="Waveform shape\n              The WAVE bit controls the output shape\n              0: Deactivate Set-once mode\n              1: Activate the Set-once mode" */
        volatile uint32_t WAVE: 1;  /* Waveform shape               The WAVE bit controls the outpu */
        /* @SVD:field=WAVPOL,bitOffset=21,bitWidth=1,description="Waveform shape polarity\n              The WAVEPOL bit controls the output polarity\n              0: The LPTIM output reflects the compare results between LPTIM_ARR and LPTIM_CMP registers\n              1: The LPTIM output reflects the inverse of the compare results between LPTIM_ARR and LPTIM_CMP registers" */
        volatile uint32_t WAVPOL: 1;  /* Waveform shape polarity               The WAVEPOL bit contro */
        /* @SVD:field=RSVD2,bitOffset=22,bitWidth=1,description="" */
        volatile uint32_t RSVD2: 1;
        /* @SVD:field=COUNTMODE,bitOffset=23,bitWidth=1,description="counter mode in internal clock source mode (CKSEL=0). If CKSEL=1, this bit has no effect.\n              0: the counter is incremented following each internal clock pulse\n              1: the counter is incremented following each valid pulse on the external clock" */
        volatile uint32_t COUNTMODE: 1;  /* counter mode in internal clock source mode (CKSEL=0). If CKS */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM3_CFGR_Type;

/* @SVD:register=CR,offset=0x0010,size=32,access=read-write,reset=0x00000000,description="LPTIM control register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ENABLE,bitOffset=0,bitWidth=1,description="LPTIM enable\n              The ENABLE bit is set and cleared by software. \n              0:LPTIM is disabled\n              1:LPTIM is enabled" */
        volatile uint32_t ENABLE: 1;  /* LPTIM enable               The ENABLE bit is set and cleared */
        /* @SVD:field=SNGSTRT,bitOffset=1,bitWidth=1,description="LPTIM start in Single mode\n              This bit is set by software and cleared by hardware.\n              In case of software start (TRIGEN[1:0] = 00), setting this bit starts the LPTIM in single pulse mode.\n              If the software start is disabled (TRIGEN[1:0] different than 00), setting this bit starts the LPTIM in single pulse mode as soon as an external trigger is detected.\n              If this bit is set when the LPTIM is in continuous counting mode, then the LPTIM will stop at the following match between ARR and CNT registers.\n              If this bit is set simultaneously with CNTSTRT, then LPTIM will be in continuous counting mode." */
        volatile uint32_t SNGSTRT: 1;  /* LPTIM start in Single mode               This bit is set by  */
        /* @SVD:field=CNTSTRT,bitOffset=2,bitWidth=1,description="Timer start in Continuous mode\n              This bit is set by software and cleared by hardware.\n              In case of software start (TRIGEN[1:0] = 00), setting this bit starts the LPTIM in Continuous mode.\n              If the software start is disabled (TRIGEN[1:0] different than 00), setting this bit starts the timer in Continuous mode as soon as an external trigger is detected.\n              If this bit is set when a single pulse mode counting is ongoing, then the timer will not stop at the next match between ARR and CNT registers and the LPTIM counter keeps counting in Continuous mode." */
        volatile uint32_t CNTSTRT: 1;  /* Timer start in Continuous mode               This bit is set */
        /* @SVD:field=COUNTRST,bitOffset=3,bitWidth=1,description="Counter reset\n              This bit is set by software and cleared by hardware. When set to 1 this bit will trigger a synchronous reset of the CNT register. Due to the synchronous nature of this reset, it only takes place after a synchronization delay.\n              COUNTRST must never be set to 1 by software before it is already cleared to 0 by hardware. Software should consequently check that COUNTRST bit is already cleared to 0 before attempting to set it to 1." */
        volatile uint32_t COUNTRST: 1;  /* Counter reset               This bit is set by software and  */
        /* @SVD:field=RSVD,bitOffset=4,bitWidth=28,description="" */
        volatile uint32_t RSVD: 28;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM3_CR_Type;

/* @SVD:register=CMP,offset=0x0014,size=32,access=read-write,reset=0x00000000,description="LPTIM compare register" */
/* CMP 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CMP,bitOffset=0,bitWidth=24,description="Compare value\n              CMP is the compare value used by the LPTIM." */
        volatile uint32_t CMP: 24;  /* Compare value               CMP is the compare value used by */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM3_CMP_Type;

/* @SVD:register=ARR,offset=0x0018,size=32,access=read-write,reset=0x00000000,description="LPTIM autoreload register" */
/* ARR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=ARR,bitOffset=0,bitWidth=24,description="Auto reload value\n              ARR is the autoreload value for the LPTIM. This value must be strictly greater than the CMP[15:0] value." */
        volatile uint32_t ARR: 24;  /* Auto reload value               ARR is the autoreload value  */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM3_ARR_Type;

/* @SVD:register=CNT,offset=0x001C,size=32,access=read-write,reset=0x00000000,description="LPTIM counter register" */
/* CNT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=CNT,bitOffset=0,bitWidth=24,description="Counter value\n              When the LPTIM is running with an asynchronous clock, reading the CNT register may return unreliable values. So in this case it is necessary to perform two consecutive read accesses and verify that the two returned values are identical." */
        volatile uint32_t CNT: 24;  /* Counter value               When the LPTIM is running with a */
        /* @SVD:field=RSVD,bitOffset=24,bitWidth=8,description="" */
        volatile uint32_t RSVD: 8;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM3_CNT_Type;

/* @SVD:register=RCR,offset=0x0020,size=32,access=read-write,reset=0x00000000,description="LPTIM repetition register" */
/* RCR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=REP,bitOffset=0,bitWidth=8,description="Repetition register value\n              REP is the repetition value for the LPTIM.\n              Read REP will return left repetition times. It should be noted that for a reliable REP register read access, two consecutive read accesses must be performed and compared. A read access can be considered reliable when the values of the two consecutive read accesses are equal." */
        volatile uint32_t REP: 8;  /* Repetition register value               REP is the repetitio */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} LPTIM3_RCR_Type;

/* LPTIM3 寄存器结构体 */
typedef struct
{
    LPTIM3_ISR_Type ISR;  /* 0x0000 - LPTIM interrupt and status register */
    LPTIM3_ICR_Type ICR;  /* 0x0004 - LPTIM interrupt and status clear registe */
    LPTIM3_IER_Type IER;  /* 0x0008 - LPTIM interrupt and wakeup enable regist */
    LPTIM3_CFGR_Type CFGR;  /* 0x000C - LPTIM configuration register */
    LPTIM3_CR_Type CR;  /* 0x0010 - LPTIM control register */
    LPTIM3_CMP_Type CMP;  /* 0x0014 - LPTIM compare register */
    LPTIM3_ARR_Type ARR;  /* 0x0018 - LPTIM autoreload register */
    LPTIM3_CNT_Type CNT;  /* 0x001C - LPTIM counter register */
    LPTIM3_RCR_Type RCR;  /* 0x0020 - LPTIM repetition register */
} LPTIM3_TypeDef;

/* ============================================================================ */
/* CRC2 外设定义 */
/* ============================================================================ */
/* @SVD:peripheral=CRC2,base=0x40085000,description="",groupName=CRC */

#define CRC2_BASE                                          (0x40085000UL)

/* @SVD:register=DR,offset=0x0000,size=32,access=read-write,reset=0x0,description="Data register" */
/* DR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DR,bitOffset=0,bitWidth=32,description="Data register bits.\n              This register is used to write new data to the CRC calculator.\n              It holds the previous CRC calculation result when it is read.\n              If the data size is less than 32 bits, the least significant bits are used to write/read the correct value." */
        volatile uint32_t DR: 32;  /* Data register bits.               This register is used to w */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC2_DR_Type;

/* @SVD:register=SR,offset=0x0004,size=32,access=read-write,reset=0x0,description="Status register" */
/* SR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=DONE,bitOffset=0,bitWidth=1,description="Done flag. When DR written, done flag will be cleared automatically. The flag will assert after CRC operation of current DR finished." */
        volatile uint32_t DONE: 1;  /* Done flag. When DR written, done flag will be cleared automa */
        /* @SVD:field=OVERFLOW,bitOffset=1,bitWidth=1,description="Overflow when new data arrive while last calculation not done yet" */
        volatile uint32_t OVERFLOW: 1;  /* Overflow when new data arrive while last calculation not don */
        /* @SVD:field=RSVD,bitOffset=2,bitWidth=30,description="" */
        volatile uint32_t RSVD: 30;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC2_SR_Type;

/* @SVD:register=CR,offset=0x0008,size=32,access=read-write,reset=0x0,description="Control register" */
/* CR 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=RESET,bitOffset=0,bitWidth=1,description="This bit is set by software to reset the CRC calculation unit and set the data register to the value stored in the CRC_INIT register. This bit can only be set, it is automatically cleared by hardware" */
        volatile uint32_t RESET: 1;  /* This bit is set by software to reset the CRC calculation uni */
        /* @SVD:field=DATASIZE,bitOffset=1,bitWidth=2,description="Valid input data size\n              These bits control the valid size of the input data.\n              00: lower 8-bit\n              01: lower 16-bit\n              10: lower 24-bit\n              11: all 32-bit" */
        volatile uint32_t DATASIZE: 2;  /* Valid input data size               These bits control the v */
        /* @SVD:field=POLYSIZE,bitOffset=3,bitWidth=2,description="Polynomial size\n              These bits control the size of the polynomial.\n              00: 32 bit polynomial\n              01: 16 bit polynomial\n              10: 8 bit polynomial\n              11: 7 bit polynomial" */
        volatile uint32_t POLYSIZE: 2;  /* Polynomial size               These bits control the size of */
        /* @SVD:field=REV_IN,bitOffset=5,bitWidth=2,description="Reverse input data\n              These bits control the reversal of the bit order of the input data\n              00: Bit order not affected\n              01: Bit reversal done by byte\n              10: Bit reversal done by half-word\n              11: Bit reversal done by word" */
        volatile uint32_t REV_IN: 2;  /* Reverse input data               These bits control the reve */
        /* @SVD:field=REV_OUT,bitOffset=7,bitWidth=1,description="Reverse output data\n              This bit controls the reversal of the bit order of the output data.\n              0: Bit order not affected\n              1: Bit-reversed output format" */
        volatile uint32_t REV_OUT: 1;  /* Reverse output data               This bit controls the reve */
        /* @SVD:field=RSVD,bitOffset=8,bitWidth=24,description="" */
        volatile uint32_t RSVD: 24;
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC2_CR_Type;

/* @SVD:register=INIT,offset=0x0010,size=32,access=read-write,reset=0x0,description="Initial CRC value" */
/* INIT 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=INIT,bitOffset=0,bitWidth=32,description="Programmable initial CRC value" */
        volatile uint32_t INIT: 32;  /* Programmable initial CRC value */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC2_INIT_Type;

/* @SVD:register=POL,offset=0x0014,size=32,access=read-write,reset=0x0,description="CRC polynomial" */
/* POL 寄存器联合体 */
typedef union
{
    volatile uint32_t R;  /* 完整寄存器值 */
    struct
    {
        /* @SVD:field=POL,bitOffset=0,bitWidth=32,description="Programmable polynomial \n              This register is used to write the coefficients of the polynomial to be used for CRC calculation.\n              If the polynomial size is less than 32 bits, the least significant bits have to be used to program the correct value." */
        volatile uint32_t POL: 32;  /* Programmable polynomial                This register is used */
    };  /* 位域访问（可直接访问字段，无需 .B） */
} CRC2_POL_Type;

/* CRC2 寄存器结构体 */
typedef struct
{
    CRC2_DR_Type DR;  /* 0x0000 - Data register */
    CRC2_SR_Type SR;  /* 0x0004 - Status register */
    CRC2_CR_Type CR;  /* 0x0008 - Control register */
    /* @SVD:register=RSVD1,offset=0x000C,size=32,access=read-write,reset=0x0,description="RSVD1" */
    volatile uint32_t RSVD1;  /* 0x000C - RSVD1 */
    CRC2_INIT_Type INIT;  /* 0x0010 - Initial CRC value */
    CRC2_POL_Type POL;  /* 0x0014 - CRC polynomial */
} CRC2_TypeDef;

/* ============================================================================ */
/* 外设实例定义 */
/* ============================================================================ */

#define HPSYS_RCC                      ((HPSYS_RCC_TypeDef *) HPSYS_RCC_BASE)
#define EXTDMA                         ((EXTDMA_TypeDef *) EXTDMA_BASE)
#define HPSYS_PINMUX                   ((HPSYS_PINMUX_TypeDef *) HPSYS_PINMUX_BASE)
#define ATIM1                          ((ATIM1_TypeDef *) ATIM1_BASE)
#define AUDPRC                         ((AUDPRC_TypeDef *) AUDPRC_BASE)
#define EZIP1                          ((EZIP1_TypeDef *) EZIP1_BASE)
#define EPIC                           ((EPIC_TypeDef *) EPIC_BASE)
#define LCDC1                          ((LCDC1_TypeDef *) LCDC1_BASE)
#define I2S1                           ((I2S1_TypeDef *) I2S1_BASE)
#define HPSYS_CFG                      ((HPSYS_CFG_TypeDef *) HPSYS_CFG_BASE)
#define EFUSEC                         ((EFUSEC_TypeDef *) EFUSEC_BASE)
#define AES                            ((AES_TypeDef *) AES_BASE)
#define TRNG                           ((TRNG_TypeDef *) TRNG_BASE)
#define MPI1                           ((MPI1_TypeDef *) MPI1_BASE)
#define MPI2                           ((MPI2_TypeDef *) MPI2_BASE)
#define SDMMC1                         ((SDMMC1_TypeDef *) SDMMC1_BASE)
#define CRC1                           ((CRC1_TypeDef *) CRC1_BASE)
#define PTC1                           ((PTC1_TypeDef *) PTC1_BASE)
#define DMAC1                          ((DMAC1_TypeDef *) DMAC1_BASE)
#define USART1                         ((USART1_TypeDef *) USART1_BASE)
#define USART2                         ((USART2_TypeDef *) USART2_BASE)
#define USART3                         ((USART3_TypeDef *) USART3_BASE)
#define GPADC                          ((GPADC_TypeDef *) GPADC_BASE)
#define AUDCODEC                       ((AUDCODEC_TypeDef *) AUDCODEC_BASE)
#define TSEN                           ((TSEN_TypeDef *) TSEN_BASE)
#define GPTIM1                         ((GPTIM1_TypeDef *) GPTIM1_BASE)
#define BTIM1                          ((BTIM1_TypeDef *) BTIM1_BASE)
#define WDT1                           ((WDT1_TypeDef *) WDT1_BASE)
#define SPI1                           ((SPI1_TypeDef *) SPI1_BASE)
#define SPI2                           ((SPI2_TypeDef *) SPI2_BASE)
#define PDM1                           ((PDM1_TypeDef *) PDM1_BASE)
#define I2C1                           ((I2C1_TypeDef *) I2C1_BASE)
#define I2C2                           ((I2C2_TypeDef *) I2C2_BASE)
#define I2C3                           ((I2C3_TypeDef *) I2C3_BASE)
#define I2C4                           ((I2C4_TypeDef *) I2C4_BASE)
#define HPSYS_GPIO                     ((HPSYS_GPIO_TypeDef *) HPSYS_GPIO_BASE)
#define GPTIM2                         ((GPTIM2_TypeDef *) GPTIM2_BASE)
#define BTIM2                          ((BTIM2_TypeDef *) BTIM2_BASE)
#define HPSYS_AON                      ((HPSYS_AON_TypeDef *) HPSYS_AON_BASE)
#define LPTIM1                         ((LPTIM1_TypeDef *) LPTIM1_BASE)
#define LPTIM2                         ((LPTIM2_TypeDef *) LPTIM2_BASE)
#define PMUC                           ((PMUC_TypeDef *) PMUC_BASE)
#define IWDT                           ((IWDT_TypeDef *) IWDT_BASE)
#define LPSYS_RCC                      ((LPSYS_RCC_TypeDef *) LPSYS_RCC_BASE)
#define DMAC2                          ((DMAC2_TypeDef *) DMAC2_BASE)
#define LPSYS_PINMUX                   ((LPSYS_PINMUX_TypeDef *) LPSYS_PINMUX_BASE)
#define USART4                         ((USART4_TypeDef *) USART4_BASE)
#define USART5                         ((USART5_TypeDef *) USART5_BASE)
#define BTIM3                          ((BTIM3_TypeDef *) BTIM3_BASE)
#define BTIM4                          ((BTIM4_TypeDef *) BTIM4_BASE)
#define WDT2                           ((WDT2_TypeDef *) WDT2_BASE)
#define PTC2                           ((PTC2_TypeDef *) PTC2_BASE)
#define LPSYS_CFG                      ((LPSYS_CFG_TypeDef *) LPSYS_CFG_BASE)
#define LPSYS_AON                      ((LPSYS_AON_TypeDef *) LPSYS_AON_BASE)
#define LPTIM3                         ((LPTIM3_TypeDef *) LPTIM3_BASE)
#define CRC2                           ((CRC2_TypeDef *) CRC2_BASE)

/* ============================================================================ */

#ifdef __cplusplus
}
#endif

#endif /* __SF32LB52X_H__ */
